# Sprint 72 — STATUS

**Canonical post shape (ALL lanes, incl. Codex):**
`### [T<n>] STATUS-VERB 2026-MM-DD HH:MM ET — <gist>`
STATUS-VERB ∈ {FINDING, FIX-PROPOSED, FIX-LANDED, AUDIT-PASS, AUDIT-FAIL, CHECKPOINT, BLOCKED, DONE}. Auditor posts `[T4-CODEX]`. Idle-poll regex: `^(### )?\[T<n>\] DONE\b`.

Lane discipline: stay in lane; **no version bumps, no CHANGELOG, no commits** (orchestrator closes out).

**✅ Browser launch APPROVED (Joshua, 2026-06-08).** Real Chrome in Playwright OK; prior colliding ClaimGuard process `kill -9`'d. Bring up against the local fixture first, then live grok.com. Posture still applies (Grok-only, headful, dedicated profile, human-present, not scraping).

---

### [ORCH] DISPATCH 2026-06-08 12:38 ET — Sprint 72 booted on port 3001
- Deck 3001 = Workstream B (interactive Grok web-chat panel / OUTBOUND). Runs in parallel with Sprint 71 (MCP Bridge / INBOUND) on port 3000.
- Lanes: T1 CDP/render bridge · T2 server seams + `web-chat-grok` adapter · T3 detection/selectors + client canvas · T4-CODEX auditor.
- Posture: Grok-only, real headful + dedicated profile, human-present, NOT scraping. Browser launch approved by Joshua; gate lifted.

<!-- Lanes append below this line. -->

### [T2] FINDING 2026-06-08 12:58 ET — seam plan locked + 3 cross-lane blockers flagged
**Lane:** `packages/server/src/index.js` web-chat seams + new `packages/server/src/agent-adapters/web-chat-grok.js`. Read the full PTY/capture/adapter machinery (session.js, claude/grok/agy adapters, both bundled hooks, index.js spawn/data/input/exit/close/periodic).

**Plan for the 8 seams** (additive; `if (session.pty)` PTY path stays byte-identical — Guardrail 3):
1. Session: `type:'web-chat'` ⇒ `pty:null` (Session ctor already defaults). 
2. Spawn: new branch in `spawnTerminalSession` BEFORE `if (pty)` — boots T1's driver **fire-and-forget async** (route stays sync; status `starting`→`idle` on attach). Driver **lazy-required + fail-soft** (missing/throwing driver ⇒ `errored` status, never crashes server — protects PTY panels + Sprint 71 deck).
3. Inbound: `grok.onComplete` ⇒ status (adapter `statusFor`) + `transcriptWriter.append` + broadcast `{type:'output',data}`. (NOT full `analyzeOutput` — its `_detectErrors` would false-positive 'errored' on chat prose mentioning "Error:". statusFor gives the same status outcome without that hazard. Flagging the deliberate deviation for T4.)
4. Input: web-chat branch in **both** `POST /api/sessions/:id/input` (before the `!pty` 410 guard — else every inject 410s) AND WS `case 'input'` ⇒ routes to `grok.inject`, not `pty.write`. Server-side two-stage assembler buffers bracketed-paste body, fires `inject(handle, fullText)` only on the lone-`\r` submit — so the 4+1 two-stage pattern works UNCHANGED from the orchestrator's view.
5. Status: adapter `statusFor` (idle/thinking) + event-driven (inject⇒thinking, complete⇒idle) so orchestrator inject-verify (`status:'thinking'`) works on a web-chat panel.
6. Adapter registered LAST in `agent-adapters/index.js`; `matches:()=>false` + no `patterns.prompt` ⇒ can NEVER hijack PTY output/command detection.
7. Capture: web-chat has **no `term.onExit`** ⇒ I add an idempotent `closeWebChatSession` (fires `onPanelClose` + clears periodic timer + broadcasts `exit`/`panel_exited`) wired into DELETE route + driver-disconnect. `resolveTranscriptPath` materializes the in-flight turn buffer into a Gemini-shaped `{messages:[{type,content}]}` tempfile (exact agy precedent) ⇒ `parseAutoDetect` ingests it, NO new hook parser needed. Periodic-capture timer registered same as PTY non-Claude adapters.
8. Client: **T3's** — I broadcast `{type:'web-chat-frame', frame}` (screencast) + reuse `{type:'output',data}` (text). Contract below.

**WS frame contract I emit (T3 consumes on the client):**
- `{type:'output', data:<grok response text>}` — reuse existing xterm/output dispatch (per seam 8).
- `{type:'web-chat-frame', frame:<whatever handle.screencast emits>}` — paint to canvas. (Pass-through; if you need a specific shape, tell me and I'll normalize server-side.)
- WS inbound I accept: `case 'input'` (composer text ⇒ inject) + `case 'web-chat-input'` `{event:<CDP input evt>}` (raw mouse/key ⇒ `handle.sendInput`) for direct human interaction with the live tab.

**🚩 Cross-lane blocker 1 (T1):** `web-chat-driver/src/index.js` aggregator exports only `{ cdp }` — **`grok` is not wired in**. My consume path needs `driver.grok.*`. I've added a defensive fallback (require `src/grok` directly if `driver.grok` absent) so I'm unblocked, but please make it `module.exports = { cdp, grok }`.

**🚩 Cross-lane blocker 2 (T1↔T3, the big one):** T1 builds **raw CDP over `ws`, explicitly no Playwright**; T3's `grok/index.js` requires the handle to expose a **Playwright `Page` at `handle.page`** (`selectors.resolveLocator(page,...)`, `composer.fill()`, `page.locator`). These are incompatible — `grok.inject` will throw at runtime on a raw-CDP handle. My fail-soft wrapping means the server won't crash (web-chat just degrades to errored), but the **live grok.com path is blocked until T1+T3 reconcile the handle shape** (either T1 supplies a Playwright Page, or T3 reimplements selectors/fill/wait against raw CDP `Runtime.evaluate`/`Input.*`). Not mine to resolve — flagging for ORCH/T4.

**🚩 Cross-lane note 3 (stack-installer, ORCH close-out):** seam 7 correctness needs 2 edits in `packages/stack-installer/assets/hooks/memory-session-end.js`: (a) `ALLOWED_SOURCE_AGENTS += 'grok-web'` (else provenance silently coerces to `claude` — exact agy bug; this one edit fixes BOTH hooks since pre-compact reuses `normalizeSourceAgent`); (b) byte-floor exemption for `sessionType==='web-chat'` (compact envelope <5KB else dropped — mirrors the agy exemption). I will make these minimal edits but will NOT bump the `@termdeck/stack-installer-hook` version stamp (ORCH close-out, gates install-overwrite) — flag for ORCH: live-validation needs the installed `~/.claude/hooks/` copy refreshed, and check Sprint 71 isn't also touching these hook files. Zero-touch fallback if ORCH prefers: set adapter `sourceAgent:'grok'` (already allow-listed) — one-line change.

Implementing now; will post FIX-LANDED per seam.

### [T4-CODEX] CHECKPOINT 2026-06-08 12:45 ET — boot complete; entering Phase 1 repo/diff audit
- Verified: `memory_recall(project="termdeck", query="Sprint 72 Grok web-chat panel audit...")`; broad recent-decisions memory lookup; `./AGENTS.md`; `./CLAUDE.md`; `~/.claude/CLAUDE.md`; `docs/CRITICAL-READ-FIRST-2026-05-07.md` (both historical P0 investigations resolved); `docs/sprint-72-grok-panel/PLANNING.md`; `STATUS.md`; `T4-codex-auditor.md`.
- Pending: inspect dirty worktree, sprint-owned changed files, posture/security regressions, tests/reproduction. No worker claims accepted yet.

### [T4-CODEX] AUDIT-FAIL 2026-06-08 12:47 ET — Sprint 72 implementation is absent; FINAL-VERDICT GREEN is blocked
- Reproduced structural miss: `test -d packages/web-chat-driver` returned exit `1`; `test -f packages/server/src/agent-adapters/web-chat-grok.js` returned exit `1`; `git diff -- packages/server/src/index.js packages/server/src/agent-adapters/index.js packages/server/src/agent-adapters/grok-models.js packages/client/public/app.js package.json package-lock.json --stat` returned empty.
- Required scope is explicit: PLANNING requires new `packages/web-chat-driver/`, `web-chat-grok` adapter, and client canvas panel at `docs/sprint-72-grok-panel/PLANNING.md:6`; 8 seams at `PLANNING.md:39-46`; acceptance at `PLANNING.md:65-69`.
- Current code evidence: adapter registry has only `claude`, `codex`, `gemini`, `grok`, `antigravity` and no `web-chat-grok` at `packages/server/src/agent-adapters/index.js:15-35`; `Session` defaults remain PTY-oriented (`pty:null`, `type:'shell'`) with no web-chat fields at `packages/server/src/session.js:138-221`; spawn path still only enters the `if (pty)` branch at `packages/server/src/index.js:1331-1385`; `/api/sessions/:id/input` rejects `!session.pty` with 410 at `packages/server/src/index.js:1912-1934` and writes only `session.pty.write()` at `index.js:1954-1959`; WS input writes only to PTY at `index.js:2812-2823`; client `createTerminalPanel` always instantiates xterm at `packages/client/public/app.js:475-515` with no `type==='web-chat'` canvas branch.
- Regression datapoint: root `npm run test` passed 448/448. `grok-models.js` family-A reasoning still present at `packages/server/src/agent-adapters/grok-models.js:36-55`, but this does not satisfy Sprint 72 acceptance because no web-chat feature exists.
- Posture/security audit cannot pass or fail substantively yet because there is no CDP harness to inspect. The absence itself blocks: no headful Chrome/dedicated-profile proof, no localhost-only remote-debugging proof, no fixture/live reproduction, no transcript/capture integrity path.
- Probe changes: none made to source; no restore needed. `git status --short` after tests showed no new source/package changes beyond pre-existing dirty/untracked docs and sprint folders.

### [T4-CODEX] CHECKPOINT 2026-06-08 13:01 ET — awaiting worker FIX-LANDED
- ORCH redirected: the 12:47 absence finding was valid time-specific evidence but premature as a sprint verdict while T1/T2/T3 are still building. I will not issue FINAL-VERDICT or repeat AUDIT-FAIL for incomplete work during active build-out.
- Re-read STATUS: T2 posted `FINDING 2026-06-08 12:58 ET` with seam plan and blockers, especially raw-CDP vs Playwright `handle.page` incompatibility and `grok-web` hook allowlist/byte-floor notes.
- Rechecked disk: `packages/web-chat-driver/` now exists with `src/cdp/*`, `src/grok/*`, `src/index.js`, fixtures, and tests. Pending re-audit only after worker `FIX-LANDED`/`DONE` posts.

### [T1] FINDING 2026-06-08 13:04 ET — CDP/render bridge landed + fixture GREEN; resolving T2 Blockers 1 & 2
- **Built** `packages/web-chat-driver/src/cdp/*` (attach · screencast · input · profile · transport) + `tests/cdp.test.js` + `tests/fixtures/echo.html` + README contract. **My 4 fixture tests PASS** on a real headful Chrome round-trip (Chrome 149, dedicated throwaway profile, localhost debug port): screencast streams ≥2 distinct frames in the documented shape · `insertText` lands · forwarded mouse click hit-tests onto the input · forwarded key events type chars.
- **Blocker 1 (export grok) — ACK, fixing.** Making the barrel `module.exports = { cdp, grok }` so T2's defensive `require('src/grok')` fallback becomes belt-and-suspenders, not load-bearing.
- **Blocker 2 (the big one: handle shape) — RESOLVING via Option A: T1 supplies a Playwright `Page`.** Rationale: (a) T3 already has a working, tested aria-snapshot selector/self-heal layer on the Playwright Page API (19 green tests) — forcing a raw-CDP rewrite of their lane is the high-risk path; (b) my T1 brief explicitly sanctions *"Playwright `connectOverCDP` or raw CDP"*; (c) the approval line above literally says *"Real Chrome in **Playwright** OK."* So I pivot the transport to Playwright (`launchPersistentContext`/`connectOverCDP`) to expose a real `handle.page`, **and keep raw-CDP screencast + input via `page.context().newCDPSession(page)`** — I lose nothing. `handle.cdp` stays a CDP session with the same `.send`/`.on` shape my screencast/input modules already use.
- **Published superset handle (satisfies T3 + T2 + render bridge):**
  - `handle.page` — Playwright `Page` (T3: `selectors.resolveLocator(page,…)`, `composer.fill()`, `page.locator`, aria snapshots). ✅ T3's stated contract.
  - `handle.screencast(onFrame) → {started, stop}` · `handle.sendInput(evt)` · `handle.insertText(text)` — render bridge + human drive-through (T2 `web-chat-input` `{event:<CDP evt>}` → `handle.sendInput`).
  - `handle.cdp` (raw CDPSession) · `handle.navigate(url)` · `handle.evaluate(expr)` · `handle.context`/`browser`/`proc` · `handle.close()` (launch ⇒ kills our Chrome; connect ⇒ only detaches, never the human's).
  - **Frame-channel shape** (T2 pass-through `{type:'web-chat-frame', frame}`, T3 paints): `{ format, data(base64), dataUrl, deviceWidth, deviceHeight, offsetTop, pageScaleFactor, scrollOffsetX, scrollOffsetY, timestamp, frame }`. `dataUrl` drops straight into `<img>.src`/canvas.
- **T4 — posture/security proofs you flagged are satisfiable on this transport:** headful (`headless:false`, never headless), dedicated persistent `--user-data-dir` (Chrome 136+ default-profile block ⇒ per-panel profile), localhost-only debug port (no `--remote-debugging-address` ⇒ 127.0.0.1 bind; `ws`/Playwright client sends no `Origin` ⇒ no `--remote-allow-origins` loosening), fixture reproduction green, never stealth/scraping.
- **Shared-file touches (flagging for T3/ORCH):** `src/index.js` (additive `grok` export per Blocker 1) + `package.json` (add `playwright-core` as a **local** dep; fix test glob `test/`→`tests/` since the dir was consolidated to plural). **Root `package.json`/lockfile stay untouched** (Guardrail 5) — install is local to the package only; verifying post-install.
- **Next:** land the Playwright pivot, re-green my 4 tests + confirm T3's 19 still pass, then FIX-LANDED + DONE.

### [ORCH] DECISION 2026-06-08 13:05 ET — cross-lane blockers resolved (re: T2 FINDING 12:58)
- **Blocker 2 (handle shape — the big one): standardize on Playwright `connectOverCDP`.** T1's `attach()` exposes BOTH `handle.page` (Playwright Page → T3's `page.locator`/`composer.fill()`/selectors stay) AND `handle.cdp` (CDPSession via `page.context().newCDPSession(page)` → screencast/input). connectOverCDP still attaches the REAL headful Chrome on `--remote-debugging-port`; co-pilot property preserved. Matches the research-recommended path (Playwright role/locator + a11y-snapshot resilience).
- **Blocker 1:** T1 exports `module.exports = { cdp, grok }` from `web-chat-driver/src/index.js`. T2's defensive fallback may stay.
- **Blocker 3 (provenance): zero-touch.** web-chat-grok adapter `sourceAgent:'grok'` (already allow-listed). Do NOT edit `packages/stack-installer/assets/hooks/*` or bump the hook stamp this sprint (release-sensitive; Brad runs the installed copy). Accurate `grok-web` provenance deferred to a follow-up.

### [T3] FIX-LANDED 2026-06-08 13:12 ET — grok detection/selectors/inject landed (Playwright per ORCH Blocker-2); 19 tests green
- **Files:** `packages/web-chat-driver/src/grok/{selectors,completion,index}.js` + `tests/grok-{selectors,completion}.test.js` + `tests/fixtures/grok-aria-{idle,generating}.txt` + `grok-dom.html`.
- **Aligned with T1's published handle (13:04) + ORCH DECISION (13:05):** my code operates on `handle.page` (Playwright `Page`); zero `require('playwright')` (page is supplied → package stays CommonJS, T1 owns the dep). I briefly detoured to a raw-CDP reimpl when I read T1's then-current raw-`ws` transport; reverted per ORCH "Playwright approach is CORRECT and stays." Net: T1 adapts the transport to expose `handle.page`, T3 keeps the tested locator layer.
- **Layered completion (no sleep):** (1) stop↔send button-flip PRIMARY (`stop.waitFor visible→hidden`), (2) MutationObserver "quiet ~600ms" on a STABLE response ancestor BACKSTOP (`page.evaluate(quietWaiterInPage)`), (3) copy/regenerate affordance CONFIRM — fused by the pure, unit-tested `decideComplete()`. 180s max ceiling (Acceptd too-short-timeout lesson); never `networkidle` (cib_acceptd_tagger mis-fire lesson).
- **Selectors:** ordered `testid → role/label → css` per target + ARIA-snapshot self-heal (`selectors.resolveLocator`). Extraction RE-QUERIES the last assistant node every call — never caches a Locator (Acceptd virtualized-table stale-node lesson; grok re-renders on every streamed token).
- **Contracts T2 consumes:** `driver.grok.inject(handle, text)` (focus→fill→send→await completion→returns final text + fires listeners), `onComplete(handle, cb)` → unsubscribe, `extract(handle)`.
- **Tests:** 19/19 green standalone — `node --test packages/web-chat-driver/tests/grok-selectors.test.js packages/web-chat-driver/tests/grok-completion.test.js`. Covers pure cores (decideComplete truth-table; parseAriaSnapshot/selfHeal incl. stop-absent negative + generating-tree positive) and the `waitForComplete` state machine via a fake Playwright page (button-flip / quiet+affordance / quiet-only / timeout).

### [T3] FINDING 2026-06-08 13:12 ET — client contract locked to T1 frame-shape + T2 WS; root test-glob is an ORCH close-out item
- **Client will consume exactly:** paint `{type:'web-chat-frame', frame}` via `frame.dataUrl` (T1's 13:04 frame-channel shape); input box → `{type:'input', data}` (T2's inject route). I will also forward canvas mouse/key as `{type:'web-chat-input', event:<CDP evt>}` (T2 accepts it) so the in-deck canvas is genuinely interactive, not just a mirror — coordinate-mapped canvas→page via `frame.deviceWidth/deviceHeight` + the loaded image's natural size.
- **`web-chat-driver/package.json` is T1's, not touched by me.** T1 (13:04) owns the `test/`→`tests/` glob fix + the local `playwright-core` dep. My tests already live in `tests/` (the dir consolidated to plural). Flagging so we don't both edit it.
- **Root `npm test` does NOT include web-chat-driver** (it enumerates `server`/`cli`/`stack-installer` explicitly). My 19 tests pass standalone but won't run under root `npm test` until the glob gains `packages/web-chat-driver/tests/**/*.test.js`. **ORCH close-out item** — Guardrail 5 forbids me editing root `package.json` while Sprint 71 runs in parallel, so I am flagging, not editing.
- Client (`app.js` web-chat canvas panel) implementing now; FIX-LANDED to follow.

### [T4-CODEX] CHECKPOINT 2026-06-08 13:15 ET — T3 FIX-LANDED detected; entering focused T3 audit
- Detected `### [T3] FIX-LANDED 2026-06-08 13:12 ET` for `packages/web-chat-driver/src/grok/{selectors,completion,index}.js` and 19 standalone tests.
- Scope now: audit T3 landed Grok selectors/completion/inject against posture and T1/T2 contract. Still no FINAL-VERDICT; T1/T2 have not posted `FIX-LANDED`/`DONE`, and T3 client work is still pending.

### [T4-CODEX] FINDING 2026-06-08 13:16 ET — T3 completion detector treats timeout as success and can extract partial/stale text
- Reproduced with T3's own focused suite: `node --test packages/web-chat-driver/tests/grok-selectors.test.js packages/web-chat-driver/tests/grok-completion.test.js` passes 19/19, but includes a test where no stop button, no quiet signal, and no affordance still resolves as `{complete:true, via:'timeout'}` at `packages/web-chat-driver/tests/grok-completion.test.js:119-128`.
- Code path: `waitForComplete()` always returns `{ complete: true, via }` after `waitForQuietAndAffordance()` regardless of `via` at `packages/web-chat-driver/src/grok/completion.js:145-148`; `waitForQuietAndAffordance()` returns `'timeout'` when `quietReached` is false and no affordance corroborates completion at `completion.js:161-185`.
- User-facing impact: `inject()` trusts `waitForComplete()`, immediately calls `extract()`, notifies listeners, and returns final text at `packages/web-chat-driver/src/grok/index.js:69-74`. On a long Grok reasoning turn or selector drift that never reaches a reliable completion signal, this can broadcast/capture a partial or stale assistant message as if it were final.
- Required fix: make timeout an explicit failure/degraded state that T2 can surface as `errored`/still-thinking, or at minimum do not call it `complete:true`; tests should assert timeout does not satisfy final completion. `quiet-only` also deserves a deliberate policy decision because `decideComplete()` says quiet without affordance is not done (`completion.js:58-60`), but the helper still returns success via `'quiet-only'` (`completion.js:182-185`).
- Probe changes: none; focused tests only.

### [T1] FIX-LANDED 2026-06-08 13:15 ET — handle exposes {page, cdp}; Blockers 1&2 closed; 24/24 green
- **Implemented exactly the ORCH DECISION (13:05) + T3's expectation (13:12).** `attach()` spawns a **real, independent headful Chrome** (actual Chrome binary, only our flags — *no* Playwright `--enable-automation` infobar, human co-owns the window) on a dedicated `--user-data-dir` + localhost `--remote-debugging-port`, then **Playwright `connectOverCDP`** attaches. The handle exposes **both**:
  - `handle.page` — Playwright `Page` ⇒ T3's `selectors.resolveLocator(page,…)` / `composer.fill()` / aria self-heal run unchanged. (Locked by a contract test: `typeof handle.page.locator === 'function'`.)
  - `handle.cdp` — `CDPSession` via `page.context().newCDPSession(page)` ⇒ `Page.startScreencast` + `Input.dispatch*`.
- **Tests: 24/24 GREEN** via `cd packages/web-chat-driver && npm test` — **my 5** (page+cdp contract · screencast ≥2 distinct frames in documented shape · `insertText` lands · forwarded mouse click hit-tests · forwarded key events type) **+ T3's 19** (coexist cleanly in `tests/`). Deterministic teardown: `close()` awaits Chrome exit before profile cleanup ⇒ 0 leftover tmp profiles.
- **Blocker 1 CLOSED:** `src/index.js` now `module.exports = { cdp, grok }` (grok required defensively so a transient T3 edit can't break `cdp` consumers; T2's direct-require fallback is now belt-and-suspenders).
- **Contract convergence with T3/T2 — confirmed:** my frame-channel shape carries `dataUrl` (paint) + `deviceWidth`/`deviceHeight` (T3's canvas→page coord-map) ✓. `handle.sendInput(evt)` consumes T2's `{type:'web-chat-input', event:<CDP evt>}` verbatim ✓. `web-chat-grok` adapter does `const {cdp,grok}=require('web-chat-driver')` then `cdp.attach` + `grok.inject(handle,…)` ✓.
- **@T4 — posture/security proofs (all green on the fixture, re your 12:47 list):** headful (`headless:false`, never headless) · dedicated persistent profile (Chrome 136+ default-profile-block ⇒ per-panel `--user-data-dir`) · **localhost-only debug port** (no `--remote-debugging-address` ⇒ 127.0.0.1 bind; `connectOverCDP`'s Node client sends no `Origin` ⇒ **no `--remote-allow-origins` loosening needed**, verified) · real-Chrome-not-Chromium · never stealth/scraping · `grok-models.js` untouched.
- **Guardrail 5 — root untouched:** `playwright-core@1.60.0` installed **local** to `packages/web-chat-driver` only; root `package.json`/`package-lock.json` verified CLEAN. `package.json` is T1's (per T3's flag) — fixed test glob `test/`→`tests/`. Root test-glob inclusion of web-chat-driver is an ORCH close-out item (I won't edit root pkg mid-parallel-sprint).
- **Next:** live grok.com **transport** validation (attach to the real site, screencast frames flow, `handle.page` works on real DOM — no auto-inject of a prompt; full inject is login-gated on the dedicated profile), then DONE.

### [T4-CODEX] FINDING 2026-06-08 13:18 ET — T1 default launch is OK, but option surface can violate headful/local-CDP posture
- Positive reproduction: `cd packages/web-chat-driver && npm test` passed 24/24 with real Chrome. Extra security probe launched the driver and `lsof -nP -iTCP:<port> -sTCP:LISTEN` showed Chrome listening on `127.0.0.1:<port>` only, so the default launch path is currently localhost-bound.
- Release-blocking posture gap: `attach()` passes `headful: opts.headful !== false` and `extraArgs: opts.args || []` into Chrome launch at `packages/web-chat-driver/src/cdp/attach.js:43-50`. `launchChrome()` then pushes `--headless=new` when `headful` is false and appends every `extraArgs` value verbatim after the safe flags at `packages/web-chat-driver/src/cdp/transport.js:104-121`.
- Impact: the Sprint 72 guardrail says never headless, never stealth-patched, localhost-only, dedicated profile. This API surface allows callers to pass `headful:false`, `--headless=*`, `--remote-debugging-address=0.0.0.0`, `--remote-allow-origins=*`, or another `--user-data-dir=...` and silently bypass those guarantees. Even if T2 does not pass those today, the driver contract itself is not posture-hard.
- Required fix: remove/reject the headless escape hatch in production code and validate/sanitize `opts.args` against forbidden flags (`--headless`, `--remote-debugging-address`, `--remote-allow-origins`, `--user-data-dir`, `--profile-directory`, stealth/automation-disabling flags). If CI needs a no-browser mode, keep the existing `WEB_CHAT_DRIVER_NO_BROWSER=1` test skip rather than adding a headless path to the driver.
- Probe changes: none; browser test/probe closed Chrome and deleted the temp profile.

### [T1] FIX-LANDED 2026-06-08 13:23 ET — posture hard-locked in the driver (re: T4 FINDING 13:18) — valid catch, fixed
- **Agreed — default-safe ≠ posture-hard.** The contract is now enforced in code, not merely defaulted:
  - **Headless escape hatch removed.** `launchChrome()` no longer emits `--headless` under any path; an explicit `headful:false` now **throws** a posture error instead. CI no-browser stays on `WEB_CHAT_DRIVER_NO_BROWSER=1` (your recommended path). `transport.js`.
  - **Caller `args` sanitized.** New `assertArgsAllowed()` rejects (throws before spawn) any extra flag matching `--headless`, `--remote-debugging-address`, `--remote-allow-origins`, `--remote-debugging-port`, `--user-data-dir`, `--profile-directory`, `--disable-blink-features`, or `AutomationControlled`. So a caller can't widen the bind, loosen origins, swap the dedicated profile, or stealth-patch. `transport.js` `FORBIDDEN_ARG_PATTERNS`.
- **New unit test locks it** (runs with NO browser ⇒ executes even on display-less CI, never SKIP-gated): asserts `attach({headful:false})` and each of the six forbidden flags reject. `tests/cdp.test.js` — *"posture: driver rejects headless + posture-breaching flags."*
- **Suite: 25/25 GREEN** (my **6** + T3's **19**), 0 leftover tmp profiles. Root pkg/lockfile still CLEAN.
- Thanks for the OOD catch — exactly the silent-downgrade vector the enforcement-over-convention rule exists for. Ready for re-audit.

### [T1] DONE 2026-06-08 13:23 ET — CDP/render bridge complete; fixture + LIVE grok.com green; contracts published
- **Lane deliverable shipped:** `packages/web-chat-driver/src/cdp/*` (`attach` · `screencast` · `input` · `profile` · `transport` · `index`) + `tests/cdp.test.js` + `tests/fixtures/echo.html` + `README.md` (published contract). Barrel `src/index.js` → `{ cdp, grok }`.
- **Handle shape (final, per ORCH 13:05):** `attach()` spawns real headful Chrome (dedicated `--user-data-dir`, localhost `--remote-debugging-port`, no stealth/automation flags) → Playwright `connectOverCDP` → handle exposes `page` (Playwright Page, T3's selectors) **and** `cdp` (CDPSession via `newCDPSession`, screencast + `Input.*`), plus `screencast`/`sendInput`/`insertText`/`navigate`/`evaluate`/`close`.
- **Fixture: 25/25 GREEN.** **LIVE grok.com validated** (real site, dedicated `~/.termdeck/web-chat-profiles/grok` profile):
  - `connectOverCDP` attached; `title:"Grok"`, full SPA rendered (`nodeCount:465`, `bodyHtml:429KB`); **`isCloudflare:false`** ⇒ our real-Chrome attach is **not bot-walled** (key viability proof).
  - **`handle.page.getByRole('textbox')→1`, `getByRole('button')→7`** — T3's Playwright-locator path resolves on the **real** grok.com DOM, not just the fixture.
  - Screencast streamed real frames (9 frames / 2 distinct) off the live page; clean teardown, 0 leaked Chrome.
- **Not done (out of T1 transport scope, flagged):** an actual prompt **inject→response** on the live site needs (a) the human logged into the `grok` profile once (it's now created + ready), and (b) T3's `grok.inject`. The composer is present + locatable pre-login; full reasoning-model turn is login-gated. → integration/Joshua follow-up, not a transport gap.
- **Guardrails honored:** Grok-only · headful · dedicated profile · localhost-only · no scraping/stealth · `grok-models.js` untouched · root `package.json`/lockfile untouched (local `playwright-core` only) · no version bump / CHANGELOG / commit (ORCH closes out).
- **Consumers unblocked:** T2 `web-chat-grok` adapter (`cdp.attach`/`grok.inject`/`handle.screencast`/`handle.sendInput`), T3 client canvas (frame `dataUrl` + `deviceWidth/Height`; `{type:'web-chat-input', event}` → `handle.sendInput`). T1 lane closed.

### [T4-CODEX] AUDIT-PASS (cdp/render) 2026-06-08 13:27 ET — T1 CDP/render bridge posture + security + contracts verified
- Reproduced from disk and live path. `cd packages/web-chat-driver && npm test` passed 25/25, including the no-browser posture test that rejects `headful:false` plus forbidden flags. A no-prompt live `https://grok.com` probe with `userDataDir:'grok'` returned `userDataDir=/Users/joshuaizzard/.termdeck/web-chat-profiles/grok`, `title:"Grok"`, `nodeCount:464`, `bodyLen:428030`, `cloudflare:false`, `textboxes:1`, `buttons:7`.
- Posture verified: real Google Chrome candidates only at `packages/web-chat-driver/src/cdp/transport.js:23-32`; launch is hard-locked against headless/unsafe flags by `FORBIDDEN_ARG_PATTERNS` and `headful === false` rejection at `transport.js:87-136`; spawned args include dedicated `--user-data-dir` + `--remote-debugging-port` and no emitted `--headless` branch at `transport.js:142-158`.
- CDP security verified: `probe()` and `connectOverCDP()` target `http://127.0.0.1:<port>` at `transport.js:68-76` and `packages/web-chat-driver/src/cdp/attach.js:55-58`; independent lsof probe showed Chrome listening on `127.0.0.1:<port>` only. No `--remote-debugging-address`, `--remote-allow-origins`, or caller-supplied profile/debug flags can pass the sanitizer (`transport.js:91-100`, `transport.js:102-114`).
- Dedicated profile verified: bare profile names resolve under `~/.termdeck/web-chat-profiles/<name>` at `packages/web-chat-driver/src/cdp/profile.js:14-30`; live probe used the `grok` dedicated profile, not the primary Chrome profile. Connect/close semantics do not kill a human-owned browser in connect mode at `attach.js:111-151`.
- Contracts verified: handle exposes `page` and `cdp` at `attach.js:75-83`; handle methods wire `screencast`, `sendInput`, and `insertText` at `attach.js:100-109`; screencast emits `dataUrl`/dimension frame shape and ACKs immediately at `packages/web-chat-driver/src/cdp/screencast.js:30-84`; package barrel exports `{ cdp, grok }` at `packages/web-chat-driver/src/index.js:19-29`.
- Secret/cookie leakage check: no CDP code reads cookies/localStorage/sessionStorage/tokens; screencast only emits visible frame images (`screencast.js:47-59`) and does not log them; no transcript capture path exists in T1 CDP files. Grep for disallowed providers found no claude.ai/gemini.google.com/chatgpt.com automation in the driver.
- Non-blocking doc drift: README still says `headful:false` adds `--headless=new` at `packages/web-chat-driver/README.md:64`; code/tests now reject it. This should be corrected before docs close-out, but CDP/render code is green. Overall FINAL-VERDICT remains held for T2/T3 and the open T3 completion finding.

### [T2] FIX-LANDED 2026-06-08 13:25 ET — all 8 seams wired; consumption aligned to T1's finalized handle; 21 new tests; root `npm test` 469/469
**ORCH Blocker-3 applied:** `sourceAgent:'grok'` (allow-listed); stack-installer hooks left **pristine** (`git diff packages/stack-installer/` empty — verified). grok-web provenance + the <5KB byte-floor exemption deferred (documented in adapter header).

**Consumption now matches T1's 13:15 handle contract:**
- `require('web-chat-driver')` → `{cdp, grok}` (Blocker 1 closed; my direct-`./grok` fallback is now belt-and-suspenders).
- `cdp.attach({ userDataDir:'grok', port, startUrl:'https://grok.com' })` — I pass **startUrl** (T1's attach defaults to `about:blank`; a Grok panel must land on grok.com), sourced from the adapter's new `webChatUrl`, and the **profile NAME** `'grok'` (→ T1's `resolveProfileDir` → `~/.termdeck/web-chat-profiles/grok`, the warm-login dir) — not a raw path.
- `handle.screencast(cb)` / `handle.sendInput(evt)` / `handle.close()` are methods on T1's handle ✓; `handle.page`/`handle.cdp` are T3's internal concern, not mine.
- Per T1's profile.js "T2 allocates a distinct port per panel": per-server panel counter (panel 0 → 9333/`grok`; panel N → +N/`grok-(N+1)`). **Multi-panel caveat flagged:** panel ≥2 gets an ISOLATED Grok login (Chrome locks a profile per process) — shared-browser-multi-tab is a follow-up; single-panel is the sprint scope.

**Files (in-lane):** NEW `agent-adapters/web-chat-grok.js`; `agent-adapters/index.js` (+register); `index.js` (+~480 LOC: driver DI, web-chat closures, spawn/input/WS/DELETE/buffer seams); NEW `adapter-web-chat-grok.test.js` (12) + `web-chat-seams.test.js` (9, fake driver via DI); 1 regression fix `adapter-spawn-shell-wrap.test.js` (the "every adapter declares spawn.shellWrap===false" contract now skips non-PTY adapters — web-chat-grok has no `spawn`).

**Tests:** server suite **323/323**; root `npm test` **469/469** at verification. (A transient red in `dashboard-panels-client.test.js` "panel_exited … 3 switches" ~13:20 was T3's app.js + client-test WIP, not mine — T3 reconciled it and root went green.)

**For T4 — 2 documented judgment calls:** (a) web-chat response routes through adapter `statusFor`, NOT full `analyzeOutput`, to avoid `_detectErrors` false-positiving 'errored' on chat prose; (b) `/buffer` made web-chat-aware (read-only) so inject-verify works — PTY behavior unchanged (`!isWebChat && !session.pty` ≡ `!session.pty`).

### [T2] DONE 2026-06-08 13:25 ET — web-chat server seams + adapter complete; tests green; no PTY regression
Lane fully delivered: a `web-chat` session creates (`pty:null`), accepts injected text via `/input` (two-stage→`grok.inject`), broadcasts `{type:'output'}`, reports status (`starting→idle→thinking→idle`), and captures to Mnestra on close + periodic tick — proven with a fake driver; existing PTY panels + root `npm test` unaffected. Live grok.com inject→response is login-gated (T1: profile ready; needs the human's one-time Grok login) and rides T3's `grok.inject` — my seams are driver-agnostic and consume it the moment the human logs in. Not bumping versions / CHANGELOG / committing — orchestrator close-out.

### [T3] FIX-LANDED 2026-06-08 13:30 ET — completion-detector correctness fix (T4 FINDING 13:16 + ORCH relay): timeout/quiet-only are DEGRADED, never final
**Accepted — it was a real bug, and a sharp catch.** `waitForComplete()` returned `{complete:true}` regardless of `via`, so `inject()` could extract + push PARTIAL/STALE text as final on a long Grok reasoning turn (the whole point of the panel) or on selector drift. Fixed:
- `completion.js` `waitForComplete()` returns `complete: via === 'quiet+affordance'` for the backstop (button-flip still returns `complete:true` early). **`timeout` and `quiet-only` → `complete:false`** — now consistent with `decideComplete()` (quiet-without-affordance is NOT done).
- `index.js` `inject()` fires `onComplete` **ONLY when `result.complete` is true**, and returns `{ text, complete, via }` (was bare text). A degraded turn returns best-effort text to the pull caller but is NEVER pushed to listeners as final.
- **For T2:** onComplete now fires only on a CONFIRMED completion → your "complete⇒idle" only triggers on real completions; on a degraded turn onComplete does NOT fire, so the panel stays `thinking` (or you surface `errored`/still-thinking via `statusFor`). The `via` tag rides the onComplete meta + the inject return for rendering.
- **Tests:** the two stale assertions (old `grok-completion.test.js:119-128`) now assert `complete:false` for `timeout` + `quiet-only`; added 2 `inject()` tests — confirmed (button-flip) fires onComplete + returns `{complete:true}`; **degraded (timeout) WITH partial text on screen returns `{complete:false}` and does NOT fire onComplete**. Suite **21/21 green**.

### [T3] FIX-LANDED 2026-06-08 13:30 ET — client web-chat (Grok) canvas panel landed in app.js; root npm test 469/469
- **`createTerminalPanel` branches on `type==='web-chat'`** right after `setupPanelDragDrop` → `mountWebChatPanel`, then returns — the xterm path stays **byte-identical** for every other panel type (Guardrails 3/4). Sole owner of `app.js`.
- **Canvas** paints T1's `{type:'web-chat-frame', frame}` via `frame.dataUrl`, letterboxed + DPR-aware, re-letterboxing on resize (a no-op `fitAddon.fit()` hooks into `fitAll()`).
- **Input box** → `{type:'input', data}` (T2 routes to `grok.inject`). **Canvas mouse/wheel** → `{type:'web-chat-input', event:<CDP evt>}` (T2 → `handle.sendInput`), coordinate-mapped canvas→page via `frame.deviceWidth/Height` + image-natural size, so the human can click/scroll the live tab in-deck.
- **Own reconnect** (`reconnectWebChat`) + a shared `handleWebChatWsMessage` (mirrors the xterm non-output cases; frames→canvas, grok text→transcript). Web-chat panels never enter the xterm `reconnectSession`.
- **Terminal-less safety:** guarded every shared site that assumed an xterm — `closePanel`, `changeTheme`/`resetTheme`, theme-on-broadcast, focus shortcut + the two focus paths (now focus the inject box), drawer-resize, and `askAI` (mnestra output via a no-op writer; Memory tab still caches). Audited all 29 `entry.terminal` refs: every one guarded or xterm-only.
- **Cross-lane touch (flagging):** `packages/server/tests/dashboard-panels-client.test.js` — `panel_exited`-routing count `2→3` (my web-chat WS router adds a third legitimate handler for dead-tile parity). T2 already saw + expected this (their 13:25 note); one-line count + comment, no other server-test changes.
- `node --check app.js` clean; **root `npm test` 469/469**.

### [T3] DONE 2026-06-08 13:30 ET — detection/selectors + client canvas complete; auditor finding closed
Lane delivered: `packages/web-chat-driver/src/grok/{selectors,completion,index}.js` (Playwright on `handle.page` per ORCH Blocker-2; layered no-sleep completion with the T4-fix degraded semantics; `inject`/`onComplete`/`extract`) + the `app.js` web-chat canvas panel (sole-owner, xterm path untouched). Tests: grok **21/21** standalone; root **469/469**; `app.js` syntax clean. Contracts confirmed against T1's 13:15 handle (`page`+`cdp`, frame `dataUrl`+`deviceWidth/Height`, `sendInput` shape) and T2's 13:25 WS routes (`input`/`web-chat-frame`/`web-chat-input`). Handoff: live grok.com inject→response is login-gated (T1 profile ready; needs the human's one-time Grok login) — my code is ready the moment the session is authed. Root test-glob inclusion of `web-chat-driver/tests/**` remains an ORCH close-out item (Guardrail 5). No version bumps / CHANGELOG / commits — orchestrator close-out.

### [T4-CODEX] AUDIT-PASS (server-seams) 2026-06-08 13:33 ET — T2 web-chat server seams verified; PTY path stays fenced
- Reproduced root test claim: `npm run test` passed **469/469**, fail 0, duration ~10.8s.
- No PTY regression found in the audited seams. Web-chat spawns are explicitly gated on `session.meta.type === 'web-chat'`, set `session.pty = null`, and return before the existing PTY launch branch (`packages/server/src/index.js:1593-1607`, `packages/server/src/index.js:1697-1708`). Existing PTY launch/input branches remain behind `if (pty)` / `session.pty` guards (`packages/server/src/index.js:1708`, `packages/server/src/index.js:2348-2383`, `packages/server/src/index.js:3255-3259`).
- Input routing verified. POST `/api/sessions/:id/input` routes only `web-chat` panels to `routeWebChatInput()` before the PTY 410 guard; non-web-chat sessions still normalize CR/LF and call `session.pty.write()` + `session.trackInput()` (`packages/server/src/index.js:2282-2338`, `packages/server/src/index.js:2348-2383`). WS `case 'input'` likewise sends web-chat to `routeWebChatInput()` and PTY to `session.pty.write()` (`packages/server/src/index.js:3249-3260`). The assembler strips bracketed-paste markers, buffers stage-1 text, and fires `grok.inject(handle, full)` only on trailing CR/LF / lone `\r` submit (`packages/server/src/index.js:1471-1502`, `packages/server/src/index.js:1518-1521`); `web-chat-seams.test.js:228-267` repros exactly one inject after the lone-CR submit and `web-chat-seams.test.js:246-249` verifies `/buffer` no longer 404s web-chat panels.
- Adapter registration cannot hijack PTY detection. `web-chat-grok` is registered last in `AGENT_ADAPTERS` (`packages/server/src/agent-adapters/index.js:32-47`), `detectAdapter()` can only return adapters via `patterns.prompt` or `matches(command)` (`packages/server/src/agent-adapters/index.js:64-73`), and the web-chat adapter has `matches: () => false` with no `patterns.prompt` (`packages/server/src/agent-adapters/web-chat-grok.js:211-238`). `adapter-web-chat-grok.test.js:34-62` fences this against command/output detection and preserves the CLI `grok` adapter mapping.
- Memory capture provenance verified. The adapter materializes in-memory web-chat turns to a tmpdir Gemini-shaped `{messages:[{type,content}]}` envelope (`packages/server/src/agent-adapters/web-chat-grok.js:153-179`); the bundled hook's `parseAutoDetect()` tries `parseGeminiJson()` on object envelopes and `parseGeminiJson()` consumes `messages` arrays (`packages/stack-installer/assets/hooks/memory-session-end.js:307-337`, `packages/stack-installer/assets/hooks/memory-session-end.js:398-410`). `onPanelClose()` and periodic capture pass `source_agent: adapter.sourceAgent || adapter.name` (`packages/server/src/index.js:303-331`, `packages/server/src/index.js:364-399`), and the adapter's `sourceAgent` is `'grok'`, not `'grok-web'` (`packages/server/src/agent-adapters/web-chat-grok.js:211-218`). The hook allowlist already includes `'grok'` (`packages/stack-installer/assets/hooks/memory-session-end.js:656-672`), and `git diff packages/stack-installer/assets/hooks/memory-session-end.js packages/stack-installer/assets/hooks/memory-pre-compact.js` is empty.
- Server seam tests cover the claimed behavior with a fake driver: create uses `pty:null` + `cdp.attach` args (`packages/server/tests/web-chat-seams.test.js:201-220`), close captures `sessionType=web-chat` + `source_agent=grok` + both turns (`packages/server/tests/web-chat-seams.test.js:333-367`), periodic capture emits `mode=periodic_checkpoint` + `source_agent=grok` (`packages/server/tests/web-chat-seams.test.js:405-429`), and driver-unavailable/attach-throw cases degrade without crashing (`packages/server/tests/web-chat-seams.test.js:440-469`).
- Residual note, not a T2 blocker per ORCH zero-touch decision: the adapter documents the deferred <5KB hook byte-floor behavior for short web-chat sessions (`packages/server/src/agent-adapters/web-chat-grok.js:52-57`). Substantive sessions should exceed the floor; a future `grok-web` provenance/byte-floor hook change remains an orchestrator follow-up.

### [T4-CODEX] CHECKPOINT 2026-06-08 13:34 ET — T2 audit closed; T3 DONE detected, starting targeted T3 completion/client audit
- T3 posted DONE at 13:30 while the T2 audit was running. Per ORCH idle-watch rule, moving immediately to adversarial T3 re-audit before any overall FINAL-VERDICT.

### [T4-CODEX] AUDIT-FAIL 2026-06-08 13:37 ET — T3 client send form buffers forever; no FINAL-VERDICT GREEN
- Required green gates reproduced but uncovered a client/server contract miss. `packages/web-chat-driver` tests pass **27/27** when rerun outside the sandbox for headful Chrome launch; root `npm run test` passes **469/469**. `node --check packages/client/public/app.js` is clean, and the T3 completion fix itself is good: `waitForComplete()` returns `complete:false` for `quiet-only` and `timeout` (`packages/web-chat-driver/src/grok/completion.js:150-159`), matching `decideComplete()` (`completion.js:50-60`), and the previously bad timeout test now asserts `complete:false` (`packages/web-chat-driver/tests/grok-completion.test.js:124-135`). `grok.inject()` only notifies `onComplete` when `result.complete` is true (`packages/web-chat-driver/src/grok/index.js:72-81`), with a degraded timeout test proving no `onComplete` fire (`packages/web-chat-driver/tests/grok-completion.test.js:156-169`).
- Blocking repro: the T3 web-chat form sends the raw input value over WS with no trailing submit sentinel (`packages/client/public/app.js:762-779`). T2's `routeWebChatInput()` only submits to `grok.inject()` when the text matches trailing CR/LF; without that, it appends to `session._webChatInput.pending` and returns `{ ok:true, buffered:true }` (`packages/server/src/index.js:1480-1488`). The only `grok.inject()` call is below the submit branch (`packages/server/src/index.js:1489-1502`). Existing seam tests explicitly prove no trailing CR means no inject and lone `\r` is required (`packages/server/tests/web-chat-seams.test.js:239-257`).
- Impact: clicking the visible web-chat "send" button or pressing Enter in the T3 input box clears the field (`packages/client/public/app.js:773-779`) but never sends a Grok turn; the prompt is just buffered server-side until some later CR-bearing payload arrives. Root tests miss this because they cover two-stage POST input and raw CDP canvas input, not the T3 form's WS `input` payload shape. xterm render path remains fenced for non-web-chat panels (`packages/client/public/app.js:475-481`, then normal xterm path starts at `app.js:483-661`), but the web-chat client panel is not functionally complete.
- Non-blocking item still stands separately: README doc drift says `headful:false` adds headless at `packages/web-chat-driver/README.md:64`; code/tests reject it. Not a blocker, but close-out should fix the docs.

### [T3] FIX-LANDED 2026-06-08 13:44 ET — web-chat form now drives T2's two-stage assembler (fixes T4 AUDIT-FAIL 13:37); 473/473
**Accepted — real blocker, confirmed exactly as described.** The form sent one raw `{type:'input',data:text}` with no submit sentinel; T2's `routeWebChatInput` (`index.js:1480-1491`) injects only on a trailing CR, so Send/Enter cleared the field and buffered server-side forever.
- **Fix (client-only, minimal, no T2 change):** new pure `webChatSubmitFrames(text)` (`app.js`) builds the **two-stage** payload — `{type:'input',data:'\x1b[200~'+text+'\x1b[201~'}` then `{type:'input',data:'\r'}` — and the form submit now sends both via `sendComposerSubmit` (replaced the old single raw send; field-clear still gated on the WS send). Bracketed-paste wrapper = MULTI-LINE safe: T2 submits on a TRAILING CR, so a naive `text+'\r'` would submit early on an embedded newline. Same shape as the 4+1 orchestrator inject — and the orchestrator's POST `/input` path was already correct, so this only brings the human-facing form to parity.
- **Coverage for the exact gap you named (form WS payload → `grok.inject`):** 4 new tests in `dashboard-panels-client.test.js` — exactly-one-def; two-stage shape; **a T2-assembler oracle that replicates the `index.js:1482-1485` strip + trailing-CR regex and proves the frames inject the FULL multi-line prompt exactly once with nothing left buffered**; and a stage-1-body-never-ends-in-newline guard (no early submit).
- **Tests:** `dashboard-panels-client` **51/51**; grok **21/21**; **root `npm test` 473/473**; `node --check app.js` clean.
- README `headful:false` doc-drift is T1's non-blocking close-out item, not mine. **[T3] DONE (13:30) stands — lane complete; this was the last open T3 blocker.**

### [T4-CODEX] FINAL-VERDICT GREEN 2026-06-08 13:47 ET — Sprint 72 Grok web-chat panel holds after T3 form fix
- Re-verified latest T3 fix after the 13:44 FIX-LANDED. The web-chat form now sends T2's two-stage assembler frames via `sendComposerSubmit()` (`packages/client/public/app.js:776-785`) and the submit listener clears the field only after those WS frames send (`packages/client/public/app.js:792-800`). The helper emits bracketed-paste body + lone CR (`packages/client/public/app.js:689-700`), so the server reaches the existing `grok.inject()` submit branch (`packages/server/src/index.js:1480-1502`) instead of buffering forever.
- New client tests cover the prior failure and multiline path: `webChatSubmitFrames` emits two frames (`packages/server/tests/dashboard-panels-client.test.js:320-326`); a T2-style assembler oracle injects the full embedded-newline prompt exactly once and leaves nothing buffered (`dashboard-panels-client.test.js:328-345`); the stage-1 body fixture does not end in CR/LF before the lone-CR submit (`dashboard-panels-client.test.js:348-355`). Focused client test: `node --test packages/server/tests/dashboard-panels-client.test.js` passed **51/51**.
- T3 completion-detector finding remains closed. `waitForComplete()` returns `complete:false` for `quiet-only` and `timeout`, consistent with `decideComplete()` (`packages/web-chat-driver/src/grok/completion.js:50-60`, `packages/web-chat-driver/src/grok/completion.js:150-159`); `inject()` only fires `onComplete` when `result.complete` is true (`packages/web-chat-driver/src/grok/index.js:72-81`). Package-local `cd packages/web-chat-driver && npm run test` passed **27/27** with headful Chrome enabled.
- T1/T2 prior passes still hold: T1 CDP/render posture/security/contracts were AUDIT-PASS at 13:27; T2 server-seams/no-PTY-regression/memory provenance were AUDIT-PASS at 13:33. The xterm path remains fenced: `type==='web-chat'` returns into `mountWebChatPanel()` before the normal xterm construction, while all other panel types continue through the existing Terminal/FitAddon/WS path (`packages/client/public/app.js:475-481`, `packages/client/public/app.js:483-661`).
- Final reproduction: root `npm run test` passed **473/473**, fail 0; `node --check packages/client/public/app.js` clean. Non-blocking close-out item: README still says `headful:false` adds `--headless=new` at `packages/web-chat-driver/README.md:64`; code/tests reject that posture breach, so this is documentation drift only.

### [ORCH] CLOSE 2026-06-08 16:02 ET — Deck B GREEN stands; folded into v1.8.0 close-out
Sprint 72 FINAL-VERDICT GREEN (T4-CODEX 13:47) unchanged. Ships in @jhizzard/termdeck@1.8.0 (web-chat server/client seams; the web-chat-driver itself is private/repo-only). Deck A (Sprint 71) cleared its held verdict via the live Claude.ai round-trip this session. Close-out: v1.8.0 + CHANGELOG + kitchen-memory harvest + commit/publish/push/tag + Brad email.
