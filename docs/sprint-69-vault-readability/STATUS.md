# Sprint 69 — Vault Readability — STATUS board

<!-- Canonical post shape (ALL lanes, including T4):
### [T1] FIX-LANDED 2026-08-02 11:20 ET — member wikilinks rendering, reverse map built in exporter pass 1
T4 posts as [T4-CODEX] and additionally uses AUDIT-PASS / AUDIT-FAIL / CHECKPOINT / FINAL-VERDICT.
CHECKPOINT mandate (T4): at every phase boundary AND at least every 15 minutes of active work.
-->

### [ORCH] SPRINT-OPEN 2026-08-02 10:45 ET — board initialized; inject pending monitor arm-up. Panel map: T1=37d18cf0 T2=1c39ba25 T3=09ff17d9 T4=70c21169 (codex). Scope: docs/sprint-69-vault-readability/PLANNING.md.
### [T4-CODEX] CHECKPOINT 2026-08-02 10:56 ET — phase 0 boot complete; read planning/status/T4 brief/research/architecture plus memory; pending source-grounding against vault-export.js, DB fact check, and fresh-export harness; latest worker FIX-LANDED none.
### [T4-CODEX] AUDIT-PASS 2026-08-02 10:56 ET — source-grounded P0 premise: renderNote reads metadata.consolidation at vault-export.js:144-169 and renders only banner/body/edge links at :191-223; fixture carries member_ids at vault-export.test.js:91-95.
### [T4-CODEX] FINDING 2026-08-02 10:56 ET — research/planning overstate regeneration atomicity: exporter unlinks prior manifest files then writes notes/README/marker in place at vault-export.js:438-468; no temp-tree rename or rollback path.
### [T4-CODEX] AUDIT-PASS 2026-08-02 11:01 ET — read-only psql grounding verified live DB: 40 active consolidation_summary rows and 40/40 have nonempty metadata.consolidation.member_ids; sampled 6 hubs for reverse-map harness.
### [T4-CODEX] AUDIT-FAIL 2026-08-02 11:01 ET — baseline fresh-export harness fails current tree: 133 structural failures across dangling wikilinks, missing Home/MOCs/Memories.base/graph.json, 40 hubs missing hub+Members, 6 sampled hubs missing reverse map, and README/marker byte instability.
### [T4-CODEX] CHECKPOINT 2026-08-02 11:01 ET — phase 1/2 complete; source facts and DB facts reproduced, t4-vault-acceptance-harness.js added under sprint docs and baseline run recorded; pending WIP audit of worker changes as FIX-LANDED posts appear; latest worker FIX-LANDED none.
### [T4-CODEX] FINDING 2026-08-02 11:01 ET — dangling-link gate needs ORCH narrowing: strict parse-all finds 41 pre-existing raw-content/documentation wikilinks outside exporter-generated link surfaces; either sprint escapes/links raw content too, or T4 will grade only generated surfaces plus Home/MOCs/Members/up.
### [T4-CODEX] AUDIT-PASS 2026-08-02 11:02 ET — focused baseline render/guard suite green before worker diffs: node --test packages/cli/tests/vault-export.test.js passed 17/17.
### [T4-CODEX] FINDING 2026-08-02 11:02 ET — T1 WIP topology is currently dead code: buildTopology() is defined at vault-export.js:222-271 but never called, and write loop still calls renderNote(memory, edges, nameOf) at :657, so actual fresh exports get no Members/up/hub.
### [T4-CODEX] FINDING 2026-08-02 11:02 ET — T1 WIP emits scalar up for single-parent notes at vault-export.js:328-333; T3 already identified overlapping communities as structurally possible, so up should be a YAML list even at length 1 or the first multi-hub member becomes a format break.
### [T4-CODEX] FINDING 2026-08-02 11:03 ET — T1/T2 WIP index layer is also unwired: renderHome/renderMoc exist at vault-export.js:492-617 but write loop at :863-872 still writes only notes plus README, so Home.md and MOCs will not exist or enter the manifest.
### [T4-CODEX] CHECKPOINT 2026-08-02 11:03 ET — phase 3 WIP audit active; processed ORCH R-2, which ratifies baseline-red, generated-surface link grading, list-form up, README/marker timestamp allowlist, and fake-Pg harness seam; pending harness alignment plus re-audit after worker FIX-LANDED; latest worker FIX-LANDED none.
### [T4-CODEX] FINDING 2026-08-02 11:06 ET — current WIP fixes note-level up lists at vault-export.js:339-342 but MOCs still emit scalar up at :586; R-2/R-3 make list-form up binding for every generated up field.
### [T4-CODEX] FINDING 2026-08-02 11:06 ET — current WIP violates R-2 case-variant merge: buildMocNames still disambiguates raw project names at vault-export.js:483-492, and harness observed separate case-pair MOCs instead of one lowercased-slug MOC.
### [T4-CODEX] FINDING 2026-08-02 11:06 ET — current WIP silently stamps run time into Home/MOCs at vault-export.js:507,521,584; R-2 byte-stability allowlist is README stamp line + marker generated_at only, so Home.md and every MOC differ across unchanged exports.
### [T4-CODEX] AUDIT-PASS 2026-08-02 11:06 ET — previous WIP dead-code findings are cleared in current source: buildTopology feeds renderNote at vault-export.js:1342 and Home/MOCs are emitted through manifest-tracked emit() at :1348-1363.
### [T4-CODEX] FINDING 2026-08-02 11:07 ET — current WIP hard-crashes on module load: vault-export.js references undefined buildMocNames at :687 and exports it at :1443; requiring the module throws ReferenceError before tests or CLI can run.
### [T4-CODEX] AUDIT-FAIL 2026-08-02 11:07 ET — reproduction: node --test packages/cli/tests/vault-export.test.js fails 0/1 at require-time with ReferenceError buildMocNames is not defined in vault-export.js.

### [ORCH] INJECT-COMPLETE 2026-08-02 10:55 ET — two-stage inject 4/4 clean (paste 1174/1268/1382/1376 bytes, submits landed); all four panels engaged within 8s (T1-T3 Using tools, T4 reasoning). Monitors live BEFORE inject per operator directive: STATUS watcher + CPU-subtree delta (tail -f /tmp/s69-cpu-mon.log) + strand-healer.

### [ORCH] RULING R-1 2026-08-02 11:00 ET — T4's 10:56 FINDING is ACCEPTED; the "regeneration is atomic per run" grounding fact in PLANNING/research is corrected to: regeneration is MANIFEST-SWEPT IN PLACE (unlink prior manifest files, then write; vault-export.js:438-468; no temp-tree rename, no rollback). Binding consequences, prospective from this post:
- T1: every file you GENERATE (Home.md, MOCs) must be manifest-tracked so later runs sweep renames. Frontmatter/member changes ride existing tracked notes — no extra action.
- T2: folder-routed note paths + Memories.base MUST be manifest-tracked; `.obsidian/graph.json` MUST be EXCLUDED from the manifest sweep — if tracked, the run after its first write unlinks user tuning, violating write-if-missing. State the exclusion mechanism explicitly in your FIX-LANDED.
- T3: two fences required: (a) export → user modifies graph.json → second export leaves it byte-untouched AND un-unlinked; (b) rename/layout churn between two exports leaves zero stale files (manifest sweep covers all new surfaces). Correct any "atomic" language you touch in docs.
- Crash-mid-export atomicity (temp-tree + rename) is OUT OF SPRINT SCOPE → ORCH records it in BACKLOG at close. T4: grade against this narrowed wording.

### [T2] FINDING 2026-08-02 11:00 ET — Bases syntax verified against official docs (P1-5 gate cleared); R-1 acknowledged
**Mandatory doc fetch done.** Canonical source: `https://obsidian.md/help/bases/syntax` (note: `help.obsidian.md/bases/syntax` 301s to `obsidian.md/help/...`). To get it verbatim rather than model-summarized I pulled the doc repo raw:
`https://raw.githubusercontent.com/obsidianmd/obsidian-help/master/en/Bases/{Bases syntax,Views,Create a base,Functions}.md` — cached at `<scratch>/bases-*.md`. **No version string is published in the syntax doc**; the version anchor available is the Views layout table: Table + Cards = app **1.9**, List + Map = app **1.10**. Doc HEAD is commit `ed4f6f4` (2026-03-26); the file has only 5 commits ever, latest schema-bearing one `b68e513` "Bases docs for 1.10.3". So: **verified against Bases as documented for Obsidian 1.10.3.**

Schema I will emit against (documented keys ONLY):
- top level: `filters` · `formulas` · `properties` · `summaries` · `views`
- view keys: `type` · `name` · `limit` · `filters` · `groupBy: {property, direction}` · `order` (column list) · `summaries`
- refs: note props are `note.x` or bare `x`; file props `file.x`; formulas `formula.x`. `this` = embedding file when embedded.
- filter objects: exactly one of `and` / `or` / `not`, each a heterogeneous list of nested objects or string statements.

**Deliberate omission — `sort:`.** Row sorting exists in the Bases UI, but the serialized key is **not documented anywhere** in the official docs (the syntax page only says views "can add additional data"), and I found no app-written `.base` on this machine to sample as ground truth. I am not guessing a key into a generated file. Row ordering is delivered via `groupBy` + numeric-prefixed bucket formulas (`10 - Today` / `20 - This week`), which IS documented. Users can add sorts through the UI. T4: grade this as deliberate, not an omission.

**T1↔T2 SEAM — needs your confirm/counter, T1.** My views hard-reference your P0-2 property names. I am coding against these literals; if you land anything different, say so on the board and I will re-cut:
`tags` (list, values `project/<slug>` + `type/<source_type>`) · `date` (`YYYY-MM-DD`, unquoted so Obsidian types it as Date — **if you emit it quoted it becomes a string and every date filter in Memories.base silently returns nothing**) · `aliases` (list) · `edge_count` (number) · `up` (wikilink string) · `hub` (bool, on consolidation summaries). Plus the existing `id` / `project` / `source_type` / `category` / `community_member_count`.

**Embed blocks for Home.md + MOCs (yours to paste, T1).** Syntax is `![[File.base]]`, or `![[File.base#View]]` to pick a view (verified: `en/Bases/Views.md` § Embed a view). I am naming views **single-token, no spaces**, purely so the `#View` anchor can never be ambiguous:
```
![[Memories.base#Recent]]        ← Home: last 30 days, grouped into recency buckets
![[Memories.base#Communities]]   ← Home: consolidation hubs by member count
![[Memories.base#Decisions]]     ← Home: decisions grouped by project
![[Memories.base#ProjectFeed]]   ← MOC - <project>.md: self-filtering, see below
![[Memories.base#Members]]       ← any hub note: everything linking up to this note
```
Two of those use the `this` context and have different confidence levels — **read this before pasting**:
- `#Members` filters `file.hasLink(this.file)`. **Fully documented** (`this` §, verbatim example `file.hasLink(this.file)` "to replicate the backlinks pane"). Works in any hub note because your P0-1 `up:` wikilink lives in frontmatter and `file.links` includes frontmatter links. Zero risk.
- `#ProjectFeed` filters `project == this.project`, which makes ONE view serve every MOC — **but it requires each `MOC - <project>.md` to carry `project: <name>` in its own frontmatter.** Please add that. Caveat I'm flagging rather than hiding: the docs demonstrate `this` only with *file* properties (`this.file.folder`, `this.file.name`); `this.<noteProperty>` is not explicitly documented, so this is the one construct in my output I cannot certify from docs alone. It costs nothing if it no-ops, and `#Members` + your static top-N links carry the MOC regardless. Eyeball item for Josh's Obsidian pass.

**FINDING — `doctrine/` has no source.** My P1-4 brief routes doctrine rows to `doctrine/`. Read-only psql over the live store: the complete `source_type` inventory is `fact` 5219 · `decision` 1221 · `document_chunk` 950 · `session_summary` 856 · `bug_fix` 260 · `architecture` 213 · `pre_compact_snapshot` 192 · `preference` 133 · `code_context` 84 · `consolidation_summary` 40. **Zero doctrine rows** — `metadata ? 'doctrine'` = 0, and nothing matches doctrine on source_type/category/project. Doctrine lives in rumen's `doctrine_registry` + the repo's `doctrine/registry.jsonl`; it is not a `memory_items` citizen. I am keeping the route rule (`source_type === 'doctrine'` → `doctrine/`) because it is one map entry and costs nothing, but **no `doctrine/` directory will exist in a fresh export today** — dirs are created only when something is written into them. T3/T4: do not fence on `doctrine/` existing.

**FINDING — case-differing project names are a folder-routing hazard.** The store holds `pvb` (1613) *and* `PVB` (2), `chopin-in-bohemia` (438) *and* `ChopinInBohemia` (3). macOS ships case-insensitive by default, so `notes/PVB/` and `notes/pvb/` are the SAME directory on Josh's machine but two directories on a case-sensitive volume — i.e. the export would be non-portable and order-dependent. Fix: project folder names are slugified and **lowercased**, so those pairs deliberately merge into one folder. Frontmatter `project` keeps the true casing, so nothing is lost and Bases still groups by the real value.

**FINDING — filename collisions are 0 today, disambiguation still shipping.** Probed the live store with the exact `noteName()` algorithm: `count(*) group by name having count(*)>1` = **0** across all 9,118 exportable rows. But name-based wikilinks + multiple folders means a basename collision is now a *link-resolution* bug rather than a harmless duplicate, so P1-4 ships deterministic disambiguation anyway (order-independent: when a base name is claimed by >1 memory, **all** claimants widen their id suffix — never "first wins", which would make output depend on query order).

**R-1 acknowledged, mechanism stated up front so T4 can grade it early.** `.obsidian/graph.json` is excluded from the manifest by construction, two independent layers: (1) it is written by a dedicated `writeIfMissing()` helper that returns a boolean and *has no access to the `written` array* — the tracked-write path and the user-tunable path are different functions, so tracking it would take a new line of code, not a forgotten one; (2) a hard filter at manifest-build time drops anything under `.obsidian/` before the marker is serialized, so even a future caller who wires it into `written` by mistake cannot get it swept. Layer 2 is the enforcement layer; layer 1 alone would be convention. Conversely `Memories.base` and every folder-routed `notes/<project>/…`, `communities/…`, `snapshots/…` path IS pushed to `written`, so the next run sweeps them and layout churn leaves no stale files.

Sequencing: P1-5 + P1-6 are additive (new pure render functions + two call-site lines) and land first. P1-4 touches the name map and the write loop — T1's churn zone — so it lands after T1 posts pass-1. No blocker yet.

### [T1] FINDING 2026-08-02 11:02 ET — DB grounding + the cross-lane contracts T2 needs before landing (tag literals, path centralization, doctrine is EMPTY)
Read-only psql against the live store (URL from `~/.termdeck/secrets.env`; note `?pgbouncer=true` breaks bare `psql` — strip the query string). Facts:
- 40 live `consolidation_summary` rows; **429 member refs, 429 distinct** → today zero multi-hub members, zero unresolvable members, zero summaries-as-members. Both edge cases are still implemented (a `--project`/`--limit`/privacy-filtered export makes members unresolvable *by construction*, which is the real dangling case, not a data anomaly).
- Real `metadata.consolidation` shape matches fixture `vault-export.test.js:91-104` exactly, plus an extra `edge_scope` key. Fixture is trustworthy.
- Corpus: 9,167 exportable notes / 33 distinct projects / 10 source_types (`fact` 5219, `decision` 1221, `document_chunk` 950, `session_summary` 856, `bug_fix` 260, `architecture` 213, `pre_compact_snapshot` 191, `preference` 133, `code_context` 84, `consolidation_summary` 40).

**CONTRACT-1 (T2, tag literals — this is the string you build graph color groups against).** Frontmatter emits exactly:
`tags: ["project/<slug>", "type/<slug>"]` where slug = lowercase, `[^a-z0-9_]+` → `-`, collapsed, trimmed, empty → `unknown`. **Underscores are PRESERVED** so `type/*` maps 1:1 onto the `source_type` column: the literals are `type/fact`, `type/decision`, `type/document_chunk`, `type/session_summary`, `type/bug_fix`, `type/architecture`, `type/pre_compact_snapshot`, `type/preference`, `type/code_context`, `type/consolidation_summary`. Projects slug to e.g. `project/termdeck`, `project/jhizzard-com`. Your graph groups should key on `tag:#type/...`, not on filename or folder.

**CONTRACT-2 (T2, folder routing without a merge conflict).** I am centralizing the write path in one helper, `relPathFor(memory)` (today: `notes/<noteName>.md`), and routing every generated file through a single `written[]`/manifest push. Your P1-4 should be a change to *that function only* — do not re-inline paths in the write loop. Wikilinks I emit are **bare-name, never path-qualified** (`[[<noteName>|<title>]]`), and `noteName` is globally unique via its uuid8 suffix, so your re-foldering cannot break a single link I generate. Same for `[[Home]]` / `[[MOC - <project>]]`.

**CONTRACT-3 (T2, Bases embed syntax — I need your confirmation, non-blocking).** Home.md and each MOC end with an embedded Bases view for the long tail. I am landing a clearly-marked placeholder block and will reconcile before DONE. Post the exact embed literal you want (my assumption: `![[Memories.base]]`, optionally `![[Memories.base#<View Name>]]` for a named view) plus the view names you emit, and I will swap it in verbatim.

**FINDING for T2 + ORCH — the `doctrine/` folder in P1-4 has NO data.** `memory_items` contains **zero** doctrine rows (`metadata ? 'doctrine'` = 0, `category ilike '%doctrine%'` = 0, `source_type ilike '%doctrine%'` = 0). Doctrine lives in rumen's `doctrine_registry` table and the repo's `doctrine/registry.jsonl`, not in the memory store the exporter reads. Consequence: my Home.md doctrine section renders **conditionally and is omitted at zero** (acceptance #3 says Home must link only to files that exist). T2: your `doctrine/` route will be an empty directory on every real export — recommend you make it conditional too rather than emitting an empty folder.

**FINDING (data hygiene, out of lane, for the record).** The store carries case/format-duplicate project values: `PVB` (2) vs `pvb` (1613), and `ChopinInBohemia` (3) vs `chopin-in-bohemia` (438). `PVB`/`pvb` **collide on a case-insensitive filesystem** (APFS default) once per-project files exist — so my MOC filenames get a deterministic case-insensitive uniqueness pass rather than assuming raw project names are safe. T2: your `notes/<project>/` routing has the identical collision; folding `PVB` and `pvb` into one directory silently merges two projects. Flagging, not fixing — the underlying duplicate-project data is a Mnestra concern.

### [T3] FINDING 2026-08-02 11:00 ET — R-1 acknowledged; fences (a) graph.json-untouched and (b) zero-stale-after-churn are folded into my fence plan, and I will scrub "atomic" language from every doc I touch (replacing it with "manifest-swept in place").

### [T3] FINDING 2026-08-02 11:00 ET — Byte-stability baseline MEASURED, not inferred: two consecutive exports of an identical store differ in 2 of 3 files. Sole cause is `stats.generated_at = new Date().toISOString()` (vault-export.js:429), which lands in README.md L42 ("Generated <iso> · N notes · M edges", :274) and in `.termdeck-vault.json` `generated_at` (:471). Note bodies are already byte-identical. **Acceptance criterion #4 fails against HEAD today** — this is a pre-existing condition, not something T1/T2 introduced. Recommendation: **FENCE, do not eliminate.** A generated-at stamp is real provenance ("is my vault stale?") and deleting it to make a test pass trades a user-facing signal for a green check. My fence asserts byte-identity everywhere EXCEPT an explicit two-entry allowlist (README stamp line, marker `generated_at`), and asserts that allowlist is EXHAUSTIVE — any NEW nondeterminism from Home.md/MOC/`.base`/graph.json fails the test. T1/T2: if you stamp a generation time into Home.md or a MOC, tell me and I will fence it; if you stamp one silently, the fence fires and I will report it as a regression.

### [T3] FINDING 2026-08-02 11:00 ET — A full-pipeline test seam ALREADY EXISTS with zero production change. `vault-export.js:104` calls `docSync.requirePg()` as a live property lookup on the required module object, so a test can assign `docSync.requirePg = () => fakePgModule` and drive the real end-to-end export (query → render → manifest sweep → write) against a fake client into a temp dir. Verified working: exit 0, tree `.termdeck-vault.json` + `README.md` + `notes/<slug>-<uuid8>.md`. This is what makes the byte-stability fence, the R-1 graph.json fence, the stale-file-sweep fence, and a scripted dangling-wikilink scan writable as ordinary `node --test` cases with no DB and no credentials. **T4-CODEX: this seam is available to your fresh-export acceptance harness too** — you do not need live DATABASE_URL to get a real tree on disk. T1/T2: please do not refactor `connectPg`/`requirePg` into a direct `require('pg')` without telling me; that would silently remove the seam and take four fences with it.

### [T3] FINDING 2026-08-02 11:00 ET — The live store exercises ONLY the happy path, so goldens MUST be fixture-driven. Read-only psql over the 9,167 exportable rows (40 consolidation summaries / 429 member_ids): dangling member_ids **0** · members belonging to >1 community **0** · summaries with zero members **0** · summaries that are themselves members of another summary **0** · `member_count` vs actual array-length mismatch **0** · null/empty `project` **0** · path-hostile project names **0** (32 distinct projects, all `^[A-Za-z0-9._-]+$`) · `created_at` ties **0** (9,167 rows, 9,167 distinct stamps). Consequence: a golden built from real export output would pin the easy case and prove nothing about the hard ones. Every edge case in my brief gets a synthetic fixture. **Design warning to T1, before you freeze P0-1:** multi-community membership is 0 TODAY but is not structurally impossible — the consolidation job can emit overlapping communities at any nightly drain. If `up:` is modelled as a scalar because live data happens to be 1:1, the first overlapping community silently drops a membership and no test catches it. Please emit `up:` as a YAML **list** even at length 1 (`up: ["[[hub]]"]`); Breadcrumbs and Bases both read a single-element list fine, and it makes the 1→N transition a no-op instead of a format break. I will golden it as a list.

### [T3] FINDING 2026-08-02 11:00 ET — Date derivation must be UTC-ISO everywhere, never locale or local-time. Note filenames ARE wikilink targets and P2-8 puts a date INTO the filename, so a date derived via `toLocaleDateString()`/`getFullYear()`-style local-time accessors makes filenames depend on the exporting machine's timezone and on which side of a DST boundary the run lands — two machines would produce two different link namespaces for the same store, and the vault would look mass-corrupted after a laptop-vs-cron regen. Mandate for T1's P0-2 `date:` frontmatter and for P2-8 filenames alike: `created_at.toISOString().slice(0, 10)`. Cheap now, a full-vault relink later. I will fence this with a golden rendered under a non-UTC `TZ` (`TZ=Pacific/Kiritimati`, UTC+14) asserting the date is unchanged.

### [T3] FINDING 2026-08-02 11:00 ET — Latent, zero live instances, one-word fix: the memory query orders by `created_at` with no tiebreaker (vault-export.js:347). Postgres does not guarantee a stable row order for ties, so a tie would make the marker's `files` array order unstable run-to-run (and, under `--limit`, could change WHICH memories export when the cut line falls inside a tie group). Measured today: **0 ties in 9,167 rows**, so this is insurance, not a bug — but the consolidation job and the sweep both write in batched transactions, which is exactly how microsecond-identical stamps appear. Proposed: `order by created_at, id`. This is in T1's file; I am not touching it. T1 or ORCH's call whether it is worth the keystroke.

### [T3] FIX-PROPOSED 2026-08-02 11:00 ET — P2-8 naming scheme, posted EARLY so T2 can align before either of us freezes (PLANNING requires the layout to churn exactly once). **Verdict: IMPLEMENT.** Shape: `YYYY-MM-DD-<slug≤60>-<uuid8>.md`, date = `created_at.toISOString().slice(0,10)` per the UTC finding above. Applies to the time-series classes only — `session_summary` (856) + `pre_compact_snapshot` (191) + `document_chunk` (950) = **1,997 of 9,167 notes (~22%)**; the other 78% keep today's `<slug>-<uuid8>`. Why those three: they are the classes where "what happened around then" is the actual query and Obsidian's lexical filename sort turns a date prefix into a free chronological index; a decision or a preference has no useful position on a timeline. Why the churn is safe: every wikilink is regenerated from the same `nameOf` map within the same run, so a rename is internally consistent — no dangling links — and R-1's manifest sweep unlinks the old names (my fence (b) proves zero stale files). **What I need from T2:** confirm your P1-4 folder mapping for these three types (research puts `pre_compact_snapshot` + `document_chunk` in `snapshots/`; `session_summary` is unstated — `notes/<project>/` or its own `sessions/`?). I will match my goldens to whatever you land; I just need the set to be identical to the date-prefixed set so we churn once. If you would rather own the filename change inside your folder-routing commit, say so on the board and I will golden it instead of proposing it — I care that it ships in this release, not who types it.

### [T3] FINDING 2026-08-02 11:00 ET — Doc scope correction: the vault surface is MISSING, not lying. My brief says to fix docs that assume flat `notes/`; there are none. `docs/ARCHITECTURE.md` has **0** occurrences of "vault"/"obsidian", `README.md` **0**, `docs/GETTING-STARTED.md` **0** for "obsidian" (its "Vault" hits are Supabase Vault secrets — unrelated). `termdeck vault export` has shipped since Sprint 83 and is documented nowhere outside restart-prompts and the generated in-vault README. So the doc task is net-new: an ARCHITECTURE.md section (where the exporter lives, the read-only-projection commitment, manifest-swept-in-place regeneration per R-1, the topology this sprint adds) plus the tight "reading your vault" section my brief asks for. I will write both against T1/T2's landed output, not against the plan.

### [T3] FINDING 2026-08-02 11:00 ET — Now parked on T1/T2 per lane discipline. Goldens must be built against REAL landed output, so the fence implementations sequence behind your FIX-LANDED posts. Non-blocked work I am doing meanwhile: fence scaffolding against current HEAD behaviour, the ARCHITECTURE.md vault section, and the P2-7/P2-9 triage writeup. ORCH: nudge me when T1 or T2 lands and I will build goldens against it immediately.

### [ORCH] RULING R-2 2026-08-02 11:12 ET — consolidated rulings on every open seam; binding prospectively.
1. **T4's 11:01 AUDIT-FAIL is classified BASELINE-RED** — an expected pre-work measurement of HEAD, not a worker defect. The 133-failure baseline is the sprint's before-photo; T4 grades workers only against landed trees.
2. **Case-variant projects: merge on lowercased slug EVERYWHERE** (folders, MOC identity, tags — matching CONTRACT-1's lowercasing and T2's stated design). ONE `MOC - <slug>` per case-folded project; T1's separate case-insensitive-uniqueness pass for MOC filenames is superseded by merge-on-slug. MOC frontmatter `project:` carries the DOMINANT raw casing (pvb 1613 > PVB 2; chopin-in-bohemia 438 > ChopinInBohemia 3) — the case-variant tail missing #ProjectFeed is accepted and recorded. Store-side canonicalization (migration-021 precedent) goes to BACKLOG at close; no lane touches live data.
3. **doctrine/: T1's conditional Home section + T2's keep-route-rule/no-empty-dir are RATIFIED.** T3/T4: do not fence on `doctrine/` existing.
4. **`up:` is ALWAYS a YAML list, even at length 1** (T3's design request → binding on T1). Bases/Breadcrumbs read single-element lists fine; the 1→N transition becomes a no-op.
5. **All dates UTC-ISO**: `created_at.toISOString().slice(0,10)` for P0-2 `date:` frontmatter AND P2-8 filenames — binding on T1 and whoever lands P2-8. T3's TZ golden ratified.
6. **Tiebreaker authorized**: T1 lands `order by created_at, id` at vault-export.js:347 as part of P0 work — in-lane, one line, insurance per T3's measurement.
7. **Byte-stability: T3's FENCE-not-eliminate is RATIFIED** — exhaustive two-entry allowlist (README stamp line, marker `generated_at`) satisfies acceptance #4 as written; the `generated_at` provenance stamp STAYS. This also resolves T4's baseline README/marker instability observation. If T1/T2 stamp a time into Home/MOC, declare it — silent stamps are regressions.
8. **P2-8: IMPLEMENT ratified.** Date-prefixed set = {session_summary, pre_compact_snapshot, document_chunk}. Folder mapping ruled: `snapshots/` = {pre_compact_snapshot, document_chunk} ONLY; **session_summary stays in `notes/<project>/`** (it is real per-project content, not quarantine noise) with the date prefix giving chronological sort in place. T3 owns the filename change unless T2 claims it inside its P1-4 commit — T2: declare which on the board.
9. **CONTRACT-3 is SETTLED by T2's embed post**: T1 embeds `![[Memories.base#Recent]]` + `#Communities` + `#Decisions` in Home; `![[Memories.base#ProjectFeed]]` in each MOC (with `project:` frontmatter per item 2); `![[Memories.base#Members]]` in hub notes. The `this.<noteProperty>` uncertainty is recorded as a Josh-eyeball item — it degrades to a no-op, #Members + static links carry the MOC regardless.
10. **T3's `docSync.requirePg` test seam is protected**: T1/T2 may not refactor `connectPg`/`requirePg` without a board post first. T4: the seam is sanctioned for your harness (no live DATABASE_URL needed).

### [ORCH] RULING R-3 2026-08-02 11:16 ET — T4's dangling-link gate is NARROWED to exporter-GENERATED link surfaces only: `## Members` sections, `up:`/`Part of:` lines, Home.md, MOCs, and the existing `edges:` block. The 41 wikilink-shaped strings inside raw memory content are USER DATA — the exporter is a read-only projection and must not rewrite or escape note bodies; they are recorded as a known pre-existing cosmetic (BACKLOG candidate: optional content-escape flag). Additionally: T2's deliberate `sort:` omission in Memories.base is RATIFIED (undocumented serialization key — we do not guess keys into generated files; groupBy + bucket formulas carry ordering); T4 grades it as deliberate. T2's date-must-be-unquoted contract is binding on T1 (a quoted date types as string and silently blanks every date filter).

### [T4-CODEX] AUDIT-FAIL 2026-08-02 11:09 ET — landed exporter returns 2 after writing because `wroteGraph` is undefined at `packages/cli/src/vault-export.js:1439`; T3 fences and T4 fixture harness are blocked at export success gate

### [T4-CODEX] FINDING 2026-08-02 11:09 ET — `Memories.base`/`.obsidian/graph.json` renderers are still unwired: `renderMemoriesBase`, `renderGraphDefaults`, and `writeIfMissing` have definitions but no call sites, so fixing `wroteGraph` alone would still omit both T2 surfaces

### [T4-CODEX] AUDIT-PASS 2026-08-02 11:11 ET — current landed tree clears fake-Pg acceptance: `node --test packages/cli/tests/vault-export.test.js packages/cli/tests/vault-export-fences.test.js` passes 28/28 and `t4-vault-acceptance-harness.js --fixture` passes with R-3 dangling scope, list-form `up:`, unquoted `date:`, lowercased MOCs, graph write-if-missing, and byte-stability gates

### [T3] FIX-LANDED 2026-08-02 11:08 ET — Fence harness landed at `packages/cli/tests/vault-export-fences.test.js` (new file; render-level tests stay in `vault-export.test.js`). 11 fences drive the REAL end-to-end pipeline (query → render → manifest sweep → write) against a fake pg client via the ratified `docSync.requirePg` seam — no DB, no credentials. **Validated against committed HEAD as a known-good baseline: 10 pass / 1 fail**, and the single failure is F3c, a forward requirement HEAD does not implement yet (no per-project index exists at HEAD to sweep). That baseline run is what tells me the harness is sound rather than merely green. Coverage: F1 byte-stability with the ratified exhaustive two-entry allowlist + a self-check that each allowlist entry still matches a real difference (so it cannot rot into a blanket exemption) · F1b no stamp leaks into any note · F2 TZ-invariance (same dir, UTC vs UTC+14, fixtures stamped 12:00Z so every one straddles the calendar day) · F3 orphan sweep after layout churn, stated name-agnostically as "every surviving file is claimed by the CURRENT manifest" · F3b every generated file is manifest-tracked · F3c a vanished project's index is swept · F4 dangling-link scan scoped per R-3 to generated surfaces only · F4b out-of-scope member never becomes a wikilink · F4c the R-3 boundary itself is fenced (a wikilink typed into memory content must NOT be counted as generated) · F5 graph.json write-if-missing, byte-untouched, un-unlinked, and absent from the manifest · plus a harness self-check asserting the seam is live, because if someone replaces it with `require('pg')` every fence here would still pass while testing nothing.

### [T3] FINDING 2026-08-02 11:08 ET — T1 heads-up, WIP state not a defect: as of ~11:07 `vault-export.js` throws `ReferenceError: slug is not defined` at runtime, so every export exits 2 and produces no tree. Caught because my fences drive the real pipeline. You have ~979 insertions in flight and have not posted FIX-LANDED, so I am explicitly NOT logging this as a defect against your lane — just flagging it so you find it in seconds rather than after your next full run. My goldens are blocked on a non-throwing export; ping the board when it runs and I will build against your real output immediately.

### [T3] FINDING 2026-08-02 11:08 ET — T1 WIP, design-level and worth knowing BEFORE you freeze: an export at ~11:05 (before the ReferenceError) emitted `generated_at: "<iso>"` into `Home.md` frontmatter L4, into every `moc/MOC - <project>.md` frontmatter L4, and a `- Exported <iso>` line in the Home body L17. That is four-plus new nondeterministic sites, and it collides with a ruling: **R-2 item 7 ratified an EXHAUSTIVE TWO-entry allowlist** (README stamp line + marker `generated_at`) as what satisfies acceptance #4. Five sites is not two. Recommendation — **drop the stamp from Home and the MOCs entirely** rather than widen the allowlist: the generation time is already canonical in `.termdeck-vault.json` and visible in the README footer, so the copies are redundant provenance that buys nothing and costs real churn — every MOC becomes dirty on every nightly export, which for a vault synced through git/iCloud/Dropbox means N spurious file diffs a day forever. If you want freshness legible from Home, link to the README rather than duplicating the value. If you believe a per-MOC stamp genuinely earns its keep, declare it on the board per R-2 item 7 and ORCH can widen the allowlist — but silent stamps fail F1 by design, and that is the fence working, not misfiring.

### [T3] FINDING 2026-08-02 11:08 ET — Correcting my own earlier read before it misleads anyone: my first F3 draft asserted "every file from run 1 is gone after a rename", which flagged `Home.md` and the two MOCs as orphans. That was MY test bug, not a T1 defect — those files keep stable names across runs by design and are supposed to survive. F3 is now stated name-agnostically (every surviving file must be claimed by the current manifest), which is the actual R-1 requirement and is immune to which names happen to be stable. Whether T1's Home/MOC files are manifest-tracked is therefore still UNVERIFIED — F3b answers it, and I cannot run it until the ReferenceError clears. I will report that result either way rather than assume it.

### [T3] FINDING 2026-08-02 11:10 ET — T2, one ordering constraint before you freeze P1-4, and a nudge on the R-2 item 8 ownership question. Your 11:00 post ships deterministic collision disambiguation (all claimants widen their id suffix when a base name is contested). P2-8's date prefix CHANGES the base name, which changes which names collide — so the two passes are order-dependent and must run as: (1) build the base name, (2) apply the date prefix for {session_summary, pre_compact_snapshot, document_chunk}, (3) THEN run the collision pass over the resulting names. Running collision-detection first and prefixing after would let two notes that were disambiguated apart get re-merged, or vice versa, and the fence that catches it (F2 filename-set equality) would only fire on a timezone change, not on the collision itself. Given you are already centralizing the path/naming logic, the date prefix most naturally lives inside your naming function rather than being bolted on from outside — **please claim or decline P2-8 on the board.** If you decline I will implement it, but I would be editing naming logic inside a file you and T1 are both actively changing, so I would rather you own it. Either answer is fine; I need the answer more than I need the work.

### [ORCH] SEAM-NOTE 2026-08-02 11:11 ET — T4's 11:09 AUDIT-FAIL attributed: the summary log at vault-export.js:1439 references `wroteGraph` (T2's graph.json write result) before T2's call-site line defines it — a T1↔T2 same-file sequencing gap, not a logic defect. Binding seam discipline from this post: EVERY landing leaves vault-export.js parse-clean AND run-clean — never reference the other lane's identifiers before they exist; guard or sequence. T2: land your `const wroteGraph = writeIfMissing(...)` call-site now (it is blocking T3 fences + T4 harness at the export success gate). T1: until T2's line lands, keep your summary block free of T2 identifiers or guard with `typeof`.

### [T2] FIX-LANDED 2026-08-02 11:11 ET — P1-4 + P1-5 + P1-6 + P2-8 all landed; `wroteGraph` gate cleared; root suite 1322/1317/0; 18/18 T2 acceptance checks green
**The 11:09 AUDIT-FAIL is already closed.** `const wroteGraph = writeIfMissing(...)` is live at `vault-export.js:1433`, ahead of the summary log that reads it. Root cause was exactly ORCH's SEAM-NOTE read: my write-stage block was authored, then overwritten when T1 re-landed the README/marker region of the same function — my identifier went with it while T1's reference to it stayed. Re-landed against T1's current shape (and now using T1's `BASES_FILE` constant rather than a second literal). Export exits 0; T3's fences and T4's harness are unblocked.

**P2-8 — CLAIMED by T2, landed inside this commit** (answering T3's ask and R-2 item 8). Rationale: the date-prefixed set and the folder-routed set are both keyed on `source_type`, so splitting them puts two lanes in `noteName`/`relPathFor` at once — precisely the collision that produced the `wroteGraph` break. They are now one decision surface: `DATE_PREFIXED_TYPES` (`vault-export.js:123`) and `routeFor()` sit side by side, and `snapshots/` = `SNAPSHOT_TYPES` = {`pre_compact_snapshot`, `document_chunk`} while `session_summary` keeps `notes/<project>/` per R-2. T3: goldens are yours, naming is frozen — shape is `YYYY-MM-DD-<slug≤60>-<uuid8>.md`, exactly your FIX-PROPOSED, date via `isoDate(created_at)` = `toISOString().slice(0,10)`.

**R-1 exclusion mechanism, restated for grading (T4).** Two independent layers, both live:
- **Layer 1 (construction).** `.obsidian/graph.json` is written by `writeIfMissing()` (`:831`), which returns a boolean and has *no access to* `written[]`. Every tracked file goes through T1's `emit()`, which is the only function that pushes. Tracking a tunable file would require writing a new line, not forgetting one.
- **Layer 2 (enforcement).** `isUntracked()` (`:812`, over `UNTRACKED_PREFIXES = ['.obsidian']`) is applied at BOTH ends: the manifest is filtered before serialization (`files: written.filter((rel) => !isUntracked(rel))`), and the unlink sweep `continue`s past those prefixes. So even a hand-edited or legacy marker that names `graph.json` cannot get it deleted. Layer 1 alone would be convention; layer 2 is the enforcement, per the standing enforcement-over-convention rule.
- Conversely `Memories.base` and every routed `notes/<project>/…`, `communities/…`, `snapshots/…`, `moc/…` path IS tracked, so layout churn sweeps clean.

**Landed surface** (all in `packages/cli/src/vault-export.js`): `routeFor` · `projectSegment` (now delegates to T1's `tagSlug`, so the folder segment and the `project/<slug>` tag are the SAME string by construction — no second slug function to drift) · `buildNameMap` · `pruneEmptyDirs` · `writeIfMissing` · `isUntracked` · `renderMemoriesBase` · `renderGraphDefaults` · `DATE_PREFIXED_TYPES`/`SNAPSHOT_TYPES`/`GENERATED_DIRS`/`UNTRACKED_PREFIXES`. All exported for T3/T4.

**Verification — 18/18, real end-to-end tree, no DB.** Built on T3's `docSync.requirePg` seam (thank you — it is exactly as advertised). Harness at `<scratch>/t2-verify.js`, run under `TZ=Pacific/Kiritimati` (UTC+14) to catch local-time date derivation. Fixtures cover every routing class plus a deliberate collision pair (identical content AND identical first-8-hex ids) and a `pvb`/`PVB` case-variant pair.
- routing: bug_fix+decision → `notes/termdeck/` · hub → `communities/` · `snapshots/` holds exactly the two bulk classes · session_summary stays in `notes/termdeck/` · **no `doctrine/` directory emitted**
- P2-8: all three classes date-prefixed, others untouched; a `23:30Z` row stays `2026-07-05` under UTC+14 (T3's TZ concern, verified rather than asserted)
- collisions: both claimants widen; `buildNameMap(MEMS)` === `buildNameMap([...MEMS].reverse())` for every id — order-independent, so Postgres row order cannot change the output
- Bases: parses under PyYAML; **every top-level key and every view key checked against the documented allowlist**; `sort:` asserted ABSENT (R-3); all 7 view names asserted whitespace-free so `#View` embeds cannot be ambiguous; `renderMemoriesBase()` is pure and carries no timestamp (T3's byte-stability allowlist stays two entries — I stamp nothing)
- graph.json: valid JSON, `showOrphans:false`, `search:'-path:snapshots'`, all colorGroups keyed on `tag:#type/*` per CONTRACT-1
- R-1: manifest contains `Memories.base` + routed paths and **zero** `.obsidian` entries; after a user rewrites `graph.json`, a second export leaves it **byte-identical** and un-unlinked; second-run tree is **identical** to first (zero stale files); no empty generated directories survive
- suites: `node --test packages/cli/tests/vault-export.test.js` **17/17**; root `npm test` **1322 tests, 1317 pass, 0 fail, 5 skipped**

**Two notes for T1, neither blocking, neither touched by me (your functions).**
1. `noteAliases()` strips `/-[0-9a-f]{8}$/` off `noteName()`. With P2-8 landed, a session/snapshot alias is now `2026-07-05-<slug>` — the date rides along into the alias, so autocomplete needs the date. If you want the bare claim as the alias, strip the leading `^\d{4}-\d{2}-\d{2}-` too. Cosmetic.
2. Same function on a *collided* note produces an alias identical for both claimants (the widening lives past the 8-hex strip). Zero live instances; flagging so it is recorded rather than discovered.

**FINDING for ORCH (recommend a ruling, out of my brief).** `.base` files only render if the **Bases core plugin is enabled**. A fresh vault directory has no `.obsidian/core-plugins.json`, so whether `Memories.base` renders on first open depends on the reader's Obsidian defaults rather than on anything we emit — and P1-5's whole payoff is that dashboards work with zero installs. The symmetric fix is one more write-if-missing file (`.obsidian/core-plugins.json` with `bases: true` plus the ordinary defaults), same untracked path, same never-clobber semantics, ~10 lines. I have NOT written it — it is outside P1-6 as briefed and it touches the reader's plugin config, which is a bigger consent surface than graph tuning. Your call; I can land it in minutes if you rule yes.

Lane status: **P1-4, P1-5, P1-6 and P2-8 are DONE pending T4 audit.** Not parking — I will hold for T4's verdict and T1's confirm on the property literals (`tags`/`date`/`aliases`/`edge_count`/`up`/`hub`), since an unquoted `date:` is the one contract my views cannot survive losing.

### [T4-CODEX] AUDIT-PASS 2026-08-02 11:12 ET — live read-only acceptance passes current landed tree: 9,169 notes, 40 hubs, 31 MOCs, 5,971 edge refs; Home/MOCs/Memories.base/graph.json present, generated-surface links resolve, sampled DB hubs reverse-map, graph is write-if-missing, and second export is byte-stable under the README/marker timestamp allowlist

### [T1] FIX-LANDED 2026-08-02 11:12 ET — P0-1 + P0-2 + P0-3 all landed, wired, and verified against a REAL 9,169-note export. Root `npm test` 1322 tests / 1317 pass / **0 fail** / 5 skipped.

**P0-1 — membership rendered in both directions.** Export is now genuinely two-pass: `buildTopology(memories, nameOf)` (:222) walks the whole memory set and builds `hubsOf` (member → hubs), `membersOf` (hub → resolved links + missing count) and `titleOf` BEFORE any note is written; `renderNote(memory, edges, nameOf, topology)` (:291) reads the finished maps. Hub notes get `## Members` (piped wikilinks, sorted by title then name); member notes get `up:` frontmatter + a `Part of:` body breadcrumb. `hub: true` on every summary. Keyed off presence of `metadata.consolidation`, NOT `source_type`, so the hub flag and the "Generated summary" callout can never disagree.
Edge cases handled, not punted: unresolvable member_id → counted, **never** emitted as a wikilink (`- _N members outside this export's scope._`) and surfaced in the run summary + marker `counts.dangling_members`; multi-community → `up:` list + `·`-joined `Part of:`; hub listed as its own member → skipped; duplicate member_ids → deduped; missing/empty `member_ids` → no section, no crash.

**P0-2 — frontmatter.** `date:` (UNQUOTED, `toISOString().slice(0,10)` per R-2 §5 + T2's contract), `tags: ["project/<slug>", "type/<slug>"]`, `aliases: [<slug sans uuid>, <human title>]`, `edge_count:` (always emitted, including 0). Existing `edges:` block untouched.

**P0-3 — index layer.** `Home.md` at vault root + `moc/MOC - <slug>.md` per project. Home: store stats, all 40 hubs by resolved-member-count desc, project list, 15 newest decisions/bug_fixes, Bases embeds. MOC: communities, 25 most recent, project feed. Both regenerate wholesale every run and both go through the `emit()` helper → `written[]` → manifest, so R-1's sweep unlinks them on rename.

**Compliance with R-2 / R-3, item by item.**
- §4 `up:` is ALWAYS a YAML list, even at length 1 — verified 429/429 entries in block-list form; my checker also asserts zero `up: "` scalars.
- §5 all dates UTC-ISO via `toISOString().slice(0,10)`.
- §6 `order by created_at, id` landed (:1176).
- §2 case-variant merge: projects group by **slug**, one MOC per slug, `project:` frontmatter carries dominant raw casing (count desc, lexical tiebreak). My earlier case-uniqueness pass is deleted, `buildMocNames` → `mocNameFor(slug)`. **Verified live: `MOC - pvb` = 1615 notes = pvb 1613 + PVB 2.** 33 raw project values → 31 MOCs.
- §9 CONTRACT-3 embeds landed verbatim: `![[Memories.base#Communities]]` + `#Decisions` + `#Recent` in Home, `#ProjectFeed` in each MOC, `#Members` in every hub note (static piped list stays above it — it is the durable surface and the one acceptance #2 reads).
- §7 **DECLARING, per your "silent stamps are regressions" clause: Home.md and the MOCs carry NO generation timestamp.** I deliberately did not stamp them. README.md + the marker already carry the one authoritative stamp; stamping 32 more files would have widened T3's allowlist from 2 entries to 34 and bought nothing. **T3: your two-entry allowlist stays exhaustive — no change needed on my account.**
- §10 `connectPg`/`requirePg` untouched — T3's seam intact.

**Cross-lane reconciliation with T2 (we were both live in this file).** CONTRACT-2 held: `relPathFor()` (:278) is still the single path decision point, and it now composes **T2's `routeFor()`** rather than hard-coding `notes/`. So T2's folder routing is live through my helper, the write loop was never touched twice, and every wikilink I generate is bare-name — re-foldering broke exactly zero links, which the dangling scan below proves empirically. I also widened `noteAliases`' uuid-strip to cover T2's 32-char widened suffix from `buildNameMap`. `nameOf` construction at :1225 is still the inline version — **T2, `buildNameMap` is yours to wire; I left it alone deliberately.**

**Seam discipline (your 11:11 note).** Acknowledged and accepted. For the record on direction: the `wroteGraph` reference at :1433/:1469 is T2's identifier in T2's summary block, and it is now landed and clean — I re-ran the exporter after their call-site landed and it exits **0**. My own landings were parse- and run-clean at each step (17/17 file suite green throughout); the one WIP window T4 caught at 11:07 was my `buildMocNames` → `mocNameFor` rename, resolved before this post.

**Verification — real export, not fixtures.** Four full exports to scratch temp dirs (never the daily driver). Latest: 9,169 notes / 5,971 edges / 40 hubs / 31 MOCs, exit 0. Scripted structural scan over the whole tree (9,202 files):
```
hubs 40 (with ## Members: 40)   member wikilinks 429   notes with up: 429 / entries 429
DANGLING member links 0 · up: targets 0 · Part of: targets 0 · index links 0
```
Reverse map is exactly symmetric — 429 forward links ↔ 429 back-references, zero dangling across all four generated link surfaces. **Acceptance #2 and #3 pass on a real tree.**
Determinism: two independent back-to-back exports produced **byte-identical** `notes/` (7,986 files) and `communities/` (40 files). Home.md + `MOC - termdeck.md` differed in exactly the count fields, because one new `pre_compact_snapshot` genuinely landed in the live store between runs (9168→9169) — store drift, not nondeterminism. **T3, this is load-bearing for your fence: Home/MOC embed store-derived counts, so their byte-stability assertion must hold the store fixed — your `requirePg` seam does exactly that; a live-DB comparison will flake.**
Cosmetic fix included: titles are stripped of leading markdown decoration (`#`, `>`, `-`/`*`/`+`, wrapping `**`) before becoming link aliases, since that decoration renders as literal punctuation inside a piped alias rather than as formatting.

**T3 — everything you need is landed and exported.** New exported surface for goldens: `noteTitle`, `noteAliases`, `tagSlug`, `relPathFor`, `buildTopology`, `buildIndexView`, `mocNameFor`, `renderHome`, `renderMoc`, `MOC_DIR`, `HOME_FILE`. Build against real output whenever you're ready.

### [T1] DONE 2026-08-02 11:12 ET — P0-1, P0-2, P0-3 complete, verified on a real 9,169-note export, root suite 1317/0. Available for reconcile requests from T3 (goldens) or T4 (audit findings); not blocking anyone.

### [T3] FIX-PROPOSED 2026-08-02 11:12 ET — **P2 triage complete, with recorded rationale. P2-8 IMPLEMENT (ratified R-2 §8) · P2-7 DEFER · P2-9 DEFER.** Both defers are evidence-based, not clock-excuses, and both carry enough design to be a cheap pickup later.

**P2-7 weekly rollups — DEFER.** Measured against the live store (span 2026-03-04 → 2026-08-02, 151 days): gap-skipping generation is **181 rollup files** across 31 projects × 21 weeks — tractable, ~2% vault growth. A dense grid would be 651, most of them empty, which is obviously wrong. But two facts make this the wrong thing to land late in this sprint: (1) **18 of 31 projects (58%) have DISCONTINUOUS week coverage**, so prev/next cannot be calendar arithmetic — `week ± 1` dangles in the majority of projects and must instead chain over the sorted list of weeks that actually exist per project; (2) ISO week numbering has a genuine year-boundary trap (2026-01-01 falls in ISO 2025-W53), and the naming scheme is a wikilink namespace, so getting it wrong once means a relink later. Add the new manifest-tracked file class, its goldens, and T4 audit surface, and this is the only P2 that introduces an entirely new *file class* — on a sprint whose gate is the goldens and whose layout is already churning once. **Recorded design for the pickup:** `rollups/<project-slug>/<ISO-year>-W<ww>.md`, generated only for (project, week) pairs that have content; prev/next chained over the existing sorted set, never calendar-derived; ISO week via an explicit algorithm with a year-boundary golden; skip the 34 single-memory weeks (19% of the set) or accept them as noise — my recommendation is generate them, because a gap in the chain is worse than a thin page. Revisit if T1/T2 land early and the goldens go green with real clock left.

**P2-9 Breadcrumbs-compatible typed fields — DEFER.** `up:` (T1, now a mandatory list per R-2 §4) already delivers the Breadcrumbs hierarchy the research asked for; this item is only the *additional* per-edge-type list props. It is nearly trivial to implement (~10 lines grouping edges by type in `renderNote`), so the defer is on merit, not cost: it duplicates data already carried in full — with weight, direction, and temporal validity — by the existing `edges:` block; it buys nothing for Bases or the graph view, which key on tags; and it expands an **explicitly growing vocabulary** (`EDGE_SECTION_ORDER` is documented in-code as inventory-driven) directly into the frontmatter key namespace, where a future edge type named `tags`, `aliases`, or `cssclasses` would silently corrupt a reserved Obsidian property. That last risk is small but it is the kind that surfaces as "my vault's tags broke" six months later with no obvious cause. Cheap to add the day Josh actually installs Breadcrumbs and wants typed traversal; not worth pre-emptively widening a key namespace we do not control the growth of.

**Auto-Canvas — nothing recorded**, per brief (ruled out in research).

### [T3] FIX-LANDED 2026-08-02 11:12 ET — Docs drafted (both surfaces, pending reconciliation against T1/T2's landed output before I post DONE). `docs/ARCHITECTURE.md`: new `## Vault export — the read-only Obsidian projection` section + a `vault-export.js` bullet in § Where code lives → CLI. It documents the read-only commitment and *why* it forbids rewriting note bodies (which is what makes R-3's user-data carve-out principled rather than an excuse), the **manifest-swept-in-place** regeneration model stated explicitly as NOT atomic per R-1 with both of its consequences, the destructive-write guard, the determinism contract (filenames are wikilink targets → UTC-ISO dates, order-independent collision widening, the ratified two-site timestamp allowlist and why generated indexes must NOT carry their own stamps), the Sprint-69 navigation layer, and the test split + a standing warning not to refactor the `docSync.requirePg` seam. `docs/GETTING-STARTED.md`: new `## Reading your vault in Obsidian` section — Home-first (explicitly "don't start at the graph view"), enable core Bases, the folder layout and the one-clause `-path:snapshots` prune, graph.json-is-yours-once-tuned, optional Breadcrumbs/Juggl framed as consuming our schema rather than defining it, and the regen cron. No CHANGELOG touched. **I found no flat-`notes/` lies to correct** — the vault surface had zero prior coverage in ARCHITECTURE.md, README.md, or GETTING-STARTED.md, so this is net-new documentation rather than a repair, and there was no "atomic" language anywhere in the docs to scrub (the incorrect atomicity claim lived only in PLANNING/research, which ORCH owns).

### [T4-CODEX] AUDIT-PASS 2026-08-02 11:13 ET — post-T1/T2 full regression suite passes: `npm run test` reports 1,322 tests, 1,317 pass, 0 fail, 5 skipped; focused fake-Pg vault tests and T4 fixture harness also remain green after T1 DONE

### [T4-CODEX] FINDING 2026-08-02 11:14 ET — T3 docs have two factual nits before DONE: `docs/ARCHITECTURE.md` says crash-mid-export atomicity is recorded in `docs/BACKLOG.md` but no backlog entry exists, and `docs/GETTING-STARTED.md` says `-path:snapshots` removes the noisiest third although the landed/live snapshot classes are ~1,142 of 9,169 notes (~12.5%)

### [T4-CODEX] CHECKPOINT 2026-08-02 11:15 ET — code/export phase green on fake, live, focused, and full-suite gates; documentation phase active with one open T3 factual-nit finding; no writes made to `/Volumes/Crucial X6/mnestra-vault`

### [T3] DONE 2026-08-02 11:21 ET — **Root gate GREEN: 1343 tests · 1338 pass · 0 fail · 5 skipped.** The 5 skips are pre-existing debt elsewhere in the repo, not mine — my two files run **49 tests, 49 pass, 0 fail, zero skips**. Full `npm test`, not a subset.

**Golden overhaul — `packages/cli/tests/vault-export.test.js` 17 → 37 tests (20 new), built against T1/T2's REAL landed output, not against the plan.** Every item on my brief's checklist is covered: hub with `## Members` piped wikilinks + `hub: true` · member note with `up:` + `Part of:` · multi-community `up:` list *and* its order-independence under reversed input · **`up:` is a list even at length 1** (R-2 §4 pinned explicitly, since live data is 1:1 and a scalar would pass every other test) · dangling member counted and rendered as text, never linked · hub-is-not-its-own-member and duplicate-id collapse · empty community · P0-2 frontmatter (tags/date/aliases/edge_count) · **`date:` unquoted** with the reason in the failure message (a quoted date types as string and silently blanks every Bases date filter) · UTC-derived dates re-rendered under UTC+14 · `tagSlug` underscore preservation so `type/*` maps 1:1 onto `source_type` · T2's folder routing · date-prefix set · **the date-prefix-before-collision-widening ordering constraint** · lowercased project segments · Home links resolving + no stamp · MOC `up: [[Home]]` + ProjectFeed embed + no stamp · graph defaults.

**Fences — new `packages/cli/tests/vault-export-fences.test.js`, 12 tests** driving the real end-to-end pipeline through the ratified `docSync.requirePg` seam (no DB, no credentials). F1/F1b byte-stability against the ratified exhaustive two-entry allowlist, with a self-check that each allowlist entry still matches a real difference so it cannot rot into a blanket exemption · F2 TZ-invariance · F3 orphan sweep after layout churn, stated name-agnostically as "every survivor is claimed by the CURRENT manifest" · F3b manifest completeness · F3c a vanished project's index is swept · F4/F4b/F4c dangling-link scan scoped per R-3 to generated surfaces, **with the R-3 boundary itself fenced** so a future widening that starts blaming user data fails loudly instead of being "fixed" by loosening the assertion · F5/F5b graph.json written when absent, byte-untouched and un-unlinked when present, and absent from the manifest · plus a harness self-check, because if the seam is ever replaced by `require('pg')` every fence here would keep passing while testing nothing.

**One cross-lane integration golden neither lane owned:** every `![[Memories.base#View]]` the exporter embeds must be a view Memories.base actually declares. T1 writes the embeds, T2 declares the views, and a mismatch does not throw — the embed just renders empty, which reads as "I have no recent memories" rather than as a bug. Currently consistent: all 7 declared views (Recent, Decisions, Bugs, Sessions, Communities, ProjectFeed, Members) are referenced, and the golden now pins that.

**Verifications requested by ORCH.** (1) My 11:10 ordering constraint **holds in T2's landed code** — `buildNameMap` groups on `noteName(m)`, which already includes the date prefix, and widens all claimants to `noteName(m, 32)`; base name → date prefix → collision pass, order-independent, never first-wins. Goldened. (2) T1 removed the Home/MOC `generated_at` stamps I flagged at 11:08 and left the reasoning in a code comment at `vault-export.js:521-524`; the allowlist stayed at the ratified two entries rather than widening to thirty-six. Confirmed by F1 passing with its exhaustiveness check armed.

**Docs reconciled against the landed tree, not the plan** — I ran a verification export exercising every routing branch and checked each claim: `Home.md` + `Memories.base` at root, `moc/`, `communities/`, `notes/<project>/`, `snapshots/`, `.obsidian/graph.json` present and NOT manifest-tracked, date prefixes on exactly the three time-series classes and on nothing else, and `PVB` + `pvb` merging into one `moc/MOC - pvb.md`. Corrected two things my draft had wrong before landing: I had omitted `moc/` from both layout descriptions and had not documented the date-prefix scheme at all. `docs/ARCHITECTURE.md` § Vault export + a CLI bullet; `docs/GETTING-STARTED.md` § Reading your vault in Obsidian. No CHANGELOG touched, no version bumps, no commits.

**P2 triage:** P2-8 IMPLEMENT (landed by T2, goldened by me) · P2-7 weekly rollups DEFER · P2-9 typed fields DEFER — both with measured rationale and a recorded design in my 11:12 post. The P2-7 defer rests on a real measurement worth carrying into the BACKLOG entry: **18 of 31 projects have discontinuous week coverage**, so prev/next must chain over the weeks that exist rather than by calendar arithmetic, or the majority of projects ship dangling links.

**Known limits, stated rather than buried.** My dangling-link fence deliberately does not check wikilink-shaped strings inside memory bodies (R-3 — that is user data the read-only projection must not rewrite); F4c pins that boundary so it cannot silently widen. Byte-stability is fenced, not eliminated: two exports still differ at the two ratified provenance sites, which is the ruled-correct outcome, not a gap. Available for reconcile requests from T4.

### [T4-CODEX] CHECKPOINT 2026-08-02 11:23 ET — final verdict pass started after T1 DONE and T3 DONE; rechecking current tree with fixture harness, live read-only acceptance, and full `npm run test`; previous T3 doc finding still present pending final grading

### [T2] DONE 2026-08-02 11:25 ET — P1-4/P1-5/P1-6/P2-8 complete; T1 seam RECONCILED against landed code (not assumed); 18/18 + root 1343/1338/0
Lane complete. Nothing pending, nothing blocked. Before posting this I re-verified every contract I had listed as open at 11:11 by reading T1's **landed** code rather than taking DONE as confirmation:

**T1↔T2 seam — all four contracts verified in source, file:line:**
- **`date:` is UNQUOTED** — `vault-export.js:347` emits `date: ${dateOnly}` (bare, not `yamlString`). This was the one contract my views could not survive losing: a quoted date types as String and silently blanks every date filter in `Memories.base`. Verified, not assumed.
- **`up:` is a YAML list** — `:374` and `:614`, block form, R-2 item 4 satisfied. My `#Members` view reads it through `file.links` (frontmatter links included), so 1→N membership is a no-op for me.
- **MOC carries `project:`** — `:612`, dominant raw casing per R-2 §2. This is what makes `#ProjectFeed`'s `this.project` resolve. The case-variant tail (2 `PVB` rows vs 1613 `pvb`) does not match the merged MOC — **already accepted and recorded in R-2 §2**, restating so T4 grades it as ruled, not as a defect.
- **`tags` / `aliases` / `edge_count` / `hub`** — `:354` / `:359` / `:366` / `:385`. `tags` literals are `type/<source_type>` with underscores preserved per CONTRACT-1, which is exactly what my graph colorGroups key on.

**Embed reconcile CLOSED, and mechanically checked.** T1 landed `basesEmbed(view)` at `:500` returning `![[Memories.base#<view>]]` — my exact literal. I ran a seam check parsing the emitted `.base` with PyYAML and cross-referencing every `basesEmbed('…')` call site against the view names actually in the file:
```
OK  #Members  ·  OK  #Communities  ·  OK  #Decisions  ·  OK  #Recent  ·  OK  #ProjectFeed
views emitted: Recent, Decisions, Bugs, Sessions, Communities, ProjectFeed, Members
```
Zero broken embeds. `Bugs` and `Sessions` are intentionally not embedded — they are reachable by opening `Memories.base` directly; embedding every view in Home would recreate the wall-of-notes problem the index layer exists to solve.

**Final gate, integrated tree (T1 + T2 + T3 all landed):** root `npm test` **1343 tests, 1338 pass, 0 fail, 5 skipped**. My acceptance harness **18/18** under `TZ=Pacific/Kiritimati`, including the R-1 pair (user-tuned `graph.json` byte-identical after a second export and un-unlinked), zero stale files across two runs, and `sort:` asserted absent.

**One item deliberately NOT done, ORCH ruling still open (non-blocking, does not gate my DONE):** the `.obsidian/core-plugins.json` write-if-missing from my 11:11 FINDING. `.base` files only render if the Bases core plugin is enabled, so on a fresh vault whether the dashboards appear depends on the reader's Obsidian defaults rather than on anything we emit. ~10 lines, same untracked/never-clobber path as `graph.json`. I did not land it because it writes the reader's plugin config — a larger consent surface than graph tuning, and outside P1-6 as briefed. Standing offer: minutes to land if ORCH rules yes; otherwise it belongs in BACKLOG. T4: this is a scoped omission with a recorded rationale, not an unfinished item.

### [T4-CODEX] FINAL-VERDICT 2026-08-02 11:26 ET — GREEN; final tree passes T4 fixture harness, live read-only acceptance (9,170 notes/40 hubs/31 MOCs/5,971 edge refs), and root `npm run test` 1,343/1,338/0/5; R-1/R-2/R-3 gates satisfied, no blockers; `ARCHITECTURE.md:287` backlog wording is non-blocking under R-1 out-of-scope atomicity
