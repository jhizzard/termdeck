# Sprint 34 — Project-tag regression fix — STATUS

Append-only. Format:
- `[YYYY-MM-DDTHH:MM:SSZ] [Tn] CLAIM <file>`
- `[Tn] FINDING — <kind>: <evidence>`
- `[Tn] FIX-PROPOSED — <description>`
- `[Tn] DONE — <one-line>`
- `[Tn] BLOCKED — <reason>`
- `[Tn] HANDOFF to <Tn> — <safe to proceed with X>`

Use `date -u +%Y-%m-%dT%H:%M:%SZ`.

---

- [2026-04-26T22:00:00Z] [T5/orchestrator] Sprint opened. v0.7.1 just shipped (commit 6c46725, npm @jhizzard/termdeck@0.7.1 + @jhizzard/termdeck-stack@0.3.1). Sprint 33 POSTMORTEM at docs/sprint-33-flashback-debug/POSTMORTEM.md is your input. T3's BROKEN-AT chopin-nashville finding is what this sprint fixes. PLANNING.md + four briefings written. Awaiting four parallel terminals.
- [2026-04-26T22:06:00Z] [T2] CLAIM scripts/migrate-chopin-nashville-tag.sql, docs/sprint-34-project-tag-fix/SQL-PLAN.md. Live SELECT-only probes against petvetbid via DATABASE_URL.
- [2026-04-26T22:06:17Z] [T3] CLAIM tests/project-tag-invariant.test.js (NEW), tests/flashback-e2e.test.js (extension only — preserve v0.7.1 PHASE-A instrumentation).
- [2026-04-26T22:09:00Z] [T2] FINDING — pre-flight counts (aggregate-only, content samples skipped per user permission denial; sample step deferred to Josh's manual run of the dry-run block):
  - total chopin-nashville rows: **1,165** (was 1,126 in Sprint 33 T3 audit; ~39 added by Rumen since)
  - global project distribution check: chopin-nashville=1,165 vs termdeck=68 vs pvb=1,599 vs chopin-scheduler=996 — confirms the regression shape (chopin-nashville is bloated, termdeck is underweight)
  - termdeck-family content match (broad keywords: termdeck|@jhizzard/|mnestra-bridge|resolveProjectName|xterm.js|node-pty|flashback|packages/server/src|packages/cli/src|packages/client/public): **352**
  - tight termdeck-only branch (`termdeck` substring): **162**
  - broad mnestra: 72; broad rumen: 140; pvb: 7; gorgias/claimguard: 4
  - source_file_path is NULL for all 1,165 chopin-nashville rows — path-based heuristic branch contributes ZERO. Rumen-synthesized rows do not carry `source_file_path`. Heuristic must rely on content keywords + (where available) `metadata->>'cwd'`.
  - rows_with_session_id: 1,159 of 1,165 (so source_session_id traceability works in principle, but there is no sessions table in Mnestra schema to join against — session_id is foreign-only to TermDeck's local SQLite)
- [T2] FINDING — narrative number "1,126 mis-tagged TermDeck rows" appears optimistic. Direct broad-keyword union catches 352. The remaining ~700 chopin-nashville rows are either (a) genuine festival/Chopin-in-Bohemia content, (b) pianist/competition references, or (c) Rumen-synthesized session summaries that mention TermDeck only obliquely and would be a coin-flip to reclassify. Heuristic stays conservative: only flip rows with strong content match.
- [2026-04-26T22:09:27Z] [T3] FINDING — running tests/project-tag-invariant.test.js against the LIVE corpus (pre-T2-backfill state) all four content-vs-tag invariants fail as expected, confirming the tests catch the bug they're written to pin:
  - termdeck identifiers → top tag is `chopin-nashville` (expected `termdeck`)
  - claimguard identifiers (gorgias-ticket-monitor / Unagi/) → top tag is `gorgias` (19/30) — NOT `claimguard`. Suggests the Sprint 21 T2 chopin-nashville→termdeck/etc. backfill never renamed `gorgias`→`claimguard` in memory_items, OR a current writer is still emitting `gorgias`. Flagging to T1 for awareness — out of v0.7.2 scope, but worth a note in T4's POSTMORTEM. Tests use `claimguard` as the canonical name to avoid baking the regression into test expectations.
  - mnestra identifiers (engram dir, @jhizzard/mnestra) → top tag is `chopin-nashville` (8/19) — same family of mis-tag as the termdeck case.
  - residual guard: 86% (113/131) of termdeck-identifier rows are still tagged chopin-nashville pre-backfill. Will drop below the 25% threshold once T2's UPDATE block runs.
- [2026-04-26T22:09:27Z] [T3] FINDING — skip behavior verified: with `DATABASE_URL=postgresql://nope:nope@127.0.0.1:1/nope` all 6 invariant tests skip cleanly with `pg connect/probe failed: ECONNREFUSED`. With `TERMDECK_BASE_URL=http://127.0.0.1:1` all 3 flashback-e2e tests skip cleanly with `server unreachable`. CI without a live stack stays green.
- [2026-04-26T22:09:27Z] [T3] DONE — tests/project-tag-invariant.test.js created (6 tests: 1 distribution guard + 4 content-vs-tag invariants for termdeck/pvb/claimguard/mnestra + 1 residual guard); tests/flashback-e2e.test.js extended with project-bound test (now 3 tests, v0.7.1 PHASE-A instrumentation preserved). Pre-flight queries `/api/ai/query` with `project='termdeck'` and skips with `needs-backfill` directive when zero matches, avoiding generic 8s timeouts. No version bumps, no commits — handing off to T4.
- [2026-04-26T22:09:27Z] [T3] HANDOFF to T4 — both test files committed-ready; CHANGELOG entry for v0.7.2 should mention `tests/project-tag-invariant.test.js` (regression pin) and the project-bound flashback-e2e extension. Suggested CHANGELOG narrative: "Regression tests now pin the project-tag invariant: termdeck-identifier content must be tagged termdeck, not chopin-nashville. Skips when no live DATABASE_URL."
- [2026-04-26T22:09:30Z] [T4] CLAIM CHANGELOG.md, packages/stack-installer/CHANGELOG.md, docs-site/src/content/docs/termdeck/changelog.md, docs/sprint-34-project-tag-fix/POSTMORTEM.md (Phase A skeletons; package.json bumps deferred to Phase B per brief).
- [2026-04-26T22:10:30Z] [T4] PHASE A DONE — v0.7.2 skeleton inserted into root CHANGELOG.md (above [0.7.1]) with HTML-comment placeholders for T1/T2/T3 DONE-summary fills + Notes/test-count/backfill-status. [0.7.2] and [0.7.1] compare-links added at file foot; [Unreleased] retargeted to v0.7.2...HEAD. packages/stack-installer/CHANGELOG.md gets a 0.3.2 audit-trail skeleton matching v0.3.1 voice (no compare-link convention there). POSTMORTEM.md skeleton mirrors Sprint 33 structure (What was reported / What was found per lane / Root cause / Fix shipped / Why it stayed silent / Regression defense / Timeline / Decision needed) with HTML-comment placeholders. Versions NOT bumped — package.json files untouched.
- [2026-04-26T22:10:30Z] [T4] FINDING — Sprint 33 oversight: docs-site/src/content/docs/termdeck/changelog.md was MISSING the v0.7.1 entry entirely (root CHANGELOG.md has it, but docs-site jumps from `[Unreleased]` straight to `[0.7.0]`; no v0.7.1 entry, no `[0.7.1]:` compare-link). Fixed in-place: mirrored a condensed v0.7.1 entry into docs-site (matches its tighter Astro-rendered voice) and added both `[0.7.1]:` and `[0.7.2]:` compare-links + retargeted `[Unreleased]:` → v0.7.2...HEAD. Slightly outside strict Sprint 34 lane but kept docs-site coherent. Flagging to orchestrator.
- [2026-04-26T22:10:30Z] [T4] STANDBY — Phase A complete. Have T3 DONE summary + T2 FINDINGs (no DONE yet) + T2's note that the 1,126 narrative is optimistic (broad-keyword union catches 352; heuristic stays conservative). Awaiting T1 DONE + T2 DONE before Phase B fills (placeholder content + version bumps to 0.7.2 / 0.3.2 / 0.3.2 + POSTMORTEM convergence). Will incorporate T3's claimguard/gorgias note as a "deferred follow-up" line in the POSTMORTEM regression-defense section.
- [2026-04-26T22:14:00Z] [T2] FIX-PROPOSED — `scripts/migrate-chopin-nashville-tag.sql` (NEW, ~210 LOC) + `docs/sprint-34-project-tag-fix/SQL-PLAN.md` (NEW). Three-block layout: Block 1 dry-run (zero mutations, runs always), Block 2 UPDATE (commented out, requires manual uncomment after orchestrator+Josh approval of Block 1 output), Block 3 REVERT (commented out, one-shot rollback via `metadata.rebrand_v0_7_2_from` stash). Multi-branch OR heuristic, priority-ordered: termdeck > mnestra > rumen > pvb > claimguard. Conservative-by-design: rows matching no branch stay chopin-nashville. Reversibility: every reclassified row carries its old project under `metadata.rebrand_v0_7_2_from` so Block 3 restores via a single statement. Path branch falls back on `metadata->>'cwd'` because `source_file_path IS NULL` for all 1,165 chopin-nashville rows. Estimated reclassification: ~340–352 rows to termdeck, ~5–20 mnestra, ~10–25 rumen, ~3–7 pvb, ~2–4 claimguard, ~750–800 stay. Out of scope (flagged): gorgias→claimguard, PVB case-dupe, engram→mnestra.
- [2026-04-26T22:14:30Z] [T2] BLOCKED on content-sample sanity check — user permission system denied row-content SELECTs into the transcript. Sample inspection (10 stays + 5 moves) is the second SELECT in Block 1 of the SQL file; Josh runs it himself before approving Block 2. Counts from aggregate-only probes are sufficient to ship the script; samples gate the EXECUTE decision, not the SHIP decision.
- [2026-04-26T22:14:45Z] [T2] DONE — SQL file + SQL-PLAN.md written; pre-flight counts posted; UPDATE not executed; reversibility built in.
- [2026-04-26T22:09:48Z] [T1] CLAIM packages/server/src/rag.js, packages/server/src/mnestra-bridge/index.js, tests/project-tag-resolution.test.js (NEW)
- [2026-04-26T22:11:00Z] [T1] FINDING — CONFIRMED-OK: `resolveProjectName(cwd, config)` in rag.js:19-41 implements longest-prefix-wins correctly. Sort by `resolved.length` desc, prefix match with explicit `path.sep` boundary so `/a/b` does NOT spuriously match `/a/bb`. Ground-truth probe against live `~/.termdeck/config.yaml` (15 projects):
  - `/Users/joshuaizzard/Documents/Graciella/ChopinNashville/SideHustles/TermDeck/termdeck` → `termdeck` ✓
  - `.../TermDeck/termdeck/packages/server` → `termdeck` ✓ (subdirectory of registered project)
  - `~/Documents/Graciella/ChopinNashville` (unmapped ancestor) → `ChopinNashville` (PascalCase basename), NOT `chopin-nashville`. The fallback never produces `chopin-nashville`.
- [2026-04-26T22:11:00Z] [T1] FINDING — CONFIRMED-OK: every TermDeck-side writer call site routes through `_projectFor(session)` which prefers explicit `session.meta.project` then falls back to `resolveProjectName(meta.cwd, config)`. All 5 hooks (onSessionCreated, onCommandExecuted, onStatusChanged, onSessionEnded, onFileEdited) at rag.js:106-147 use it. `mnestra-bridge.queryMnestra` at index.js:228-249 falls back via `resolveProjectName(ctxCwd, config)` when caller passes no project. No path-segment side-channel found.
- [2026-04-26T22:11:00Z] [T1] FINDING — CRITICAL: TermDeck's `rag.js` writer chain does NOT write to `memory_items`. It writes to legacy v0.1 tables (`mnestra_session_memory|project_memory|developer_memory|commands` per `config.rag.tables.*`). Flashback queries `memory_items` via `memory_hybrid_search`. The 1,165 chopin-nashville `memory_items` rows T2 just counted came from a writer OUTSIDE the TermDeck repo entirely. No fix to TermDeck-side code can heal them, and no regression in TermDeck-side code can produce them. Sprint 33 T3's "Sprint 21 T2 was supposed to fix this; the fix never landed in the writer (or regressed)" framing was off by one repo: the fix that landed in Sprint 21/22 (resolveProjectName + _projectFor wiring) is intact and correct; the writer that needs fixing is in `~/.claude/hooks/`, never was in TermDeck.
- [2026-04-26T22:11:00Z] [T1] FINDING — BROKEN-AT external writer: `~/.claude/hooks/memory-session-end.js:17` is the chopin-nashville source. `{ pattern: /ChopinNashville|ChopinInBohemia/i, project: 'chopin-nashville' }` — first-match-wins on cwd against a fixed array. Since TermDeck lives at `~/Documents/Graciella/ChopinNashville/SideHustles/TermDeck/termdeck`, EVERY Claude Code session-end inside any TermDeck panel pattern-matches `ChopinNashville` (no `termdeck` entry exists, no `SideHustles` precedence). The hook spawns `~/Documents/Graciella/rag-system/src/scripts/process-session.ts --project chopin-nashville <transcript>`, which ingests the full transcript into `memory_items` with that tag. Same root cause for the `gorgias`-vs-`claimguard` mis-tag T3 flagged (line ~25: `{ pattern: /gorgias/i, project: 'gorgias' }` with no `claimguard` mapping).
- [2026-04-26T22:11:00Z] [T1] FINDING — AMPLIFIER: Rumen's extract step at `~/Documents/Graciella/rumen/src/extract.ts:62` aggregates source memories via `(ARRAY_AGG(m.project))[1] AS project` — picks the FIRST project tag from each session group as the synthesis tag. Every 15 minutes, Rumen synthesizes new insights tagged with whatever the source memories had. So the harness hook's bad tag propagates into rumen_insights and (via Rumen's writeback) back into `memory_items` as synthesized rows. Rumen does not re-resolve from cwd; it inherits.
- [2026-04-26T22:11:00Z] [T1] FIX-PROPOSED — TermDeck-side defenses (in scope for v0.7.2):
  - Add `[rag] write project=<tag> source=<explicit|cwd|fallback> session=<id> event=<type>` log line in `rag.js:record()` — every legacy-table write becomes observable, future drift in `_projectFor` chain visible. ~6 LOC.
  - Add `[mnestra-bridge] query project=<tag> source=<explicit|cwd|none> mode=<direct|webhook|mcp>` audit line at the top of `queryMnestra` — every Flashback query becomes observable, including which resolution path filled the project slot. ~5 LOC.
  - NEW `tests/project-tag-resolution.test.js` — cases: leaf wins over ancestor, explicit `meta.project` beats cwd resolution, missing config falls back to basename, empty config returns null, regression pin (`.../ChopinNashville/SideHustles/TermDeck/termdeck` → `termdeck` not `chopin-nashville`), trailing-slash cwd handled, sibling path with shared prefix does NOT cross-match (boundary case for the `path.sep` guard), `_projectFor` honors explicit meta.project over cwd-derived value.
  - Total: ~11 LOC source + ~100 LOC test. Zero behavior change in the resolver itself; pure observability + regression lock.
- [2026-04-26T22:11:00Z] [T1] FLAG (out of v0.7.2 scope, blocking effectiveness):
  - **Claude harness hook** (`~/.claude/hooks/memory-session-end.js`): user-owned global config, NOT in any TermDeck/Mnestra/Rumen repo. Fix shape: insert `{ pattern: /SideHustles\/TermDeck\/termdeck/i, project: 'termdeck' }` BEFORE the ChopinNashville entry, and add equivalent leaf entries for engram/mnestra, rumen, podium, etc. Or: rewrite `detectProject` to longest-substring-wins over a project map keyed by canonical name. This is a one-paste fix Josh owns; flag for post-sprint follow-up. Same shape addresses T3's gorgias-vs-claimguard finding.
  - **Rumen extract.ts:62**: `(ARRAY_AGG(m.project))[1]` blindly inherits source tag. Could ship as `@jhizzard/rumen@0.4.4` with mode-of-source-projects or distinct-count guard. But fixing the harness hook first heals Rumen on next tick (synthesized rows pick up the correct upstream tag), so this is lower priority than the hook.
  - T2's backfill SQL is the only thing that heals the existing ~352 confidently-mis-tagged rows; the source-side fixes only stop the bleed.
- [2026-04-26T22:15:00Z] [T1] DONE — `packages/server/src/rag.js` (+22 LOC: new `_resolveProjectAttribution`, `_recordForSession`; refactor 5 hooks to route through it), `packages/server/src/mnestra-bridge/index.js` (+8 LOC: project-attribution audit log on every queryMnestra), `tests/project-tag-resolution.test.js` (NEW, 12 tests, all green). Smoke-test against live `~/.termdeck/config.yaml`: `_resolveProjectAttribution(termdeck cwd)` → `{ tag: 'termdeck', source: 'cwd' }`. Bridge constructor + queryMnestra signature unchanged externally; existing v0.7.1 tests (analyzer-error-fixtures, theme-persistence, init-mnestra-resume, etc.) all green. Pre-existing failing tests (flashback-e2e and project-tag-invariant) are T3's intentional pre-backfill regression catches and will pass after T2's UPDATE block executes — not my lane.
- [2026-04-26T22:15:00Z] [T1] HANDOFF to T4 — CHANGELOG entry for v0.7.2 should mention: (1) writer-side audit logs `[rag] write project=...` and `[mnestra-bridge] query project=...` now print resolution source on every event/query — future drift becomes observable in stdout; (2) `tests/project-tag-resolution.test.js` pins TermDeck-side resolver behavior so the chopin-nashville regression cannot return through this path; (3) the actual chopin-nashville source is `~/.claude/hooks/memory-session-end.js:17` (Josh-owned, out of repo) — POSTMORTEM should call this out so the v0.7.2 narrative doesn't overclaim a fix that lives downstream of a still-broken upstream writer. No version bumps, no commits — T4 integrates.
- [2026-04-26T22:17:00Z] [T4] PHASE B integration complete. Filled all four HTML-comment placeholders in root CHANGELOG.md from T1+T2+T3 DONE summaries; matched v0.7.1's voice (specific filenames, LOC counts: T1 +22 rag.js / +8 mnestra-bridge / 12 tests; T2 ~210 LOC SQL; T3 +13 test cases; pre-flight count 1,165 with the 352-broad-keyword honesty about the 1,126 narrative). Stack-installer CHANGELOG 0.3.2 entry filled to match the v0.3.1 narrative voice. docs-site v0.7.2 entry mirrored (condensed for the Astro-rendered tighter voice). Honest about scope: v0.7.2 ships TermDeck-side regression lock + observability + corpus backfill, NOT a source fix — the source is `~/.claude/hooks/memory-session-end.js:17` (Josh-owned, out of repo). Both CHANGELOG and POSTMORTEM call this out explicitly per T1's HANDOFF guidance.
- [2026-04-26T22:17:00Z] [T4] Version bumps applied: root `package.json` 0.7.1 → 0.7.2; `packages/cli/package.json` 0.3.1 → 0.3.2 (cli-internal, private); `packages/stack-installer/package.json` 0.3.1 → 0.3.2. `[Unreleased]:` retargeted to `v0.7.2...HEAD` in both root and docs-site changelogs. POSTMORTEM.md converged with all per-lane FINDINGs/DONE summaries, root cause (TermDeck not at fault; harness hook is the source; Rumen amplifies), regression defense matrix (in-scope vs out-of-scope), and timeline. Three orchestrator decisions surfaced: (1) run Block 2 vs defer; (2) fix the harness hook in same session; (3) portfolio bump (recommend skip).
- [2026-04-26T22:17:00Z] [T4] READY — v0.7.2 release artifacts staged: CHANGELOG.md, packages/stack-installer/CHANGELOG.md, docs-site/.../changelog.md, package.json (root + cli-internal + stack-installer), docs/sprint-34-project-tag-fix/POSTMORTEM.md. NO COMMIT, NO NPM PUBLISH per brief — orchestrator handles both. Suggested commit message subject: `v0.7.2: chopin-nashville corpus repair + TermDeck-side regression lock (Sprint 34)`. Suggested tag: `v0.7.2`. Per Josh's publishing constraint (passkey-in-browser, no automation token, no OTP app), npm publish for both `@jhizzard/termdeck@0.7.2` and `@jhizzard/termdeck-stack@0.3.2` requires Josh at the keyboard.
- [2026-04-26T22:32:00Z] [T5/orchestrator] EXECUTED Block 2 against petvetbid (Josh pre-approved 22:18Z; sample inspection of 5 termdeck-bound rows confirmed clean; 0 leftover-with-stash sanity row). Results:
  - termdeck:    363 reclassified
  - rumen:        16
  - claimguard:    5
  - mnestra:       5
  - pvb:           3
  - TOTAL:       392 reclassified, metadata.rebrand_v0_7_2_from stash on each (revert is one statement)
  - chopin-nashville remaining: 827 (real NICPC festival content; T3's invariant probe should now pass)
- [2026-04-26T22:32:00Z] [T5/orchestrator] HEADLINE for Sprint 35: T1's flagged finding — `~/.claude/hooks/memory-session-end.js:17` PROJECT_MAP iteration order — is the upstream root cause. Locked in global memory; Sprint 35 leads with this. Two more Sprint 35 candidates also locked: per-panel subject-summary feature (Josh requested), Rumen Insights badge counter not decrementing on modal mark-as-seen (Josh reported mid-Sprint-34).
