# Sprint 81 — STATUS

**Post shape (exact, all lanes):** `### [T<n>] <VERB> 2026-07-05 HH:MM ET — <gist>`
VERB ∈ `FINDING` · `FIX-PROPOSED` · `FIX-LANDED` · `BLOCKED` · `CHECKPOINT` · `DONE` (auditors also `AUDIT-PASS` / `AUDIT-FAIL`).
Example: `### [T1] DONE 2026-07-05 18:10 ET — 031 provenance + G2 wiring + 032 column landed; npm test 260/260`

**Decks:** A=:3001 (T1 engram · T2 rumen · T3 gates · T7 codex-a) · B=:3002 (T4 proof-surface · T5 proof-harness · T6 miser · T8 codex-b)
**Critical path:** T1:031→T4/T5 · T1:032-col→T2:THING-2 · T1:030+T3:hook→ORCH-index(close-out). Parked lanes do NOT auto-resume — ORCH nudges.

---
## T1 — engram DB chain (mnestra 0.9.0)

### [T1] FIX-LANDED 2026-07-05 17:22 ET — 031 recall-log provenance + G2 (R2 trusted-producer) landed; engram baseline GREEN (274 pass / 0 fail). **T4/T5 UNBLOCKED.**
**031 (`migrations/031_recall_provenance.sql`, file-only):** EXTENDS 027 (no fork). ADD nullable `source_type` / `token_budget` / `recall_group_id` to `memory_recall_log` (does NOT re-CREATE 027-owned `recall_count`/`last_recalled_at` — collision-guarded); `create index … memory_recall_log_group_idx (recall_group_id)` for the reinjection-event lookup; `log_recall_hits(jsonb)` recordset+INSERT extended to carry the 3 fields (batched denorm bump preserved verbatim); re-REVOKE/GRANT service_role-only; **OID-form** hard-failing receipt (proname→oid, no text-sig — dodges the Supabase named-args trap). `source_type` is per-hit, `token_budget` per-call, `recall_group_id` one-uuid-per-recall (generated in `recall_log.ts`).
**G2 (R2 — trusted producer, NOT tool args):** reverted the mcp-handler wiring; the read now lives at the single recall-log choke point — `src/recall_log.ts` resolves `source_session_id`/`source_agent` from explicit `log_*` (webhook wins) else falls back to `process.env.MNESTRA_SESSION_ID` / `MNESTRA_SOURCE_AGENT`. **This covers ALL FIVE surfaces** (recall/search/index/timeline/graph), closing T7's 17:14 finding — not just recall/search. Acceptance test proves env-set → non-null `source_session_id` written; explicit-ctx-wins + `${VAR}`-placeholder-ignored also tested. **@T4: you own the PRODUCER** — export `MNESTRA_SESSION_ID`=<panel session id> + `MNESTRA_SOURCE_AGENT`=<claude|codex|…> into the spawned agent's env at panel spawn (per R2); until then the everyday MCP path stays fail-soft-NULL (honest).
**R4:** fixed stale `tests/migration-028-hygiene.test.ts:76-90` → now expects the precompact index DEFERRED in 028 (+ documents the 030 arbiter-free redefine); the real index-present assertion moves to the 030 test. This was the sole pre-Sprint-81 engram red (T7 16:59). **Baseline now GREEN: 274 pass / 0 fail (was 256/1); `tsc --noEmit` clean.**
Write sites (`recall.ts`/`search.ts`/`layered.ts`) pass per-hit `source_type` (+ `tokenBudget` on recall); graph omits source_type (RPC doesn't return it → NULL). Fire-and-forget contract intact; `MNESTRA_DISABLE_RECALL_LOG=1` honored. **Next: 032 (R5 contract confirm below), 030 (R3 arbiter-free), receipt OID sweep.** ORCH applies 031 live at close-out.

### [T1] FIX-LANDED 2026-07-05 17:28 ET — 032 recall_boost landed (`migrations/032_recall_boost.sql`, file-only). **@T2: your `set_recall_boost` contract is CONFIRMED AS-IS — code against it, no divergence.** 283 pass / 0 fail.
**@T2 contract (R5) — implemented EXACTLY as you proposed (STATUS 17:01), do not adjust:**
- `set_recall_boost(p_updates jsonb)`, payload `[{"id":"<uuid>","boost":<numeric>}, ...]` — recordset `u(id uuid, boost numeric)`, ONE batched set-based UPDATE (never per-row).
- Server-side clamp `least(greatest(u.boost, 1.0), 2.0)` → **floor 1.0** (reinforcement only elevates; pruning moratorium honored) · **ceiling RECALL_BOOST_MAX = 2.0** (your requested shared constant — hardcoded identically in the RPC clamp AND the `memory_hybrid_search` factor; ping me to move it, don't fork).
- **Doctrine-clean confirmed:** touches ONLY `recall_boost`. I deliberately do NOT bump `updated_at` either (would conflate reinforcement with a content write + pollute `mnestra_capture_health.last_write`). Returns int rows updated. service_role-only, `SET search_path=public,pg_catalog`, REVOKE EXECUTE FROM public/anon/authenticated.
**Column:** `memory_items.recall_boost numeric NOT NULL DEFAULT 1.0` (non-volatile default → fast metadata-only add).
**Ranking wire-in (the "on" half of T5's A/B):** `memory_hybrid_search` CREATE OR REPLACE (identical 8-arg sig / 9-col RETURNS TABLE; overload-drop guard + REQUIRED re-pin since DROP loses grants) with `* (least(greatest(coalesce(recall_boost,1.0),1.0),2.0))::double precision`. **STRICT no-op at 1.0** — applying 032 changes ranking for ZERO rows until you populate, so it's safe to land before `reinforce.ts` is live. **@T5:** this is your recall_boost off-vs-on lever (all rows 1.0 = "off"; boosted rows = "on"). OID receipt, 5 gates. ORCH applies at close-out.


### [T1] FIX-LANDED 2026-07-05 17:42 ET — 030 precompact coupled unit (R3 arbiter-free) + receipt OID sweep. 292 pass / 0 fail.
**030 (`migrations/030_precompact_rolling.sql`, file-only):** (1) keep-NEWEST-per-session collapse of active `pre_compact_snapshot` dups (reversible: `is_active=false`+`superseded_by`, never DELETE) + hard-failing completeness guard. (2) **R3: `ingest_capture` CREATE OR REPLACE with an ARBITER-FREE pre_compact_snapshot branch** — `pg_advisory_xact_lock(hashtextextended('mnestra_precompact:'||session,0))` then SELECT-active → UPDATE-in-place else INSERT. **No `ON CONFLICT` on the deferred index** → no 42P10 when T3 switches the hook. The content_hash branch is **byte-identical to 028** (proven by `diff` — only the precompact branch + 2 now-unused declares changed). (3) The partial-unique index is left **COMMENTED** (§3) — ORCH creates it LAST as a pure integrity guard. OID receipt, 5 gates.
**Receipt OID sweep (026/027/028, file-only, receipt-ONLY):** the three text-sig `has_function_privilege(role, '<sig>', …)` receipt blocks now resolve the function OID first and call `has_function_privilege(role, v_oid, …)` (029 portable form; dodges the Supabase named-args trap). **Proven receipt-only:** every diff hunk sits inside a receipt `DO` block; removed lines are exclusively the old text-sig privilege/proconfig lookups; a `CREATE|INSERT|UPDATE|ALTER|CONSTRAINT|GRANT` scan of the diff is **empty** — zero DDL/backfill change. 027's `fn_sigs` list kept intact so its hygiene test is untouched; 026/028 tests already only assert the surviving `has_function_privilege('role'` substrings. **@T7:** all 6 migrations (026/027/028 swept + 029 + new 030/031/032) now use OID-form receipts.

### [T1] DONE 2026-07-05 17:42 ET — engram DB chain complete: 030/031/032 + G2(R2) + R3/R4/R5 all landed, file-only. **npm test 292 pass / 0 fail (baseline was 256/1); `tsc --noEmit` clean.**
**Shipped (all file-only — ORCH applies live SQL at close-out):**
- **031** recall-log provenance (`source_type`/`token_budget`/`recall_group_id` + group index + extended `log_recall_hits`) — the CENTERPIECE columns T4/T5 read.
- **G2 / R2** trusted-producer provenance at the `recall_log.ts` choke point (env `MNESTRA_SESSION_ID`/`MNESTRA_SOURCE_AGENT`, explicit-ctx-wins) — covers **all 5 recall surfaces**. **@T4 owes the PRODUCER** (export those env vars at panel spawn) — until then the everyday MCP path is honestly NULL.
- **032** `recall_boost` column + `set_recall_boost(jsonb)` (T2's confirmed contract, clamp [1.0,2.0]) + `memory_hybrid_search` factor (STRICT no-op at 1.0 — safe to apply pre-T2).
- **030** precompact collapse + arbiter-free `ingest_capture` (R3).
- **R4** stale 028-hygiene test fixed (the sole pre-sprint engram red). **Receipt OID sweep** across 026/027/028.
- Tests: `migration-030/031/032-hygiene.test.ts` (new) + recall-log provenance/env cases; +36 net tests.
**ORCH close-out checklist (locked apply order):**
1. Apply engram **030 → 031 → 032** (after existing 023–029). 032's ranking change is a no-op until T2 populates `recall_boost`.
2. 026/027/028 need NO live re-apply (receipt-only edits — matter on fresh-install/CI replay); re-applying is idempotent if you prefer.
3. Precompact index is created **LAST**, by ORCH, AFTER T3's hook is confirmed round-tripping through `ingest_capture` (exact SQL in `030 §3`/`§5a`): collapse (030) → arbiter-free RPC (030) → T3 hook switch → **ORCH `create unique index … memory_items_precompact_session_uidx`**.
4. Run each migration's commented post-apply verification + `get_advisors(type='security')` — expect ZERO new 0011/0013 lints.
No version bump / CHANGELOG / commit / publish done (per lane discipline). Staying available for T7 re-audit + any ORCH nudges.

## T2 — rumen improvement + recall-feedback (rumen 0.8.0)

### [T2] CHECKPOINT 2026-07-05 17:01 ET — booted; baseline green, plan locked, starting THING 1
Read global+repo CLAUDE, PLANNING §T2, STATUS, T2 charter; memory recall done. Repo mapped end-to-end (confidence.ts, synthesize.ts, relate.ts, index.ts, extract.ts, db.ts, types.ts, doctrine-scan/inbox-promote/rumen-tick edge fns, MNESTRA-COMPATIBILITY, CONTRIBUTING, fixtures). Baseline `npm run test` = **126 pass / 1 skipped / 0 fail** (matches T7 16:59). Confirmed the RRF-drown bug: `memory_hybrid_search` returns RRF `score` in the **0.01–0.3** band (documented `index.ts:97-101`), but `computeConfidence` (`synthesize.ts:579`) treats `maxSim` as 0–1 → `0.5*maxSim`≈0.05 typ., while the flat `0.3*crossProjectBonus` dominates ~6:1. **THING 1 (independent) starting now**; THING 2 parks on T1's 032 contract (see FINDING below).

### [T2] FINDING 2026-07-05 17:01 ET — set_recall_boost RPC contract I need from T1's mig 032 (please confirm/adjust)
THING 2's `reinforce.ts` writes ONLY `memory_items.recall_boost` via your service-role RPC. Proposed shape (mirrors your `log_recall_hits(jsonb)` batch idiom so it's one RLS surface, service_role-only, `SET search_path=public,pg_catalog`, REVOKE EXECUTE FROM PUBLIC):
```sql
-- set_recall_boost(p_updates jsonb) -- [{"id":"<uuid>","boost":<numeric>}, ...]
-- clamps each boost server-side to [1.0, RECALL_BOOST_MAX] (default 2.0); returns int rows updated.
-- ONLY touches recall_boost; never content/embedding/any other column (doctrine-clean).
```
Requests for your 032: (a) column `memory_items.recall_boost numeric NOT NULL DEFAULT 1.0` (bounded multiplier, strict no-op at 1.0) — as PLANNING §T1.2; (b) server-side clamp floor **1.0** (reinforcement only ELEVATES; never demote below 1.0 — honors the pruning moratorium, a never-recalled memory stays 1.0); (c) ceiling constant exposed so my caller and your `memory_hybrid_search` wire-in agree (propose 2.0). I'll code `reinforce.ts` against this shape now and adjust if you diverge. If you'd rather I write via `execute_sql`-style parameterized UPDATE instead of an RPC, say so — RPC is cleaner for the single-writer + clamp guarantee.

### [T2] FIX-LANDED 2026-07-05 17:12 ET — THING 1 synthesis-quality pass complete (file-only); npm test 140 pass / 1 skipped / 0 fail
All three THING-1 charter items landed in `src/confidence.ts` + `src/synthesize.ts` + `tests/synthesize.test.ts` (typecheck clean, +14 net tests over 126 baseline):
- **RRF-band recalibration** — new `normalizeSimilarity()` in `confidence.ts` maps the 0.01–0.3 RRF band onto 0..1 (floor→0, ceiling→1, linear between, saturates + NaN-safe). `computeConfidence` (`synthesize.ts`) now = `(0.55*simScore + 0.30*crossProject + 0.15*ageSpread) * noveltyFactor`. Concrete fix proven by a test: a strong same-project match (0.55) now OUTRANKS a weak cross-project one (~0.32); pre-v2 the weak cross-project (0.31) beat the strong (0.15) — the drown bug. `NORMALIZE_VERSION` 1→2 (cosmetic; nothing consumes it, honest calibration stamp).
- **Down-rank near-duplicate prior art** — new exported `noveltyFactor()`: union-find clusters related memories by normalized-content equality OR token-Jaccard ≥ 0.85 (n≤TOP_K=5, trivial cost); scales composite by distinct-cluster ratio, bounded `[0.5, 1]` (never zeroes a real match). Identical-content cluster of 2 → 0.638 vs 0.85 distinct.
- **buildUserPrompt enrichment** — now exported + `nowMs`-injectable (deterministic under test); adds per-memory `age=Nd`, a per-signal `cross-project spread: N projects (…) across M memories`, and a `recency: newest Xd ago, oldest Yd ago` line; SYSTEM_PROMPT nudged to use recency/breadth. Pure prompt-content change, no output-shape change.
Doctrine-clean: THING 1 touches only Rumen's synthesis math + prompt; zero DB writes, zero new deps. **THING 2 next — coding `reinforce.ts` against the proposed `set_recall_boost` contract above (offline-unit-testable via mock pool); still want T1 confirm on the RPC shape.**

### [T2] FIX-LANDED 2026-07-05 17:45 ET — THING 2 recall-feedback loop complete (file-only); full npm test 156 pass / 1 skipped / 0 fail
**@T1 — your 17:28 CONFIRM matched my proposal exactly, ZERO code adjustment needed.** `reinforce.ts` sends `[{"id","boost"}]` to `set_recall_boost($1::jsonb)` and reads back the int count; floor 1.0 / ceiling 2.0 line up. Landed:
- **`src/reinforce.ts`** (new) — `runRumenReinforce(pool, opts)` + pure `computeRecallBoost(input, tunables)`. Reads the DURABLE denorm (`recall_count`/`last_recalled_at`, since raw log purges at 90d) for candidates + a windowed `memory_recall_log` cited/surfaced tally, computes a bounded weight, writes changed rows via ONE `set_recall_boost` call. Weight = `EWMA(prev, target)`, `target = 1 + (2-1)·usage·recency`; **usage saturates** (`1-e^-x`, cited weighted 3× surfaced, older-than-window recalls a discounted floor) and **recency decays** (30d half-life). Bounded `[1.0, 2.0]`, **strict no-op at 1.0** → never-recalled memories untouched (pruning moratorium). All three T7 guards met: doctrine-clean (only `recall_boost`, via the column-scoped RPC — never a raw `UPDATE memory_items`), no rich-get-richer (bound + saturation + decay), builds on `cited` not `acted_upon`.
- **`supabase/functions/rumen-reinforce/`** (new) — thin-wrapper edge fn mirroring doctrine-scan (npm-pin `@0.8.0`, watchdog, DATABASE_URL fallback, fail-soft). No model key. `RUMEN_REINFORCE_DRY_RUN=1` supported (complements T1's ranking wire-in as T5's off-vs-on lever).
- **`tests/reinforce.test.ts`** (new, 16 tests) — pure fn (bound, no-op floor, cited>surfaced, saturation, recency decay, relax-down-not-below-floor, Infinity/NaN safety) + `runRumenReinforce` mock-pool wiring (scan→tally→compute→write, dry-run, no-op skip, fail-soft, batch-saturation, payload clamp).
- **`tests/fixtures/mnestra-minimal.sql`** — added `recall_count`/`last_recalled_at`/`recall_boost` cols, `memory_recall_log` table, a `set_recall_boost` stand-in (mirrors your set-based `jsonb_to_recordset` shape), + cited/surfaced telemetry seed.
- **Docs** — `MNESTRA-COMPATIBILITY.md § What Rumen writes` 4th write surface; `CONTRIBUTING.md` ground rule 1 + log tag; `README.md` deploy section + log table.
Two self-caught bugs fixed pre-DONE: (1) budget deadline measured off injected `nowMs` → spurious skip; now real wall-clock. (2) `finiteOr(Infinity,0)` made a NULL `last_recalled_at` read as "fresh"; now Infinity passes through → recency 0.

### [T2] DONE 2026-07-05 17:45 ET — rumen lane complete (THING 1 + THING 2), file-only
Both charter items green (`npm test` 156 pass / 1 skipped / 0 fail, `tsc --noEmit` clean). No version bump / CHANGELOG / commit / publish (ORCH: rumen 0.7.0→0.8.0). **ORCH close-out for this lane:** (a) bump rumen 0.8.0 + CHANGELOG; (b) `rumen-reinforce` edge fn pins `npm:@jhizzard/rumen@0.8.0` — deployable only after 0.8.0 publish + engram 032 live (`recall_boost` col + `set_recall_boost` RPC); (c) optional pg_cron schedule for `rumen-reinforce` is a follow-on (T3/ORCH-owned like doctrine-scan's) — manually invocable meanwhile; (d) fixture SQL validated by CI integration-test job (live SQL deferred per lane rule). Available for any T7 finding.

### [T2] FIX-LANDED 2026-07-05 18:51 ET — T7 novelty-floor doc/formula mismatch corrected (comment-only); typecheck clean, synthesize 45/45
**Agreed with T7's finding and took the RECOMMENDED low-risk path** — the comments, not the formula, were wrong. `noveltyFactor` returns `0.5 + 0.5*(distinct/N)`, so an all-duplicate cluster of N gets `0.5 + 0.5/N` (0.75 at N=2, 0.667 at N=3, → 0.5 only as N→∞) — an ASYMPTOTIC floor, not a hard one. Formula + test (`0.5+0.5*(1/3)` for 3 identical, `tests/synthesize.test.ts`) were already internally consistent, and the softer penalty for small duplicate clusters is a defensible/intended design, so I did NOT switch to `(distinct-1)/(N-1)`. Corrected `src/synthesize.ts` at the `NOVELTY_FLOOR` const comment (~:694), the `noveltyFactor` JSDoc (~:748, now states `0.5 + 0.5/N` and range `(NOVELTY_FLOOR, 1]`), and the module-doc formula block (`(NOVELTY_FLOOR, 1]` + asymptotic bullet). Zero behavior change. **This closes my lane's last item — T2 fully green.**

## T3 — advise→gate + pre-compact hook switch (termdeck 1.14.0 / stack 1.12.0)

### [T3] CHECKPOINT 2026-07-05 17:08 ET — recon complete, design locked (integrating T7 17:04 + 16:58), starting build
Read: charter §T3, INSTALLER-PITFALLS (10-class taxonomy), doctrine/index.js+SCHEMA.md+registry.jsonl, doctrine-cli.js `cmdPromote`, engram `028_capture_gates.sql` `ingest_capture(jsonb)` contract, PreCompact hook, installer hook-trio + init-mnestra refresh twins, verified Claude Code PreToolUse deny contract.

**(A) 2 PreToolUse deny gates** — net-new bundled hooks, **self-contained/fail-soft, INLINE (not shared-lib)** so a bug in one can't break the other:
- `gate-publish-before-push.js` → rule **`publish-before-push`**. Trigger `git push`; fast-allow unless HEAD on default branch (main/master), then deny if any local `@jhizzard/*` version is strictly ahead of `npm view <name> version`. `npm view` fail / non-main / uncertainty → ALLOW.
- `gate-migration-without-rls.js` → rule **`rls-five-gates`**. Trigger `git commit`; fast-allow unless staged (`--cached`) migration `*.sql` exists; deny if a staged blob has `create table [public.]X` lacking `enable row level security` for X in the same file. Any parse difficulty → ALLOW.
- **Deny protocol (verified):** exit 0 + stdout `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"…"}}`. Fail-soft = exit 0, **no** output. NEVER exit 2.
- **Registry-driven / inert-until-promoted:** each gate reads a registry file (`$TERMDECK_DOCTRINE_REGISTRY` → `~/.claude/doctrine/registry.shipped.jsonl` → source-tree `doctrine/registry.jsonl` → else allow) and enforces ONLY when its rule is `status:active` + `surface:preToolUse-deny` + `max_severity:block`. Both entries are `inject-advisory`/`warn` today → gates ship as safe no-ops until ORCH `doctrine promote`s. Reversible per-rule kill-switch.
- Installer: mirror PreCompact trio (`installPreToolUseHook`/`_mergePreToolUseHookEntry`/`_isPreToolUseHookEntry`, matcher `Bash`, both commands) + main-install wire + init-mnestra refresh set + `runSettingsJsonMigration`.

**(B) pre-compact hook → `ingest_capture`** — **primary** `POST /rest/v1/rpc/ingest_capture` (028 RPC; payload content/embedding/source_type/category/project/source_session_id/source_agent/metadata). **Graceful fallback to the proven raw append `POST /memory_items` on any non-clear-success.** Rationale (integrates T7 17:04 + 16:58): (i) 028's precompact `ON CONFLICT` needs the 030 arbiter index — switch-before-index → ingest_capture 42P10s; (ii) TermDeck bundles mnestra migrations only to 022 (T7 16:58), so `ingest_capture` may be undeployed (PGRST202). Fallback makes the hook **never worse than baseline** and **correct once 030 lands**, regardless of ORCH index-vs-refresh order. Fallback fires only when NOT (HTTP 2xx AND body.ok===true) → no double-write on success. `source_session_id` already non-null (guard `:221`). Bump hook stamp **v2→v3** (FILE already in init-mnestra refresh set `:1111`; the bump propagates the switch).

**⚠ Coordination:**
- **@T7 (re 17:04):** acknowledged — fallback closes the capture-loss window from BOTH directions; DONE will include a round-trip test proving both with-arbiter (rolling) and no-arbiter (append-fallback) paths. Agree "URL change alone ≠ DONE."
- **@T1/ORCH (ordering):** correct close-out = collapse → CREATE INDEX → refresh hook; fallback tolerates either order, index-before-refresh cleanest.
- **@ORCH (promote must reach the gate):** no in-repo bake script for `packages/stack-installer/assets/doctrine/registry.shipped.jsonl` (hand-maintained; it's what the vendored gate reads). `doctrine promote` edits only canonical `doctrine/registry.jsonl` → promote must ALSO update the shipped copy (+ re-vendor) or gates ship inert.

### [T3] DONE 2026-07-05 17:53 ET — (A) 2 PreToolUse gates + (B) hook→ingest_capture + (R1) migration-bundle-sync FULLY landed, file-only. 48/48 T3 tests; stack-installer 92/92, cli 174/174; gitleaks clean.
**(A) advise→gate — 2 PreToolUse deny gates (net-new, self-contained, fail-open):**
- `packages/stack-installer/assets/hooks/gate-publish-before-push.js` (rule `publish-before-push`) + `gate-migration-without-rls.js` (rule `rls-five-gates`). Deny = exit 0 + `{hookSpecificOutput:{…permissionDecision:'deny'…}}`; ALLOW = exit 0 no-output; **NEVER exit 2**. Registry-driven: enforce ONLY when the rule is `active`+`preToolUse-deny`+`block` (read `$TERMDECK_DOCTRINE_REGISTRY` → `~/.claude/doctrine/registry.shipped.jsonl` → source-tree). Inert today (both entries advisory) → **ship-safe no-ops until ORCH promotes**.
- Installer trio in `stack-installer/src/index.js`: `installPreToolUseHook`/`_mergePreToolUseHookEntry`/`_isPreToolUseHookEntry` (matcher `Bash`, both commands, idempotent+partial-add+tilde-migrate), wired into main install. **Class-N twin** hoisted into `cli/src/init-mnestra.js` (`migrateSettingsJsonPreToolUseEntry` + refresh-set loop + `runSettingsJsonMigration` wiring) — twins proven **byte-identical** by test.
- Caught+fixed a real P0-class bug pre-audit: `isGitPush`/`isGitCommit` matched `push`/`commit` as ANY token → `git commit -m "push"` would've been read as a push (false-deny a commit on main). Tightened to subcommand-position (allowing git global options).

**(B) pre-compact hook → `ingest_capture` (v2→v3):** PRIMARY `POST /rest/v1/rpc/ingest_capture` (`{p_payload:{content,embedding,source_type:'pre_compact_snapshot',source_session_id,source_agent,category,project,metadata}}`) + **transition-safe fallback** to the raw `/memory_items` append on any non-clear-success (fires only when NOT (2xx AND body.ok===true) → no double-write). Shared `buildCapturePayload` → primary/fallback rows byte-identical. **R3 satisfied:** verified round-trip against T1's redefined **arbiter-free** 030 `ingest_capture` (same payload + `{ok:true}` contract T1 shipped 17:42) — unit fences emulate success + the full failure matrix (42P10 no-arbiter, PGRST202 undeployed, {ok:false}, network-throw → fallback; both-fail → insert-failed). Rewrote `pre-compact-hook.test.js` (was silently exercising only the fallback via the mock's throw).

**(R1) migration-bundle-sync (T7 16:58 / ORCH R1) — CONFIRMED FIXED:** `listMnestraMigrations` is bundled-FIRST and the bundle stalled at **022**, shadowing engram **023–032** from `termdeck init --mnestra`. Synced **023–032 byte-identical** into `packages/server/src/setup/mnestra-migrations/` (retroactive 023–029 gap + Sprint-81 030–032, done AFTER T1's 17:42 DONE) → `listMnestraMigrations` now returns **001–032**. Added 10 `MIGRATION_PROBES` entries (025 null=comment-only; 029/030 body-discriminating probes). Idempotent `scripts/sync-mnestra-migrations.js` (dev-only, NOT in tarball) + drift fence `mnestra-migration-bundle-drift.test.js` (CI-safe self-consistency always; engram byte-parity for 023+ when the sibling repo is present). Found the 001–022 range legitimately diverges (bundle carries TermDeck-only 008/011 engram never had) → scoped the mirror contract to 023+.

**INSTALLER-PITFALLS trace:** gates avoid **E** (zero private-path dep — os.homedir()+PATH only) & **N** (self-contained inline, not shared-lib; installer/cli twins byte-identical; hook-file + settings.json wired together in BOTH install paths). Hook switch avoids **M** (write-path is a strict superset of the old — fallback guarantees no capture-loss) & **A/O** (tolerates undeployed/arbiter-missing RPC). R1 fixes **A** (schema-drift shadowing) + **H** (runner reads the complete set; verified `npm pack` ships both gates + all synced migrations, excludes the dev script).

**@ORCH close-out (2 items):** (1) after `doctrine promote publish-before-push` + `doctrine promote rls-five-gates`, ALSO update `assets/doctrine/registry.shipped.jsonl` + re-vendor, or the gates ship inert (no in-repo bake). (2) R1 is COMPLETE through 032 — no pending migration sync; if any engram migration lands post-DONE, run `node scripts/sync-mnestra-migrations.js` + bump `BUNDLE_MAX`. No version bump / CHANGELOG / commit / publish (ORCH). Available for T7 re-audit.

### [T3] FIX-LANDED 2026-07-05 18:50 ET — T7 AUDIT-FAIL resolved: publish gate now ALLOWS the non-writing `git push --dry-run` / `--help` probes.
**Bug (real):** `gate-publish-before-push.js::isGitPush` matched `push` as the subcommand but did NOT exempt `--dry-run`/`--help` — so with the rule promoted + an ahead version on main, `git push --dry-run` (a preview that writes nothing) would have been DENIED, blocking a user from inspecting what would push. The sibling `isGitCommit` already exempted them; the two were asymmetric.
**Fix:** mirrored `isGitCommit` exactly — `isGitPush` now returns false for any segment carrying `--dry-run` or `--help` (after the subcommand match). Header comment documents the parity.
**Tests:** added pure-helper assertions (`git push --dry-run` / `git push origin main --dry-run` / `git push --help` → false) + a new end-to-end subprocess fence proving all three ALLOW **even under the exact DENY conditions** (main + `@jhizzard` version 9.9.9 > npm 1.0.0 + promoted rule + fake npm). Behavior suite now **14/14** (was 13). Full T3 set green; gitleaks clean. File-only. **@T7:** ready for re-verify — this was the last open item.

## T4 — memory-proof surface + doctrine render/checks

### [T4] CHECKPOINT 2026-07-05 17:03 ET — booted, anchors verified, starting Part 1 (render.js extraction)
- Boot done: memory_recall ×2, both CLAUDE.md, PLANNING §T4, this STATUS, T4 briefing, ULTRAPLAN §6 L200. Baseline I must preserve = T8's 16:58 **36/36** (`doctrine-registry-shape.test.js` + `doctrine-cli.test.js`).
- **Anchors verified (match T8 16:57/16:58):** `renderDoctrineMarkdown` (`doctrine-sync.js:173`) + `buildRegistryEntry` (`:235`). Importers = `doctrine-sync.js` (defs+exports) + `doctrine-cli.js` (`:210/:228/:341/:413` reach `docSync.{branchNameFor,shortId,registryEntryIdFor}`). Tests via `docSync`: `doctrine-registry-shape.test.js`, `packages/cli/tests/doctrine-cli.test.js:57`. Client `app.js` `shortId`/`slugify` are LOCAL + unrelated.
- **Extraction boundary (locked):** move the coherent pure closure — `slugify,truncate,shortId,branchNameFor,docRelPathFor,registryEntryIdFor,renderDoctrineMarkdown,buildRegistryEntry` — into zero-dep `doctrine/render.js`. `doctrine-sync.js` requires + **re-exports all 8** (every existing `docSync.*` caller keeps resolving → verbatim move, zero behavior change). `screenableFromRow` STAYS in doctrine-sync (scrub-prep, not render).
- **⚠ FINDING (packaging, tarball-class — flag for ORCH close-out):** `package.json` `files[]` lists `doctrine/index.js|registry.jsonl|SCHEMA.md` **individually — no `doctrine/**` glob**. New `doctrine/render.js` won't ship, so `require('../../../doctrine/render')` breaks in the *installed* `@jhizzard/termdeck`. FIX in this lane: add `doctrine/render.js` + `doctrine/checks.js` to `files[]` (not a version/CHANGELOG/commit action). Same trap Sprint 78 T1 dodged by listing index.js/registry/SCHEMA explicitly.
- Part 2 (proof surface) parks on T1's `031` FIX-LANDED; scaffolding the fail-soft route/UI meanwhile so parking is minimal.

### [T4] FIX-LANDED 2026-07-05 17:27 ET — Part 1 (render.js + checks) AND R2 (trusted provenance producer) both landed; full server+cli **747/747**, doctrine suite **77/77**, spawn suite **12/12**
**PART 1 — doctrine render/checks (file-only):**
- **`doctrine/render.js`** (NEW, zero-dep CJS): verbatim move of the pure closure `slugify,truncate,shortId,branchNameFor,docRelPathFor,registryEntryIdFor,renderDoctrineMarkdown,buildRegistryEntry`. `doctrine-sync.js` requires + **re-exports all 7 prior exports** — a test asserts `docSync.<fn> === render.<fn>` (identity, proving a pure move not a fork). `screenableFromRow` stayed in doctrine-sync (scrub-prep). `doctrine-cli.js` now imports `branchNameFor/shortId/registryEntryIdFor` from `render.js` directly (both importers updated per charter).
- **`package.json` `files[]`** — added `doctrine/render.js` + `doctrine/checks.js` (tarball-class trap; individual-listing, no glob). **`npm pack --dry-run` verified:** render.js (6.5kB) + checks.js (8.1kB) both ship; no local-overlay leak.
- **`doctrine/checks.js`** (NEW, zero-dep, fail-soft, never-throws): starter checks `frontmatter-present` + `one-principle-shape` + `parseFrontmatter`. `runChecks()` → `{ok,results,deferred}`; **deferred** honestly names the remaining 13-battery (ULTRAPLAN §6 L200) so coverage can't lie by omission. `SCHEMA.md` gained the doctrine-doc front-matter contract section (the "retrofit"). New `doctrine-checks.test.js` (10 tests, canonical glob) + render-extraction identity guard added to `doctrine-registry-shape.test.js`.

**R2 — trusted recall-provenance PRODUCER (my ORCH-assigned enabler; @T1 your 031 reader now has its producer):**
- `spawnTerminalSession` pty-env build now exports **`MNESTRA_SESSION_ID=session.id`** + **`MNESTRA_SOURCE_AGENT=<adapter.sourceAgent||adapter.name>`** into every panel's env, alongside the existing `TERMDECK_SESSION`. The mnestra MCP server (spawned by the agent) inherits it → your `recall_log.ts` env-fallback writes non-null `source_session_id`/`source_agent`. Unspoofable (TermDeck sets it; an agent can't forge another panel's id). Session-id set for every panel; source-agent omitted (never blank) for a bare shell.
- **Adopted T7 17:22 fix:** switched `.name` → **`sourceAgent || name`** (established convention at index.js:350/:418) so aliased adapters stamp canonical agents (web-chat-grok → `grok-web`). Good catch, T7.
- Tests (real server + captured pty env): claude→`claude`, codex→`codex`, both `MNESTRA_SESSION_ID=<panel id>`; bare shell → session-id only; convention-lock proves alias precedence. **12/12** in `adapter-spawn-shell-wrap.test.js`.
- **@T8 to adversarially verify:** codex's mnestra MCP server env-inheritance. Claude Code stdio MCP inherits parent env (standard) → works. Codex's MCP config is a **static** `~/.codex/config.toml` that structurally CANNOT carry a per-session id → env inheritance is the ONLY viable mechanism; please confirm codex passes parent env to its stdio MCP servers (if not, codex provenance is a documented follow-up, not fixable via config.toml).

**Now starting Part 2 (proof surface) — UNBLOCKED by 031.** `GET /api/recall-events` reading extended `memory_recall_log` (fail-soft empty) + `renderMemoryTab` extension (score, doctrine chip, recall_group_id grouping, session backlink).

### [T4] DONE 2026-07-05 17:45 ET — Part 2 (memory-proof surface) landed; lane COMPLETE. Full server+cli **764/764**, recall-events suite **11/11**.
**Server — `GET /api/recall-events` + `/api/recall-events/:sessionId` (`index.js`, after the flashback routes):**
- Reads `public.memory_recall_log` (027 base + your 031 provenance cols) via the daily-driver pg pool (`getRumenPool()` — same DATABASE_URL that holds `memory_items`/`rumen_insights`), LEFT JOIN `memory_items` for project + 160-char content preview, grouped into **reinjection events** by `recall_group_id` (one recall_group_id = one recall call = the K hits reinjected together). Each event carries `sourceSessionId` (which panel), `sourceAgent`, `tokenBudget`, `sourceTypeMix`, `hasDoctrine`, and rank-sorted hits. Per-session variant filters `where source_session_id = $1`.
- **FAIL-SOFT EMPTY (charter contract):** no DATABASE_URL / missing table+cols (031 not yet applied live) / any query error → HTTP **200 `{events:[], degraded:'…'}`**, so the panel NEVER breaks. Reads real columns once ORCH applies 031 + panels recall with the R2 producer. Read-only; zero live writes from this lane.
- Grouping logic is a **pure, dep-free `packages/server/src/recall-events.js::groupRecallEvents`** — 10 exhaustive unit tests (grouping, source_type_mix, hasDoctrine, NULL-group singletons, rank sort, partial-row per-call fields, maxEvents cap, garbage-input never-throws) + 1 **behavior fail-soft route test** (createServer, no DATABASE_URL → 200 empty). **11/11.**
**Client — `renderMemoryTab` extension (`app.js` + `style.css`):**
- Extracted `sourceTypeChip()` with a **📜 doctrine highlight** (purple accent) + `renderMemoryHitRow()`; restructured the tab into live-hits + a durable **reinjection-events** section (`loadReinjectionEvents`/`renderReinjectionEvents`) fetched from `/api/recall-events/:sessionId` — grouped by `recall_group_id`, showing source_agent (the consuming panel, id-chip), token budget, source_type mix with doctrine chips, RRF score (labeled RRF per T2), and per-hit project/preview. Fully fail-soft (empty/error/degraded → blank section, live hits untouched), 10s-TTL cached so WS re-renders don't spam the endpoint. End-to-end wiring: R2 sets `MNESTRA_SESSION_ID=session.id` → your `recall_log.ts` writes `source_session_id=session.id` → this tab filters on it → a panel shows ITS OWN recalls.
- Matching CSS added to `style.css` using existing `--tg-*` vars (no new dependency). `node --check` clean; no fn-name collisions.
**Honest caveats for T8:** (a) the client render funcs (`app.js`) have **no unit test** — the repo has no jsdom/app.js harness; syntax-checked + reviewed, needs a browser eyeball (drive a panel that recalls after 031 is live). (b) Happy-path pg read is verified by ORCH live at close-out (lane is file-only). (c) A cross-panel aggregate view could consume the no-filter `/api/recall-events` as a follow-on — the data is already exposed.
**Files (mine only):** NEW `doctrine/render.js`, `doctrine/checks.js`, `packages/server/src/recall-events.js`, `packages/server/tests/{doctrine-checks,recall-events}.test.js`; M `package.json`, `doctrine/SCHEMA.md`, `packages/cli/src/doctrine-cli.js`, `packages/server/src/{doctrine-sync,index}.js`, `packages/client/public/{app.js,style.css}`, `packages/server/tests/{doctrine-registry-shape,adapter-spawn-shell-wrap}.test.js`. gitleaks-clean (no internal project name/refs). No version bumps / CHANGELOG / commits — ORCH close-out.

## T5 — cold-vs-warm harness + web-write runbook

### [T5] CHECKPOINT 2026-07-05 17:05 ET — booted, grounded, building independent scaffold
- Grounded on the recall path: `engram/src/recall.ts` (`estimateTokens = ceil(len/4)` at `:49`; `RecallOutput={hits,tokens_used,text}`; warm reinjection block = `memoryRecall(query).text`), `recall_log.ts`, migration `027_recall_telemetry.sql` (the `memory_recall_log` schema T1 extends in 031). Runbook machinery mapped: bridge `memory_propose` (`TERMDECK_BRIDGE_ENABLE_PROPOSE=1`, default-off) → `memory_inbox` → `inbox-promote` Edge Fn (cron via `005`-style `net.http_post`).
- **Building now (independent):** `scripts/proof/` cold-vs-warm harness — pluggable recall adapter (`fixture` done+tested / `http` for ORCH live) + answerer adapter (`stub` / `claude` / `cmd:` so T8 can reproduce with Codex) + mechanical metrics (rows K, tokens T via exact `ceil(len/4)`, `source_type` mix, provenance) + `factKey` verdict + report (md+json). Honesty guardrails: frozen probe set, run-all-report-all, threats-to-validity section. Tests land in `packages/server/tests/` (npm-test glob). `scripts/` isn't in `files[]` → harness stays repo-resident (like `demo.js`), correct for a proof tool.
- **Integrated cross-lane findings:** (a) re T7 17:02 G2 trap — harness supplies EXPLICIT caller provenance (`proof-harness` session/agent) to prove the plumbing, and honestly caveats that the everyday MCP-stdio panel path stays NULL until T1 lands a trusted producer; (b) re T2 17:01 — report labels `score` as **RRF (0.01–0.3 band)**, not "similarity."
- **Parked on:** T1 `031` (recall-ON provenance) + `032` (recall_boost A/B). Coordinating report consumption with T4's `/api/recall-events`.

### [T5] DONE 2026-07-05 17:30 ET — cold-vs-warm harness + web-write runbook landed (file-only); 21/21 tests; unblocked by T1 031, producer landed by T4 17:27
**Harness (`scripts/proof/`, repo-resident — `scripts/` not in `files[]`, like `demo.js`):** `cold-vs-warm.js` CLI + `lib/{tokens,metrics,recall-adapter,answerer-adapter,runner,report}.js` + 6 frozen probes (`fixtures/probes.json` + `probes.lock`) + generated recall fixtures (`fixtures/build-recall-fixtures.js`) + committed `reports/SAMPLE-plumbing-demo.md`. Tests: `packages/server/tests/proof-cold-vs-warm.test.js` — **21/21 under `npm test`**. `node --check` clean; docs-lint green; zero forbidden strings.
- **What it measures (mechanical + honest):** per probe, COLD (no reinjection) vs WARM (memory_recall reinjected) holding system+task IDENTICAL (only the memory block differs, `runner.js::composePrompt`); reports rows K, tokens T via the EXACT `ceil(len/4)` from `recall.ts:49` (a test re-derives every fixture's `tokens_used` through the same formatter so fixtures can't be fudged or drift), `source_type` mix, provenance (`recall_group_id`/`source_session_id`/`source_agent` — matches 031's shape), and a verdict from a memory-resident `factKey` grep. Emits Markdown (the "session cold-started, recalled N rows / T tokens, here's how the answer changed" narrative) + JSON.
- **Anti-rig guardrails (built for T8):** frozen+locked probe set chosen A-PRIORI with documented criteria (`fixtures/README.md`), including a deliberate `rrf-meaning` **no-delta control**; **RUN-ALL, REPORT-ALL** (no-delta/cold-wins/error bucketed, never dropped — a test asserts a no-delta probe survives into the report); mechanical verdict; always-on **Threats-to-Validity**; `score` labeled **RRF (0.01–0.3 band)**; and the default `stub` answerer's report is banner-stamped **"PLUMBING DEMO, NOT EVIDENCE."**
- **@T8 REPRODUCTION (dead simple):** `node scripts/proof/cold-vs-warm.js` → offline plumbing demo (no creds/store/model). REAL proof = `--recall=http --answerer='cmd:codex exec'` (ANY model CLI reading prompt on stdin → answer on stdout). An out-of-distribution model reproducing the warm-wins IS the anti-rig check. See `scripts/proof/README.md`.
- **Unblocked by T1 031 (17:22):** my `log` fields already match 031 (recall_group_id per-call, source_type per-hit, token_budget per-call). `http` adapter POSTs `source_session_id`/`source_agent` to the Mnestra webhook (webhook wins over env per T1) → attributable. **ORCH runs the live cold-vs-warm at close-out** after applying 031: `export TERMDECK_PROOF_RECALL_URL=<mnestra webhook>; node scripts/proof/cold-vs-warm.js --recall=http --answerer=anthropic`.
- **@T4 coordination (2):** (a) my `http` adapter OPTIONALLY consumes your `/api/recall-events?session=<id>&limit=N`, expecting `{events|rows|data:[{recall_group_id,...}]}` — **confirm/adjust your envelope and I'll align** (purely additive: it only upgrades `recall_group_id` attribution, never blocks recall); (b) acknowledged your 17:27 panel-spawn producer — the report's provenance threat now states the landed T1-reader+T4-producer mechanism (not "WIP"), with the Codex env-inheritance item noted as open.
- **Boost mode (axis 2):** wired (`--mode=boost` + `rankDelta`) but **PARKS on T1 032 + T2** populating `recall_boost` (needs boost-off/on fixtures or a boost-aware endpoint) — deliberately did NOT fabricate a boost delta.
- **Web-write runbook:** `docs/WEB-WRITE-ACTIVATION-RUNBOOK.md` — deploy `inbox-promote` (mig 026 + Edge Fn secrets + `005`-style pg_cron) + flip `TERMDECK_BRIDGE_ENABLE_PROPOSE=1` (propose-map, `tools` 2→3) + verify/rollback/safety. **DOCS ONLY — no deploy fired; Josh-go-gated** (this is the runbook `cloud-origin.md:188` defers to).
- **In-lane discipline honored:** no version bumps / CHANGELOG / commits / publish; file-only; no live MCP/SQL from this lane (ORCH runs the live pass at close-out).

## T6 — miser Mac port

### [T6] CHECKPOINT 2026-07-05 16:59 ET — booted; baseline green, repo mapped, on feat/mac-port
Cloned repo read end-to-end. Baseline `npm test` = **20/20 PASS** (spec said 17; Phase 2 OpenAI translate tests added +3; T8 independently confirmed 20/20 + `node --check` all src). Env on this Mac: `node` v23.11.0 @ `/usr/local/bin/node` (Intel Homebrew), `ollama` @ `/usr/local/bin/ollama`, `brew` present. Proxy is pure `node:http` + built-ins, zero deps — **cross-platform, no code changes** (`config.js` env-driven, `index.js` binds `127.0.0.1`, no platform APIs). Now on **`feat/mac-port`** (resolves T8's 16:58 branch finding; no commits — ORCH close-out). Building: `miser.plist` template + `install-mac.sh` + `install-linux.sh` + README Install/caveats.

### [T6] FINDING 2026-07-05 17:00 ET — router uses ONLY fallbackModels[0]; installer must pull THAT exact model
`router.js:122` `const model = config.fallbackModels[0];` — miser never iterates the fallback list; only the **first** entry is ever used on a 429→Ollama fallback. R730 default `MISER_FALLBACK_MODELS=qwen2.5-coder:14b,...` → [0]=14b. A naive Mac installer that pulls "the smallest" (`qwen2.5:3b`) but keeps the R730 list would 429-fallback to `qwen2.5-coder:14b` → **Ollama 404, dead fallback**. Reconciliation in my install (packaging, not code): the pulled model is written FIRST in `MISER_FALLBACK_MODELS` in `~/.termdeck/secrets.env` AND baked as the plist default, so pulled≡[0] always. Default pull = `qwen2.5:3b` (charter "default smallest"); `--all` pulls the R730 trio and restores the 14b-first list.

### [T6] FINDING 2026-07-05 17:00 ET — compress drop-oldest can orphan tool_use/tool_result pairs
`compress.js:37-39` truncates whole turns via `truncated.shift()` (MIN_KEEP=4), no tool-pair awareness. If the drop boundary lands between an assistant `tool_use` and its paired user `tool_result`, Anthropic 400s (orphaned tool block). Real caveat for long orch sessions crossing 32K mid-tool-loop → documenting in README (charter-listed), not fixing (out of lane).

### [T6] DONE 2026-07-05 17:14 ET — Mac port complete; verified end-to-end, no code changes, on feat/mac-port
**Delivered (4 files, all in `~/Documents/Graciella/miser`, branch `feat/mac-port`, uncommitted for ORCH):**
- **`miser.plist`** — launchd user-agent TEMPLATE (Label `com.bheath-atx.miser`). `RunAtLoad`+`KeepAlive`+`ThrottleInterval=5`. Placeholders `__NODE_BIN__`/`__MISER_INDEX__`/`__LOG_PATH__` resolved at install time. **secrets.env sourcing via inline `sh -c` `set -a; . ~/.termdeck/secrets.env; set +a; exec node …`** (launchd has no `EnvironmentFile`; `exec` so `SIGTERM` reaches node — `index.js:23-24` handles it). Env defaults: `MISER_OLLAMA_URL=http://127.0.0.1:11434` (Mac std, **NOT** R730 `:11435`), threshold 32000, `MISER_FALLBACK_MODELS=qwen2.5:3b`.
- **`install-mac.sh`** (exec) — **answers Brad Q1** (`command -v node` resolved+templated → Intel/Apple-Silicon robust), **Q2** (scope = plist+model-pull+secrets+preflight; **NO `npm install`**, zero-dep), **Q3** (standalone-first; no `termdeck-stack` wiring). Preflight node≥18/ollama/brew; auto-starts `ollama serve` for the pull if down; default pull `qwen2.5:3b` (`--all`=R730 trio, `--models`, `--no-pull`, `--uninstall`, `--port/--ollama-url/--threshold`). **Idempotent secrets.env merge** (strips only MANAGED_KEYS, `.bak` saved, preserves TermDeck's other vars, `chmod 600`). `plutil -lint` gate + placeholder-leak guard; health-poll verify.
- **`install-linux.sh`** (exec) — parity port of the systemd flow (Brad's file table asked for it); defaults `:11434`, `--ollama-url :11435` for R730.
- **`README.md`** — Install (Mac launchd + Linux systemd) + **Security & operational caveats** (all 3 charter-listed: local cleartext TLS-termination on `:20128`, silent frontier→Ollama 429 downgrade, drop-oldest compression breaking `tool_use`/`tool_result` pairing).

**Verification (all green, non-intrusive — Josh's real `~/.termdeck`/`~/Library/LaunchAgents` untouched):** `npm test` **20/20** (unchanged — zero code edits); `node --check` all 7 src OK; `bash -n` both installers; **live proxy** `GET /api/miser/health`+`/api/miser/quota` → 200 (real `node src/index.js`); **sandboxed full `install-mac.sh --no-pull`** under scratch `HOME` + stubbed `launchctl`/`ollama` → rc 0, correct `launchctl unload`+`load -w`, secrets.env correct, health-verify passed; **rendered plist** = 0 leftover placeholders, valid XML, `sh -c` sources secrets.env + execs resolved node/index path.
**Packaging FINDING baked in:** `router.js:122` uses ONLY `fallbackModels[0]` → installer guarantees pulled model ≡ `MISER_FALLBACK_MODELS[0]` (else 429-fallback 404s). **For T8:** reproduce via sandbox recipe (scratch HOME + stub launchctl); fallback-model-mismatch + tool-pair-orphan caveats are the adversarial-worthy items.

## T7 — Codex Auditor A (DB/security/enforcement)

### [T7] CHECKPOINT 2026-07-05 16:56 ET — Phase 0 boot complete; memory recall/CLAUDE/CRITICAL/ARCHITECTURE/PLANNING/STATUS/T7 charter read. Verified so far: no worker FIX-LANDED posts yet in STATUS.md; Sprint docs require T1/T2/T3 adversarial audit with file:line evidence. Pending: dirty-tree map across termdeck/engram/rumen, baseline receipt/RLS/fire-and-forget inspection, targeted tests where local substrate permits. Latest worker FIX-LANDED: none.

### [T7] FINDING 2026-07-05 16:58 ET — BLOCKER before any T1 migration DONE claim: TermDeck's installer still uses bundled-first Mnestra migrations, so engram-only 030/031/032 would be invisible to `termdeck init --mnestra`. Evidence: `packages/server/src/setup/migrations.js:196-202` returns `packages/server/src/setup/mnestra-migrations` whenever bundled SQL exists; root `package.json:9-14` includes `packages/server/src/**` in the published tarball; current TermDeck mirror stops at `packages/server/src/setup/mnestra-migrations/022_source_agent_backfill.sql` while engram has `migrations/026_memory_inbox.sql` through `029_doctrine_recall_boost.sql` and Sprint 81 will add 030-032. Required remediation: sync/bundle new engram migrations plus add `MIGRATION_PROBES` entries for 023-032 or explicitly change the installer resolution contract; otherwise fresh installs and upgrade re-runs shadow `@jhizzard/mnestra` and never apply recall provenance/boost/precompact DDL.

### [T7] FINDING 2026-07-05 16:59 ET — Engram baseline is already red before Sprint 81 code: `npm run test` in `~/Documents/Graciella/engram` fails one test, `tests/migration-028-hygiene.test.ts:76-90`, because it still requires `memory_items_precompact_session_uidx` inside migration 028. The migration itself explicitly defers that index at `migrations/028_capture_gates.sql:212-219`, and Sprint 81 assigns it to 030/ORCH-last. Required remediation: move/update the static assertion to the new 030 artifact or land the 030 index SQL under the new test target; T1 cannot claim a green test gate while the stale 028 expectation remains.

### [T7] CHECKPOINT 2026-07-05 16:59 ET — Phase 1 baseline complete. Verified: termdeck/engram/rumen had no tracked Sprint 81 code edits at first poll; TermDeck hook baseline tests pass (`node --test packages/server/tests/pre-compact-hook.test.js packages/server/tests/periodic-capture.test.js` = 10/10); rumen baseline passes (`npm run test` = 126 pass, 1 skipped real-PG opt-in); engram baseline fails on stale 028 precompact-index assertion above. Pending: re-poll worker WIP, audit any new T1/T2/T3 code against findings and RLS/fire-and-forget/fail-soft gates. Latest worker FIX-LANDED: none.

### [T7] CHECKPOINT 2026-07-05 17:02 ET — Phase 2 provenance-boundary audit after compaction resume. Verified: STATUS.md still has no T1/T2/T3 FIX-LANDED; termdeck/engram/rumen tracked code still unchanged; current MCP stdio recall path exposes only retrieval inputs at `mcp-server/index.ts:330-367` and calls `memoryRecall()` without log provenance at `mcp-server/index.ts:379-387`, while the webhook recall path does pass caller provenance at `src/webhook-server.ts:146-160`. Pending: audit the concrete G2 producer design once T1 posts WIP/FIX-LANDED; latest worker FIX-LANDED: none.

### [T7] FINDING 2026-07-05 17:02 ET — Acceptance trap for T1's G2 fix: non-null MCP provenance cannot be satisfied by only threading optional `log_session_id`/`log_source_agent` fields through `mcp-server/index.ts`. Evidence: Sprint 81 requires the MCP path to actually land non-null `source_session_id`/`source_agent` (`PLANNING.md:21-23`, `T7-codex-auditor-a.md:11-14`); current MCP stdio registers one process-level server/transport at `mcp-server/index.ts:209-214` and `mcp-server/index.ts:822-824`, with no per-panel context; the current `memory_recall` schema exposes only `query/project/token_budget/min_results/source_agents/include_null_source/include_privacy` at `mcp-server/index.ts:330-367`; TermDeck boot prompts invoke plain `memory_recall(project=..., query=...)` / `memory_recall(query=...)` with no provenance arguments at `packages/server/share/termdeck/templates/codex-worker.txt:3-7`, `packages/server/share/termdeck/templates/claude-code-worker.txt:3-5`, `packages/server/src/agent-adapters/codex.js:302-319`, and `packages/server/src/agent-adapters/claude.js:190-207`. Required remediation: add a trusted producer for session/source-agent provenance (for example TermDeck-generated boot calls or a documented per-spawn env/config contract) plus a test proving the primary MCP path writes non-null values; optional self-reported fields alone leave the centerpiece proof blank/spoofable.

### [T7] FINDING 2026-07-05 17:04 ET — T1/T3 apply-order trap: the planned "hook switches before index; ORCH creates index last" ordering still has a capture-loss window because the target RPC already requires the deferred index. Evidence: migration 028 explicitly defers `memory_items_precompact_session_uidx` at `migrations/028_capture_gates.sql:212-219`, but `ingest_capture`'s precompact branch uses `ON CONFLICT (source_session_id) WHERE (source_type = 'pre_compact_snapshot' AND is_active = true)` at `migrations/028_capture_gates.sql:325-345`; PostgreSQL requires a matching unique/exclusion arbiter for that statement. Sprint 81 meanwhile tells T3 to switch the hook to `/rpc/ingest_capture` at `PLANNING.md:66-69` and says ORCH creates the index last at `PLANNING.md:56` / `PLANNING.md:100`. Required remediation: make 030/T3 close-out specify and test an actually executable transition, e.g. redefine `ingest_capture` in 030 so the precompact branch succeeds before the arbiter exists, or treat hook activation + index creation as one coordinated close-out window with an explicit fail-soft gap and a post-index RPC round-trip test. A hook URL change alone is not a valid T3 DONE signal.

### [T7] FINDING 2026-07-05 17:05 ET — T2 WIP red before FIX-LANDED: the confidence recalibration is documented but not wired. Evidence in `/Users/joshuaizzard/Documents/Graciella/rumen`: `src/confidence.ts:41-47` adds `normalizeSimilarity()`, and `src/synthesize.ts:18-38` documents v2 as RRF-normalized similarity plus novelty scaling, but `computeConfidence()` still uses the old raw formula at `src/synthesize.ts:677-699` (`0.5 * maxSim + 0.3 * crossProjectBonus + 0.2 * ageSpreadBonus`) and never calls `normalizeSimilarity`; `normalizeSimilarity` is only imported at `src/synthesize.ts:42-45`. Required remediation before T2 can claim THING 1 landed: compute from `normalizeSimilarity(maxSim)`, implement/test the claimed novelty factor or remove the claim, and add unit coverage proving a same-project high-RRF hit is not drowned by a low-RRF cross-project pair.

### [T7] CHECKPOINT 2026-07-05 17:06 ET — Phase 3 T2 WIP audit. Verified: rumen has tracked WIP in `src/confidence.ts` + `src/synthesize.ts`; `npm run test` on that WIP still passes (126 pass / 1 skipped), and `npm run typecheck` passes, but tests are stale because `tests/synthesize.test.ts:323-404` still asserts the v1 raw confidence formula. Pending: re-poll T2 for remediation, continue watching for T1/T3 first diffs/FIX-LANDED; latest worker FIX-LANDED: none.

### [T7] FINDING 2026-07-05 17:07 ET — T2 WIP is now behavior-changing but the acceptance gate is red: `npm run test` in `/Users/joshuaizzard/Documents/Graciella/rumen` fails 5 tests after `computeConfidence()` started calling `normalizeSimilarity()` at `src/synthesize.ts:703-704` and applying the v2 composite at `src/synthesize.ts:714-722`. Failing assertions are the stale v1 confidence expectations at `tests/synthesize.test.ts:323-404` plus placeholder normalization at `tests/synthesize.test.ts:414-435` (`actual 0.413/0.638/0.525/0.75/0.525` vs old expected `0.4/0.75/0.45/1.0/0.7`). Separate semantic issue before FIX-LANDED: the code comments promise "all-duplicate → the floor" at `src/synthesize.ts:740-744`, but `noveltyFactor()` computes `NOVELTY_FLOOR + (1 - NOVELTY_FLOOR) * (distinct / related.length)` at `src/synthesize.ts:748-752`, so two identical memories return `0.75`, not the configured `0.5` floor. Required remediation: update tests to assert the v2 RRF-normalized formula and add coverage for the drown case and novelty floor, or adjust the formula/docs if this softer duplicate penalty is intentional. `npm run typecheck` remains green.

### [T7] CHECKPOINT 2026-07-05 17:08 ET — Phase 4 poll complete. Verified: no T1/T3 tracked implementation diff yet (`/Users/joshuaizzard/Documents/Graciella/engram` clean; TermDeck tracked diffs are `package.json`, `packages/cli/src/doctrine-cli.js`, and `packages/server/src/doctrine-sync.js`, which are T4/T8 packaging/render surfaces rather than installer/gate hook work). Current T7 open blockers remain: bundled-migration shadowing for T1, stale Engram 028 test, MCP provenance producer gap, precompact RPC/index apply-order trap, and T2 red test gate above. Latest T1/T2/T3 FIX-LANDED: none.

### [T7] CHECKPOINT 2026-07-05 17:09 ET — Phase 5 receipt-sweep baseline anchored. Verified: the three sanctioned receipt-only edit targets still use text/regprocedure receipts (`migrations/026_memory_inbox.sql:316-319`, `migrations/027_recall_telemetry.sql:462-470`, `migrations/028_capture_gates.sql:557-563`), and `migrations/029_doctrine_recall_boost.sql:271-292` is the correct OID-form reference. Pending for T1: prove the sweep changes only those receipt blocks, does not alter DDL/backfill semantics in 026/027/028, and uses OID-form `has_function_privilege(role, oid, 'EXECUTE')` for all new 030/031/032 receipts. Latest T1/T2/T3 FIX-LANDED: none.

### [T7] FINDING 2026-07-05 17:12 ET — T1's G2 WIP now reads MCP provenance from env, but the trusted producer is still absent, so the primary TermDeck panel path can still log NULL. Evidence: Engram now reads `MNESTRA_SOURCE_SESSION_ID` / `MNESTRA_SOURCE_AGENT` at `mcp-server/index.ts:222-238` and threads them into `memoryRecall()` at `mcp-server/index.ts:416-420`, but TermDeck has no producer for those env vars (`rg "MNESTRA_SOURCE_SESSION_ID|MNESTRA_SOURCE_AGENT" packages/server packages/cli packages/stack-installer doctrine package.json` returns no matches). The current TermDeck boot prompts still issue plain `memory_recall(...)` at `packages/server/share/termdeck/templates/codex-worker.txt:3-8`, `packages/server/share/termdeck/templates/claude-code-worker.txt:3-6`, `packages/server/src/agent-adapters/codex.js:302-319`, and `packages/server/src/agent-adapters/claude.js:190-207`. Required remediation before G2 can pass: TermDeck must set these env vars in the MCP process/config or generated session boot contract with stable session/source-agent values, plus a test proving the everyday MCP-stdio path writes non-null `source_session_id`/`source_agent`. The Engram env reader alone is still fail-soft-to-NULL.

### [T7] CHECKPOINT 2026-07-05 17:12 ET — Phase 6 T1 031 WIP audit. Verified positive: `migrations/031_recall_provenance.sql` extends 027 with nullable `source_type`, `token_budget`, `recall_group_id` at `:79-84`, updates the existing `log_recall_hits(jsonb)` insert/recordset at `:133-160`, preserves the batched denorm update at `:170-179`, re-pins service-role-only EXECUTE at `:195-198`, and uses OID-form receipt checks at `:279-292`; new static coverage in `tests/migration-031-hygiene.test.ts:32-145` and payload coverage in `tests/recall-log.test.ts:101-145` exercise those pieces. Verified red gate: escalated `npm run test` in `/Users/joshuaizzard/Documents/Graciella/engram` still reports **256 pass / 1 fail**, with the remaining failure the already-posted stale `tests/migration-028-hygiene.test.ts` precompact-index assertion. Pending: TermDeck producer for G2, stale 028 test fix, T1 030/032 artifacts, and T3 implementation diff. Latest T1/T2/T3 FIX-LANDED: none.

### [T7] FINDING 2026-07-05 17:13 ET — T2 fixed the red test gate, but the novelty contract is still internally inconsistent. Verified: `/Users/joshuaizzard/Documents/Graciella/rumen` now passes `npm run test` (**140 pass / 1 skipped / 0 fail**) and `npm run typecheck`, and the new tests cover RRF normalization plus the strong-same-project-vs-weak-cross-project drown case at `tests/synthesize.test.ts:428-469`. Remaining issue: `src/synthesize.ts:690-694` and `:740-744` say `NOVELTY_FLOOR` is the lowest multiplier for a fully duplicated/all-duplicate cluster, but the formula at `src/synthesize.ts:748-752` returns `0.5 + 0.5*(distinct/N)`, and the new tests explicitly assert that all-identical three-row input returns `0.5 + 0.5*(1/3)` at `tests/synthesize.test.ts:502-510` (not the floor `0.5`). Required remediation before THING 1 DONE: either update the comments/contract to describe the tested asymptotic floor, or change the formula/tests to make all-duplicate finite clusters hit `NOVELTY_FLOOR` (for example mapping `(distinct-1)/(N-1)`). This is no longer a test-red blocker, but it is still a correctness/documentation mismatch.

### [T7] FINDING 2026-07-05 17:14 ET — T1's MCP provenance WIP covers only `memory_recall`/`memory_search`, leaving other telemetry-producing MCP recall surfaces NULL. Evidence: `mcp-server/index.ts:416-420` passes env provenance into `memoryRecall()` and `mcp-server/index.ts:505-508` passes it into `memorySearch()`, but `memory_index` calls `memoryIndex()` without provenance at `mcp-server/index.ts:629-636`, `memory_timeline` calls `memoryTimeline()` without provenance at `mcp-server/index.ts:668-674`, and `memory_recall_graph` calls `memoryRecallGraph()` without provenance at `mcp-server/index.ts:462-469`. The callee types also have no provenance slots for index/timeline (`src/layered.ts:42-54`, `:56-60`), so their `logRecallHits()` calls still pass only `{ surface, query }` at `src/layered.ts:125-132` and `src/layered.ts:215-221`; graph likewise logs only `{ surface:'graph', query }` at `src/recall_graph.ts:126-129`. Sprint 81's grounding says all 5 recall surfaces produce telemetry and the proof asks "which panel pulled which memory"; if the acceptance scope is only the `memory_recall` tool, state that explicitly, otherwise extend/test provenance across index/timeline/graph too.

### [T7] CHECKPOINT 2026-07-05 17:15 ET — Phase 7 T3 first-gate WIP audit. Verified: new `packages/stack-installer/assets/hooks/gate-publish-before-push.js` is syntactically valid (`node --check`), is registry-gated/inert unless `publish-before-push` is `status:active` + `preToolUse-deny` + `block` at `:104-130`, exits 0 with no stdout for non-Bash input and inactive-registry `git push`, and its classifier handles `git push` / compound-command basics via exported `isGitPush()` (`:148-153`). No AUDIT-PASS yet: only one of two gates exists, no installer merge/install/refresh trio has landed, no hook tests are present, and the active-deny path has not been reproduced with stubbed npm/git. Latest T1/T2/T3 FIX-LANDED: none.

### [T7] CHECKPOINT 2026-07-05 17:16 ET — Phase 7a T3 active-deny probe complete. Reproduced the `publish-before-push` deny path using a temp active registry, fake `git` reporting branch `main`/repo root, and fake `npm view @jhizzard/testpkg version` returning `1.0.0` while local `package.json` was `9.9.9`: hook exited `0`, stderr empty, stdout contained `hookSpecificOutput.permissionDecision="deny"` with the unpublished-version reason. This validates the positive block path for the first gate. Still pending before T3 pass: second gate, installer/refresh wiring, shipped registry vendoring behavior, and repo-owned tests.

### [T7] CHECKPOINT 2026-07-05 17:17 ET — Phase 7b T3 second-gate WIP audit. Verified: new `packages/stack-installer/assets/hooks/gate-migration-without-rls.js` is syntactically valid (`node --check`), registry-gated/inert at `:108-133`, classifies `git commit` while allowing `--dry-run`/`--help` at `:150-158`, and its pure scanner flags `create table public.foo` without RLS while allowing same-file `alter table public.foo enable row level security` (`violationsForFile()` at `:235-240`). Runtime reproduction with a temp active registry + fake staged migration `migrations/123_bad.sql` produced exit `0`, empty stderr, and `hookSpecificOutput.permissionDecision="deny"` naming `public.foo`. Still pending before T3 pass: installer merge/install/refresh trio, repo-owned tests, and pre-compact hook switch/fallback implementation.

### [T7] FINDING 2026-07-05 17:19 ET — T2 THING-2 WIP adds the recall-boost writer but has not updated Rumen's own write-contract doc, so the code and safety doctrine now contradict each other. Evidence in `/Users/joshuaizzard/Documents/Graciella/rumen`: new `src/reinforce.ts:13-16` says reinforce writes `memory_items.recall_boost` via `set_recall_boost`, and the edge wrapper repeats that at `supabase/functions/rumen-reinforce/index.ts:3-9`; but `docs/MNESTRA-COMPATIBILITY.md:68-74` still says there are exactly three non-Rumen-table write surfaces and "No UPDATE or DELETE ever targets `memory_items`." The T2 charter explicitly required documenting the new bounded write at `T2-rumen-recall-feedback.md:13-16`. Required remediation before THING-2 DONE: update `MNESTRA-COMPATIBILITY.md` to list `recall_boost` as the fourth narrow write surface, with the RPC-only / bounded / no content-update guarantees, or remove the writer.

### [T7] FINDING 2026-07-05 17:19 ET — T2 THING-2 WIP is not covered by the repo's test gate despite being a new DB-writing surface. Verified: `package.json:21` runs only `tests/synthesize.test.ts tests/extract.test.ts tests/relate.test.ts tests/surface.test.ts tests/promote.test.ts tests/doctrine-scan.test.ts`, and `rg "reinforce|computeRecallBoost|set_recall_boost" tests package.json` finds no reinforce tests. The charter required extending `tests/fixtures/mnestra-minimal.sql` with `memory_recall_log` rows + `recall_count` for offline unit testing (`T2-rumen-recall-feedback.md:16`). Current `npm run test` and `npm run typecheck` in Rumen are green (140 pass / 1 skipped), but they only prove the new file compiles through `src/index.ts`; they do not prove bounded boost math, candidate scan, cited/surfaced tallying, dry-run behavior, or RPC-only write shape. Required remediation: add focused tests for `computeRecallBoost()` bounds/saturation/decay and `runRumenReinforce()` mock-pool query/write behavior, and include them in `npm run test`.

### [T7] FINDING 2026-07-05 17:20 ET — T3 PreToolUse installer WIP is only fresh-install wired; `init-mnestra` refresh still cannot repair or upgrade the new gates. Positive evidence: `packages/stack-installer/src/index.js:90-107` defines the two gate assets, `_mergePreToolUseHookEntry()` is present at `:649-699`, `installPreToolUseHook()` copies both files and merges `hooks.PreToolUse` at `:1049-1155`, main install calls it at `:1355-1361`, and the helpers/constants are exported at `:1419-1426`. Verified syntax/current regression gate: `node --check packages/stack-installer/src/index.js` exits 0 and focused existing hook tests pass (`node --test packages/stack-installer/tests/hook-command-absolute.test.js packages/cli/tests/init-mnestra-hook-command-absolute.test.js packages/cli/tests/init-mnestra-content-drift.test.js` = 31/31). Missing acceptance: T3 charter required `init-mnestra.js` refresh wiring at `T3-advise-gate-hook-switch.md:11`; current `packages/cli/src/init-mnestra.js:1102-1135` refreshes only `memory-pre-compact.js`, and `rg "gate-publish-before-push|gate-migration-without-rls|PreToolUse" packages/cli/src/init-mnestra.js packages/cli/tests packages/stack-installer/tests` finds no refresh/test coverage for the new gates. Required remediation: add the two gate files to the `init-mnestra` refresh pass, prove dry-run/content-drift/custom-preserve behavior for both, and add focused installer tests for fresh merge, partial existing gate, legacy `~` rewrite, and idempotency.

### [T7] CHECKPOINT 2026-07-05 17:22 ET — Phase 8 T1 G2/031 re-audit after WIP updates. Positive re-verification: T1 corrected the env-var contract to `MNESTRA_SESSION_ID` / `MNESTRA_SOURCE_AGENT` (Engram help at `mcp-server/index.ts:136-142`; `recall_log.ts:239-240`), moved fallback into the single `logRecallHits()` choke point so recall/search/index/timeline/graph all inherit it (`src/recall_log.ts:191-211`), added tests for env fallback / explicit-ctx precedence / `${VAR}` placeholders (`tests/recall-log.test.ts:99-146` plus the env tests immediately below), and fixed the stale 028 hygiene assertion to document that the precompact index is deferred to 030 (`tests/migration-028-hygiene.test.ts:73-110`). Verified official Engram gate: `npm run test` now passes **274/274**. Still no full T1 AUDIT-PASS: migrations 030 and 032 are not present yet, and TermDeck producer semantics below still need cleanup.

### [T7] FINDING 2026-07-05 17:22 ET — TermDeck's new trusted producer uses `adapter.name` instead of the established `adapter.sourceAgent || adapter.name`, so panels with explicit provenance aliases can stamp non-canonical recall agents. Evidence: the new env producer computes `mnestraSourceAgent` from `provenanceAdapter.name` at `packages/server/src/index.js:2411-2415`, while both existing memory writers deliberately use `adapter.sourceAgent || adapter.name` at `packages/server/src/index.js:343-350` and `:415-418`. The live web-chat adapter declares `name: 'web-chat-grok'` but canonical `sourceAgent: 'grok-web'` at `packages/server/src/agent-adapters/web-chat-grok.js:212-218`, and Engram's pinned source-agent taxonomy includes `grok-web` but not `web-chat-grok` (`src/types.ts:161-182`). Current spawn test coverage checks only `TERMDECK_SESSION` at `packages/server/tests/adapter-spawn-shell-wrap.test.js:199-208`; it does not assert `MNESTRA_SESSION_ID` or `MNESTRA_SOURCE_AGENT`. Required remediation before producer-side G2 can pass: compute `MNESTRA_SOURCE_AGENT` with `adapter.sourceAgent || adapter.name`, and add server tests proving direct Claude/Codex get `MNESTRA_SESSION_ID=<session id>` and canonical `MNESTRA_SOURCE_AGENT`, plus an explicit-sourceAgent adapter fixture uses the alias rather than raw adapter name.

### [T7] CHECKPOINT 2026-07-05 17:23 ET — Phase 9 T3/T2 repoll. T3 has now added `init-mnestra` PreToolUse wiring/refresh code (`packages/cli/src/init-mnestra.js:766-775`, `:901-958`, `:1066-1100`, `:1168-1193`, `:1271-1305`) and exports; auditor smoke against both stack-installer and init-mnestra `_mergePreToolUseHookEntry()` passed. Focused TermDeck regression run is green: `node --test packages/stack-installer/tests/hook-command-absolute.test.js packages/cli/tests/init-mnestra-hook-command-absolute.test.js packages/cli/tests/init-mnestra-content-drift.test.js packages/server/tests/adapter-spawn-shell-wrap.test.js packages/server/tests/pre-compact-hook.test.js packages/server/tests/periodic-capture.test.js` = **51/51**. Remaining T3 blockers: `rg "PreToolUse|gate-publish|gate-migration|migrateSettingsJsonPreToolUse|PRETOOLUSE" packages/cli/tests packages/stack-installer/tests packages/server/tests` finds no repo-owned tests for the new PreToolUse helper/refresh paths, and `git diff -- packages/stack-installer/assets/hooks/memory-pre-compact.js` is still empty (no `ingest_capture` switch/fallback). T2 repoll: fixture additions landed, but `tests/*reinforce*` still absent, `package.json:test` still excludes reinforce, and `docs/MNESTRA-COMPATIBILITY.md:68-74` still says exactly three write surfaces / no `memory_items` UPDATE; Rumen `npm run test` and `npm run typecheck` remain green but do not cover THING-2.

### [T7] FINDING 2026-07-05 17:24 ET — T3 pre-compact v3 implementation still writes raw `/memory_items`; the claimed `ingest_capture` adoption is comment-only. Evidence: the header now says the primary write path is `/rest/v1/rpc/ingest_capture` at `packages/stack-installer/assets/hooks/memory-pre-compact.js:45-49` and `:74-97`, but the only write helper remains `postPreCompactSnapshot()` posting to `${supabaseUrl}/rest/v1/memory_items` at `:170-212`, and `processPreCompactPayload()` calls that helper directly at `:292-299` with no RPC attempt/fallback branch. Runtime reproduction with a mocked `fetch` confirmed a successful precompact payload returns `status:"ingested"` after one request to `https://example.supabase.co/rest/v1/memory_items` and zero requests to `/rpc/ingest_capture`. Required remediation before T3 hook-switch DONE: add an actual `ingest_capture` RPC call, treat only HTTP 2xx plus `body.ok===true` as clear success, and fall back to raw append only on non-clear success; then add tests proving RPC success does not double-write and RPC 42P10/PGRST202/non-ok falls back.

### [T7] FINDING 2026-07-05 17:30 ET — T2 reinforce test gate is red/hanging after adding coverage, and the failure exposes a real clock bug in the runner. Evidence: `package.json:21` now includes `tests/reinforce.test.ts`, but `npm run test` hung for multiple minutes after normal output and had to be interrupted; focused `npx tsx --test --test-timeout=5000 tests/reinforce.test.ts` fails with a file-level timeout. Manual execution of the first intended write path shows why the assertions cannot pass: `runRumenReinforce()` derives `deadlineAt` from the injected logical clock at `src/reinforce.ts:209-216` (`nowMs + budgetMs`) but checks it against wall-clock `Date.now()` at `:287`; the tests inject `NOW = 2026-07-05T12:00:00Z` at `tests/reinforce.test.ts:185` and call the runner with `{ nowMs: NOW }` at `:199`, so the current wall clock is already past `deadlineAt` and the write is skipped with `skipped_reason:"budget_exceeded"`. Required remediation: separate deterministic telemetry time from budget wall-clock timing (or inject both clocks), then prove the full `npm run test` exits cleanly and the reinforce write-path tests actually assert `set_recall_boost`.

### [T7] CHECKPOINT 2026-07-05 17:31 ET — Phase 10 T3/T1 re-audit after fresh diffs. T3 fixed the 17:24 hook-switch defect: `packages/stack-installer/assets/hooks/memory-pre-compact.js:166-205` now builds a shared payload and POSTs `{p_payload: ...}` to `/rest/v1/rpc/ingest_capture`, treats only HTTP 2xx plus `{ok:true}` as clear success at `:190-215`, and falls back to raw `/memory_items` only on non-clear-success at `:347-364`. New coverage in `packages/server/tests/pre-compact-hook.test.js:137-282` proves RPC success/no-double-write plus 42P10/PGRST202/{ok:false}/network fallback, and the focused TermDeck gate passed **58/58**. TermDeck's trusted producer also now uses `sourceAgent || name` at `packages/server/src/index.js:2417-2420`, with alias-aware test coverage at `packages/server/tests/adapter-spawn-shell-wrap.test.js:191-210` plus Claude/Codex env assertions at `:216-264`. Remaining T3 caveat: `rg "PreToolUse|gate-publish-before-push|gate-migration-without-rls|migrateSettingsJsonPreToolUseEntry|_mergePreToolUseHookEntry" packages/cli/tests packages/stack-installer/tests packages/server/tests` still finds no repo-owned tests for the new PreToolUse gate install/refresh helpers beyond unrelated preservation coverage. T1 status: migration 032 landed with static hygiene coverage (`migrations/032_recall_boost.sql`, `tests/migration-032-hygiene.test.ts`) and Engram official `npm run test` passes **283/283**; still no `migrations/030*.sql`, and TermDeck's bundled installer mirror still stops at `packages/server/src/setup/mnestra-migrations/022_source_agent_backfill.sql`, so the 16:58 installer-shadowing blocker remains open for fresh `termdeck init --mnestra` installs.

### [T7] FINDING 2026-07-05 17:34 ET — T3 publish-before-push gate blocks `git push --dry-run` even though the hook's stated safety posture is fail-open and the migration gate already exempts dry-run. Evidence: `packages/stack-installer/assets/hooks/gate-publish-before-push.js:148-153` classifies any segment containing `git ... push`, with no `--dry-run` / `--help` exclusion; by contrast the sibling migration gate explicitly excludes `git commit --dry-run` / `--help` at `packages/stack-installer/assets/hooks/gate-migration-without-rls.js:148-156`. Runtime reproduction with an active registry, fake main-branch git repo, fake npm reporting published `1.0.0`, and local `@jhizzard/testpkg@9.9.9` showed `git push --dry-run` exits `0` but emits `hookSpecificOutput.permissionDecision="deny"`. Required remediation: exclude dry-run/help/non-writing push probes (and add a unit/runtime test) so users can inspect what would be pushed without tripping the release-order blocker.

### [T7] CHECKPOINT 2026-07-05 17:35 ET — Phase 11 fresh-fix audit. T1 has now landed `migrations/030_precompact_rolling.sql` + `tests/migration-030-hygiene.test.ts`; static review confirms the R3 branch is arbiter-free (`pg_advisory_xact_lock` + select/update/insert at `migrations/030_precompact_rolling.sql:153-197`), the only remaining `ON CONFLICT` is the content_hash branch at `:203-220`, the precompact unique index is commented/deferred at `:240-253`, and OID-form privilege receipt checks are present at `:290-314`. Official Engram gate passes **292/292**. T1 still cannot receive full installer-path pass because TermDeck's bundled migration mirror remains at `packages/server/src/setup/mnestra-migrations/022_source_agent_backfill.sql`. T2 partially fixed the documentation finding (`docs/MNESTRA-COMPATIBILITY.md:68-75`, `CONTRIBUTING.md:7` now document the fourth recall_boost write surface), but the reinforce runner still computes `deadlineAt = nowMs + budgetMs` and checks `Date.now()` at `src/reinforce.ts:209-216` / `:287`; focused `npx tsx --test --test-timeout=5000 tests/reinforce.test.ts` still times out, while `npm run typecheck` passes. T3 added `packages/stack-installer/tests/pretooluse-gate-behavior.test.js` and focused TermDeck gate passes **35/35**, but the publish gate still returns `true` for `isGitPush('git push --dry-run')` and `isGitPush('git push --help')`, so the 17:34 dry-run FINDING remains open.

### [T7] AUDIT-FAIL 2026-07-05 18:47 ET — Final pass green on bundle-sync/reinforce, but publish gate still blocks `git push --dry-run`/`--help` and the novelty-floor doc/formula mismatch remains. Verified fixed: `listMnestraMigrations()` returns 32 contiguous SQL files `001`-`032`; `MIGRATION_PROBES` covers `023`-`032`; bundle drift test passes 5/5 and byte comparison reports no drift for `023`-`032`; Rumen focused reinforce passes 16/16 and full `npm run test` passes 156 with 1 real-PG skip; Engram `npm run test` passes 292/292; TermDeck `npm run test` passes 1048 with 5 browser skips. Remaining fail 1: `packages/stack-installer/assets/hooks/gate-publish-before-push.js:152-156` has no dry-run/help exclusion, runtime helper output is `dryRun:true, help:true`, and `packages/stack-installer/tests/pretooluse-gate-behavior.test.js:251-260` still lacks those non-writing probe assertions. Remaining fail 2: `/Users/joshuaizzard/Documents/Graciella/rumen/src/synthesize.ts:690-694` and `:740-752` still describe all-duplicate/fully-duplicated novelty as the floor, while `/Users/joshuaizzard/Documents/Graciella/rumen/tests/synthesize.test.ts:502-509` asserts `0.5 + 0.5*(1/3)`.

### [T7] FINAL-VERDICT 2026-07-05 18:47 ET — NOT GREEN FOR CLOSE-OUT: two minor but real acceptance items remain, the `git push --dry-run`/`--help` exemption and the novelty-floor doc/formula mismatch.

## T8 — Codex Auditor B (proof/app/miser)
### [T8] CHECKPOINT 2026-07-05 16:56 ET — Boot complete: memory recall wired; read global/repo CLAUDE, critical P0 note, PLANNING/STATUS/T8 charter; entering baseline diff/file-ownership audit for T4/T5/T6 surfaces.
### [T8] CHECKPOINT 2026-07-05 16:57 ET — Baseline map before worker fixes: no T4/T5 code diff in termdeck (`git diff --stat` empty; Sprint 81 docs untracked only). Existing Memory drawer renders only `entry.memoryHits` with source/project/similarity/time at `packages/client/public/app.js:2376-2406`; no `/api/recall-events` route exists around the flashback-history model route at `packages/server/src/index.js:3691-3702`. Doctrine render helpers still live inside `packages/server/src/doctrine-sync.js:173-259`. Miser repo clean but on `main`; baseline is Linux/R730-oriented (`src/config.js:6` default `:11435`, `miser.service:12-19`, `README.md:29-41`) with no `install-mac.sh`/plist yet. Pending: re-diff on T4/T5/T6 FIX-LANDED and reproduce T5 harness once authored.
### [T8] CHECKPOINT 2026-07-05 16:58 ET — Baseline verification green: miser `node --check src/config.js src/router.js src/translate.js src/compress.js src/index.js src/proxy.js src/quota.js` passed and `npm test` passed 20/20; TermDeck focused doctrine baseline `node --test packages/server/tests/doctrine-registry-shape.test.js packages/cli/tests/doctrine-cli.test.js` passed 36/36. Still no worker FIX-LANDED posts for T4/T5/T6 in STATUS.md; pending first implementation diff.
### [T8] FINDING 2026-07-05 16:58 ET — Preventive T6 process finding: `/Users/joshuaizzard/Documents/Graciella/miser` is currently on `main` (`git branch --show-current`), while the T6 charter requires Brad-repo work on a branch and no commits to Brad's main (`docs/sprint-81-recall-reinjection-proof/T6-miser-mac-port.md:20-21`). No miser files are dirty yet, so T6/ORCH can still fix this before packaging edits.
### [T8] AUDIT-PASS 2026-07-05 18:52 ET — T4 proof surface PASS on implementation/tests. Verified `/api/recall-events` and `/:sessionId` read REAL `public.memory_recall_log` with `left join public.memory_items` (`packages/server/src/index.js:3789-3801`), return fail-soft empty on no DB/query errors (`:3780-3783`, `:3809-3812`), and route through pure `groupRecallEvents` (`packages/server/src/recall-events.js:40-103`) with doctrine detection at `:77-80`. Client Memory tab keeps live hits and adds durable reinjection events (`packages/client/public/app.js:2405-2458`) with 📜 doctrine chip at `:2379-2384`. R2 producer sets `MNESTRA_SESSION_ID=session.id` and canonical `MNESTRA_SOURCE_AGENT=adapter.sourceAgent||adapter.name` into spawned panel env (`packages/server/src/index.js:2418-2459`); Codex config is stdio `mnestra` with no explicit env (`codex mcp get mnestra` shows `env: -`), and the npm wrapper passes parent `process.env` to the native Codex binary (`/usr/local/lib/node_modules/@openai/codex/bin/codex.js:140-154`). Black-box nested-Codex MCP env probe was not executed because approval policy rejected an external model call, but the local static/env-path evidence supports the producer design. Verification: `node --test packages/server/tests/recall-events.test.js packages/server/tests/adapter-spawn-shell-wrap.test.js` = 23/23; root `npm run test` = 1049 pass / 5 skip / 0 fail.
### [T8] AUDIT-FAIL 2026-07-05 18:52 ET — T5 harness mechanics are honest, but the required live proof was NOT reproduced from this substrate. Positive: `node scripts/proof/cold-vs-warm.js` ran the frozen 6-probe fixture set, reported all 6 probes, and stamped the run as stub/plumbing only; `node --test packages/server/tests/proof-cold-vs-warm.test.js` = 21/21; `--verify-frozen` passed. Static review confirms COLD vs WARM differs only by the memory block (`scripts/proof/lib/runner.js:18-31`), verdict is mechanical fact-key matching (`scripts/proof/lib/metrics.js:36-59`, `:97-101`), and aggregate reports no-delta/cold-wins/errors rather than filtering (`:164-200`). Negative acceptance: the requested real form `node scripts/proof/cold-vs-warm.js --recall=http --answerer='cmd:codex exec'` produced 6/6 probe errors in sandbox because nested `codex exec` cannot initialize; the escalated rerun was rejected because it would pass live recalled memory into an external model; `TERMDECK_PROOF_RECALL_URL` is unset and `127.0.0.1:37778/mnestra` is not listening. Additional T5 code finding: optional T4 event confirmation is misaligned — it calls `${eventsUrl}?session=<id>&limit=50` and expects snake_case `recall_group_id` (`scripts/proof/lib/recall-adapter.js:143-153`), while T4 exposes the per-session route as `/api/recall-events/:sessionId` (`packages/server/src/index.js:3814-3820`) and returns camelCase `recallGroupId` (`packages/server/src/recall-events.js:53-60`). Result: the live report's optional actual-log `recall_group_id` confirmation will not work against T4's route as documented.
### [T8] AUDIT-PASS 2026-07-05 18:52 ET — T6 miser Mac port PASS. Branch is now `feat/mac-port`; delivered `miser.plist`, `install-mac.sh`, `install-linux.sh`, and README updates. Verified `install-mac.sh` resolves `command -v node` and templates it (`install-mac.sh:87-91`, `:158-165`), writes Mac-standard `MISER_OLLAMA_URL=http://127.0.0.1:11434` and pulled model as `MISER_FALLBACK_MODELS[0]` (`:45-48`, `:80-82`, `:147-153`), and the plist sources `~/.termdeck/secrets.env` then execs templated node (`miser.plist:32-44`). README documents local cleartext loopback, silent 429 frontier→Ollama downgrade, and tool_use/tool_result compression caveat (`README.md:161-167`). Verification: miser `npm test` = 20/20, `node --check` all src, `bash -n install-mac.sh install-linux.sh`, `plutil -lint miser.plist`, scratch-HOME `install-mac.sh --no-pull --port 21282 --models qwen2.5:3b` with stub launchctl/ollama and live local health server passed, and a patched one-off router probe forced Anthropic 429 → Ollama with `x-miser-provider=ollama` and model `fallbackModels[0]=qwen2.5:3b`.
### [T8] FINAL-VERDICT 2026-07-05 18:52 ET — AUDIT-FAIL overall: T4 PASS, T6 PASS, T5 implementation/honesty mechanics PASS but T5 live `--recall=http --answerer='cmd:codex exec'` proof was not reproduced and the optional T4 event-confirmation path is mismatched. Close-out should either (a) fix the T5 events URL/property mismatch and run the live proof from an approved substrate with `TERMDECK_PROOF_RECALL_URL` set, or (b) explicitly accept a code-green/live-proof-not-run caveat.

---
## Cross-lane coordination / ORCH notes

### [ORCH] 2026-07-05 19:07 ET — miser (T6) lane DROPPED
T8 gave the Mac PORT an AUDIT-PASS, but a separate adversarial review of the miser **core** (Brad's software, not the packaging) concluded it is **not ready to sit in the Claude Code↔Anthropic path**: two reproduced request-breaking bugs (drop-oldest `compress` orphans tool_use/tool_result + leaves assistant-first → Anthropic 400 mid-tool-loop; Ollama-error body → truncated SSE → client hang), core premise fights the preserve-context thesis, 429→Ollama silent-downgrade footgun, several false README claims. Key is pass-through (never leaked); miser was never wired into the live loop. **T6/miser is DROPPED from Sprint 81 close-out;** `feat/mac-port` stays parked/uncommitted, not handed back as ready. Peer-review findings drafted to Brad. Remaining proof-lane items: T5↔T4 route/property mismatch (`recall-adapter.js:143-153` vs `/api/recall-events/:sessionId`+`recallGroupId`) + the live cold-vs-warm proof (ORCH close-out step).
