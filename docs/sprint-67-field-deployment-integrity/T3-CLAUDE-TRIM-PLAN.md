# Sprint 67 · T3 · `~/.claude/CLAUDE.md` trim plan (for T4 audit)

**Authored:** 2026-05-23 ~09:27 ET by Sprint 67 T3 lane.
**Purpose:** Brief mandates a FIX-PROPOSED before the actual edit so T4 can audit the rule-keep + promote-to-Mnestra plan first. STATUS.md is being concurrently appended by T4 every few minutes; placing the full enumeration here keeps the audit substrate stable.

## Baseline

- **File:** `~/.claude/CLAUDE.md`
- **Pre-trim line count:** 434 (per fresh `wc -l ~/.claude/CLAUDE.md`; T4-CODEX 09:19 ET snapshot SHA `2a03051f8dcf2c56bedeb3fb149c29fef70a4be62a4bd7e89e7cade6be191d28`, copy at `/private/tmp/sprint67-t4-claude-pretrim.md`)
- **Trim target:** ~265 lines (~40% trim of actual 434)
- **Substance rule:** preserve every load-bearing rule verbatim **in substance** — "in substance" allows compressing rule-adjacent parenthetical history to one line; it does NOT permit removing any of the 12 enumerated rule sections below.

## Master rule of the trim

Every "MANDATORY: ..." section header survives. Every numbered list of gates / steps / sections survives. Every code-block template (SQL, AppleScript, bash, JSON) survives. What gets promoted to Mnestra is the prose backstory — the "Sprint N case study" paragraphs, the "Verified working YYYY-MM-DD ..." breadcrumbs, the "(History: ...)" footer parentheticals.

## 12 sections preserved in substance (T4: enumerate against the trimmed file; any of these missing = FAIL)

1. **§ Time check** (current 3-7) — `date`-before-time-indicative-statements rule + time-neutral-framing fallback. Both paragraphs kept.
2. **§ Session-End Email** (9-27) — rule itself; 5-section requirement (What was done / planned next / restart-prompt-doc paths / restart prompt #4 / `cd <cwd> && claude --resume <uuid>` resume command incl. JSONL-must-exist verify, `<pre>`-wrapped shape, why both #4 and #5 exist); subject format (`TermDeck Wrap — <summary> — YYYY-MM-DD HH:MM ET`); rich-text-HTML via `htmlBody`; **CRITICAL** raw-angle-brackets-not-entity-encoded rule + pre-flight-scan defense.
3. **§ Check Memory First** (29-37) — 3-step opening with both `memory_recall` calls + never-say-I-don't-know-without-memory rule. Kept entirely.
4. **§ RAG Memory System** (41-96) — all 5 subsections: Session Start, During Work, Before Context Gets Long (its 4-numbered list of hook mechanisms + crash-near), Kitchen vs recipes (its 4 concrete tests), Cross-Project Search, Memory Tools Available table.
5. **§ Project Directory Map** (98-137) — full table (every row), mnestra/engram alias note, 2026-05-15 map-corrections note (Maestro + ClaimGuard), Antigravity-scratch-risk warning, TermDeck+Mnestra+Rumen stack-trio explanation, the Supabase-project-backing line. The internal-project-name literal IS allowed inside `~/.claude/CLAUDE.md` because the file is private to Joshua's machine and never ships in any artifact — the no-external-leak rule does not apply here. T4 confirm: file path is `~/.claude/CLAUDE.md`, not under any repo.
6. **§ Sprint role 3+1+1** (139-165) — 3-bullet definition (Orchestrator/Workers/Auditor), auditor-is-reviewer-not-author principle, cost/benefit one-liner, application-to-every-project rule, `[T4-CODEX]` post-shape pattern. The "Three hardening rules" stay numbered 1/2/3 with their **How to apply** lines intact; **Why** lines compressed from full case studies to one-sentence "Why".
7. **§ Orchestrator-centralized close-out** (167-182) — rule, 4-step harvest procedure, "fewer denser kitchen memories (~5-8 per sprint)" guidance, "supersedes worker-self-harvest" line.
8. **§ 3+1+1 inject mandate** (184-243) — PREFLIGHT substrate-verification block (5 sub-rules: GET /api/sessions FIRST, substrate-mention trigger, autonomous-mode-NOT-substrate, Agent-tool-reserved-for-ORCH-recon, Codex-process-identification, rationalization-as-signal). Inject mechanism: bracketed-paste markers, **MANDATORY two-stage submit pattern** + 3-step deterministic fix, across-4-sessions cadence (paste-A→D, 400ms settle, submit-A→D), post-submit `GET /api/sessions/:id/buffer` verify, `/api/sessions/:id/poke` recovery. Boot-prompt-template code block. "Cardinal sin is leaving a panel waiting for a human Enter press" rule (with the ClaimGuard + Sprint 36 reminders KEPT inline as emotional load-bearing). "Pre-sprint intel goes in the prompt body" guidance. "Same convention applies on any project" portability note.
9. **§ Never copy-paste — always inject** (245-281) — rule, WhatsApp two-step send pattern (full bash + osascript code block kept verbatim — paste-ready operational template), `sleep 3` / `delay 1` / `key code 36` rationale, iMessage/SMS rule (`mcp__imessage__send_imessage`, SMS service flag for Android, David Hyman as canonical Android contact), `to: "self"` resolver, contact-lookup-is-Claude's-job rule, Brad-uses-WhatsApp-not-iMessage rule + his number, "add new channel patterns here the same turn you learn them" rule.
10. **§ Supabase RLS hygiene** (283-347) — all 5 hygiene gates verbatim in substance, Standing release checklist with all 4 SQL queries kept (operational reference), migration template code block, "Why this is in stone, not memory" justification.
11. **§ No-internal-Supabase-project-name externally** (349-379) — rule, where-the-rule-applies bullet list (every bullet kept — individually load-bearing), project-ref-also-forbidden rule, 4-option substitutions table, pre-commit-guardrail rule, retroactive-cleanup guidance.
12. **§ Gitleaks + mirror backups** (381-435) — rule, Part 1 (gitleaks pre-commit + pre-push hooks: binary path, config path, hooks paths, git config commands, what-gets-blocked bullets, bypass rule, maintenance bullets), Part 2 (nightly mirror backup: script path, backup root, logs paths, schedule + plist install commands, when-to-update-the-script rule), Part 3 (4-item standing behavior list).

## Paragraphs / sentences promoted to Mnestra (T4: any of these surviving inline = trim insufficient)

- "(Format updated 2026-04-26 from prior `TermDeck session wrap — YYYY-MM-DD HH:MM ET — <summary>`.)" subject-line history
- "(Added 2026-04-26 after a plain-text draft slipped through.)" rich-text history
- "(Amended 2026-05-21 after Session 53 emitted a draft with entity-encoded structural tags despite the 2026-04-26 amendment — the rich-text-required rule did NOT prevent the encoding mistake. The failure mode is mechanical: when composing JSON tool-call arguments, the LLM's instinct to 'be safe with special characters' triggers entity-encoding even though JSON itself permits raw angle brackets. The pre-flight scan is the load-bearing defense.)" — promoted; the pre-flight-scan defense rule itself STAYS inline
- "(History: rule added 2026-05-05; the `cd` prefix amendment landed 2026-05-14 14:35 ET after five panels failed `claude --resume <uuid>` from a sibling cwd — `claude --resume <uuid>` alone is never acceptable.)" resume-command history
- "Sprint 51.5 was all-Claude (4 worker lanes, no auditor). The lanes shipped a structurally-correct sprint that nonetheless missed source_agent emission misattribution + Phase-B-doesn't-refresh-hook GAP + upsert on_conflict idempotency bug + migration constraint-guard scoping nit — because all four workers shared the same Claude training and made the same shared assumptions. Sprint 51.6 added Codex as auditor (T4); Codex caught all four bugs in 14 minutes by reading the workers' WIP rather than waiting for FIX-LANDED. Triangulation prevents shared blind spots from reaching production." → replaced inline with one-sentence "Why: shared-model lanes share blind spots; an out-of-distribution auditor breaks the tie."
- "Codex compacted mid-Sprint-51.6 at 20:53 ET and again mid-Sprint-51.7 around 11:25 ET" timestamps — promoted; the auditor-compacts-during-long-sprints principle stays
- "This shape mismatch caused Sprint 51.7 T3 to sit idle for many minutes — T3's idle-poll regex matched Codex-shaped posts only and missed T1's bare `[T1] FIX-LANDED 11:25 ET` post entirely (T3 needed Joshua's manual 'bump' to wake up)." case → Mnestra; rule "without uniform shape, cross-lane visibility breaks" stays
- "Why. Sprint 65 (2026-05-16): the Codex auditor panel (T4) died after posting FINAL-VERDICT GREEN — Joshua could not harvest memories from it or have it run its session-close. A lane that crashes, is killed, or compacts badly cannot self-harvest. But T4's entire substance — both AUDIT-REDs, every AUDIT-CONCERN, four CHECKPOINTs, the FINAL-VERDICT — was already durable in STATUS.md, because the 3+1+1 discipline requires every lane to post verbosely there as it works. The panel is disposable; STATUS.md is the substrate." → replaced inline with: "Why: workers may crash, compact, or hang mid-sprint; STATUS.md is the only substrate that survives."
- "Added 2026-05-16 at Sprint 65 close, after the Codex auditor panel died post-FINAL-VERDICT and Joshua directed: 'I will just have you execute all the protocols yourself, harvesting everything needed from all four terminals. That should be a new rule in the future.'" footer → Mnestra
- "(added 2026-05-22 after Maestro Sprint 9 substrate-failure incident)" PREFLIGHT header parenthetical — compressed to just "PREFLIGHT: substrate verification"
- "Sprint 9 (Maestro, 2026-05-21) shipped correct code via Agent-tool sub-agents but left Joshua with zero observability, made CPU monitoring + `/poke` recovery structurally unusable, and produced a fictional chat narrative using TermDeck vocabulary to describe sub-agent state. See Maestro `docs/SPRINT-9-FAILURE-REPORT-2026-05-21.md`. Recall corrective via `memory_recall(query='3+1+1 SUBSTRATE DISCIPLINE never substitute Agent-tool')`." — the case → Mnestra; the principle "Agent-tool dispatch is reserved for ORCH-internal recon, never primary worker dispatch" stays; the `memory_recall` corrective pointer also stays inline as an active recall instruction
- "Symptom: 3 of 4 panels auto-fire, the 4th sits at a visually populated input box waiting for a human to press Enter. This caused Joshua's overnight broken-sleep incident during ClaimGuard Sprints 4–5 and the Sprint-36 inject on 2026-04-27." overnight-sleep story → Mnestra; rule "two-stage submit, never single-stage" stays
- "That cost Joshua broken sleep on 2026-04-26 during ClaimGuard Sprint 4-to-5 and again on 2026-04-27 during Sprint 36 inject. It must never happen again." — closing emotional appeal **STAYS INLINE** (the appeal makes the rule stick)
- "Until 2026-05-14 the WhatsApp half of this rule stopped at pre-fill ('ready for him to press send'); the AppleScript Return-key trick (documented below) closes that gap." historical drift note → Mnestra; current rule (send-not-pre-fill) stays
- "(2026-04-26 for the open step, 2026-05-14 for the AppleScript send step)" sanction-history parenthetical → Mnestra
- "Verified working 2026-05-14 09:30 ET sending a confirmation to Joshua's own number (`+17735736571`) from the Podium UX session." verification breadcrumb → Mnestra
- "The `mnestra_doctor_vault_secret_exists` family (Sprint 51.5) shipped this hole — Brad's morning-of-2026-05-06 sweep caught it externally." gate-2 case → Mnestra
- "A search this morning even bit memory_hybrid_search itself in this very session — the function failed because of related schema-cache shape — proving the lint is operationally relevant, not pedantic." gate-3 case → Mnestra; rule "search_path on every function" stays
- "podium-prod (Chopin Festival applicant portal, Prisma + Next.js stack) shipped 17 tables with RLS off — including `RefreshToken`, `WixSyncConfig`, `User`, `Applicant`, `Application`, `Review`, `Tearsheet`, `Document` — until Supabase's own advisor email flagged it on 2026-05-06 and we locked it down across four projects in one wave (podium-prod, claimguard-ai, [internal-name-elided], termdeck-dogfood)." specific-table-list → Mnestra; rule "RLS on every public-schema table, in the same migration that creates the table" stays
- "Brad's external sweep on 2026-05-06 surfaced four open INSERT policies + five anon-callable SECURITY DEFINER doctor functions in shipped Mnestra v0.4.3; on a project where service-role is the only intended writer, the holes weren't being USED, but the next anon-key leak (CI image, leaked .env, scraped tutorial repo) would have made them exploitable. The 'convention only' pattern is exactly the failure mode the orchestrator-enforcement rule warns about — codify in migrations, don't rely on operator discipline." Why-stone-not-memory case → Mnestra; principle "convention-only fails silently; codify in migrations" stays
- "Added 2026-05-06 after Brad's flag landed. Cross-references: project-scoped follow-up tracked in `~/Documents/Graciella/engram/docs/SECURITY-HARDENING-2026-05-06.md` for the Mnestra-specific fix; same checklist applies to every other Supabase project before each release." footer → Mnestra
- The "Added 2026-05-06 — promoted from project-scoped memory to global standing rule after the literal was leaked three times in the same publish wave (Mnestra@0.4.4 CHANGELOG, migration 019 comment, SECURITY-HARDENING doc). Memory-scoped rules aren't enforced reliably enough for this one — the cost of a single leak is high (embarrassment, professionalism), so it goes in stone." footer → Mnestra; replaced inline with one line: "Codified in stone because the cost of a single external leak is high."
- "Verified working 2026-05-06. Test: created a temp repo with a fake Supabase JWT in `leak.js`, attempted commit, hook fired, `gitleaks` reported 2 leaks (the JWT and the project-ref custom rule), commit returned exit code 1, `git log` shows no commit landed. Behavior is correct." gitleaks verification → Mnestra; replaced with "Verified 2026-05-06."
- "Verified working 2026-05-06. First run: 8 repos cloned in ~8 seconds (termdeck, engram, rumen, podium, pvb, DOR, gorgias-ticket-monitor, joshuaizzard-dev). Two skipped because they aren't git repos (rag-system, SchedulingApp) — those should be `git init`'d separately if Josh wants them backed up." mirror-backup verification → Mnestra; replaced with "Verified 2026-05-06."
- The "Added 2026-05-06 immediately after the multi-project Supabase RLS lockdown and the Mnestra 0.4.4 → 0.4.5 docs hygiene scrub. The same morning's incident was the trigger: schema-level holes in 4 projects + accidental internal-project-name references in 3 npm tarball artifacts. Mechanical defense > memory rules; hooks > vigilance." gitleaks-section footer → Mnestra; replaced inline with one line: "Mechanical defense > memory rules; hooks > vigilance."

## 12 Mnestra `memory_remember` calls I will issue (kitchen-level per § Kitchen vs recipes)

1. `(decision, project=termdeck)` — Sprint 51.5 → 51.6 case for 3+1+1: all-Claude sprint missed 4 bugs (source_agent timing misattribution, Phase-B-doesn't-refresh-hook, upsert idempotency, migration constraint scope); Codex auditor on Sprint 51.6 caught all 4 in 14min by reading WIP. Why: shared training = shared blind spots. Out-of-distribution auditor breaks the tie. THIS is the canonical evidence for adding a non-Claude auditor lane to every multi-lane sprint.
2. `(decision, project=global)` — Auditor-panel-compacts-mid-sprint cases. Codex compacted mid-Sprint-51.6 (20:53 ET) and mid-Sprint-51.7 (~11:25 ET). On compact, in-context audit state evaporates. Recovery: orchestrator-side re-bootstrap inject pointing at the auditor's most recent STATUS.md CHECKPOINT (~30s recovery in 51.6). Operational truth: STATUS.md is the only durable substrate post-compact. Mandate `### [T4-<MODEL>] CHECKPOINT` ≥ every 15 min.
3. `(decision, project=global)` — Lane-shape-mismatch case (Sprint 51.7). T3 idle-poll regex `^### \[T1\] DONE` missed T1's bare `[T1] FIX-LANDED 11:25 ET` post (no `### ` prefix); T3 sat idle until Joshua manually bumped. Cross-lane visibility depends on uniform post shape. Mandate: all lanes — workers AND auditor — use `### [Tn] VERB ...` with `### ` prefix REQUIRED.
4. `(decision, project=termdeck)` — Sprint 65 close: Codex auditor panel died after FINAL-VERDICT GREEN. Joshua could not harvest memories from it or run its session-close. But T4's substance was already durable in STATUS.md because the 3+1+1 discipline made every post verbose. The panel is disposable; STATUS.md is the substrate. Lesson: orchestrator harvests centrally from STATUS.md at sprint close — never rely on each lane to self-harvest before `/exit`. Joshua codified: 'I will just have you execute all the protocols yourself.'
5. `(decision, project=global)` — Maestro Sprint 9 (2026-05-21) substrate-failure incident. Claude-Code-Opus orchestrator dispatched primary worker work via Agent-tool sub-agents instead of TermDeck panels, leaving zero observability + structurally-unusable CPU/`/poke` recovery + a fictional chat narrative using TermDeck vocabulary. See Maestro `docs/SPRINT-9-FAILURE-REPORT-2026-05-21.md`. THIS is why every sprint dispatch must begin with `GET /api/sessions` substrate verification + Agent-tool reserved for ORCH-internal recon only + rationalization-as-signal detector.
6. `(decision, project=global)` — Overnight-broken-sleep cases for inject-submit pattern. Single-stage `<text>\\x1b[201~\\r` injection: ClaimGuard Sprints 4-5 (2026-04-26) and TermDeck Sprint 36 (2026-04-27) both produced 3-of-4-panels-fire-1-stuck-waiting-for-Enter. Joshua was woken at sleep hours both times. Root cause: non-deterministic OS PTY write chunking. Fix: two-stage submit (paste body, settle 400ms, submit `\\r` alone). THE cardinal sin is leaving a panel waiting for a human Enter press.
7. `(decision, project=termdeck)` — Session-end-email failure modes: (a) 2026-04-26 plain-text draft slipped through; rule amended to require `htmlBody`. (b) 2026-05-21 Session 53 emitted draft with entity-encoded structural tags (`&lt;p&gt;` instead of `<p>`) despite the 2026-04-26 amendment — the rich-text-required rule did NOT prevent the encoding mistake. Failure mode is mechanical: LLM's instinct to "be safe with special characters" triggers entity-encoding even though JSON itself permits raw angle brackets. Fix: pre-flight scan of htmlBody for `&lt;` adjacent to structural tag names.
8. `(decision, project=global)` — Supabase RLS holes in shipped Mnestra v0.4.3 (Brad sweep 2026-05-06): 4 PUBLIC INSERT policies + 5 anon-callable SECURITY DEFINER doctor functions (`mnestra_doctor_vault_secret_exists` family) + 6 functions with mutable search_path. On a service-role-only-writer project the holes weren't being USED, but next anon-key leak would have made them exploitable. Plus: podium-prod shipped 17 tables with RLS off (incl. RefreshToken, WixSyncConfig, User, Applicant, Application, Review, Tearsheet, Document) until Supabase's advisor email flagged it the same morning. Net: convention-only fails silently across a whole portfolio; codify in migrations + standing release SQL checklist + mechanical pre-commit hooks.
9. `(decision, project=global)` — Gitleaks + mirror-backup install verification (2026-05-06). Gitleaks: temp repo + fake Supabase JWT in `leak.js`, attempted commit, hook reported 2 leaks (JWT + project-ref custom rule), commit returned exit code 1. Mirror backup first run: 8 repos cloned in ~8s (termdeck, engram, rumen, podium, pvb, DOR, gorgias-ticket-monitor, joshuaizzard-dev); rag-system + SchedulingApp skipped (not git repos).
10. `(decision, project=global)` — WhatsApp inject pattern history. 2026-04-26: Joshua sanctioned the `open <wa.me URL>` deep-link step as permanent standing authorization. 2026-05-14: AppleScript Return-key trick added (`sleep 3` + `osascript ... key code 36`) — closes the half-step where messages sat pre-filled waiting for him to press send. Verified 2026-05-14 09:30 ET on Joshua's own number (`+17735736571`) from the Podium UX session. The draft-and-wait pattern is retired.
11. `(decision, project=global)` — Resume-command history. Rule added 2026-05-05. The `cd <cwd> && claude --resume <uuid>` prefix amendment landed 2026-05-14 14:35 ET after five panels failed `claude --resume <uuid>` from a sibling cwd — `claude --resume` resolves per-project-directory, not globally. Always render as a `<pre>`-wrapped one-liner; verify the JSONL file exists at `~/.claude/projects/<encoded-cwd>/<session-uuid>.jsonl` before rendering.
12. `(decision, project=global)` — No-internal-Supabase-project-name leak history. Same publish wave on 2026-05-06: leaked the literal 3 times in shipped artifacts (`@jhizzard/mnestra@0.4.4` CHANGELOG, `migrations/019_security_hardening.sql` comment, SECURITY-HARDENING.md doc). Promoted from project-scoped memory to global standing rule that same morning. Cost of a single external leak: high (embarrassment, professionalism). Cost of mechanical defense (gitleaks + global hook): one-time. Hooks > vigilance.

## Section line-count delta estimate

| Section | Pre lines | Target lines | Trim mechanism |
|---|---:|---:|---|
| Title + § Time check | 7 | 6 | Light compression |
| § Session-End Email | 19 | 13 | Promote 4 history parentheticals |
| § Check Memory First | 9 | 9 | Kept |
| § RAG Memory System | 56 | 50 | Light compression of "Before Context Gets Long" backstory |
| § Project Directory Map | 40 | 35 | Light compression of "Map corrections" paragraph |
| § Sprint role 3+1+1 | 27 | 19 | Promote Sprint 51.5/51.7 cases, compress "Why" lines |
| § Orchestrator-centralized close-out | 16 | 11 | Promote Sprint 65 case |
| § 3+1+1 inject mandate | 60 | 37 | Promote Sprint 9 Maestro + overnight-sleep + 2026-04-27 cases |
| § Never copy-paste — always inject | 37 | 25 | Promote 2026-04-26/05-14 historical parentheticals |
| § Supabase RLS hygiene | 65 | 47 | Promote gate-2/3/5 cases + Brad-flag history |
| § No-internal-Supabase-project-name | 31 | 20 | Promote leak-three-times history |
| § Gitleaks + mirror backups | 55 | 36 | Promote 2× "Verified working" detail blocks + footer |
| **TOTAL** | **434** | **~265** | **~40% trim** |

## Operational order

1. **Now:** post FIX-LANDED for the AUDIT-RED scrub + a short FIX-PROPOSED pointer in STATUS.md → this file.
2. **Concurrent:** do deliverable 3.3 (`orch` layout retirement) and 3.1 (BACKLOG rewrite). Neither depends on T4 audit of the trim plan.
3. **After T4 has reviewed** (AUDIT-CONCERN issued, or implicit clear after reasonable wall-clock): issue the 12 `memory_remember` calls, then edit `~/.claude/CLAUDE.md`.
4. **Verify post-edit:** `wc -l ~/.claude/CLAUDE.md` shows ~265 ± 15 lines; T4 enumerates the 12 rule sections from the trimmed file and confirms each is present in substance.
