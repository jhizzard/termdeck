# Sprint 78 — STATUS

<!-- POST SHAPE — ALL lanes incl. workers: ### [T<n>] VERB 2026-MM-DD HH:MM ET — <gist>
     VERB ∈ FINDING | FIX-PROPOSED | FIX-LANDED | AUDIT-PASS | AUDIT-FAIL | CHECKPOINT | HANDOFF-REQUEST | HANDOFF-ACK | DONE
     Auditor posts as [T4-CODEX]. Tolerant cross-lane idle-poll regex: ^(### )?\[T<n>\] DONE\b
     STATUS.md is APPEND-ONLY — never edit a prior post; add a new one. -->

## Sprint
**Sprint 78 — Doctrine substrate + agent-facing advisory + recall telemetry.** 3+1+1 (T1/T2/T3 Claude workers + T4 Codex auditor + ORCH). Plan: `PLANNING.md`. Design: `ULTRAPLAN-2026-06-12.md`. §5 decisions locked in PLANNING §2.

## Lane status (one-line, updated by each lane's latest post)
- **T1 — Doctrine registry core** (termdeck) — STAGED
- **T2 — Advisor MVP** (termdeck) — STAGED
- **T3 — Recall telemetry + webhook hardening** (engram) — STAGED
- **T4 — Codex auditor** (termdeck, read engram) — STAGED

## Cross-lane seams (HANDOFF-REQUEST/ACK required)
- T3 `op:'feedback'` webhook ↔ T2 flashback-clicked route (the one known cross-boundary file touch).

## Log
<!-- append below, newest at bottom -->

### [ORCH] STAGED 2026-06-13 — Sprint 78 authored: PLANNING.md + 4 lane briefs (T1–T4). Awaiting substrate verification (GET /api/sessions, 4 panels at the termdeck cwd) + two-stage inject. Workers: do NOT bump versions / edit CHANGELOG / commit — ORCH owns close-out.

### [T4-CODEX] CHECKPOINT 2026-06-13 18:57 ET — Phase 0 boot complete: memory_recall ran for auditor precedent + recent TermDeck decisions; read ~/.claude/CLAUDE.md, ./CLAUDE.md, CRITICAL-READ-FIRST, PLANNING §4/§8, STATUS, and T4 brief. Verified current STATUS has no worker FIX-LANDED yet (only ORCH STAGED), so audit is pending landed evidence from T1/T2/T3. Next: RUN-based boot anchor verification and dirty-worktree/landedness scan.

### [T4-CODEX] FINDING 2026-06-13 18:58 ET — Phase 1 landedness scan: Sprint 78 worker artifacts are not landed in this worktree yet. Evidence: `rg --files doctrine packages/server/src/advisor packages/server/tests` exits 2 for missing `doctrine` and `packages/server/src/advisor`; `rg -n "advisory_events|ADV-ACK|/api/advisor" packages/server/src/index.js packages/server/src/advisor packages/server/tests` exits 2 for missing advisor dir and only finds pre-existing `/input` tests; engram `ls -1 migrations` stops at `026_memory_inbox.sql`, so migration 027 is absent. `curl -s http://127.0.0.1:3000/api/sessions` exits 7, so no local TermDeck substrate is reachable on :3000 from this panel. Pending: wait for T1/T2/T3 FIX-LANDED or final RED if asked to grade current checkout.

### [T4-CODEX] FINAL-VERDICT 2026-06-13 18:59 ET — RED on current checkout: Sprint 78 acceptance surfaces are not landed, so adversarial e2e/fatigue/five-gate probes cannot pass. T1 RED: `find . -maxdepth 3 -type d -name doctrine` returns empty; `package.json:9-24` files whitelist has no scoped `doctrine/index.js`, `doctrine/registry.jsonl`, or `doctrine/SCHEMA.md`; `npm pack --dry-run` reports 146 files and no `doctrine/` tarball entries; `node --test --test-name-pattern=doctrine packages/server/tests/**/*.test.js` ran the existing 29 server tests with no doctrine tests listed. T2 RED: `find packages/server/src -maxdepth 2 -type d -name advisor` returns empty; `rg -n "advisor.onTrigger|/api/advisor|advisory_events|ADV-ACK|agent-injected" packages/server/src/index.js packages/server/tests` exits 1; the only verified relevant base anchors are the pre-existing error hook at `packages/server/src/index.js:2134` and `/input` route at `packages/server/src/index.js:2353/2467/2539`, with no advisor integration. T3 RED: `find ~/Documents/Graciella/engram/migrations -maxdepth 1 -type f -name '027*'` returns empty; `rg -n "memory_recall_log|log_recall_hits|recall_count|last_recalled_at" ~/Documents/Graciella/engram/migrations ~/Documents/Graciella/engram/src` finds no Sprint 78 recall telemetry objects; `~/Documents/Graciella/engram/src/webhook-server.ts:341-344` still reads JSON then calls `dispatchOp` directly, and `:379-380` still `server.listen(port, ...)` without a 127.0.0.1 host. Required T4 probes blocked by absence: forbidden-string loader screen, malformed-overlay fail-soft, T2 different-error e2e, 6x/2-panel fatigue budget, ADV-ACK, T3 no-secret/with-secret curl, K-row recall logging, five-gate SQL, memory_get cited, and purge registration.

### [ORCH] NOTE 2026-06-13 19:05 ET — SUBSTRATE PORT :3001. The live TermDeck server for this sprint is on :3001 (non-default; :3000 was occupied by the daily-driver instance). Any LIVE-substrate probe (e.g. reproducing T2's advisor e2e against the running server) targets http://127.0.0.1:3001, NOT the documented default :3000. Acceptance e2e can also run via the npm-test fence harness (boots its own ephemeral server — no live port needed). T4: your :3000 check refused for this reason — re-check against :3001 (and if your sandbox blocks localhost entirely, reproduce via the fence harness).

### [T4-CODEX] CHECKPOINT 2026-06-13 19:06 ET — Phase 1 correction acknowledged: re-ran live-substrate probe against :3001. Evidence: `lsof -nP -iTCP:3001 -sTCP:LISTEN` shows `node` listening on `127.0.0.1:3001`; `curl -v --max-time 2 http://127.0.0.1:3001/api/sessions` fails inside this Codex sandbox with `Immediate connect fail ... Operation not permitted`, so live :3001 HTTP is sandbox-blocked here. Continuing audit with npm-test fence harness for T2 e2e when advisor lands. Latest worker state still has no T1/T2/T3 FIX-LANDED in STATUS; pending worker artifacts remain as listed in the 18:58 landedness scan.

### [T1] FINDING 2026-06-13 19:11 ET — Anchors re-verified (briefs-are-hypotheses); 3 decisions logged; starting build
All brief anchors hold against the live tree:
- `database.js`: `flashback_events` INLINE_SQL `:26-43`, `new Database(dbPath)` `:51`, exec at `:180`, `return db` `:185`, exports `:225`. Adding `doctrine_events` sibling via idempotent CREATE, DDL sourced from `doctrine/index.js`. ✓
- `package.json` files[] has NO doctrine entry (`:9-24`). Test glob = `packages/server/tests/**` + cli/stack-installer/mcp-bridge/web-chat-driver ONLY; root `tests/` excluded (`:36`). Tests → `packages/server/tests/doctrine-*.test.js`. ✓
- gitleaks v8.30.1 + `~/.gitleaks.toml` (3806 b) present; `--no-git --source <dir>` verified clean. Screen = temp-dir + JSON report (StartLine→entry map), fail-soft if gitleaks absent. ✓
- `doctrine/`, `~/.claude/doctrine/`, `~/.termdeck/doctrine-local/` ALL ABSENT (expected) — loader treats both overlays as optional-absent; overlay paths resolved at CALL-time (os.homedir()) + opts-overridable so tests don't mutate HOME. ✓
- stack-installer 4KB-head stamp `_readHookSignatureVersion` slice(0,4096) `:646/667` — NOT replicating; doctrine uses FULL-FILE sha256 (avoids INSTALLER-PITFALLS 4KB-head class).

**3 decisions (flagging for ORCH/T4 review):**
1. **files[] = SCOPED, not `doctrine/**`** — brief scope-text says `doctrine/**` but PLANNING §8.4 (which supersedes) says scoped entries; using `doctrine/index.js` + `doctrine/registry.jsonl` + `doctrine/SCHEMA.md`.
2. **stack-installer dest (brief left open)** — load-bearing delivery = main-pkg files whitelist (loader reads package-relative, `npm pack`-gated). stack-installer vendors a READ-ONLY inspectable copy to `~/.claude/doctrine/registry.shipped.jsonl` (`.shipped` ≠ `.local` overlay → no collision), baked audience:'all'+active only, full-file-hash stamped. NOT a loader read-path (doctrine/index.js is always co-located with the package registry).
3. **`doctrine_events` DDL canonical in `doctrine/index.js`** (exported `DOCTRINE_EVENTS_SQL`); `database.js` execs it into the shared handle (one-way dep server→doctrine; doctrine requires only node builtins → no circular require, no 2nd DB file).

### [T2] FINDING 2026-06-13 19:08 ET — Booted; anchors re-verified against live tree. Two brief reconciliations (briefs-are-hypotheses) I'm acting on, flagged for T4: (1) **onTrigger placement.** Brief says call advisor.onTrigger "AFTER the existing toast send" (index.js:2198), but the toast only fires on the Mnestra-HIT path inside `if(hit){ if(ws ready){…send…} }` (:2183-2232) and returns early on no-hit (:2181). The advisor is registry-only / Mnestra-independent (A1), so gating it behind the toast would MISS every registry-matching error that has zero Mnestra memories — exactly the e2e case (raw shell, project=null). Resolution: I place the fail-soft `advisor.onTrigger(sess,ctx,{db})` at handler ENTRY (~:2136, after `question` is built, before queryMnestra), so it fires on EVERY detected error. Toast frame still gets `agent_injected` (set synchronously by onTrigger's registry match, read in the .then). (2) **Idle gate vs status clobber.** session.js `_detectErrors` sets `meta.status='errored'` (:458) BEFORE calling onErrorDetected (:504), so status is ALWAYS 'errored' at trigger time. Treating only literal 'idle' as deliverable would never deliver. Resolution: deliverable = status ∉ {thinking,editing,starting,exited} (errored/idle/active/listening deliver; thinking/editing queue). "Never interrupt a turn" preserved (thinking→queue). (3) T1 `doctrine/index.js` not landed yet — consuming against PLANNING §4 contract with a fail-soft null-object stub (loadDoctrine→[] ⇒ advisor no-ops; matches the "delete registry ⇒ no-op+one warning" acceptance); tests inject a fake registry via deps.doctrine. Will swap to T1's real module on its FIX-LANDED. Delivery target gated to non-Claude PTY panels (type ∉ {claude-code, web-chat}).

### [T4-CODEX] AUDIT-PASS 2026-06-13 19:08 ET — T2's two brief reconciliations are justified by live code, not just worker preference. Evidence: `packages/server/src/index.js:2134-2181` builds `question`, calls `mnestraBridge.queryMnestra`, then returns early on no hit before any toast, so an advisor call placed strictly after `sess.ws.send(frame)` would miss registry-only/no-memory errors and fail the Sprint 78 e2e shape. `packages/server/src/session.js:457-504` sets `this.meta.status = 'errored'` before invoking `this.onErrorDetected(...)`, so a literal idle-only gate at trigger time would also fail. Pending: verify the final T2 implementation preserves fail-soft/no-Mnestra-T-ERR/no-PTY-spam behavior when code lands.

### [T4-CODEX] AUDIT-FAIL 2026-06-13 19:11 ET — T2 WIP violates the Sprint 78 delivery contract: `packages/server/src/advisor/deliver.js` does NOT reuse the v1.10.1 `/input {submit:true}` contract and instead reimplements two-stage PTY writes locally. Evidence: `deliver.js:17-24` claims it "inherits" `{submit:true}` without using the route; `deliver.js:70-85` writes bracketed paste then a separate lone `\r`; `deliver.js:105-106` sends `\r\r\r` as a local cr-flood. `rg -n "submit\\s*:\\s*true|submit === true|writeBytes\\('\\r'\\)" packages/server/src/advisor/deliver.js packages/server/src/index.js` finds `submit === true` only in `index.js:2467/2539` and no actual `submit:true` use in advisor. PLANNING/T2 acceptance requires verifying `{submit:true}` is used rather than a raced separate `\r`; this WIP cannot pass that gate until delivery calls the server-sequenced submit path or exposes the same production helper behind `/input` and advisor.

### [T4-CODEX] AUDIT-FAIL 2026-06-13 19:12 ET — T2 WIP has a separate suppression-contract bug: it consumes T1 `shouldNotify` as `{ok:false}` but T1 returns `{notify:false}`. Evidence: `packages/server/src/advisor/suppress.js:216-228` calls `doctrine.shouldNotify(ruleId, dedupeKey)` and suppresses only when `verdict.ok === false`; `doctrine/index.js:450-496` defines `shouldNotify(...)` and returns `{ notify:false, reason:'lane-hourly-budget-exceeded' }`, `{ notify:false, reason:'rule-cooldown-active' }`, or `{ notify:true, ... }` — never `ok:false`. Result: T1's 30-min per-rule cooldown and 3-advisories/lane/hour overflow can be silently ignored, so the required fatigue/budget probe cannot be trusted until T2 checks `verdict.notify === false` and passes lane/session/surface opts into T1.

### [T4-CODEX] AUDIT-FAIL 2026-06-13 19:13 ET — Cross-lane T1/T2 WIP integration gap: the production registry currently has ZERO entries reachable by T2's planned `loadDoctrine({event:'T-ERR', audience:'all'})`, so the advisor cannot fire for any real terminal error. Evidence: `doctrine/index.js:343-355` filters by `opts.event` and keeps only entries whose `trigger` is absent/`always`/contains the event; `node -e "const d=require('./doctrine'); console.log(d.loadDoctrine({event:'T-ERR',audience:'all',noCache:true,gitleaksBin:'/no/such/gitleaks'}).length)"` returns `0`; `node -e` over `doctrine/registry.jsonl` shows triggers only `pre-release`, `sprint-inject`, `status-append`, `sprint-audit`, `sprint-coordination`, `commit`, `compaction-near`, `pre-publish`, `session-end`, `messaging` — no `T-ERR` or `always`. Required T4 e2e says force a registry-matching non-Claude error; that is impossible until T1 adds at least one safe universal T-ERR/always entry or T2 intentionally queries a broader event set.

### [T3] FINDING 2026-06-13 19:08 ET — Boot anchors verified against the live engram tree; all brief hypotheses confirmed + 3 deliberate hardening decisions logged (flagging for T4)
- **Migration number = 027** ✓ — `ls migrations/` highest is `026_memory_inbox.sql` (025 + 026 present). Landing `migrations/027_recall_telemetry.sql`.
- **Secret-gate seam (PLANNING §8.5 confirmed):** stale `:282-303` is inside `handleObservation`; `dispatchOp` is the exported fn at `webhook-server.ts:89`; the live pre-dispatch seam is the `createServer` request handler at `webhook-server.ts:341-344` (`const result = await dispatchOp(body, deps)`). `webhook-server.ts:379` `server.listen(port, cb)` binds all interfaces (no host arg) ✓.
- **HARDENING-1 (gate placement, beyond brief-literal — for T4):** placing the auth gate at the **handler top with `/healthz` the sole exemption**, not "only before dispatchOp." `GET /observation/:id` (`:352`) is a memory-content **read-exfil** path outside `dispatchOp`; the mission ("never a remote memory-poisoning endpoint") + the LAN-bind future require it authed too. Still satisfies acceptance (`curl /mnestra` w/o secret → 401, dispatchOp never reached). `/healthz` stays open (liveness only, aggregate counts, no content).
- **HARDENING-2 (telemetry test isolation):** `logRecallHits`/`markRecallFeedback` resolve their Supabase client via `getSupabase()` internally (try/catch, fire-and-forget) — NOT the recall fns' injected fake client. Existing recall/search/layered/read-after-write test fakes are therefore **never touched** by telemetry writes (no fake-RPC-name collisions, event-ordering assertions stay pristine) regardless of ambient creds. Zero churn to those suites.
- **HARDENING-3 (constant-time compare):** secret check compares `sha256(presented)` vs `sha256(configured)` via `crypto.timingSafeEqual` (fixed 32-byte buffers → no length/early-out leak). Header accepted as `x-mnestra-secret: <s>` OR `authorization: Bearer <s>`. **Fail-CLOSED** when `MNESTRA_WEBHOOK_SECRET` absent (every request 401), fail-SOFT for process (log once, no crash). Secret read from env then `~/.termdeck/secrets.env` (KEY=VALUE parse, no new dep); the key is NOT yet present in secrets.env (operator sets it).
- **Tests requiring auth-aware updates (my lane only):** `webhook-server.test.ts` (1 HTTP malformed-JSON test) + `webhook-read-after-write.test.ts` (real HTTP POSTs) — threading the test secret + header. `webhook-propose.test.ts` (dispatchOp-direct) + `db-endpoint.test.ts` (pure units) need no change.
- **Migration design:** 3 SECURITY DEFINER RPCs — `log_recall_hits(jsonb)` (insert-K returned-set + ONE statement-level denorm bump, no per-row storm), `mark_recall_feedback(jsonb,text)` (most-recent-row cited/dismissed), `purge_recall_log(int)` (90d age-out) — all five-gated. `recall_count`/`last_recalled_at` denorm on `memory_items` (collision-guarded per §8.6 — **Sprint 79 must NOT re-add**); pg_cron purge guarded on `pg_extension` existence (fail-soft if absent) + unschedule-before-schedule (idempotent); HARD-FAILING receipt block mirroring 026. Surface tagging: webhook recall/search stamp `surface:'webhook'`; MCP-stdio paths use native surfaces.
- **HANDOFF seam status:** I own the `op:'feedback'` receiver + the `memory_get`→`cited=true` server-side path end-to-end (PLANNING §8.3). Defining the op contract `{memory_id, event:'cited'|'dismissed'}`; NOT touching T2's client file — no HANDOFF-REQUEST needed unless T2 wires the flashback-click POST and needs the payload pinned.

Starting build now (ITEM ZERO first: webhook secret + bind).

### [T1] FIX-LANDED 2026-06-13 19:18 ET — `doctrine/index.js` + registry + DDL stable for T2; T-ERR entries added (closes T4 19:13); contract is `{notify}` not `{ok}` (re T4 19:12)
**T2: you can build against this now.** All smoke-verified on a real better-sqlite3 handle.

**Contract (re T4 AUDIT-FAIL 19:12 — `{ok}` bug is on T2's side):** `shouldNotify(rule_id, dedupe_key, opts)` returns **`{ notify, reason, outcome, recorded, route? }`** — there is NO `ok` field (never was). Suppress on **`verdict.notify === false`**. Pass `opts={ lane, surface, session_id, db, now }` so the per-lane budget binds — `lane` is the load-bearing key for the 3/lane/hr overflow (`reason:'lane-hourly-budget-exceeded'`, `route:'orch'`). `db` is auto-injected at server startup via `doctrine.setDb(db)` (in `database.js` initDatabase), so you only pass `opts.db` in tests. No-db ⇒ `{notify:true, reason:'no-db-failsoft'}` (your per-entry layer backstops). Full contract: `doctrine/SCHEMA.md` § Throttle contract. The two throttle layers STACK (PLANNING §8.2): mine = per-RULE (30-min cooldown + 3/lane/hr); yours = per-ENTRY (24h/5-per-session/1-per-10min).

**Closes T4 AUDIT-FAIL 19:13 (zero T-ERR-reachable entries):** added **5 universal `audience:'all'` `check.type:'regex'` entries with `trigger:["T-ERR"]`** — `err-git-push-rejected`, `err-pg-permission-denied`, `err-npm-publish-auth`, `err-port-in-use`, `err-gitleaks-blocked`. Verified: `loadDoctrine({event:'T-ERR', audience:'all'})` returns exactly those 5, each `_checkRegex` matches realistic error text, discipline entries (publish-before-push etc.) are correctly EXCLUDED from the T-ERR set. **Matching contract for your advisor:** load with `{event:'T-ERR', audience:'all'}`, then test each returned entry's compiled `entry._checkRegex` against the error text; a match surfaces `entry.advisory.one_line` + `procedure_path`. (Phase/lifecycle rules use other triggers and are intentionally NOT in the T-ERR set, so they won't spam on errors.)

**API surface (exported, stable):** `loadDoctrine({event,cwd,audience,noCache})`→Array (validated/regex-compiled/screened/merged; never throws; both overlays absent ⇒ repo entries), `validateEntry`, `compileTrigger`, `screenEntries`, `clearCache`, `setDb`, `recordGateEvent`, `shouldNotify`, `DOCTRINE_EVENTS_SQL`, `DOCTRINE_REGISTRY_VERSION`, enums. Delete the registry file ⇒ `loadDoctrine` returns `[]` (one logged warning) ⇒ your advisor no-ops (matches your fail-soft acceptance). If you need a schema change, open a HANDOFF-REQUEST — else I treat the signature as frozen.

Landed: `doctrine/{index.js,registry.jsonl,SCHEMA.md}` (NEW), `package.json` files[] (scoped, §8.4), `database.js` `doctrine_events` sibling (appended AFTER your `advisory_events` block — PERIPHERY WATCH: shared infra file, no clobber). Remaining T1: behavior tests + stack-installer vendoring + full test run + adversarial self-review → DONE.

### [T4-CODEX] AUDIT-FAIL 2026-06-13 19:17 ET — T1 closed the "zero T-ERR entries" half, but T2 still cannot match them: T1 now emits array triggers (`"trigger":["T-ERR"]`), while T2 `matchEntry` rejects any trigger not equal to the literal string. Evidence: `doctrine/registry.jsonl:13-17` has five `trigger:["T-ERR"]` rows; `packages/server/src/advisor/index.js:121-127` does `if (e.trigger && e.trigger !== 'T-ERR') continue;`; direct command `node - <<'NODE' ... loadDoctrine({event:'T-ERR'}); advisor.matchEntry(entries, '... non-fast-forward ...') ... NODE` returned `{count:5, ids:[...], match:null}`. The T2 e2e remains impossible until `matchEntry` accepts array triggers or trusts T1's event-filtered entries.

### [T4-CODEX] AUDIT-PASS 2026-06-13 19:18 ET — T1 partial behavior probe passed for loader/screen/throttle core. Evidence from independent `node - <<'NODE'` fixture: overlays+repo absent returned `absentCount:0` without throw; `block` on `server-monitor` was dropped (`badSeverityCount:0`); malformed JSONL skipped one bad line and loaded `good-after-bad`; local temp overlay containing the forbidden-string canary was dropped by gitleaks (`forbiddenScreenedCount:0`) and fail-soft gitleaks-absent path loaded it (`forbiddenUnscreenedWhenGitleaksAbsentCount:1`); `loadDoctrine({event:'T-ERR',audience:'all'})` returned `tErrCount:5`; in-memory SQLite throttle returned first notify ok, second same rule `rule-cooldown-active`, and 4th in-lane notify attempt `lane-hourly-budget-exceeded` with `route:'orch'`. Pending T1 gates: canonical tests in npm glob, package whitelist/tarball, stack-installer vendoring/full-file stamp, and gitleaks clean over shipped set.

### [ORCH] RELAY 2026-06-13 19:20 ET — T1 FIX-LANDED (19:18) its real doctrine module; relaying T4's three T2 AUDIT-FAILs to T2 via inject (T2 is heads-down, doesn't poll): (a) deliver.js must use /input {submit:true} not local two-stage [T4 19:11]; (b) suppress.js check verdict.notify not verdict.ok [T4 19:12]; (c) matchEntry must accept array triggers ["T-ERR"] [T4 19:17]. T4: re-audit on T2's next FIX-LANDED — I'll wake you.

### [T4-CODEX] CHECKPOINT 2026-06-13 19:21 ET — Phase 2 active: current checkout still shows T2 open failures by command, not spec. Evidence: `rg -n "verdict\\.ok|trigger !== 'T-ERR'|submit|writeBytes\\('\\\\r'" packages/server/src/advisor packages/server/src/index.js` still finds `packages/server/src/advisor/suppress.js:227` using `verdict.ok === false`, `packages/server/src/advisor/index.js:126` rejecting non-string triggers, and `packages/server/src/advisor/deliver.js:70-85` writing local paste/CR while `submit === true` remains only in the server `/input` route. `git status --short` shows T1/T2 artifacts present, with T3 engram artifacts still outside this repo for the next probe. Moving to T3 webhook + migration five-gate audit; T2 live-port e2e remains blocked by Codex sandbox on `127.0.0.1:3001`, so fallback is npm-test fence harness after T2 fixes land.

### [T4-CODEX] AUDIT-FAIL 2026-06-13 19:23 ET — T3 migration 027 passes the SQL five-gate receipt under a rollback apply, but the engram test suite is RED. SQL evidence: rollback-only `psql ... begin; -f migrations/027_recall_telemetry.sql; rollback;` exits 0 and prints `[027] memory_recall_log RLS enabled: t`, `policy count: 0`, all three RPCs `anon:f, authenticated:f, public:f; service_role:t; proconfig: search_path=public, pg_catalog`, and `receipt: five gates verified`. Test evidence: escalated `npm run test` in engram reports `tests 215`, `pass 210`, `fail 5`. Four failures are stale unauthenticated HTTP expectations after the new fail-closed gate: `tests/quarantine-proof.test.ts:537/541` starts `startWebhookServer({ port:0, ... })` and expects `/observation/<inbox-uuid>` 404 but got 401; `tests/webhook-read-after-write.test.ts:167/176` and `:231/248` start without `secret` and expect POST 200 but got 401; `tests/webhook-server.test.ts:178` starts without `secret` and expects malformed JSON 400 but got 401. Code causing the intended fail-closed behavior is `src/webhook-server.ts:480` no-secret branch and `:508-509` pre-route 401 gate, with default loopback bind at `:550`. Separate logic mismatch: `tests/recall-log.test.ts:241-242` expects `hashQuery(null)` and `hashQuery(42)` to return `''`, but `src/recall_log.ts:138-142` hashes the normalized empty string for non-string input, and the test got `e3b0c44298fc1c149afbf4c8996fb924`. T3 cannot be GREEN until tests are updated or implementation changed, then rerun.

### [T4-CODEX] AUDIT-FAIL 2026-06-13 19:24 ET — T1 behavior tests pass, but the stack-installer doctrine asset is NOT in the npm tarball. Evidence: `node --test packages/server/tests/doctrine-registry.test.js packages/server/tests/doctrine-throttle.test.js` exits 0 with `tests 27`, `pass 27`, including malformed JSONL, malformed regex, gitleaks-screen, fail-soft, T-ERR, and SQLite throttle gates. `gitleaks detect --no-git --source doctrine --no-banner` exits 0 with `no leaks found`. The installer path is wired at `packages/stack-installer/src/index.js:99-101` to `packages/stack-installer/assets/doctrine/registry.shipped.jsonl`, and `:970-978` uses full-file sha256 (`fs.readFileSync(filepath)`, no 4KB slice); an independent tail-drift probe returned `missingDest=true`, `same=false`, `tailDrift=true`. But `package.json:9-24` whitelists only `packages/stack-installer/assets/hooks/**` plus scoped top-level doctrine files, and a temp-cache `npm pack --dry-run --json` filtered to doctrine paths returns only `doctrine/index.js`, `doctrine/registry.jsonl`, `doctrine/SCHEMA.md` — no `packages/stack-installer/assets/doctrine/registry.shipped.jsonl`. Result: published `termdeck stack` would call `installDoctrineRegistry()` (`packages/stack-installer/src/index.js:1154`) but `sourcePath` would be absent and return `no-bundled-asset`; the read-only Brad copy would not land. T1 needs a package whitelist fix (and rerun pack) before GREEN.

### [T4-CODEX] AUDIT-PASS 2026-06-13 19:28 ET — T2 remediated the three prior advisor faults and passes the independent in-process e2e/fatigue/ACK/fail-soft probes. Code evidence: `packages/server/src/index.js:89/2507` and `packages/server/src/advisor/index.js:25/188` both call shared `submitToPty`; `packages/server/src/pty-submit.js:34-69` owns the single body→settle→lone-CR submit helper; `rg -n "writeBytes\\('\\\\r'|writeBytes\\('\\\\r\\\\r\\\\r'|trigger !== 'T-ERR'|verdict\\.ok" packages/server/src/advisor packages/server/src/index.js` returns no stale hits; `packages/server/src/advisor/index.js:122-139` accepts array triggers; `packages/server/src/advisor/suppress.js:222-237` calls `doctrine.shouldNotify(ruleId,dedupeKey,{db,lane:sessionId,surface:'inject-advisory',session_id:sessionId,now})` and suppresses on `verdict.notify === false`. Runtime evidence from a real-registry + in-memory SQLite harness using a distinct `EADDRINUSE` error class: e2e returned `{matched:true,delivered:true,ruleId:'err-port-in-use'}`, delivery returned `{delivered:true,agentInjected:true,finalStatus:'idle'}`, `advisory_events` row had `delivered:1, agent_injected:1`, `doctrine_events` row had `lane:'p1', outcome:'notified'`, and `ADV-ACK err-port-in-use` flipped `acked_at` to `2026-06-13T20:01:00.000Z`. Fatigue probe (same error 6x across `p1/p2`) produced exactly one injected advisory (`sentCount:1`) and five suppression rows with reasons: `cooldown`, `dup_key`, `cooldown`, `dup_key`, `cooldown`; `failSoftNoRegistry:null` for an empty registry. TermDeck `npm run test` now exits 0 with `tests 817`, `pass 812`, `fail 0`, `skipped 5`.

### [T4-CODEX] AUDIT-PASS 2026-06-13 19:29 ET — T3 remediated the engram test failures; recall telemetry + webhook hardening now pass both SQL and test gates. SQL evidence remains the rollback-only `psql ... begin; -f migrations/027_recall_telemetry.sql; rollback;` exit 0 with `[027] memory_recall_log RLS enabled: t`, `policy count: 0`, all three RPCs `anon:f, authenticated:f, public:f; service_role:t; proconfig: search_path=public, pg_catalog`, and `receipt: five gates verified`. Test evidence: escalated `npm run test` in engram now exits 0 with `tests 215`, `pass 215`, `fail 0`. Source fixes observed by grep: `src/recall_log.ts:138-140` now returns `''` for non-string `hashQuery`; `tests/webhook-server.test.ts:178/182` starts malformed-JSON HTTP with `secret:'wh-test-secret'`; `tests/webhook-read-after-write.test.ts:171/180` and `:252` start with `secret: RAW_SECRET`; `tests/migration-027-hygiene.test.ts:41` and neighboring tests cover gates 1-5 for migration 027. Webhook hardening line evidence remains `src/webhook-server.ts:480` fail-closed no-secret warning, `:508-509` pre-route 401 gate for every route except `/healthz`, and `:550` `server.listen(port, host)` with default host `127.0.0.1`.

### [T4-CODEX] FINAL-VERDICT 2026-06-13 19:30 ET — RED. T2 is GREEN by in-process e2e/fatigue/ACK/fail-soft probe and TermDeck `npm run test` (`tests 817`, `fail 0`); T3 is GREEN by rollback SQL five-gate receipt and engram `npm run test` (`tests 215`, `fail 0`). T1 remains RED on the publish/tarball gate: `packages/stack-installer/src/index.js:101` resolves the shipped doctrine asset to `packages/stack-installer/assets/doctrine/registry.shipped.jsonl`, `:988` uses that as the default source, and `:1157` calls `installDoctrineRegistry()` during stack install, but `package.json:9-24` whitelists `packages/stack-installer/assets/hooks/**` only and omits `packages/stack-installer/assets/doctrine/**`. Final command evidence with temp npm cache: `npm pack --dry-run --json` filtered to doctrine paths returns only `doctrine/index.js`, `doctrine/registry.jsonl`, `doctrine/SCHEMA.md`; the required `packages/stack-installer/assets/doctrine/registry.shipped.jsonl` is absent. Published `termdeck stack` would therefore hit the fail-soft `no-bundled-asset` path and not install the read-only Brad doctrine copy. Required fix before GREEN: add the stack-installer doctrine asset to the package whitelist, rerun `npm pack --dry-run --json`, and verify the filtered doctrine paths include both top-level doctrine files and `packages/stack-installer/assets/doctrine/registry.shipped.jsonl`.

### [T1] FINDING 2026-06-13 19:31 ET — re T4 AUDIT-FAIL 19:24: cross-package false positive — the asset ships in the package that RUNS the function. No code change; refuting with commands.
T4's 19:24 checked the **root `@jhizzard/termdeck`** tarball, but `installDoctrineRegistry` is defined in and invoked ONLY from the **separate `@jhizzard/termdeck-stack`** package, whose tarball DOES ship the asset. Four commands:
1. **Package identity:** `node -e` on `packages/stack-installer/package.json` → `name: @jhizzard/termdeck-stack`, `bin: {"termdeck-stack":"./src/index.js"}`, `files: ["src/**","assets/**",...]`. So `npx @jhizzard/termdeck-stack` → `src/index.js main()` → `installDoctrineRegistry` (`:1154`).
2. **Asset ships in the package that runs it:** `cd packages/stack-installer && npm pack --dry-run --json` → `assets/doctrine/registry.shipped.jsonl` **present** (10.7kB). `sourcePath` (`__dirname/../assets/doctrine/registry.shipped.jsonl`) resolves to a file present in the stack tarball → returns `refreshed`, NOT `no-bundled-asset`.
3. **Main CLI never calls it:** `grep -rn installDoctrineRegistry packages/cli/src/` → ZERO hits (only HOOK-refresh refs). So the root `@jhizzard/termdeck` tarball CORRECTLY omits the asset — nothing in the main package reads it. The hooks asset IS in the root files[] because the main CLI's `runHookRefresh` reads it (`init-mnestra.js:580/1068/1110`); doctrine has no such main-CLI read by design (decision #2: loader reads the package-relative `registry.jsonl`, which IS in the root tarball — the load-bearing delivery).
4. **No functional gap:** Brad's load-bearing registry is `doctrine/registry.jsonl` shipped in `@jhizzard/termdeck` (root `npm pack` cites it; loader reads it package-relative). The `@jhizzard/termdeck-stack` read-only copy is the supplementary inspectable artifact and lands on `npx @jhizzard/termdeck-stack`.
**Conclusion:** the asymmetry (root ships hooks-asset but not doctrine-asset) is intentional + correct. Adding `packages/stack-installer/assets/doctrine/**` to the ROOT files[] would ship dead weight (nothing in the main package reads it) and falsely imply a main-CLI doctrine refresh that doesn't exist. T4: please re-verify against `cd packages/stack-installer && npm pack --dry-run`, not the root tarball.

### [T3] FIX-LANDED 2026-06-13 19:31 ET — All T3 landed + GREEN (215/215, tsc exit 0, gitleaks-clean); ACK T4 AUDIT-PASS 19:29 + FINAL-VERDICT 19:30 (T3 GREEN).
Close-out record. Acceptance (brief §4) → evidence:
- Auth: 401 w/o secret (dispatchOp never reached) / 200 with `x-mnestra-secret` / 200 `Bearer` / 401 wrong / **fail-closed** unconfigured; bind **127.0.0.1** default + host override — `tests/webhook-auth.test.ts`. Constant-time via sha256+`timingSafeEqual`.
- K returned hits → exactly K log rows (NOT the 10–40 candidate pool): `recall.ts` logs `kept`; `recall-log.test.ts`.
- Latency unchanged: every log call un-awaited + never throws into caller (getSupabase-throw / rpc-reject / sync-throw swallowed). T4's swallowed "Could not find function log_recall_hits" = proof telemetry uses `getSupabase()`, a *different* client than the recall test fakes (HARDENING-2 isolation).
- `memory_get`→cited (`markRecallCited`, `layered.ts`); pg_cron purge registered (guarded+idempotent+fail-soft); `op:'feedback'` validated (bad → 400, no fire).
- Five gates: T4 rollback-apply receipt PASS + `migration-027-hygiene.test.ts`. `get_advisors` 0011/0013 = ORCH post-apply step (I don't apply in-lane).
**Files:** NEW `src/recall_log.ts`, `migrations/027_recall_telemetry.sql`, `tests/{recall-log,webhook-auth,migration-027-hygiene}.test.ts`; MOD `src/{webhook-server,recall,search,layered,recall_graph,types}.ts` + `tests/{webhook-server,webhook-read-after-write,quarantine-proof}.test.ts`. No version/CHANGELOG/commit (ORCH close-out). HANDOFF: I own the `op:'feedback'` receiver + `memory_get`→cited path end-to-end; T2 client POST is optional/Sprint-80 — no blocking seam.
Running an in-distribution adversarial self-review (weighted on migration SQL + fire-and-forget); DONE on its clean verdict.

### [ORCH] RELAY 2026-06-13 19:31 ET — T4 FINAL-VERDICT RED (19:30): T2 GREEN (817 tests, 0 fail), T3 GREEN (215 tests, 0 fail), T1 RED on ONE gate — stack-installer vendored doctrine asset (packages/stack-installer/assets/doctrine/registry.shipped.jsonl) is NOT in package.json files[] (only hooks/** whitelisted) → won't ship → Brad's read-only copy won't install. Relayed the fix to T1 via inject: add packages/stack-installer/assets/doctrine/** to files[] (vendored baked asset, NOT a local overlay — §8.4 does not forbid it) + re-pack-verify. On T1 FIX-LANDED I will explicitly re-audit-inject T4 (Codex won't re-audit from a passive poke).

### [T2] FIX-LANDED 2026-06-13 19:36 ET — Advisor MVP landed. Files: NEW `packages/server/src/advisor/{index,suppress,deliver}.js` + NEW `packages/server/src/pty-submit.js` (shared submit, see item 4) + `database.js` (advisory_events + advisory_quarantine inline DDL) + `index.js` (require advisor+submitToPty; fail-soft `advisor.onTrigger(sess,ctx,{db})` at onErrorDetected ENTRY; `agent_injected` on proactive_memory frame; `onAdvAck` wiring; `GET /api/advisor/{diag,stats}`; `/input {submit:true}` branch now calls shared `submitToPty`) + `session.js` (`onAdvAck` + `_detectAdvAck` scan). Tests: `packages/server/tests/advisor-mvp.test.js` (21 tests). **425/425 server tests pass; 833/833 repo pass; 0 fail.** Routes verified live (diag+stats return ok:true). Forbidden-string screen clean on all new files.

ALL FOUR T4 AUDIT ITEMS ADDRESSED (re ORCH relay):
1. **Real module:** `resolveDoctrine()` requires T1's `../../../../doctrine` and uses it; verified by the e2e test — the REAL registry matches a seeded EADDRINUSE error and produces `[ADVISOR err-port-in-use] …`. The NULL stub is the fail-soft fallback ONLY (module absent/throws ⇒ loadDoctrine→[] ⇒ no-op).
2. **`{notify}` + opts:** `suppress.js` now checks `verdict.notify === false` and passes `{ db, lane:sessionId, surface:'inject-advisory', session_id, now }` into `shouldNotify` so T1's 30-min per-rule cooldown + 3/lane/hr budget actually bind. shouldNotify self-records to doctrine_events (db wired via T1's `setDb` in database.js init).
3. **Array triggers:** `matchEntry` uses `isErrTrigger()` (Array.isArray ? includes('T-ERR'/'always') : ===/absent) and prefers T1's compiled `_checkRegex` (else compiles check.pattern+flags).
4. **Shared submit:** extracted the v1.10.1 server-sequenced submit into `pty-submit.js::submitToPty` (body→server-held settle→lone `\r`); BOTH the `/input {submit:true}` route AND the advisor now call it — single code path, no drift. deliver.js no longer reimplements bracketed-paste/lone-\r/**cr-flood** (cr-flood removed). The advisory line is the paste BODY (bracketed-paste wrapped, exactly as a {submit:true} caller wraps TUI text); the submit MECHANISM is shared. The 7 input-submit-option.test.js contract tests still pass (route refactor is behavior-preserving). NOTE: I unified on the `{submit:true}` server path per your item 4, NOT on sprint-inject.js (the multi-lane runner is a separate path); unifying sprint-inject.js→pty-submit.js is a possible fast-follow, out of lane.

Two reconciliations from my 19:08 FINDING stand and are implemented: onTrigger fires at handler ENTRY (registry-only, Mnestra-independent — A1 — so it fires on errors with zero Mnestra hits, the e2e case) and deliverable = status ∉ {thinking,editing,starting,exited} (session.js clobbers status→'errored' before onErrorDetected, so 'errored' is the post-error "waiting" state = deliverable; mid-turn 'thinking' queues). Delivery gated to non-Claude AGENT panels (codex/gemini/grok/antigravity) — NOT raw shells (injecting a line into a shell would execute it). Two cooldown layers kept distinct (§8.2): T1 shouldNotify = per-rule 30min+3/hr; my suppress = 24h-per-entry(honors registry cooldown_hours)+5/session+1/10min+dedupe+quarantine(3 unheeded recurrences→7d, non-silent WS toast). HANDOFF: did NOT build the client-side op:'feedback' caller (§8.3 — T3 owns receiver, client caller OPTIONAL this sprint). Ready for T4 re-audit.

### [T1] FIX-LANDED 2026-06-13 19:38 ET — re T4 19:24/19:30 + ORCH directive: added `packages/stack-installer/assets/doctrine/**` to ROOT package.json files[]. Blocker cleared.
Followed ORCH's call (my 19:31 refutation was architecturally accurate — `installDoctrineRegistry` runs only from the separate `@jhizzard/termdeck-stack` pkg, whose tarball already shipped the asset — but ORCH's directive is sound: mirror the existing `assets/hooks/**` precedent for defense-in-depth so NO path can hit `no-bundled-asset`, and §8.4-safe since the dir holds only the baked asset).
- **Change:** `package.json:17` files[] now has `"packages/stack-installer/assets/doctrine/**"` immediately after `"packages/stack-installer/assets/hooks/**"` (one line; mirrors the hooks precedent).
- **Asset-dir is clean:** `ls packages/stack-installer/assets/doctrine/` → ONLY `registry.shipped.jsonl` (10.7kB), no `~/.claude`/`~/.termdeck`/overlay file. The wildcard is safe (this is a build-asset dir, never an overlay dir).
- **Pack verification (`npm pack --dry-run --json`, filtered to /doctrine/):** now lists ALL FOUR — `doctrine/index.js`, `doctrine/registry.jsonl`, `doctrine/SCHEMA.md`, AND `packages/stack-installer/assets/doctrine/registry.shipped.jsonl`. Overlay-leak filter (`registry.local|.claude/doctrine|doctrine-local`) → `[]`.
- **gitleaks:** clean on `doctrine/` AND `packages/stack-installer/assets/doctrine/` (both `no leaks found`).
- **Tests:** 34/34 green (`doctrine-registry` 13 + `doctrine-throttle` 14 ... 27 server + `doctrine-vendor` 7).
T4: please re-grade — `npm pack --dry-run --json | grep doctrine` now cites the stack-installer asset. Hardening pass (5 low/nit findings from my own adversarial review) landing next; DONE after.

### [ORCH] RE-AUDIT-REQUEST 2026-06-13 19:40 ET — All 3 workers final-landed (T3 19:31, T2 19:36, T1 19:38); T4's 19:30 RED predates T1+T2 final states. Explicitly waking parked T4 to re-grade: (1) T1 cleared the tarball blocker (ORCH-verified: root pack now ships all 4 doctrine assets incl. stack-installer/assets/doctrine/registry.shipped.jsonl); (2) T2 final 19:36 added shared pty-submit.js (both /input and advisor use it — resolves T4 19:11) + 833 tests, supersedes the 19:30-graded state; (3) T3 unchanged. Requesting fresh FINAL-VERDICT.

### [T1] FIX-LANDED 2026-06-13 19:42 ET — hardening pass: 5 findings from my own adversarial-review workflow (14 agents, refute-then-confirm) — all low/nit, all fixed + regression-guarded
Ran a 6-dimension adversarial self-review (fail-soft / validation / throttle / packaging / T2-contract / spec) with a per-finding refutation stage BEFORE claiming DONE. 5 confirmed (0 blocker, 0 high — AMEND-3 invariant held throughout). All fixed in `doctrine/index.js`:
1. **(low) validateEntry now REQUIRES `enforcement.max_severity`** — was validate-if-present; SCHEMA marks it required. Now `!MAX_SEVERITIES.includes(...)` rejects absent/invalid (all 17 shipped entries already carry it).
2. **(low) `severity` enum now validated** — added `SEVERITIES=['critical','high','medium','low']`; matches the existing scope/check.type/surface strictness + SCHEMA's required marker.
3. **(low) `status` enum now validated** — added `STATUSES=['active','proposed','deprecated']`; Sprint 79 elevation keys off `status`, so a typo'd status no longer silently validates.
4. **(low) falsy-lane insert/guard split** — `recordGateEvent` recorded `evt.lane || null` (coerces `''`/`0`→NULL) while `shouldNotify`'s budget guard is `if (lane != null)`; now records `evt.lane != null ? evt.lane : null` so guard+insert agree (was unreachable in prod — session.id is always a truthy UUID — but a real internal inconsistency). Regression-guarded by a new throttle test.
5. **(nit) `compileTrigger(null)` threw on the exported surface** — added a top-of-fn object guard so the T2-facing helper is as fail-soft as `validateEntry`; the documented malformed-REGEX throw (caught per-entry by `loadDoctrine`) is unchanged.
Verification after hardening: all 17 shipped entries still valid (0 dropped); `loadDoctrine`→17, `{event:'T-ERR'}`→5; **T2's 21 advisor tests still pass (no cross-lane regression from the stricter validator)**; new regression tests added for #2/#4/#5.

### [T1] DONE 2026-06-13 19:42 ET — Doctrine registry core complete; all 7 acceptance gates met; full repo green
**Landed surface:** `doctrine/index.js` (loader+validator+gitleaks-screen+throttle+DDL, CJS, ZERO non-builtin deps, fail-soft, no work at require-time), `doctrine/registry.jsonl` (17 entries: 9 universal advisory/inventory + 1 block-on-git-hook inventory + 1 operator-local pattern-less scrub stub + 1 server-monitor + 5 `T-ERR` regex error-advisories + 3 operator/manual honesty entries), `doctrine/SCHEMA.md` (field contract + max_severity-per-surface table + the T2 throttle contract), `packages/server/src/database.js` (`doctrine_events` sibling, appended after T2's `advisory_events`, lazy-required + fail-soft so it can't break server boot, `setDb(db)` wired), `package.json` files[] (scoped doctrine + `assets/doctrine/**`), `packages/stack-installer/src/index.js` + `assets/doctrine/registry.shipped.jsonl` (read-only Brad copy, FULL-FILE sha256 stamp — NOT 4KB-head), 3 test files (40 tests).
**Acceptance gates (PLANNING §4 / brief — all behavior, not file-existence):**
1. ✅ Behavior tests in the canonical glob (`packages/server/tests/doctrine-*.test.js` + `packages/stack-installer/tests/doctrine-vendor.test.js`) — 40 tests, load/merge/validate/throttle/screen/vendor.
2. ✅ A `block`-on-`server-monitor` entry FAILS validation (dropped+logged, valid remainder loads) — tested; the inverse (`block` on git-hook/preToolUse-deny/inject-refusal) passes.
3. ✅ `npm pack --dry-run` cites all 4 doctrine paths (3 top-level + stack-installer asset), NO overlay/local file (`registry.local`/`.claude/doctrine`/`doctrine-local` → `[]`).
4. ✅ gitleaks clean on every shipped doctrine file (both dirs `no leaks found`); zero hardcoded forbidden patterns in index.js (screen shells out).
5. ✅ `loadDoctrine` succeeds with BOTH overlays absent — returns repo entries, never throws.
6. ✅ Fail-soft demonstrated: gitleaks-absent ⇒ warn+continue unscreened; broken db ⇒ logged, never throws; malformed overlay/regex/JSONL ⇒ that entry dropped, remainder loads.
7. ✅ `shouldNotify` enforces per-rule 30-min cooldown + hard 3/lane/hr budget (4th → `overflow-orch`, `route:'orch'`); budget counts only delivered rows.
**Cross-lane closures:** T4 19:13 (zero T-ERR-reachable entries) → 5 `T-ERR` regex entries added, matching-contract documented for T2. T4 19:12 (`{ok}` vs `{notify}`) → contract is `{notify}`, documented; T2 adapted (now GREEN). T4 19:24/19:30 (asset not in root tarball) → added `assets/doctrine/**` to root files[] per ORCH; pack now cites it.
**Final state:** full repo `npm test` = 841 tests, 836 pass, 0 fail, 5 pre-existing skips. Baked asset byte-current with the audience:all+active subset (zero drift). PERIPHERY: only T1-owned files touched; `database.js` shared with T2's `advisory_events` (appended after, no clobber, both tables verified present on a real handle). No version bump / CHANGELOG / commit — ORCH owns close-out.

### [T1] FINDING 2026-06-13 19:43 ET — correction to the 19:42 DONE: the doctrine test count is **37**, not 40 (registry 17 + throttle 13 + vendor 7). `node --test packages/server/tests/doctrine-registry.test.js packages/server/tests/doctrine-throttle.test.js packages/stack-installer/tests/doctrine-vendor.test.js` → `tests 37, pass 37, fail 0`. All other figures in the DONE (17 registry entries, 841/836/0/5 full-repo, all 7 gates) stand verified.

### [T4-CODEX] FINAL-VERDICT 2026-06-13 19:42 ET — GREEN. Re-audited current tree after all worker FIX-LANDED posts. T1 GREEN: root `package.json:16-17` now whitelists both `packages/stack-installer/assets/hooks/**` and `packages/stack-installer/assets/doctrine/**`; `packages/stack-installer/package.json:9-11` ships `assets/**` for `@jhizzard/termdeck-stack`; root `npm pack --dry-run --json` filtered to doctrine paths returns `doctrine/index.js`, `doctrine/registry.jsonl`, `doctrine/SCHEMA.md`, and `packages/stack-installer/assets/doctrine/registry.shipped.jsonl`; `npm pack --dry-run --json` from `packages/stack-installer` returns `assets/doctrine/registry.shipped.jsonl`; baked asset probe returns `count:14, nonAll:[], inactive:[], operatorLocal:[]`; targeted doctrine tests (`doctrine-registry`, `doctrine-throttle`, `doctrine-vendor`) exit 0 with `tests 37, pass 37, fail 0`. T2 GREEN: shared submit helper is `packages/server/src/pty-submit.js:34-69`; `/input {submit:true}` imports/uses it at `packages/server/src/index.js:89` and `:2498-2509`; advisor delivery uses the same helper at `packages/server/src/advisor/index.js:183-188`; idle/TTL gate is `packages/server/src/advisor/deliver.js:31-90`; array trigger support is `packages/server/src/advisor/index.js:122-140`; T-ERR path loads doctrine only at `packages/server/src/advisor/index.js:395-402`, and `rg -n "mnestra|queryMnestra" packages/server/src/advisor` returns no matches; T1 `{notify}` contract is consumed at `packages/server/src/advisor/suppress.js:222-237`; focused `node --test packages/server/tests/input-submit-option.test.js packages/server/tests/advisor-mvp.test.js` exits 0 with `tests 28, pass 28, fail 0`, covering shared submit, idle queue/TTL, suppression/no-spam, ACK, fail-soft, and real-registry e2e. Full TermDeck `npm run test` exits 0 with `tests 841, pass 836, fail 0, skipped 5`. T3 GREEN: engram `npm run test` exits 0 with `tests 218, pass 218, fail 0`; rollback-only `psql ... begin; -f migrations/027_recall_telemetry.sql; rollback;` exits 0 and prints RLS enabled `t`, policy count `0`, all three RPCs `anon:f authenticated:f public:f service_role:t` with `search_path=public, pg_catalog`, and `[027] receipt: five gates verified`; webhook auth/bind evidence is `src/webhook-server.ts:500-501` default `127.0.0.1`, `:514-515` pre-route 401 gate except `/healthz`, and `src/recall_log.ts:142-145` fail-soft non-string `hashQuery`. No remaining RED/YELLOW from T4.

### [T3] FIX-LANDED 2026-06-13 19:47 ET — Adversarial self-review hardening pass (5-dimension workflow, refute-then-confirm); 4 should-fix/nits all fixed. POST-DATES T4 FINAL-VERDICT 19:42 — heads-up on 2 changed surfaces.
Ran a 5-dimension self-review (migration-SQL-execution / fire-and-forget / auth-gate / redaction+feedback / regression) with a synthesis stage that re-verified every must-fix against source. **0 must-fix, 0 auth-bypass, 0 content-exfil, 0 apply-failure** — but 4 confirmed real defects (1 of them in my OWN HARDENING-2 reasoning), all fixed + regression-guarded:
1. **(should-fix — correctness, the important one) Telemetry was NOT structurally test-isolated.** My boot-FINDING claim that telemetry "never touches the recall test fakes" only held while `SUPABASE_URL` was unset; in a credentialed shell the fire-and-forget writes hit the REAL daily-driver DB. Harmless today (RPC 404s pre-apply) — but **the instant ORCH applies 027, `npm test` would INSERT fixture-derived junk into the live `memory_recall_log` + bump `recall_count`**, corrupting the exact signal this sprint creates. Fix: `resolveClient()` gate — explicit `deps.client` always wins (recall_log's own tests); the `getSupabase()` fallback is skipped when `MNESTRA_DISABLE_RECALL_LOG=1`, which the engram **`package.json` test script now sets**. (This is why the `[mnestra-recall-log] telemetry write failed` line you saw earlier is now gone — the suite no longer reaches a live DB.) Disabling telemetry is fail-safe; the flag is purpose-built (not the ambient-infra-guard anti-pattern). `src/recall_log.ts` + `package.json:31` + 2 new tests.
2. **(should-fix — security DiD) Empty-string host silently flipped loopback → all-interfaces.** `??` doesn't catch `''`, so `export MNESTRA_WEBHOOK_HOST=` + `mnestra serve` would `listen(port,'')` = bind `::`. Fix: `(… ?? '127.0.0.1').trim() || '127.0.0.1'`. `src/webhook-server.ts` + a new test (`host:''` → `address==='127.0.0.1'`). Secret still gated content, so DiD-loss not a bypass.
3. **(should-fix — redaction completeness) `Bearer <opaque-token>` leaked the credential.** `SECRET_KV` only masked the `authorization`/`bearer` keyword, leaking the token after it; bare `bearer <tok>` matched nothing. Fix: a `(?:authorization\s*:\s*)?bearer\s+\S+` shape added to the redactor + 2 tests (both forms fully redact). Bounded impact (query_preview is service-role-only) but the contract claimed to mask it.
4. **(nit — telemetry fidelity) `memory_timeline` double-logged its anchor.** The query-path calls `memoryIndex` internally → fired a phantom `index`-surface row + double-bumped the anchor. Fix: `IndexInput.log_surface:null` suppresses the internal anchor probe's telemetry. `src/layered.ts` (telemetry fidelity = the point of T3).
Plus: DB-level `left(query_preview,200)` backstop in 027; `markRecallFeedback` sync-throw test; **test fixtures de-fanged** (fake JWT/Stripe secrets reassembled from fragments at runtime so gitleaks doesn't flag the test's own fakes).

**Final state:** engram `npm test` = **219/219 pass, 0 fail**; `tsc` build exit 0; **gitleaks 0 leaks** (was flagging the 2 redaction-test fixtures — fixed); forbidden-string scan clean. No version/CHANGELOG/commit (ORCH close-out).

**⚠ 2 surfaces changed AFTER your 19:42 verdict (both strict improvements, all gates still pass):**
- `migrations/027_recall_telemetry.sql` — added `left(h.query_preview, 200)` in the insert select (trivially-safe SQL, can't affect the five gates/receipt). **Re-running your rollback-apply (`psql begin; -f 027; rollback;`) is a ~5s re-confirm if you want it before publish.**
- `package.json:31` test script — added `MNESTRA_DISABLE_RECALL_LOG=1` (test-only env gate; production `mnestra serve` is unaffected).

### [T3] DONE 2026-06-13 19:47 ET — Recall telemetry + webhook hardening COMPLETE. engram 219/219, tsc 0, gitleaks 0; five-gate migration execution-verified by T4 rollback-apply.
**Acceptance (brief §4) — all met:** ITEM ZERO webhook secret (constant-time `sha256`+`timingSafeEqual`, fail-closed, `x-mnestra-secret`/`Bearer`) + bind `127.0.0.1` (empty-host-safe) — `webhook-auth.test.ts`; K returned hits → exactly K log rows (`kept`, not the 10–40 candidate pool) — `recall.ts` + `recall-log.test.ts`; recall latency unchanged (every log call un-awaited, never throws into caller); `memory_get`→cited; pg_cron purge registered (guarded+idempotent+fail-soft); five gates (T4 rollback receipt + `migration-027-hygiene.test.ts`); `op:'feedback'` `{memory_id,event}` validated. `get_advisors` 0011/0013 zero-new-lints = **ORCH post-apply step** (not applied in-lane).
**Landed (engram, no commit):** NEW `src/recall_log.ts`, `migrations/027_recall_telemetry.sql`, `tests/{recall-log,webhook-auth,migration-027-hygiene}.test.ts`; MOD `src/{webhook-server,recall,search,layered,recall_graph,types}.ts`, `package.json`, `tests/{webhook-server,webhook-read-after-write,quarantine-proof}.test.ts`. **HANDOFF:** T3 owns the `op:'feedback'` receiver + `memory_get`→cited path end-to-end; T2's client-side flashback POST is optional/Sprint-80 (T2 confirmed at 19:36 it did NOT build it — no open seam). **Collision guard for Sprint 79:** `recall_count`/`last_recalled_at` on `memory_items` are owned by migration 027 — Sprint 79 must NOT re-add. Ready for ORCH close-out.
