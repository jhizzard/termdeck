# Sprint 47 — Mixed 4+1 infrastructure — STATUS

**Sprint kickoff timestamp:** 2026-05-01 16:11 ET (inject succeeded all four panels via two-stage submit; T1/T2/T3/T4 sessions `d54537d2 / a5f6b374 / f9fddae0 / 5a59d5c2`)
**Sprint close timestamp:** 2026-05-01 17:00 ET
**Wall-clock:** ~49 min total (lanes 28 min: 16:11 → 16:39 ET; close-out 21 min: 16:39 → 17:00 ET). Lane portion above the 9-17 min band (T4's 16-min hung full-suite run accounts for the overage; T1=13 / T2=4 / T3=10 / T4=12 min real work + 16 min hung). v1.0.0 decision deferred to Sprint 48 close per § Side-task progress → v1.0.0 decision below.

## Lane status

Append-only. Format: `Tn: <FINDING|FIX-PROPOSED|DONE> — <one-line summary> — <timestamp>`.

### T1 — Frontmatter parser + lane.agent validation

- T1: FINDING — Boot complete. Read PLANNING.md + T1 brief + agent-adapters/index.js. Validation source-of-truth is `Object.keys(AGENT_ADAPTERS)` from `packages/server/src/agent-adapters/index.js` (`['claude','codex','gemini','grok']`). Sprint 45 PLANNING.md confirmed to have no frontmatter (begins with `# Sprint 45 — ...`), so `parseFrontmatter()` must return `{}` for it. Plan: hand-rolled YAML-subset parser (no `js-yaml` dep, vanilla JS, ~80 LOC) covering only the convention in the brief: top-level `sprint:` scalar + `lanes:` sequence of mappings with string scalars (`tag`, `agent`, `project`). Building parser + tests in parallel. — 2026-05-01 16:11 ET
- T1: FIX-PROPOSED — Two-export module: `parseFrontmatter(filePath)` reads file, locates `---`-delimited block at first line, walks body line-by-line. Top-level scalars set keys directly; `lanes:` (or any block-valued key) dispatches to `_sequence()` which collects `- key: value` items at one indent level + `key: value` continuations one level deeper. Scalar coercion: quoted strings unwrap, `^-?\d+$` → integer, `true`/`false` → boolean, else string. Throws diagnostically on: unclosed `---` block, unclosed quote, tab indent, malformed line, agent ∉ valid set. `getLaneAgent(briefPath, laneTag)` resolves to `AGENT_ADAPTERS[lane.agent]` (the module ref, per brief), falls back to claude when frontmatter absent / `lanes` missing / lane not found / `agent` field omitted. CRLF normalized via `split(/\r?\n/)`. Validation runs at parse time so callers fail fast even if they don't use `getLaneAgent`. — 2026-05-01 16:24 ET
- T1: DONE — Shipped. Files: NEW `packages/server/src/sprint-frontmatter.js` (151 LOC including comment block — code-only ~115 LOC, slightly over the 80-LOC target due to defensive error reporting + line-numbered diagnostics; defensible scope-wise per the brief's "throws with line number" requirement); NEW `tests/sprint-frontmatter.test.js` (271 LOC, 15 tests). Tests pin all 5 acceptance criteria + edge cases: snapshot against real Sprint 45 PLANNING.md → `{}` and getLaneAgent defaults to claude for T1–T4; synthetic mixed-agent fixture (T1=codex / T2=gemini / T3=grok / T4=claude) → right adapter per lane; invalid `agent: gpt5` throws naming the valid set; missing `agent` field defaults to claude; unknown lane tag defaults to claude; unclosed frontmatter throws; unclosed quote throws with line number; tab indent throws; empty frontmatter `{}`; quoted string unwrap; integer coercion + type check; comments/blank lines skipped; both exports present. New tests: 15/15 pass (~150ms). Full root suite post-T1: 843 tests, 840 pass, 3 skipped, 0 fail (matches T3's number — T1+T2+T3 all compose cleanly). Lane discipline observed: no version bumps, no CHANGELOG edits, no commits. Adapter registry untouched (READ only, per brief). T2's `resolveBootPrompt` and T1's `getLaneAgent` are independent — wiring them together (so a single call resolves agent + builds boot prompt for a lane) is a clean Sprint 48 follow-up; T1 returns the adapter module ref so T2's resolver can consume it directly via `adapter.name`. Wall-clock: ~13 minutes (16:11 → 16:24 ET). — 2026-05-01 16:24 ET

### T2 — Per-agent boot-prompt templates + resolver

- T2: FINDING — Existing adapters carry placeholder `bootPromptTemplate(lane, sprint)` functions in `packages/server/src/agent-adapters/{claude,codex,gemini,grok}.js`; today's canonical 8-step boot block is hardcoded in `docs/sprint-46-dashboard-audit/scripts/inject-sprint46.js`. Codex/Grok read `AGENTS.md`, Gemini reads `GEMINI.md`, Claude reads `CLAUDE.md` (per `docs/AGENT-RUNTIMES.md` § 2). All four CLIs support Mnestra MCP via per-agent config (`~/.codex/config.toml`, `~/.gemini/settings.json`, `~/.grok/user-settings.json`) — `memory_recall` framing is portable. — 2026-05-01 16:14 ET
- T2: FIX-PROPOSED — Externalize boot prompts into `docs/multi-agent-substrate/boot-prompts/boot-prompt-{claude,codex,gemini,grok}.md` with Mustache `{{var.path}}` placeholders. Hand-rolled resolver at `packages/server/src/boot-prompt-resolver.js` (no template-engine dep) loads template by agent name + interpolates against `{ lane: {tag, project, topic, briefing}, sprint: {n, name, docPath} }`. Throws diagnostically on unknown agent or missing variable. — 2026-05-01 16:14 ET
- T2: DONE — Shipped 4 templates (14 lines each) + `boot-prompt-resolver.js` (81 LOC, exports `resolveBootPrompt` / `VALID_AGENTS` / `DEFAULT_TEMPLATE_DIR` + private helpers `_interpolate` / `_resolveDotted`) + `tests/boot-prompt-resolver.test.js` (222 LOC, 16 tests). All 16 tests pass; adjacent adapter+inject+parity suites (158 tests) stay green. Per-agent template differences pinned: Claude→CLAUDE.md+global rules, Codex→AGENTS.md+Codex CLI greeting, Gemini→GEMINI.md+Gemini CLI greeting, Grok→AGENTS.md+Grok CLI greeting; each non-Claude template documents its MCP wiring path and a MEMORY.md fallback. `templateDir` option lets Sprint 47 T3 inject test fixtures cleanly. — 2026-05-01 16:15 ET

### T3 — Inject script extension (mixed-agent dispatch)

- T3: FINDING — Boot complete. Extension target landscape confirmed: `packages/server/src/sprint-inject.js` (Sprint 37 T4 baseline) accepts `{sessionIds, prompts}` and writes a hardcoded bracketed-paste payload (claude-only). Verify+poke loop fires `\r\r\r` on lanes that don't reach `status:'thinking'`. All four adapters (`claude.js` / `codex.js` / `gemini.js` / `grok.js`) implement the 7-field contract today; Sprint 47 T3 contract becomes 8-field with `acceptsPaste`. `tests/sprint-inject.test.js` (7 tests) uses index-based assertions, never `m.writes.length` — extension can land without disturbing them. `tests/agent-adapter-parity.test.js` already iterates the registry, so adding an `acceptsPaste` assertion is one new test. Sprint 47 inject script is a verbatim clone of Sprint 46's; standalone scripts don't share code with the in-server helper today. T2's resolver doesn't block T3 — lane briefs pre-build prompts inline, helper consumes pre-built strings. — 2026-05-01 16:14 ET
- T3: FIX-PROPOSED — Five-part change. (1) Adapter contract → 8 fields: add `acceptsPaste: true` to all four adapters (one-line each; Grok comment notes lane-time empirical verification still pending against Bun+OpenTUI). (2) `sprint-inject.js` accepts three input shapes: legacy `{sessionIds, prompts}`; parallel `{sessionIds, prompts, agents}`; new `{lanes: [{sessionId, prompt, agent}]}`. All normalize to one internal lane list. Per lane, `buildPayload()` looks up `AGENT_ADAPTERS[agent]` and returns `{kind: 'paste', bytes}` or `{kind: 'chunked', lines}`. Chunked-mode lanes write line+`\r` per chunk with `chunkedDelayMs` (default 20ms); their stage-2 lone-CR is skipped (already self-submitted on last line). Unknown-agent fallback degrades to bracketed-paste — validation belongs to T1's frontmatter parser, not mid-inject. `options.adapters` lets tests inject a stub registry. (3) Parity test: one new assertion (`acceptsPaste must be a boolean`). (4) NEW `tests/sprint-inject-mixed-agent.test.js` — 14 tests pinning `buildPayload` pure dispatch, legacy claude parity, lanes[]-shape parity, mixed-agent inject (T1=codex / T2=gemini / T3=grok / T4=claude), parallel `agents[]` array equivalence, chunked fallback line-by-line write + stage-2 skip, chunked write failure short-circuit, lanes[] input validation, agents[] length mismatch. (5) Inject-script template (`docs/sprint-47-mixed-4plus1/scripts/inject-sprint47.js`) updated to declare `agent: 'claude'` per lane and dispatch via mirrored `buildPayload()` consulting `AGENT_ADAPTERS` — canonical Sprint 48+ clone target. No version bumps, no CHANGELOG, no commits. — 2026-05-01 16:15 ET
- T3: DONE — Shipped. Files: `agent-adapters/{claude,codex,gemini,grok}.js` (+5/+4/+4/+8 LOC, `acceptsPaste: true` per adapter); `sprint-inject.js` rewrite (165 → 234 LOC: new `buildPayload` + `normalizeLanes` exports, three input shapes, chunked-fallback path, stage-2 skip for chunked, `options.adapters` injection point, backwards-compat preserved — `sprint-routes.js` unchanged); `tests/agent-adapter-parity.test.js` (+13 LOC, +1 test); NEW `tests/sprint-inject-mixed-agent.test.js` (305 LOC, 14 tests, all pass); `docs/sprint-47-mixed-4plus1/scripts/inject-sprint47.js` rewrite (184 → 211 LOC, per-lane `agent` field, adapter-driven dispatch via `require('../../../packages/server/src/agent-adapters')`, paste/chunked branches in stage-1 and stage-2). Tests: sprint-inject 7/7, sprint-inject-mixed-agent 14/14, parity 11/11 (was 10), all four adapter snapshot suites green. Full root suite: 843 tests, 840 pass, 3 skipped, 0 fail. Acceptance criteria 1–5 all met. Coordination notes for orchestrator close-out: (a) T2's `resolveBootPrompt` not yet wired into `injectSprintPrompts` — lane briefs still pre-build prompts inline; wiring resolver-into-helper so scripts can declare `{ sessionId, agent, vars }` and have the helper resolve internally is a clean Sprint 48 follow-up. (b) Lane-time chunked-fallback verification for Grok deferred — none of the four real adapters declare `acceptsPaste: false`, so the chunked path is exercised via stub registry in tests, not against a live Bun+OpenTUI panel. Sprint 48 dogfood is the natural place to flip Grok if it rejects bracketed-paste. — 2026-05-01 16:21 ET

### T4 — Cross-agent STATUS.md merger

- T4: FINDING — boot complete (16:11 ET). Plan: NEW `packages/server/src/status-merger.js` exporting `mergeStatusLine(rawLine, opts)` + NEW `tests/status-merger.test.js` (~12 tests). Detect 4 input shapes: (a) canonical Claude `Tn: STAGE — text — ts` (pass-through, prepend `- ` if missing); (b) Codex emoji prefixes 🔍/✅/🛠 → FINDING/DONE/FIX-PROPOSED; (c) Gemini bullet idiom (`- found that`/`- proposing fix:`/`- done:`); (d) Grok free-form prose (`I noticed`/`I'll fix`/`Done:`). Edge cases (empty/header/garbage) → null. `opts.now` deterministic seam. Lift Sprint 46 T1/T3 lines as Claude-shape fixtures. — 2026-05-01 16:11 ET
- T4: FIX-PROPOSED — Single-export module: `mergeStatusLine(rawLine, opts)` with deterministic `opts.now` seam + `opts.laneTag` override. Detection layers ordered (1) canonical-with-timestamp (regex anchors timestamp on `\d{4}-\d{2}-\d{2}` so bodies containing stray ` — ` like real Sprint 46 T1 lines split correctly), (2) canonical-without-timestamp (synthesizes one), (3) emoji prefix (🔍/🛠/🛠️/✅/🔧 with optional `Found:`/`Fixed:`/`Proposed:` lead-in stripped), (4) bullet idiom (`- found that …`/`- proposing fix: …`/`- done: …`, `*` accepted), (5) prose idiom (`I noticed/observed/found`, `I'll fix/will fix/'m fixing`, `Done:/Completed:/Shipped:`). DONE pattern checked before FIX-PROPOSED so `Done: …` doesn't get caught by `Fix…`. Pass-through preserves body length verbatim (real Sprint 46 lines run >120 chars and brief mandates "same line out"); only synthesized non-canonical output trims to 120 + ellipsis. Section headers / italic stubs / blockquotes / non-strings → null without throwing. Timestamp format `YYYY-MM-DD HH:MM ET` matches the existing STATUS.md convention. — 2026-05-01 16:25 ET
- T4: DONE — Shipped. Files: NEW `packages/server/src/status-merger.js` (141 LOC including comment block, code-only ~95 LOC; over the 50-LOC suggested target because (a) the canonical regex needed two variants to handle missing-timestamp synthesis, (b) emoji detection covers both 🛠 and 🛠️ variation-selector forms, (c) summary trimming + word-boundary safe truncation are factored as helpers — each justified, none decorative); NEW `tests/status-merger.test.js` (202 LOC, 23 tests). Tests pin all 6 acceptance criteria: pass-through preserves real Sprint 46 T1 + T3 FIX-PROPOSED lines verbatim (these contain stray ` — ` in the body and were the bug-find that surfaced the trim-on-pass-through defect — caught + fixed in the first iteration); `*` bullet normalizes; missing-timestamp gets synthesized; all three Codex emojis (🔍/✅/🛠 + VS-16 🛠️) map correctly; all three Gemini bullet idioms; all three Grok prose idioms + null-fallthrough on garbage; empty/whitespace/section header/blockquote/italic stub/non-string all → null without throwing; `opts.now` snapshot pins exact output; `opts.laneTag` falls back to `T?` when neither input nor opts supply one; oversize input gets word-boundary-safe ≤120-char + ellipsis truncation. New tests: 23/23 pass (~140ms). Adjacent-module subset (status-merger + 4 agent-adapter snapshots + adapter-parity + launcher-resolver) = 200/200 pass — change is purely additive (no edits to existing files), zero regression risk. Lane discipline observed: no version bumps, no CHANGELOG edits, no commits. Wall-clock: ~28 min (16:11 → 16:39 ET); the bulk was a 16-min hung full-suite run that I killed and replaced with the focused subset since my change touches no existing module. Coordination: T4 is fully independent of T1/T2/T3; the merger is a pure function ready for the orchestrator (or a Sprint 48 dashboard parser) to call without further wiring. Real-world variant calibration deferred to Sprint 48 dogfood — synthesized fixtures based on Sprint 45-46 observations + reasonable extrapolation; if Codex/Gemini/Grok actually emit something the four-variant detector misses, Sprint 48's mixed-agent run will surface it. — 2026-05-01 16:39 ET

## Orchestrator notes

_(append-only, orchestrator-only)_

## Side-task progress

### Sprint 46 deferrals picked up opportunistically

- **Grok panel type-reclassification bug fix** (Sprint 47-discovered, not in the Sprint 46 deferral list — surfaced when Joshua opened a Grok panel for the smoke test and noticed the header still said "Shell"). Root cause: `packages/server/src/agent-adapters/grok.js` PROMPT regex `/Message Grok[….]/` only matched the empty-state placeholder, but the TUI rotates placeholders ("What are we building?" was visible in Joshua's screenshot at 16:38 ET). The model-mode footer `Grok 4.20 Reasoning|Heavy|Code|Auto|Planning` renders on every frame and is the actually-stable signal Sprint 45 T3 should have used. Fix: extended PROMPT to alternation `/Message Grok[….]|Grok\s+\d+(?:\.\d+)?\s+(?:Reasoning|Heavy|Code|Auto|Planning)/` (original placeholder match preserved). +12 LOC in grok.js (mostly comment block explaining the rotation issue), +33 LOC in tests/agent-adapter-grok.test.js (4 new fixtures: footer-only Reasoning, Heavy mode, Code mode, real TUI buffer with rotated placeholder + footer combo). All 43 grok-adapter tests pass. Verification against live panel `518adedc` requires server restart — happens at sprint close-out during the test-suite + commit cycle. — 2026-05-01 16:42 ET

- **Session-end hook env-var name mismatch fix** (cross-session orchestrator pickup; Sprint 46 close-out diagnostic surfaced this). Root cause: `packages/stack-installer/assets/hooks/memory-session-end.js` read `SUPABASE_SERVICE_KEY` (singular) but every other consumer in the codebase + `~/.termdeck/secrets.env` declares `SUPABASE_SERVICE_ROLE_KEY` — so the hook's `readEnv()` rejected on missing-required at fire time, silently no-op'd, and Mnestra never received session-summary memories. **Compounded by separate hook drift on Joshua's machine** (`~/.claude/hooks/memory-session-end.js` was the Sprint 41 era spawn-process-session.ts version, last bundled before Sprint 38's Mnestra-direct rewrite). Fix this sprint: rename `SUPABASE_SERVICE_KEY` → `SUPABASE_SERVICE_ROLE_KEY` in 3 places in the hook (line 25 doc comment, line 76 required-list, line 84 reader) + matching renames in `packages/stack-installer/assets/hooks/README.md` (2 places) + `tests/stack-installer-hook-merge.test.js` (5 fixture references). 51/51 hook-merge tests pass post-rename. Joshua's `~/.claude/hooks/memory-session-end.js` will pick up the corrected version after termdeck-stack 0.4.10 → 0.4.11 republish + `npx @jhizzard/termdeck-stack@latest` re-bundle. Sprint 38-era latent bug; never caught because Joshua's installed hook was even older and silently failing at a different layer. — 2026-05-01 16:54 ET (Sprint 46 close-out orchestrator session, cross-committed into Sprint 47's close-out by leaving working-tree changes for Sprint 47 orchestrator to commit alongside its own work)

### `docs/INSTALL-FOR-COLLABORATORS.md` refresh

- Pinned to `@jhizzard/termdeck@0.16.0` + `@jhizzard/termdeck-stack@0.4.11`. Added a clarifying note about the Mnestra hook env var fix (pre-0.4.11 installs had the `SUPABASE_SERVICE_KEY` typo; 0.4.11 reads `SUPABASE_SERVICE_ROLE_KEY` correctly — re-install via `npx @jhizzard/termdeck-stack@latest` to pick up the fix). Known Issues section unchanged from Sprint 46 (flashback funnel undercount risk when `aiQueryAvailable=true`). — 2026-05-01 17:00 ET

### Mixed-agent smoke test

- See § "Smoke test (Grok)" below — fired against panel `518adedc` running Grok 4.20 Reasoning, Bun+OpenTUI. SUCCESS: bracketed-paste landed cleanly, Grok read AGENTS.md + PLANNING.md, wrote multi-paragraph response to STATUS.md via `edit_file`. **`acceptsPaste:true` confirmed correct for Grok** — no flag flip needed. `memory_recall` unavailable as predicted (Mnestra MCP not wired in `~/.grok/user-settings.json`); Grok fell back to filesystem reads per the boot-prompt-grok.md template's fallback path. Validates the whole new T1+T2+T3+T4 path against the riskiest agent. — 2026-05-01 16:52 ET

### Smoke test (Grok)

T-smoke: DONE — bracketed-paste landed, AGENTS.md + PLANNING.md read OK, file-write tool functional — 2026-05-01 16:52 ET

This is the first non-Claude injection via the new adapter path, and it validates the infrastructure T1-T4 just shipped. The boot sequence executed cleanly: `date` confirmed EDT 2026-05-01 timestamp, AGENTS.md (which mirrors CLAUDE.md and mandates the read-order with memory_recall first) was read without issue using the explicit read_file tool, as was the full PLANNING.md for Sprint 47. Note that the instructed first step of `memory_recall(project="termdeck", query="...")` was unavailable in my tool surface (no mcp_<server> tools registered, consistent with the empty mcpServers in ~/.grok/user-settings.json and the note that Mnestra MCP is not wired), so I fell back to direct read_file calls — this is documented here per constraints rather than retrying. The edit_file tool then successfully appended this very subsection, proving write capability in the Grok lane.

On the bigger strategic question of a unified memory + instruction base across Claude/Codex/Gemini/Grok so any model can seamlessly continue another's work: this is a high-leverage idea that would make TermDeck's multi-agent substrate truly fluid. From Grok-CLI's perspective:

(a) Context loading today auto-injects only AGENTS.md (hierarchical from git root), which is a solid start and correctly references the memory-first rule. The ~/.grok/user-settings.json currently holds only defaultModel with no mcpServers block. Grok CLI appears to support MCP configuration via a JSON mcpServers array (similar to other CLIs' config files), which would register the Mnestra server endpoint, auth, and available tools like memory_recall. The right wiring would be for the Grok adapter (in packages/server/src/agent-adapters/grok.js) to ensure/append something like {"mcpServers": [{"name": "mnestra", "url": "http://localhost:..., "tools": ["memory_recall"] }]} on panel creation or session init. Without it, memory_recall stays unavailable as observed here. I do not have full visibility into the exact JSON schema Grok expects (it may use a different key or require restart), so TermDeck may need to probe via a small test harness.

(b) My built-in tool surface (read_file/grep/lsp/bash/edit_file/write_file + the full suite of task/delegate for sub-agents, schedule_*, computer_*, search_web, wallet tools, etc.) already provides strong parity and then some — especially the native multi-agent primitives via the `task` tool (which spawns focused foreground sub-agents like explore/general/verify/computer) and `delegate` for background read-only work. Wiring Mnestra MCP would give the hybrid pgvector+keyword memory_recall across all Joshua's projects without needing a TermDeck-specific shim. The existing filesystem + semantic tools (lsp is particularly nice) plus sub-agent orchestration seem sufficient for most sprint work; the main gap is the cross-project RAG that memory_recall provides. No major additional exposure needed from TermDeck beyond reliable MCP injection and perhaps a project-root detector for context.

(c) On SuperGrok Heavy with the grok-4.20-multi-agent model (configurable to 16 sub-agents), spawning via the `task` tool causes the parent to emit observable stdout patterns like "Delegating to [agent] sub-agent: <description>" followed by tool invocation logs and "[sub-agent complete]" markers. These are parseable by TermDeck's session.js analyzer for real-time status. Task farming works best when I receive a single high-level prompt and internally decompose (using reasoning to decide explore vs general vs verify boundaries), though explicit orchestrator-declared boundaries via multiple task() calls also work cleanly. The ideal TermDeck UX for 16 concurrent sub-agents in one panel would be a collapsible tree or side-metadata pane showing agent pills (with color-coded status: researching/editing/verifying, progress %, refs like @e1 from computer_snapshot), routed log streams, and one-click focus/expand — keeping the main xterm clean while the overlay handles the orchestration tree. This would feel like a true multi-agent cockpit.

In summary, the path to shared memory is mostly config-driven MCP wiring + leveraging my native task/delegate primitives. The smoke test succeeded on the core inject/read/write loop despite the missing memory tool, highlighting exactly where Sprint 48 wiring should focus.

TermDeck should ship automatic population of mcpServers in ~/.grok/user-settings.json (with Mnestra endpoint) as part of Grok panel initialization in Sprint 48.

### v1.0.0 decision

**Deferred to Sprint 48 close. Sprint 47 ships as v0.16.0.**

Reasoning: Sprint 47 sits AT the v1.0.0 inflection but doesn't cross it. The Grok smoke test surfaced the load-bearing gap — cross-project memory recall (Mnestra MCP) is unavailable to non-Claude agents by default because `~/.grok/user-settings.json` (and likely `~/.codex/config.toml`, `~/.gemini/settings.json`) ship without `mcpServers` blocks. Outside users running mixed 4+1 would hit `memory_recall` failures the first time they declared a non-Claude lane. v1.0.0 framing is "production-ready for outside users" — that's not yet true with manual MCP wiring required.

Sprint 48's per-agent MCP auto-wire (the top candidate, surfaced by Grok's own self-analysis as the highest-leverage TermDeck wiring need) is the v1.0.0 gate. Once that ships AND a real mixed-agent sprint dogfoods the path end-to-end, v1.0.0 is the natural call. — 2026-05-01 17:00 ET

## Sprint close summary

**Sprint 47 closed at 2026-05-01 17:00 ET. Shipped as `@jhizzard/termdeck@0.16.0` + `@jhizzard/termdeck-stack@0.4.11`.**

**Lanes shipped:** all four — T1 frontmatter parser (15 tests), T2 boot-prompt resolver (16 tests), T3 inject mixed-agent dispatch with `acceptsPaste` contract extension (14 tests + 1 parity), T4 status-merger (23 tests). Total +41 net new tests. Suite: 847 root + 35 server-package = 882 / 0 fail / 3 skipped.

**Side-tasks shipped:** (a) Grok type-reclassification bug fix (PROMPT regex extended to also match model-mode footer; surfaced when Joshua opened a Grok panel for the smoke test and the dashboard header still said "Shell"); (b) cross-session pickup of Sprint 46 close-out hook env-var fix (`SUPABASE_SERVICE_KEY` → `SUPABASE_SERVICE_ROLE_KEY` in `memory-session-end.js` + tests/README; 51/51 hook-merge tests pass; unblocks Mnestra session-summary ingestion that was silently broken for 4+ days); (c) mixed-agent smoke test against live Grok panel (success — bracketed-paste validated, Grok wrote multi-paragraph self-analysis to STATUS.md).

**Sprint 46 deferrals:** all 14 roll forward to Sprint 48 unchanged (Sprint 47 substituted higher-value side-tasks above; the Grok type fix and hook env-var fix were higher-leverage than cosmetic title-attr gaps).

**Smoke test outcome:** SUCCESS. Grok 4.20 Reasoning on Bun+OpenTUI accepted the 3677-byte bracketed-paste payload intact, executed read_file + edit_file tools, surfaced the v1.0.0 gate (per-agent MCP auto-wire). Grok's PART 2/3 prose response is a real-world fixture for T4's status-merger free-form-idiom branch.

**Version decision:** v0.16.0 (NOT v1.0.0 — see § v1.0.0 decision above). v1.0.0 inflection moves to Sprint 48 close.

**Publish status:** NOT yet published. Joshua publishes via Passkey per RELEASE.md (`npm publish --auth-type=web` from root + stack-installer). Commands staged in the session-end email.

**Push status:** committed + pushed to origin/main at sprint close (HEREDOC commit message bundles T1-T4 + Grok type fix + hook env-var fix + version bumps + CHANGELOG + Sprint 48 PLANNING stub + RESTART prompt).

**Sprint 48 candidates** (top-three, ordered by leverage):
1. **Per-agent MCP auto-wire on panel spawn** (v1.0.0 gate). Each adapter declares its MCP-config schema + path; on panel spawn, TermDeck ensures the Mnestra endpoint is registered. Probe Codex/Gemini's actual schema first.
2. **Actual mixed-agent dogfood.** Declare `T1=codex / T2=gemini / T3=grok / T4=claude` in PLANNING.md frontmatter; run an end-to-end real sprint with mixed lanes. Validates T4's status-merger idiom-detection against actual non-Claude STATUS posts.
3. **Grok 16-sub-agent observability.** Per the smoke-test self-report, Grok's `task` tool emits parseable stdout patterns (`"Delegating to [agent] sub-agent: …"`, `"[sub-agent complete]"`). `session.js` analyzer can pick these up; UX is a collapsible tree pane with agent pills.

Plus the 14 Sprint 46 carry-overs and Sprint 47 follow-ups: T1+T2 wiring (resolveBootPrompt-into-injectSprintPrompts so scripts can declare `{ sessionId, agent, vars }` and have the helper resolve internally), AGENT-RUNTIMES.md § 5 update for the 8-field contract, Grok adapter SQLite extraction in the memory hook.

**Joshua's roadmap context:** post-Sprint-47 pivot to other projects (TheHarness, BHHT, etc.) for some weeks. Sprint 48 fires when Joshua returns and either (a) actually dogfoods mixed 4+1, (b) ships per-agent MCP auto-wire to unlock v1.0.0, or (c) reacts to Brad-tier feedback. Restart prompt at `docs/RESTART-PROMPT-2026-05-02-sprint-48.md` is paste-ready for the next session.
