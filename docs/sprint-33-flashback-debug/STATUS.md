# Sprint 33 — Flashback debug — STATUS

Append-only coordination log. Each Tn posts CLAIM / FINDING / FIX-PROPOSED / DONE / BLOCKED.

Format: `[YYYY-MM-DDTHH:MM:SSZ] [Tn] <KIND> <details>`. Use `date -u +%Y-%m-%dT%H:%M:%SZ`.

---

- [2026-04-26T21:25:00Z] [T5/orchestrator] Sprint opened. PLANNING.md + four briefings written. v0.7.0 just shipped (commit 41170ae, npm @jhizzard/termdeck@0.7.0 + @jhizzard/termdeck-stack@0.3.0). Awaiting four parallel terminals for Flashback debug.
- [2026-04-26T21:38:00Z] [T5/orchestrator] Pre-sprint intel: ran tests/flashback-e2e.test.js against current code (no live external server flag). Result: ✖ "error in PTY → output analyzer → mnestra-bridge query pipeline fires" FAILS at 8.7s; ✔ "mnestra bridge returns well-shaped response when there are zero hits" passes. So the bridge CONTRACT is intact (zero-hit response shape correct) but the actual end-to-end pipeline (pty → analyzer → bridge query → response → ws frame) does NOT fire within 8.7s. T1+T2+T3 should treat this as the canonical reproduction. T4: this is your starting point for Phase A.
- [2026-04-26T21:40:09Z] [T1] CLAIM packages/server/src/session.js + tests/analyzer-error-fixtures.test.js — auditing analyzer + error detection layer (PATTERNS.error, _detectErrors, onErrorDetected wiring, rate limit).
- [2026-04-26T21:41:30Z] [T2] CLAIM packages/server/src/rag.js + packages/server/src/mnestra-bridge/index.js + index.js:775-817 (proactive_memory emit) + tests/failure-injection.test.js — auditing bridge wiring, three-mode dispatch, RPC body shape, WS frame emit.
- [2026-04-26T21:43:00Z] [T2] FINDING — CONFIRMED-OK: T2 lane is intact. Evidence:
  1. **RAGIntegration is constructed unconditionally** at index.js:114 (`new RAGIntegration(config, db)`); enabled flag only gates telemetry push, not Flashback bridge calls. `rag.record()` writes `rag_events` to SQLite regardless of `enabled`, so the e2e test's `rag_events` poll is NOT gated by RAG-disabled config.
  2. **session.onErrorDetected wired** at index.js:781 inside the POST /api/sessions handler, immediately after `pty.spawn()` succeeds and before any input can hit the PTY. Wiring runs even when `rag.enabled === false` (intentional, see comment lines 776-780).
  3. **Bridge dispatch** (mnestra-bridge/index.js:241-249) routes by `config.rag.mnestraMode || 'direct'`. Live config is `direct`. queryDirect builds the EXACT 8-key body (`query_text, query_embedding, match_count, full_text_weight, semantic_weight, rrf_k, filter_project, filter_source_type`) — Sprint 21's fix held, no recency_weight/decay_days regression.
  4. **Bridge contract test passes** ("mnestra bridge returns well-shaped response when there are zero hits"). Since this test hits `/api/ai/query` which calls the same `mnestraBridge.queryMnestra(...)` the Flashback path uses (index.js:1261), it directly proves the bridge layer talks to Mnestra successfully and maps the response shape correctly.
  5. **proactive_memory WS emit** (index.js:802-812) correctly checks `sess.ws && sess.ws.readyState === 1`, sends `{ type: 'proactive_memory', hit }`, and has diagnostic `[flashback]` logs at every branch (no-match / hit-sent / ws-not-open / send-failed). No server-side similarity threshold — first hit always sent (filtering happens upstream in Mnestra's RRF).
  6. **The e2e failure at 8.7s is upstream of T2 lane**. The failing assertion is on `rag_events` row presence (status_changed→errored). That row is written by `rag.onStatusChanged()` → `record()` → `logRagEvent()`, fired from `_detectErrors` in session.js BEFORE onErrorDetected runs. If the analyzer doesn't detect the error, no rag_events row, test fails — and the bridge is never called either. Bridge wiring beyond the analyzer cannot be the cause.
- [2026-04-26T21:43:30Z] [T2] FINDING — minor observation (NOT the bug, NOT blocking): index.js:781-793 calls `mnestraBridge.queryMnestra({ ..., sessionContext: { type, project, lastCommands, status } })` but does NOT pass `cwd` (neither top-level `cwd` nor inside `sessionContext`). The bridge has a `cwd || sessionContext.cwd` fallback (mnestra-bridge/index.js:235) for project resolution when `sess.meta.project` is empty. Sessions created without a project (like the e2e test's `bash` session) therefore route with `filter_project = null` (search-all), which is benign for Flashback (more hits, not fewer) but slightly off-spec. Surgical 1-line fix would be `cwd: sess.meta.cwd` in the queryMnestra call. Holding unless T4 wants it.
- [2026-04-26T21:43:45Z] [T2] DONE — T2 lane (rag.js + mnestra-bridge + proactive_memory WS emit) audited end-to-end. CONFIRMED-OK. The Flashback silence is NOT in the bridge layer. Look at T1 (analyzer pattern matching `cat /nonexistent` output) or T3 (Mnestra returning unexpected shape — though contract test rules this out for direct mode). No fix proposed in T2 lane. No version bump, no CHANGELOG, no commit.
- [2026-04-26T21:41:51Z] [T3] CLAIM Mnestra query path. Read-only on `~/Documents/Graciella/engram/`; live SQL via DATABASE_URL.
- [2026-04-26T21:46:30Z] [T3] FINDING — **BROKEN-AT project tag (active regression).**
  Live store snapshot (petvetbid, `aws-1-us-east-2.pooler.supabase.com`):
  - **Corpus:** 4,892 total / 4,889 searchable rows. 100% have `embedding` populated, all `vector(1536)`. `memory_status_aggregation()` returns `total_active=4889, sessions=214` — schema healthy on Josh's store, no embeddings missing.
  - **Project tag distribution:** `pvb` 1,599 · **`chopin-nashville` 1,126** · `chopin-scheduler` 996 · `global` 378 · `gorgias` 368 · `gorgias-ticket-monitor` 117 · `imessage-reader` 114 · **`termdeck` 68** · `podium` 62 · `antigravity` 30 · `claimguard` 10 · others ≤6. Notes: `pvb`/`PVB` collision (1,599 vs 2 rows) and a stray `engram`/`rag-system` legacy tag — minor, not the headline.
  - **Smoking gun #1 — `chopin-nashville` is mostly mis-tagged TermDeck content.** The 10 most-recent `chopin-nashville` rows (all created 2026-04-26) are 100% TermDeck/Sprint 32 facts: theme persistence, mtime cache, session.js getter, app.js reset link, contract test counts. Same content shape as the 68 `termdeck` rows.
  - **Smoking gun #2 — bug is ongoing, not a backfill remnant.** Writes by week to `chopin-nashville` vs `termdeck`: week of 2026-04-20 → 198 vs 11 (18× ratio); week of 2026-04-13 → 788 vs 29 (27× ratio). Sprint 21 T2 was supposed to fix this; either the fix never landed in the upstream writer or a later change reverted the project resolution. In the last 7 days, 228 of 232 new `chopin-nashville` rows are `source_type=fact` — fingerprint matches Rumen-synthesis output.
  - **Smoking gun #3 — Flashback's project filter excludes the relevant rows.** Live RPC test, query `"websocket connection refused on port 3000"`, real OpenAI 1536-d embedding:
    - `filter_project='termdeck'` (what the bridge sends for a TermDeck panel) → top-10 hits are version-history bug_fix entries (Brad's v0.6.x incidents, Unagi demo retrospectives). None about WebSockets, ports, or connection failures. Scores 0.124–0.163.
    - `filter_project=NULL` (cross-project) → top-10 hits are exactly the WebSocket / port-3000 / Flashback-e2e content one would expect. **Every single one** is tagged `chopin-nashville` or `global` — none `termdeck`. Scores 0.120–0.158.
    The strict `WHERE m.project = filter_project` clause in `memory_hybrid_search` walls off the 1,126 `chopin-nashville` rows entirely; the 68 `termdeck` rows that remain are mostly post-mortems and don't match operational error patterns.
  - **NOT broken:** RPC signature (still 8 args, matches bridge `queryDirect` call site verbatim); embeddings (4,889/4,889 populated, all 1536-d); `memory_status_aggregation()` works; no threshold cutoff exists in the SQL or in `mnestra-bridge/index.js` (Flashback is not threshold-gated, so the score distribution above is a non-issue — top 10 always returned regardless of magnitude); zero `null_embedding` rows means the ingest path is not silently dropping embeddings.
  - **Important interaction with T2's CONFIRMED-OK + minor finding:** T2 noted the bridge falls back to `filter_project=null` when `sess.meta.cwd` isn't passed (e2e test path → "search-all", which T2 called *benign*). Against the live store, search-all actually *helps* — it surfaces the chopin-nashville-tagged TermDeck rows that the project filter would otherwise hide. So the e2e test's silence is genuinely T1-territory (analyzer not detecting the error), and Josh's live silence is T3-territory (project filter applied to a panel with `meta.project='termdeck'` returns the wrong 68-row slice).
- [2026-04-26T21:46:30Z] [T3] FIX-PROPOSED — TWO surgical actions, both safe, NEITHER committed:
  1. **One-shot SQL backfill** (Mnestra-side, ~10 LOC). Re-tag the obviously-TermDeck rows from `chopin-nashville` → `termdeck`. Conservative WHERE keyed off content fingerprints unique to the stack:
     ```sql
     UPDATE memory_items
     SET project = 'termdeck'
     WHERE project = 'chopin-nashville'
       AND (
         content ILIKE '%termdeck%'
         OR content ILIKE '%mnestra%'
         OR content ILIKE '%rumen%'
         OR content ILIKE '%flashback%'
         OR content ILIKE '%xterm.js%'
         OR content ILIKE '%node-pty%'
         OR content ILIKE '%@jhizzard/%'
       );
     ```
     **Recommend dry-run first:** swap `UPDATE…SET…` for `SELECT count(*)` and post the count to STATUS.md before committing. Reversible via `superseded_by` if any row is mis-classified the other direction.
  2. **Upstream writer fix** (T2's lane — `resolveProjectName` in `packages/server/src/rag.js`, plus Rumen's project-tagging during `extract`). Without this, the backfill repeats every day. Suspected: `resolveProjectName` matches the deepest config.yaml-known path segment, which for `~/Documents/Graciella/ChopinNashville/SideHustles/TermDeck/termdeck` is the `chopin-nashville` ancestor before reaching the leaf `termdeck`. T2 already audited rag.js for Flashback-bridge correctness; the project-resolution audit is a separate pass.
- [2026-04-26T21:46:30Z] [T3] DONE — corpus + embeddings + RPC + status all healthy; mis-tagged-project regression is the silent-Flashback root cause for TermDeck panels with `meta.project` set. Read-only against `~/Documents/Graciella/engram/`; only SELECTs (no UPDATE/DELETE) against petvetbid; no commits, no version bumps. Hand-off: backfill SQL + writer-side audit need orchestrator/T4 sign-off.
- [2026-04-26T21:43:57Z] [T4] CLAIM tests/flashback-e2e.test.js — adding Phase A console-level instrumentation (WS frame log, session.meta sampling per poll, all rag_events for the session, transcript snapshot) so a failure dump shows which layer fell over.
- [2026-04-26T21:48:30Z] [T4] FINDING — Phase A. Ran instrumented test against the live TermDeck (http://127.0.0.1:3000, Mnestra reachable, 4,889 memories). Test fails as expected: `error in PTY → output analyzer → mnestra-bridge query pipeline fires (8606ms)`. **The failure is a TIMEOUT, not a wrong-shape assertion.** The instrumentation pinpoints WHERE the pipeline died:
  - **bash spawn ✓** — pid=33707, initialStatus=`active`. Session created at 7ms.
  - **WebSocket ✓** — opened at 15ms, connected to `?session=<id>`.
  - **Input post ✓** — `cat /nonexistent/file/path\n` POSTed at 518ms, returned 200.
  - **bash actually emitted the error string** at 521ms (captured verbatim from a WS `output` frame): `"cat: /nonexistent/file/path: No such file or directory\r\n"`. This is exactly the line `PATTERNS.error` is supposed to match.
  - **The bash prompt arrives in the same chunk window** at 521ms: `"bash-3.2$ "`. So the analyzer sees the error AND the prompt nearly back-to-back.
  - **Analyzer never flips to `errored`.** session.meta samples: 521ms `status=active`, 733ms `status=idle` — straight to idle, skipping `errored`. The rag_events row that the test polls for confirms it: `status_changed payload={"from":"active","to":"idle","detail":"Ready","type":"shell"}` at 3699ms. The only status_changed row is active→idle.
  - **Zero `proactive_memory` WS frames.** Bridge was never called. WS frame counts: `meta:1, output:7, status_broadcast:4`. T2's CONFIRMED-OK is consistent with this — the bridge layer is fine, but it never gets invoked because the analyzer never marks the session errored.
  - rag_events for this session in chronological order: `command_executed` (input handler), `session_created`, `status_changed active→idle`. **No `error_detected` event, no `status_changed *→errored` event** — the upstream proof that `_detectErrors` did not flip status to 'errored' for this PTY chunk.
  - **Diagnosis (e2e lane):** the failure is in T1's layer — analyzer / `_detectErrors` / `PATTERNS.error` matching for the "cat: <path>: No such file or directory" shape. Two plausible causes (T1 to confirm): (a) the regex doesn't match this exact prefix shape, or (b) the prompt-pattern detection in the same chunk wins over the error-pattern detection and forces status to 'idle'. Either way, the e2e test's silence is independent of T3's project-tag regression — the e2e test runs against a freshly-created `bash` session with `meta.project=null`, so the bridge would route `filter_project=null` (search-all) IF it were ever called. T3's bug only bites the live demo; T4's bug bites the e2e test (and bites Josh too whenever an error chunk includes the prompt).
- [2026-04-26T21:48:30Z] [T4] DONE — Phase A. Instrumentation landed in tests/flashback-e2e.test.js (test-only file, no source touched). Awaiting T1 FINDING before writing POSTMORTEM.md (Phase B). No version bump, no CHANGELOG, no commit.
- [2026-04-26T21:46:35Z] [T1] FINDING — **BROKEN-AT analyzer (regex coverage gap).** PATTERNS.error in session.js:68 does NOT match the most common Unix shell error shape: `<cmd>: <path>: No such file or directory`. This is the canonical fixture the e2e test injects (`cat /nonexistent/file/path` → `cat: /nonexistent/file/path: No such file or directory`), and it's the exact byte sequence T4's Phase A instrumentation captured at 521ms.
  - **The code comment at session.js:64-67 LIES.** It says PATTERNS.error "Includes the literal 'No such file or directory' phrase because many Unix tools (cat, ls, cd, rm, etc.) report filesystem misses in plain English without ever emitting the ENOENT errno code. Flagged as a gap by Rumen's first production kickstart insight on 2026-04-15." But the regex itself was never updated to include that phrase. Pure "documentation is not verification" — a known gap stayed in the comment but was never enforced by the code.
  - **PATTERNS.errorLineStart (the Claude-Code variant) DOES include "No such file or directory"**, but it's `^\s*(...)` line-anchored — it requires the line to START with the keyword. Real bash output prefixes the phrase with `<cmd>: <path>: ` (mid-line content), so even Claude-Code panels miss the same class of errors.
  - **Confirmed via direct probe** against `Session.prototype._detectErrors`: pre-fix, `cat: /nonexistent/file/path: No such file or directory` → `meta.status='active'` (no flip). T4's instrumented sample matches: status=active at 521ms, status=idle at 733ms, never errored. **Same root cause for both the e2e test failure AND Josh's daily silence.**
  - **Probed coverage gap is broad** (table inline below). PATTERNS.error misses: `cat`/`ls`/`cd`/`rm` ENOENT or Permission Denied, `bash`/`zsh` `command not found`, `curl: (NN) Could not resolve host`, `Segmentation fault`, `ModuleNotFoundError:`, lowercase git `fatal:`. PATTERNS.error catches: `Error:`-prefix, `error:`-prefix, `Traceback`, `npm ERR!`, `error[Ennn]:`, `Uncaught Exception`, capital `Fatal:`. So the analyzer fires on structured Node/Python/Rust errors but is silent for the plain-English shell errors that dominate real terminal usage.
  - **Sprint 21 wiring fix held.** session.onErrorDetected IS set in index.js:781 (T2 confirmed). The `[flashback] error detected … but no onErrorDetected handler wired` log line is unreachable in current code paths — wiring runs in the POST /api/sessions handler before any input can hit the PTY. The bug is upstream of wiring: the analyzer never reaches the wiring branch because the regex doesn't match.
  - **Rate limit is fine** (30s, `_lastErrorFireAt` initialized to 0). Not the culprit. session.test.js:195 already locks this in.
  - **NOT broken in T1 lane:** _detectErrors logic flow (exitCode-0 guard, status flip, onStatusChange mirror, rate limit, lastCommand/tail context); existing fixture tests (analyzer-error-fixtures.test.js — 2 passes); onErrorDetected wiring (T2 confirmed); rate limit. The fix is purely the regex.
- [2026-04-26T21:46:35Z] [T1] FIX-PROPOSED — Surgical, T1-lane-only, **62 LOC across 2 files (14 src + 48 tests)**. NOT committed.
  1. **session.js (+14 LOC, -2 LOC).** Add `PATTERNS.shellError`:
     ```js
     shellError: /(?:^|\n)(?:[^\n]*:\s+(?:.*?:\s+)?(?:No such file or directory|Permission denied|Is a directory|Not a directory|command not found)\b|[^\n]*?\(\d+\)\s+Could not resolve host\b|\s*ModuleNotFoundError:\s+\S|\s*Segmentation fault\b|\s*fatal:\s+\S)/m
     ```
     Each branch requires real shell-error structure (colon-prefix `<cmd>: <path>: <phrase>`, or anchored stand-alone keywords). Tested against an adversarial prose suite — no false-positives on Claude Code prose mentioning "no such file or directory", "permission denied", "segmentation fault", "ModuleNotFoundError", "command not found" in narrative context. Update `_detectErrors` to fall through to shellError when the primary pattern misses:
     ```js
     if (!pattern.test(clean) && !PATTERNS.shellError.test(clean)) return;
     ```
     Comment explains why (the v0.7.0 docstring promise that PATTERNS.error catches Unix tool errors finally enforced by code).
  2. **tests/analyzer-error-fixtures.test.js (+48 LOC).** Add `SHELL_ERROR_SHOULD_TRIGGER` (10 fixtures: cat ENOENT, ls cannot access, bash/zsh command not found, rm Permission denied, curl resolve, Python ModuleNotFoundError, segfault, lowercase git fatal, multi-line w/ prompt) and `SHELL_ERROR_SHOULD_NOT_TRIGGER` (7 prose fixtures: ENOENT/permission/segfault/ModuleNotFoundError/command-not-found mentioned in prose, cat help text, ls listing). Two new tests assert each direction. All 4 tests in this file now pass.
  - **Net test impact:** `tests/analyzer-error-fixtures.test.js` 4/4 pass. `packages/server/tests/session.test.js` net-positive: pre-existing failures `'ENOENT: no such file or directory'` (no `<cmd>:` prefix) and `'"GET /api/broken HTTP/1.1" 503 -'` (HTTP-5xx detection) and `stripAnsi removes standard CSI sequences` were already failing on HEAD (verified by checking out HEAD's session.js and re-running). My fix turns the pre-existing failure `'zsh: command not found: kubectl'` GREEN (now passes). No previously-passing test breaks.
  - **End-to-end unit verification:** post-fix, `Session.analyzeOutput('cat /nonexistent/file/path\r\ncat: /nonexistent/file/path: No such file or directory\r\nuser@host:~$ ')` flips meta.status to 'errored' and fires onErrorDetected with `tail` set to the stderr line. T4's e2e test should now pass once orchestrator integrates and a server restart picks up the new session.js.
  - **Coordinated-bug nuance:** T3's chopin-nashville mis-tagging regression hides Josh's relevant memories from the bridge filter; T1's regex gap means even when memories are reachable (e2e test runs against a `meta.project=null` session → bridge sends `filter_project=null` → search-all surfaces them), the analyzer never invokes the bridge in the first place. **Both fixes are needed for Flashback to fire end-to-end on Josh's live workflow.** T1's fix alone unblocks the e2e test.
- [2026-04-26T21:46:35Z] [T1] DONE — analyzer + error detection lane fully audited. PATTERNS.error has a "documentation is not verification" gap that silenced Flashback for the most common Unix shell errors. Surgical regex addition (+14 src LOC) plus locked-in fixtures (+48 test LOC). All changes are uncommitted, no version bump, no CHANGELOG. Files touched: `packages/server/src/session.js`, `tests/analyzer-error-fixtures.test.js`. Hand-off to T4/orchestrator for POSTMORTEM and integration.
- [2026-04-26T21:50:00Z] [T4] READY — Phase B complete. `docs/sprint-33-flashback-debug/POSTMORTEM.md` written, synthesizing all four lanes. Headline: TWO independent converging bugs (T1 analyzer regex coverage gap + T3 chopin-nashville project-tag regression). T1's fix alone unblocks the e2e test; both are needed for Josh's live workflow. T4 recommendation in the postmortem is Option A (bundle T1 src+tests + Phase A instrumentation as v0.7.1; ship T3 backfill + writer fix in Sprint 34). The Phase A instrumentation (uncommitted, in tests/flashback-e2e.test.js) is itself the regression defense — turns black-hole timeouts into labeled fault-isolating diagnostics, would have prevented the 5-sprint silent-vaporware gap. No version bumps, no CHANGELOG edits, no commits — orchestrator owns the integration call.
