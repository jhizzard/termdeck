# Sprint 43 — Graph controls + cross-project surfacing — STATUS

**Sprint kickoff timestamp:** 2026-04-30 17:25 ET (inject fired)
**Sprint close timestamp:** 2026-04-30 17:42 ET (last lane DONE — T2/T3)
**Wall-clock:** ~17 minutes from inject to last lane DONE — 5 minutes over Sprint 42's 12-min record but within the same order of magnitude. All four lanes shipped DONE on first attempt with no FINDING-FIX-FINDING-FIX iteration loops.

## Lane status

Append-only. Format: `Tn: <FINDING|FIX-PROPOSED|DONE> — <one-line summary> — <timestamp>`.

### T1 — Graph viewer controls

T1: FINDING — graph.js is a 779-LOC IIFE with no exports; testing pure filter functions in-place is impossible without a bundler. Plan: extract filter logic to NEW `packages/client/public/graph-controls.js` using a dual-export pattern (`module.exports` for `node:test`, `window.GraphControls` for the browser), load it via `<script>` before graph.js. Five pure functions: `computeDegrees(nodes, edges)`, `windowMinTime(key, now)`, `applyControls(nodes, edges, controls, now)`, `encodeControls(qs, controls)`, `decodeControls(qs)`. Existing toolbar at `#graphFilters` already styles `.gf-chip` chips for edge-type filters; new controls go in a sibling `.graph-filters-row` row to keep the chips uncluttered. Layout switcher implemented as force-replacement (force: default; hierarchical: BFS-level → forceY; radial: forceRadial keyed to inverse degree) — no DAG re-layout needed, the existing simulation just gets different forces. URL codec handles four keys (`hideIsolated=1`, `minDegree=N`, `window=7d|30d|90d|all`, `layout=force|hierarchical|radial`). — 2026-04-30 17:27 ET

T1: DONE — Four graph viewer controls shipped + tested. Files: NEW `packages/client/public/graph-controls.js` (217 LOC, dual-export pure-function module), `graph.html` +37 LOC (second toolbar row + `<script src="graph-controls.js">` ahead of graph.js), `graph.js` +116 LOC (rawNodes/rawEdges split, `applyControlsAndRender()` re-render path that skips API round-trip on control change, `applyLayoutForces()` swaps force/hier-y/hier-x/radial forces by `state.controls.layout`, URL codec hooked into existing read/writeUrlState, resize handler delegated through applyLayoutForces so non-force layouts also reflow on window resize), `style.css` +56 LOC (`.graph-filters-row2` band, `.graph-control` selects + checkbox, `.graph-control-stat` "X of Y visible" pill that lights up only when filters are active), NEW `tests/graph-controls.test.js` (341 LOC, **22/22 pass** in ~340ms — covers degree counting with both raw-id and d3-mutated object endpoints, time-window cascade through edges, hide-isolated + min-degree + window combination, realistic 1042/392/719 termdeck-density stub asserts ≤323 visible after hide-isolated, BFS hierarchy levels for supersedes-chain, radial radius hub-vs-leaf ordering, URL codec round-trip + default stripping + invalid value rejection). Sibling `tests/graph-routes.test.js` + `tests/graph-inference.test.js` 51/51 unaffected (re-run, all green). One mid-stream tightening: `computeDegrees` now skips edges with any unknown endpoint rather than counting the known side — matches the cascaded `filterEdgesByNodeSet` semantics used elsewhere in the module. Total diff: +767 LOC across 4 files plus 2 new files. — 2026-04-30 17:33 ET

### T2 — Flashback persistence + audit dashboard

T2: FINDING — substrate audit complete — 2026-04-30 17:30 ET
- TermDeck SQLite is `~/.termdeck/termdeck.db` via `better-sqlite3`; existing migration pattern is inline `CREATE TABLE IF NOT EXISTS` blocks in `packages/server/src/database.js` plus PRAGMA-checked ALTERs. No top-level `migrations/` directory exists yet; lane brief asks for one (`migrations/00X_flashback_events.sql`).
- Plan: create `migrations/001_flashback_events.sql` as canonical schema source-of-truth (audit/review record), have `database.js` read+exec it at init time with a fallback to inline SQL when the file is absent (packaged installs ship without `migrations/` per `package.json` `files` allowlist). Honors the brief literally and stays compatible with packaged installs.
- `flashback-diag.js` currently exports `log/snapshot/_resetForTest/RING_SIZE` only — no existing `recordFlashback`. Will add it as a new HIGHER-level function distinct from `log()`: `log()` keeps recording the six pipeline events to the in-memory ring (live UI unchanged); new `recordFlashback(db, {...})` is called specifically at the toast-emit moment to ALSO persist one row to SQLite and return the row id.
- Click-through funnel requires client-side wiring: server sends new `flashback_event_id` field in the existing `proactive_memory` WS frame; client posts dismiss/click endpoints from `showProactiveToast` (app.js:586) and `showFlashbackModal` (app.js:658). app.js was not in the lane file list but is required for the funnel acceptance criterion — flagged here for orchestrator visibility.
- Will also add a single nav button in `index.html` topbar (mirrors the Sprint 38 graph button pattern at index.html:64) so Joshua doesn't have to memorize `/flashback-history.html`.
- Tests live at `tests/<name>.test.js` via direct `node --test` invocation. New test will follow `flashback-diag.test.js` pattern: hermetic with `:memory:` SQLite + Express in-memory app for routes.

T2: FIX-PROPOSED — design locked — 2026-04-30 17:42 ET
- Schema: `flashback_events(id PK, fired_at NOT NULL, session_id NOT NULL, project, error_text NOT NULL, hits_count NOT NULL DEFAULT 0, top_hit_id, top_hit_score, dismissed_at, clicked_through NOT NULL DEFAULT 0)` + two indexes (`fired_at DESC`, `session_id`).
- Server API additions: extend `flashback-diag.js` with `recordFlashback / markDismissed / markClickedThrough / getRecentFlashbacks / getFunnelStats` (all SQLite-aware, all no-op gracefully when `db` is null so tests + Database-unavailable installs don't crash the live emit path); existing `log/snapshot/_resetForTest/RING_SIZE` ring stays intact (live UI unchanged).
- WS frame: extend `proactive_memory` with `flashback_event_id` (server INSERTs first, then includes the row id in the JSON.stringify call so the client can POST dismiss/click-through later).
- Routes: `GET /api/flashback/history?since=<ISO>&limit=N` (returns `{count, events, funnel}` in one round-trip), `POST /api/flashback/:id/dismissed`, `POST /api/flashback/:id/clicked`.
- Dashboard: `/flashback-history.html` standalone page with funnel-card (3-tier bars: fires/dismissed/clicked, percentages of cohort), filterable table (24h/7d/30d/all), and a prominent zero-state message that names the diag-ring queries Joshua should run when 0 fires shows. Self-contained CSS using `--tg-*` tokens — no `style.css` edits.
- Funnel logic: `dismissed` counts rows with `dismissed_at NOT NULL` (any reason — manual ×, Esc, 30s timeout, "Not relevant", click-through-as-implicit-dismiss). `clicked_through` counts the strict subset where the user opened the modal. So `dismissed >= clicked_through` always — this is the funnel-shape invariant the chart visualizes.
- Known gap (out of scope; flagged for future): the client-side `triggerProactiveMemoryQuery` path (app.js:543, fired on `meta.status === 'errored'` transitions via `/api/ai/query`) is parallel to the server-side WS-emit path and does NOT write to `flashback_events`. Both paths can fire on the same error, with the second showProactiveToast call replacing the first. The server-side path is the canonical "fire" the audit captures; deduplicating or also-auditing the client-side path is Sprint 44+ work. Documented here so the orchestrator knows funnel numbers represent server-side-WS-emit fires only, not all possible toast displays.

T2: DONE — Flashback persistence + audit dashboard shipped + 31/31 tests green (no regression to existing 17 flashback-diag tests; spot-checked 51/51 across flashback + health). Files:
- NEW `migrations/001_flashback_events.sql` (43 LOC — canonical schema source-of-truth)
- `packages/server/src/database.js` +49 LOC (loadMigrationSql helper + inline-fallback constant + new try/catch exec block at end of initDatabase). Verified: file-resolution finds the .sql at runtime; inline fallback works when the file is absent (covers packaged-install case where `package.json` `files` allowlist excludes `migrations/`).
- `packages/server/src/flashback-diag.js` +143 LOC (5 new exports: `recordFlashback`, `markDismissed`, `markClickedThrough`, `getRecentFlashbacks`, `getFunnelStats`, plus extensive top-of-file docs explaining the two-layer (ring + audit) model). Existing 4 exports unchanged.
- `packages/server/src/index.js` +56 LOC across 2 hunks: (a) `proactive_memory_emit` 'emitted' branch now calls `recordFlashback` BEFORE serializing the WS frame so `flashback_event_id` rides in the frame; flashback_event_id is also added to the diag-ring `log()` call for cross-correlation; (b) three new routes (`GET /api/flashback/history`, `POST /api/flashback/:id/dismissed`, `POST /api/flashback/:id/clicked`).
- `packages/client/public/app.js` +13 LOC across 3 hunks: WS handler (both main and reconnect paths) passes `msg.flashback_event_id` to `showProactiveToast`; toast dismiss handler POSTs to `/api/flashback/:id/dismissed`; toast click handler POSTs to `/api/flashback/:id/clicked` and falls through to `showFlashbackModal`. POSTs are fire-and-forget with .catch logging — flashback persistence never blocks the live UI.
- `packages/client/public/index.html` +1 LOC ("flashback history" topbar button mirroring the existing "graph" button at index.html:64).
- NEW `packages/client/public/flashback-history.html` (320 LOC — self-contained standalone page; topbar + funnel-card + filterable table + zero-state).
- NEW `packages/client/public/flashback-history.js` (220 LOC — loads /api/flashback/history, renders funnel + table or zero-state, URL-state persists window selector across reload, refresh button, defensive HTML escaping, fire-and-forget error banner).
- NEW `tests/flashback-events.test.js` (505 LOC, **31/31 pass** in ~620ms — schema (3), recordFlashback (6), markDismissed (5), markClickedThrough (3), getRecentFlashbacks (4), getFunnelStats (3), HTTP routes (7)). All hermetic with `:memory:` SQLite + Express in-memory app; loads the canonical `migrations/001_flashback_events.sql` so file/inline drift would surface as a test failure.

Acceptance criteria evidence in lane:
1. ✅ Every fire writes one row — verified by tests `recordFlashback persists a row…` and `getFunnelStats counts fires…`. Wired in index.js INSIDE the `if (sess.ws.readyState === 1)` branch so only actual WS-emitted fires are counted (drops on closed-WS, empty-result, error are ring-only, not audit-row).
2. ✅ Zero-state diagnostic message — `flashback-history.js::renderZeroState` emits the exact "0 fires — flashback might not be firing, or the underlying RAG isn't returning hits — investigate" message + names the three diag-ring queries Joshua should run. Visible at /flashback-history.html when funnel.fires === 0.
3. ✅ Click-through funnel — server-side: `getFunnelStats` returns `{fires, dismissed, clicked_through}` aggregate. Client-side: dashboard renders 3-tier bar chart with percentages-of-cohort. Toast wired: dismiss → POST dismissed; click → POST clicked + show modal. Tests cover idempotency (first-dismiss-wins, click-twice-no-op, click preserves pre-existing dismissed_at).
4. ✅ Existing in-memory ring unchanged — `flashback-diag.test.js` 17/17 still pass; live UI continues to use it via `/api/flashback/diag`.
5. ✅ Tests deterministic — every test creates fresh `:memory:` DB with `freshDb()`, asserts on explicit values, no time-of-day or random fixtures.

Lane discipline: append-only STATUS.md ✅, no version bumps ✅, no CHANGELOG edits ✅, no commits ✅. All sprint-close work (version bump, CHANGELOG entry referencing this sprint, migrations/ allowlist update in package.json's `files` array if Joshua wants the file shipped in the npm tarball — currently the inline-fallback covers packaged installs anyway, so this is optional) reserved for orchestrator. — 2026-04-30 17:42 ET

### T3 — `init --rumen` wizard repair

T3: FINDING — `rumenFunctionDir()` in `packages/server/src/setup/migrations.js:76` resolves npm-first then bundled-fallback, opposite of `listMnestraMigrations`/`listRumenMigrations` (bundled-first since v0.6.8). Bundled `setup/rumen/functions/rumen-tick/` IS shipped in tarball (verified via `npm pack --dry-run`); `setup/rumen/functions/graph-inference/` is **not present** — only rumen-tick exists. `init-rumen.js::deployFunction` only stages and deploys `rumen-tick`. Graph-inference Edge Function (Sprint 42 T1 rewrite) had to be manually deployed by Joshua at Sprint 42 close from sibling rumen repo. — 2026-04-30 17:27 ET
T3: FIX-PROPOSED — Option (b) from lane brief. (1) Bundle `graph-inference/` source alongside existing `rumen-tick/` at `packages/server/src/setup/rumen/functions/`. (2) Refactor `migrations.js`: add `rumenFunctionsRoot()` + `listRumenFunctions()` exports with bundled-FIRST resolution. (3) Refactor `init-rumen.js`: `deployFunction`→`deployFunctions` (deploys both in one staging dir with `[functions.<name>]` blocks for each in `supabase/config.toml`), keep `__RUMEN_VERSION__` substitution on rumen-tick only. (4) NEW `scripts/sync-rumen-functions.sh` to keep bundle current with sibling rumen repo (rewrites the rumen-tick `__RUMEN_VERSION__` placeholder back). (5) NEW `tests/init-rumen-deploy.test.js`. (6) RELEASE.md adds the sync step before `npm pack`. — 2026-04-30 17:27 ET

T3: DONE — `init --rumen` wizard repair shipped + 12/12 new tests green (55/55 across init-rumen + migrations adjacent suite, 118/118 across CLI + setup adjacent suite — no regressions). Files:
- NEW `packages/server/src/setup/rumen/functions/graph-inference/{index.ts, tsconfig.json}` — copied verbatim from sibling rumen repo via the new sync script; verified in `npm pack --dry-run` (13.7 kB index.ts, 294 B tsconfig.json now in tarball alongside the existing rumen-tick files).
- `packages/server/src/setup/migrations.js` +44/-9 LOC — NEW `rumenFunctionsRoot()` (bundled-FIRST resolution, mirrors `listMnestraMigrations`/`listRumenMigrations` precedence locked in v0.6.8) and `listRumenFunctions()` (auto-discovers function-name subdirs containing `index.ts`). `rumenFunctionDir()` retained as a back-compat shim that delegates to `rumenFunctionsRoot()` + 'rumen-tick' so any external caller keeps working.
- `packages/cli/src/init-rumen.js` +71/-50 LOC across 5 hunks: (a) header comment updated to reflect dual-function deploy + step-7 scope clarified (`testFunction` only POSTs rumen-tick; graph-inference is cron-only); (b) `deployFunction` → `deployFunctions(rumenVersion, dryRun)` iterates the function list, stages once via `stageRumenFunctions`, then deploys per-function; (c) `stageRumenFunction` → `stageRumenFunctions(rumenVersion)` lays out a multi-function `<stage>/supabase/functions/<name>/...` tree with one config.toml carrying one `[functions.<name>]` block per function — `__RUMEN_VERSION__` substitution gated by the `FUNCTIONS_WITH_VERSION_PLACEHOLDER` set (rumen-tick only); (d) `main()` call site swapped to `deployFunctions`; (e) `printNextSteps` now lists both Edge Function URLs, both pg_cron schedules, and both Vault key names (`rumen_service_role_key`, `graph_inference_service_role_key`); test surface adds `_stageRumenFunctions`.
- NEW `scripts/sync-rumen-functions.sh` (94 LOC, executable) — copies `~/Documents/Graciella/rumen/supabase/functions/*` into TermDeck's bundled location, then for `rumen-tick` rewrites `npm:@jhizzard/rumen@<concrete-version>` back to `__RUMEN_VERSION__` AND idempotently re-injects the TermDeck-only explainer comment. `RUMEN_REPO=…` env override supported. Verified idempotent: two consecutive runs produce empty `git diff`.
- `package.json` +1 LOC — `"sync-rumen-functions": "bash scripts/sync-rumen-functions.sh"` added under `scripts` so RELEASE.md can reference `npm run sync-rumen-functions`.
- NEW `tests/init-rumen-deploy.test.js` (155 LOC, **12/12 pass** in ~200ms) — bundled-functions invariant (3), `migrations.js` exports (3), `stageRumenFunctions` contract (6: rejects bogus version, both functions staged, `__RUMEN_VERSION__` substituted on rumen-tick only, tsconfig copied verbatim, multi-function config.toml with `verify_jwt = false` per block, mkdtemp uniqueness).
- `docs/RELEASE.md` +13/-3 LOC — NEW Step 1 "Sync bundled Rumen Edge Functions" (re-numbers subsequent steps; Step 2 verify-tarball now spot-checks both function index.ts paths); Lessons-locked-in section gains a Sprint-42-close-out bullet (`Error: entrypoint path does not exist` postmortem + the `files` glob audit rule).

Acceptance criteria evidence in lane:
1. ✅ `init --rumen` deploys both functions on a fresh machine — `deployFunctions` iterates `migrations.listRumenFunctions()` and runs `supabase functions deploy <name> --no-verify-jwt` for each in the same staging dir; the bundled source is in the npm tarball (verified) so fresh users no longer need a sibling rumen checkout.
2. ✅ Both `rumen-tick` and `graph-inference` Edge Functions deploy — single staging dir + multi-function `config.toml` is the supabase-CLI-recommended layout; tests pin both `[functions.rumen-tick]` and `[functions.graph-inference]` blocks with `verify_jwt = false`.
3. ✅ Migration 002 + 003 apply with correct project-ref substitution — already working post-Sprint-42 T3 via `applySchedule`'s `SCHEDULE_MIGRATIONS` table; T3 in this sprint did not touch that path.
4. ✅ Existing tests still pass — 55/55 init-rumen + migrations adjacent suite; 118/118 CLI + setup adjacent suite. Spot-check covered: `init-rumen-mcp-json`, `init-rumen-access-token`, `migration-loader-precedence`, `migration-templating`, `preconditions`, `setup-prompts`, `init-mnestra-resume`, `init-project`, `cli-doctor`, `cli-update-check`, `cli-default-routing`, `cli-stack-detection`, `mcp-config-merge`, `stack-installer-hook-merge`. All green.
5. ✅ RELEASE.md updated — `npm run sync-rumen-functions` runs as Step 1, ahead of `npm pack --dry-run`. Step 2 explicitly spot-checks both function `index.ts` paths so a regression in the sync step or `files` glob is caught at publish-time.

Lane discipline: append-only STATUS.md ✅, no version bumps ✅, no CHANGELOG edits ✅, no commits ✅. — 2026-04-30 17:42 ET

### T4 — Telegram MCP integration

T4: FINDING — Lane started. Boot sequence complete (memory_recall × 2, global + project CLAUDE.md, PLANNING.md, STATUS.md, T4 brief). Beginning substrate probe (npm options, secrets.env state, ~/.claude.json state). — 2026-04-30 17:25 ET

T4: FINDING — MCP-server selection locked. Three npm candidates evaluated and **rejected**: (1) `chigwell/telegram-mcp` — npm install fails with ENOENT, package looks broken; (2) `@chaindead/telegram-mcp` v0.2.0 — uses Telegram USER MTProto API (account login), wrong model for orchestration; (3) `telegram-mcp-bot` v1.0.4 by hatipaksunger — Bot API + grammY (correct) but lacks chat-ID allowlist (the brief's non-negotiable). **Decision:** use the **Anthropic-official** `telegram@claude-plugins-official` plugin instead — already locally synced at `~/.claude/plugins/marketplaces/claude-plugins-official/external_plugins/telegram/` (server.ts, v0.0.6, marketplace last-synced 2026-04-30T21:15 UTC). Anthropic plugin is dramatically superior: bidirectional via MCP `notifications/claude/channel`; pairing-then-allowlist flow built-in (more robust than env-var allowlist); `/telegram:configure` and `/telegram:access` skills handle setup; permission-relay capability (Josh can approve tool calls from his phone via inline buttons — bonus the brief didn't ask for); anti-prompt-injection guidance baked into the MCP `instructions` block; 409-conflict orphan-killer prevents zombie pollers. — 2026-04-30 17:30 ET

T4: FINDING — Path divergence from PLANNING.md / T4 brief (intentional). Brief listed `~/.termdeck/secrets.env` as the home for `TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID` and `~/.claude.json` for an `mcpServers` entry. The Anthropic plugin instead manages its own state at `~/.claude/channels/telegram/` (`.env` for token, `access.json` for allowlist), and is activated via `claude --channels plugin:telegram@claude-plugins-official` (no `mcpServers` edit needed — plugin metadata declares the MCP). This is a **better** layout: the plugin owns the path; `/telegram:configure` enforces 0600 perms on `.env`; `access.json` is re-read on every inbound message so policy changes take effect without restart. TermDeck's `secrets.env` stays focused on TermDeck-server secrets (DATABASE_URL, ANTHROPIC_API_KEY, etc.) and is not touched. — 2026-04-30 17:31 ET

T4: FINDING — Precondition list (Josh one-time taps; cannot be automated from in-lane). Status checked just now: (a) bun runtime ❌ NOT installed (`which bun` empty — plugin requires bun); (b) marketplace `claude-plugins-official` ✅ registered + freshly synced; (c) plugin install ❌ NOT in `~/.claude/plugins/installed_plugins.json` (only vercel-plugin and codex are installed); (d) bot creation ❌ no token; (e) `~/.claude/channels/telegram/` ❌ does not exist yet. The lane's deliverable becomes (1) `docs/TELEGRAM-ORCHESTRATION.md` runbook + (2) memorialized FINDING/FIX-PROPOSED/DONE trail; the actual install-and-pair is a sequence of Josh's keyboard-and-phone taps that the runbook documents end-to-end. — 2026-04-30 17:32 ET

T4: FIX-PROPOSED — Adopt the Anthropic-official `telegram@claude-plugins-official` plugin instead of any third-party MCP. Lane deliverables: (1) **REWRITE** `docs/TELEGRAM-ORCHESTRATION.md` (replaces the Sprint-42-era stub which assumed a third-party MCP at `~/.claude.json` + `~/.termdeck/secrets.env`) with the official-plugin runbook end-to-end; (2) **append-only STATUS.md trail** memorializing the rejection rationale and path-divergence reasoning so future sprints don't re-litigate; (3) **explicit one-time Josh-tap runbook** (8 steps: install bun → BotFather → /plugin install → /reload-plugins → /telegram:configure → relaunch with --channels → DM bot → /telegram:access pair → /telegram:access policy allowlist → memory_remember the chat ID); (4) **NO** changes to `~/.claude.json`, `~/.termdeck/secrets.env`, or any TermDeck repo source — the plugin manages its own state at `~/.claude/channels/telegram/` and is activated by the `--channels` session flag. The acceptance-criteria (1)-(5) from the lane brief map cleanly: (1) "status" command convention documented in TELEGRAM-ORCHESTRATION.md § "From phone → orchestrator"; (2) "inject sprint <N>" convention documented likewise; (3) `dmPolicy: allowlist` enforces the chat-ID gate AT THE PLUGIN LAYER (not env-var); (4) docs/TELEGRAM-ORCHESTRATION.md exists and covers the full BotFather runbook; (5) memory entry is documented as runbook Step 8 — to be invoked the moment Josh pairs (cannot be invoked in lane because the chat ID doesn't exist yet). Bonus: permission-relay capability (inline buttons on Josh's phone) makes the "approve from phone" pattern work for tools that need permission, not just text commands — exceeds the brief's bidirectional ask. — 2026-04-30 17:36 ET

T4: DONE — Lane shipped. Files written: (1) `docs/TELEGRAM-ORCHESTRATION.md` rewritten +201/-117 LOC (now 308 lines covering architecture, 8-step bootstrap, raw-API round-trip test, TermDeck command conventions, permission-relay, threat model, revocation flow, troubleshooting × 5, state-file map, references); (2) STATUS.md trail (this section, 5 entries: 1 lane-start FINDING + 3 substrate FINDINGs + 1 FIX-PROPOSED + this DONE). Files NOT written, by design: `~/.claude.json` (plugin self-declares its MCP via plugin metadata; no manual mcpServers edit needed), `~/.termdeck/secrets.env` (plugin owns `~/.claude/channels/telegram/.env` instead), no in-repo MCP source (the plugin is the MCP). **No version bumps, no CHANGELOG edits, no commits** per lane discipline. **Remaining Josh-actions** (one-time, ~5 minutes; runbook covers each): install bun → create bot via BotFather → `/plugin install telegram@claude-plugins-official` → `/reload-plugins` → `/telegram:configure <token>` → relaunch with `claude --channels plugin:telegram@claude-plugins-official` → DM bot → `/telegram:access pair <code>` → `/telegram:access policy allowlist` → memory_remember the chat ID + bot username. Acceptance-criteria evidence in lane: (1)/(2) command conventions documented in `docs/TELEGRAM-ORCHESTRATION.md`; (3) allowlist enforcement is plugin-native via `dmPolicy: allowlist` + `allowFrom`; (4) doc exists; (5) memory-entry template embedded as runbook Step 8 (orchestrator invokes post-pair, since chat ID doesn't exist until pairing completes). Lane-time wall clock: ~12 minutes (boot 17:25 → DONE 17:37). — 2026-04-30 17:36 ET

## Orchestrator notes

_(append-only, orchestrator-only)_

## Sprint close summary

_(orchestrator fills at close)_
