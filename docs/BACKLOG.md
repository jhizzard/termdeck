# TermDeck Backlog

Single source of truth for everything queued beyond the active sprint set.

**Last updated:** 2026-08-02 (Sprint 69 "Vault Readability" closed FINAL-VERDICT GREEN 11:26 ET — ~31 min inject-to-verdict, zero RED verdict cycles. The PRIME candidate SHIPPED: the vault projection got its navigation layer — T1 hub-and-spoke topology (two-pass export, `## Members` piped wikilinks + `up:` list backlinks + `hub: true`, 429↔429 symmetric reverse map with 0 dangling on a real 9,169-note export), P0-2 frontmatter schema (unquoted UTC-ISO `date`, `project/`+`type/` nested tags, aliases, edge_count), generated `Home.md` + 31 per-project MOCs; T2 folder routing (merge-on-lowercased-slug, deterministic collision widening) + `Memories.base` (verified against official Bases 1.10.3 docs, `sort:` deliberately absent) + `.obsidian/graph.json` write-if-missing with two-layer manifest exclusion + P2-8 date-prefix filenames; T3 11-fence harness (byte-stability exhaustive 2-entry allowlist, TZ-invariance, R-3-scoped dangling scan, `requirePg` seam self-check) + net-new ARCHITECTURE/GETTING-STARTED vault docs. Root gate 1,343/1,338/0/5. R-1 corrected the atomicity premise (manifest-swept in place, NOT atomic). Wave: termdeck 1.19.0 / stack 1.17.0. Six Sprint-69 follow-on items added under §A. Prior: 2026-07-31 Sprint 84 "Write-Side Completion" closed FINAL-VERDICT GREEN 19:29 ET — Phase 4 of the execution arc pulled forward from ~08-13 because the promotion dry-run clock could not start on a 1-row inbox. §A `ingest_capture` extraction sweep SHIPPED (rumen `src/extract-sweep.ts` + ledger 007 + cron 008, covering all THREE extraction-less write origins — the sweep upgraded from hygiene to prerequisite when T3 found the promoter's own INSERT bypasses extraction too). Also landed: T1 Sheets intake harvester (dependency-free SA-JWT flow, one A–D fingerprint as dedup key AND pre-stamp guard, quarantine-not-coerce, default-OFF), T2 `memory_session_record` second write channel (engram 035 server-minted `web:` session_id + double-narrowed upsert; bridge gate default-OFF) + strict-map knob (default-OFF; the propose channel was ALREADY open to ChatGPT/Grok via the client_name heuristic), T3 inbox hygiene (engram 036 purge + health view) + promoter cron restore (`rumen-inbox-promote` @ `*/10`, both-name collapse). 6 T4 audit catches, all fixed in-flight, zero unresolved. Everything ships dark — all three flags default OFF. Wave: mnestra 0.12.0 / rumen 0.11.0 / termdeck 1.17.0 / stack 1.15.0 — publish + live-apply pending operator gate. Five Sprint-84 follow-on items added/updated under §A. Prior: same-day Sprint 83 "Graph Layer + Label Producer" closed FINAL-VERDICT GREEN + GREEN-REAFFIRMED, zero RED cycles — §A recall-citation label producer SHIPPED (engram 034 `mark_recall_cited_group` + `recall_group_id` end-to-end + `memory_cite`); graph layer landed: edge temporality + FK vocabulary + entities (034), write-time extraction, typed flashback expansion, rumen graph-consolidation, `termdeck vault export`. 034's §2c also repaired the solved-problem decay arms (Category-vs-SourceType item, decay half). Wave: mnestra 0.11.0 / rumen 0.10.0 / termdeck 1.16.0 / stack 1.14.0 — publish + live-apply pending operator gate. Seven Sprint-83 follow-on items added under §A. Prior: 2026-07-30 Sprint 82 "Calibration & Solved-Problem Recall" closed FINAL-VERDICT-4 GREEN — §A `memory_hybrid_search` two-phase rewrite SHIPPED as engram migration 033 and applied live; V4-3 bridge contract drift closed by the Sprint 82 `mapMemoryRow` unification; four new Sprint-82 follow-on items added under §A. Wave: mnestra 0.10.0 / rumen 0.9.0 / termdeck 1.15.0 / stack 1.13.0. Prior: 2026-07-06 Brad feedback intake: FR-7 `contextWindowPct` + Mnestra `memory_hybrid_search` perf rewrite logged — see §A / §C. The 2026-07-06 panel outage Brad reported originated entirely in his own local proxy + a separate (non-canonical) Supabase store on his machine — NOT TermDeck/Mnestra. No stack change owed for it; do not re-triage. Prior: 2026-07-05 Sprint 79 "elevation capture" closed FINAL-VERDICT GREEN on code/tests — doctrine elevation loop's second half: engram capture gates 028/029, rumen doctrine-scan 004/005, termdeck materialize+ratify. Wave: mnestra 0.8.0 / rumen 0.7.0 / termdeck 1.13.0 / stack 1.11.0. Live-landedness pending ORCH migration apply. **Next: Sprint 80** — advise→gate enforcement tiers + hook→ingest_capture wiring + web-write activation, ULTRAPLAN §6; worker terminals move to Opus 4.8.).

Active sprint plans live in `docs/sprint-N-<name>/`. When an item gets scoped into a sprint, move it from this file into that sprint's `PLANNING.md`. Don't leave it in two places — the sprint plan becomes authoritative once an item is in flight. New items surface from external feedback (Brad, future external testers), incident postmortems, and sprint close-out deferrals — add them here in the right category. Don't create ad-hoc roadmap docs; consolidate.

For history on closed items, see § Archived at the bottom. For deeper context on any closed item: `git log --grep '<sprint-id>'`, `gh pr list --state merged`, or `memory_recall(project="termdeck", query="<topic>")`.

**TermDeck has two known users today:** Joshua (daily driver) and Brad (only outside user). Anything that breaks the daily-driver flow for either is treated as a release-quality issue, not a backlog item.

---

## P0 — User-impacting blockers

(Items urgent enough to ship in a hotfix wave or be slotted into the next sprint, not deferred.)

- **None currently open.** P0 queue cleared with Sprint 67's hook field-deployment + CI hygiene + BACKLOG/CLAUDE.md trim work. Brad's daily-driver issues from Sprints 59-65 have all closed; the 2026-05-23 v1.6.1 hotfix cleared the most-recent v1.6.0 focus-mode regression.

---

## A. Correctness gaps (test debt + flagged bugs)

- **V4-1 Rumen `relate` embedding test coverage.** 4 failure modes (AbortController timeout, non-2xx, malformed vector, network error) handled in production code with zero unit tests. Mock `fetch` at the module boundary. Flagged by 4 auditors over Sprints 6/12.
- ~~**V4-3 Mnestra direct-bridge contract drift.**~~ **CLOSED — Sprint 82 (2026-07-30).** Bridge direct mode posts `recency_weight + decay_days` (10 args) but bundled `memory_hybrid_search` SQL takes 8; bridge reads `m.similarity`, SQL returns `score`. CONTRADICTIONS #1, open since Sprint 8. Closed by Sprint 82 T2's unified `mapMemoryRow` across all three bridge modes (direct/webhook/mcp): one row-similarity contract, `id` + `semantic_similarity` passthrough restored (the dropped `id` had made the Sprint 57 dismissal blacklist dead code), webhook mode reads `data.memories || data.hits`, and new-param sends (`p_decay_profile`) are 404-capability-latched against pre-033 stores.
- **V4-5 Auth brute-force rate limiting.** `auth.js` does strict-equality token comparison with no failure delay or IP throttle. Add in-memory bucket delay; switch to `crypto.timingSafeEqual`. Flagged by 3 auditors.
- **V4-6 Security/deployment doc drift.** `SECURITY.md` says `termdeck_auth` cookie; code uses `termdeck_token`. `DEPLOYMENT.md` says `/healthz`; server exposes `/api/health`. Deployment example uses `server.host`; code reads `config.host`. Doc-only, low risk.
- **Migration-001 idempotency.** `CREATE OR REPLACE FUNCTION` return-type collision when re-running migrations against an already-upgraded store. Doesn't affect fresh installs. Two fix options: explicit `DROP FUNCTION ... CASCADE` before recreate, or a `schema_migrations` tracking table.
- **Rumen-MCP gap.** Memories written via the MCP path land with NULL `source_session_id`, so they never reach Rumen Extract. Sprint-33 candidate at original triage; still open.
- **Root-glob `npm test` hang in the MCP/bridge test region** (Sprint 80 T4 baseline + T1 close, 2026-07-01). Exact `npm test` from repo root hangs/fails after the bridge section on some runs; per-package `node --test packages/server/tests/*.test.js` is fully green (512/512 at Sprint 80 close). Ruled pre-existing carry-forward by ORCH (Sprint 80 STATUS 21:40 ET). Likely intersects the repo-root glob gap below + mcp-bridge test lifecycle (unclosed handles/servers keeping the runner alive). Diagnose with `--test-force-exit` vs proper teardown; fix belongs with the glob consolidation.
- **Repo-root test glob gap** (Sprint 65 deferral + Sprint 67 T1+T4 surfaced). `package.json:34` runs only `packages/server/tests/**`, `packages/cli/tests/**`, `packages/stack-installer/tests/**`. Repo-root test files (`tests/init-mnestra-hook-refresh.test.js`, `tests/init-mnestra-cli-refresh.test.js`, `tests/init-mnestra-settings-migration.test.js`, `tests/stack-installer-hook-merge.test.js`, plus `tests/per-agent-hook-trigger.test.js`, `tests/ws-handler-contract.test.js`, `tests/escapehtml-client.test.js`) sit outside the glob — silent-skipped on CI. This is the test-side mirror of the Sprint 67 Class N drift T1 found. Either move these into a `packages/*` glob path, or extend the npm-test script to include repo-root. Surfaced explicitly by T4-CODEX in Sprint 67 (AUDIT-CONCERN 09:14 ET).
- ~~**Mnestra `memory_hybrid_search` two-phase rewrite (perf).**~~ **SHIPPED — Sprint 82, engram migration `033_two_phase_hybrid_search.sql`, applied live 2026-07-30.** Live function (engram migration 032) full-scans: the FTS branch computes `ts_rank_cd` with no `@@` prefilter, and the semantic branch never does `ORDER BY embedding <=> q LIMIT k` — so neither the GIN nor the HNSW index is used. Verified 2026-07-06 against the live fn on the canonical store. Rewrite as two-phase top-k: `fts @@ plainto_tsquery LIMIT k` ∪ HNSW top-k → RRF fuse → apply recency/type/project/recall_boost scoring only to the fused set (target <500 ms). Surfaced by Brad 2026-07-06. **Outcome note:** the original premise here was wrong on one point — the canonical store never had an FTS-servable index at all (001's GIN is `gin_trgm_ops`, which cannot serve `@@`; there is no generated `fts` tsvector column), so 033 CREATEs `memory_items_content_fts_gin` on `gin(to_tsvector('english', content))` plus a partial `memory_items_embedding_hnsw_live_idx` for the vector branch. Applied live at Sprint 82 close: receipt green, both EXPLAIN plans index-served at ~9.3k rows, vector branch 3.1 ms. The 028 `source_type` CHECK was NOT widened (as this item directed) — but see the new Category-vs-SourceType item below.
- ~~**Label producer for `memory_recall` citations** (Sprint 82 follow-on, P1)~~ **SHIPPED — Sprint 83 (2026-07-31).** Sprint 82 T3's headline finding: ~39k `memory_recall_log` rows, 0 real positives — ordinary `memory_recall` returned content inline with no identifier, so nothing on the dominant path could ever emit a citation. Closed end-to-end by Sprint 83 T1/T2: engram 034's group-keyed `mark_recall_cited_group` RPC (idempotent, honest-zero, `group_resolved_at` observed-negative stamping on the uncited complement), `recall_group_id` minted in `memoryRecall` and returned on every surface, `[n]` citation handles in recall text + cite-on-use prompt line, and the new `memory_cite` MCP tool. Acceptance proven through fit-platt's own label query (`positives` 0→1, with a negative control for the excluded shapes). Remaining tail is adoption, not architecture: install the cite-on-use CLAUDE.md snippet (ORCH close-out) and let real citations accumulate — see the fit-platt rerun item below.
- **NEW (Sprint 83 follow-on) — Root `tests/` directory not in termdeck's npm-test glob; 5 assertions provably stale since Sprint 81.** T1's close FINDING (verified pre-existing at HEAD via a detached worktree): `tests/migration-tracker.test.js` + `tests/migration-loader-precedence.test.js` never run under `npm test`/CI and are RED — they hard-code migration counts frozen at Sprint 61/62 that Sprint 81's 023–032 bundle-sync invalidated. Fix = re-derive the counts from the bundle (the drift test's approach, which is why it stayed honest) + an ORCH scope call on the glob itself (changing it lights up everything else in root `tests/` — merges with the existing repo-root glob-gap item above).
- **NEW (Sprint 83 follow-on) — Tighten rumen's alternate-guard test assertion.** `rumen/tests/graph-consolidation.test.ts:184-195` ("EVERY mutating statement carries the ownership predicate") accepts the string `'community_summary'` as an alternative to `OWNED_ROW_PREDICATE`, so it is weaker than the source it fences — the unguarded-`ON CONFLICT` defect class it exists to prevent could pass it. T4 residual note at FINAL-AUDIT (not blocking: the shipped source carries the correct owned-row `WHERE` + affected-row checks).
- ~~**`ingest_capture` writes bypass write-time extraction** (Sprint 83 follow-on)~~ **SHIPPED — Sprint 84 (2026-07-31), rumen `src/extract-sweep.ts` + migrations 007/008 + the `rumen-extract-sweep` Edge Function.** T3's ground-truthing widened the finding: not one gap but THREE (panel `memory_remember` where `MNESTRA_EXTRACT_ENABLED` never reaches the stdio MCP server — the majority of the miss; SQL-direct `ingest_capture`; and the promoter's own raw INSERT in `promote.ts`, which meant every proposal the Sprint-84 intake ramps promote would land extraction-less). The sweep selects on `memory_items` state, never on writer, so it covers all three for free. Shipped as a sibling Edge Function + 04:40 UTC cron (ORCH ruling R5 — a phase inside the tick would starve the insight phases against the 110s budget), ledger-idempotent, dry-run-first. Live applies + first `RUMEN_SWEEP_DRY_RUN=1` run are the operator tail.
- **SR-7: entity↔entity edge table** (Sprint 83 follow-on; **Sprint 84 update: recommendation NO, evidence path now built — revisit AFTER the first live sweep**). Entity-level triples (e.g. `recall_log.ts —part_of→ mnestra`) are extracted but unpersisted: `memory_relationships` is memory↔memory and 034's entity linkage is the `memory_entity_mentions` join table, so entity↔entity relations have nowhere to live. Sprint 84 T3 held at NO on zero evidence (0 extractions had ever run — building storage for a volume nobody has measured, against a schema nobody has seen real data shaped like) and instead made the decision cheap: the sweep caps triples, reports `triples_found` + a 10-row `triples_sample` per pass, and persists per-item counts in `rumen_extraction_sweep.triples_found`. After one live sweep the ruling is a query: `select count(*) filter (where triples_found > 0) as items_with_triples, sum(triples_found) as triples_total, round(avg(triples_found) filter (where triples_found > 0), 2) as avg_per_item from rumen_extraction_sweep;` — real density → SR-7 has a case and a shape; near-zero → the table was never needed, found out for the price of a column.
- **NEW (Sprint 84 follow-on) — `memory_sessions` RLS-was-off drift on clean chains: audit whether OTHER tables share the pattern.** T2's non-superuser replay showed a clean 001→034 chain leaves `memory_sessions` with RLS **OFF** — the daily driver's ON state was hand-applied, never chain-applied, so every fresh install inherited the gap silently (035 closes it for this table, idempotently, receipt-hard-failed). The pattern to audit: any table whose live RLS state came from an out-of-band hand `ALTER` rather than a migration (028's fix for `memory_items`/`memory_relationships` was this exact class). Sweep: for each public-schema table, replay the chain in a clean container and diff `pg_class.relrowsecurity` against the daily driver; any table ON-live but OFF-on-chain needs its idempotent enable folded into the next migration. Mechanical defense > operator discipline (§ Supabase RLS, gate 5).
- **NEW (Sprint 84 follow-on) — rumen repo vs termdeck-vendored migration chain: `003` divergence (historical, documented — do NOT renumber).** Repo `003` = `pg_cron_inbox_promote` (shipped in the 0.10.0 tarball, immutable); vendored `003` = `graph_inference_schedule`. Sprint 84 vendored 006/007/008 keeping repo numbers; repo-003 is superseded by 006 (both-name unschedule + canonical `rumen-inbox-promote` @ `*/10`), so the vendored chain never needs a copy of it. The divergence is frozen history: renumbering either chain would re-apply or skip migrations on installs that tracked the other numbering. If the vendored chain ever grows a tracker like mnestra's `MIGRATION_PROBES`, probe by artifact, not by filename number.
- **NEW (Sprint 84 follow-on) — Sheets no-CAS re-read→write window (documented residual; revisit ONLY if Google ships revision tokens).** The harvester's pre-stamp guard re-reads the row immediately before writing, narrowing the mutation exposure from the whole propose pass to the re-read→write gap — but the Sheets `values` API has no compare-and-swap, so a mutation landing inside those milliseconds is still stampable. Closing it fully needs a per-row revision token the API does not expose. Documented in the code comment and `docs/SHEETS-INTAKE.md`. No action possible on our side; parked here so nobody re-derives it as a new bug.
- **NEW (Sprint 84 follow-on) — fingerprint `ts`-edit re-propose semantics: ACCEPTED, promoter dedup is the collapse point.** The one-fingerprint-over-A–D design means editing ANY operator-authored cell on an already-forwarded row re-proposes it — including a `ts`-only edit, which re-proposes identical text under a new `captured_at`. Ruled correct (T1, ORCH-accepted): `ts` becomes `captured_at` in the payload, so it IS payload-affecting, and narrowing the key back is exactly how the original stamp-over-edited-content bug was born. Near-identical text collapses at the promoter's dedup gates, not the harvester's. Recorded as a decision so a future "why did this row propose twice" triage lands here instead of "fixing" the fingerprint.
- **NEW (2026-07-31 claude.ai-feedback audit) — recall recency-vs-keyword-density ranking.** Live reproduction: an on-topic ledger query ranked a Jul-18 session-state row #1 while the Jul-31 AUTHORITATIVE ledger row ranked #15/23, and the two freshest correct rows (embeddings present) missed the top-23 entirely — keyword-dense older siblings outrank dated close anchors. This, not the vector layer, is why a claude.ai session acted on stale state. Candidates: stronger recency term in the hybrid blend, or same-project topic-cluster down-ranking of older siblings once a newer dated anchor exists.
- **NEW (2026-07-31 claude.ai-feedback audit) — prose supersession is a no-op: convert to structural links.** Rows containing text like "SUPERSESSION WARNING / SUPERSEDES the …" never set `superseded_by` — only the `memory_remember(supersedes=)` parameter does, and retrieval DOES enforce it (verified end-to-end: superseded row went `is_active=false` and stopped surfacing). Two fixes: (a) a rumen/doctrine-scan pass that detects prose supersession claims and converts them into structural links; (b) expose the `supersedes` parameter to web surfaces via the bridge propose/session-record path so web writers can link at write time.
- **NEW (2026-07-31 claude.ai-feedback audit) — surface `scoreBandPercentile` in bridge-served recall.** Consumers keep misreading the calibrated RRF band (analytic ceiling ~0.0738, live p50 0.022 / p99 0.049) as degraded cosine similarity — a claude.ai session diagnosed a healthy vector layer as broken from scores of 0.01–0.07. Mnestra (formerly Engram) already computes a band percentile; render it alongside (or instead of) the raw score in bridge recall output so a 0.05 reads as "p99", not "5%".
- **NEW (2026-07-31 claude.ai-feedback audit) — claude.ai web surface writes carry `source_agent=NULL`.** All recent CIB rows written from the web surface landed with NULL `source_agent` — the connector identity is not being stamped through to `memory_items`, which weakens provenance filtering (`source_agents=["claude-web"]` misses them). Trace the propose→promote→insert path for where the stamp drops.
- **NEW (Sprint 83 follow-on) — Giant-component risk is unmeasured; dry-run before any consolidation cron.** At the 0.85 inference threshold, connected components may yield one blob swallowing most of the corpus — then `too_large` skips dominate and consolidation writes little (designed-for, but it means deliverable value is unknown). The FIRST `GRAPH_CONSOLIDATION_DRY_RUN=1` run must report the component size distribution before any cron is scheduled. Leiden is the upgrade path if one blob dominates; deliberately not built in Sprint 83.
- **NEW (Sprint 83 follow-on) — 033 FTS branch hides unembedded rows entirely.** T1 observation (pre-existing, untouched): the full-text branch carries `and m.embedding is not null` (`033:330`) on top of the vector branch's own guard, so a memory with no embedding is unreachable by BOTH hybrid branches — invisible to `memory_hybrid_search`, not merely un-ranked semantically. If the re-embed backfill ever lags, those rows are dark rather than degraded. Review whether that is intent or accident.
- **Category-vs-SourceType vocabulary drift** (Sprint 82 follow-on — **decay half RESOLVED by Sprint 83**, vocabulary half open). `debugging`/`convention` exist as `Category` values (`engram/src/types.ts`) but are not legal `source_type` under 028's CHECK, so `memory_hybrid_search`'s decay arms for them were unreachable dead code and `p_decay_profile='solved-problem'` reached only `bug_fix` rows (252 of ~631 — 40%). **Sprint 83's 034 §2c shipped Option B deliberately, with its own acceptance test**: the debugging arm now keys on `category='debugging'`, the dead `convention` arm is dropped, coverage 252→576 and every solved-problem-class row that *could* be buried is protected. Still open: the vocabulary confusion itself (two overlapping taxonomies that produced the bug) — reconcile the `Category`/`SourceType` unions deliberately if it bites again.
- **NEW (Sprint 82 follow-on) — Measure whether 001's unqualified HNSW index is redundant.** 033 added the partial `memory_items_embedding_hnsw_live_idx` (live-row predicate) and every vector query in the codebase (`memory_hybrid_search`, `match_memories`, `email_assistant_recall`) applies the live-row predicate — so 001's unqualified `memory_items_embedding_hnsw_idx` may now be pure write-amplification (every insert/update maintains two HNSW graphs). Measurement then decision — deliberately NOT dropped as a Sprint 82 side effect. Check `pg_stat_user_indexes.idx_scan` on the daily driver after a few weeks of 033 being live.
- **`fit-platt` rerun once real citations accumulate** (Sprint 82 follow-on, producer now SHIPPED by Sprint 83). `engram/scripts/calibration/fit-platt.ts` is built, tested, read-only, and EPV-gated (`INSUFFICIENT LABELS` below ≥100 positives absolute AND ≥20 per feature — refused honestly on 2 synthetic positives). The label producer now exists (Sprint 83: `memory_cite` + group-keyed citation RPC + observed negatives via `group_resolved_at`) alongside the Sprint 82 flashback funnel; once real citations clear the EPV gate, re-run the fit and flip the constants in `engram/src/calibration.ts` (`CALIBRATION_FITTED=true` + coefficients) — `score_calibrated` starts appearing in recall output with **zero other code change anywhere**.

## B. Adoption levers (cut new-user friction)

- **Supabase MCP in setup wizard.** Today's setup is many manual steps for new users (Supabase project, multiple SQL migrations, secrets.env, config.yaml, mnestra serve, termdeck). The setup wizard could use a Supabase MCP server to automate provision + migrations + secrets, cutting it to "paste 2 credentials, click 3 buttons." Highest-leverage adoption lever in the backlog. Detail in `IDEAS-AND-STATUS.md` § Installation Fear.
- **Fully-local Mnestra path (V5-1).** SQLite + local embeddings. Today Mnestra requires Supabase + OpenAI. Ship an opt-in local-only mode so TermDeck works with zero external dependencies. Reinforces Codex "MCP mode least-proven" concern.
- **One-click install button.** A web page or CLI wizard that detects what's installed → provisions Supabase (or connects to existing) → runs migrations → writes config → starts everything. Pairs with the Supabase MCP item above.

## C. DX improvements (orchestration/dashboard polish)

- **PTY drag/drop window rearrangement.** Dashboard panel reorder via drag-handle. Inject identifier is the session UUID, not visual position, so drag-reorder won't break inject. Pure CSS Grid + drag-handle work in `packages/client/public/app.js`. Light lift.
- **Control panel dashboard for agent permission prompts (V5-3).** Aggregate activity feed with Yes/No buttons so AI agents running in panels can be approved without context-switching to each terminal.
- **`app.js` feature-module split (V5-4).** Currently >5,900 LOC and growing. Split into `app.js` + `health.js` + `transcripts-ui.js` + `settings.js` + `chips.js` (Sprint 65 added project chips + ORCH-pin) + `graph.js` before the next major UI addition. Flagged by Sprint 12 auditor; pressure grows every sprint.
- **Multi-tab dashboard sync hardening.** Sprint 36 T3 added `config_changed` WS broadcast for the RAG toggle; the same pattern should generalize to other config edits and session lifecycle events. Probably grows out of dashboard work in a future sprint.
- **FR-7 (Brad, 2026-07-06) — Sessions API `contextWindowPct`.** `GET /api/sessions/:id` (+ the list endpoint) should expose in-window fill as an integer 0–100 so orchs can rotate on their own threshold (`if (session.contextWindowPct >= 75) rotate()`) without parsing JSONL or depending on our `contextLevel` thresholds. Root cause of the ask: `contextK` is JSONL file size, not in-window token fill — the two diverge after compaction, so `contextLevel` stayed `'ok'` at a real 83% fill and an orch rotation watchdog never fired (manual rotation required). Derive from the last assistant turn's usage (input + cache-read tokens) ÷ the model's context limit; update each turn at the same cadence as `contextK`; 0 for a fresh session, 100 when full. Well-scoped, self-contained. Post-CIB-week (after 2026-07-19).
- **FR-8 (Brad, 2026-08-02, via his R730 orchestrator; not urgent) — scoped read-only Mnestra credential mode.** A deployment that only registers `memory_recall`/`memory_search` shouldn't have to hold the full service-role key ("anything touching this shared store outside mnestra's own control should have to declare its actual blast radius" — same theme as the §A migration-safety item). Current state: `init-mnestra.js:228` hard-requires all four keys incl. service-role (shape-validated at `:167-168`). Split: **DB side cheap** — a `mnestra_reader` role granted EXECUTE only on the recall/search RPCs (the standing RLS-hygiene REVOKE+GRANT pattern); **one real wrinkle** — recall is no longer purely read since Sprint 83's citation layer (`recall_group_id` minting + `mark_recall_cited_group` telemetry), so scoped mode needs either a narrow grant on the telemetry path or graceful degrade (no group id, skip citing — the pre-0.11.0 behavior is the precedent); **cross-repo** — tool-registration gating belongs to `@jhizzard/mnestra` (engram repo), termdeck's share is a secrets-mode knob + doctor check. Low priority per Brad's own framing.

## D. Scaling concerns

- **Multi-user data validation (V5-2).** All current testing is single-developer. Run a multi-user session against Mnestra and confirm no cross-tenant leakage. Required before any beyond-localhost story.
- **MCP bridge verification (V5-5).** `mnestra-bridge` MCP mode (vs direct/webhook) jumps directly into `tools/call` against a stdio child with no repo-local test coverage. Add a contract test parallel to the direct/webhook paths.
- **Edge inference for legacy tables.** Sprint 38 ships edge inference for `memory_items` only. Legacy `mnestra_*_memory` tables and transcripts are out of scope. If those corpora get re-activated (V0.5.x event-stream RAG users), inference needs to extend.
- **Cross-Mnestra-instance graph federation.** Multi-machine Mnestra users (Joshua + Brad sharing structural-mvp memories) will eventually want graph queries that span instances. Sprint 38 ships single-instance only; federation is a later sprint.

---

## D.5 Sprint 66+ candidates

### Active

- **🪟 Dashboard text-break + copy-paste rendering directive (Joshua 2026-05-14 ~17:55 ET).** Two failure modes: (i) line breaks injected mid-content when pasting into a TermDeck terminal, AND (ii) extra spaces injected (likely between wrap-points or at line boundaries). Two compatible interpretations:
  - **Path A — render-as-one-line (visual).** When a multi-line text block is pasted into a TermDeck terminal (via the dashboard input box OR via cross-panel paste-from-output), the rendered visual collapses internal line breaks into spaces, displaying as one line. Doesn't alter the underlying PTY input — the agent receives the original multi-line text — but the operator sees a clean single line. Cheaper, doesn't touch semantics. Surface: client-side render hook in `packages/client/public/app.js` around the `clipboard.copy` / `clipboard.paste` event flow + xterm.js paste handling.
  - **Path B — programmatic formatting fix (semantic).** Strip stray line breaks at the boundary BEFORE they reach the PTY's input handler. Replace mid-paragraph `\n` with spaces (preserving real paragraph breaks); collapse runs of `\n\n+` into single `\n\n`; strip trailing whitespace on each line. Touches both client-side (paste from clipboard / from another panel) and server-side (the `/api/sessions/:id/input` boundary). Surface: server-side `normalizePasteShape(text)` helper invoked from the input route + client-side mirror for cross-panel paste.
  - **Likely root cause hypothesis.** xterm.js wraps long output lines at the column width with `\n` insertions; the user's clipboard captures those wraps as real `\n` characters; when pasted into another panel's input, the wraps become submit-keys in the receiving shell, breaking command continuity. Verify at scoping by examining one of Joshua's actual broken-paste blocks via DevTools clipboard inspection.
  - **Acceptance.** From a TermDeck panel running a wide-output command, copy a wrapped line, paste into another TermDeck panel's input box. Verify: the pasted text appears as one continuous line, the command executes correctly without premature submit, and the visual rendering doesn't show artifacts. Cross-panel paste between Claude / Codex / Gemini / Grok / shell panel types behaves consistently.

- **🪟 Multi-port TermDeck instances as a first-class workflow (Joshua 2026-05-14 14:00 ET ask).** The technical capability exists today — verified 2026-05-14 15:28 ET against running :3000 instance. `termdeck --port 3001 --no-open` boots cleanly, attaches to existing Mnestra, all 7 health checks pass on :3001, `POST /api/sessions` on :3001 returns 201, shell-session lifecycle works end-to-end, WAL-mode SQLite cross-process writes confirmed safe. Three rough edges to close before declaring "first-class":
  - **Per-instance SQLite** (e.g., `~/.termdeck/termdeck-3000.db` / `termdeck-3001.db`) so writes don't serialize across instances; eliminates the small perf cost + the theoretical UUID-collision risk + makes per-instance data exportable cleanly. Also fixes the orphan-cleanup bug at second-instance boot ("Marked 4 orphaned session(s) as exited" — the second instance reads SQLite, sees 4 sessions from :3000, marks them exited at boot).
  - **`TERMDECK_PORT` env var threaded into bundled hooks** so `~/.claude/hooks/memory-session-end.js` can attribute Mnestra writes + cost-panel rollups to the right instance. Precedent: `TERMDECK_SESSION` is already injected at `packages/server/src/index.js:1174`; adding `TERMDECK_PORT` is a 1-line change.
  - **`termdeck doctor` multi-instance awareness:** auto-discover running ports (probe `127.0.0.1:3000-3010` for live TermDeck signature), report N instances, run diagnostics per-instance.
  - **Launcher SIGTERM propagation.** `kill -TERM <launcher-PID>` does NOT bring down the underlying node server on :3001; the launcher forks the real server (`node packages/cli/src/index.js`) but doesn't trap-and-forward signals to the child. Real operational bug for multi-port workflows. Separate surface from `start-termdeck.sh` (the hand-rolled outer wrapper).
  - **Stack-installer + `init-mnestra.js` / `init-rumen.js`** today assume one instance. Document the supported pattern for "run a second TermDeck on a second port" (likely: same Mnestra + Rumen backing services, just per-port db).
  - **README + GETTING-STARTED docs** — add a "Running multiple instances for parallel projects" section once the above ships.

- **🪟 Joshua's grid row/column resizing ask (Sprint 65 deferral + 2026-05-15/16 client ask).** Draggable dividers between grid rows/cols for non-uniform "less traditional" layouts; bigger lift than a preset (resize handles + CSS Grid `fr`-track manipulation + per-layout persistence). Cousin of the deferred PTY-drag/drop window rearrangement in § C. ~200-350 LOC, needs its own scoping pass.

- **🪟 "CLI windows open invisible" hypotheses A/C/D (Brad 2026-05-12 item 2a, Sprint 65 deferral).** Hypothesis B (born-hidden under filter) was fixed in Sprint 65. Remaining hypotheses pending Brad's repro:
  - **A:** panel created in a grid slot that's off-screen in the current layout (e.g., 6-panel grid in a 4-slot layout shows only 4)
  - **C:** dark-veil modal blocking visibility — `ARCHITECTURE.md` Known Issue #8
  - **D:** WS / cross-client spawn race
  - Full analysis at `docs/sprint-65-dashboard-reliability/2A-OPENS-INVISIBLE-ANALYSIS.md`.

- **🪟 `meta.role` persistence into `session_summary` / `memory_items` / `memory_sessions` rows (Sprint 65 deferral).** Descoped from v1.4.0 (dashboard-only); revisit when a role-analytics consumer exists. Touches the bundled session-end hook in `packages/stack-installer/`, a separate package.

- **📡 Brad's 2026-05-15 Telegram-mediated orchestrator↔operator bridge proposal.** Brad built a two-way Telegram bridge on his R730: dedicated bot (`@nacho_orch_bot`) + bash daemon (`~/bin/tg-poll.sh`, ~150 LOC) long-polling Telegram `getUpdates` + systemd user service (`Restart=always`, linger-on) + injection into the orch panel via TermDeck's existing `POST /api/sessions/:id/input`. Reliability mechanisms: heartbeat file, `(chat_id,message_id)` dedupe via `inbox.jsonl`, Telegram offset not advanced past failed injects, re-reads orch session UUID per inject. Constraint learned: Telegram `getUpdates` is exclusive — one long-poller per bot token (HTTP 409 if two) — so cross-orch coordination wants **two bots in one shared channel** (each orch polls its own bot, posts as itself, filters inbound to "from the other bot"). Brad proposes Josh mirror the shape (`@josh_orch_bot` + daemon) — the build-out of Josh's 2026-05-13 "shared coordination channel" idea. Source brief on Brad's R730 at `/home/nacho/josh-thread/2026-05-15-telegram-orch-setup-for-josh-mirror.md`; daemon at `/home/nacho/bin/tg-poll.sh`. Standalone candidate — its own architecture pass and needs Joshua's explicit go-ahead.

- **🪧 Blog post + docsite refresh — "The 3+1+1 pattern: how a Codex auditor caught four bugs in fourteen minutes."** Sprint 51.5 → 51.6 is THE narrative case study for the canonized 3+1+1 pattern (`~/.claude/CLAUDE.md` § Sprint role architecture). Source material: the Sprint 51.5 → 51.6 narrative arc (v1.0 ships → Brad's drift bug surfaces same day → v1.0.1 hotfix → live psql reveals two-bug picture → v1.0.2 mini fixes both), Codex's twelve audit posts in Sprint 51.6 STATUS.md, the two structural gaps (compaction-vulnerability + Codex-MCP-not-wired), the Class M discovery, and the 53-min inject-to-FIX-LANDED Sprint 51.6 wall-clock. **Docsite updates needed:** new blog post page (long-form, Joshua's voice required for personal narrative pieces, orchestrator drafts technical scaffolding); update TermDeck product page; changelog mirror entries for recent versions; possibly update homepage hero or product positioning. Cross-link from product page + blog index. Schedule after a future cost-panel sprint so cost-panel screenshots can land in the docs-site update.

- **3+1+1 compaction-vulnerability hardening + Codex MCP-not-wired gap.** Two interlocking gaps in the canonized 3+1+1 pattern: (a) Codex CLI does not have a PreCompact hook equivalent to Claude Code's; on compact, the auditor's in-context audit state evaporates. (b) MCP-not-wired in Codex CLI runtime — Codex can write to Mnestra via direct pg-direct path but cannot call `memory_recall` because Mnestra MCP isn't registered in Codex's runtime. Codex's auditor lane brief already mandates verbose detailed posts + CHECKPOINT cadence (Sprint 51.6 → 51.7 hardening rules in `~/.claude/CLAUDE.md`). Remaining actions: (a) wire Mnestra MCP into Codex CLI by configuring `~/.codex/config.toml` MCP servers (Codex's MCP wiring spec may differ from Claude Code's); (b) propose a Codex CLI PreCompact hook upstream OR build a wrapper that snapshots Codex working memory to STATUS.md / Mnestra before compact; (c) consider a dual-auditor approach (Codex + Gemini, or Codex + Grok) for high-stakes sprints so one compaction doesn't reset the audit pipeline.

- **Memory-budget tracking table + Active health dashboard.** A canonical table mapping `(sprint_shape, agent_type, panel_count)` → expected memory write count + write surface (memory_sessions vs memory_items vs MCP-direct). Lets a post-sprint health check answer "we ran 4 panels in a 4+1 sprint; we expected ≥4 memory_sessions rows + ≥16 memory_items rows; we got 0 + 25; what's broken?" — instead of discovering days later via live psql probe that the hook was silently failing. Concrete shape: `expected_memory_writes` table keyed by `(sprint_shape, agent_type, panel_count)`, populated by the orchestrator at sprint inject; a post-sprint reconciliation job compares actuals against expected; surfaces drift as a STATUS.md alert. Active health dashboard: `/api/health` reports passive checks today; needs ACTIVE pings — (a) trigger synthetic /exit via a test session every N hours, verify `memory_sessions` row count grows; (b) run `mnestra doctor` on a schedule, surface red findings as alerts; (c) compare expected-vs-actual memory writes per session against the budget table; (d) integrate with `termdeck doctor` so the standard developer-facing diagnostic surfaces all of this. Foundation for catching Class I (silent no-op) bugs before they hide for days.

- **Per-panel cwd switch.** Today, when you spawn a Claude Code panel in TermDeck, the `cwd` is fixed at panel-creation time. For multi-repo sprints — e.g., the lane that operates in `~/Documents/Graciella/engram` while siblings operate in the termdeck repo — the agent has to `cd` into the right repo itself as part of its boot sequence. That works (briefs tell the agent to cd) but it's friction: the panel's `cwd` metadata stays out of sync with what the agent is actually working in. **Scope sketch:** add a `PATCH /api/sessions/:id` endpoint that accepts `{ cwd: string }`, validates the path exists and is readable, sends a `cd <path>` keystroke (with bracketed-paste markers + `\r` to submit) to the PTY, and updates `session.meta.cwd`. UI: small dropdown or "switch directory" button in the panel header. <100 LOC server + ~50 LOC client.

- **Mnestra connection-topology routing layer.** Brad runs four Supabase projects (his bridge brain + Structural + aetheria-phase1 + aetheria-payroll) and is deciding how Claude Code's MCP wiring should map to them. Three shapes sketched: A. Single store (collapse to one canonical Supabase, lose isolation). B. Four MCP entries in `~/.claude.json`, one per project (canonical Mnestra pattern; manual to manage but trivial to wire). C. **Single MCP entry that routes to the right Supabase based on the calling Claude session's `cwd`** — looks like one MCP from Claude Code's perspective, each project still gets its own store. Brad's read: Shape B for now, revisit Shape C as a TermDeck v1.x+ feature ask if Shape B's management overhead bites. **Scope sketch:** ~half-day — a wrapper script (Node CLI in `packages/cli/src/mcp-router.js`?) reads a project-to-Supabase mapping from a config file, picks the right env vars based on `process.env.PWD` or the inbound MCP session's working dir, execs the real Mnestra MCP server. Could ship as a new `termdeck mcp-route` subcommand or as a standalone `@jhizzard/mnestra-router` micro-package.

---


### Sprint 70/71 follow-ons (filed 2026-08-05 at dual-deck close)

- **Billing exclusion completeness (Aug-4 spec §3a residue):** add `ANTHROPIC_AUTH_TOKEN` to the billing exclusion (same precedence trap, different var) + per-panel `apiBilling: true` opt-in on `POST /api/sessions` (re-inject after scrub via the adapter-overlay last-spread; persist on `session.meta` for fleet legibility; 400 when no key available). Spec: `docs/ANTHROPIC-KEY-PANEL-ENV-FIX-2026-08-04.md` §3a.
- **Operator until 1.20.0 is installed globally + decks restarted:** the RUNNING deck servers still carry the key in env and execute pre-fix code — comment out the `ANTHROPIC_API_KEY` line in `~/.termdeck/secrets.env`, restart decks from a key-free shell, verify with `/status` in a fresh panel (must show the Max login, not "API key"). Post-1.20.0 the spawn-site scrub makes this structural.
- **engram `src/objectives.ts` comment cleanup** (B-T4 non-blocking note): stale "pair"/"RPCs"/"two mutation paths" wording at :16, :28, :84, :356 — runtime/constants/tests are correct.
- **Bundle `rumen-objective-guard` into `init --rumen`:** the new Edge Function lives only in the rumen repo; `scripts/sync-rumen-functions.sh` + the `files` glob only cover rumen-tick/graph-inference. Add it (dark) or document the manual deploy. RELEASE.md Sprint-42/43 lesson applies.
- **GATE 6 scope note (B-T1's own flag):** the single-mutation-door scan checks `has_function_privilege('service_role', ...)` only — a mutation function granted to a different custom role would slip it. Out of scope while service_role is the only key in play; revisit if custom roles land (pairs with §C FR-8 scoped-credential work).
- **Publish-before-push gate fail-open under load** (B-T4 residual): decide whether the release-governance gate belongs in sprint scope or here; currently documented-by-design.
- **Sprint 70's termdeck half (deliberately cut from Deck A):** panel boot/flashback consumes chains hub-first + session-start entity scan of the opening message — the injection-side consumption of `memory_recall_graph_boosted`. Natural next-sprint candidate alongside flipping `MNESTRA_GRAPH_RECALL`/`MNESTRA_TIER0_INJECT` ON after live soak.

## D.6 Sprint 70+ candidates (added 2026-05-23 from Brad's 2026-05-17 → 2026-05-22 multi-thread wave + v1.6.1 focus-mode hotfix)

The v1.6.1 single-line CSS fix closed Brad's most-recent v1.6.0 focus-mode regression. The remaining items from his 8-email wave are queued here for a follow-up sprint (or single-orchestrator hotfix wave) post-airport. Source threads all forwarded to `admin@nashvillechopin.org` from `bheath.tbhcoach@gmail.com` and `brad@nacho-money.com` on 2026-05-23 12:33 UTC.

- **🪟 Focus mode — focused-orch-panel edge case (v1.6.2 candidate).** v1.6.1 ships `.orch-pin-row:has(~ .grid-container.layout-focus) { display: none; }` which hides the whole row when grid is in focus mode. When the focused panel itself lives inside the orch-pin-row (operator focused an orchestrator-role panel), the focused panel gets hidden too. Fix shape: replace the single rule with two — (1) `.orch-pin-row:has(~ .grid-container.layout-focus):not(:has(.term-panel.focused)) { display: none; }` for the focused-worker case, and (2) `.orch-pin-row:has(~ .grid-container.layout-focus) .term-panel:not(.focused) { display: none; }` for the focused-orch case. Tracked here so it doesn't get lost if Brad reports the edge case later.

- **🪟 Brad's lastActivity heartbeat watchdog gap.** A panel was confirmed hung (no file activity on the underlying transcript) but `lastActivity` on `GET /api/sessions` kept not advancing for ~2 hours while the panel was simultaneously *accepting and queuing* input via `POST /api/sessions/:id/input`. The panel had to be killed + respawned. Possible relation to Sprint 69 T3's `meta.parked` work — could be a case where the periodic-capture / status-broadcast path stops updating `lastActivity` without the panel actually being parked. Or a separate watchdog/heartbeat issue. **Investigation:** instrument `lastActivity` write sites in `packages/server/src/session.js` + the WS message handler that drives status-broadcast; correlate against the PTY's own write events. Sprint shape: small focused diagnostic + fix, likely 1-2 hours wall-clock.

- **🪟 Brad's Sprint 65 ORCH-pin gold-border concern — verify on ≥v1.5.0.** Brad's 2026-05-17 17:34 follow-up reported he could not get the gold ORCH border + ORCH badge to render on a 1.4.0 install — he tried `PATCH /api/sessions/:id` with every plausible field (`role`, `isOrchestrator`, `pinned`, etc.) and the server 200-OK'd but the field stayed `null`. This is **almost certainly resolved by Sprint 66 / v1.5.0**, which made `meta.role` mutable post-spawn via `PATCH /api/sessions/:id` (whitelist-validated). v1.4.0 was the version where the field was fixed at spawn and there was no setter. **Action:** confirm Brad has upgraded to ≥1.5.0 (ideally 1.6.1 after this release) and re-test. If still broken on ≥1.5.0, surface as a real bug; otherwise close as "shipped, awaiting verification."

- **🪟 Rumen Class K runbook addendum + `init --rumen @latest` tracking (Brad 2026-05-18 12:14).** Brad's Claude root-caused the "Rumen 0 insights" issue (his `rumen-tick` Edge Function was pinned at `@jhizzard/rumen@0.5.2` from a 2026-05-04 deploy; the picker-filter + prepublishOnly fix landed in `0.5.3` per Sprint 56). They self-resolved by deploying v36 of `rumen-tick` against Brad's project. Two suggestions: (1) Default `termdeck init --rumen` to track `@latest` rather than pinning at install time, OR document the per-project re-deploy procedure for users whose pin pre-dates a Class-K fix. (2) Memory entry / runbook addendum for the Class K silent-zero-output fingerprint: `sessions_processed > 0, insights_generated = 0, error_message = NULL, status = done` should suggest Bug B class first, npm-pack inspect second. **Surface:** `packages/cli/src/init-rumen.js` (default-to-latest flag or interactive prompt) + `docs/INSTALLER-PITFALLS.md` Class K entry expansion.

- **🪟 Mnestra `privacy_tags text[]` + `include_privacy[]` schema PR proposal (Brad 2026-05-18 16:46).** Mnestra-side, not TermDeck. Adds `memory_items.privacy_tags text[]` column + GIN index + optional `include_privacy?: string[]` parameter on RecallInput + MCP tool. Filter implemented at `src/recall.ts` (NOT in `memory_hybrid_search`) to preserve the canonical 8-arg RPC signature — direct lesson from the Sprint 54 → 56 signature-drift fix. Non-breaking — default `[]` excludes tagged items; explicit opt-in surfaces them. Migration SQL ready at Brad's `migrations/023_privacy_tags_column.sql`. Brad's PKA (Personal Knowledge Archive) project is blocked on this + a few other prereqs. **Action:** review the PR proposal, answer Brad's 4 open questions, and either land the migration on the Mnestra repo or ask Brad's orchestrator to open a real GitHub PR against `jhizzard/mnestra`. Most useful open question: do we want migration 023 to also extend `memory_hybrid_search`'s `RETURNS TABLE` with `privacy_tags`? Brad's recommendation: yes (non-breaking output-column add, eliminates a follow-up SELECT in recall.ts). **Repo:** `~/Documents/Graciella/engram` (Mnestra). **Status:** PR `#15` `feat/privacy-tags-column` is open at `jhizzard/mnestra` as of 2026-05-23 (first-time-contributor workflow approval pending; Sprint 67 T2 flagged for orchestrator awareness).

- **✅ SHIPPED — Sprint 68-REDUX (2026-08-01, FINAL-VERDICT GREEN 16:51 ET, termdeck 1.18.0 / stack 1.16.0).** Residuals now tracked here: (a) read-side `memory_recall` does not surface the fresh canary `session_summary` rows — rows verified present + correctly labeled by direct query; pre-existing ranking behaviour, likely the same recency-vs-keyword item above; (b) 5 pre-existing uncharacterized test skips in the default gate (`total == pass` is false; characterize or fix); (c) Gemini standalone capture deliberately out of scope (legacy, API-key-only). Supersedes the item below on approach. Brad's three findings were right about the *cause* (native hook surfaces are unstable) and the plan drew the wrong conclusion from it — the fix is not a better hook inventory but **not depending on hooks at all**: PATH shims at `~/.termdeck/shims/{codex,grok,agy}` capture at the process boundary via `script(1)`, which no upstream CLI release can churn out from under us. Brad's recommendation (1)(a) — "in-flight stdout capture, RIGHT architecture regardless, sidesteps storage format entirely" — is what shipped. His `source_agent` enum concern is closed: `agy` normalizes to `antigravity`, pinned by a fence. Gemini stays out of scope. Wave target: termdeck 1.18.0 / stack 1.16.0.

- **🪟 Antigravity migration heads-up — Sprint 68 scope correction (Brad 2026-05-21 23:44).** Brad ran a parallel Codex-CLI (live R730 stack audit) + Claude (public docs / GitHub / forums) audit ahead of the Gemini-CLI → Antigravity migration deadline 2026-06-18. Both lanes converged: **planned ~2-4h Sprint 68 scope is materially undersized; revised estimate 16-19h.** Three findings change the work shape (not just the line count): (1) **JSONL recovery pattern is dead** — Antigravity writes binary protobuf (`.pb` files) under `~/.gemini/antigravity/brain/<GUID>/`, schema closed-source; the existing `packages/server/src/agent-adapters/gemini.js:130-156` single `JSON.parse(raw)` is already broken. Architecture decision needed: (a) in-flight stdout capture via `unbuffer` / `stdbuf -oL` — RIGHT architecture regardless, sidesteps storage format entirely — Brad recommends; (b) reverse-engineer protobuf — fragile; (c) wrap unofficial LSP-API — requires keeping `agy` alive past panel exit. (2) **PreCompact + SessionEnd hooks don't exist in `agy`** — Antigravity's "JSON hooks" are subagent/artifact events, not compaction/session events. "Port the PreCompact hook" → "design + implement filesystem-watcher daemon OR Bash wrapper around `agy -p` that runs the memory webhook on exit." ~4-6h, not a port. Also: Brad's `~/.claude/hooks/` lacks `memory-pre-compact.js` despite TermDeck's stack-installer shipping one — PreCompact may be broken on his machine for Gemini panels today; worth a doctor-command check independent of the Antigravity work. (3) **`--model` flag may not exist + Mnestra `source_agent` enum only allows `'gemini'`** — `dist/mcp-server/index.js:242-245` + `dist/src/types.d.ts:52` don't include `'agy'`/`'antigravity'`; the installed `memory-session-end.js:635-639` normalizes unknown source agents to `'claude'`, so renaming the adapter to `'agy'` would silently break Brad's reviewer-rotation queries. **Sprint 68 scope re-decision needed BEFORE kickoff.** Brad's full synthesis package + line-cited findings at `/home/nacho/josh-thread/antigravity-migration-research/` on his R730.

- **ℹ️ PKA (Personal Knowledge Archive) — informational only (Brad 2026-05-18 14:58 kickoff brief).** Brad's new project on `bheath-atx/pka` (private GitHub) — unified semantic layer over ~20 years of personal corpus (Evernote, OneNote, Outlook PSTs, Gmail/Outlook live), integrated with the existing Mnestra/Rumen stack on R730. Locked architecture: F1 OpenAI `text-embedding-3-small` 1536-dim; F2 existing Supabase project shared with Mnestra/Rumen, new dedicated `archive` schema; F3 mirror everything to `public.memory_items` with `privacy_tags text[]` column (drives the Mnestra schema PR above). 9-stage Phase-1 ENEX ingest pipeline. **No TermDeck action required** — listed here so future sessions know the context behind the Mnestra `privacy_tags` PR. Brad's repo + specs at `/opt/projects/pka/docs/` on his R730.

---

## E. Risky dependency upgrades (deferred indefinitely)

- **Express 5 migration.** High risk, currently pinned at 4.x. Likely blocks many other items if attempted.
- **Node 18 → 20 LTS bump.** Not currently urgent but worth assessing as Node 18 EOLs. TermDeck's CI matrix sets the target.
- **Mnestra Zod 4 migration.** Open as of 2026-05-22; Dependabot PR `jhizzard/mnestra#11` was merged 2026-05-23 13:18Z during Sprint 67 T2's CI hygiene pass. Track downstream impact — Mnestra is now at Zod 4 from 3.x.

---

## F. How to use this file

When you (a future session) finish a scoped sprint and need to pick the next one, read this file top-to-bottom and pattern-match against:

1. **Urgency from external signals** — Brad reports a regression, an audit lands, a launch-readiness deadline arrives. Those override priority order.
2. **Theme grouping** — Don't run a Correctness sprint right after another Correctness sprint if morale needs a feature win. Mix categories.
3. **Compounding leverage** — A single fix that closes multiple items below counts higher than a 1:1 fix.

When an item gets scoped into a sprint, move it from this file into the sprint's `PLANNING.md` and delete it here. Don't leave it in two places — the sprint plan becomes authoritative once an item is in flight.

When new items surface (audit, user feedback, incident postmortem), add them here in the right category. Don't create a new ad-hoc roadmap doc; consolidate.

---

## Archived (closed since 2026-04)

One-liners for items closed since the last BACKLOG rewrite. For detail: `git log --grep '<sprint-id>'`, `gh pr list --state merged`, `memory_recall(project="termdeck", query="<topic>")`, or the closed sprint's `docs/sprint-N-<name>/PLANNING.md` § Resolution.

- **Sprint 38 (v0.10.0) — Brad's empty-Mnestra ingestion fix.** Bundled `~/.claude/hooks/memory-session-end.js` rewritten to call Mnestra MCP tools directly (no `rag-system` dependency); closes the "bundled hook silent-fails on fresh users" gap.
- **Sprint 51.5 / v1.0.1 — Brad's Class A drift hotfix.** Source_agent emission fix; first install on Brad's box.
- **Sprint 51.6 / v1.0.2 — Joshua's `memory_sessions` ingestion break.** Daily-driver `memory_sessions` write path repaired (Phase B reconciliation of canonical schema with rag-system writer's rich shape). Codex auditor caught 4 bugs in the all-Claude Sprint 51.5 attempt — canonical case for 3+1+1.
- **Sprint 51.7 / v1.0.3 — wizard wire-up bug.** `runHookRefresh` install/upgrade ordering.
- **Sprint 51.8 / v1.0.4 — settings.json Stop→SessionEnd migration.**
- **Sprint 53 — public-tree internal-name scrub** (commit `32d3e78`). Note: ongoing hygiene — new docs since have re-introduced the literal; gitleaks hook + Sprint 67 T3 BACKLOG/CLAUDE.md scrub continue the work.
- **Sprint 59 / v1.0.13 — install-smoke + systemd-nightly hardening.** All five install-smoke EXPECTED sites green; F-6 command uses `--include=optional`; upload cleanup removes per-session tempdir on PTY exit.
- **Sprint 60 / v1.0.14 — five-item hotfix bundle.** (1) Body-parser hardening; (2) WS ioctl EBADF/ENOTTY race guard; (3) per-adapter idle/parked status detection (Codex `Worked for Xm Ys` terminator + claude/gemini/grok idle-prompt patterns + 30s `toJSON` sticky-status guard); (4) launcher stderr separation + log rotation; (5) supervisor wrapper / systemd unit.
- **Sprint 61 / v1.1.0 — Convergence Keystone.** Uninstall + fresh-install harness + upgrade-detection path. Closes Brad's 2026-05-02 "schema-vs-package drift on existing installs" P0.
- **Sprint 62 / v1.2.0 — Mnestra session-end coverage gap.** Cross-agent capture verified on non-Claude `/exit`; Phase-B-doesn't-refresh-hook GAP closed.
- **Sprint 63 — Wave 2 (Brad's bug-bundle).** PTY-leak diagnosis + body-parser SyntaxError hardening + ws ioctl race + 410 Gone on dead-PTY resize.
- **Sprint 64 / v1.3.0 — auto-commit on context-compaction-near.** `PreCompact` harness hook + TermDeck server-side periodic-capture timer for non-Claude panels. Closes Investigation 2 of `docs/CRITICAL-READ-FIRST-2026-05-07.md`.
- **Sprint 65 / v1.4.0 — Dashboard reliability + ORCH-panel awareness.** Project-filter chips + ORCH-pin row via `meta.role` + panel-exit auto-removal + `410 Gone` on dead-panel inject + `?includeExited` filter + per-adapter idle/parked verification. Closes Brad's 2026-05-12 "next wave" feedback (3 items) + 2026-05-13 v2 spec (chips + ORCH pin) + Joshua's 2026-05-15/16 1×2 layout + per-panel font-size asks (font-size shipped as global stepper; per-panel refinement deferred to Sprint 66+).
- **Sprint 66 / v1.5.0 — Public-scrutiny cleanup.** CI reliability (secret-gated workflows skip-neutral); Sprint-65 reception gap; dependency hygiene (4 termdeck Dependabot PRs closed/superseded); `meta.role` mutable post-spawn via `PATCH /api/sessions/:id`.
- **Sprint 69 / v1.6.0 — Orchestration-hardening primitives.** Template engine + inject/nudge endpoints + `meta.parked` detection. 438/0/0 test baseline.
- **Sprint 69 hotfix / v1.6.1 (2026-05-23) — orch-pin-row sibling-container gap.** Single-line CSS fix (`.orch-pin-row:has(~ .grid-container.layout-focus) { display: none; }`) for the focus-mode regression Brad reported against v1.6.0. Commit `1b659fa`, tag `v1.6.1`.
- **Sprint 51.6 — `mnestra_doctor_*` SECURITY DEFINER + PUBLIC INSERT hygiene** (Mnestra v0.4.4 / migration 019). Brad's external sweep caught 4 PUBLIC INSERT policies + 5 anon-callable SECURITY DEFINER doctor functions + 6 mutable search_path on shipped v0.4.3; closed by migration 019 across multiple projects on 2026-05-06.
- **2026-05-06 — gitleaks pre-commit + pre-push hooks + nightly mirror-backup.** Mechanical defense for forbidden-literal scrub + RLS hygiene. Standing protection going forward.
- **Sprint 61.1 — classifier hygiene pass.** Migration 011 / 012 (project-tag classifier) carve-out; allowlisted in `~/.gitleaks.toml`. Three resolution candidates documented; deferred indefinitely until corpus-rewriting work is queued.
- **NEW (2026-08-01 field incident, Sprint-69 candidate) — shim staging via bare `npm i -g @jhizzard/termdeck` violates the Class-N lockstep unit.** The global-install path staged `~/.termdeck/shims/` files but never wrote the rc PATH fence (the fence write lives behind the stack installer's consent flow), leaving doctor at 8/9 with all three CLIs resolving to real binaries — standalone capture silently dark on the operator's machine while the artifacts looked installed. `index.js:1237` states the contract: "{shim files, PATH fence} are ONE lockstep unit. Any path that stages shims also reconciles the fence; neither ships without the other" — the npm postinstall/staging path breaches it. Fix options: that path also reconciles the fence, or it stops staging shims and defers wholly to the consent flow. Field remediation 2026-08-01: fence appended manually (canonical `_shimPathBlock()` bytes, backup at `~/.zshrc.bak-2026-08-01-shimfence`), doctor now 12/12. Related probe lesson: `zsh -l -c` does NOT read `.zshrc` — shim/PATH verification must use `zsh -i -c`.
- **NEW (2026-08-01 activation follow-on, Sprint-69 candidate) — Sheets harvester should ride the bridge supervisor, not a side launchd.** `termdeck-supervise.sh` has no harvest awareness; activation stood up an interim launchd (`com.jhizzard.termdeck-sheets-harvest.plist`, `--once` every 300s, RunAtLoad, logs at `~/.termdeck/sheets-harvest{,.err}.log`). Proper home: supervise starts/stops the harvester with the bridge when `TERMDECK_SHEETS_INTAKE_ENABLED=1`, one lifecycle, one log surface. Retire the plist when that lands.
- **NEW (2026-08-01, Josh proposal, Sprint-69/70 candidate) — Gemini-web READ ramp: mirror a Mnestra slice to a Google surface Gemini can read.** Write-side for Gemini is solved (the Sheets intake ramp, activated tonight); read-side is zero — Gemini web has no MCP/connector path, so it cannot recall. Josh's original proposal stands: mirror Mnestra outbound to a Google-readable service. Cheapest concrete shape: the harvester's service account ALREADY holds Sheets API access — add a read-only "memory mirror" surface it maintains (a second tab/sheet of recent+top-recalled kitchen memories, or a Drive folder of Markdown reusing `termdeck vault export`'s existing renderer), refreshed on the same 300s/nightly cadence. Gemini (in-sheet side panel or Workspace grounding) then reads memory the way it just proved it can read the intake sheet. Scope guards: outbound mirror is EXPORT-only (no write-back), redaction/forbidden-strings scrub on the mirrored slice, and the mirror is derived data — regenerable, never a substrate. Precedents to reuse: session-index→Sheets rig, vault-export, the SA-JWT Sheets client. **VALIDATED 2026-08-01 late:** Gemini-web read the live Intake sheet accurately in-chat (both rows + stamps, dialog PDF at `SideHustles/Append Row to Spreadsheet.pdf`) — the read path is proven; Gemini also confirmed its write limits from its own mouth (read/fetch + CREATE-new only, can NEVER edit existing files — the in-sheet Gemini side panel is the only Gemini writer) and independently re-proposed this exact Sheets-relay architecture as its own "Option 3." Caution for the design: Gemini hallucinated append-success twice in that dialog (claimed the operator's in-sheet row as its own) — any Gemini-side workflow must verify row-level, never trust its self-report.
- ~~**Vault readability: the exporter must generate the navigation layer** (2026-08-02, Josh priority — PRIME Sprint-69 candidate)~~ **SHIPPED — Sprint 69 (2026-08-02), FINAL-VERDICT GREEN.** Full record: `docs/sprint-69-vault-readability/{PLANNING,STATUS}.md` + CHANGELOG [1.19.0]. Six follow-on items below.
- **SHIPPED — Sprint 70 (2026-08-05), FINAL-VERDICT GREEN 20:33 ET.** Record: `docs/sprint-70-graph-boosted-recall/{PLANNING,STATUS}.md` + mnestra CHANGELOG [0.13.0] + termdeck [1.20.0]. Live-apply pending operator: `bash docs/sprint-70-graph-boosted-recall/apply-s70-s71-live.sh`. Follow-ons filed below. Original entry (design history): **Graph-Boosted Recall: wire the graph substrate into the recall path so keywords trigger context CHAINS, not flat lists.** Josh's stated end-state: "a few key words trigger the exact context chain and walk across the edges… so I basically don't have to provide context," with staleness eliminated structurally. **Live diagnosis (2026-08-02, one call):** `memory_recall_graph(project=termdeck, query="vault readability navigation layer", k=6, depth=2)` returned `d0=6` — all vector seeds, ZERO graph neighbors — because the walk expands ONLY via `memory_relationships` and ignores the entire 034 substrate (540 `memory_entities` + mentions, 40 consolidation communities, typed edges from nightly inference). The primitive exists; the substrate is unwired. Scope (natural 3+1+1): **(1) Expand the walk's edge set** — entity co-mention + community co-membership + typed relationship edges, weight-tunable, in `memory_recall_graph`'s SQL (and consider promoting graph-walk to the DEFAULT `memory_recall` path behind a flag). **(2) Keyword→entity triggering** — match query terms against `memory_entities` FIRST (the literal "few key words" mechanism), seed the walk from matched entities' mention sets, union with vector seeds. **(3) Coarse-to-fine via hubs** — when ≥N members of one community hit, return the `consolidation_summary` hub as the primary unit with members as expandable citations (this is the Karpathy/Graphify "compiled knowledge that compounds" insight applied to OUR store — retrieval returns compiled artifacts, not raw chunks). **(4) Structural staleness** — fold in the two existing §A items (read-side recency ranking; prose-supersession→structural links): newest-dated-anchor downranks older same-cluster siblings, consolidation near-dup clusters propose `supersedes` links mechanically. **Injection side (TermDeck half):** panel boot/flashback consumes the chain hub-first; session-start entity scan of Josh's opening message auto-pulls the chain. **Feedback:** `memory_cite` seeds activation and labels chain quality for the fit-platt calibration rerun. Graphify itself evaluated and passed over (2026-08-02): its philosophy (compiled, index-first, compounding) validates this design but the tool is a one-shot folder→wiki compiler with no store, no provenance, no judged promotion, no staleness machinery — everything it outputs, Sprint 69's exporter already emits from a real DB.
- **SHIPPED — Sprint 71 (2026-08-05), FINAL-VERDICT GREEN/RATIFY 20:53 ET** (dual-deck with Sprint 70). Record: `docs/sprint-71-objective-tier/{PLANNING,STATUS}.md` + mnestra [0.13.0] + rumen [0.12.0] + termdeck [1.20.0]. Live-apply pending operator (same script as Sprint 70). Follow-ons filed below. Original entry (design history): **Objective Tier: hierarchical memory with ENFORCED top-level objectives (anti-drift).** Josh's framing: engineer a hierarchy into Mnestra that enforces top-level objectives so the system cannot drift. Assessment: ~60% of the substrate exists — the doctrine layer (Sprint 79 elevation capture, `doctrine_registry`, repo `doctrine/registry.jsonl`), the kitchen-over-recipe taxonomy, the judge, and the PreCompact hook. What's missing is the ENFORCEMENT tier. Design: **Tier 0 = OBJECTIVES** (per-project, tiny ~5-15 rows: what this project is for, what must never happen, current strategic position) with four enforcement properties: (1) ALWAYS INJECTED at session start + RE-INJECTED at PreCompact (compaction is where drift actually happens, and the hook already fires there); (2) pinned ABOVE recall results — retrieval can never bury them; (3) mutable ONLY via explicit ratification (`supersedes` chain, operator-gated like doctrine ratify) — never decay, never judge-rejected; (4) rendered at the top of the vault's `Home.md` + each MOC so the human sees the same tier-0 the agents get. **Tier 1** = doctrine/kitchen (graph-chain recalled), **Tier 2** = evidence (facts/sessions/chunks). **Rumen anti-drift jobs:** contradiction scan (new decisions vs tier-0 → FLAG, never silently absorb), objective-coverage report (sustained activity with zero tier-0 linkage = drift signal), objective-staleness review flags. **Claude Code parity note (the answer to "what slash commands do this"):** Claude Code enforces exactly this hierarchy natively — CLAUDE.md global→project (always-loaded, overrides defaults) = tier 0; hooks (SessionStart/UserPromptSubmit/PreCompact, harness-executed so the model cannot skip them) = the enforcement mechanism; skills/commands = on-demand tier-1 procedures; plan mode = intent ratification. The Mnestra Objective Tier is that same layering moved SERVER-SIDE so ALL surfaces (codex/grok/agy shells, web chats via bridge, panels via flashback) get identical tier-0 — CLAUDE.md for every agent, generated from the store. R-1 established the true model: regeneration is manifest-swept IN PLACE (unlink prior manifest files, then write; no rollback) — a crash mid-export leaves a partial tree until the next run. Fix shape: write to a temp sibling dir on the SAME filesystem (the X6 vault means the temp dir must live on the X6, not `os.tmpdir()`), then swap. Ruled out of Sprint-69 scope; documented in ARCHITECTURE.md.
- **NEW (Sprint 69 follow-on) — Store-side project-value canonicalization** (migration-021 precedent). Live store carries case/format duplicates: `PVB` (2) vs `pvb` (1613), `ChopinInBohemia` (3) vs `chopin-in-bohemia` (438). The exporter merges on lowercased slug (R-2 §2) so the vault is safe; this is data hygiene — a small UPDATE folding the 5 minority rows, so Bases `project ==` filters and provenance queries stop splitting.
- **NEW (Sprint 69 follow-on) — `.obsidian/core-plugins.json` write-if-missing (T2 design, ~10 lines).** `.base` files render only if the Bases core plugin is enabled; a fresh vault dir has no `.obsidian/core-plugins.json`, so first-open dashboard rendering depends on the reader's Obsidian defaults. T2's design: one more write-if-missing file (`bases: true` + ordinary defaults), same untracked/never-clobber path as graph.json. Deliberately NOT ruled in-sprint: it vendors Obsidian's default core-plugin list, a version-drift surface, and touches reader plugin config (bigger consent surface than graph tuning). Revisit when a second fresh-vault reader exists; today's single reader enables Bases once by hand (documented in GETTING-STARTED).
- **NEW (Sprint 69 follow-on) — Optional content-escape flag for wikilink-shaped strings in note bodies.** 41 live instances of `[[...]]` inside raw memory content render as (mostly dangling) wikilinks in Obsidian. R-3 ruled them USER DATA — the read-only projection must not rewrite bodies — so they are out of the acceptance gate. A future opt-in flag could escape them at render time; default stays off.
- **NEW (Sprint 69 follow-on) — P2-7 weekly rollups, deferred WITH design.** Full recorded design in Sprint-69 STATUS (T3 11:12): `rollups/<project-slug>/<ISO-year>-W<ww>.md`, generate only weeks-with-content (181 files, ~2% growth), prev/next chained over the sorted EXISTING week set (58% of projects have discontinuous coverage — calendar arithmetic dangles), explicit ISO-week algorithm with a year-boundary golden (2026-01-01 = ISO 2025-W53). New file class → new goldens + audit surface; that's why it deferred.
- **NEW (Sprint 69 follow-on) — P2-9 Breadcrumbs per-edge-type list props, deferred on merit.** `up:` already delivers the hierarchy; the extra props duplicate the `edges:` block and expand a growing edge-type vocabulary directly into the frontmatter key namespace, where a future edge type named `tags`/`aliases`/`cssclasses` would silently corrupt a reserved Obsidian property. Pick up the day Breadcrumbs is actually installed and typed traversal is wanted. Research complete (`docs/VAULT-READABILITY-RESEARCH-2026-08-02.md` — X-post reconstruction, 8 patterns, code-grounded plan). One-line verdict: the DB-as-store / vault-as-projection architecture is right; the projection lacks topology + schema, and only the exporter can fix topology. P0 trio (one lane): render `consolidation.member_ids` as member wikilinks + `up:` backlinks (the orphan-cloud→hub-and-spoke fix; fixture at `packages/cli/tests/vault-export.test.js:95` proves the data flows), frontmatter schema (nested tags/date/aliases/edge_count), generated `Home.md` + per-project MOCs. P1: folder routing + emitted `Memories.base` dashboards + write-if-missing `graph.json`. Supersedes the older "MOC hub notes per project/community" one-liner. Related same-day fix, done: nightly vault regen was dying at exit 127 (launchd default PATH lacks /usr/local/bin) — PATH export added to `sprint-toolkit/vault-nightly-regen.sh`; manual regen green (9,118 notes / 5,970 wikilinks).
- **🪟 NEW (Brad 2026-08-02, msg 19fc2cd5c711ed8a, VERIFIED against source) — stack-installer has no working path on native Windows.** (a) `packages/stack-installer/src/index.js:247,254,344` spawn `npm` without `shell:true` — fails 100% on win32 (npm is `npm.cmd`; CreateProcess won't exec it). Also unswept: `src/launcher.js:77,244`, `src/uninstall.js:990` (`which`, generic spawn). (b) `_detectRcTarget()` (`index.js:1341-1369`) has no PowerShell `$PROFILE`/cmd branch; `$SHELL` unset on Windows → generic fallback whose manual instruction maps to nothing there. Decision FIRST: support native Windows (all spawn sites + $PROFILE fence + test matrix) OR declare WSL-only officially (early `process.platform==='win32'` guard printing "use WSL" — small, honest, matches Brad's own read; WSL worked cleanly for him). No urgency — Brad unblocked via WSL.
- **⚠ NEW (Brad 2026-08-02, SECOND field occurrence — prior self-fixed 2026-07-12; see his 2026-07-06 "deferred migration 028 source_type" thread 19f3609ba345507e + the §A 028-not-widened note) — `termdeck init --mnestra` audit-upgrade has no diverged-schema safety.** 034's DROP+ADD CHECK (`packages/server/src/setup/mnestra-migrations/034_graph_layer.sql:355-364`) fails validation on stores deliberately carrying extra source_type values; the runner (`packages/cli/src/init-mnestra.js:415-430`) just applies it — no `--dry-run`/`--check`, no skip/reconcile — and an ADD CONSTRAINT takes an ACCESS EXCLUSIVE lock + full validation scan against live production unprompted (Brad's 53K-row store; failed atomically, zero damage, Brad DB-verified — but "safe by accident of Postgres constraint semantics" is not a design). **DESIGN GROUNDED 2026-08-02 (Brad's R730 orchestrator delivered the live distribution — better than asked):** his store is **50,385 of 53,118 rows (~94.9%) foreign types** — `doc` 34,583 · `expert_insight` 11,895 · `conversation` 3,888 · `gemini_gem` 2 · `rumen_pending_link` 14 · `rumen_stale_flag` 2 · `test` 1 (written by pkachu/rumen/other consumers of the shared store; note the parallel taxonomy: their `doc`/`conversation` ≈ our `document_chunk`/`session_summary`). **Critical: his store has NO source_type constraint at all** (their 2026-07-12 self-fix dropped it) — so the originally-planned arm-introspecting UNION rebuild hits its empty branch on the exact store that motivated it and fails identically. The design is now FOUR-BRANCH: (1) probe `pg_constraint` by `conrelid + contype='c' + pg_get_constraintdef ILIKE '%source_type%'` (match on definition, not name — self-fixed stores rename); (2) constraint found → existing arms ∪ bundled arms ∪ `SELECT DISTINCT source_type` (covers NOT VALID + drift), rebuild via `ADD ... NOT VALID` + `VALIDATE CONSTRAINT` (kills the ACCESS EXCLUSIVE full-scan); (3) **constraint absent + rows present (Brad's actual state) → SKIP the constraint clause + doctor/init warning — divergence is a SUPPORTED state on a multi-writer store, not an error**; widened install only behind an explicit opt-in flag (installing bundled∪DISTINCT would convert today's failed migration into tomorrow's INSERT failures the moment a foreign writer mints a new type); (4) constraint absent + table empty (fresh install) → bundled list as-is. The `--dry-run`/`--check` umbrella reports exactly those detection criteria. Fixtures assert **SQLSTATE 23514 + constraint name, never byte-exact message text** (Brad released from the shell-history pull — the literal string is not load-bearing). Implementation seam: the runner applies static SQL (`init-mnestra.js:424-449` via `audit-upgrade.js` PROBES), so the branchy rebuild lives in a `DO $$` block inside 034's successor or in the JS runner — decide at design time. Long-term pointer: 034's own comment defers lookup-table conversion to a dedicated sprint; Brad's "declare your blast radius" framing agrees. Also: new INSTALLER-PITFALLS ledger entry + failure-class candidate.
