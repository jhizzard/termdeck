# TermDeck Post-Sprint-38 Backlog

Single source of truth for everything queued beyond Sprints 37 (Orchestrator-as-product) and 38 (Knowledge graph + D3.js).

**Last updated:** 2026-04-27 (Sprint 36 v0.8.0 close-out)

This is the compressed view. Detailed audits and historical context live in `POST-LAUNCH-ROADMAP.md`, `IDEAS-AND-STATUS.md`, and the Mnestra memory store. Future sessions: read this file first; drop into the sources only when an item is scoped into an actual sprint.

## Scheduling note

Sprint 37 and Sprint 38 are the next two sprints, in that order. Their plans are at `docs/sprint-37-orchestrator-product/PLANNING.md` and `docs/sprint-38-knowledge-graph/PLANNING.md`. Joshua's stated goal at Sprint 36 close: ship 37 and 38 before Brad's next test round. Items below are POST-Sprint-38 unless explicitly tagged otherwise — **except the P0 section directly below, which is urgent enough to slot into 37/38 timeframe (exact placement TBD per orchestrator's recommendation)**.

---

## P0 — User-impacting blockers (urgent, schedule alongside 37/38)

- **🚨 Joshua's `memory_sessions` ingestion broke on 2026-05-01 (discovered 2026-05-03 during Sprint 51.5 inject).** Live psql probe of `petvetbid` (luvvbrpaopnblvxdxwzb) on 2026-05-03 17:50 ET shows `memory_sessions` last row at `2026-05-01 20:40 UTC` (Sprint 47 close). `rumen_insights` last row `2026-05-01 20:45 UTC`. `rumen_jobs` continues running every 15 min, all `done`, no error_message, but `started_at` is NULL on every recent row — they exit without doing real work because there's nothing to summarize. **289 memory_sessions rows total, 321 rumen_insights total.** Sprints 48 → 49 → 50 → 50.5 → 51 → 51.5 all completed but produced ZERO new memory_sessions rows. Mnestra-direct via MCP (`memory_remember`) is still working (memory_items grew from 6,261 → 6,262 during this Sprint 51.5), so this is specifically the **session-end hook → memory_sessions write path** that's broken. T1's substrate probe in this sprint surfaced "25 pre-existing failures in `tests/project-taxonomy.test.js`" — that suite tests `~/.claude/hooks/memory-session-end.js`, which is the bundled hook responsible for the missing writes. Failures were flagged out-of-scope for Sprint 51.5 (hotfix-shaped for Brad's Class A drift); they remain open and are now correlated with the live ingestion break. **Class:** Likely **E (hidden dependency)** — Sprint 38 v0.10.0 rewrote the bundled hook to call Mnestra MCP tools directly to fix Brad's identical bug; possible regression on Josh's box, OR Sprint 50's per-agent SessionEnd trigger introduced a code path that no longer writes memory_sessions. **Investigation steps:** (1) tail `~/.claude/hooks/memory-session-end.js` exit codes for the next few session ends; (2) diff the bundled hook at `packages/stack-installer/assets/hooks/memory-session-end.js` against what's actually installed at `~/.claude/hooks/memory-session-end.js`; (3) check if the hook is even firing (instrument with a timestamp file). **Sprint shape:** small focused hotfix (Sprint 51.6 or fold into 52); ships before any further features so ingestion is healthy on Josh's daily-driver before Sprint 52 cost-panel work begins.



- **v1.0.15 hotfix improvements — Brad's 2026-05-07 18:00 ET patch upgrades on top of v1.0.14.** Brad sent a v1.0.12-relative bug report ~30 min after our v1.0.14 ship covering the SAME 2 bugs we'd already fixed (idle-detection / WS ioctl race / body-parser hardening). His patch contained 3 improvements worth folding into v1.0.15: (1) `term.onExit` should null `session.pty = null` so existing `if (session.pty)` guards become correct everywhere — cleaner than v1.0.14's `meta.status === 'exited'` check, no behavioral diff but tighter architecture. (2) Body-parser `verify` callback should capture raw body via `req.rawBody = buf` so the error middleware can hex-escape a 32-byte prefix into the warn line — identifies WHICH caller is sending bad JSON without exposing full body. (3) HTTP `POST /api/sessions/:id/resize` should return `410 Gone` (not `409 Conflict`) when the PTY has exited — semantically more correct (Gone = resource was here, has been intentionally removed). Single-orchestrator hotfix, ~30 min wall-clock. Plus: tell Brad to upgrade once v1.0.14 is verified clean (his patch will conflict cleanly because we already shipped the underlying fixes).

- **🚨 v1.0.14 HOTFIX BUNDLE — same-day post-Sprint-59 close (queued 2026-05-07 ~16:35 ET).** Five focused items, single-orchestrator (no 3+1+1), expected wall-clock 1-2 hours. (1) **Body-parser hardening** — Brad's r730 log shows 9× `SyntaxError: Bad control character in string literal in JSON at position 9` over 13h uptime; Express receives PTY output as JSON body without sanitization. Fix: custom `verify` callback on `express.json()` that strips/rejects control chars, OR per-route raw-body parser with explicit JSON.parse + try/catch returning 400. (2) **WS ioctl EBADF/ENOTTY race guard** — Brad's log shows 25× `ws message handler error: Error: ioctl(2) failed, EBADF/ENOTTY`; WS resize-event race against pty-reaper closing fd. Fix: in WS resize/setSize handler, check `session.pty && !session.pty._destroyed` before calling pty.resize; downgrade EBADF/ENOTTY to debug log. (3) **Per-adapter idle/parked status detection** — promoted from § C 2026-05-07 ~16:33 ET; bit Sprint 59 TWICE in 90 min. Codex `Worked for Xm Ys` terminator + Claude idle-prompt-cursor pattern + 30-60s stale-lastActivity heuristic in `packages/server/src/session.js`. (4) **Launcher stderr separation + log rotation** — Brad's analysis: log file stops 36s BEFORE crash #1 timestamp because `~/start-termdeck.sh` doesn't `2> ~/.termdeck/termdeck.err`. Single 260KB log spans Apr 25 → May 7 with one boot banner. Fix: redirect stderr separately in start-termdeck.sh; add per-boot banner with timestamp; document logrotate config. (5) **Supervisor wrapper for start-termdeck.sh** — Brad's #1 recommendation: a real supervisor so crash → restart leaves a fingerprint. Could be systemd unit (already canonical post-Sprint-59), or a tiny bash watchdog. **User-model update (recorded 2026-05-07 ~16:35 ET):** TermDeck distribution is now 3+ external testers (Brad + 2 others), not 1. Hotfix urgency reflects that.

- **✅ CLOSED (Sprint 65 / v1.4.0, 2026-05-16) — Per-adapter idle/parked status detection.** Mechanism shipped Sprint 60 v1.0.14 (per-adapter end-of-turn rules: codex `Worked for Xm Ys` terminator + claude/gemini/grok idle-prompt patterns; plus the 30s `toJSON` sticky-status guard). Sprint 65 T2 sub-task 2.5 verified the shipped behavior and pinned it with 7 regression tests — no new production code (per ORCH SCOPE ruling C). _Original P0 write-up retained for history:_ **🚨 Per-adapter idle/parked status detection — orchestrator can't reliably tell when a panel has stopped (PROMOTED to P0 2026-05-07 ~16:33 ET; surfaced + bit Sprint 59 TWICE in 90 min wall-clock).** When a panel finishes its turn and parks at the prompt waiting for next input, `GET /api/sessions` reports `meta.status: "active"` with `meta.statusDetail: "<adapter> is reasoning..."` and a recently-updated `lastActivity` — indistinguishable from a panel that is genuinely mid-reasoning. The Codex transcript ends with a `─ Worked for Xm Ys ─` terminator line that IS unambiguous, but `/api/sessions/:id/buffer` returns input-buffer state only, not scrollback. Only `/api/transcripts/:sessionId` exposes the terminator. Same false-positive shape observed on Claude panels (status = `active`/`Using tools` or `thinking`/`reasoning` with `lastActivity` 500-700s stale, post-DONE-parked). **Sprint 59 cost incurred (twice):** (1) T4-CODEX parked at 15:36 ET after Phase 1; orchestrator misread as "reasoning" for 22 min until Joshua manually flagged. (2) T4-CODEX parked at 16:31 ET after Phase 6 GREEN verdict; orchestrator misread as "reasoning" again — Joshua had to manually flag a second time within the same sprint. The orchestrator's STATUS-update reports literally said "T4 currently reasoning" while T4 was idle waiting. **Promoted from § C to P0 because it's a structural blocker for 3+1+1 reliability** — every long-running auditor lane that pauses mid-sprint will mis-report, and the orchestrator's health/coordination decisions depend on accurate status. **Fix area:** server-side per-adapter status detection in `packages/server/src/session.js` PATTERNS / `packages/server/src/agent-adapters/`. Add adapter-aware end-of-turn rules: Codex `Worked for Xm Ys` terminator, Claude Code idle-prompt cursor shape, similar for Gemini/Grok. On detection, flip to `status: 'active'` with empty `statusDetail` (canonical idle shape). Plus a 30-60s stale-`lastActivity` heuristic as belt-and-suspenders. **Sprint shape:** small focused hotfix — likely ~50-80 LOC + a unit test per adapter + verification probe via `/api/transcripts/:id` tail-match. Could ride v1.0.14 immediately after Sprint 59 close (single-orchestrator, no 3+1+1 ceremony), or fold into a near-term Sprint 60 alongside Brad's crash-class root cause if his data points there.



**TermDeck has two users: Joshua (daily driver) and Brad (only outside user).** Anything that breaks the daily-driver flow for either is treated as a release-quality issue, not a backlog item.

- **🚨 Stack-installer has no upgrade-detection path — schema-vs-package drift on existing installs (reported 2026-05-02 by Brad's Claude Code).** Project: `jizzard-brain` (Supabase ref `rrzkceirgciiqgeefvbe`), originally provisioned 2026-04-25. Brad runs the latest npm packages (`termdeck@1.0.0`, `termdeck-stack@0.6.0`, `mnestra@0.4.0`, `rumen@0.4.4`) but everything added Sprint 38 onward never landed on his database: `graph-inference` Edge Function never deployed, vault key `graph_inference_service_role_key` never created, `graph-inference-tick` cron (TermDeck migration 003) never applied, Mnestra migrations 009–015 never applied. Confirmed via the Edge Function's own diagnostic: `{"ok":false,"error":"awaiting migration 009"}`. Compounding symptom: `rumen-tick` ran ~6 days returning `sessions_processed=0, insights_generated=0` every cycle — looked healthy, wasn't. **Root cause:** `init-rumen.js::applySchedule` correctly applies migrations 002 and 003 with templating but only on the *fresh-install* path. There is no code that diffs an existing install against the bundled migration set. After `npm install -g @latest` the npm packages are current, the database is frozen at first-kickstart. **Fix area (Brad's options, both reasonable):** (1) cheap version — schema introspection diff (probe for `memory_relationships.weight`, `memory_items.source_agent`, `cron.job WHERE jobname='graph-inference-tick'`, `pg_proc WHERE proname='memory_recall_graph'` — apply the corresponding migration if absent), or (2) durable version — dedicated `mnestra_migrations`/`rumen_migrations` tracking table the runner consults on every re-run, with back-fill for existing installs. **Symptom-side regardless:** add `mnestra doctor` warning on N consecutive zero-result cron cycles. Brad's manual fix (deploy graph-inference, clone vault key, apply migrations 003 + 009/010/012/013/014/015) is idempotent and proven on his project — captured as a recovery script template in `docs/INSTALLER-PITFALLS.md` ledger entry #13. **Full failure-class synthesis at `docs/INSTALLER-PITFALLS.md`** (NEW, 2026-05-02) — read before scoping the sprint, the class taxonomy and pre-ship checklist apply to whichever fix path we pick.

- **🚨 FLASHBACK NOT FIRING IN JOSHUA'S DAILY FLOW (reported 2026-04-27 16:54 ET).** Joshua: "I still have not seen a SINGLE FLASHBACK since they stopped appearing." Sprint 21 fixed Flashback (root cause: stale RPC params causing 404 errors on `memory_hybrid_search`). Sprint 33 was a debug sprint that surface-validated the happy path: `[flashback] diagnostics fire in order, Mnestra returns 10 rows, proactive_memory WS frame delivered with real hit content`. **But Joshua has not actually seen a Flashback toast trigger in his real daily work since.** The likely cause is one of the deferred test failures from Sprint 36's "pre-existing 4" — specifically the two `PATTERNS.error` session.js cases (regex too narrow on Node errno + HTTP 5xx + canonical Unix shell `cat: /nope: No such file or directory` mid-line format). Sprint 33 identified the regex-too-narrow issue but the fix may not have shipped or may have regressed. **Action:** the "analyzer-pattern fix sprint" originally categorized in Section A.2 below is hereby promoted to P0 — it must ship before users will see what Sprint 21 promised. Likely shape: small focused sprint touching `packages/server/src/session.js` PATTERNS.error regex + a real-shell-fixture test pass + verification by triggering a `cat /nope` in a panel and confirming the toast renders. Could be folded into Sprint 38 sidecar (alongside Brad's hook fix) OR shipped as a v0.8.1/v0.9.1 hotfix. **Orchestrator's call at sprint planning time.**

- **🚨 Brad's empty-Mnestra ingestion fix — Mnestra-direct session-end hook.** Sprint 36 v0.8.0 shipped the MCP path migration + hook bundling, but the bundled `~/.claude/hooks/memory-session-end.js` delegates to `~/Documents/Graciella/rag-system/src/scripts/process-session.ts` — Joshua's private repo. Fresh users (i.e., Brad) get a hook that runs but silent-fails because `rag-system` doesn't exist on their box. Net effect: Brad's `pgvector` stays at 0 memories despite MCP wired and webhook bridge alive. **Joshua sent Brad a Mnestra-direct workaround hook on 2026-04-27 (`/tmp/brad-mnestra-hook.js`, ~120 LOC) — that proves the shape; the fix is to bring it home as the canonical bundled hook.** Two paths to close this in the published product:

- **🚨 Brad's empty-Mnestra ingestion fix — Mnestra-direct session-end hook.** Sprint 36 v0.8.0 shipped the MCP path migration + hook bundling, but the bundled `~/.claude/hooks/memory-session-end.js` delegates to `~/Documents/Graciella/rag-system/src/scripts/process-session.ts` — Joshua's private repo. Fresh users (i.e., Brad) get a hook that runs but silent-fails because `rag-system` doesn't exist on their box. Net effect: Brad's `pgvector` stays at 0 memories despite MCP wired and webhook bridge alive. **Joshua sent Brad a Mnestra-direct workaround hook on 2026-04-27 (`/tmp/brad-mnestra-hook.js`, ~120 LOC) — that proves the shape; the fix is to bring it home as the canonical bundled hook.** Two paths to close this in the published product:
  1. **Rewrite the bundled hook to call Mnestra MCP tools directly** (no `rag-system` dependency). The Brad workaround already does this — POSTs transcript JSONL summary to OpenAI for embeddings, then to Supabase REST `/rest/v1/memory_items`. Adapt and replace the current bundled file. **Recommended path** — closes the gap without depending on publishing another repo. ~120 LOC, well-bounded.
  2. Publish `rag-system` to npm. More work, exposes more surface area, doesn't help users who don't run `rag-system`. Not recommended.
  - **Scheduling decided 2026-04-27 16:55 ET:** **folded into Sprint 38 as orchestrator pre-sprint task.** Single-track, ~120 LOC, applied by orchestrator before the 4+1 graph lanes kick off, ships as part of the same v0.10.0/v1.0.0 release. Full scope in `docs/sprint-38-knowledge-graph/PLANNING.md` § "Orchestrator pre-sprint task — Brad's bundled-hook Mnestra-direct rewrite (P0)".

---

## A. Correctness gaps (test debt + flagged bugs)

- **~~Analyzer-pattern fix sprint~~** — **PROMOTED TO P0 above (2026-04-27 16:54 ET).** The 4 pre-existing test failures are no longer "deferred test debt" — at least 2 of them (the `PATTERNS.error` session.js cases) are the proximate cause of Flashback not firing in Joshua's daily flow. See P0 section.
- **V4-1 Rumen `relate` embedding test coverage** — 4 failure modes (AbortController timeout, non-2xx, malformed vector, network error) handled with zero unit tests. Mock `fetch` at the module boundary. Flagged by 4 auditors over Sprints 6/12.
- **V4-3 Mnestra direct-bridge contract drift** — bridge direct mode posts `recency_weight + decay_days` (10 args) but bundled `memory_hybrid_search` SQL takes 8; bridge reads `m.similarity`, SQL returns `score`. CONTRADICTIONS #1, open since Sprint 8. Partially closed by recent migration work; verify and close fully.
- **V4-5 Auth brute-force rate limiting** — `auth.js` does strict-equality token comparison with no failure delay or IP throttle. Add in-memory bucket delay; switch to `crypto.timingSafeEqual`. Flagged by 3 auditors.
- **V4-6 Security/deployment doc drift** — `SECURITY.md` says `termdeck_auth` cookie; code uses `termdeck_token`. `DEPLOYMENT.md` says `/healthz`; server exposes `/api/health`. Deployment example uses `server.host`; code reads `config.host`. Doc-only, low risk.
- **Migration-001 idempotency** — `CREATE OR REPLACE FUNCTION` return-type collision when re-running migrations against an already-upgraded store. Doesn't affect fresh installs. Two fix options: explicit `DROP FUNCTION ... CASCADE` before recreate, or a `schema_migrations` tracking table. Sprint-32 candidate, deferred. Currently in CHANGELOG `[Unreleased] Planned`.
- **Rumen-MCP gap** — Memories written via the MCP path land with NULL `source_session_id`, so they never reach Rumen Extract. Sprint-33 candidate. Currently in CHANGELOG `[Unreleased] Planned`.

## B. Adoption levers (cut new-user friction)

- **Supabase MCP in setup wizard** — Today's setup is 15+ manual steps for new users (Supabase project, 6 SQL migrations, secrets.env, config.yaml, mnestra serve, termdeck). The setup wizard could use a Supabase MCP server to automate provision + migrations + secrets, cutting it to "paste 2 credentials, click 3 buttons." Highest-leverage adoption lever in the backlog. Detail in `IDEAS-AND-STATUS.md` § Installation Fear.
- **Fully-local Mnestra path (V5-1)** — SQLite + local embeddings. Today Mnestra requires Supabase + OpenAI. Ship an opt-in local-only mode so TermDeck works with zero external dependencies. Already in CHANGELOG `[Unreleased] Planned`. Reinforces Codex "MCP mode least-proven" concern.
- **One-click install button** — A web page or CLI wizard that detects what's installed → provisions Supabase (or connects to existing) → runs migrations → writes config → starts everything. Pairs with Supabase MCP item.

## C. DX improvements (orchestration/dashboard polish)

- **PTY drag/drop window rearrangement** — Dashboard panel reorder via drag-handle. Inject identifier is the session UUID, not visual position, so drag-reorder won't break inject. Pure CSS Grid + drag-handle work in `packages/client/public/app.js`. Light lift. Sprint 36 candidate, deferred.
- **Control panel dashboard for agent permission prompts (V5-3)** — Aggregate activity feed with Yes/No buttons so AI agents running in panels can be approved without context-switching to each terminal. From March 31 ideas backlog.
- **`app.js` feature-module split (V5-4)** — Currently >3000 LOC and growing. Split into `app.js` + `health.js` + `transcripts-ui.js` + `settings.js` + `graph.js` (post-Sprint-38) before the next major UI addition. Flagged by Sprint 12 auditor.
- **Multi-tab dashboard sync hardening** — Sprint 36 T3 added `config_changed` WS broadcast for the RAG toggle; the same pattern should generalize to other config edits and session lifecycle events. Probably grows out of Sprint 37 dashboard work.

## D. Scaling concerns

- **Multi-user data validation (V5-2)** — All current testing is single-developer. Run a multi-user session against Mnestra and confirm no cross-tenant leakage. Required before any beyond-localhost story.
- **MCP bridge verification (V5-5)** — `mnestra-bridge` MCP mode (vs direct/webhook) jumps directly into `tools/call` against a stdio child with no repo-local test coverage. Add a contract test parallel to the direct/webhook paths. Flagged by Codex.
- **Edge inference for legacy tables** — Sprint 38 ships edge inference for `memory_items` only. Legacy `mnestra_*_memory` tables and transcripts are out of scope. If those corpora get re-activated (V0.5.x event-stream RAG users), inference needs to extend.
- **Cross-Mnestra-instance graph federation** — Multi-machine Mnestra users (Joshua + Brad sharing structural-mvp memories) will eventually want graph queries that span instances. Sprint 38 ships single-instance only; federation is a later sprint.

## D.5 Sprint 52+ candidates (added 2026-05-03 from Brad's 4-project install pass + Sprint 51.5/51.6 dogfood findings)

- **✅ CLOSED (Sprint 65 / v1.4.0, 2026-05-16) — Brad's "next wave" feedback 2026-05-12.** All three asks landed: item 1 (per-product visibility) shipped as the project-filter chips; item 2 — 2b auto-close shipped (`panel_exited` WS frame + tile auto-removal), 2a "opens invisible" hypothesis B fixed (born-hidden guard) with A/C/D deferred pending Brad's repro; item 3 (orchestrator-aware-of-dead-panels) shipped (`?includeExited` filter + `410 Gone` on dead-panel inject + verified idle/parked detection). _Original entry retained for history:_ **🪟 Brad's "next wave" feedback 2026-05-12 ~10:15 ET — three orchestrator-reliability + visibility asks.** Brad sent his next observation block for the next wave/sprint set, verbatim: *"two helpful feature requests. ability to hide windows for all but chosen product.  way to ensure that CLI windows don't open invisible and are closed.  Orch needs to be more aware of status.  at one point I had 18 windows open.  10 were dead codex cli"*. Parses to three items:

  1. **Per-product panel-visibility filter.** Show only panels for a chosen "product" (project — keyed on `session.meta.cwd` resolving to a known project tag, OR a new `session.meta.project` field if cwd-to-project isn't 1:1). UI shape: project picker dropdown above the grid that filters which panels render; "All" is the default; clicking a project hides all panels not tagged to it (CSS `display: none` on the grid item — don't tear down PTYs, just hide). Touches `packages/client/public/app.js` (grid render + new control), `packages/server/src/session.js` (per-session project tagging if not already present), and possibly `/api/sessions` to surface the project tag. Pairs with the **Per-panel cwd switch** entry below in this section because both touch panel-metadata-as-first-class-citizen. ~150-250 LOC client + ~30 LOC server.

  2. **CLI panels must (a) open visible AND (b) auto-close when their child process exits.** Two distinct sub-bugs:
     - **(a) "Don't open invisible."** Symptom unclear without repro from Brad — candidate causes: panel is created in a grid slot that's off-screen in the current layout (e.g., 6-panel grid in a 4-slot layout shows only 4); panel is created with `display: none` from a layout-state hangover; panel is rendered behind a stuck modal/overlay (the 2026-04-27 "dark veil" bug pattern, see BACKLOG § A). Need repro before scoping the fix; ask Brad for the steps that produced it at sprint inject.
     - **(b) "Are closed."** When a CLI panel's underlying child process exits (Codex `/exit`, Claude SIGKILL, shell `exit`, PTY reaper SIGTERM — any exit path), the UI panel should auto-remove from the grid, not linger as a "dead" tile. Currently `term.onExit` at `packages/server/src/index.js:1163` fires the close handler (Sprint 50 T1's `onPanelClose`) which drains transcript to Mnestra and marks `meta.status: 'exited'`, but the panel's grid tile stays. Fix: add a client-side WS frame (`panel_exited` or extend an existing lifecycle event) that the dashboard listens for and removes the tile + xterm.js viewport; OR auto-hide any session whose `meta.status === 'exited'` AND `lastActivity` is older than N seconds (lets the user see the final post-exit lines for a brief grace period before the tile collapses). PTY reaper at `packages/server/src/pty-reaper.js` already detects + SIGTERMs orphaned processes, but death doesn't propagate to the client beyond a buffer-state flip. ~80 LOC server + ~50 LOC client.

  3. **Orchestrator-aware-of-dead-panels.** Brad's smoking gun: *"at one point I had 18 windows open. 10 were dead codex cli."* The orchestrator polls `GET /api/sessions` for routing/inject/coordination decisions; today, dead panels remain in that list because the session metadata isn't cleared on PTY exit. **EXTENDS the existing P0 "Per-adapter idle/parked status detection"** (BACKLOG § P0, Sprint 59-surfaced) — that P0 is about idle-vs-active conflation; Brad's report extends it to dead-vs-live conflation, which is structurally similar but a degree more severe (idle panels MIGHT get used again; dead panels can never be). Fix scope overlaps heavily with 2b above: when a panel exits and `meta.status` flips to `exited`, `/api/sessions` should EITHER filter exited sessions out entirely (default) OR surface them with a clear `is_alive: false` boolean the orchestrator can filter on. Coupled with the inject API: if the orchestrator POSTs to `/api/sessions/:id/input` on a dead session, return `410 Gone` immediately rather than the current `200 {"ok":true,"bytes":N}` (which silently no-ops on a closed PTY — semantically wrong and a debugging trap).

  **Sprint shape recommendation.** Single 3+1+1 sprint, half-to-full-day wall-clock:
  - **T1**: items 1 (per-product filter) + UI half of 2b (panel removal on exit) — single client-side lane in `app.js`.
  - **T2**: server half of 2b + item 3 (filter exited sessions from `/api/sessions`, return `410 Gone` on inject to dead panels, propagate exit via WS frame). Also subsumes the existing P0 "Per-adapter idle/parked status detection" — bundle them since they share the `session.js` status-flip surface.
  - **T3**: ask Brad for 2a repro at inject; if it reproduces, scope the fix; if not, defer 2a as a "needs repro" issue.
  - **T4 (Codex auditor)**: 3+1+1 audit lane per `~/.claude/CLAUDE.md` § Sprint role architecture — independent reproduction of T1/T2, adversarial review of the exit-propagation flow (race conditions between PTY death + WS frame delivery + client render).

  Joshua's framing: *"for when we kick off our next wave/set of sprints"* — explicitly forward-looking, not a hotfix demand. Schedule as Sprint 64+ candidate. Brad's source quote captured verbatim above; do not paraphrase when briefing the sprint.

- **✅ CLOSED (Sprint 65 / v1.4.0, 2026-05-16) — Brad's 2026-05-13 v2 spec: project-filter chips + ORCH panel pin.** Shipped in full — chips auto-discovered from `meta.project` with live panel counts + `localStorage` persistence; ORCH pin via explicit `meta.role` flag (Approach A) with gold/amber border, ORCH badge, and a dedicated pinned row. _Original entry retained for history:_ **🪟 Brad's 2026-05-13 v2 spec refinement — project-filter chips + ORCH panel pin (consolidates with the 2026-05-12 item above).** Brad forwarded a detailed spec via Gmail 2026-05-13 ~18:59 ET, observed against `@jhizzard/termdeck@1.2.0 / @jhizzard/mnestra@0.4.9 / @jhizzard/rumen@0.5.3` on his R730 (Ubuntu 24.04, Node v22.22.2). Source spec on Brad's box at `/home/nacho/termdeck-feature-request-project-filter-2026-05-13.md` — mirror to `docs/sprint-65-dashboard-reliability/BRAD-SOURCE-SPEC-2026-05-13.md` at sprint scoping for verbatim lane-brief reference. Two interlocking refinements:

  1. **Project-filter chips at top of dashboard.** Concrete UI: horizontal row of chips above the panel grid — one chip per known project (auto-discovered from `meta.project` across active sessions) plus an "All" chip. Chip content: project name + active panel count, e.g. `[ All (12) ] [ aetheria (3) ] [ structural360 (8) ] [ orch (1) ]`. Click switches filter (single-select; multi-project filter is out of scope per Brad's "let single-select prove out first"). Selected state persists in `localStorage` (key suggestion `termdeck.dashboard.projectFilter`). Counts auto-update from the existing 2-second `status_broadcast` WS frame — no protocol change needed; pure client-side filter over existing `meta.project` data. **Supersedes item 1 of the 2026-05-12 entry above.**

  2. **Orchestrator panel always visible + visually distinct (NEW; not in the 2026-05-12 entry).** Orch panel is the operator's primary control surface; needs to (a) be always visible regardless of project filter (pinned in a dedicated row ABOVE the filtered grid — even when filter = "structural360", the orch panel still shows); (b) be visually distinct via gold/amber border + explicit "ORCH" text badge in the title bar + always-pinned top-left of its row (consistent position so muscle memory works); (c) be identified via an explicit role flag rather than inferred from cwd.

  **Approach A vs B for orch identification (Brad's open question 1; orchestrator recommendation = A).** Brad sketched two paths:
  - **A (explicit role flag — RECOMMENDED).** Add `meta.role: "orchestrator" | "worker" | "reviewer" | "auditor" | null` field accepted by `POST /api/sessions` body. Operators tag explicitly at spawn time. Dashboard renders ORCH treatment when `meta.role === 'orchestrator'`. **Why A wins:** explicit, supports future role expansion (auditor for 3+1+1, reviewer for code-review panels), matches the canonized 3+1+1 pattern where roles are first-class citizens (`~/.claude/CLAUDE.md` § Sprint role architecture), zero ambiguity vs cwd inference. Cost: 1 new optional field on the `POST /api/sessions` body shape + ~10 LOC in `spawnTerminalSession` (`packages/server/src/index.js:1118`) + persistence in `sessions.create()`; the `meta.role` automatically flows through `status_broadcast` (line 2409, `JSON.stringify({ type: 'status_broadcast', sessions: allMeta })`) without any WS protocol change.
  - **B (inferred from project absence + cwd).** Treat session as orch when `meta.project` is null/empty AND `meta.cwd` is the user's home (or matches a configurable "orch home" path). Fragile: orchestrators that aren't in `$HOME` break (Brad runs orchestrators in project dirs); sub-project orchestrators break; the existing per-panel cwd switch backlog item (D.5 above) makes this even more fragile because cwd can change mid-session. Plus zero forward-compat for other roles.

  **WS session-update protocol notes (Brad's open question 3).** No protocol change required for either feature:
  - The existing `status_broadcast` WS frame at `packages/server/src/index.js:2409` fires every ~2 seconds with the full meta dict per session (`{ type: 'status_broadcast', sessions: allMeta }`). `meta.project` is already populated and surfaces in the broadcast; chip filtering is pure client-side over this existing data.
  - Adding `meta.role` as a new field flows through the same broadcast unchanged (it's just another key in the per-session meta object the dashboard already iterates).
  - Active-panel-count per chip = client-side `count(sessions, where meta.project === chip)` on each frame; no per-chip endpoint needed.
  - localStorage is naturally scoped to the dashboard origin (`http://127.0.0.1:3000` by default); no extra cross-origin work needed.
  - Reference precedent: Sprint 41 canonized `session.meta.project` as the load-bearing tag for graph filtering (`packages/server/src/mnestra-bridge/index.js` RPC `filter_project`); Sprint 36 T3's `config_changed` + Sprint 42 T4's `projects_changed` broadcasts are the templates for any future per-event WS messages, but neither is needed for this feature.

  **Sprint placement (Brad's open question 2; orchestrator recommendation = Sprint 65, NOT Sprint 64).** Sprint 64 is the install-polish convergence sprint per `docs/RESTART-PROMPT-2026-05-11.md` § Sprint 64 candidates (Supabase MCP auto-provision + OS-detection + Mnestra companion patch + Sprint 63 carve-outs + Investigation 2 from CRITICAL-READ-FIRST-2026-05-07) — this is the keystone before MacBook Air dogfood per `docs/CONVERGENCE-PLAN.md`. Bundling a UI feature into Sprint 64 dilutes the convergence focus. **Sprint 65 = "Dashboard reliability + orch-panel awareness wave"** is the natural slot, bundling:
  - This 2026-05-13 v2 spec (chips + orch pin) — supersedes item 1 of the 2026-05-12 entry above.
  - Item 2 of the 2026-05-12 entry (CLI panels open visible + auto-close on PTY exit; both sub-bugs).
  - Item 3 of the 2026-05-12 entry (orchestrator-aware-of-dead-panels — extends the existing P0 idle/parked detection; bundle since they share the `session.js` status-flip surface).

  **Sprint 65 lane shape:** single 3+1+1 sprint, full-day wall-clock (heavier than the 2026-05-12 estimate because of the orch-pin addition + the mandatory 3+1+1 auditor lane).
  - **T1 (client):** project-filter chips + orch-pin visual treatment (gold/amber border, ORCH badge, dedicated pinned row) + UI half of panel-removal-on-exit + the new `panel--role-orch` CSS class. Single client-side lane in `app.js`, ~250-350 LOC + `style.css` additions. Coordinate with the existing V5-4 app.js feature-module split (BACKLOG § C) — if the split happens before Sprint 65, this lane should land in `dashboard.js` or `chips.js` rather than further bloating `app.js`.
  - **T2 (server):** `meta.role` field accepted by `POST /api/sessions` body + `spawnTerminalSession` signature wiring (`packages/server/src/index.js:1118`) + `sessions.create()` persistence + filter exited sessions from `/api/sessions` (default) + `410 Gone` on inject to dead panels + propagate panel-exit via WS frame + the existing P0 per-adapter idle/parked status detection. Bundle these because they share `session.js` and `index.js:1118-1395` surface area. ~150-200 LOC.
  - **T3 (verification + 2a repro):** ask Brad for the "opens invisible" repro at sprint inject; if reproduces, scope the fix; if not, defer 2a as "needs repro." Then run T1 + T2 against Brad's 18-panel-2-project repro shape from his 2026-05-12 quote; verify chip switch <100ms, orch pin distinguishable from 6+ feet (per Brad's acceptance criterion), panel-exit auto-removes tile within 1s, dead-panel-inject returns 410.
  - **T4 (Codex auditor — 3+1+1 mandate per `~/.claude/CLAUDE.md` § Sprint role architecture):** independent reproduction of T1/T2; adversarial review of the role-flag schema (does `meta.role` need RBAC? does adding it break the existing wizard? does the dashboard's WS handler choke on unknown role values? does it land in `session_summary` rows correctly?); race-condition audit of the exit-propagation flow (PTY death + WS frame delivery + client render); verify localStorage scope hygiene; verify the count-update doesn't thrash on rapid panel spawn/exit. CHECKPOINT discipline mandatory per § Three hardening rules.

  **Out of scope for Sprint 65 (defer to Sprint 66+):** per-project background tint across the whole grid; keyboard shortcuts (Ctrl+1/Ctrl+2 for switch); multi-project filter (let single-select prove out first per Brad's framing); "recent projects" auto-ordering of chips by last-active. **Stray-letters typing duplication** Brad noted at the bottom of his email is explicitly NOT a Claude/TermDeck bug per his own analysis (likely OS-level clipboard manager or remote-desktop sticky-paste mode); document only if it recurs with a screenshot at moment-of-typing.

- **🪟 Joshua's 2026-05-14 ask — many parallel projects: 8-panel grid OR multi-port TermDeck instances.** Surfaced 2026-05-14 ~14:00 ET as Joshua prepared to restart his session in TermDeck for parallel project orchestration. Direct quote: *"a way to open 8 panels or to use two different ports so I have many multiple parallel projects going at once."* Two paths, not mutually exclusive — could ship one, the other, or both:

  - **WHAT ALREADY WORKS IN v1.2.0 TODAY (verified 2026-05-14 14:08 ET via grep + read):**
    - **8 panels = SHIPPING.** `packages/client/public/app.js:4368` defines `const layouts = ['1x1', '2x1', '2x2', '3x2', '2x4', '4x2', 'orch']`. Both `2x4` (2 cols × 4 rows) AND `4x2` (4 cols × 2 rows) are 8-panel grids. Keyboard handler at the same line binds number keys 1-7 to the layouts in order. **Joshua can hit 8 panels RIGHT NOW by pressing key 5 (2x4) or key 6 (4x2) in his dashboard.** CSS at `packages/client/public/style.css:318-319` defines both grid templates.
    - **Multi-port = SHIPS, technically.** `packages/cli/src/index.js:332` parses `--port`; line 154 explicitly hints `"Use --port <other> for a second instance."` This is documented behavior, not a hidden flag. Joshua can run `termdeck --port 3001` simultaneously with the default-port instance and get a second browser tab on `127.0.0.1:3001`. **Caveat:** the SQLite db at `~/.termdeck/termdeck.db` (`packages/server/src/database.js:46`) is hardcoded shared across instances — WAL mode is enabled (line 54) so cross-process writes don't corrupt, but writes serialize on a single lock. Practical impact: small perf cost on simultaneous session-create bursts; session UUIDs prevent collisions. Browser tabs are separate origins so `localStorage` is per-instance.

  - **Path A — Extend grid layout presets to 10/12/16+ panels.** What's actually missing (after the 8-panel correction above) is layouts for N>8. Add `layout-10` (5×2 or 2×5), `layout-12` (4×3 or 3×4), `layout-16` (4×4). Pure client-side CSS Grid + new dropdown options in the layout switcher + keyboard bindings for keys 8/9/0. Pairs naturally with Brad's project-filter chips (Sprint 65 above) because chip-filtered views often have 6-12 panels visible. **Light lift, ~30-60 LOC in `app.js` + a few CSS rules in `style.css`.** Fold-into-Sprint-65 candidate — same client lane (T1) as the chip + orch-pin work.

  - **Path B — Multi-port TermDeck instances as a first-class supported workflow.** The technical capability already exists (see above); what Path B adds is making it CLEAN and SUPPORTED, not making it WORK. Scope:
    - **Per-instance SQLite (e.g., `~/.termdeck/termdeck-3000.db`)** so writes don't serialize across instances. Eliminates the small perf cost + the theoretical UUID-collision risk + makes per-instance data exportable cleanly.
    - **`TERMDECK_PORT` env var threaded into bundled hooks** so `~/.claude/hooks/memory-session-end.js` can attribute Mnestra writes + cost-panel rollups to the right instance. Precedent: `TERMDECK_SESSION` already gets injected at `packages/server/src/index.js:1174` via the same `env:{...}` block; adding `TERMDECK_PORT` is a 1-line change there.
    - **`termdeck doctor` multi-instance awareness:** auto-discover running ports (probe `127.0.0.1:3000-3010` for live TermDeck signature), report N instances, run diagnostics per-instance.
    - **Stack installer:** today `init-mnestra.js` + `init-rumen.js` assume one instance. Document the supported pattern for "run a second TermDeck on a second port" (likely: same Mnestra + Rumen backing services, just per-port db).
    - **Cost-monitoring panel** (planned per `project_cost_monitoring_panel.md`): per-instance views by default; optional cross-instance rollup as a v2.
    - **README + GETTING-STARTED docs:** add a "Running multiple instances for parallel projects" section once Path B ships.

  - **Interaction with Brad's chip-filter (Sprint 65):** Path A (more layouts) complements chips (more panels × project filter). Path B (multi-port clean) partially obsoletes chips for users who run one instance per project (URL-per-project does the same job at OS-process level), but chips remain valuable for the single-all-projects view (Joshua's current workflow). The two coexist.

  **Sprint placement recommendation:** Path A → fold into Sprint 65 (Dashboard reliability + orch-panel awareness wave; same T1 client-lane surface as chips + orch-pin). Path B → Sprint 67+ candidate (architectural depth — touches DB layer, hooks, stack-installer, doctor, cost panel; needs its own scoping pass + a 3+1+1 sprint with Codex auditor). **Immediate workaround for Joshua's parallel-projects need:** press key 5 or 6 for 8-panel grid today; run `termdeck --port 3001` for a second instance if 8 panels isn't enough.

  **Multi-port verification 2026-05-14 15:28 ET (live test against running :3000 instance).** Findings:
  - **Works:** `termdeck --port 3001 --no-open` boots cleanly; full stack launcher fires all 4 steps; Mnestra **attach-to-existing already works in v1.1.0** (launcher detected "already running, 6,641 memories" — Brad's §3 #4 is already shipping); all 7 health checks pass on :3001; `POST /api/sessions` on :3001 returns 201; shell-session lifecycle works end-to-end (spawn → echo → exit 0 → status flip to `exited`); :3000's in-memory state of 4 panels stays correct throughout.
  - **WAL-mode SQLite cross-process writes confirmed safe.** Both instances share `~/.termdeck/termdeck.db` with `.db-shm` + `.db-wal` files; concurrent session-end UPDATEs from both instances did not corrupt. WAL is the unlock that makes Path B technically optional.
  - **Rough edge #1 — orphaned-session marking at second-instance boot.** `[db] Marked 4 orphaned session(s) as exited` fired at :3001 startup. The second instance read SQLite, saw 4 sessions with `exited_at IS NULL`, didn't recognize them as owned by the live :3000 instance, marked them exited at boot time. :3000's in-memory state stays correct (live), but SQLite metadata is now wrong. Next time a `termdeck doctor` or dashboard "recent sessions" view reads from SQLite, those 4 will surface with stale exited_at timestamps. **Fix:** Path B's per-instance SQLite (`termdeck-3000.db` / `termdeck-3001.db`) eliminates this; alternatively, the orphan-cleanup logic should check `pgrep -f 'termdeck.*--port'` for live siblings before marking.
  - **Rough edge #2 — SIGTERM not propagated from launcher to actual server process.** `kill -TERM <launcher-PID>` did NOT bring down the underlying node server on :3001. Had to `lsof -ti :3001` then `kill -KILL <child-PID>`. The launcher script forks the real server (`node packages/cli/src/index.js`) but doesn't trap-and-forward signals to the child. **This is a real operational bug for multi-port workflows** (operators can't cleanly shut down second instances). Either fold into Sprint 64 T1 (since T1 touches the launcher surface for unified `init`) OR carve to a Sprint 66+ "launcher signal handling" mini-sprint. Cross-reference: Brad's "supervisor wrapper" ask in v1.0.14 hotfix bundle (§ P0 above) is about `start-termdeck.sh` (the hand-rolled outer wrapper) — this is a DIFFERENT surface, in the bundled `termdeck` CLI itself.
  - **Rough edge #3 — Joshua's globally-installed `termdeck` is v1.1.0**, NOT v1.2.0 from 2026-05-11. The Wave 2 crash-class fixes (PTY-leak, WS ioctl race, body-parser hardening, 410 Gone on resize) are NOT yet on his daily-driver. `npm i -g @jhizzard/termdeck@latest` is needed to pick up v1.2.0. Note for Joshua at next convenient moment.
  - **Net conclusion:** multi-port WORKS today for Joshua's parallel-projects need; ship Path B in Sprint 67+ to clean up the rough edges. Sprint 64 T1's `init` work + Sprint 65 T2's lifecycle work indirectly improve the multi-port story (cleaner inject, cleaner panel-exit propagation); explicit Path B sprint addresses the remaining items.

- **🪟 Dashboard text-break + copy-paste rendering directive (Joshua 2026-05-14 ~17:55 ET, reiterated at ~18:25 ET mid-Sprint-64-publish).** Verbatim 17:55 ET: *"There are lots of text breaks that I see in TermDeck terminals, and the copy-paste blocks are never clean. We should have a directive to always render them as one line or to fix the formatting programmatically."* Verbatim 18:25 ET reinforcement: *"we have to fix the way text copy and pastes out of terminals - it breaks lines and introduces spaces."* — explicitly calls out TWO failure modes: (i) line breaks injected mid-content, AND (ii) extra spaces injected (likely between wrap-points or at line boundaries). The second observation is new — Path B (programmatic) must strip spurious whitespace not just collapse `\n`s. Two compatible interpretations:

  - **Path A — render-as-one-line (visual).** When a multi-line text block is pasted into a TermDeck terminal (via the dashboard input box OR via cross-panel paste-from-output), the rendered visual collapses internal line breaks into spaces, displaying as one line. Doesn't alter the underlying PTY input — the agent receives the original multi-line text — but the operator sees a clean single line. Cheaper, doesn't touch semantics. Surface: client-side render hook in `packages/client/public/app.js` around the `clipboard.copy` / `clipboard.paste` event flow + xterm.js paste handling. Risk: doesn't fix the actual problem if the underlying PTY truly receives broken paste shape.

  - **Path B — programmatic formatting fix (semantic).** Strip stray line breaks at the boundary BEFORE they reach the PTY's input handler. Replace mid-paragraph `\n` with spaces (preserving real paragraph breaks); collapse runs of `\n\n+` into single `\n\n`; strip trailing whitespace on each line. Touches both client-side (paste from clipboard / from another panel) and server-side (the `/api/sessions/:id/input` boundary). Surface: server-side `normalizePasteShape(text)` helper invoked from the input route + client-side mirror for cross-panel paste.

  - **Likely root cause hypothesis.** TermDeck's PTY-output → clipboard → PTY-input round-trip introduces stray line breaks because (a) xterm.js wraps long output lines at the column width (default 80 cols) with `\n` insertions; (b) the user's clipboard captures those wraps as real `\n` characters; (c) when pasted into another panel's input, the wraps become submit-keys in the receiving shell, breaking command continuity. The dashboard input box's bracketed-paste shielding (Sprint 36 era) may also be inconsistent across panel types. Verify at scoping by examining one of Joshua's actual broken-paste blocks via DevTools clipboard inspection.

  - **Sprint placement recommendation.** Fold into Sprint 65 T1 (Dashboard reliability + orch-panel awareness wave) — T1 already touches `app.js` paste / render surfaces for the chip filter + ORCH pin work. Adding paste-normalization is ~30-60 LOC client + ~20 LOC server. Or carve to its own Sprint 66+ if Sprint 65 T1 lane is already loaded. **Out of scope:** changing xterm.js's column wrap behavior (that's upstream); changing how the user's OS clipboard handles wraps (out of TermDeck's hands).

  - **Acceptance criteria.** From a TermDeck panel running a wide-output command (e.g., a long ls or a wrapped git log line), copy a wrapped line, paste into another TermDeck panel's input box. Verify: the pasted text appears as one continuous line (no mid-line `\n` chars), the command executes correctly without premature submit, and the visual rendering in the input box doesn't show artifacts. Cross-panel paste between Claude / Codex / Gemini / Grok / shell panel types all behave consistently.

- **🪟 Global `~/.claude/CLAUDE.md` size trim (Joshua noted 2026-05-14 ~20:20 ET, post-Sprint-64 wrap).** Joshua received an automated harness notification that `~/.claude/CLAUDE.md` is "already very large." Current size after Sprint 64 close-out edits: 387 lines. Sprint 64 added ~50 lines to it (Before Context Gets Long expansion + Kitchen vs recipes directive). The file's grown organically across 60+ sprints with new mandates layered on top of old ones; no consolidation pass has been run. **Risk if not addressed:** every Claude Code session boot pre-loads CLAUDE.md into context — at some point the file's own size starts to crowd out actual session work. **Sprint shape:** 1-2 day single-orchestrator (no 3+1+1 ceremony). Pass: re-read the entire file; collapse redundant sections; promote any "Sprint N" historical-context paragraphs to Mnestra memories (kitchen-level, not recipes — per the new directive the trim itself codifies); preserve the load-bearing rules (3+1+1 hardening, RLS hygiene, gitleaks discipline, kitchen-vs-recipes, Auto-commit on compaction-near, never-copy-paste-messages, time-check, session-end email). Aim for ~200-250 lines while preserving every load-bearing directive. Cross-reference: Sprint 53's similar trim attempt for project-level docs (commit `32d3e78`); same pattern at the global file level. Sprint 67+ candidate; not load-bearing for any in-flight work.

- **◑ PARTIALLY CLOSED (Sprint 65 / v1.4.0, 2026-05-16) — Joshua's 2026-05-15/16 client asks.** `1×2` layout SHIPPED (Path A presets); per-panel CLI font-size SHIPPED as a global font-size stepper (a true per-panel variant remains a Sprint 66 refinement option). Grid row/column resizing DEFERRED → Sprint 66 (~200-350 LOC, own scoping pass — see the Sprint 65 deferrals entry below). _Original entry retained for history:_ **🪟 Joshua's 2026-05-15/16 client asks — `1×2` layout, grid row/column resizing, per-panel CLI font-size (surfaced 2026-05-16 during Sprint 65 boot).** Three client-side dashboard items: **(1) `1×2` layout** — 1 col × 2 rows, two panels stacked vertically; a new preset in the `app.js` layouts list (`['1x1','2x1','2x2','3x2','2x4','4x2','orch']` at `app.js:4368`) — folds straight into Sprint 65 T1 Path A (sub-task 1.4, layout presets). **(2) Grid row/column resizing** — draggable dividers between grid rows/cols for non-uniform "less traditional" layouts; bigger lift than a preset (resize handles + CSS Grid `fr`-track manipulation + per-layout persistence). Cousin of the deferred "PTY drag/drop window rearrangement" in § C. **(3) Per-panel CLI font-size control** — reduce xterm.js `fontSize` in CLI windows; small-to-moderate, per-panel or global with localStorage persistence. **Sprint placement:** (1) folds into Sprint 65 T1 Path A; (2) + (3) are new — fold into Sprint 65 T1 if lane bandwidth allows (orchestrator adjudicates at inject), else Sprint 66 standalone.

- **Sprint 65 close-out — deferrals to Sprint 66 (logged 2026-05-16 / v1.4.0).** Four items adjudicated out of Sprint 65 and queued: **(1)** draggable grid row/column resizing for non-uniform layouts (Joshua 2026-05-15/16 ask; ~200-350 LOC, own scoping pass — cousin of the deferred "PTY drag/drop window rearrangement" in § C). **(2)** "CLI windows open invisible" (Brad 2026-05-12 item 2a) hypotheses A (off-screen grid slot), C (dark-veil modal — `ARCHITECTURE.md` Known Issue #8), D (WS/cross-client spawn race) — deferred pending Brad's repro; hypothesis B (born-hidden under filter) was fixed in Sprint 65. Full analysis + paste-ready entry at `docs/sprint-65-dashboard-reliability/2A-OPENS-INVISIBLE-ANALYSIS.md`. **(3)** `meta.role` persistence into `session_summary` / `memory_items` / `memory_sessions` rows — descoped from v1.4.0 (dashboard-only); revisit when a role-analytics consumer exists (touches the bundled session-end hook in `packages/stack-installer/`, a separate package). **(4)** legacy `orch` grid layout vs the new ORCH-pin row — selecting the legacy `orch` layout makes the last worker span oddly now that a role-tagged ORCH tile moves to `#orch-pin-row`; cosmetic, non-blocking; gate or retire the legacy layout. Plus the broader repo-root `tests/` glob gap (strands `per-agent-hook-trigger.test.js`, `ws-handler-contract.test.js`, `escapehtml-client.test.js` outside the `npm test` glob) — a test-hygiene consolidation item.

- **📡 Brad's 2026-05-15 Telegram-mediated orchestrator↔operator bridge — cross-orch coordination proposal (forwarded via Josh 2026-05-16).** Brad built a two-way Telegram bridge on his R730: dedicated bot (`@nacho_orch_bot`) + bash daemon (`~/bin/tg-poll.sh`, ~150 LOC) long-polling Telegram `getUpdates` + systemd user service (`Restart=always`, linger-on) + injection into the orch panel via TermDeck's existing `POST /api/sessions/:id/input`. Reliability mechanisms: heartbeat file, `(chat_id,message_id)` dedupe via `inbox.jsonl`, Telegram offset not advanced past failed injects (retry next cycle), re-reads orch session UUID per inject. **Constraint learned:** Telegram `getUpdates` is exclusive — one long-poller per bot token (HTTP 409 if two) — so cross-orch coordination wants **two bots in one shared channel** (each orch polls its own bot, posts as itself, filters inbound to "from the other bot"). Brad proposes Josh mirror the shape (`@josh_orch_bot` + daemon) — the build-out of Josh's 2026-05-13 "shared coordination channel" idea. Source brief on Brad's R730 at `/home/nacho/josh-thread/2026-05-15-telegram-orch-setup-for-josh-mirror.md`; daemon at `/home/nacho/bin/tg-poll.sh`. **Sprint placement:** NOT in Sprint 65 as scoped (Sprint 65 = dashboard-reliability — Brad's "for Sprint 65" framing is his assumption, not the plan). New candidate — cross-orch coordination is its own architecture pass and needs Joshua's explicit go-ahead (standing up bot + daemon + systemd on his box). Sprint 66+ or a standalone mini-sprint.

- **Sprint 61.1 — classifier hygiene pass (post-v1.1.0).** Mnestra migrations `011_project_tag_backfill.sql:160` (TermDeck-bundle-only DML) and `012_project_tag_re_taxonomy.sql:252` (mirrored in engram + termdeck bundle) contain a project-tagging classifier whose load-bearing matcher is a literal forbidden by the global hygiene rule. Surfaced by Sprint 61 T2 19:01 FINDING + ORCH 19:11 SCOPE adjudication; deferred from Sprint 61 because substituting the literal would silently break tagging on legacy `memory_items` rows. Allowlisted in `~/.gitleaks.toml` for v1.1.0 ship. Three resolution candidates to evaluate at Sprint 61.1 scoping: **(a)** substitute classifier matcher (e.g., `re_tag_lookup` table that maps legacy project markers → canonical tag) — best long-term shape, requires data-migration to populate the lookup from a one-time scan over the affected rows. **(b)** move classifier rules into a runtime config (read at function-call time from a SECURITY DEFINER lookup function or a non-shipped settings table) — keeps the classifier behavior intact but pushes the literal out of the SQL artifact. **(c)** accept as allowlisted permanently with a `SECURITY.md` § "Known carve-outs" entry explaining why the literal is structural — cheapest, no behavior change, but leaves the literal in shipped artifacts indefinitely. Joshua's call at scoping. Likely small focused sprint (~1 day single-orchestrator); could fold alongside Sprint 62's security-by-default work. Cross-references: CHANGELOG `[Unreleased]` § "Sprint 61 — DRAFT carve-outs" (gets folded to `[1.1.0] § Known carve-outs` at sprint close); Sprint 61 STATUS.md `[ORCH] SCOPE 19:11 ET` + `[T2] FINDING 19:01 ET`.

- **🪧 Blog post + docsite refresh — "The 3+1+1 pattern: how a Codex auditor caught four bugs in fourteen minutes" (Joshua 2026-05-03 21:04 ET).** Sprint 51.5 → 51.6 is THE narrative case study for the canonized 3+1+1 pattern (`~/.claude/CLAUDE.md` § Sprint role architecture). Worth a long-form blog post on joshuaizzard.com — not the usual release-notes shape, more a "here's what we learned" essay. **Source material to draw from:**
  - The Sprint 51.5 → 51.6 narrative arc: v1.0 ships → Brad's drift bug surfaces same day → v1.0.1 hotfix → live psql at 19:13 ET reveals two-bug picture → v1.0.2 mini fixes both → 24 hours, 25+ memories landed, 4 auditor catches, 2 structural-gap discoveries.
  - Codex's twelve audit posts in Sprint 51.6 (`docs/sprint-51.6-memory-sessions-hook-fix/STATUS.md`) — 4 caught real bugs in T3's WIP that all-Claude Sprint 51.5 missed (source_agent timing misattribution, Phase-B-doesn't-refresh-hook GAP, upsert on_conflict=session_id, root-package-excludes-hook HARD BLOCKER empirically confirmed via `npm pack --dry-run --json`, unsigned-custom-hook overwrite SAFETY GAP).
  - The two structural gaps the sprint surfaced: (1) compaction-vulnerability — Codex compacted mid-sprint and lost in-context audit state; recovered via orchestrator-side rebootstrap inject in 30 sec; (2) Codex CLI runtime missing Mnestra MCP — `memory_recall` not exposed; STATUS.md is the sole durable substrate.
  - The "second-layer-of-fundamental-bug" framing Joshua named at 20:00 ET (each hotfix surfaces the next layer until the auditor pattern breaks the cycle).
  - The Class M discovery (architectural omission, not execution failure) and the rag-system-hook-overwritten-on-2026-05-02 narrative — Joshua's daily-driver was silently capturing zero session summaries for 2 days because of a hook swap.
  - Live psql as a debugging primitive: the autoMode.allow rule for psql against secrets.env was the unlock — without it, this session would have been blind to the actual ground-truth row counts.
  - The 53-min inject-to-FIX-LANDED Sprint 51.6 wall-clock and what made it possible (heavy-context orchestrator + worker fluency + auditor independence).
  - The "I love this project and how it is improving itself" moment at 20:17 ET when Joshua named the pattern and asked for canonization.
  **Docsite updates needed (joshuaizzard.com):**
  - NEW blog post page using the source material above. Long-form. Joshua's voice required for the personal narrative pieces; orchestrator/worker drafts the technical scaffolding.
  - Update TermDeck product page (`docs-site/src/content/docs/termdeck/`) with the 3+1+1 pattern as a feature — auditor lane is now part of the canonical flow.
  - NEW changelog mirror entries for v1.0.1 + v1.0.2 (the docs-site changelog page may be stale at v1.0.0).
  - Update the homepage hero or product positioning to reflect "TermDeck orchestrates 3+1+1 sprints" — though this is a marketing call Joshua makes, not a routine doc edit.
  - Cross-link the blog post from the TermDeck product page + from `docs-site/src/content/docs/blog/` index.
  - Possibly update or extend the `docs/AGENT-RUNTIMES.md` (in the TermDeck repo) with a "Pattern: 3+1+1 mixed-role" section that's distinct from Sprint 47/49's mixed-peer pattern.
  **Sprint shape recommendation:** 1-2 day sprint, single track or 2+1 lanes (T1 = blog post draft from orchestrator + worker pair; T2 = docs-site page edits; +1 = Codex auditor of the blog post for clarity / over-claiming / under-claiming). Schedule after Sprint 52 (cost-monitoring panel) so cost-panel screenshots can also land in the docs-site update.



- **🚨 3+1+1 pattern compaction-vulnerability hardening + Codex MCP-not-wired gap (Sprint 51.6 post-publish 20:53 ET — Codex compacted mid-Phase-B; lost in-context audit state).** Two interlocking gaps in the canonized 3+1+1 pattern (`~/.claude/CLAUDE.md` § Sprint role architecture):

  - **Compaction vulnerability.** Codex CLI does not have a PreCompact hook equivalent to Claude Code's `Stop` / `PreCompact` lifecycle hooks. When Codex auto-compacts mid-sprint, the auditor's in-context reasoning (multi-phase audit progress, "verified items 1-4, still pending 5-7" tracking, WIP review state) evaporates.
  - **MCP-not-wired (Codex CLI runtime).** Per Codex's own 2026-05-03 20:54 ET REBOOTSTRAPPED post: *"direct `memory_recall` is not exposed in this Codex runtime, so reboot recovery used the append-only sprint record as the durable source of truth."* Codex CAN write to Mnestra (it landed 4 chopin-in-bohemia memories earlier tonight via direct Supabase pg-direct path) but CANNOT call `memory_recall` because the Mnestra MCP server isn't registered in Codex's runtime. So the auditor has no way to retrieve prior cross-sprint context via memory.

  **Net effect:** STATUS.md is the SOLE durable surface the auditor can read post-compact. Lane-brief discipline (verbose detailed audit posts, never abbreviated; phase-boundary checkpoint posts; explicit recovery-instruction at the top of each major audit phase) becomes load-bearing.

  **Recovery pattern that worked tonight:** orchestrator-side re-bootstrap inject pointing the auditor at its own prior STATUS.md posts + lane brief. ~30 sec recovery (Codex re-read 12 prior posts + lane brief + posted REBOOTSTRAPPED). Sprint completed.

  **Backlog actions for Sprint 52+:** (a) wire Mnestra MCP into Codex CLI (configure `~/.codex/config.toml` MCP servers — research what Codex's MCP wiring spec looks like; may require building an adapter); (b) propose a Codex CLI PreCompact hook upstream OR build a wrapper that snapshots Codex's working memory to STATUS.md / Mnestra before compact; (c) the auditor's lane brief MUST mandate verbose detailed posts (no abbreviations) + explicit phase-boundary checkpoint posts so post-compact recovery is fast; (d) document the compact-recovery inject pattern itself in `~/.claude/CLAUDE.md` § Sprint role architecture so future orchestrators handle compact-mid-sprint cleanly without flailing; (e) consider a dual-auditor approach (Codex + Gemini, or Codex + Grok) for high-stakes sprints so one auditor compacting doesn't reset the audit pipeline.

  **Same failure pattern as Brad's Class A drift, just at the agent-context layer instead of DB-schema layer.** Lane brief design must assume compact will happen; STATUS.md is the durable record that survives.

 The canonized 3+1+1 pattern (`~/.claude/CLAUDE.md` § Sprint role architecture) has a structural hole: Codex CLI does not have a PreCompact hook equivalent to Claude Code's `Stop` / `PreCompact` lifecycle hooks. When Codex auto-compacts mid-sprint, the auditor's in-context reasoning (multi-phase audit progress, "verified items 1-4, still pending 5-7" tracking, WIP review state) evaporates. STATUS.md is a partial backstop because lane FINDING/AUDIT posts are durable in git, but the *situational* state isn't. **Recovery pattern that worked tonight:** orchestrator-side re-bootstrap inject pointing the auditor at its own prior STATUS.md posts + lane brief — ~10 min recovery overhead, sprint completed. **Backlog actions for next sprint:** (a) propose a Codex CLI PreCompact hook upstream (file with OpenAI/Anthropic if applicable, or build a wrapper that snapshots Codex's working memory to STATUS.md or Mnestra before compact triggers); (b) for sprints using 3+1+1, the auditor's lane brief should explicitly require a `checkpoint memory_remember` call after every major audit phase so post-compact recovery is fast (e.g., "after each Phase A/B/C completes, write a [T4-CODEX] CHECKPOINT post + a memory_remember entry tagging the phase boundary"); (c) document the recovery pattern itself in `~/.claude/CLAUDE.md` § Sprint role architecture so future orchestrators handle compact-mid-sprint cleanly. **Same failure pattern as Brad's Class A drift, just at the agent-context layer instead of DB-schema layer.** Lane brief design must assume compact will happen; the durable record (STATUS.md + Mnestra) is what survives.

- **3+1+1 Orchestrator+Workers+Auditor pattern adoption across all projects (Joshua 2026-05-03 20:17 ET — surfaced during Sprint 51.6 when Codex caught 4 bugs in 14 min that all-Claude Sprint 51.5 missed).** This is now the canonical sprint pattern per `~/.claude/CLAUDE.md` § Sprint role architecture. Backlog action: retroactively designate one lane as auditor (Codex or Gemini or Grok) in the next multi-lane sprint of every active project — Maestro/chopin-scheduler Sprint 24, Mnestra/Rumen sprints, Podium, PVB, ClaimGuard, DOR, Portfolio, Scheduling. For Sprint 24 specifically (already authored): retrofit the lane brief structure so T4 designates a non-Claude auditor; Codex is the natural fit since Codex authored the source review docs that motivated Sprint 24 — assigning Codex as the auditor of the Maestro SaaS-readiness sprint matches the pattern where the agent that surfaced the gap is the agent that audits the fix. Future Sprint: extend the inject script template to autodetect the auditor lane (e.g., `T_audit` tag or `agent: codex` in PLANNING.md frontmatter) and customize the boot prompt accordingly.

- **Memory-budget tracking table (Joshua 2026-05-03 19:15 ET — surfaced during Sprint 51.6 mini orchestration).** A canonical table mapping (sprint shape, agent type, # of panels) → expected memory write count + write surface (memory_sessions vs memory_items vs MCP-direct). Lets a post-sprint health check answer "we ran 4 panels in a 4+1 sprint; we expected ≥4 memory_sessions rows + ≥16 memory_items rows; we got 0 + 25; what's broken?" — instead of discovering 2 days later via live psql probe that the hook was silently failing. Concrete shape: a `expected_memory_writes` table keyed by `(sprint_shape, agent_type, panel_count)`, populated by the orchestrator at sprint inject; a post-sprint reconciliation job compares actuals against expected; surfaces drift as a STATUS.md alert. Sprint shape: ~1 day; small. Most valuable as a FOUNDATION for the active health dashboard below.
- **Active health dashboard (Joshua 2026-05-03 19:15 ET — surfaced during Sprint 51.6 mini orchestration).** Currently the TermDeck `/api/health` endpoint reports passive checks (Mnestra reachable, DB connected, paths exist). The session-end hook silent-failure gap demonstrates that passive "everything is up" health checks are insufficient — `rumen-tick` was running every 15 min for 2 days reporting `{"ok":true}` while writing zero rows. The dashboard needs ACTIVE pings: (a) trigger a synthetic /exit via a test session every N hours; verify memory_sessions row count grows; (b) run `mnestra doctor` on a schedule; surface red findings as alerts; (c) compare expected-vs-actual memory writes per session against the budget table above; (d) integrate with `termdeck doctor` so the standard developer-facing diagnostic surfaces all of this. Sprint shape: 1-2 days; depends on memory-budget tracking table landing first. **This is the Sprint 38-onward gap that lets Class I (silent no-op) bugs hide for days.** mnestra doctor (Sprint 51.5 T2) is reactive; this dashboard is proactive.

## D.5 Sprint 52+ candidates (continued — earlier additions)

- **Per-panel cwd switch (Joshua 2026-05-03 — surfaced during Sprint 51.5 inject).** Today, when you spawn a Claude Code panel in TermDeck, the `cwd` is fixed at panel-creation time (driven by the active project's path). For multi-repo sprints — e.g., Sprint 51.5 T2 which operates in `~/Documents/Graciella/engram` while T1/T3/T4 operate in the termdeck repo — the agent has to `cd` into the right repo itself as part of its boot sequence. That works (T2's brief tells the agent to cd) but it's friction: the panel's `cwd` metadata stays out of sync with what the agent is actually working in, and the inject API's session-routing-by-cwd pattern (proposed in this file's Mnestra topology routing-layer item) would mis-route. **Scope sketch:** add a `PATCH /api/sessions/:id` endpoint that accepts `{ cwd: string }`, validates the path exists and is readable, sends a `cd <path>` keystroke (with bracketed-paste markers + `\r` to submit) to the PTY, and updates `session.meta.cwd`. UI: small dropdown or "switch directory" button in the panel header showing the cwd and letting the user type or pick another. Scoped at <100 LOC server-side + ~50 LOC client-side. Not in Sprint 51.5 scope (hotfix-shaped); candidate for Sprint 52 alongside the cost-monitoring panel since both touch panel metadata + UI. **Lateral effect:** unlocks cleaner orchestrator inject for cross-repo lanes (T2 in 51.5, future TMR sprints that span termdeck + mnestra + rumen).

- **Mnestra connection-topology routing layer (Brad 2026-05-03 architectural ask).** Brad runs four Supabase projects (`jizzard-brain`, `Structural`, `aetheria-phase1`, `aetheria-payroll`) and is deciding how Claude Code's MCP wiring should map to them. He sketched three shapes: A. Single store (collapse to one canonical Supabase, lose isolation). B. Four MCP entries in `~/.claude.json`, one per project (canonical Mnestra pattern; manual to manage but trivial to wire). C. **Single MCP entry that routes to the right Supabase based on the calling Claude session's `cwd`** — looks like one MCP from Claude Code's perspective, each project still gets its own store. Brad's read: Shape B for now (5 minutes to wire), revisit Shape C as a TermDeck v1.1+ feature ask if Shape B's management overhead bites. **Scope sketch:** ~half-day of work — a wrapper script (Node CLI in `packages/cli/src/mcp-router.js`?) that reads a project-to-Supabase mapping from a config file, picks the right env vars based on `process.env.PWD` or the inbound MCP session's working dir, and execs the real Mnestra MCP server. Could ship as a new `termdeck mcp-route` subcommand, or as a standalone `@jhizzard/mnestra-router` micro-package. Either way, the user's `~/.claude.json` has a single `mnestra` entry that points at the router. Not in Sprint 51.5 scope (hotfix-shaped); candidate for Sprint 52+ if Brad's Shape-B usage surfaces real friction.

## E. Risky dependency upgrades (deferred indefinitely)

- **Express 5 migration** — High risk, currently pinned at 4.x. Likely blocks many other items if attempted.
- **Mnestra Zod 4 migration** — High risk, deferred. Mnestra is at Zod 3.x.
- **Node 18 → 20 LTS bump** — Not currently flagged but worth assessing as Node 18 EOLs.

## F. Companion artifacts (recommended but not required)

- **v0.8.0 blog post** at `docs-site/src/content/docs/blog/v08-launcher-parity.mdx` — Phase B reconciliation + the four-server-kill saga is good narrative material. Per RELEASE.md, recommended for notable releases. Joshua's voice required.
- **v0.9.0 blog post** at sprint 37 close — Orchestrator-as-product story. Should mark the moment TermDeck graduates from "browser tmux" to "orchestration platform."
- **v0.10.0 / v1.0.0 blog post** at sprint 38 close — Knowledge graph debut. Pair with marketing screenshots from T4's D3 view.

## How to use this file

When you (a future session) finish a scoped sprint and need to pick the next one, read this file top-to-bottom and pattern-match against:

1. **Urgency from external signals** — Brad reports a regression, an audit lands, a launch-readiness deadline arrives. Those override priority order.
2. **Theme grouping** — Don't run a Correctness sprint right after another Correctness sprint if morale needs a feature win. Mix categories.
3. **Compounding leverage** — A single fix that closes multiple items below counts higher than a 1:1 fix. Example: Mnestra-direct hook rewrite (B) closes Brad's full ingestion gap (B) AND obsoletes the rag-system publish path AND simplifies fresh-install testing.

When an item gets scoped into a sprint, move it from this file into the sprint's `PLANNING.md` and delete (or strike-through) it here. Don't leave it in two places — the sprint plan becomes authoritative once an item is in flight.

When new items surface (audit, user feedback, incident postmortem), add them here in the right category. Don't create a new ad-hoc roadmap doc; consolidate.
