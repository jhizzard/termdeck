# Handoff — Sprints 71 (MCP Bridge) + 72 (Grok Panel) — 2026-06-08

Two-deck 3+1+1 session. Live, evolving handoff so the **next sprint can harden** what shipped.

## Shipped / state
- **Sprint 72 — Grok web-chat panel — FINAL-VERDICT GREEN ✅ (verified genuine).**
  New `packages/web-chat-driver/` (CDP attach via Playwright `connectOverCDP`; screencast; input forwarding; `grok/` selectors+completion+inject), `web-chat` session type in `packages/server/src/index.js` (8 additive seams; `web-chat-grok` adapter), client canvas panel in `packages/client/public/app.js`. Posture-locked (headful, dedicated profile, localhost CDP, **Grok-only**). Root tests **473/473**. All 3 lanes AUDIT-PASS.
- **Sprint 71 — MCP Bridge — code-complete, fully audited (T1/T2/T3 AUDIT-PASS), 86/86 tests; FINAL-VERDICT GREEN on bridge correctness + the live `memory_recall` round-trip. One narrow consumer-UI residual deferred — see § Deck A live-acceptance (2026-06-08) below.**
  New `packages/mcp-bridge/`: Streamable-HTTP MCP server + OAuth 2.1/PKCE + tunnels; read-only tools (Mnestra memory + TermDeck panel state); egress redaction + read-only policy + **default-deny** allowlist; connect docs (claude/chatgpt/grok) + `tunnel.md`.

## Pending (this session)
- **Deck A go-live — substantially DONE (2026-06-08).** The live claude.ai Custom Connector round-trip succeeded: `memory_recall` returned **24 memories** end-to-end (DCR → OAuth 2.1/PKCE + operator-secret consent → tunnel → Streamable-HTTP `/mcp` → tool dispatch → egress-redacted response → claude.ai). See § Deck A live-acceptance below. **Only residual:** a live smoke of the **approval-gated terminal-state tools** (`list_panels` / `read_panel`) through claude.ai with open panels — deferred to the next double-panel session (panels currently closed). Low-risk: all bridge-side behavior is covered by 86/86 tests incl. approval `_meta` emission, default-deny allowlist, and a full MCP SDK-client round-trip; the unproven slice is purely claude.ai's consumer-side approval-prompt UI + live-panel render.

## NEXT-SPRINT HARDENING BACKLOG
1. **✅ DONE (2026-06-08).** README doc-drift — `packages/web-chat-driver/README.md:64` now states `headful:false` is **rejected** (`transport.js` throws) and points to `WEB_CHAT_DRIVER_NO_BROWSER=1` as the real CI/display-less escape. (Also refreshed the drifted `packages/mcp-bridge/README.md` status line + layout table as part of the Deck A close — it claimed "in progress / stub / todo" for code that's now landed + tested 86/86.)
2. **`grok-web` provenance — STILL DEFERRED (re-scoped 2026-06-08; deliberate).** Spec unchanged: flip `web-chat-grok` `sourceAgent:'grok'`→`'grok-web'`, add `grok-web` to `ALLOWED_SOURCE_AGENTS` + a `web-chat`/`grok-web` byte-floor exemption in **both** bundled hooks, bump the hook stamps, refresh the installed copy. **Why it is NOT a quick autonomous edit (confirmed this session):** (a) the bundled hooks ship in the published tarball (`files: packages/stack-installer/assets/hooks/**`) → editing them is release-sensitive (version bump + republish per `docs/RELEASE.md`, governed by `docs/INSTALLER-PITFALLS.md`); (b) **cross-repo** — the Mnestra/engram recall `source_agents` enum (`["claude","codex","gemini","grok","orchestrator"]`) must add `grok-web` or the provenance *filter* can't target it (rows are still stored + recalled unfiltered, just not filterable by agent); (c) the adapter + hook edits **must ship together** — `normalizeSourceAgent` (memory-session-end.js:672) coerces any non-allowlisted tag to `'claude'`, so a half-edit silently MISLABELS web-chat rows as Claude. → Belongs in a small, deliberate hooks-release sprint, not an in-session tweak.
3. **Bridge ops hardening:** named cloudflared tunnel (stable hostname) vs ephemeral; Anthropic MCP Tunnels provisioning (verify current invocation); operator-secret rotation; make the redact denylist + allowlist first-class config (wizard step).
4. **Multi-provider connect:** validate ChatGPT (Developer Mode) + Grok (BYO-MCP) connect round-trips (docs exist).
5. **Grok panel live-hardening:** selector-drift maintenance (~4–8 wk cadence); completion-detector edge cases beyond timeout/quiet-only; verify long reasoning-turn behavior on the actual reasoning model.
6. **Brad open items (PR sweep):** mnestra **#15/#20** (privacy_tags — #15 awaits Josh's 4 answers; de-dup #20); termdeck **issue #12** (layout-focus + input-buffer-accumulation — *relevant to the new web-chat client input; review against it*); termdeck **#11** body has internal identifiers → scrub.
7. **✅ DONE (2026-06-08).** Root `npm test` now globs `packages/mcp-bridge/test/*.test.js` + `packages/web-chat-driver/tests/*.test.js`, with `WEB_CHAT_DRIVER_NO_BROWSER=1` set on the script so the browser `cdp.test.js` auto-skips (no window, no playwright needed) while the pure grok selector/completion tests run. Added `packages/mcp-bridge` to root `workspaces` so its deps install on a fresh clone. Root suite now **589 / 584 pass / 0 fail / 5 skipped** (the 5 = cdp browser tests). CI unaffected — `ci.yml` runs syntax/lint/install, not `npm test`.
8. **✅ DONE (2026-06-08), with a correction.** "Monitor v2" = `status-parser.js`'s `POST_RE` was **already** header-anchored — a `DONE` in a CHECKPOINT gist is captured as gist (group 5), never matched as the verb (traced + now locked with a regression test). The *real* adjacent bug it was masking: the v2 verb vocabulary was stale — missing `FIX-LANDED` / `FIX-PROPOSED` / `AUDIT-PASS` / `AUDIT-FAIL` and the `(qualifier)` form (e.g. `AUDIT-PASS (cdp/render)`), so it silently under-parsed real STATUS.md — `landed_since_last_red` never fired because real posts say `FIX-LANDED`, not `LANDED`. Fixed: expanded the verb set + optional parenthetical + `FIX-LANDED` now counts as a landing. +3 tests (7/7 in `sprint-status-parser.test.js`).

## Close-out (when BOTH decks GREEN)
Orchestrator-executed: kitchen-level memory harvest from both STATUS.md; version/CHANGELOG/BACKLOG; gitleaks (new packages — org literals ONLY in the external denylist, never in-repo); commit/publish/push/tag per `docs/RELEASE.md`; **Brad MD email → `bheath.tbhcoach@gmail.com`, BCC `admin@nashvillechopin.org`**; session-end self-email.

> **Note (2026-06-08):** v1.8.0 already shipped both decks' code to npm + pushed `main` (commit `58c1dce`). The close-out list above largely executed at that ship; declaring Deck A GREEN now is an **acceptance/documentation event, not a new release** — no version bump or republish required.

## Deck A live-acceptance — 2026-06-08 (Mon ~16:45 ET)

**Verdict: GREEN on bridge correctness + transport + memory tools. One narrow consumer-UI residual deferred to the next double-panel sprint.**

- **Live round-trip proven.** Josh ran the claude.ai Custom Connector smoke test; `memory_recall` returned **24 memories**. That single call exercises the entire external chain — Dynamic Client Registration, OAuth 2.1 + PKCE consent (operator secret), the Anthropic MCP tunnel, Streamable-HTTP `/mcp` dispatch, the Mnestra webhook client, egress redaction, and the structured result rendered back in claude.ai.
- **Bridge side fully tested — 86/86** (`node --test test/*.test.js`): SDK-client list+call round-trip (egress-redacted); the six read-only tools; OAuth `401` gate w/ `WWW-Authenticate` PRM; approval `_meta` hint emission for the four terminal tools; the **real** default-deny allowlist (`integration.test.js` drives `list_panels` against the real `policy`); no existence-leak on non-allowlisted ids; tidy `isError` on client failure.
- **`list_panels` rides the identical `/mcp` dispatch path as `memory_recall`** (`panels.js` → `clients.termdeck.listSessions()` → `policy.visiblePanels()` → metadata-only summary). Since `memory_recall` traversed the live chain successfully, the transport for `list_panels` is proven; its only unique surface (reading `/api/sessions` + the allowlist) is covered by integration tests.
- **Residual (deferred, low-risk).** The four terminal-state tools are approval-gated, so a live claude.ai call additionally exercises claude.ai's *consumer-side* approval-prompt UI + live-panel metadata render — which `memory_recall` (ungated) did not. That needs open panels + the connector live. Josh closed the panels awaiting the next double-panel sprint, so this single live smoke folds into that sprint's opening checklist. Server-side approval emission + allowlist gating are already proven by tests.
