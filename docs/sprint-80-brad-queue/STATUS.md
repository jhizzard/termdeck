# Sprint 80 — STATUS

<!-- Canonical post shape (ALL lanes, including workers — the `### ` prefix is mandatory):
### [T1] FIX-LANDED 2026-07-02 10:14 ET — \xNN normalization + 6 tests green
### [T4-CODEX] CHECKPOINT 2026-07-02 10:30 ET — phase 2/4; verified BR-1 repro (index.js:661); pending FR-6 guard; last seen T2 FIX-LANDED 10:21
Verbs: FINDING | FIX-PROPOSED | FIX-LANDED | AUDIT-PASS | AUDIT-FAIL | CHECKPOINT | HANDOFF-REQUEST | HANDOFF-ACK | DONE | FINAL-VERDICT
Idle-poll (tolerant): ^(### )?\[T<n>\] DONE\b
-->

### [ORCH] SPRINT-AUTHORED 2026-07-01 18:52 ET — dispatch-ready; not yet injected (no panels open; Josh away). Rumen hotfix pre-work already landed on main: rumen ed5e233 (v0.6.1 guards) + termdeck 564e788 (wrapper watchdog); rumen 0.6.1 npm publish pending Josh Passkey — rides this sprint's wave.

### [T4-CODEX] CHECKPOINT 2026-07-01 20:51 ET — phase 1 boot/baseline; TAP-equivalent test baseline green 852 tests/847 pass/0 fail/5 skip; exact npm test hung twice after bridge section; verified pre-sprint parser anchors; pending BR-1 live repro, hazards, FR-5/FR-6; last FIX-LANDED none
Evidence: `env WEB_CHAT_DRIVER_NO_BROWSER=1 node --test --test-reporter=tap packages/server/tests/**/*.test.js packages/cli/tests/**/*.test.js packages/stack-installer/tests/**/*.test.js packages/mcp-bridge/test/*.test.js packages/web-chat-driver/tests/*.test.js` exited 0 with `# tests 852`, `# pass 847`, `# fail 0`, `# skipped 5`. Exact `npm test` was attempted twice and interrupted after no new output past the same bridge-test region. Current pre-sprint anchors: global `express.json()` before route middleware at `packages/server/src/index.js:621`, parse-error handler at `packages/server/src/index.js:668`, `/input` route at `packages/server/src/index.js:2384`, submit:true path at `packages/server/src/index.js:2498` + `packages/server/src/pty-submit.js:34`, and no `contextK` in `PATCHABLE_META_FIELDS` at `packages/server/src/session.js:658`.

### [ORCH] DISPATCHED 2026-07-01 20:52 ET — Sprint 80 injected on :3001. Josh's 4 manually-opened panels were at the wrong cwd (another project's dir — server default); ORCH spawned 4 fresh panels at the termdeck cwd via POST /api/sessions and injected those. Lane map: T1=2cfb1e2f (input-api), T2=ca541f4e (context-telemetry), T3=3fedbcbe (ux-roles-release), T4=7c193da4 (codex auditor). T1/T2/T4 engaged on inject; T3 parked post-submit, recovered via cr-flood poke 20:49 ET, now working. rumen 0.6.1 published + live on npm; Brad notified via WhatsApp (flip pin + redeploy). Monitor stack armed (STATUS wake-verbs / parked-lane / strand-heal, python3).

### [T4-CODEX] FINDING 2026-07-01 20:54 ET — BR-1 independently reproduced with Brad's literal-backslash-x curl shape on temp audit server
Evidence: disposable `cat -v` panel `8d185e83-d2a7-4684-84ca-a3d26c84b617` on `127.0.0.1:31080`; `curl -d '{"text":"\x1b[200~boot\x1b[201~"}'` returned HTTP 400 with `{"error":"Malformed JSON body","detail":"Bad escaped character in JSON at position 10..."}`. Server log emitted `[body-parser] entity.parse.failed ... prefix="{"text":"\\x1b[200~boot\\x1b[201~"…"` from `packages/server/src/index.js:668`. Bounded controls: valid JSON `\u001b` body returned 200; `{submit:true}` returned 200/`submitted:true` via `packages/server/src/index.js:2498`; valid literal text JSON `{"text":"\\x1b","submit":true}` returned 200/5 bytes, proving escaped-backslash text is currently not corrupted.

### [ORCH] INCIDENT 2026-07-01 21:02 ET — :3001 deck crashed ~20:55 ET mid-sprint. Listener gone (connection refused), all four lane PTYs died; node process 1930 (up since Jun 24) zombied with no listen socket. Timing correlation: crash occurred the instant ORCH used the v1.10.1 POST /input {submit:true} path to re-engage T3 (response came back empty = connection dropped mid-request); all prior plain two-stage POSTs were fine. TREAT AS FINDING FOR T1/T4: pty-submit.js {submit:true} path is a suspected server-crash trigger — reproduce under test before trusting it; do NOT use submit:true for lane relays until cleared. Also: T3's first turn had died on a transient Anthropic API 529 before this. All lane transcripts intact on disk (T1=3e4a6447 ~238KB, T2=ed371c00 ~81KB, T3=bde981ec ~49KB). Recovery: Josh kills 1930 + restarts deck; ORCH respawns lanes via claude --resume.

### [T4-CODEX] CHECKPOINT 2026-07-01 21:11 ET — phase 1 reboot/baseline; exact root npm test run reproduced the bridge-region hang; TAP-equivalent completed 858 tests/850 pass/3 fail/5 skip; pending scratch-port crash repro; last FIX-LANDED none
Evidence: boot docs read (`PLANNING.md`, `STATUS.md`, `T4-codex-auditor.md`, T1/T2/T3 briefs, `CLAUDE.md`, `CRITICAL-READ-FIRST-2026-05-07.md`, `ARCHITECTURE.md`). Exact `npm test` was started at 21:06 ET and interrupted after no output past the mcp-bridge region. Full TAP-equivalent command (`env WEB_CHAT_DRIVER_NO_BROWSER=1 node --test --test-reporter=tap packages/server/tests/**/*.test.js packages/cli/tests/**/*.test.js packages/stack-installer/tests/**/*.test.js packages/mcp-bridge/test/*.test.js packages/web-chat-driver/tests/*.test.js`) exited 1 with `# tests 858`, `# pass 850`, `# fail 3`, `# skipped 5`. A repeated full-suite run redirected to `/tmp/sprint80-t4-baseline.tap` hung again and showed sandbox/listener partial failures (`listen EPERM: operation not permitted 127.0.0.1`); isolated `packages/mcp-bridge/test/*.test.js` passed 165/165, so baseline failures are full-suite/interference-shaped, not accepted as Sprint-owned yet.

### [ORCH] RECOVERED 2026-07-01 21:10 ET — fleet respawned on :3002 (Josh restarted the deck; crash confirmed as server-side Uncaught Exception). T1r=48c5558b / T2r=423a2604 / T3r=89771f3c resumed via claude --resume with full context; T4r=f44ddc99 fresh Codex boot. Recovery inject 8/8, all four verified working (T1-T3 Using tools, T4 reasoning). T1 scope now includes the {submit:true} crash repro+fix; T4 audit target #0 = the crash callpath. submit:true BANNED for orchestration relays until T1/T4 clear it. Monitor v2 armed (adds server-down detection).

### [T3] BOOTED 2026-07-01 21:12 ET — respawned on :3002 via claude --resume post-crash; context intact, first turn (had 529'd, no work) now executing. Read PLANNING + STATUS (incl. ORCH INCIDENT 21:02 + RECOVERED 21:10 — noted: no {submit:true} relays in my lane; I don't relay anyway). Lane plan: FR-2 (role tier) → FR-3 (verify cap, post FINDING first) → FR-1 (transcript toggle) → release staging drafts. Anchors re-verified: ALLOWED_SESSION_ROLES at index.js:168 (`['orchestrator','worker','reviewer','auditor',null]`); role→gold is a BINARY `isOrchestratorRole()` in app.js (1174) driving `panel--role-orch` (gold border + ORCH-pin-row + "ORCH " badge, style.css:430). FR-2 is a two-TIER split, not a new class: master-orchestrator=gold, orchestrator=silver. Design + FINDING on the toggle path coming next.

### [T2] FINDING 2026-07-01 21:14 ET — recovered on :3002 via --resume (T2r=423a2604); anchors re-verified, INCIDENT factored, design locked
Recovery: clean working tree (`git diff --stat` empty) — no lane had committed source edits pre-crash, nothing lost; only untracked source file is T1's `input-xnn-normalization.test.js`. Re-verified anchors on current disk: encoded-cwd resolver to REUSE = `agent-adapters/claude.js:122 resolveTranscriptPath` (scans `~/.claude/projects/<cwd-slash→dash>/` for newest `*.jsonl` ≥ createdAt). Usage formula confirmed on a live transcript: `input_tokens+cache_read_input_tokens+cache_creation_input_tokens` (sampled 534+307919+872=309325≈309K — exactly the crash regime). `toJSON()` (session.js:549) spreads `{...meta}`, so **`meta.contextK` rides the existing 2s `status_broadcast` (index.js:3513) to the client with ZERO new WS plumbing.** Periodic-capture anchors: register at index.js:1965, clear at 1565 & 2081 — I co-locate the fs.watch + cleanup at these exact sites.
INCIDENT impact on MY lane: FR-6 `inject` would call `submitToPty` = the exact suspect crash path. Since **FR-6 default = `notify` (locked §3.3)**, the suspect path is OFF the default; `inject` stays opt-in, gated behind config, and I flag the shared-suspect-path risk for T1/T4 (who own the crash fix per ORCH 21:10). ALL my tests use a stubbed PTY (no live server, no submit:true). Design: new pure module `context-meter.js` (computeContextK tail-read + classifyContext + evaluateEnforcement hysteresis/kill-grace) → compute + threshold + kill-guard 100% unit-testable off-server.

### [T2] HANDOFF-REQUEST 2026-07-01 21:14 ET — session.js PATCHABLE_META_FIELDS (NOT an input-buffer field)
@T1: FR-5 external-writer parity (Brad's ask, PLANNING §3.4) needs `'contextK'` added to `SessionManager.PATCHABLE_META_FIELDS` (session.js:658, the Set that currently ends at the `'role'` entry). One-line Set addition; does NOT touch `_inputBuffer`/input-queue fields you own for FR-4. I'll also add `'maxContextK'` + `'contextAction'` for per-panel FR-6 overrides. Doing all non-shared work first (context-meter.js, index.js wiring, config.js, client, tests); landing the session.js Set edit LAST. Flag now if your FR-4 work rewrites that Set or the `updateMeta` loop so we avoid a collision.

### [ORCH] HANDOFF-ACK 2026-07-01 21:16 ET — T2's session.js:658 PATCHABLE_META_FIELDS addition is pre-authorized by PLANNING §3.4 (contextK; + maxContextK/contextAction for FR-6 overrides). Adjudication for BOTH lanes: that Set is now a shared surface — additive single-line entries ONLY, no wholesale rewrite of the Set or the updateMeta loop by either lane. T1: if FR-4's holdInjectsWhileTyping becomes a per-panel meta flag, add it as your own single line; whoever lands second rebases around the other's line. T2's plan (shared edit last) approved.

### [T3] FINDING 2026-07-01 21:21 ET — FR-2 is a two-TIER split of the existing binary orch-role, not a bolt-on class; toggle routes to master (gold preserved)
The brief's suggested class names (`role-master-orchestrator`/`role-orchestrator`) diverge from the shipped convention. Ground truth: role→gold today is a BINARY `isOrchestratorRole(role)==='orchestrator'` (app.js:1174) driving THREE things off ONE class `panel--role-orch` — (a) gold border+shadow (style.css:430), (b) "ORCH " badge via `.panel-type::before` (style.css:439), (c) ORCH-pin-row membership (placePanel:1353 / reconcileOrchRow:1372). Server validation is a single `ALLOWED_SESSION_ROLES.includes()` shared by POST (index.js:2293) + PATCH (index.js:2344), so the whitelist edit at index.js:168 auto-covers both routes; only the exact-array test (session-lifecycle-api.test.js:294) needs updating. DESIGN (locked, minimal-churn): `isOrchestratorRole()` becomes the PIN-FAMILY predicate (true for BOTH tiers → both pin, both survive filter-reveal/reconcile) + new pure helper `isMasterOrchestratorRole()` for the gold-vs-silver split. CSS: `.panel--role-orch` recolors to SILVER (new `--tg-accent-orch-secondary`, the plain-orchestrator default) and `.panel--role-orch.panel--role-master` overrides to GOLD (`--tg-accent-orch`). Toggle decision (the one non-obvious call): `nextRoleForToggle(null)` now yields `'master-orchestrator'` (was `'orchestrator'`) — the solo-operator one-click "mark orch" affordance KEEPS its gold border (zero visual regression; it just becomes master tier), while plain silver `orchestrator` is the fleet path Brad drives via `PATCH {role:'orchestrator'}`. Satisfies acceptance verbatim: PATCH master→gold, existing/API orchestrator→silver, invalid still 400.

### [T3] HANDOFF-REQUEST 2026-07-01 21:21 ET — FR-2 role-render regions in app.js + style.css (role logic, NOT the token-counter header markup)
@T2: my FR-2 edits touch app.js at role-logic sites only — `isOrchestratorRole`/`nextRoleForToggle`/`orchToggleLabel` (~1174-1191, + new `isMasterOrchestratorRole`), `placePanel` (~1353), `reconcileOrchRow` (~1372), `syncOrchToggle` tooltip (~2689) — plus style.css role block (~412-444) + one accent var (~15). I do NOT add/edit any element in the `createTerminalPanel` header MARKUP template (the `overview-controls`/panel-header DOM your FR-5 `NNK ctx` counter lands in). My border/badge changes are pure-CSS on `.panel--role-orch`; my JS is class add/remove in reconcile. Collision risk low, but flag NOW if your ctx-counter work rewrites `reconcileOrchRow`/`refreshDashboardChrome`/`updatePanelMeta` or adds markup inside `.panel-type`. Landing FR-2 first (smallest), then FR-1 (transcript view, separate region) — staying out of your header slot.

### [ORCH] HANDOFF-ACK 2026-07-01 21:23 ET — T3's FR-2 scope (role-logic sites + style.css role block, zero header markup) approved. Boundary rule for the app.js seam, BOTH lanes: T3 owns role-class logic (isOrchestratorRole family, placePanel/reconcileOrchRow class add/remove) + CSS role block. T2's FR-5 counter must be a NEW dedicated DOM node rendered by a NEW function (e.g. updateContextBadge) — do NOT restructure reconcileOrchRow/refreshDashboardChrome; if updatePanelMeta needs wiring, additive call-out lines only. Land order: T3 FR-2 first (smallest), T2 header counter second, second-lander rebases. Both post FIX-LANDED naming exact functions touched.

### [T1] HANDOFF-ACK 2026-07-01 21:23 ET — read T2 HANDOFF-REQUEST 21:14 + ORCH adjudication 21:16
@T2: acked. My FR-4 work does NOT rewrite the `PATCHABLE_META_FIELDS` Set or the `updateMeta` loop (session.js:658/674). FR-4 input-queue state lives on internal `session._injectQueue` / `session._lastHumanKeystrokeAt` (non-PATCH). IF FR-4's per-panel opt-out (`holdInjectsWhileTyping`) becomes PATCH-mutable I add it as ONE additive line per ORCH 21:16 and rebase around your contextK/maxContextK/contextAction lines if you land first. No collision on `_inputBuffer`.

### [T1] FINDING 2026-07-01 21:23 ET — BR-1 reproduced RED + INCIDENT crash root-caused (node-pty unhandled 'error' → uncaught throw). Both P0; crash fix is one line.
BR-1 RED: new `packages/server/tests/input-xnn-normalization.test.js` vs real `createServer` + fake pty, RAW-wire POST of Brad's `{"text":"\x1b[200~boot\x1b[201~"}` (literal backslash-x) → HTTP 400 (`entity.parse.failed`), inject silently lost. Confirms Brad's log + T4's 20:54 repro at the express-5/body-parser-2.x layer.
CRASH root cause (INCIDENT 21:02): NOT logic in `pty-submit.js` — that helper is fully try/caught and cannot throw. Crash is in node-pty: `@homebridge/node-pty-prebuilt-multiarch/lib/unixTerminal.js:114-140` — the pty master-socket `'error'` handler swallows EAGAIN+EIO but for ANY OTHER errno (EBADF/ENOTTY/EPIPE — what a write to a pty whose child just died produces) does `if (this.listeners('error').length < 2) throw err;` **inside an async socket callback → uncaughtException → whole-process death.** Real node-pty's baseline `'error'`-listener count is **1** (empirically verified right after spawn), so `1 < 2` ⇒ it throws. TermDeck's spawn wiring (index.js:1993/2029) attaches only `onData`/`onExit`, never `.on('error')`. `submit:true` didn't CAUSE the throw — it WIDENED the race: the 400ms server-held settle (`pty-submit.js:35`) keeps the request in flight while the body-write's async socket error surfaces on a dying panel (T3 had just 529'd). Root fix (protects EVERY write path, not just submit:true): attach ONE `term.on('error', …)` in `spawnTerminalSession` → count 2 → node-pty delivers instead of throwing. FIX-LANDED incoming with a crash-guard regression test (EventEmitter fake pty mirroring the real `<2`-throw). Recommend submit:true stays BANNED for relays only until my FIX-LANDED + T4 AUDIT-PASS.

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:18 ET — target #0 crash callpath reproduced on throwaway v1.10.1 server and T1 listener fix verified on WIP; BR-1 normalization still RED
Evidence: isolated v1.10.1 tag `fca6dba` unpacked via `git archive` to `/tmp/t4-termdeck-fca6dba-src`, scratch HOME `/tmp/t4-s80-fca-home`, never live `:3002`. Repro command shape: `env HOME=/tmp/t4-s80-fca-home DATABASE_URL= TERMDECK_SESSION_LOGS=0 node -e '<createServer({port:31982}); POST /api/sessions; sessions.get(id).pty._socket.emit("error", Object.assign(new Error("synthetic EBADF via throwaway server"), {code:"EBADF"}))>'` against throwaway `127.0.0.1:31982`. Result: POST-created panel `6fa9261c...`, real node-pty `listeners("error").length === 1`, synthetic EBADF produced `UNCAUGHT_STACK` and process exit 42. Native crash rerun on `:31983` exited 1 immediately after `term error listeners before synthetic 1`.
Callpath: v1.10.1 TermDeck spawn attaches `term.onData`/`term.onExit` only (`fca6dba:packages/server/src/index.js` around 2002/2038), no `term.on("error")`. node-pty master socket handler at `node_modules/@homebridge/node-pty-prebuilt-multiarch/lib/unixTerminal.js:114-140` closes/emits close, suppresses EAGAIN/EIO, then `if (_this.listeners("error").length < 2) throw err;` at `unixTerminal.js:139`. Standalone direct node-pty probe reproduced the exact throw with baseline count 1; adding one TermDeck-style listener raised count to 2 and logged `alive after synthetic error true`.
Fix verification: current WIP has T1's listener at `packages/server/src/index.js:2252`. Same throwaway-server probe on `127.0.0.1:31986` created panel `fbc8b975...`, printed `term error listeners before synthetic 2`, logged `[pty] non-fatal socket error (..., EBADF): synthetic EBADF via throwaway server`, then `GET /api/sessions` returned 200 with `uncaught false`. Focused tests run: `node --test packages/server/tests/input-xnn-normalization.test.js packages/server/tests/input-submit-option.test.js packages/server/tests/crash-class-wiring.test.js packages/server/tests/crash-class-helpers.test.js` → 40 tests / 37 pass / 3 fail. The 3 failures are all BR-1 normalization/hint tests in `input-xnn-normalization.test.js` (literal `\x1b` still 400; uppercase/boundary `\xNN` still 400; missing extended hint), not the crash listener path. Last FIX-LANDED seen: none yet.

### [T3] FIX-LANDED 2026-07-01 21:34 ET — FR-2 master-orchestrator tier: gold master / silver orchestrator, both pin. Tests 55/55 client-contract + 20/20 role route = green
Server: `master-orchestrator` added to `ALLOWED_SESSION_ROLES` (index.js:168), auto-covering POST(:2293)+PATCH(:2344) `.includes()` validation. Client: `isOrchestratorRole()` is now the pin-family predicate (both tiers pin); new pure `isMasterOrchestratorRole()` drives the gold-vs-silver split; `placePanel`+`reconcileOrchRow` toggle a `panel--role-master` modifier (incl. the in-place `orchestrator⇄master` re-skin that stays in the pin row). CSS: `.panel--role-orch` = SILVER default (`--tg-accent-orch-secondary #9aa0a6`, "ORCH " badge); `.panel--role-orch.panel--role-master` = GOLD override (`--tg-accent-orch`, "ORCH★ " badge). Toggle promotes to master (preserves the pre-sprint one-click gold). Evidence: `node --test dashboard-panels-client.test.js session-lifecycle-api.test.js` → 75 pass / 0 fail; added tests cover both predicates, toggle target, silver+gold CSS tiers, master-modifier wiring, and the PATCH `{role:"master-orchestrator"}`→200 acceptance path. Files: packages/server/src/index.js, packages/client/public/app.js, packages/client/public/style.css, packages/server/tests/{dashboard-panels-client,session-lifecycle-api}.test.js. NO edits to the createTerminalPanel header markup (stayed out of T2's ctx-counter slot). Next: FR-3 (cap FINDING first).

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:19 ET — T3 FR-2 role tier verified; server/client/CSS/tests green; no blocking hardcoded-role miss found
Evidence: `node --test packages/server/tests/dashboard-panels-client.test.js packages/server/tests/session-lifecycle-api.test.js` passed `76/76`. Server whitelist is `['master-orchestrator','orchestrator','worker','reviewer','auditor',null]` at `packages/server/src/index.js:177`, covering both POST and PATCH validation via the existing `ALLOWED_SESSION_ROLES.includes()` checks. Client helpers at `packages/client/public/app.js:1179-1205` make `isOrchestratorRole()` a pin-family predicate for both tiers, `isMasterOrchestratorRole()` master-only, and the toggle target `master-orchestrator`. Reconcile wiring at `app.js:1372-1416` keeps both tiers pinned and reskins `orchestrator⇄master-orchestrator` in place. CSS at `style.css:438-463` makes `.panel--role-orch` silver and `.panel--role-orch.panel--role-master` gold. Grep sweep for `orchestrator`/`master-orchestrator` across client/server/tests found no remaining behavioral client hardcoded list that excludes the master tier; only stale wording remains in old test comments. Pending: T3 FR-3/FR-1, T2 FR-5/FR-6, T1 BR-1 normalization/FR-4.

### [T4-CODEX] CHECKPOINT 2026-07-01 21:20 ET — phase 2 partial audit; verified T2 server-side context core/default safety; pending T2 client badge + FIX-LANDED, T1 BR-1/FR-4, T3 FR-3/FR-1
Evidence: `node --test packages/server/tests/context-meter.test.js` passed 23/23. Real JSONL cross-check on `ed371c00-60a4-4d2f-850c-32bd7b45e699.jsonl`: manual `input_tokens + cache_read_input_tokens + cache_creation_input_tokens = 206753` → `207K`, exactly matching `computeContextK()` output. Exported wiring probes: default config resolves `contextAction:'notify'` with `maxContextK` disabled; `notify` set `contextAlert`/webhook only and did not call submit/kill; `inject` called the submit hook once; `kill` while `meta.status:'thinking'` deferred without kill. PATCH fallback precedence probe: non-Claude `contextK=77` stayed unchanged; Claude with readable transcript overwrote patched `contextK=5` to computed `120`. Current diff still has no `contextK`/ctx badge render in `packages/client/public/app.js`, so FR-5 is not audit-passable yet. Last FIX-LANDED seen: T3 FR-2 21:34.

### [T1] FIX-LANDED 2026-07-01 21:19 ET — INCIDENT crash fixed: pty 'error' listener in spawnTerminalSession. 3/3 crash-guard tests green; T4 independently verified.
Fix: one guarded `term.on('error', …)` attached right after `session.pty = term` in `spawnTerminalSession` (packages/server/src/index.js, ~:2201). Raises the terminal's `'error'`-listener count from node-pty's baseline 1 to 2, so node-pty's `unixTerminal.js:139` `if (listeners('error').length < 2) throw err` no longer fires on a non-EAGAIN/EIO write error (EBADF/ENOTTY/EPIPE from a write to a pty whose child just died). Protects EVERY write path (plain, {submit:true}, WS, sprint relays), not just submit:true. `typeof term.on` guard keeps the plain-object fake ptys in existing harnesses untouched.
Tests: NEW `packages/server/tests/input-submit-crash-guard.test.js` (EventEmitter fake pty mirroring node-pty's baseline-1 + `<2`-throw rule verbatim) — 3/3 green: (1) spawn attaches listener → count ≥2; (2) {submit:true} to a dying panel (fd dead, meta still 'active') → no uncaughtException + clean HTTP response; (3) mechanism: baseline-1 `_deadWriteWouldThrow()===true`, +1 listener flips it to false. Repro banked: an earlier revision literally threw `Error: write EBADF` from the fake's async path, and T4-CODEX independently reproduced the native crash (exit 42) on a throwaway `:31982` + verified my WIP listener (count 2, `uncaught false`) on `:31986`.
@ORCH/@T4: submit:true crash callpath is CLEARED for the ban re-eval — the root vuln was node-pty's unhandled 'error', now guarded at the source. Still recommend routine two-stage POSTs for relays until final npm test green. Continuing to BR-1 \xNN normalization next (the 3 remaining reds T4 saw are that, not the crash path).

### [ORCH] NOTE 2026-07-01 21:38 ET — INCIDENT root cause CONFIRMED by T1 (FINDING 21:23) + T4 (AUDIT-PASS 21:18, independent repro on throwaway v1.10.1): unhandled node-pty master-socket 'error' — TermDeck attached onData/onExit but no term.on('error'), and node-pty unixTerminal.js:139 THROWS when error-listener count < 2 → Uncaught Exception → deck death. The {submit:true} correlation was the trigger vector (write into a dying PTY), not the defect; supersedes the 21:02 suspicion. submit:true relay ban STAYS until T1 posts FIX-LANDED for the listener + BR-1 tests green and T4 re-verifies on the final tree. T3 FR-2 FIX-LANDED noted — proceeding to FR-3.

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:21 ET — T1 INCIDENT crash listener FIX-LANDED re-verified; submit:true crash vector closed, BR-1 normalization still separate/pending
Evidence: `node --test packages/server/tests/input-submit-crash-guard.test.js` passed 3/3. Test file faithfully models node-pty's baseline one `error` listener plus `<2` throw rule and drives the real `createServer` + real `/api/sessions/:id/input {submit:true}` route. Source fix remains at `packages/server/src/index.js:2252`: guarded `term.on("error", ...)` attached immediately after `session.pty = term`, raising real node-pty listener count to 2. This matches my independent throwaway-server verification on WIP (`127.0.0.1:31986`: listener count 2, non-fatal EBADF log, follow-up `GET /api/sessions` 200, `uncaught false`) and closes audit target #0. Remaining RED in T1 lane is BR-1 `\xNN` normalization/hint (focused run still had 3 failing `input-xnn-normalization.test.js` assertions), not the crash listener.

### [ORCH] NOTE 2026-07-01 21:48 ET — T1 crash-listener FIX-LANDED (21:19) + T4 re-verify (21:21) acknowledged: the crash class is closed IN THE TREE. Operational caveat: the LIVE :3002 deck process was started from pre-fix code — the submit:true relay ban remains for the RUNNING deck until it restarts on the fixed tree (natural point: sprint close-out). Remaining before FINAL-VERDICT: T1 BR-1 normalization (3 red tests), T2 FR-5/FR-6, T3 FR-3+FR-1+release staging, T4 remaining targets (hazard probe, FR-6 kill guard, FR-5 math).

### [T4-CODEX] FINDING 2026-07-01 21:24 ET — FR-3 cap route/tests are green, but the required README Panel cap section is missing
Evidence: `node --test packages/server/tests/session-lifecycle-api.test.js` passed `24/24`, including the new FR-3 cases: `effectivePanelCap()` normalizes unset/0/negative/non-numeric to unlimited, `maxPanels:2` returns structured 429 `{code:"panel_cap_reached",limit:2,current:2,hint:...}`, freeing a live panel re-opens a slot, and default/no cap permits repeated 201 spawns. Source read: `packages/server/src/config.js` defaults `maxPanels:null` and supports `TERMDECK_MAX_PANELS`; `packages/server/src/index.js` enforces the cap only on user-facing `POST /api/sessions` and counts live panels. Blocking doc gap: the 429 hint says `See the README "Panel cap" section`, and T3 brief/PLANNING require README per-OS PTY headroom notes, but `rg "Panel cap|panel cap|maxPanels|TERMDECK_MAX_PANELS|PTY headroom|4096" README.md docs/GETTING-STARTED.md docs/ARCHITECTURE.md` found no README panel-cap section. Also no T3 FR-3 FINDING/FIX-LANDED post is present yet.

### [T4-CODEX] AUDIT-FAIL 2026-07-01 21:26 ET — BR-1 byte-safety is green, but ARCHITECTURE.md lacks the required Input API hazard documentation
Evidence: focused tests now pass: `node --test packages/server/tests/input-xnn-normalization.test.js packages/server/tests/input-submit-option.test.js packages/server/tests/input-submit-crash-guard.test.js` → `16/16` pass. Independent raw-HTTP scratch probe on ephemeral `127.0.0.1:52999` with fake PTY verified: Brad literal `{"text":"\\x1b[200~boot\\x1b[201~"}` returns 200 and writes bytes `1b 5b 32 30 30 7e ... 1b 5b 32 30 31 7e`; escaped doc-quote intent `{"text":"use \\\\x1b[200~ docs"}` stays literal bytes `5c 78 31 62`; non-hex `\\xZZ` stays 400 with hint and zero writes; uppercase/boundary `\\X1B`, `\\x00`, `\\xff` writes `41 1b 42 00 43 ff 44`; `/api/sessions/:id/input/../../other` strict-400s with zero writes; empty JSON body and `text/plain` both return `Missing text` with zero writes; explicit Content-Length exact body `\\x41` writes `41`. Source guard is at `packages/server/src/index.js:899-989` and exact route regex is `^/api/sessions/[^/]+/input$`. Blocking doc gap: `rg "Input API|xNN|\\\\x1b|auto-normal|JSON escape|bracketed paste|/input" docs/ARCHITECTURE.md README.md docs/GETTING-STARTED.md` found no ARCHITECTURE.md Input API section, while the source comment says the accepted single-backslash hazard is documented there.

### [T1] FIX-LANDED 2026-07-01 21:24 ET — BR-1 \xNN normalization on /input + louder 400 (hint). 6/6 new + 111/111 existing green, zero regressions.
Fix: a route-scoped pre-parse middleware mounted BEFORE `express.json()` (packages/server/src/index.js, just above the Sprint-60 verify block ~:898). Scoped to `POST` + `^/api/sessions/[^/]+/input$` + `application/json` only. It consumes the raw stream, rewrites REAL `\xNN`/`\XNN` escapes to `\u00NN`, `JSON.parse`es, and sets `req.body`; `express.json()` then short-circuits via `onFinished.isFinished(req)` (verified vs body-parser 2.2.2 read.js:40 — stream complete + not readable) and never re-parses. Original bytes kept in `req.rawBody` for the hex prefix. Every other route stays strict.
Even/odd backslash guard (divergence from the naive brief, SHRINKS the hazard): only an ODD backslash run (`\x1b`) is normalized; a properly-escaped `\\x1b` (valid-JSON literal-text intent) is left untouched — so the accepted hazard reduces to a caller who writes SINGLE-backslash `\x1b` (already invalid JSON pre-fix). Documented next in docs/ARCHITECTURE.md.
Louder 400 (§3.2): parse-error 400 now carries a `hint` naming the fix (use the `` unicode escape; /input auto-normalizes `\xNN`). Applies to /input AND other routes.
Tests: `packages/server/tests/input-xnn-normalization.test.js` 6/6 — literal `\x1b[200~...\x1b[201~` to real ESC + 200 (RAW-wire repro of Brad's curl), proper `` byte-identical, uppercase `\X1B`/`\x00`/`\xff` edges, even-backslash `\\x1b` stays literal, non-/input strict-400, still-malformed to 400+hint. Regression sweep green: input-submit-option + body-parser-rawbody + body-parser-production-fence + input-guard + session-lifecycle-api + session = 111/111.
@T4: the 3 reds you saw at 21:19 are now green — BR-1 + hint ready for your independent repro (RAW-wire `-d` with literal-backslash `\x1b` bracketed paste should now 200 + land real ESC). FR-4 (inject-vs-typing queue) next in my lane.

### [T3] FINDING 2026-07-01 21:52 ET — FR-3: confirmed NO TermDeck-imposed panel cap exists anywhere; Brad's ~30-40 ceiling was host/PTY/RAM exhaustion, not a code limit
Verified across `packages/server/src` + `packages/cli/src` + `config.js`: `grep` for maxPanels/maxSession/panel-cap/"too many"/MAX_* returns nothing; `POST /api/sessions` (index.js:2649) validates `role` then calls `spawnTerminalSession` with ZERO count guard; no `sessions.size >= N` anywhere; config had no cap key. So FR-3 is NOT "raise a hidden cap" — it's "add the configurable guard that never existed," per PLANNING §3.6. Design (locked): `maxPanels` top-level config key, **default `null` = UNLIMITED** = exact pre-FR-3 behavior (honors §3.6 "do not invent a lower default than current behavior" literally, and mirrors FR-6's `maxContextK: null`-disabled sibling in the same config). The one debatable call — null vs a generous positive default — I resolved to `null` for zero-regression; if ORCH/T4 prefer a positive generous default (e.g. 64) it's a one-line change in config.js `defaultConfig()`. Counts LIVE panels only (`getAll({includeExited:false})`), so dead panels never block a spawn; internal respawn + sprint-runner bypass the cap (recovery must never be blocked).

### [T3] FIX-LANDED 2026-07-01 21:52 ET — FR-3 configurable panel cap + clear 429 + README per-OS PTY notes. Tests 24/24 green
`config.js`: `maxPanels: null` default + documented yaml stanza + explicit env-override resolution (`TERMDECK_MAX_PANELS` env > config.yaml > default). `index.js`: exported pure `effectivePanelCap(config)` (null/0/neg/NaN/non-numeric → unlimited; positive → floor), and a route guard in `POST /api/sessions` returning `429 {ok:false, code:'panel_cap_reached', limit, current, hint}` when live-panel count ≥ cap. `README.md`: new "Panel cap" section under Configuration — env/yaml usage, resolution order, live-only counting, respawn bypass, and per-OS PTY headroom (Linux `kernel.pty.max` 4096 / macOS `kern.tty.ptmx_max`+`ulimit -n` / process-fanout caveat). Tests: `effectivePanelCap` normalizer table; route 429 (cap=2 → 3rd spawn 429 w/ correct limit+current+hint; DELETE frees a slot → next spawn 201, proving live-count not cumulative); uncapped default → 6 spawns all 201. Evidence: `node --test session-lifecycle-api.test.js` 24 pass / 0 fail; `adapter-session-end-writer + sprint-65-acceptance` 19 pass (config.js edits safe); smoke: `loadConfig().maxPanels` null default → 7 under `TERMDECK_MAX_PANELS=7`. Files: config.js, index.js (+ export), README.md, session-lifecycle-api.test.js. Next: FR-1 transcript newest-first toggle.

### [T4-CODEX] AUDIT-FAIL 2026-07-01 21:27 ET — T2 FR-5 patch-only fallback is invisible in the header
Evidence: PLANNING/T2 brief requires `contextK` in `PATCHABLE_META_FIELDS` so Brad's external watchdog can write fallback context for non-Claude/no-JSONL panels, with server-computed Claude JSONL values winning when available. The tree allows PATCHing only `contextK`, `maxContextK`, `contextAction` (`packages/server/src/session.js:679-681`), but the client hides the badge unless both `meta.contextK` and `meta.contextLevel` are present (`packages/client/public/app.js:3950-3954`). `contextLevel` is not PATCHABLE and is only set by server JSONL compute (`packages/server/src/index.js:574-575`), so patch-only fallback never renders. Independent VM probe of `updateContextBadge`: `updateContextBadge("a",{contextK:77})` produced `{display:"none", textContent:""}`, while `{contextK:410, contextLevel:"over", contextAlert:{action:"notify",maxContextK:400}}` rendered `⛔ 410K ctx ⚡`. Focused tests are green (`context-meter + context-telemetry-wiring + dashboard-panels-client` → `91/91`), but there is no dashboard test for `panel-ctx` / `updateContextBadge` (`rg "panel-ctx|updateContextBadge|contextK" packages/server/tests/dashboard-panels-client.test.js` returned no matches), so this regression is unfenced. Separate doc gap remains: T2 brief also says document PATCH/server precedence in ARCHITECTURE.md, and current `rg` finds no context section there.

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:28 ET — T3 FR-3 panel cap verified after README fix
Evidence: T3's missing-doc gap from my 21:24 FINDING is closed: `README.md:291-321` now documents `maxPanels`, `TERMDECK_MAX_PANELS`, resolution order, live-only counting, internal respawn/sprint-runner bypass, structured 429, and Linux/macOS PTY headroom. Focused test evidence remains green from the current tree: `node --test packages/server/tests/session-lifecycle-api.test.js` → `24/24`, including `effectivePanelCap()` normalization, `maxPanels:2` 429 with `{code:"panel_cap_reached",limit:2,current:2}`, DELETE frees a slot, and uncapped default allows repeated spawns. Source matches acceptance: `packages/server/src/config.js` default `maxPanels:null`; `TERMDECK_MAX_PANELS` env override; `packages/server/src/index.js` enforces only on user-facing `POST /api/sessions` and counts live panels.

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:28 ET — T3 FR-1 transcript newest-first toggle verified in-tree
Evidence: `node --test packages/server/tests/dashboard-panels-client.test.js` → `60/60` pass. Source read: `packages/client/public/app.js:5771-5799` defines `TRANSCRIPT_ORDER_KEY`, `loadTranscriptNewestFirst()`, `saveTranscriptNewestFirst()`, and seeds `transcriptState.newestFirst` from localStorage with default `false` (oldest-first unchanged). `orderTranscriptContent()` at `app.js:6075-6085` leaves oldest-first unchanged, reverses line order when newest-first is on, and drops a single trailing blank before reversing. `renderTranscriptReplay()` at `app.js:6088-6115` renders the `#transcriptOrderBtn`, sets `aria-pressed`, persists toggles, and re-renders the same replay payload; copy uses the displayed order. CSS for `.transcript-order-toggle` and `[aria-pressed="true"]` is present at `packages/client/public/style.css:2623-2650`. No server surface touched. I have not yet seen a T3 FR-1 FIX-LANDED post, but the current tree meets the FR-1 acceptance.

### [T4-CODEX] FINDING 2026-07-01 21:29 ET — T1 FR-4 queue state exists but is not wired into input paths yet
Evidence: `packages/server/src/session.js:216-224` adds `_lastHumanKeystrokeAt` and `_injectQueue`; `session.js:407-435` adds `markHumanKeystroke()` / `_humanKeyFlushSignal()` for submit/clear detection. But the WebSocket human input path at `packages/server/src/index.js:3935-3941` still only does `session.pty.write(parsed.data); session.trackInput(parsed.data);` and never calls `markHumanKeystroke()`, so the "recent human keystroke" signal is never stamped from real browser typing. The `/api/sessions/:id/input` path at `index.js:2951-3046` writes immediately via `submitToPty()` or `session.pty.write(normalized)` and never checks `_inputBuffer`, `_lastHumanKeystrokeAt`, `_injectQueue`, role/default opt-in, queue TTL, or flushes FIFO on submit/clear. `rg --files packages/server/tests | rg "input|inject|queue|human"` shows no FR-4-specific regression test file yet. Acceptance `inputBufferLength > 0 + recent human keystrokes => N API injects queue and flush in order after submit/clear; zero interleaving` is therefore not implemented in the current tree. Treating this as in-progress; no T1 FR-4 FIX-LANDED post seen.

### [T3] FIX-LANDED 2026-07-01 22:02 ET — FR-1 transcript newest-first toggle (per-user, localStorage, default oldest-first). Tests 60/60 client-contract green
Scoped to the full-transcript **Replay** view (app.js:~6054 `renderTranscriptReplay`), where entries are actually rendered chronologically. Added: pure `orderTranscriptContent(content, newestFirst)` (oldest-first → unchanged; newest-first → split/drop-trailing-newline/reverse/rejoin — line-level, the pragmatic semantic for a line-oriented log); `transcriptState.newestFirst` seeded from `localStorage['termdeck.transcripts.newestFirst']` via hoisted load/save helpers (default false = unchanged); an `#transcriptOrderBtn` toggle in the replay-header actions cluster (aria-pressed, accent when active) that flips + persists + re-renders in place; copy-to-clipboard is WYSIWYG (matches displayed order). CSS: `.tr-replay-actions` flex cluster + `.transcript-order-toggle` + `[aria-pressed="true"]` accent state. Pure-client, NO server change (per brief). Evidence: `node --test dashboard-panels-client.test.js` 60 pass / 0 fail; new tests cover the reorder helper (unchanged/reversed/trailing-newline/empty/single-line), the toggle+localStorage wiring, and the CSS. `node --check` clean on app.js. Files: app.js, style.css, dashboard-panels-client.test.js. All three FRs (FR-1/FR-2/FR-3) now landed; running broad regression pass + starting release staging next.

### [T1] FIX-LANDED 2026-07-01 21:30 ET — (doc) ARCHITECTURE.md Input API section added; clears T4 AUDIT-FAIL 21:26.
@T4: `docs/ARCHITECTURE.md` now has `## Input API — POST /api/sessions/:id/input` (line 109) with a `### \xNN escape normalization (BR-1, Sprint 80)` subsection covering all four required points: (1) normalization scope — `POST` + `^/api/sessions/[^/]+/input$` + `application/json` only, all other routes strict; (2) the accepted hazard with one example each — `{"text":"\x1b"}` (odd/single backslash) → real ESC byte, `{"text":"\\x1b"}` (even/escaped backslash) → literal text preserved; (3) the 400 + `hint` error contract + the single-line hex-prefix log; (4) pointer to `packages/server/tests/input-xnn-normalization.test.js`. Also added a `### Server-crash safety — pty error listener` subsection documenting the INCIDENT fix + its regression test. `rg 'Input API' docs/ARCHITECTURE.md` → line 109. Ready for re-verify → AUDIT-PASS. (My source comment's "documented in docs/ARCHITECTURE.md § Input API" is now true.)

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:29 ET — BR-1 Input API docs now match byte-safety behavior
Evidence: re-read `docs/ARCHITECTURE.md:109-166`; it now documents `/input` body shape, exact normalization scope (`POST` + anchored `/api/sessions/:id/input` + `application/json`), the accepted single-backslash hazard, even-backslash literal preservation, loud 400 + `hint` contract, raw-body hex-prefix logging, `input-xnn-normalization.test.js`, and the pty `error` listener crash fix. Focused verification stayed green: `node --test packages/server/tests/input-xnn-normalization.test.js packages/server/tests/input-submit-option.test.js packages/server/tests/input-submit-crash-guard.test.js` → `16/16` pass. This clears my 21:26 BR-1 doc AUDIT-FAIL. Remaining open T4 blockers: T2 FR-5 patch-only fallback display/doc gap and T1 FR-4 queue wiring.

### [T4-CODEX] CHECKPOINT 2026-07-01 21:30 ET — phase 3 audit state: incident/BR-1/FR-1/FR-2/FR-3 green; blockers are T2 patch-only ctx fallback and T1 FR-4 queue wiring
Verified since reboot: target #0 crash callpath reproduced on throwaway v1.10.1 and listener fix re-verified; BR-1 byte normalization + hazard/bypass probes green; ARCHITECTURE.md Input API docs now match; T3 FR-1/FR-2/FR-3 audited green. FR-6 state machine/default safety focused tests green (`context-meter + context-telemetry-wiring`), but FR-5 is still blocked by my 21:27 finding: patch-only `contextK` fallback remains invisible because `updateContextBadge()` requires `contextLevel` and `contextLevel` is not patchable. T1 FR-4 remains blocked by my 21:29 finding: queue state/helper exists in `session.js`, but WS human input and `/api/sessions/:id/input` still do not call/consult/flush it. Root `npm test` not rerun yet because unresolved audit blockers remain and baseline exact `npm test` already reproduced the bridge-region hang.

### [T4-CODEX] CHECKPOINT 2026-07-01 21:32 ET — phase 4 re-audit; prior T2/T1 blockers cleared in current tree; root npm test + glob/release-staging checks pending
T2 re-check: `node --test packages/server/tests/context-badge-client.test.js packages/server/tests/context-meter.test.js packages/server/tests/context-telemetry-wiring.test.js packages/server/tests/dashboard-panels-client.test.js` passed `106/106`. The prior FR-5 blocker is fixed: `packages/client/public/app.js:3967-3979` renders any numeric `meta.contextK`, derives a client band with `classifyContextLevel()` when `contextLevel` is absent, and `packages/server/tests/context-badge-client.test.js` now covers PATCH-only `{contextK:77}`. `docs/ARCHITECTURE.md:169-208` now documents JSONL source, PATCH fallback, server-computed precedence, default `notify`, and kill grace/hysteresis. T1 FR-4 re-check: throwaway fake-PTY server on ephemeral `127.0.0.1:54497` + actual WS client typed `abc`, two POST `/api/sessions/:id/input` injects returned queued positions 1/2 with zero inject writes, then WS Enter flushed FIFO; observed writes exactly `["abc","\r","INJ1","INJ2"]`, queue length 0. Remaining before FINAL-VERDICT: root `npm test`, glob check, final release-staging/doc sweep, and any late worker DONE posts.

### [T2] FIX-LANDED 2026-07-01 21:35 ET — FR-5 + FR-6 complete; clears T4 AUDIT-FAIL 21:27 (patch-only fallback + fence + precedence doc). Full server suite 502/502, 0 fail
@T4: your three remediation points are all closed. Functions/files touched:
(1) **Patch-only fallback now renders + bands.** `updateContextBadge()` (app.js) no longer requires `contextLevel` — it renders whenever `contextK` is a finite number; `contextLevel` is used only when the server set it ("server-computed wins"), else a NEW pure client helper `classifyContextLevel(k, warnK, overK)` derives the band from thresholds. Thresholds are shipped to the client: `publicConfigPayload()` (index.js) now returns `contextThresholds:{warnK,overK}` (read-only projection of `config.context`). Your exact repro `updateContextBadge("a",{contextK:77})` now → `display:'', textContent:'77K ctx', class 'panel-ctx panel-ctx--ok'` (was `display:none`).
(2) **Fence added.** NEW `packages/server/tests/context-badge-client.test.js` (10 tests, brace-extract+vm like dashboard-panels-client) — covers `classifyContextLevel` bands + default fallback, server-set-level authoritative render, the PATCH-only-no-level case (77K→ok, 415K→over), default-threshold fallback, ⚡ enforcement marker, absent-node no-op, and the app.js/style.css id+class contract. Kept in its own file (NOT dashboard-panels-client.test.js) to respect the app.js land-order boundary — T3 owns that file this sprint.
(3) **Precedence doc.** `docs/ARCHITECTURE.md` new `## Context telemetry + enforcement (FR-5 + FR-6, Sprint 80)` section states the rule explicitly: server-computed value overwrites when JSONL readable; null-compute retains prior; PATCH `contextK` is fallback-only and superseded by the next real reading; `contextLevel` not PATCH-mutable → client bands PATCH-only values.
Full FR-5 landscape (reuse mandate honored): compute in NEW pure `packages/server/src/context-meter.js` (`computeContextK` tail-reads 256KB, sums `input_tokens+cache_read+cache_creation` off the last assistant `usage`; `classifyContext`; `evaluateEnforcement`). Watch/wiring in index.js: `establishContextWatch` (fs.watch on the transcript DIR, debounced, Claude-only; reuses the shared `claude` adapter `resolveTranscriptPath` — no second encoded-cwd resolver), `updatePanelContext` (resolve→compute→set `meta.contextK`/`meta.contextLevel`, rides the 2s status_broadcast via `toJSON()` spread), `teardownContextWatch` at both close sites (1875/2432-region, beside the periodic-capture clear). FR-6: `enforceContext`+`fireContextAction`+`isMidToolUse`+`_scheduleKillRecheck`; default `notify` (alert+webhook), `inject` via production `submitToPty` (opt-in — note the INCIDENT crash is a node-pty unhandled-`error`, fixed by T1's listener, NOT submitToPty logic), `kill`+respawn opt-in with the mid-tool-use grace guard. `session.js` PATCHABLE_META_FIELDS += `contextK`/`maxContextK`/`contextAction` (per ORCH 21:16). config.js `context{}` block (warnK 350/overK 400/maxContextK null/action notify/...).
Tests: context-meter 23 + context-telemetry-wiring 13 + context-badge-client 10 = **46 T2 tests**; whole `packages/server/tests/*.test.js` = **502 pass / 0 fail** (WEB_CHAT_DRIVER_NO_BROWSER=1, tap reporter). Manual-verify (real JSONL, no live server per recovery guidance): drove exported `updatePanelContext` with a fake Claude session at this repo's real cwd → resolved `bde981ec….jsonl`, computed **222K** (222159 tokens, level ok), matching a raw `computeContextK` cross-check. Files: context-meter.js (new), config.js, index.js, session.js, app.js, style.css, docs/ARCHITECTURE.md, 3 new test files.

### [T2] DONE 2026-07-01 21:35 ET — FR-5 + FR-6 shipped; 46 lane tests, server suite 502/502 green; awaiting T4 re-verify
FR-5 per-panel context counter (Claude JSONL, no CLI) + FR-6 `maxContextK` enforcement (default notify, kill grace-guarded) complete per PLANNING §4 acceptance. External-writer parity via PATCHABLE_META_FIELDS + client-side band derivation. No version bump / CHANGELOG / commit (orchestrator owns close-out). Shared-file etiquette honored: session.js Set additive-only (ORCH pre-auth 21:16), app.js ctx counter is a dedicated node + `updateContextBadge`/`classifyContextLevel` functions with a single additive `updatePanelMeta` call — zero edits to T3's role logic (isOrchestratorRole family / reconcileOrchRow / placePanel) or T1's input paths. Manual real-JSONL verify: 222K computed off this session's live transcript. Ready for T4 FR-5/FR-6 re-verify.

### [T3] DONE 2026-07-01 22:20 ET — FR-1 + FR-2 + FR-3 landed + release staging drafted. All T3 tests green (84/84 in my files; full server suite 499/0/0)
**FR-2** master-orchestrator tier (gold master / silver orchestrator, both pin; toggle→master preserves one-click gold; whitelist auto-covers POST+PATCH). **FR-1** transcript newest-first toggle (Replay view, localStorage-persisted, default oldest-first, pure-client, WYSIWYG copy). **FR-3** configurable `maxPanels` cap (default null=unlimited=zero-regression; env>yaml>default; live-only 429 `panel_cap_reached`; respawn/sprint-runner bypass; README per-OS PTY notes) — after posting the verified FINDING that NO hardcoded cap ever existed. **Release staging:** `docs/sprint-80-brad-queue/RELEASE-STAGING.md` — draft CHANGELOGs (termdeck 1.12.0 incl. d5436cf supervise fix; stack 1.10.0 = REAL code change 564e788 rumen-tick watchdog, not audit-trail-only), publish checklist per RELEASE.md STRICT, + Brad reply-email skeleton with all 3 of his 06-09 cutover answers sourced (two-instance=port isolation proven; flush-before-recall=NO staleness, Sprint 74 verdict; Gemini=Enterprise static-OAuth bridge client, Sprint 75). Two release-hygiene corrections flagged: (1) **rumen 0.6.1 is ALREADY published/live** (brief said "publishes this session" — it's done; wave is 2 Passkey publishes not 3); (2) stack-installer CHANGELOG.md is stale at 1.3.0 (published 1.9.0). Evidence: 84/84 (60 client-contract + 24 session-lifecycle), full server suite 499 pass/0 fail/0 skip (pty-fake available → FR-3 429 + FR-2 PATCH tests actually RAN), `node --check` clean on index.js/config.js/app.js. Visual items evidenced by DOM-assert/source-contract tests (browser screenshot skipped — Playwright shared-Chrome takeover is BANNED per standing P0). Files: packages/server/src/{index.js(+export),config.js}, packages/client/public/{app.js,style.css}, README.md, packages/server/tests/{dashboard-panels-client,session-lifecycle-api}.test.js, docs/sprint-80-brad-queue/RELEASE-STAGING.md. NO createTerminalPanel header-markup edits (stayed out of T2's ctx-counter slot); no version bumps / CHANGELOG edits / commits (ORCH close-out owns those). Idle now — will re-engage on ORCH request or a HANDOFF-ACK/collision flag from T2.

### [T1] FIX-LANDED 2026-07-01 21:37 ET — FR-4 inject-vs-typing queue. 10/10 new tests green.
Behavior: an API inject is HELD when the target panel's `_inputBuffer` is non-empty AND a real human keystroke landed within the window (default 4000ms, `TERMDECK_INJECT_HOLD_WINDOW_MS`). Held response is `200 {ok:true, queued:true, queuePosition, status, inputBufferLength}`. Flush FIFO on the human's next submit (Enter) or clear (Ctrl-C/Ctrl-U/bare-Esc) via `session.markHumanKeystroke` on the WS keystroke path, OR when a later inject arrives after typing stops (route drains backlog before its own write). TTL default 30000ms (`TERMDECK_INJECT_QUEUE_TTL_MS`) — a held inject older than that is DROPPED on flush (never fires into a changed context). `{submit:true}` held injects flush via the same server-sequenced body+lone-`\r`. Default ON for orchestrator-tier panels only (`orchestrator`/`master-orchestrator`); per-panel override `PATCH {holdInjectsWhileTyping:true|false}`.
Files: `session.js` (constructor `_lastHumanKeystrokeAt`/`_injectQueue`; `markHumanKeystroke`+`_humanKeyFlushSignal`; `holdInjectsWhileTyping` added to PATCHABLE_META_FIELDS as ONE additive line AFTER T2's contextAction — no rewrite of the Set/updateMeta, per ORCH 21:16). `index.js` (module-scope `resolveInjectHoldWindowMs`/`resolveInjectQueueTtlMs`/`isInjectHoldEnabled`/`shouldHoldInject`/`deliverQueuedInject`/`flushInjectQueue`, all exported for unit test; route enqueue+drain after the rate-limit block; WS human-input flush trigger). Only real human keystrokes stamp the typing clock — API injects never do (that's the whole point).
Tests: `packages/server/tests/input-inject-queue.test.js` 10/10 — hold+queue, FIFO drain-on-next-inject, human-Enter flush over a REAL WebSocket, TTL drop, worker-role default-OFF, opt-out flag, empty-buffer no-hold, stale-keystroke no-hold, submit:true flush, markHumanKeystroke classification (incl. arrow-key `\x1b[` is NOT a clear).
Doc: `docs/ARCHITECTURE.md` § Input API → new `### Inject-vs-human-typing queue (FR-4)` subsection.

### [T1] DONE 2026-07-01 21:37 ET — lane complete: BR-1 + INCIDENT crash + FR-4, all green, zero regressions.
Delivered (4 landings): (1) INCIDENT crash fix — pty `error` listener in spawnTerminalSession (index.js ~:2201) raising node-pty's `'error'` count to ≥2 so `unixTerminal.js:139` never re-throws; protects EVERY write path; T4 independently AUDIT-verified (exit-42 native repro + count-2 fix on throwaway servers). (2) BR-1 `\xNN`→`\u00NN` route-scoped normalization + louder 400 hint. (3) FR-4 inject-vs-typing queue. (4) `docs/ARCHITECTURE.md` § Input API (all 4 T4-required points + FR-4 + crash-safety).
Test evidence: NEW files input-xnn-normalization (6/6), input-submit-crash-guard (3/3), input-inject-queue (10/10). Regression: FULL server suite `node --test packages/server/tests/**/*.test.js` = 512 tests / 512 pass / 0 fail / 0 skip. No source-edits to another lane's owned surface; the only shared-file touch (PATCHABLE_META_FIELDS) is one additive line per ORCH 21:16.
@ORCH: root-glob `npm test` still hangs in the mcp-bridge region (T4's pre-existing baseline observation, NOT introduced by this lane — server surface is fully green). Recommend lifting the submit:true relay ban now that the root vuln (node-pty unhandled 'error') is closed + audited. No version bumps / CHANGELOG / commits per lane discipline — yours at close.

### [ORCH] NOTE 2026-07-01 21:40 ET — T1 DONE ack (512/512 full server suite). Rulings: (1) submit:true ban stays for the LIVE :3002 deck — the fix is in the tree, the running process predates it; lifts automatically when the deck restarts on the fixed tree at close-out. (2) root-glob npm-test hang in the mcp-bridge region = pre-existing, carried to close-out notes/BACKLOG, not a Sprint 80 blocker (server suite is the acceptance surface, 512/512). All three workers now DONE; T4 holds the queue: T2 FR-5/FR-6 re-verify → T1 FR-4 → completeness sweep → FINAL-VERDICT.

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:39 ET — T2 FR-5/FR-6 DONE re-verified; patch-only ctx fallback and enforcement safety are fenced
Evidence: `node --test packages/server/tests/context-badge-client.test.js packages/server/tests/context-meter.test.js packages/server/tests/context-telemetry-wiring.test.js packages/server/tests/dashboard-panels-client.test.js` → `106/106` pass. FR-5 remediation is live: `updateContextBadge()` renders any finite `contextK`; absent `contextLevel` bands via client `classifyContextLevel()` using shipped `contextThresholds`; `context-badge-client.test.js` fences `{contextK:77}` → visible `77K ctx` and `{contextK:415}` → over band. Server-computed precedence is documented and implemented: JSONL compute overwrites PATCH values; null/unreadable compute retains PATCH fallback; non-Claude panels stay PATCH-only. FR-6 focused evidence: `context-meter` covers default/unknown action → `notify`, one fire per breach episode, hysteresis reset below WARN, kill deferral while mid-tool-use up to max deferrals; `context-telemetry-wiring` verifies notify alert/webhook only, inject through stubbed submit, kill at rest, kill deferred when `status:'thinking'`. `docs/ARCHITECTURE.md` now has the context section with server-wins precedence, default notify, kill grace, and hysteresis. No open T2 audit fail remains.

### [T4-CODEX] AUDIT-PASS 2026-07-01 21:39 ET — T1 FR-4 queue wiring DONE re-verified; no interleave class found
Evidence: `node --test packages/server/tests/input-inject-queue.test.js packages/server/tests/input-xnn-normalization.test.js packages/server/tests/input-submit-option.test.js packages/server/tests/input-submit-crash-guard.test.js` → `26/26` pass. The new `input-inject-queue.test.js` covers hold+queue, FIFO drain before a later inject, real-WebSocket Enter flush, TTL stale drop, worker default-OFF, `holdInjectsWhileTyping:false` opt-out, empty-buffer/stale-keystroke no-hold, queued `{submit:true}` body+lone-CR flush, and `markHumanKeystroke` signal classification including arrow-key non-clear. Independent throwaway probe also passed on fake PTY + real WS: human typed `abc`, two HTTP injects queued with zero PTY writes, Enter flushed observed writes exactly `["abc","\r","INJ1","INJ2"]`. Source wiring now stamps real WS keystrokes, gates `/input` on `_inputBuffer` + recent human keystroke + role/override, drains FIFO, drops stale queued items, and documents FR-4 in `docs/ARCHITECTURE.md` Input API. No open T1 audit fail remains.

### [T4-CODEX] FINAL-VERDICT 2026-07-01 21:39 ET — GREEN under ORCH 21:40 acceptance boundary; root npm-test hang is pre-existing carry-forward
All Sprint 80 audit targets are PASS/refuted with evidence. Target #0 crash: reproduced on throwaway v1.10.1 `fca6dba` as node-pty `unixTerminal.js:139` uncaught throw with listener count 1; T1 fix adds `term.on("error")`, WIP/final tests verify listener count ≥2 and `{submit:true}` to a dying panel no longer crashes. BR-1/hazard/bypass: literal `\xNN` on `/input` normalizes to real bytes; even-backslash literal text is preserved; non-`/input`, malformed, empty, content-type, and path-bypass probes stay strict/loud; docs match. T2 FR-5/FR-6: context math, PATCH fallback, client badge, notify default, kill guard, and hysteresis are green. T1 FR-4: inject-vs-human-typing queue is green. T3 FR-1/FR-2/FR-3 were already audited green; release staging exists and ORCH reviewed it. Glob check: new Sprint 80 tests are under `packages/server/tests/` (`context-*`, `input-*`), not root. Verification: full server suite `env WEB_CHAT_DRIVER_NO_BROWSER=1 node --test packages/server/tests/*.test.js` → `512 tests / 512 pass / 0 fail / 0 skipped`; `node --check` clean for touched JS (`index.js`, `session.js`, `config.js`, `context-meter.js`, `app.js`). Exact root `npm test` still hangs/fails in the pre-existing MCP/bridge/root-glob region observed at baseline; per ORCH 21:40 this is carried to close-out/BACKLOG and is not a Sprint 80 blocker. Operational caveat remains: live `:3002` was started before the pty-error-listener fix; submit:true relay ban lifts only after deck restart on the fixed tree.
