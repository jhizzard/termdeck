---
title: "I built a terminal that remembers"
description: "Why TermDeck exists, what Flashback does, and what the three-tier stack looks like in practice."
---

_This post is in draft. Terminal 4 will fill in the narrative before launch._

## Outline

1. **Cold open** — a real "Tuesday" story where a failing command surfaced a memory from last month's work.
2. **Every session starts from zero** — the framing problem: LLM-assisted coding sessions don't remember what you already learned.
3. **Flashback in plain language** — what TermDeck does when a panel hits an error.
4. **Three real flashbacks** — concrete examples pulled from my own use, each with a screenshot.
5. **Architecture in 150 words** — TermDeck → Mnestra → Rumen, the three tiers and how they connect.
6. **Install in two lines** — `npx @jhizzard/termdeck` for tier 1, `termdeck init --mnestra` for tier 2.
7. **"I built this because I needed it"** — the honest pitch.

Screenshots will be pulled from `docs/screenshots/` once Terminal 1 captures them during the Sprint 3 GIF shoot.
