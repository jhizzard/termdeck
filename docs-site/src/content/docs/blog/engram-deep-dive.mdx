---
title: "Mnestra — persistent memory for Claude Code and Cursor"
description: "The memory MCP server behind TermDeck, explained on its own terms: six tools, hybrid search, three-layer progressive disclosure."
---

_This post is in draft. Terminal 4 will fill in the narrative before launch._

## Outline

1. **Why memory matters** for LLM-assisted coding — every new session starts cold.
2. **Six MCP tools** — `remember`, `recall`, `search`, `status`, plus the three-layer `index` / `timeline` / `get` progressive-disclosure tools, each with a concrete example.
3. **The 3-layer pattern** — why returning compact snippets first and expanding to full rows only on demand beats loading everything up front.
4. **Setup** — `npm install -g @jhizzard/mnestra` plus the MCP config JSON for Claude Code, Cursor, Windsurf, Cline, Continue.
5. **Hybrid search with recency decay** — how keyword + semantic + time-weighting combine to produce the ranked list.
6. **The TermDeck connection** — "and if you want a visual layer that watches your terminals and fires memories at you proactively..." linking back to the main TermDeck post.

Target publication: dev.to + Hashnode, ~3 days after the TermDeck launch.
