---
title: "Rumen — async learning for persistent memory"
description: "The Extract → Relate → Synthesize → Surface loop that turns a passive memory store into one that thinks while you sleep."
---

_This post is in draft. Terminal 4 will fill in the narrative before launch._

## Outline

1. **The problem** — memory stores are passive. They remember, but they don't learn.
2. **The Rumen loop** — Extract (pull signals from recent sessions) → Relate (hybrid search against prior memories) → Synthesize (Claude Haiku generates an insight with citations) → Surface (write back into Mnestra with confidence scores).
3. **Cost guardrails** — soft cap with placeholder fallback, hard cap that aborts the job cleanly, token logging per synthesize call.
4. **Deployment model** — Supabase Edge Function + `pg_cron` every 15 minutes. Why this shape beats a long-lived worker process.
5. **Rumen v0.3 preview** — question generation, the next step after synthesis.
6. **The full stack** — TermDeck captures, Mnestra remembers, Rumen learns. Link back to both earlier posts.

Target publication: dev.to + Hashnode, ~1 week after the TermDeck launch.
