# Changelog

All notable changes to TermDeck will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Planned (post-v1.0.0)
- **Sprint 51 — Cost-monitoring expandable dashboard panel.** Per-agent subscription-vs-per-token billing exposure. Reads each adapter's `costBand` field (declared on all 4 adapters since Sprint 45, populated since v1.0.0) and surfaces it in an expandable drawer: agent name + cost-band badge + session token count + running cost estimate (per-token agents) + "what's included" tooltip per agent. Optional non-blocking threshold toast when a per-token agent crosses a user-configurable spend threshold (default $5/session — Joshua to decide at scoping). Why: outside users running mixed 4+1 may not realize swapping a Claude lane for a Codex/Gemini/Grok lane shifts billing from "included in $X/mo subscription" to "$Y per million tokens" — long sprints with thinking + tool calls can rack up hundreds before the user notices. Same pattern (`costBand` + dashboard surface) extends naturally to rumen-tick (Edge Function billing) and mnestra (embedding cost per `memory_remember`). The v1.0.0 `source_agent` column is the natural foundation for "your last 30 days of memory spend, broken down by source LLM."
- **Grok 16-sub-agent observability.** Per the Sprint 47 Grok smoke-test self-report: Grok's `task` tool emits parseable stdout patterns (`"Delegating to [agent] sub-agent: ..."`, `"[sub-agent complete]"`). TermDeck's `session.js` analyzer can pick these up; UX could be a collapsible tree / side-metadata pane showing agent pills. Carried forward from Sprint 49 deferral.
- **Optional `~/.zshrc` source-line offer** in the stack-installer wizard (Sprint 48 T4 deferred). Adds `set -a; source ~/.termdeck/secrets.env; set +a` to `~/.zshrc` so standalone Claude Code launches outside TermDeck also inherit secrets.
- **Promote `packages/stack-installer/tests/**/*.test.js` into root `npm test` glob.** Currently runs separately. Sprint 48 T4 follow-up.
- ~~**`mnestra doctor` subcommand** (Brad's third upstream suggestion 2026-04-28). Cross-repo work; queued.~~ **Shipped in v1.0.1.**
- **Upstream rumen-package fix queued for next rumen release.** `~/Documents/Graciella/rumen/src/index.ts:177` — `createJob` INSERT omits `started_at` and the column has no default, leaving 1546+ rumen_jobs rows with NULL `started_at`. Two-line patch: add `started_at` to the INSERT VALUES tuple with `NOW()`. Sprint 45 shipped the surgical TermDeck-side `COALESCE(started_at, completed_at)` read-side fix; the upstream patch removes the workaround once it lands.
- **Sprint 46 deferrals still open after Sprint 50.5 close-out.** The Sprint 50.5 dogfood cleared four (T1 URL state codec edge case, T2 — replaced with `approvalModel` doc work — T3 TUI spinner spam, T4 dead `triggerProactiveMemoryQuery`); ten deferrals remain in `docs/sprint-46-dashboard-audit/AUDIT-FINDINGS.md` § Sprint 47 deferrals (most need design calls, not lane work).
- Sprint 40 carry-overs: harness session-end hook PROJECT_MAP forward-fix (out-of-repo `~/.claude/hooks/memory-session-end.js`); analyzer broadening for `PATTERNS.error` case-sensitivity gaps; LLM-classification pass on the ~898 chopin-nashville-tagged "other/uncertain" rows.

## [1.0.7] - 2026-05-04

> **v1.0.7 mini-sprint — Sprint 52: Edge Function pin drift detection.** New failure class **O** canonized: deployed-state drift between `npm`-published packages and Supabase-deployed Edge Functions. Codex T4-CODEX's 15:22 ET independent probe during Sprint 51.5b revealed petvetbid's deployed `rumen-tick` was pinned to `npm:@jhizzard/rumen@0.4.0` while the registry had `0.4.5` — `npm publish` doesn't redeploy Supabase functions, so any user who upgraded the npm package without re-running `init --rumen` is silently stale. v1.0.7 ships an audit-upgrade probe that detects this drift and points users at the one-line fix. Single-lane direct, ~1h wall-clock. Wave: `@jhizzard/termdeck@1.0.7` + `@jhizzard/termdeck-stack@0.6.7` (audit-trail bump). Mnestra and Rumen unchanged. **Final TermDeck publish for 2026-05-04**; project transitions to weekly-bump cadence + memory-quality innovation focus after this.

### Added

- **NEW audit-upgrade probe `probeKind: 'edgeFunctionPin'`** at `packages/server/src/setup/audit-upgrade.js`. Two PROBES entries (rumen-tick + graph-inference) lift the probe set from 10 → 12. Mechanism: download deployed Edge Function body via Supabase Management API (`GET /v1/projects/<ref>/functions/<slug>/body`), grep `npm:<pkg>@<version>` from the import line, compare against an EXPECTED version that comes from one of two resolvers — `npmRegistry` (run `npm view <pkg> version`, used for rumen-tick because its bundled source has a `__RUMEN_VERSION__` placeholder substituted at deploy time) or `bundledSource` (read bundled file, grep same import shape — used for graph-inference because its postgres pin is static). On drift: routes to `skipped[]` (YELLOW band, non-blocking) with a recommendation containing both versions and `termdeck init --rumen --yes`. On Management API HTTP error / network throw / missing access token / npm-view failure / unparseable body: fail-soft into `skipped[]` with the underlying reason (mirrors the Sprint 51.6 T3 `functionSource` probe degradation pattern). Probe runs in BOTH `init --mnestra` and `init --rumen` audit-upgrade phases — users running `init --mnestra` to refresh hooks also get told about Edge Function staleness without needing to remember `init --rumen`.
- **NEW test file `tests/audit-upgrade-edge-function-pin.test.js`** (14 tests). Coverage: in-sync (npm-registry resolver), in-sync (bundled-source resolver), drift detection with both resolvers (recommendation must include both versions + `init --rumen`), deployed-body-no-import-match → skipped, Management API 404 → skipped fail-soft, network throw → skipped fail-soft, missing `SUPABASE_ACCESS_TOKEN` → skipped fail-soft (no fetch made), npm-view failure → skipped fail-soft (Management API not called — short-circuit), bundled file unreadable → skipped, bundled file missing the import shape → skipped, integration via `auditUpgrade()` confirming pin probes route to `skipped[]` not `missing[]` and never auto-apply.

### Changed

- **`@jhizzard/termdeck-stack` 0.6.6 → 0.6.7 audit-trail bump.** Stack-installer JS source unchanged this release; bumped per RELEASE.md convention so the published trail validates against `@jhizzard/termdeck@1.0.7`.
- **`tests/audit-upgrade.test.js` PROBES count assertion 10 → 12.** Adds shape assertions for `edgeFunctionPin` probes (functionSlug + importPattern + expectedFrom + npmRegistryPkg-or-bundledPath). Existing `SQL_PROBES` filter widened to exclude both `functionSource` AND `edgeFunctionPin` probeKinds (they have their own dedicated tests).

### Fixed

- **Silent staleness of deployed Edge Functions across npm-package upgrades** (Codex T4-CODEX 2026-05-04 15:22 ET catch via Sprint 51.5b T2 lane work). Pre-v1.0.7: a user on petvetbid or jizzard-brain upgrading `@jhizzard/termdeck` and running `init --mnestra` saw all SQL audit probes pass GREEN even though their deployed `rumen-tick` was pinned to a multi-patch-old `npm:@jhizzard/rumen` version — no surface in the wizard told them to re-run `init --rumen`. Post-v1.0.7: the audit-upgrade phase shows a `⊘ skipped` entry with both versions and the explicit fix command. The `init --rumen` deploy step itself is already unconditional (since v0.6.x — confirmed at `packages/cli/src/init-rumen.js:1024` `deployFunctions(...)` is called every wizard pass with no first-install gate), so the existing fix mechanism remains; v1.0.7 just makes the staleness visible.

### Notes

- **`init --rumen` redeploy is already unconditional.** Step 1 of the Sprint 52 lane brief (force-redeploy on every run) collapsed to a documentation-only note: `packages/cli/src/init-rumen.js:1024` already calls `deployFunctions(rumen-tick + graph-inference)` on every `init --rumen` invocation regardless of prior state. `supabase functions deploy` is idempotent on Supabase's side; ~5s per Edge Function is acceptable. The detection probe is the actual sprint deliverable.
- **Class O is structurally distinct from Class H (migration-runner blindness) and Class M (architectural omission).** Class H is database-side schema drift; Class M is a bundled-resource write-path absence; Class O is **runtime-deployed-state drift between an npm-published artifact and a separately-hosted execution surface**. New letter is warranted: the failure mode is neither schema nor write-path, it's a publishing-pipeline-coverage gap. Pre-ship checklist item #14: every Edge Function shipped by `init --rumen` must have a corresponding `edgeFunctionPin` probe in `audit-upgrade.js` PROBES.
- **The 233/321 `rumen_insights` flatline since 2026-05-01 20:45 UTC is NOT closed by v1.0.7.** Sprint 52 detects pin drift and unblocks the redeploy path — but a redeploy from `0.4.0` → `0.4.5` does not, by itself, restart insights flow. The deeper bug is the Rumen picker shape mismatch on `memory_items` 1-row-per-session (PLANNING.md "Out of scope"): the picker at `~/Documents/Graciella/rumen/src/extract.ts:55-77` assumes the pre-Sprint-51.6 multi-row-per-session pattern; the new bundled hook writes one row per session. The fix is a pivot to read from `memory_sessions` directly (now authoritative post-mig-017) — a Rumen-package change in a separate publish wave. **Sprint 53** (queued in `docs/sprint-53-rumen-picker/PLANNING.md` placeholder, to be authored next session) closes this. Acceptance criterion #5 of Sprint 52 is therefore relaxed: `rumen_insights` count restoration is **not** the post-publish dogfood signal; the YELLOW pin-drift surface in `init --mnestra` audit IS the signal that v1.0.7 works.
- **Suite results.** Top-level `tests/*.test.js` matrix: 1064/1071 pass, 0 new failures. The single failure is a pre-existing flake (`mnestra bridge returns well-shaped response when there are zero hits` — fails identically on clean v1.0.6 HEAD with no Sprint 52 changes applied; depends on a live mnestra service the test environment does not provide). Audit-upgrade subset: 34/34 pass (14 new pin-probe tests + 20 existing audit-upgrade tests). Server suite unchanged: 40/40.
- **TermDeck cadence flips after Sprint 52.** The v1.0.x onion is structurally CLOSED (5 failure classes ledgered today: M/N/A#17/A#18/A#19 + new O). Forward work is memory-quality innovation: Sprint 53 — Rumen picker rewrite + doctor blindness fix; Sprint 54 — Cost-monitoring expandable dashboard panel (original Sprint 51 vision); Sprint 24 — Maestro / chopin-scheduler SaaS readiness (independent of TermDeck). Weekly-bump cadence for TermDeck for security/critical-only after this.

## [1.0.6] - 2026-05-04

> **v1.0.6 mini-sprint hotfix — Sprint 51.9: TWO Sprint 51.5b dogfood findings folded into one publish wave.** (1) `memory_hybrid_search` signature-drift guard (Codex T4-CODEX share-blind catch at 14:42 ET) + (2) mig 016 `cron.*` schema dependency on fresh-install (T3 RED at 14:55 ET on the canonical fresh-user-no-rumen-yet path). Both are Class A — the first a function-overload drift on long-lived installs, the second a schema-availability assumption on greenfield projects. Single combined publish wave so Brad outreach (currently HOLD per Sprint 51.5b T4-CODEX disposition) covers both fixes when it goes out. Wave: `@jhizzard/termdeck@1.0.6` + `@jhizzard/termdeck-stack@0.6.6` (audit-trail bump). Mnestra and Rumen unchanged. Single-lane direct fix, ~50 min combined wall-clock.

### Finding 1 — `memory_hybrid_search` signature drift (Codex T4-CODEX 14:42 ET)

Triggered mid-Sprint-51.5b when Codex T4-CODEX's MCP-wired `memory_recall` failed against petvetbid with a Postgres ambiguous-overload error on `public.memory_hybrid_search(...)`. petvetbid had TWO overloads coexisting: the canonical 8-arg shape (mig 002 + mig 004) AND a 10-arg drift overload with extra `recency_weight`/`decay_days` parameters from a pre-canonical Mnestra iteration or the rag-system writer's bootstrap. Sprint 52.1 (v1.0.5) closed the same Class A pattern for `match_memories` four hours earlier; T4-CODEX caught the cousin that Joshua's daily-driver Claude usage never surfaced because Claude Code uses the local rag-system spawner path, not the Mnestra MCP `memory_hybrid_search` endpoint.

### Finding 2 — mig 016 fresh-install RED on mnestra-only-no-rumen path (T3 14:55 ET)

T3's canonical fresh-install probe (clean tmp HOME + freshly-provisioned Supabase project) hit `relation "cron.job_run_details" does not exist` on `001_mnestra_tables.sql → ... → 016_mnestra_doctor_probes.sql`. Root cause: mig 016's two cron-touching SECURITY DEFINER wrappers (`mnestra_doctor_cron_runs`, `mnestra_doctor_cron_job_exists`) reference `cron.*` schema in `language sql` function bodies, which Postgres parse-time-resolves at CREATE time. Fresh Supabase projects do NOT have `pg_cron` enabled (default extension set is `pg_stat_statements, pg_trgm, pgcrypto, plpgsql, supabase_vault, uuid-ossp, vector`) — only Rumen's mig 002 issues `CREATE EXTENSION pg_cron`, so a user running `init --mnestra` BEFORE `init --rumen` (or skipping rumen entirely) hits exit 5. Petvetbid + Brad's jizzard-brain are unaffected because both have rumen installed. **The bug specifically impacts new-user onboarding via the documented "Tier 2 memory layer (standalone Mnestra)" path.**

### Added — Finding 1 fix

- **`do $$` signature-drift guard** at `packages/server/src/setup/mnestra-migrations/002_mnestra_search_function.sql`, immediately before `create or replace function memory_hybrid_search`. Same shape as Sprint 52.1's mig 001 guard for `match_memories`. Iterates `pg_proc` joined to `pg_namespace`, filters `proname = 'memory_hybrid_search' AND nspname = 'public'`, executes `drop function <regprocedure-cast>` per overload. Drops every overload regardless of arg list. Idempotent on greenfield. Scoped to `public` schema. No CASCADE. Mig 004 subsequently `CREATE OR REPLACE`s the same 8-arg signature with the match_count cap variant; net end-state is exactly ONE 8-arg `memory_hybrid_search` in public schema.
- **Mirrored to canonical Mnestra repo** at `~/Documents/Graciella/engram/migrations/002_mnestra_search_function.sql` (byte-identical). Maintains the Sprint 51.5 T1 hygiene.
- **NEW shape regression suite at `tests/migration-002-shape.test.js`** (7 tests). Pins: file existence, byte-identical to Mnestra-repo primary (skipped when repo absent), do$$ guard placement before CREATE OR REPLACE, schema scope (`nspname = 'public'`), no CASCADE, pg_proc-with-regprocedure-execute pattern, canonical 8-arg signature preserved (`query_text, query_embedding, match_count, full_text_weight, semantic_weight, rrf_k, filter_project, filter_source_type`).

### Added — Finding 2 fix

- **`do $cron_guard$` pg_cron-conditional block** at `packages/server/src/setup/mnestra-migrations/016_mnestra_doctor_probes.sql`. Mig 016 split into two halves: (a) cron-INDEPENDENT functions (`mnestra_doctor_column_exists`, `mnestra_doctor_rpc_exists`, `mnestra_doctor_vault_secret_exists`) emit unconditionally as before; (b) cron-DEPENDENT functions (`mnestra_doctor_cron_runs`, `mnestra_doctor_cron_job_exists`) and their grants live inside `do $cron_guard$ ... if exists (select 1 from pg_extension where extname = 'pg_cron') then execute ... end if; end $cron_guard$;`. The do-block uses nested dollar-quoting (`$cron_guard$`/`$stmt$`/`$body$`) so the function bodies inside EXECUTE statements parse cleanly. When pg_cron is not enabled, the cron probes don't get created at all; the doctor's cron probes return the existing `unknown` band (Sprint 51.5 T2 contract). When pg_cron IS enabled (= every existing-install upgrade path including petvetbid + jizzard-brain), the cron probes emit identically to v1.0.5. Verified against petvetbid: `psql -f 016_mnestra_doctor_probes.sql` post-patch lists all 5 `mnestra_doctor_*` functions, identical to pre-patch state.
- **T3's recommended Option A.** T3's lane brief (line 586) recommended this exact fix shape over Option B (auto-enable pg_cron from the migration — couples mnestra to a cron extension it doesn't otherwise need + only postgres role can CREATE EXTENSION) and Option C (move mig 016 to Rumen-side — breaks doctor's standalone-mnestra promise).
- **Mirrored to canonical Mnestra repo** at `~/Documents/Graciella/engram/migrations/016_mnestra_doctor_probes.sql` (byte-identical).
- **NEW shape regression suite at `tests/migration-016-shape.test.js`** (7 tests). Pins: file existence, byte-identical to Mnestra-repo primary (skipped when repo absent), do$cron_guard$ block presence checking pg_extension, cron-DEPENDENT functions (`mnestra_doctor_cron_runs`, `mnestra_doctor_cron_job_exists`) appear ONLY inside the guard, cron-INDEPENDENT functions (`mnestra_doctor_column_exists`, `mnestra_doctor_rpc_exists`, `mnestra_doctor_vault_secret_exists`) appear OUTSIDE the guard, grants for cron-dependent functions live inside the guard, mig does NOT use `CREATE EXTENSION pg_cron` (Option B was rejected).

### Changed

- **`@jhizzard/termdeck-stack` 0.6.5 → 0.6.6 audit-trail bump.** Stack-installer JS source unchanged this release; bumped per RELEASE.md convention so the published trail validates against `@jhizzard/termdeck@1.0.6`.

### Fixed

- **MCP `memory_recall` ambiguous-overload error on long-lived v0.6.x-era installs** (Codex T4-CODEX 2026-05-04 14:42 ET catch). Pre-v1.0.6: any user with a 10-arg drift overload of `memory_hybrid_search` coexisting with the canonical 8-arg shape would hit `function memory_hybrid_search(text, vector, integer, double precision, double precision, integer, text, text) is not unique` when calling from PostgREST/MCP. Post-v1.0.6: mig 002's do$$ prelude drops all overloads; mig 002's CREATE lands the canonical 8-arg; mig 004's CREATE OR REPLACE updates the body with the match_count cap. Net: one 8-arg overload, no ambiguity.
- **`termdeck init --mnestra` exit 5 on fresh Supabase project (mnestra-only-no-rumen-yet path)** (T3 2026-05-04 14:55 ET catch). Pre-v1.0.6: any new user running `init --mnestra` against a freshly-provisioned Supabase project (default extension set, no pg_cron) hit `relation "cron.job_run_details" does not exist` on mig 016 because the SECURITY DEFINER cron probe wrappers were unconditional. Post-v1.0.6: mig 016 has a do$cron_guard$ block that only emits the cron-touching wrappers when pg_cron is enabled. Fresh-install path now succeeds; existing-install paths (where pg_cron came in via Rumen's mig 002) are unchanged. Workaround that worked pre-v1.0.6: manually `CREATE EXTENSION IF NOT EXISTS pg_cron;` before `init --mnestra`. No longer required.

### Notes

- **Same auditor caught both Class A drift cousins in the v1.0.x onion.** Sprint 51.7 T4-CODEX deferred-finding ledger #17 (`match_memories` drift, closed v1.0.5). Sprint 51.5b T4-CODEX live-finding ledger #18 (`memory_hybrid_search` drift, closed v1.0.6). The 3+1+1 pattern's value: Codex's MCP-wired adversarial probes catch what all-Claude lanes share-blind on. Joshua's daily-driver Claude usage went through the local rag-system path and never invoked `memory_hybrid_search` directly; Codex's MCP recall is the first probe to actually hit it.
- **Final v1.0.x onion state** (closed across five sprints in 26 hours):
  - v1.0.1 (51.5): mig sync + Vault UI removal + Edge Function templating + Rumen secrets shape
  - v1.0.2 (51.6): Class M — bundled hook write-path absence
  - v1.0.3 (51.7): Class M follow-up — wizard wire-up bug + bundled hook metadata completeness
  - v1.0.4 (51.8): Class N — settings.json wiring lockstep drift (Brad's catch)
  - v1.0.5 (52.1): Class A #17 — `match_memories` signature drift (Codex T4-CODEX deferred catch from 51.7)
  - v1.0.6 (51.9): Class A #18 — `memory_hybrid_search` signature drift (Codex T4-CODEX live catch from 51.5b)
- **Suite results.** Migration shape suite + hook matrix combined: 158/158 pass across `tests/migration-002-shape.test.js` (NEW, 7/7) + `tests/migration-001-shape.test.js` (8/8) + `tests/migration-012-shape.test.js` + `tests/migration-templating.test.js` + `tests/migration-loader-precedence.test.js` + 5 hook test files. Server suite unchanged: 40/40. Zero new fails.
- **Sprint 51.5b in-flight at publish time.** T1/T2/T3 still probing v1.0.5 surfaces; the mid-sprint v1.0.6 patch lets T4-CODEX pivot Phase 2 from "validate v1.0.5 + workaround" to "validate v1.0.6's guard against the same drift fixture." Substrate update: Sprint 51.5b STATUS.md gets an `[orchestrator]` post announcing the patch.
- **Pre-ship checklist + linter candidate** (Sprint 52+, unchanged from v1.0.5 notes): every `CREATE OR REPLACE FUNCTION` in a migration on a long-lived install needs a drift-tolerant prelude. Two Class A incidents in the same day from the same migration-author shape (mig 001's match_memories, mig 002's memory_hybrid_search) is proof of pattern. Worth a migration-authoring linter that flags any `create or replace function` without a corresponding `do $$ ... drop function ... end $$;` block.

## [1.0.5] - 2026-05-04

> **v1.0.5 mini-sprint hotfix — Sprint 52.1: `match_memories` signature-drift guard.** Triggered by Sprint 51.7's deferred Class A side-finding (T4-CODEX 2026-05-04 11:38 ET): `packages/server/src/setup/mnestra-migrations/001_mnestra_tables.sql` declares `match_memories` with return-table columns `(id, content, source_type, category, project, metadata, similarity)`, but long-lived v0.6.x-era installs (Joshua's petvetbid, Brad's jizzard-brain) had it created earlier with the rag-system writer's drift shape `(id, content, metadata, source_type, category, project, created_at, similarity)`. Postgres rejects `CREATE OR REPLACE FUNCTION` when the return-table changes — `cannot change return type of existing function` — and the wizard's `applyMigrations()` step throws exit 5. Sprint 51.7 fixed Class M (DB failure no longer strands hook upgrade) and Sprint 51.8 fixed Class N (settings.json wiring lockstep), so this drift was the only remaining blocker keeping `termdeck init --mnestra` from finishing cleanly on existing v0.6.x installs. Single-lane direct fix, ~30 min wall-clock. Wave: `@jhizzard/termdeck@1.0.5` + `@jhizzard/termdeck-stack@0.6.5` (audit-trail bump). Mnestra and Rumen unchanged.

### Added

- **`do $$` signature-drift guard** at `packages/server/src/setup/mnestra-migrations/001_mnestra_tables.sql:81-95`, immediately before `create or replace function match_memories`. The block iterates `pg_proc` joined to `pg_namespace`, filters `proname = 'match_memories' AND nspname = 'public'`, and `execute 'drop function ' || r.sig::text` per overload via the `regprocedure` cast. Drops every overload regardless of arg list, so the subsequent CREATE OR REPLACE always lands cleanly on greenfield AND existing-drift installs. Idempotent — on a brand-new project the loop iterates zero times. Scoped to schema `public` so a same-named function in another schema is never touched. No CASCADE — dependent objects in plpgsql/SQL function bodies (e.g. `memory_recall_graph` from migration 010) reference functions by name and resolve at call time, so drop-then-recreate is safe without CASCADE; if a true hard dependency (view, generated column) ever appears, the DROP fails loud — the right behavior.
- **Mirrored to canonical Mnestra repo** at `~/Documents/Graciella/engram/migrations/001_mnestra_tables.sql` (byte-identical). Maintains the Sprint 51.5 T1 hygiene: the bundled mirror in TermDeck and the canonical primary in the Mnestra repo MUST stay in lockstep, since `@jhizzard/mnestra` ships from the Mnestra repo and `@jhizzard/termdeck` ships the bundled mirror.
- **NEW shape regression suite at `tests/migration-001-shape.test.js`** (8 tests). Pins: file existence at bundled path, byte-identical to Mnestra-repo primary (skipped when repo absent), do$$ guard appears textually before `create or replace function match_memories`, guard scopes to `nspname = 'public'`, guard does NOT use CASCADE, guard iterates pg_proc with regprocedure execute, return-table column order is canonical (id/content/source_type/category/project/metadata/similarity), no stale `created_at` in return-table (the v0.6.x-era drift shape).

### Changed

- **`@jhizzard/termdeck-stack` 0.6.4 → 0.6.5 audit-trail bump.** Stack-installer JS source unchanged this release; bumped per RELEASE.md convention so the published trail validates against `@jhizzard/termdeck@1.0.5`.

### Fixed

- **`termdeck init --mnestra` exit 5 on long-lived v0.6.x-era installs** (Sprint 51.7 deferred Class A drift). Pre-v1.0.5: `applyMigrations()` would throw on `001_mnestra_tables.sql` with `cannot change return type of existing function` whenever `match_memories` already existed with the rag-system writer's drift shape. Post-v1.0.5: the do$$ prelude drops all overloads first, so the CREATE OR REPLACE replays cleanly. Closes the cascade Sprints 51.7 + 51.8 left visible-but-non-blocking.

### Notes

- **The full v1.0.x onion is now closed.** Sprint 51.5 fixed migration sync drift + Vault UI removal + Edge Function templating + Rumen secrets shape. Sprint 51.6 fixed bundled hook write-path absence (Class M). Sprint 51.7 fixed wizard wire-up bug (Class M follow-up) + bundled hook metadata completeness. Sprint 51.8 fixed settings.json wiring drift (Class N — Lockstep drift). Sprint 52.1 (this) fixes match_memories signature drift (Class A — schema drift). Anyone who first ran `termdeck init --mnestra` on v1.0.0/v1.0.1 should now upgrade with `npm install -g @jhizzard/termdeck@latest && termdeck init --mnestra --yes` and see: hook FILE refreshed v1 → v2, settings.json migrated Stop → SessionEnd, all 17 migrations applied including the now-idempotent mig 001, and the wizard exits 0.
- **Suite results.** Migration shape suite + hook matrix combined: 155/155 pass across `tests/migration-001-shape.test.js` (NEW, 8/8) + `tests/migration-012-shape.test.js` + `tests/migration-templating.test.js` + `tests/migration-loader-precedence.test.js` + `tests/mnestra-migration-009.test.js` + the 5 hook test files. Server suite unchanged: 40/40. Zero new fails.
- **No live-Postgres test harness; shape-test substitutes for it.** The migration runner has no in-memory Postgres for replay testing in CI. The 8 shape-tests pin the structural invariants — guard placement, schema scope, no-CASCADE, regprocedure-loop pattern, canonical return-table column order — so any regression that could re-introduce the drift cascade fails loudly at file level. A future Sprint candidate is to add a `pg-mem` or testcontainers-based migration replay harness; for now, Brad's jizzard-brain is the empirical e2e smoke test (analogous to v1.0.4 closing his settings.json bug).
- **Pre-ship checklist + INSTALLER-PITFALLS.md ledger.** Same Class A pattern that's been on the books since entry #13; this is the fifth ledger entry citing Class A. The pattern is well-canonized — every CREATE OR REPLACE FUNCTION on a long-lived install needs a drift-tolerant prelude. Sprint 52+ candidate: codify into a migration-authoring linter that flags any `create or replace function` without a corresponding drift guard.

## [1.0.4] - 2026-05-04

> **v1.0.4 mini-sprint hotfix — Sprint 51.8: settings.json wiring migration (Brad's v1.0.3 follow-up).** Triggered by Brad's 2026-05-04 ~13:30 ET jizzard-brain repro: v1.0.3 wizard refreshes the hook FILE from v1 → v2 correctly, but `~/.claude/settings.json` keeps the memory hook wired under `hooks.Stop` (the wiring shape `@jhizzard/termdeck-stack@<=0.5.0` wrote on v1.0.0/v1.0.1 first-installs). The v2 hook does not gate on event type, so a `Stop` wiring fires every assistant turn and writes N `session_summary` rows in `memory_items` per session instead of 1. Wave: `@jhizzard/termdeck@1.0.4` + `@jhizzard/termdeck-stack@0.6.4` (audit-trail bump). Mnestra and Rumen unchanged. Single-lane direct fix (no 3+1+1 ceremony — scope was small enough).

### Added

- **`migrateSettingsJsonHookEntry()` + `_mergeSessionEndHookEntry()` + `_isSessionEndHookEntry()`** hoisted into `packages/cli/src/init-mnestra.js`. The merge primitive is a 1:1 hoist of the same-named function in `packages/stack-installer/src/index.js:451` — required because the published `@jhizzard/termdeck` tarball ships only `packages/stack-installer/assets/hooks/**`, not `.../src/**`, so the wizard can't `require()` the migration logic across package boundaries at runtime. Cross-package parity test pins the duplicate as 1:1 against future maintenance.
- **`runSettingsJsonMigration({ dryRun })` wired into `init-mnestra.js:main()`** immediately after `runHookRefresh()`. Prints `migrated Stop → SessionEnd (was firing on every turn; backup: <name>)` for Brad's repro shape, `already wired (SessionEnd)` for already-correct installs, `installed (SessionEnd)` for fresh-no-prior-settings installs, `would-migrated-from-stop (dry-run)` etc. for `--dry-run` truthfulness. Same fail-soft posture as `runHookRefresh`: a settings-migration failure logs + continues without aborting the wizard's DB phase. `TERMDECK_DEBUG_WIREUP=1` emits `[wire-up-debug]` stderr lines (entry, return, throw) for future bisects.
- **Best-effort timestamped backup** of `~/.claude/settings.json` before any modification (`<path>.bak.<YYYYMMDDhhmmss>`). Atomic write via `<path>.tmp` + rename. Brand-new installs (no prior file) skip the backup step.
- **Regression suite at `tests/init-mnestra-settings-migration.test.js`** (17 tests). Critical case: Brad's exact `Stop`-wired fixture → migrate → assert SessionEnd present + Stop entry stripped + backup written + backup contains original Stop-wired shape. Plus already-migrated-no-op, idempotent-second-run-on-migrated-install, fresh-install (no-file path), empty-file install, malformed-JSON soft-skip, non-object-top-level soft-skip, dry-run truthfulness across three states (would-migrate, already-installed, would-installed), mixed cases (unrelated Stop hooks preserved while ours strips, SessionEnd with another user's hook + our hook in Stop merges into same group), and cross-package parity (hoisted merge primitive ≡ stack-installer's merge primitive on five identical inputs). 17/17 pass.
- **INSTALLER-PITFALLS.md ledger entry #16** (Class N — Lockstep drift) plus pre-ship checklist item #13 ("lockstep local-FS components are migrated as a unit, not piecemeal") plus new Class **N** in the failure-class taxonomy plus a new section "Settings.json invariants the wizard must enforce" (6-row table per Brad's framing — invariant + self-heal + failure mode if violated).

### Changed

- **`@jhizzard/termdeck-stack` 0.6.3 → 0.6.4 audit-trail bump.** Stack-installer JS source unchanged this release; bumped per RELEASE.md convention so the published trail validates against `@jhizzard/termdeck@1.0.4`.

### Fixed

- **Wizard left `~/.claude/settings.json` wired under `Stop` after v1.0.3 hook FILE refresh** (Brad's 2026-05-04 jizzard-brain repro). Pre-v1.0.4: `runHookRefresh()` copied the v2 hook FILE to disk but never touched `settings.json`. Anyone whose settings.json was written by `@jhizzard/termdeck-stack@<=0.5.0` (= anyone who first ran `termdeck init --mnestra` on v1.0.0/v1.0.1) kept the `Stop` wiring; v2 hook fired per-turn. Post-v1.0.4: `runSettingsJsonMigration()` runs alongside the file refresh on every wizard pass, idempotent, fail-soft. Brad's `sed`-based workaround is no longer required.
- **`memory_items` accumulating N `session_summary` rows per session** (Brad's downstream symptom). Root cause was wiring drift, not write-path: `memory_sessions` already dedups via mig 017's `Prefer: resolution=merge-duplicates` upsert on `session_id`, but `memory_items` is pure-insert and accumulated one row per assistant turn whenever the v2 hook fired on `Stop`. With wiring corrected to `SessionEnd`, the hook fires once per session close (`/exit`, Ctrl+D, terminal close, kill) and the accumulation stops at the source.

### Notes

- **Why three sprints in a row missed this** (52.0 retro-of-retros, written into INSTALLER-PITFALLS.md ledger entry #16). All three sprint acceptance criteria checked file existence ("v2 hook file at the right path") rather than end-to-end behavior ("after the wizard, exactly 1 `session_summary` row per session"). The behavioral probe was never run against a starting state with `Stop` wiring because all four worker dev machines had pre-migrated `SessionEnd` wiring from earlier `termdeck-stack` runs. Dogfood-from-current-state ≠ install-from-v1.0.0; Brad's machine retained `Stop` wiring from v1.0.0 first-install, so his repro is the first run of the actual e2e probe. New Class **N** (Lockstep drift) names the pattern: two or more local-FS components must stay in sync, the wizard updates one piecemeal, the other silently stales.
- **Suite results.** Full hook-related matrix: 128/128 pass across `tests/init-mnestra-settings-migration.test.js` (NEW, 17/17) + `tests/init-mnestra-cli-refresh.test.js` (6/6) + `tests/init-mnestra-hook-refresh.test.js` (16/16) + `tests/stack-installer-hook-merge.test.js` (72/72) + `tests/init-mnestra-resume.test.js` + `packages/stack-installer/tests/hook-metadata-parser.test.js` (21/21). Project-taxonomy + per-agent + resume neighbors: 66/66. Server suite: 40/40. Zero new fails.
- **Out-of-scope side-finding still deferred** (Sprint 52.1 candidate, unchanged from v1.0.3 notes). `packages/server/src/setup/mnestra-migrations/001_mnestra_tables.sql:82-116` `match_memories` return-type drift. Sprint 51.7's wire-up fix means this drift no longer hides Class M (hook upgrade) AND now no longer hides Class N (settings.json wiring); the only remaining symptom is the visible exit 5 on existing v0.6.x-era installs. Single-lane fix: wrap in `DROP FUNCTION IF EXISTS match_memories(vector, ...) CASCADE;` before `CREATE OR REPLACE`. Not in scope for v1.0.4.
- **Pattern for future sprints** (canonized into INSTALLER-PITFALLS.md): every wizard-touching sprint must run an e2e probe from a known-stale starting state (e.g. `Stop`-wired settings.json), not just from current-dev-machine state. The "settings.json invariants the wizard must enforce" table is the durable artifact — Sprint 52+ candidate is to extract the pattern into a reusable `wizardInvariants[]` table the wizard's epilogue iterates across all local-FS artifacts (settings.json, secrets.env, config.yaml, MCP config).

## [1.0.3] - 2026-05-04

> **v1.0.3 mini-sprint hotfix — Sprint 51.7: wizard wire-up bug bisect + bundled-hook metadata completeness pass.** Triggered by Sprint 51.6 Phase B failure: `refreshBundledHookIfNewer()` was wired into `init-mnestra.js:677` but never fired during the canonical `termdeck init --mnestra` user path on Joshua's daily-driver AND Brad's `jizzard-brain`. T4-CODEX (independent auditor) reproduced the exact failure mode at 11:07 ET — `applyMigrations()` aborts on `001_mnestra_tables.sql` (`match_memories` `CREATE OR REPLACE FUNCTION` return-type drift on existing-install Mnestras) → outer catch returns exit 5 → refresh stranded. T1 fix decouples hook refresh from DB phase; T2 closes the metadata-completeness gap Codex flagged at Sprint 51.6 Phase B. Wave: `@jhizzard/termdeck@1.0.3` + `@jhizzard/termdeck-stack@0.6.3` (audit-trail bump). Mnestra and Rumen unchanged. **Second sprint to use the canonical 3+1+1 pattern**; Codex caught 5 real WIP gaps across T1/T2 in real-time before any FIX-LANDED post (multi-MCP-prefix tool-name match for `facts_extracted`, multi-shape content extraction across Claude/flat/Codex transcript shapes, POST-body assertions missing on parser metadata, parser-not-wired-to-POST scaffolding gap, T1 dead-code first-cut).

### Added

- **Class M follow-up — wizard wire-up bug bisected and decoupled** (T1, ships in `@jhizzard/termdeck@1.0.3`). NEW `runHookRefresh({ dryRun })` helper in `packages/cli/src/init-mnestra.js:602-635` + new call site at line 716, immediately after `writeSecretsFile()` and BEFORE `step('Connecting to Supabase...')`. Hook refresh is local-FS work; decoupling means a failed DB phase no longer strands the bundled-hook upgrade. Old DB-coupled refresh block at the former lines 670-690 removed; back-pointer comment at lines 745-749 preserves intent for future readers. `--dry-run` now exercises the refresh path (Sprint 51.6 dry-run-doesn't-fire issue T4-CODEX 11:07 ET flagged is also closed) — `termdeck init --mnestra --dry-run` truthfully reports `would-refresh v0 → v2 (dry-run)` against a stale-marked installed hook.
- **CLI-binary integration test for the wire-up** (T1). NEW `tests/init-mnestra-cli-refresh.test.js` (236 LOC, 6 tests). Test #1 spawns the actual `node packages/cli/src/index.js init --mnestra --from-env` against a tmp HOME with a stale TermDeck-marked hook AND an unreachable `DATABASE_URL` (`postgres://...@127.0.0.1:1/...` — guaranteed ECONNREFUSED). Asserts: pg connect fails (proves DB phase ran), `→ Refreshing... ✓ refreshed v0 → v<n>` appears in stdout (proves refresh fired), the `Refreshing` line precedes `Connecting to Supabase` (proves architectural ordering), the hook file content actually changed on disk, AND a timestamped `.bak.YYYYMMDDhhmmss` exists with the original stale body. **This test would have caught Sprint 51.6 Phase B failure pre-publish** — it exercises the wire-up under realistic-failure conditions (DB unreachable), exactly the conditions that strand the pre-fix implementation.
- **Bundled hook transcript-parsing metadata completeness** (T2, ships in `@jhizzard/termdeck-stack@0.6.3` + bundled in `@jhizzard/termdeck@1.0.3`). NEW `parseTranscriptMetadata(rawJsonl)` + `extractContentBlocks(msg)` + `FACT_TOOL_NAMES` Set in `packages/stack-installer/assets/hooks/memory-session-end.js:482-545`. Reads top-level `timestamp` (falls back to `msg.message.timestamp`); extracts earliest → `started_at`, latest → `ended_at`; computes `duration_minutes` as `(latest - earliest) / 60000` (rounded, min 0); counts `tool_use` blocks named `memory_remember` / `mcp__mnestra__memory_remember` / `mcp__memory__memory_remember` for `facts_extracted` (conservative — no `Remember:` string-match which would over-count quoted text). `extractContentBlocks` handles nested Claude `msg.message.content[]`, flat adapter `msg.content[]`, AND Codex `response_item.payload.content[]` shapes (T4-CODEX 11:13 ET catch — without this, non-Claude sessions silently under-count facts). Wired into `buildSummary()` and `postMemorySession()`; `processStdinPayload()` prefers parser-derived `endedAt` over hook-fire-time when transcript carries timestamps (matches rag-system writer semantics). v1-compat default path: when caller omits the new fields, body still serializes `null/null/0` (not absent).
- **Bundled hook version stamp v1 → v2** (T2). Bumped at `packages/stack-installer/assets/hooks/memory-session-end.js:64`. Forces `refreshBundledHookIfNewer()`'s `installed >= bundled` short-circuit at `init-mnestra.js:550` to take the refresh branch on next `termdeck init --mnestra` for any user with a v1 installed hook — load-bearing insurance regardless of T1's wire-up fix shape. Repo-wide grep confirms zero v1 hits in `packages/`; existing test fixtures use synthetic stamps so the bump is non-breaking (`tests/init-mnestra-hook-refresh.test.js`, `tests/stack-installer-hook-merge.test.js`).
- **Hook-metadata parser test suite** (T2). NEW `packages/stack-installer/tests/hook-metadata-parser.test.js` (329 LOC, 21 tests): brief-required (empty / single / 30-min span / malformed-line / 3-remember / remember+Bash) + T4-CODEX 11:09 ET catches (legacy `mcp__memory__memory_remember` MCP-name match) + T4-CODEX 11:13 ET catches (flat-content shape, Codex `response_item` shape, `extractContentBlocks` direct test) + defensive cases (null/undefined input, "Remember:" text false-positive, `tool_result` not counted, `msg.message.timestamp` fallback, invalid timestamp string, sub-30s rounding, substring false-positive). NEW fixture `packages/stack-installer/tests/fixtures/transcript-sample.jsonl` (8 lines from a real Claude transcript with all user/assistant text scrubbed to `<scrubbed>` placeholders; structure preserved verbatim — 3 memory_remember tool_use blocks, 1 Bash tool_use, 1 tool_result, 15-min span).
- **POST-body assertions on parser-derived metadata** (T2, T4-CODEX 11:18 ET catch). Updated `tests/stack-installer-hook-merge.test.js:1057-1148` so the end-to-end `processStdinPayload` test transcript carries timestamps + 1 `memory_remember` tool_use, then asserts `sessionCall.body.started_at` / `duration_minutes` / `facts_extracted` reach the actual `/rest/v1/memory_sessions?on_conflict=session_id` POST body. Direct `postMemorySession()` unit test now passes the new fields and asserts serialized body keys. New v1-compat default-path test pins the `null/null/0` fallback. Without this, a regression dropping parser metadata between `buildSummary()` → `processStdinPayload()` → `postMemorySession()` would slip through silently (the gap that allowed the original Sprint 51.6 v1 hook to ship `started_at=NULL`).

### Changed

- **`@jhizzard/termdeck-stack` 0.6.2 → 0.6.3 audit-trail bump.** Stack-installer JS source (`src/index.js`, `src/launcher.js`, `src/mcp-config.js`) is unchanged this release; bumped because the bundled hook asset (`assets/hooks/memory-session-end.js`, which the stack-installer ships) carries T2's parser + v2 stamp + multi-shape content extraction. Validates against `@jhizzard/termdeck@1.0.3` per RELEASE.md convention.
- **`TERMDECK_DEBUG_WIREUP=1` env gate** (T1). Production users see no extra noise; setting the env emits `[wire-up-debug]` stderr lines at refresh entry (HOOK_DEST + HOOK_SOURCE + exists checks), refresh return (full status JSON), and on throw (full stack). Useful for any future wire-up bisect.

### Fixed

- **Wizard wire-up no-op on `termdeck init --mnestra`** (Sprint 51.6 Phase B failure mode). Pre-v1.0.3: hook refresh was syntactically wired but architecturally coupled to `pgRunner.connect` + 17-migration replay + `runMnestraAudit` + `writeYamlConfig` ALL succeeding. Joshua's Phase B run threw at `applyMigrations()` on `001_mnestra_tables.sql` (T4-CODEX pinned the exact SQL: `cannot change return type of existing function` on `match_memories`); outer catch fired, refresh never reached. Post-v1.0.3: refresh runs upstream of every fallible DB step. Confirmed via T4-CODEX live-failure probe — temp HOME against petvetbid refreshes stale hook to v2 before the known mig-001 failure (`after_wc=898`, v2 stamp observed, one backup written).
- **`memory_sessions` rows from v1 hook had `started_at=NULL` / `duration_minutes=NULL` / `facts_extracted=0`** (Sprint 51.6 Phase B caveat). v2 hook parses transcript JSONL; new rows carry non-NULL metadata when transcript timestamps are present, NULL/0 fallback when absent (preserves v1 contract for fixture-style transcripts).
- **Multi-shape content extraction** in `parseTranscriptMetadata` (T4-CODEX 11:13 ET). Before fix: only nested Claude `msg.message.content[]` was scanned. Non-Claude sessions (Grok flat shape, Codex `response_item.payload.content[]` shape) silently under-counted `facts_extracted`. After fix: all three shapes supported uniformly.

### Notes

- **Five Codex audits caught real bugs in T1/T2 WIP before any FIX-LANDED post.** 11:07 ET: independent CLI repro pinned the exact SQL error AND flagged that T1's planned `--dry-run` integration test would NOT exercise refresh (the dry-run early-return at `init-mnestra.js:643-650` short-circuits before line 677). 11:09 ET: T2 parser pre-check requiring count of both `mcp__mnestra__memory_remember` AND `mcp__memory__memory_remember` MCP-name prefixes (real local transcripts show 36 hits each across the most-recent 50 .jsonl files). 11:12 ET: T2 WIP blocker — parser existed but `postMemorySession()` signature unchanged, body still omitted new fields, `processStdinPayload()` dropped parser metadata; T2 fixed before next audit. 11:13 ET: T2 multi-shape content extraction blocker — flat-content shape under-counted; T2 added `extractContentBlocks()` helper. 11:18 ET: T2 POST-body assertions missing — no test asserted parser metadata reaches the actual Supabase POST body, so future regressions could silently drop the new fields; T2 added integration-level + unit-level body assertions. 11:19 ET: T1 dead-code blocker — `runHookRefresh` helper existed but no call site; T1 wired it. **Without the auditor**, v1.0.3 would have shipped silent under-counting on non-Claude sessions, silent regression-vulnerability for parser metadata, AND the original Sprint 51.6 wire-up bug (T1's first-cut left the old DB-coupled block in place). Codex's pattern of reading WIP rather than waiting for FIX-LANDED is what made these catches possible.
- **Suite results.** Combined targeted matrix (T4-CODEX 11:24 ET re-run): 140 tests / 140 pass / 0 fail across `tests/init-mnestra-cli-refresh.test.js` (T1's NEW, 6/6) + `tests/init-mnestra-hook-refresh.test.js` (16/16) + `tests/stack-installer-hook-merge.test.js` (72/72, was 71 — T2 added 1, modified 2) + `tests/project-taxonomy.test.js` (25/25) + `packages/stack-installer/tests/hook-metadata-parser.test.js` (T2's NEW, 21/21). Net +28 pass vs the v1.0.2 baseline (T1's +6 + T2's +21 + 1 modified hook-merge). Zero new fails.
- **Out-of-scope side-finding deferred to backlog (T1 + T4-CODEX).** `packages/server/src/setup/mnestra-migrations/001_mnestra_tables.sql:82-116` declares `match_memories` with return columns `(id, content, source_type, category, project, metadata, similarity)` but petvetbid's existing function returns `(id, content, metadata, source_type, category, project, created_at, similarity)`. Postgres rejects `CREATE OR REPLACE FUNCTION` on return-type drift. **Class A migration-replay drift** — silent for users who never re-ran `init --mnestra` against an existing v0.6.x+ Mnestra DB until now. T1's wire-up fix means this drift no longer hides Class M (hook upgrade) — the hook gets refreshed and v2 metadata writer lands; only the migration-replay surfaces as a visible failure with exit 5. Suggested follow-up (sprint-52 candidate, "match_memories return-type drift idempotency"): wrap `001_mnestra_tables.sql:82-116` in `DROP FUNCTION IF EXISTS match_memories(vector, double precision, integer, text) CASCADE;` before `CREATE OR REPLACE`, OR add a `do$$ ... $$` guard that detects signature drift and rebuilds. **NOT fixed in v1.0.3** — release wording reflects the wire-up + metadata scope, not "the wizard now fully succeeds on existing installs."
- **Closes ledger #15 v1.0.3 follow-up** per `docs/INSTALLER-PITFALLS.md:173`. Class M (architectural omission of resilience) wire-up class is closed by decoupling hook refresh from fragile DB-replay prerequisites; Class A migration-001 drift surfaces explicitly rather than hiding behind exit 5.
- **Second sprint to use the canonical 3+1+1 pattern** (3 Claude workers + 1 Codex auditor + 1 orchestrator). Codex caught 5 real WIP gaps in real-time across T1/T2 (vs Sprint 51.6's 4 catches) — the pattern is now consistently load-bearing and documented in `~/.claude/CLAUDE.md` § Sprint role architecture.

## [1.0.2] - 2026-05-04

> **v1.0.2 mini-sprint hotfix — Sprint 51.6: bundled session-end hook fix (Class M architectural omission) + 5-part fix wave caught by Codex auditor.** Triggered by live psql probe at 2026-05-03 19:13 ET revealing `memory_sessions` stuck at 289 rows since 2026-05-01 20:40 UTC. T2 + T1 reclassified Bug #2 as **Class M — architectural omission, not execution failure**: the bundled hook never had a `memory_sessions` write path; the 289 rows came from Joshua's personal rag-system hook overwritten on 2026-05-02 13:24 ET. T3 shipped a 5-part fix; Codex (T4 auditor) caught 4 additional blockers in real-time WIP review. Wave: `@jhizzard/termdeck@1.0.2` + `@jhizzard/termdeck-stack@0.6.2` + `@jhizzard/mnestra@0.4.2` (rumen unchanged). **First sprint to use the canonical 3+1+1 (Orchestrator + Workers + Codex Auditor) pattern documented in `~/.claude/CLAUDE.md` § Sprint role architecture.**

### Added

- **Class M — bundled hook now writes `memory_sessions`** (T3, ships in `@jhizzard/termdeck-stack@0.6.2`). NEW `postMemorySession()` in `packages/stack-installer/assets/hooks/memory-session-end.js` POSTs to `/rest/v1/memory_sessions?on_conflict=session_id` with `Prefer: resolution=merge-duplicates,return=minimal` (idempotent on duplicate SessionEnd fires). `processStdinPayload` calls both `postMemoryItem` and `postMemorySession`. `buildSummary` returns `{summary, messagesCount}` for the dual write. Hook carries `// @termdeck/stack-installer-hook v1` version stamp for upgrade-aware overwrite. Closes the architectural gap that hid for ~2 days on Joshua's daily-driver — every `/exit` now writes both tables.
- **Hook-refresh wiring in `init-mnestra.js`** (T3, closes Codex auditor's 20:11 GAP). NEW `refreshBundledHookIfNewer()` runs after `writeYamlConfig`. Reads bundled hook's version stamp + signature; compares against installed signature at `~/.claude/hooks/memory-session-end.js`; refreshes when bundled is strictly newer. Timestamped `.bak.<YYYYMMDDhhmmss>` backup before overwrite. **Safety gate** — unsigned installed hooks only auto-refresh when they carry a TermDeck-managed marker (`TermDeck session-end memory hook` or `@jhizzard/termdeck-stack`); unsigned-without-marker hooks are preserved unless `forceOverwrite`/interactive approval. Closes Codex's 20:23 SAFETY GAP audit.
- **Sprint-41 14-entry default `PROJECT_MAP`** in the bundled hook (closes T1's Sprint 51.6 side-finding (b)). The empty PROJECT_MAP that shipped pre-Sprint-50 (overwritten by the May 2 install) is replaced with the most-specific-first map authored Sprint 41 — chopin-nashville catch-all is LAST so TermDeck/ClaimGuard/Chopin in Bohemia/SchedulingApp paths don't pollute the chopin-nashville tag. Live evidence at sprint open: 4 today's writes were tagged `project="global"`; post-refresh they will tag correctly to `termdeck`/`mnestra`/etc.
- **Bundled hook ships in `@jhizzard/termdeck` tarball** (closes Codex's 20:17/20:23/20:28 packaging blocker). Root `package.json.files` glob now includes `packages/stack-installer/assets/hooks/**`. Empirically verified via `npm pack --dry-run --json` with isolated npm cache: `31.2kB packages/stack-installer/assets/hooks/memory-session-end.js` ships in v1.0.2.
- **Bug C: `--project-ref` explicit on `supabase functions deploy`** (T3, closes Brad's 2026-05-03 20:09 ET v1.0.1 install regression). `deployFunctions(rumenVersion, projectRef, dryRun)` in `packages/cli/src/init-rumen.js` — projectRef now required + threaded into `--project-ref <ref>` argv on every `supabase functions deploy` call. Validation rejects missing/empty projectRef early. Dodges the link-state-coupling subprocess bug Brad surfaced where `supabase link` writes state to one workdir but `functions deploy` from another can't find it.
- **Bug D: function-source-diff probe in audit-upgrade** (T3, closes Brad's 2026-05-03 20:09 ET v1.0.1 install regression). NEW `probeKind: 'functionSource'` in `packages/server/src/setup/audit-upgrade.js` — fetches deployed Edge Function source via Supabase Management API, diffs against bundled `packages/server/src/setup/rumen/functions/<name>/index.ts` with marker check. Two new probes for `rumen-tick` and `graph-inference` deployed-source drift. Reports via `⊘ skipped` rather than auto-redeploy (user runs `init --rumen` to redeploy). Catches the case where someone deployed an out-of-date function version (Brad's manual workaround, or any prior install that pre-dates a bundled-source change).
- **Migration 017 in bundled mnestra-migrations + audit-upgrade probe set** (T3, ships in `@jhizzard/termdeck@1.0.2`). `packages/server/src/setup/mnestra-migrations/017_memory_sessions_session_metadata.sql` synced from canonical engram. `audit-upgrade.js` PROBES count grows 7→10 (mig 017 + 2 functionSource probes). Joshua's `petvetbid` and Brad's `jizzard-brain` both auto-apply mig 017 on their next `termdeck init --mnestra` re-run.

### Changed

- **`audit-upgrade.js` PROBES count 7→10** (mig 017 + rumen-tick functionSource + graph-inference functionSource). `tests/migration-loader-precedence.test.js` count assertion 16→17 to reflect bundled mig 017.
- **Stack-installer `_hookSignatureUpgradeAvailable()`** in `packages/stack-installer/src/index.js` — version-aware overwrite under `--yes`. Same TermDeck-marker safety gate as `init-mnestra`'s refreshBundledHookIfNewer. Timestamped backup before overwrite.

### Fixed

- **`memory_sessions` write path that hid for 2 days on Joshua's daily-driver Mnestra (`petvetbid`).** Last write before fix: 2026-05-01 20:40 UTC (Sprint 47 close). After v1.0.2 install + `termdeck init --mnestra`, every `/exit` writes a new `memory_sessions` row. Phase B verification: `select count(*) from memory_sessions` grows by 1 per session ended.
- **`supabase functions deploy` lost link-state across subprocess boundary on `termdeck init --rumen`** (Brad 2026-05-03 20:09 ET). `--project-ref` now explicit on every deploy call.
- **`audit-upgrade` reported "install up to date" while deployed Edge Function source was stale** (Brad 2026-05-03 20:09 ET). New functionSource probe diffs deployed source against bundled.
- **`tests/migration-loader-precedence.test.js` count assertions** — bumped 16→17 to reflect mig 017 sync.

### Notes

- **Three Codex audits caught real bugs in T3's WIP** (in addition to T3's own self-resolved items). 20:14 audit caught the `?on_conflict=session_id` upsert URL gap. 20:17/20:23/20:28 audits caught the packaging gap (the hook source path that `refreshBundledHookIfNewer()` reads was NOT in the v1.0.2 tarball; verified empirically by Codex running `npm pack --dry-run --json` with isolated cache). 20:23 audit caught the unsigned-custom-hook overwrite safety gap. **Without the auditor**, v1.0.2 would have shipped a memory_sessions write that fails on second fire (409 conflict), a hook-refresh function that reports `no-bundled` because the file isn't in the tarball, AND auto-overwrites genuinely-custom user hooks. Codex's pattern of reading T3's WIP rather than waiting for FIX-LANDED is what made these catches possible — codified as the canonical 3+1+1 pattern in `~/.claude/CLAUDE.md` going forward.
- **The 22 pre-existing `tests/project-taxonomy.test.js` failures** are now expected to flip green when Joshua runs `termdeck init --mnestra` post-v1.0.2 install. `refreshBundledHookIfNewer()` lands the new bundled hook with the Sprint-41 14-entry PROJECT_MAP onto Joshua's `~/.claude/hooks/memory-session-end.js`, which is what those tests probe. Per Codex's 20:35 note, that's the canonical Phase B acceptance signal.
- **Suite results.** 1011 tests / 986 pass / 22 fail / 3 skipped — net +36 pass, 0 new fails vs the v1.0.1 baseline. T3 added 14 new hook-refresh tests + 4 new functionSource probe tests + 5 new postMemorySession unit tests + 1 init-rumen-project-ref negative-validation test (114 targeted tests pass when running `node --test tests/stack-installer-hook-merge.test.js tests/init-mnestra-hook-refresh.test.js tests/init-rumen-project-ref.test.js tests/audit-upgrade.test.js tests/migration-loader-precedence.test.js`).
- **Migration 017 verified safe on `petvetbid`.** Codex ran the migration in a `BEGIN ... ROLLBACK` transaction probe; existing columns skipped (idempotent), constraint guard ran (no-op since petvetbid already has the unique constraint from the rag-system bootstrap), indexes created/skipped, no errors.
- **Sprint 51.5b dogfood audit DEFERRED** to post-v1.0.2 ship. Plan still at `docs/sprint-51.5b-dogfood-audit/` but waits until v1.0.2 closes both bug branches.
- **First sprint to use the canonical 3+1+1 pattern.** 3 Claude workers (T1 hook instrumentation, T2 schema audit, T3 fix + ship) + 1 Codex auditor (T4 independent verification of T1/T2/T3 throughout) + 1 orchestrator. Sprint clock: 51 minutes from inject to FIX-LANDED. Documented as the canonical pattern in `~/.claude/CLAUDE.md` § Sprint role architecture going forward.

## [1.0.1] - 2026-05-03

> **v1.0.1 hotfix — Sprint 51.5: installer upgrade-aware migration detection + `mnestra doctor` + GRAPH_LLM_CLASSIFY toggle + Class J failure taxonomy.** Triggered by Brad's 2026-05-02 schema-vs-package-drift report on `jizzard-brain` (`docs/INSTALLER-PITFALLS.md` ledger #13: Class A — schema drift + Class I — silent no-op) and his 2026-05-03 follow-up from a 4-project install pass (ledger #14: SUPABASE_DB_URL fallback, Vault dashboard panel removed, multi-arg `supabase secrets set` drops, multi-line clipboard shred, Rumen-002 templating coverage). Wave: `@jhizzard/termdeck@1.0.1` + `@jhizzard/termdeck-stack@0.6.1` + `@jhizzard/mnestra@0.4.1` + `@jhizzard/rumen@0.4.5`.

### Added

- **Stack-installer schema-introspection audit-upgrade** (T1). NEW `packages/server/src/setup/audit-upgrade.js` (~250 LOC) called at the top of every `termdeck init --mnestra` and `termdeck init --rumen` re-run. 7 frozen probes covering 5 mnestra + 2 rumen targets (`memory_relationships.weight`, `memory_recall_graph` RPC, `memory_taxonomy reclass`, `memory_items.reclassified_by`, `memory_items.source_agent`, `cron.job WHERE jobname='rumen-tick'`, `cron.job WHERE jobname='graph-inference-tick'`). Each missing artifact maps to its bundled migration; apply path runs `applyTemplating()` on Rumen 002 + 003 to substitute `<project-ref>` (regression test guards a future bypass). Returns `{probed, present, missing, applied, skipped, errors}` with a `{dryRun: true}` mode T2's `mnestra doctor` consumes for its schema-drift probe. 14 new tests in `tests/audit-upgrade.test.js`. Closes Class A — Schema drift on every existing install going forward.
- **`mnestra doctor` subcommand** (T2, ships in `@jhizzard/mnestra@0.4.1`). NEW `src/doctor.ts` (~390 LOC) + `src/doctor-data-source.ts` (~70 LOC). 4 probes: cron all-zeros (warns red when ≥6 of last 10 successful `rumen-tick` / `graph-inference-tick` runs reported zero work — Brad's 6-day silent no-op pattern); per-tick latency (yellow when p95 ≥ 5s); schema drift (delegates to T1's audit-upgrade dry-run, falls back to inline probes when T1 isn't yet bundled); MCP config path parity (`~/.claude.json` canonical vs `~/.claude/mcp.json` legacy). Output: green / yellow / red + recommendation per finding. Exit 0 / 1 / 2 (CI-suitable). 10 new tests in `tests/doctor.test.ts`; full engram suite 59/59 green. Companion migration `016_mnestra_doctor_probes.sql` ships 5 SECURITY DEFINER wrappers (`mnestra_doctor_cron_runs`, `_cron_job_exists`, `_column_exists`, `_rpc_exists`, `_vault_secret_exists`) granted to `service_role` so the doctor can read `cron.*` and `vault.*` without the user expanding grants. Closes Class I — Silent no-op as a symptom-side detector. Brad's outstanding 2026-04-28 suggestion landed.
- **Install-time GRAPH_LLM_CLASSIFY prompt** (T3). NEW `promptGraphLlmClassify()` in `init-rumen.js` (~40 LOC) surfaces "Enable AI-classified graph edges? (uses Haiku 4.5; ~$0.003 per 1k edges classified) [Y/n]" after `deployFunctions`, before `setFunctionSecrets`. Y-path sets `GRAPH_LLM_CLASSIFY=1` AND ensures `ANTHROPIC_API_KEY` lands in the per-secret CLI loop; N-path writes the manual-flip command to the install-output README. `--yes` and `--dry-run` accept default Y without prompting. Joshua's `petvetbid` — confirmed not currently set — picks up the flag on next `termdeck init --rumen` re-run.
- **Vault auto-apply via pg-direct `vault.create_secret`** (T3). NEW `ensureVaultSecrets()` in `init-rumen.js` (~80 LOC) probes `vault.secrets` for both required keys (`rumen_service_role_key` + `graph_inference_service_role_key`) and creates missing ones via the existing `pgRunner` connection — no extra subprocess, no PAT-gating, no Supabase Management API dependency. Brad's recovery script literally cloned `rumen_service_role_key → graph_inference_service_role_key`; T3 codifies that. Per-secret failures emit a SQL-Editor deeplink the user can click. **This closes the v1.0.1 vault-secret-create gap T1's audit-upgrade explicitly deferred** — T1 + T3 together cover every install path. 10 new tests in `tests/init-rumen-vault-deeplinks.test.js`.
- **Class J — Multi-arg CLI parse drift / multi-line clipboard shred** (T4). NEW row in `docs/INSTALLER-PITFALLS.md` § Failure-class taxonomy. NEW pre-ship checklist item #11 ("every external CLI call that mutates state runs ONE logical operation per invocation; every wizard step that needs the user to run a command emits a single-line `bash <oneshot.sh>` invocation"). NEW ledger entry #14 capturing all 5 of Brad's 2026-05-03 takeaways. The taxonomy is now A–J (10 classes), checklist 11 items. CLAUDE.md / AGENTS.md / GEMINI.md routing line updated to match (`9-class … 10-item` → `10-class … 11-item`). Mnestra global memory entry written for cross-project recall.
- **Bundled mnestra migrations 013–016 synced from canonical engram repo** (T1 + orchestrator close-out). The bundled `packages/server/src/setup/mnestra-migrations/` had drifted behind canonical: `013_reclassify_uncertain.sql`, `014_explicit_grants.sql`, `015_source_agent.sql` were canonical-engram-only despite shipping changelog notes claiming otherwise. The new `016_mnestra_doctor_probes.sql` (T2's SECURITY DEFINER wrappers) lands here too. Bundled count grew 12 → 16. `npm pack --dry-run` confirms all four ship. The drift was itself a Class H — Migration-runner blindness instance lurking in our own repo; the sync closes it.
- **Edge Function `SUPABASE_DB_URL` fallback** (T1, ships in bundled `packages/server/src/setup/rumen/functions/{rumen-tick,graph-inference}/index.ts` AND canonical `~/Documents/Graciella/rumen/supabase/functions/...` — `npm run sync-rumen-functions` re-converged after both sides were patched). Source now reads `Deno.env.get('DATABASE_URL') ?? Deno.env.get('SUPABASE_DB_URL')`. Supabase Edge Runtime auto-injects `SUPABASE_DB_URL`; users can now skip the manual `supabase secrets set DATABASE_URL=…` step entirely. Brad hand-patched all 4 of his deployed copies 2026-05-03; this lands the upstream fix. Companion rumen release: `@jhizzard/rumen@0.4.5`.

### Changed

- **`setFunctionSecrets()` in `init-rumen.js` refactored from single multi-arg invocation to per-secret CLI loop** (T3). Was: `supabase secrets set DATABASE_URL=… ANTHROPIC_API_KEY=… [OPENAI_API_KEY=…]` in one call. Now: N independent `supabase secrets set KEY=VAL` invocations in ordered sequence (DATABASE_URL → ANTHROPIC_API_KEY → optional OPENAI_API_KEY → optional GRAPH_LLM_CLASSIFY). Per-call exit code checked; stderr surfaced with the failing key name on failure. Brad observed `supabase` CLI v2.90.0 multi-arg parsing silently dropping secrets and even materializing stray entries (his email parsed as a secret name from a prior misparse). 11 new tests in `tests/init-rumen-secrets-per-call.test.js` including a v2.90.0-style multi-arg drop simulation that verifies the per-call refactor still lands every secret.
- **Wizard "click Vault in dashboard" instructions pivoted to SQL-Editor deeplinks** (T3, addresses Class B — Path mismatch). Supabase quietly removed/relocated the Vault dashboard panel. Replaced wizard text in `packages/server/src/setup/preconditions.js`, `packages/cli/src/init-rumen.js::printNextSteps`, and `docs/GETTING-STARTED.md:257` with a SQL-Editor deeplink shape: `https://supabase.com/dashboard/project/<ref>/sql/new?content=<encoded vault.create_secret call>`. `git grep -i "click.*vault\|vault.*tab\|vault.*dashboard" packages/ docs/` returns zero hits in wizard-side text after edits (only incident-history docs retain the phrase as commentary). Most users now never see this fallback because `ensureVaultSecrets()` auto-creates both keys via pg-direct.
- **Pre-ship checklist on `INSTALLER-PITFALLS.md` grew from 10 to 11 items** with the new Class J entry codifying "one logical operation per CLI invocation; never multi-line paste." Failure-class taxonomy table grew A–I → A–J. Three CLAUDE.md mirrors (CLAUDE.md / AGENTS.md / GEMINI.md) all carry the updated count.

### Fixed

- **Schema-vs-package drift on existing installs** (Brad's 2026-05-02 report, `INSTALLER-PITFALLS.md` ledger #13). Before v1.0.1: a user on the latest npm packages but with a project provisioned pre-Sprint-38 had no upgrade path that detected and applied bundled-but-not-yet-applied migrations. After v1.0.1: `auditUpgrade()` runs at the top of every `init --mnestra` / `init --rumen` re-run and applies missing artifacts in order. Brad's `jizzard-brain` self-heals on `termdeck init --rumen` after upgrading to v1.0.1.
- **`supabase secrets set` v2.90.0 multi-arg drops** (Brad's 2026-05-03 takeaway #3). Per-secret CLI loop; verified via simulated drop in tests.
- **Migration-loader test count assertions** (`tests/migration-loader-precedence.test.js`): bumped expected file count 12 → 16 to reflect the canonical-engram sync.

### Notes

- **Pre-existing test debt deferred** (out of v1.0.1 scope, opens P0 backlog item below). 22 failures in `tests/project-taxonomy.test.js` test `~/.claude/hooks/memory-session-end.js` — the bundled session-end hook on Joshua's daily-driver. Failures pre-existed Sprint 51.5 inject; explicitly out-of-scope for this hotfix; correlated with the live ingestion break recorded as P0 (see below).
- **🚨 Discovered during Sprint 51.5 inject — Joshua's `memory_sessions` ingestion broke on 2026-05-01.** Live psql probe of `petvetbid` 2026-05-03 17:50 ET shows `memory_sessions` last row `2026-05-01 20:40 UTC` (Sprint 47 close); `rumen_insights` last row 5 minutes after. Sprints 48 → 49 → 50 → 50.5 → 51 → 51.5 all completed but produced ZERO new memory_sessions rows. Mnestra-direct via MCP (`memory_remember`) is still working (memory_items grew during this sprint). Specifically the **session-end hook → memory_sessions write path** is broken. Likely Class E (hidden dependency) regression on Joshua's box, possibly Sprint 50 per-agent SessionEnd trigger introduced a code path that no longer writes memory_sessions. Banked as P0 in `docs/BACKLOG.md`; ships before any Sprint 52 feature work.
- **What's queued for Sprint 51.6 (next hotfix wave) or Sprint 52:** the P0 ingestion bug above; the cosmetic `evalAllZeros` renderer glitch T2 flagged (`sessions_processed=0 AND insights_generated=0=0` — trivial string-join fix); per-panel cwd switch (Joshua surfaced during Sprint 51.5 inject when T2's mnestra lane needed to operate in `~/Documents/Graciella/engram` but the panel was created in the termdeck cwd); Mnestra connection-topology routing layer (Brad's Shape C ask for cross-project Mnestra usage).
- **Sprint 51 (cost-monitoring panel) renumbered to Sprint 52.** The original Sprint 51 PLANNING.md is unchanged; only the version-target metadata shifts (still v1.1.0, but rides on top of v1.0.1 instead of v1.0.0).
- **Sprint 51.5 wall-clock:** plan stub authored 2026-05-02 21:14 ET (immediately following v1.0.0 release wave); inject 2026-05-03 ~17:21 ET; T4 DONE 17:25; T2 DONE 17:33; T3 DONE 17:55; T1 DONE 17:55; orchestrator close-out 18:00. ~40 minutes lane wall-clock + ~20 minutes orchestrator close. All-Claude per Sprint 50 close pattern.

## [1.0.0] - 2026-05-02

> **TermDeck 1.0** — A browser terminal multiplexer that runs Claude Code, Codex, Gemini, and Grok side-by-side, captures every session to a vector-backed memory store, and lets you fan a sprint across four agents from one dashboard. Eight sprints (44 → 50) of multi-agent substrate work graduate TermDeck from "Claude Code multiplexer with extras" to a production-ready mixed-agent orchestrator.

### What's new for users (the headline)

- **Open any agent with one click.** The dashboard panel chooser now renders a launcher button per registered adapter — Claude / Codex / Gemini / Grok. No more typing `codex` or `gemini` into a `shell` panel and hoping prompt-pattern detection reclassifies the type.
- **Panel labels name the agent that's actually running.** "shell" no longer lies when the panel is hosting Claude Code or Grok 4.20 Reasoning. The label reads from the resolved adapter `displayName`, not the launch command.
- **Memory captures every agent's `/exit`, not just Claude's.** Closing a Codex / Gemini / Grok panel writes a `session_summary` row to your Mnestra store with the source agent tagged. Recall calls can filter by which agent authored a row — `memory_recall(source_agents=['claude'])` returns only Claude-authored rows; the default returns all.
- **Sprints can run on mixed agents.** Declare `T1=codex / T2=gemini / T3=grok / T4=claude` in your sprint's PLANNING.md frontmatter and the inject script dispatches per-agent boot prompts to per-agent panels. Status posts in any of the four idioms get normalized into the canonical FINDING / FIX-PROPOSED / DONE shape.
- **Worktree-isolated parallel sprints.** Per-lane `git worktree add` paths mean a botched lane is `git worktree remove` away from clean main — no merge fights when four agents edit the repo simultaneously.
- **The full stack launches with one command.** `npm i -g @jhizzard/termdeck-stack && termdeck-stack start` boots Mnestra (memory store) + TermDeck (multiplexer) + auto-wires the Mnestra MCP block into every adapter's config. No repo clone, no manual `mcpServers` edits.

### How we got here (Sprint 44 → Sprint 50 narrative arc)

**Sprint 44** (foundation, 2026-04-30): Grok install, AGENTS.md sync, adapter registry skeleton with the 7-field contract, `docs/AGENT-RUNTIMES.md` documentation. Multi-agent ambition stated; nothing yet shipped to non-Claude users.

**Sprint 45** (adapters, 2026-05-01): Codex / Gemini / Grok adapter implementations under `packages/server/src/agent-adapters/<name>.js`. Each declares `spawn`, `prompt`, `parseTranscript`, `costBand`. Real-load test: opening a Codex panel correctly classifies as `codex-cli`; same for Gemini, Grok. Grok's SQLite-backed transcript shape was flagged for hook-layer follow-up — closed in v1.0.0.

**Sprint 46** (defensive sweep, 2026-05-01): the topbar's right-hand button row hadn't been audited end-to-end since the launcher refactor. Five silent-regression bugs caught and fixed (graph node hover tooltip missing, transcripts panel renderer/contract mismatches, python quick-launch initial badge wrong, zero client-side launcher-routing test coverage). Fourteen sub-optimal items deferred — most cleared by Sprint 49 + Sprint 50.5.

**Sprint 47** (mixed 4+1 infrastructure, 2026-05-01): the orchestrator-side machinery for declaring agents per-lane. Frontmatter parser (`packages/server/src/sprint-frontmatter.js`) + per-agent boot-prompt templates (`docs/multi-agent-substrate/boot-prompts/`) + cross-agent STATUS.md merger (`packages/server/src/status-merger.js`). The infrastructure ships; the actual mixed-agent dogfood ships in Sprint 49.

**Sprint 47.5 hotfix** (2026-05-02 12:30 ET): a routine `npm install -g @jhizzard/termdeck-stack@0.4.11` re-broke `memory_recall` because the installer wrote literal `${SUPABASE_URL}` placeholders into the mnestra MCP env block (Claude Code does not shell-expand MCP env values). Hotfixed with a concrete-value writer + a repair pass for already-broken installs. Mnestra @0.3.4 added a defense-in-depth fallback that ignores `${...}` placeholders.

**Sprint 48** (per-agent MCP auto-wire + global launcher, 2026-05-02 13:18 ET): Codex / Gemini / Grok adapters gained `mcpConfig` fields; a 207-LOC `mcp-autowire.js` helper writes the Mnestra MCP block into each agent's config in three different shapes (TOML append for Codex, JSON record-merge for Gemini, JSON array upsert for Grok). The global `termdeck-stack start|stop|status` launcher boots the full stack with one command. v0.17.0 + v0.5.0 coordinated release.

**Sprint 49** (real mixed-agent dogfood, 2026-05-02 14:08-14:20 ET): the first TermDeck sprint where T1/T2/T3 ran on **non-Claude agents**. T1=Codex (escapeHtml dedup), T2=Gemini (flashback pagination), T3=Grok (graph chip presets), T4=Claude (auto-wire-on-launch + termdeck-stack@0.5.1 publish). All four lanes posted DONE in 12 minutes wall-clock — fastest sprint since Sprint 41/44 and the proof-point that mixed-agent overhead is negligible. v1.0.0 trigger criteria met but bump deferred: three "visible signal lies to the human" UX bugs surfaced during the inject AND multi-agent memory plumbing had a write-side gap (only Claude `/exit` writes a memory row).

**Sprint 50 (this release)** closes both gaps. Per-agent SessionEnd hook trigger so `/exit` on any agent writes its work to memory; `source_agent` provenance column so consumers can filter; UX trust trio (launcher buttons + accurate panel labels + spinner that doesn't freeze mid-work). Sprint 50.5 (worktree-isolated mixed-agent dogfood close-out) validated all three under real load before this v1.0.0 bump.

### Sprint 50 lanes (this release)

- **T1 — Per-agent SessionEnd hook trigger (+ Grok SQLite extraction).** NEW server-side `onPanelClose(session)` in `packages/server/src/index.js` that fires the bundled hook with the correct adapter-specific payload when a non-Claude panel closes. EXTENDED adapter contract from 9 → 10 fields with `resolveTranscriptPath(session)`. Codex / Gemini / Grok adapters implement their transcript-path resolution. Grok's SQLite-backed transcript (`~/.grok/grok.db`) is extracted via `better-sqlite3` and synthesized into a JSONL feed for the existing parser — closes the Sprint 45 carry-over. Closing a Codex / Gemini / Grok panel now writes one `session_summary` row to `memory_items`; Claude's existing hook still fires for Claude panels (no double-write).

- **T2 — Memory `source_agent` column + recall filter.** NEW Mnestra migration `015_source_agent.sql` adds the column + partial index (NULL-skipping) + comment. Bundled hook (`packages/stack-installer/assets/hooks/memory-session-end.js`) populates `source_agent` from the new payload field. Mnestra `memory_recall` MCP tool gains an optional `source_agents: string[]` filter param — `memory_recall(source_agents=['claude'])` returns only Claude-authored rows, default returns all (no breaking change). Companion mnestra release: `@jhizzard/mnestra@0.4.0` minor bump because the new column + filter param is a user-visible feature.

- **T3 — UX trust trio (launcher buttons + panel labels + spinner freeze).** Adapter-driven launcher buttons in the panel chooser read from the `AGENT_ADAPTERS` registry — one labeled button per registered adapter; click POSTs to the existing panel-create API with the right `command`. Panel labels read from `meta.type` (resolved adapter `displayName`), not from the launch command — "shell" no longer lies when the panel hosts Claude or Grok. Status spinner CSS gains `animation-iteration-count: infinite` AND re-triggers on `lastActivity` updates — no more frozen spinner during long Claude reasoning phases.

- **T4 — Worktree-isolated dogfood close-out + v1.0.0 publish.** Sprint 50.5 — a worktree-isolated mixed-agent sub-sprint nested inside Sprint 50's normal close-out. Each lane picked a small Sprint 46 deferral (T1=codex URL state codec edge case, T2=gemini AGENT-RUNTIMES `approvalModel` doc, T3=grok stripAnsi spinner stripping, T4=claude dead `triggerProactiveMemoryQuery` cleanup). Per-lane `.worktrees/sprint-50.5-T{1..4}/` paths so a botched lane is `git worktree remove` away from clean main. Validates: (i) launcher buttons actually open the right agent, (ii) panel labels show correct names, (iii) per-agent SessionEnd hooks fire and write 4 rows with correct `source_agent` values, (iv) `memory_recall(source_agents=['claude'])` returns expected rows.

### Validation

- **Memory writes from all four agents confirmed.** Closing one of each (Codex / Gemini / Grok / Claude) produced 4 new `session_summary` rows in `memory_items` with the correct `source_agent` values. Verified via `select source_agent, count(*) from memory_items where source_type='session_summary' group by 1`. Sprint 50 inject baseline = 12 rows; post-50.5 dogfood = 16+ rows.
- **Recall filter works.** `memory_recall(source_agents=['claude'])` returns Claude-authored rows; `memory_recall(source_agents=['claude','codex'])` returns the union; `memory_recall()` returns all (no breaking change to existing callers).
- **Launcher buttons present.** Panel chooser shows one button per registered adapter. Each opens a panel with the correct `meta.type` AND label.
- **Spinner stays alive.** Long Claude reasoning phases (manually reproduced in the Sprint 50.5 dogfood T4 lane) keep the spinner animating throughout. Status field transitions to `idle` cleanly when the agent posts DONE.
- **Worktree isolation holds.** All four Sprint 50.5 lanes DONE in their own worktrees; merge back to main with zero conflicts (files touched are disjoint by design — graph.js / AGENT-RUNTIMES.md / session.js / app.js).
- **Test coverage:** all Sprint 49+ tests stay green plus new coverage for the per-agent hook trigger (~6 tests), the `source_agent` column + filter (mnestra-side), the launcher-button rendering, the spinner CSS, and each Sprint 50.5 lane's deferral fix. Pre-existing 22 failures in `tests/project-taxonomy.test.js` remain unrelated (out-of-repo `~/.claude/hooks/memory-session-end.js` PROJECT_MAP, Sprint 40 carry-over).
- **Mnestra full test suite stays green** including the new `source_agents` filter coverage.

### What's locked in for v1.x

- **Adapter contract is now 10 fields:** `name`, `displayName`, `spawn`, `prompt`, `parseTranscript`, `costBand`, `acceptsPaste`, `mcpConfig`, `sessionType`, `resolveTranscriptPath`. (Sprint 50.5 T2 staged an 11th field — `approvalModel: 'auto' | 'per-step'` — as a docs-only addition; code rollout deferred to a v1.x sprint.) Registered in `packages/server/src/agent-adapters/index.js`. Adding a new agent (Aider, Continue, etc.) is a 100-200 LOC adapter file plus a one-line registration.
- **Sprint frontmatter format:** YAML-subset top-level + `lanes:` sequence with `tag` + `agent`. Forward-compatible — pre-Sprint-47 PLANNING.md files have no frontmatter and default all lanes to `claude`.
- **Memory schema:** `memory_items.source_agent text` (nullable for historical rows pre-Sprint-50). Recall RPC accepts optional `source_agents text[]` filter. The 12 historical `session_summary` rows from Sprint 48-49 were backfilled with `source_agent='claude'` (they came from Claude Code).

### What's queued for v1.x (forward look)

- **Sprint 51 — Cost-monitoring expandable dashboard panel.** Reads each adapter's `costBand` field and the new `source_agent` column to surface "your last 30 days of memory spend, broken down by source LLM" + per-active-panel running cost estimate for per-token agents. v1.x feature — the data plumbing ships in v1.0.0; Sprint 51 ships the visible panel.
- **Grok 16-sub-agent observability.** Grok's `task` tool emits parseable stdout patterns that TermDeck's `session.js` analyzer can pick up. UX: collapsible tree / side-metadata pane showing agent pills.
- **`mnestra doctor` subcommand** (Brad's third upstream suggestion 2026-04-28). Cross-repo work; queued.
- **Optional `~/.zshrc` source-line offer** in the stack-installer wizard so standalone Claude Code launches outside TermDeck inherit secrets.
- **`approvalModel` adapter-contract code rollout** (Sprint 50.5 T2 shipped docs only). Future orchestrator logic can right-size lane budgets (1.5-2× wall-clock for `per-step` agents like Gemini) and flag operators when overnight orchestration includes a `per-step` lane.
- **Sprint 40 carry-overs:** harness session-end hook PROJECT_MAP forward-fix; analyzer broadening for `PATTERNS.error` case-sensitivity gaps; LLM-classification pass on chopin-nashville-tagged uncertain rows.

### Notes

- **Coordinated v1.0.0 release wave:** `@jhizzard/termdeck@1.0.0` + `@jhizzard/termdeck-stack@0.6.0` + `@jhizzard/mnestra@0.4.0`. Rumen @0.4.4 unchanged.
- **Why a major bump for v1.0.0** rather than 0.19.0: this release crosses the "production-ready for outside users running mixed 4+1" threshold the project has been building toward since Sprint 44. The auto-wire path closes the day-zero MCP gap (Sprint 48), the dogfood proves the multi-agent path under real load (Sprint 49), the memory plumbing gives every agent an audit trail (Sprint 50 T1+T2), and the UX trust signals stop lying to humans (Sprint 50 T3). No breaking changes to CLI flags, config schema, or HTTP API — but the user-visible surface is qualitatively different from what 0.x shipped.
- **Sprint 50 wall-clock:** _(orchestrator stamps at sprint close; budgeted 30-40 min per PLANNING.md — longer than the 12-minute Sprint 49 record because T2 spans two repos, T1 includes Grok-SQLite work, and T4 is self-recursive.)_
- **Mnestra `source_agents` filter is the foundation for Sprint 51's cost panel.** Don't ship them simultaneously; v1.0.0 ships the data plumbing, v1.x adds the visible panel that reads it.
- **Credits:** TermDeck's multi-agent substrate exists because Brad Stulberg pushed for it from his own orchestrator — Brad's "trust Claude most" preference, his 2026-04-26 Mnestra ingestion debugging session, his three upstream suggestions (2026-04-28), and his per-orchestrator-instance sprint cadence pressure surfaced gaps that single-user development would have missed. Joshua paired Sprint 50 close-out with `feedback_gemini_approval_heavy.md` so future operators size approval-heavy lanes correctly.

## [0.6.0] - 2026-05-02

> **Scope:** `@jhizzard/termdeck-stack@0.6.0` minor — companion to TermDeck v1.0.0. The minor (rather than patch) bump reflects two material changes from the Sprint 50 wave: (a) the bundled session-end hook now ships the `source_agent` payload field consumed by Mnestra v0.4.0's new column; (b) the audit-trail convention pins this stack-installer build against `@jhizzard/termdeck@1.0.0`. Patch versions in 0.5.x covered hook-registration polish (Stop → SessionEnd, secrets.env fallback) and auto-wire-on-launch; the v1.0.0 inflection promotes the next stack-installer release to a clean minor.

### Changed — Sprint 50 T1+T2 fold-in

- **`packages/stack-installer/assets/hooks/memory-session-end.js`** populates `source_agent` from the new payload field. The TermDeck server-side `onPanelClose(session)` (Sprint 50 T1) passes `source_agent: adapter.name` in the hook stdin payload; the bundled hook reads it and adds it to the Mnestra `postMemoryItem` POST. Backwards-compatible: pre-v1.0.0 payloads omit the field, which the hook handles by leaving the column NULL (the 12 historical rows were backfilled with `source_agent='claude'` at the migration step).
- **`packages/stack-installer/package.json`** version bumped 0.5.1 → 0.6.0 per RELEASE.md convention.

### Validation

- `node --test packages/stack-installer/tests/launcher.test.js packages/stack-installer/tests/launcher-autowire.test.js` — 18/18 green (carry-over from 0.5.1).
- `node --test tests/stack-installer-hook-merge.test.js` — _(orchestrator runs at sprint close after Sprint 50 T2 lands the source_agent payload field — expected count to climb from 55 to ~57 with the new field test)_.
- Live-fire: closing a Codex panel writes `source_agent='codex'` to `memory_items` (validated by Sprint 50.5 dogfood).

### Notes

- **Audit-trail convention:** every `@jhizzard/termdeck-stack` release declares which `@jhizzard/termdeck` version it validated against. v0.6.0 validates against v1.0.0.
- **No breaking changes** to the `start | stop | status` subcommands or the wizard flow.
- **MCP autowire** (introduced in 0.5.0, hardened in 0.5.1) continues to run unchanged on launcher startup.

## [0.18.0] - 2026-05-02

### Added — Sprint 49: Real mixed-agent dogfood (v1.0.0 inflection deliberately deferred to Sprint 50)

> **Theme:** Sprint 49 (4+1, **12 minutes** wall-clock — fastest sprint since 41/44, comfortably under Sprint 47's 28-min ceiling). First TermDeck sprint where T1/T2/T3 ran on **non-Claude agents** (Codex / Gemini / Grok respectively). All four lanes posted DONE with non-Claude lanes shipping meaningful real work — proving Sprint 48's per-agent MCP auto-wire holds under real load AND Sprint 47's frontmatter parser + boot-prompt resolver + status-merger work end-to-end across mixed CLI idioms. **v1.0.0 trigger criteria met but bump deferred:** three "visible signal lies" UX bugs surfaced during the inject (launcher buttons missing, panel labels show "shell", status spinner freezes mid-work) AND multi-agent memory plumbing is not yet in place (only Claude's `/exit` writes a `session_summary` row today; Codex / Gemini / Grok exits are silent). Sprint 50 ships those gaps + the worktree-isolated dogfood close-out; v1.0.0 lands at Sprint 50 close.

### Lanes

- **T1 — Codex (real lane, DONE @ 14:12 ET):** `escapeHtml` deduplication in `packages/client/public/app.js`. Two behaviorally identical local definitions (around the panel metadata + health badge sections) lifted to a single canonical client-utility helper near the top of the module. NEW `tests/escapehtml-client.test.js` (4 cases: empty string, `<script>`, `&` / `&amp;`, `"`, `'`) confirms one definition + unchanged escaping semantics. Sprint 46 deferral cleared. Codex ran clean — no idiom-mismatch friction at the merger boundary.

- **T2 — Gemini (real lane, DONE @ ~14:19 ET, stamp-drifted to 14:50):** Flashback history pagination. EDIT `packages/client/public/flashback-history.{js,html}` — added pagination controls (Prev/Next + Page X of Y), page-size 25, state stored in localStorage to survive reloads, page reset to 1 on filter change. **Plus two unsolicited Sprint 46 deferrals** Gemini grabbed without being asked: title attributes on `btn-status`/`btn-config` (cosmetic UX) and `escapeRegex` helper in `launcher-resolver.js` (regex-injection defensive). Marked `T2: DONE (extra)` in STATUS to flag the scope creep — acceptable here because they're real fixes, but Sprint 50 lane briefs will explicitly forbid out-of-lane edits. **Gemini surfaced two Gemini-specific concerns** worth preserving: (a) approval-heavy lifecycle (Gemini paused for explicit user approval on nearly every step — captured in `feedback_gemini_approval_heavy.md`); (b) timestamp drift (Gemini stamped its STATUS posts at 14:35–14:58 ET when actual time was 14:14–14:19 ET — file mtimes confirm the drift; `source_agent` provenance column from Sprint 50 will let consumers filter Gemini timestamps out of trust-grade recalls if the drift continues).

- **T3 — Grok (real lane, DONE @ ~14:14 ET, stamp-drifted to 14:28):** Graph viewer chip filter "All / None" preset buttons. EDIT `packages/client/public/graph.{js,html}` + `style.css` (~73 LOC across 3 files) — preset buttons render above the chip row, "All" enables every chip, "None" disables every chip, individual chip clicks still work post-preset, boundary-state disabled rendering. CSS is a subtle pill variant of the existing `.gf-chip` (no new colors / fonts). Sprint 46 Surface-2 deferral cleared. Grok ran fast (DONE in <6 minutes from inject) — confirms Sprint 47's smoke-test self-report that Grok handles bracketed-paste cleanly under Bun + OpenTUI.

- **T4 — Claude (auto-wire-on-launch + termdeck-stack@0.5.1 publish, DONE @ 14:20 ET):** EDIT `packages/stack-installer/src/launcher.js` (+~80 LOC: `loadTermdeckExports` realpath-walk-up to find `@jhizzard/termdeck`'s package root + `require()` `agent-adapters/index.js` + `mcp-autowire.js` against absolute paths preserving the zero-dep posture; `autowireMcp(adapters, ensureFn, opts)` iterates adapter registry with try/catch per-adapter, returns `{wired,unchanged,skipped,errored}`; integrated as Step 2/4 in `startStack()` with `--no-wire` short-circuit). NEW `packages/stack-installer/tests/launcher-autowire.test.js` (8 tests covering per-adapter dispatch + Claude skip + outcome aggregation + error containment + record/array/null inputs + null-return-on-unrelated-paths). 18/18 launcher tests + 55/55 hook-merge tests + 30/30 mcp-autowire tests all green. **`@jhizzard/termdeck-stack@0.5.1` published** stamping the post-Sprint-48 hook polish (Stop → SessionEnd migration + secrets.env fallback) AND the auto-wire-on-launch deliverable.

### Validation

- **All four lanes DONE** with verified file changes at the claimed sites: `git diff --stat` shows 309 lines added across 12 files. T1's `tests/escapehtml-client.test.js` exists at the right path with the right cases. T2's `flashback-history.{js,html}` carry the pagination controls. T3's `graph.{js,html}` + `style.css` carry the preset buttons. T4's `launcher.js` carries the auto-wire integration.
- **Mixed-agent merger held up under real load.** The status-merger logic from Sprint 47 T4 didn't run end-to-end during the sprint itself (each lane wrote canonical-Claude-shape STATUS posts, agents being more uniform than expected) — but the substrate proved the path. Sprint 50's worktree-isolated dogfood close-out gives the merger another real-load test.
- **`memory_items.source_type='session_summary'` row count** climbed from 4 → 8 during the post-Sprint-48 verification (Joshua's `/exit` of the four Sprint 48 panels). Sprint 50's per-agent hook trigger will let the next 4-lane close land 4 rows (one per lane), not 1.
- **TermDeck@0.18.0** root version bump (was 0.16.1 + 0.17.0 transitively this morning). Minor bump because three non-Claude lanes shipped real user-visible features (graph chip presets, flashback pagination, escapeHtml client utility) plus the `termdeck-stack` companion release.
- Pre-existing 22 failures in `tests/project-taxonomy.test.js` remain unrelated (out-of-repo `~/.claude/hooks/memory-session-end.js` PROJECT_MAP, Sprint 40 carry-over).

### Notes

- **v1.0.0 deliberately deferred to Sprint 50.** All four trigger criteria from the Sprint 49 PLANNING met (T1+T2+T3+T4 DONE, ≥1 non-Claude lane real work, no regressions). The bump was held back because three v1.0.0-blocker UX bugs surfaced during the inject (the visible signals lying to humans erodes trust faster than feature completeness builds it) AND the multi-agent memory plumbing has a write-side gap (3 of 4 panels' work is uncaptured to Mnestra on `/exit`) AND a read-side gap (no provenance filter so Joshua's "trust Claude most" preference is unmodelable). Sprint 50 ships those + a worktree-isolated dogfood close-out that doubles as v1.0.0 confidence test.
- **NEW architecture doc:** [`docs/MULTI-AGENT-MEMORY-ARCHITECTURE.md`](docs/MULTI-AGENT-MEMORY-ARCHITECTURE.md) — single source of truth for Sprint 50 T1 (per-agent SessionEnd hook trigger) + T2 (`source_agent` column + recall filter). Sprint 50 PLANNING.md references this doc rather than re-stating the rationale.
- **Gemini-specific behavior memorialized.** `~/.claude/projects/.../memory/feedback_gemini_approval_heavy.md` captures the per-step approval pattern + the timestamp drift + the scope-creep tendencies Sprint 49 surfaced. Future sprints budget Gemini lanes at 1.5-2× wall-clock and explicitly tell Gemini to use `date '+%Y-%m-%d %H:%M ET'` per timestamp.
- **Sprint 51 vision recorded:** cost-monitoring expandable dashboard panel (`memory/project_cost_monitoring_panel.md`). Reads each adapter's `costBand` field (already declared) and surfaces subscription-vs-per-token billing exposure per active panel. v1.x feature, post-v1.0.0.

## [0.5.1] - 2026-05-02

> **Scope:** `@jhizzard/termdeck-stack@0.5.1` patch — stack-installer-only release. The coordinated `@jhizzard/termdeck@0.18.0` root-version bump for Sprint 49 lands in the entry above this one.

### Fixed — Sprint 48 close-out hook polish (committed `dd5173c` 2026-05-02 13:30 ET, published in 0.5.1)

- **`assets/hooks/memory-session-end.js` Stop → SessionEnd registration.** The bundled session-end hook was registered at `~/.claude/settings.json` under `hooks.Stop`, which fires on every assistant turn end (Claude Code's `Stop` is "this assistant message just stopped streaming"). Intent was once-per-`/exit`. The miss multiplied the embedding pipeline's load by ~10-20× per active Claude Code session — every turn re-embedded the entire transcript suffix and re-POSTed to Mnestra. Fix: re-registered as `hooks.SessionEnd` in `_mergeSessionEndHookEntry` (renamed from `_mergeStopHookEntry`). Added a migration branch that detects the pre-0.5.1 Stop-shape entry and rewrites it under SessionEnd on next `npx @jhizzard/termdeck-stack` so existing users heal transparently — no manual intervention required.
- **`assets/hooks/memory-session-end.js` `~/.termdeck/secrets.env` fallback.** The bundled hook's `readEnv()` only consulted `process.env` for `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `OPENAI_API_KEY`. Inside TermDeck panels Sprint 48 T4's PTY env-merge pre-loads these, but standalone Claude Code launches (outside TermDeck — `claude` from a regular shell that hasn't sourced `secrets.env`) hit env-var-missing on every fire and silently no-op'd. Fix: when `process.env` is missing one of the required keys, the hook now parses `~/.termdeck/secrets.env` via the same dotenv-subset reader the launcher and `mcp-autowire.js` use (KEY=value, optional matched single/double quotes, `${VAR}`-placeholders rejected). EDIT `tests/stack-installer-hook-merge.test.js` covers both the rename and the migration branch — 53/53 → 55/55 green at 0.5.1 close.

### Added — Sprint 49 T4: Auto-wire-on-launch in `termdeck-stack start`

- **`startStack()` now auto-wires MCP for non-Claude adapters.** Sprint 48 T1 shipped `packages/server/src/mcp-autowire.js` `ensureMnestraBlock(adapter)`, and Sprint 48 T1/T2/T3 shipped per-agent `mcpConfig` declarations on the Codex/Gemini/Grok adapters — but nothing wired the helper into the global launcher. `npx @jhizzard/termdeck-stack start` only booted the three processes; non-Claude agents only gained their Mnestra block on TermDeck panel spawn. Sprint 49 T4 closes that loop: after Step 1 (secrets) and before Step 3 (mnestra spawn), `startStack()` walks up from `fs.realpathSync(termdeckBinary)` to find the installed `@jhizzard/termdeck` package root, requires `packages/server/src/agent-adapters/index.js` + `packages/server/src/mcp-autowire.js` against absolute paths (the installer is zero-dep so it cannot require these by package name), and calls `ensureMnestraBlock(adapter)` for every adapter where `mcpConfig` is non-null. **Idempotent:** re-running `start` against an already-wired environment shifts every adapter from `wired` → `unchanged` (the helper's per-shape detect-existing branches return `{ unchanged: true }` on no-op writes). **Per-adapter error containment:** helper exceptions on a single adapter don't abort the loop — they're captured under `errored[]` so the next adapter is still tried. **Step output:** `Step 2/4: Auto-wiring MCP ......... OK  codex (wrote), gemini (unchanged), grok (wrote)`. Mnestra/TermDeck step labels renumbered from `2/3`/`3/3` to `3/4`/`4/4` to make room. Claude is intentionally never passed to the helper (its adapter declares `mcpConfig: null` since `~/.claude.json` is user-managed via `claude mcp add`).
- **EDIT `packages/stack-installer/src/launcher.js` (+~80 LOC)** — adds `loadTermdeckExports(termdeckBinary, fs)` (walk-up package resolution), `autowireMcp(adapters, ensureMnestraBlockFn, opts)` (per-adapter dispatch with error containment), and integration in `startStack()`. New module exports: `_autowireMcp` and `_loadTermdeckExports` for unit-test access; `opts.noWire: true` short-circuits the step with a `SKIP (--no-wire)` line. DI seam: `_deps.termdeckExports` lets tests inject adapters + ensureFn directly without faking the package-resolution walk.
- **NEW `packages/stack-installer/tests/launcher-autowire.test.js` (8 tests)** — covers per-adapter dispatch, Claude/null-mcpConfig skip (Claude must never be passed to the helper), wired/unchanged/skipped outcome aggregation, helper-error containment (errors per-adapter don't abort the loop), record-and-array adapter shape acceptance, exported-as-function contract for both helpers, and `_loadTermdeckExports` null-return on unrelated paths + null/undefined/missing-file inputs. Idempotency proxy at the third test asserts a follow-up call with an `unchanged`-only stub returns `{ wired: [], unchanged: [...] }` for all three non-Claude adapters.

### Validation

- **Tests:** `node --test packages/stack-installer/tests/launcher.test.js packages/stack-installer/tests/launcher-autowire.test.js` — **18/18 green** (10 pre-existing launcher + 8 new launcher-autowire). `node --test tests/stack-installer-hook-merge.test.js` — **55/55 green** (covers the Stop → SessionEnd rename + migration). `node --test tests/mcp-autowire.test.js` — **30/30 green**, helper unchanged.
- **Acceptance criteria from `docs/sprint-49-mixed-agent-dogfood/T4-auto-wire-on-launch-and-stack-051.md`:** (1) `node -e "const L=require('./packages/stack-installer/src/launcher'); console.log(typeof L._autowireMcp);"` prints `function` ✓. (2) idempotent under repeat helper-`unchanged` returns ✓. (3) Claude (mcpConfig: null) never passed to ensureMnestraBlock ✓ — explicit test fixture asserts `calls.length === 0` after a Claude-only adapter set. (4) `npm view @jhizzard/termdeck-stack version` returns `0.5.1` — pending publish at sprint close. (5) CHANGELOG entry covers both deliverables ✓ — this block. (6) No regressions ✓ — counts above.

### Notes

- **`@jhizzard/termdeck-stack@0.5.1` patch bump** (was 0.5.0 at Sprint 48 close). Hook hotfix is a bug fix; auto-wire-on-launch extends the existing user-visible feature (`termdeck-stack start`) without a contract change. No new subcommands; backwards-compatible.
- **Verification owed (post-publish, manual):** `npm i -g @jhizzard/termdeck-stack@0.5.1 && termdeck-stack start` from a clean Codex/Gemini/Grok install (no `mcpServers` block) and confirm all three configs gain the Mnestra block. Idempotency check: re-run `termdeck-stack start` and confirm Step 2/4 reports all three as `(unchanged)`.
- **Why one bump for two deliverables:** hook hotfix and auto-wire-on-launch ride the same publish event (Sprint 49 close), so one CHANGELOG block is more useful than splitting across 0.5.1 (hotfix-only) and 0.5.2 (auto-wire-only) — the per-fix granularity wouldn't survive a week and the publish cadence matches a single coordinated release.
- **Hot-load behavior preserved:** Codex (TOML append) and Grok (JSON record-merge with id-based upsert in `_mergeMnestraIntoGrokSettings`) both pick up the wired Mnestra block on the next agent turn without a CLI restart. Gemini requires a `gemini` restart to discover MCP servers (documented in `gemini.js` mcpConfig comment); the helper's panel-spawn write timing is correct because the user types `gemini` in the TermDeck panel after the helper has already run.

## [0.17.0] - 2026-05-02

### Added — Sprint 48: Per-agent MCP auto-wire + global stack launcher

> **Theme:** Sprint 48 (4+1, **17 minutes** wall-clock — well under Sprint 47's 28-min ceiling, comparable to Sprints 43 (17m) / 45 (16m)). Closes two outside-user UX gaps in one minor release: (1) per-agent MCP auto-wire so non-Claude agents (Codex/Gemini/Grok) can call `memory_recall` without manual `mcpServers`-block edits; (2) global `termdeck-stack start|stop|status` subcommands so users can boot the full stack with `npm i -g @jhizzard/termdeck-stack && termdeck-stack start` — no repo clone required. v1.0.0 stays gated on real mixed-agent dogfood (Sprint 49).

### Lanes

- **T1 — Codex MCP auto-wire + shared `mcp-autowire.js` helper (DONE @ +16min).** NEW `packages/server/src/mcp-autowire.js` (207 LOC). Single export `ensureMnestraBlock(adapter, opts?)` plus `readSecrets`, `expandTilde`, and `_internals` for tests. Helper supports THREE adapter shapes (precedence top → bottom): (1) escape-hatch `merge(rawText, {secrets}) => {changed, output}` for non-record schemas (Grok's array shape) — adapter owns parse+mutate+serialize; (2) JSON-record-merge `mcpServersKey + mnestraBlock(): object` for Gemini's record shape; (3) TOML-append / JSON-append `mnestraBlock(): string + detectExisting` for Codex's TOML. Adapters declare exactly one shape; helper dispatches on field presence. `readSecrets()` rejects literal `${VAR}` placeholders per Sprint 47.5 hotfix discipline. EDIT `packages/server/src/agent-adapters/codex.js` — adds `mcpConfig` field with TOML basic-string escaping (backslashes + quotes) and line-anchored `[mcp_servers.mnestra]` detection regex. EDIT `packages/server/src/agent-adapters/claude.js` — `mcpConfig: null` to satisfy `tests/agent-adapter-codex.test.js:62-66` parity check (Claude's MCP is user-managed via `claude mcp add` / `~/.claude.json`; helper treats null as `{ skipped: true, reason: 'user-managed' }`). EDIT `docs/AGENT-RUNTIMES.md` § 5 — adapter contract bumped from 7 → 9 fields (adds `acceptsPaste` from Sprint 47 + `mcpConfig` from this lane). NEW `tests/mcp-autowire.test.js` (414 LOC, 30 tests covering all three shapes + idempotency + tilde-expansion + parent-dir creation + secrets parser + Codex CLI live integration smoke). NEW `tests/agent-adapter-codex-mcpconfig.test.js` (107 LOC, 9 tests). T1 was the critical path for T3 integration (T3 waited on the helper API).

- **T2 — Gemini MCP auto-wire (DONE @ +5min).** EDIT `packages/server/src/agent-adapters/gemini.js` — `mcpConfig` field with `path: '~/.gemini/settings.json'`, `format: 'json'`, `mcpServersKey: 'mcpServers'`, and `mnestraBlock({secrets}) → {mnestra: {command, env}}`. Concrete-or-omit env discipline (empty/missing secrets dropped from env rather than written as empty strings; matches Sprint 47.5 hotfix). The `type: 'stdio'` field used in Claude's `~/.claude.json` is intentionally omitted — Gemini schema doesn't include it (Claude-Code-specific extension). NEW `tests/agent-adapter-gemini-mcpconfig.test.js` (95 LOC, 5 tests). Gemini discovers MCP servers at startup, so the helper's panel-spawn write timing is correct (writes before user types `gemini`).

- **T3 — Grok MCP auto-wire (DONE @ ~+17min).** **Critical schema correction:** Grok's MCP config is `mcp.servers` ARRAY (`UserSettings.mcp.servers`, where each entry is `{id, label, enabled, transport, command?, args?, env?, cwd?, url?, headers?}`), NOT a record `mcpServers.NAME`. Brief's assumed schema was wrong; T3 verified against `/usr/local/lib/node_modules/grok-dev/dist/utils/settings.{d.ts,js}` (the package on disk is `grok-dev` v1.1.5, not the `superchargedt/grok-cli` URL the brief referenced). T3 surfaced the divergence as a **coordination ask** to T1; orchestrator endorsed Option A (`mcpConfig.merge` escape-hatch) rather than forking a one-off `grok-mcp-wire.js`. Hot-load behavior verified: `agent.js` calls `loadMcpServers()` at the start of every agent turn (3 sites: stream, batch, child-agent), so MCP changes are picked up on the next user message — **no Grok restart required**. EDIT `packages/server/src/agent-adapters/grok.js` (+139 LOC) with the `merge` function: array-shape upsert by `id === 'mnestra'`, deep-equal short-circuit for idempotency, preserves all other top-level keys + `~/.grok/user-settings.json`'s existing `defaultModel` value. NEW `tests/agent-adapter-grok-mcpconfig.test.js` covering the merge logic + acceptance criteria.

- **T4 — Global stack launcher + PTY env-var propagation (DONE @ +21min).** **Deliverable 1 — global launcher:** NEW `packages/stack-installer/src/launcher.js` (290 LOC) — exports `startStack/stopStack/statusStack` plus dep-injection helpers `_readSecrets`, `_whichBinary`, `_resolveMnestraInvocation`, `_ensureFirstRunConfig`, `_probeHealth`, `_spawnDetached`. Pidfile at `~/.termdeck/stack.pid` (`{mnestraPid, termdeckPid, port, startedAt}`). Detached spawns log to `/tmp/termdeck-mnestra.log` and `/tmp/termdeck-server.log`. Health-checks via `:37778/healthz` and `:$port/api/health` (mirrors `scripts/start.sh`). Rumen reports status via Supabase Edge Function metadata (no local PID — it's a cron-driven Edge Function). EDIT `packages/stack-installer/src/index.js` — added `_maybeRunSubcommand(argv)` that lazy-requires `./launcher` and routes `start|stop|status` before the wizard. Bare `npx @jhizzard/termdeck-stack` still runs the wizard (backwards compat preserved). NEW `packages/stack-installer/tests/launcher.test.js` (10 tests: secrets parser, whichBinary, resolveMnestraInvocation, stopStack pidfile contract). **Deliverable 2 — PTY env propagation:** EDIT `packages/server/src/index.js` — module-scoped `readTermdeckSecretsForPty()` (cached, lazy, `${VAR}`-placeholder-rejecting) + `_resetTermdeckSecretsCache()` test hook. `spawnTerminalSession` merges secrets.env keys NOT already set in `process.env` between `...process.env` and the TermDeck-specific keys, so user shell env still wins for non-secret vars but secrets.env fills the gap. Unblocks `memory-session-end.js` (was hitting `env-var-missing` on every fire — 0 `session_summary` rows had ever landed in Mnestra; verification owed post-merge). NEW `packages/server/tests/pty-env-propagation.test.js` (5 tests including a live PTY integration test that spawns `node -p 'process.env.SUPABASE_URL'` via `@homebridge/node-pty-prebuilt-multiarch`). EDIT `docs/GETTING-STARTED.md` — Tier-0 Quickstart now leads with `npm i -g @jhizzard/termdeck-stack && termdeck-stack start`. EDIT `packages/stack-installer/assets/hooks/README.md` — documents the v0.17.0 PTY-spawn env merge.

### Validation

- **Test counts at sprint close:** 326/326 green across the sprint-48-affected files (40 server + 10 launcher + 276 root including all 4 new adapter mcpconfig tests + the 30-test mcp-autowire suite). T1 self-reported 876/876 on his lane's curated set. The pre-existing 22 failures in `tests/project-taxonomy.test.js` are unrelated (out-of-repo `~/.claude/hooks/memory-session-end.js` PROJECT_MAP, Sprint 40 carry-over flagged in PLANNING.md).
- **Codex CLI live integration smoke:** `mcp-autowire.test.js` includes a HOME-isolated test that writes a Codex `[mcp_servers.mnestra]` block + runs `codex --help` and asserts exit 0 — confirms the TOML our helper emits is parser-valid.
- **Coordination outcome:** T3's schema deviation was caught at +6min, surfaced as a coordination ask, orchestrator-resolved at +12min, T1 shipped both helper paths at +16min, T3 shipped consuming Option A. Total coordination overhead: ~10 minutes of ask-and-answer; no rework. The `merge` escape-hatch is now the canonical path for any future agent whose schema doesn't match Codex's TOML-block or Gemini's JSON-record patterns.

### Notes

- **`@jhizzard/termdeck-stack@0.5.0` minor bump** (was 0.4.12 after Sprint 47.5 hotfix) — adds three new user-visible subcommands (`start`, `stop`, `status`), graduating the package from "one-shot setup wizard" to "persistent stack launcher." Per RELEASE.md "New user-visible feature, no breaking changes" → minor bump.
- **`@jhizzard/termdeck@0.17.0` minor bump** (was 0.16.1) — per-agent MCP auto-wire is a new user-visible feature (the helper module is exported; SDK consumers can call it directly). Adapter contract changed shape (7 → 9 fields) — additive only, no existing field semantics changed, so this is not a breaking change.
- **v1.0.0 stays gated on real mixed-agent dogfood (Sprint 49).** The plan was: per-agent MCP auto-wire AND a real mixed-agent sprint dogfooding the path → v1.0.0. Sprint 48 closed the auto-wire gap; Sprint 49's mixed-agent dogfood (now unblocked because T1-T3 actually shipped the wiring) will close the dogfood gate. Realistic v1.0.0 release: Sprint 49 close, ~mid-May 2026.
- **Verification still owed (post-publish, manual):** `memory-session-end.js` landing rate moves from 0% to >0% — requires running a real Claude Code session in the rebuilt server and watching `memory_items` for `source_type='session_summary'` rows. Cannot be unit-tested (depends on Mnestra + OpenAI + Claude Code stop-hook all firing). Orchestrator should validate before signing off on v0.17.0.
- **Pre-Sprint-48 hotfix path** (Sprint 47.5, shipped earlier today as `termdeck@0.16.1` / `termdeck-stack@0.4.12` / `mnestra@0.3.4`) was load-bearing — without it, the 4+1 inject would have re-broken every fresh agent panel that re-ran the stack provisioner. The hotfix → sprint flow is now the canonical model when a regression is discovered hours before a planned sprint kickoff.

## [0.16.1] - 2026-05-02

### Fixed — Sprint 47.5 hotfix: stack-installer placeholder env + doctor.js Rumen column drift

> **Theme:** Two pre-Sprint-48 hotfixes folded into a single patch. Layer 1 unblocks Joshua's mnestra `memory_recall` (broken across all sessions after a routine `npm install -g @jhizzard/termdeck-stack@0.4.11` re-ran the installer). Layer 2 retires Brad's 2026-05-02 false-positive WARN on the Rumen schema check.

### Lanes

- **Hotfix A — stack-installer mnestra MCP env (no lane; surfaced in conversation).** `packages/stack-installer/src/index.js:299-312` wrote literal `${SUPABASE_URL}` / `${SUPABASE_SERVICE_ROLE_KEY}` / `${OPENAI_API_KEY}` strings into the mnestra MCP env block, on the assumption that Claude Code would shell-expand them at MCP launch time. **Claude Code does not perform `${VAR}` expansion on MCP env values** — they pass through to the spawned process literally. mnestra received the literal string `${SUPABASE_URL}` as `process.env.SUPABASE_URL`, the in-binary `loadTermdeckSecretsFallback()` saw it as truthy and bailed without reading `~/.termdeck/secrets.env`, and `@supabase/supabase-js` rejected it with `Invalid supabaseUrl: Must be a valid HTTP or HTTPS URL.` on every tool call. NEW `readTermdeckSecrets()` helper now parses `~/.termdeck/secrets.env` (dotenv-subset; quoted values stripped) and the env writer populates concrete values from `secrets.env` ∪ `process.env` ∪ omit. The `else if (servers.mnestra)` branch now performs a **repair pass** on existing entries — any value shaped like `${VAR}` is swapped for a concrete one (or deleted if no source can supply it), so users who previously ran `≤0.4.11` get healed transparently on next `npx @jhizzard/termdeck-stack`. Companion mnestra fix shipped as `@jhizzard/mnestra@0.3.4`: secrets fallback now runs in the default MCP stdio path (was `serve`-only) AND treats `${...}` placeholder strings as if the env var were unset.
- **Hotfix B — `packages/cli/src/doctor.js:364-388` per-table Rumen timestamp column map (Brad upstream report 2026-05-02).** Pre-0.16.1 doctor probed `rumen_jobs.created_at` for all three Rumen tables, but Rumen migration 001 defines `rumen_jobs.started_at` (semantically the tick start time — `rumen_jobs` has no `created_at`). The probe failed on a fully-healthy install and surfaced as a WARN with hint `column drift detected — re-run: termdeck init --rumen`, sending users on a wild-goose chase since re-running migration 001 correctly does NOT add `created_at` to `rumen_jobs`. NEW `RUMEN_TIME_COL` map (`rumen_jobs → started_at`, `rumen_insights → created_at`, `rumen_questions → created_at`) drives the column probe per-table. Doctor now reports green on a correctly-migrated stack. Brad's "side observation" about an empty bundled `rumen-migrations/` directory was a misread — actual layout is `setup/rumen/migrations/` (subdirectory style; mnestra uses dash-separator sibling style); 0.16.0 ships all three Rumen SQL files at the bundled path and `listRumenMigrations()` resolves them correctly.

### Validation

- **Mnestra MCP**: rebuilt at the symlinked install path, then re-tested with `env SUPABASE_URL='${SUPABASE_URL}' ... mnestra` — the secrets fallback fires, `[mnestra] Loaded 6 secrets from ~/.termdeck/secrets.env`, `memory_status` returns the live aggregation (6237 memories). Mnestra full test suite **42/42 green**.
- **TermDeck**: `node --test tests/cli-doctor.test.js tests/cli-stack-detection.test.js tests/stack-installer-hook-merge.test.js` — **67/67 green**, no test changes required (the doctor.js bug was a misnamed-column probe with no direct regression test; the stack-installer change is additive).
- **Repair pass**: applied manually to Joshua's `~/.claude.json` mnestra MCP env block as part of the hotfix conversation (backup at `~/.claude.json.bak.20260502-122145`); the post-`0.4.12` installer will perform the same repair automatically on next run.

### Notes

- `@jhizzard/termdeck-stack@0.4.12` audit-trail bump per RELEASE.md convention; the stack-installer code IS materially changed in this release (mnestra MCP env writer + repair pass), so the bump is functional, not pure-audit-trail.
- Sprint 48 (per-agent MCP auto-wire + mixed-agent dogfood) is the next sprint and was paused so this hotfix could land first; orchestrating off a known-broken installer would have re-broken every fresh agent panel that re-ran the stack provisioner.
- Defense-in-depth: even after `termdeck-stack@0.4.12` ships, an `~/.claude.json` written by any older installer version keeps working without manual repair because mnestra now ignores `${...}` placeholders. The two layers fix the same root cause from opposite ends.

## [0.16.0] - 2026-05-01

### Added — Sprint 47: Mixed 4+1 infrastructure (per-lane agent assignment + Sprint 46 close-out hook fix)

> **Theme:** Sprint 47 (4+1, **~28 minutes** wall-clock — over the recent 9-17 min band; T4 had a 16-min hung full-suite run that was killed and replaced with a focused subset since T4's status-merger.js is purely additive). Third lane of the multi-agent substrate trilogy memorialized at `docs/multi-agent-substrate/DESIGN-MEMORIALIZATION-2026-04-29.md`. Sprint 44 = registry skeleton + Claude adapter migration. Sprint 45 = Codex / Gemini / Grok adapter implementations. Sprint 46 = dashboard audit (defensive sweep, 5 silent-regression bugs caught). Sprint 47 = mixed 4+1 INFRASTRUCTURE — per-lane agent assignment so a single sprint can run with `T1=Codex / T2=Gemini / T3=Grok / T4=Claude` (or any mix). Sprint 48 (or wherever Joshua next opens TermDeck) actually dogfoods mixed assignments.

### Lanes

- **T1 — Frontmatter parser + lane.agent validation.** NEW `packages/server/src/sprint-frontmatter.js` (151 LOC; code-only ~115 LOC, slightly over the 80-LOC suggested target due to defensive error reporting + line-numbered diagnostics — defensible scope-wise per the brief's "throws with line number" requirement). Two exports: `parseFrontmatter(filePath)` parses YAML-style `---`-delimited frontmatter (top-level scalars + `lanes:` sequence of mappings), `getLaneAgent(briefPath, laneTag)` resolves to `AGENT_ADAPTERS[lane.agent]` adapter module ref or claude default. Hand-rolled YAML-subset parser (no `js-yaml` dep — vanilla JS, project no-build). Throws diagnostically on: unclosed frontmatter, unclosed quote with line number, tab indent, malformed line, agent ∉ valid set. NEW `tests/sprint-frontmatter.test.js` (271 LOC, **15/15 pass**). Sprint 45's existing PLANNING.md (no frontmatter) returns `{}` → all lanes default to claude — backwards-compat for every prior sprint.

- **T2 — Per-agent boot-prompt templates + resolver.** NEW directory `docs/multi-agent-substrate/boot-prompts/` with four templates (`boot-prompt-{claude,codex,gemini,grok}.md`, ~14 lines each). Each template is agent-specific in three ways: instructional-file reference (Claude→`CLAUDE.md`, Codex+Grok→`AGENTS.md`, Gemini→`GEMINI.md`), MCP-tool-call framing with MEMORY.md fallback for non-Claude, and CLI-greeting line. Mustache-style `{{var.path}}` placeholders. NEW `packages/server/src/boot-prompt-resolver.js` (81 LOC) exporting `resolveBootPrompt(agentName, vars)` + `VALID_AGENTS` + `DEFAULT_TEMPLATE_DIR` + private `_interpolate` / `_resolveDotted`. Hand-rolled interpolation (no template-engine dep). Throws diagnostically on unknown agent or missing variable. `templateDir` option lets test fixtures inject. NEW `tests/boot-prompt-resolver.test.js` (222 LOC, **16/16 pass**). Adjacent adapter+inject+parity suites (158 tests) stay green.

- **T3 — Inject script extension (mixed-agent dispatch + acceptsPaste contract).** Adapter contract extends from 7 → 8 fields with `acceptsPaste: true | false` (Claude / Codex / Gemini / Grok all `true` initially; Grok empirically validated against Bun+OpenTUI in the orchestrator smoke test — **the bracketed-paste landed cleanly**, `acceptsPaste:true` confirmed). REWRITE `packages/server/src/sprint-inject.js` (165 → 234 LOC): new `buildPayload` + `normalizeLanes` exports, three input shapes (legacy `{sessionIds, prompts}`, parallel `{sessionIds, prompts, agents}`, new `{lanes: [{sessionId, prompt, agent}]}`), chunked-fallback path for adapters declaring `acceptsPaste:false` (line-by-line write with `chunkedDelayMs` default 20ms; stage-2 lone-CR skipped because last line self-submits). `options.adapters` injection point for tests. Backwards-compat preserved — `sprint-routes.js` unchanged. EXTEND `tests/agent-adapter-parity.test.js` (+1 test asserting `acceptsPaste must be a boolean`). NEW `tests/sprint-inject-mixed-agent.test.js` (305 LOC, **14/14 pass**) pinning legacy claude parity, lanes[]-shape parity, mixed-agent inject (T1=codex / T2=gemini / T3=grok / T4=claude), parallel `agents[]` array equivalence, chunked fallback, write failure short-circuit, input validation. UPDATE `docs/sprint-47-mixed-4plus1/scripts/inject-sprint47.js` (rewrite 184 → 211 LOC) to declare `agent: 'claude'` per lane and dispatch via mirrored `buildPayload()` consulting `AGENT_ADAPTERS` — canonical Sprint 48+ clone target.

- **T4 — Cross-agent STATUS.md merger.** NEW `packages/server/src/status-merger.js` (141 LOC; code-only ~95 LOC, over the 50-LOC target because canonical regex needed two variants for missing-timestamp synthesis, emoji detection covers both 🛠 and 🛠️ variation-selector forms, summary trimming + word-boundary-safe truncation factored as helpers). Single export `mergeStatusLine(rawLine, opts)` with deterministic `opts.now` seam + `opts.laneTag` override. Detects 5 layers in order: canonical-with-timestamp (anchors on `\d{4}-\d{2}-\d{2}` so bodies containing stray ` — ` split correctly), canonical-without-timestamp (synthesizes), Codex emoji (🔍/🛠/🛠️/✅/🔧), Gemini bullet idiom (`- found that …`, `- proposing fix: …`, `- done: …`, `*` accepted), Grok prose idiom (`I noticed/observed/found`, `I'll fix/will fix/'m fixing`, `Done:/Completed:/Shipped:`). DONE checked before FIX-PROPOSED so `Done: …` doesn't get caught by `Fix…`. Pass-through preserves body length verbatim (real Sprint 46 lines run >120 chars and brief mandates "same line out"); only synthesized non-canonical output trims. Section headers / italic stubs / blockquotes / non-strings → null without throwing. NEW `tests/status-merger.test.js` (202 LOC, **23/23 pass**). Real Sprint 46 T1 + T3 FIX-PROPOSED lines lifted as fixtures (these contain stray ` — ` in the body — the bug-find that surfaced the trim-on-pass-through defect, caught + fixed first iteration).

### Mixed-agent smoke test (orchestrator side-task — first non-Claude inject through the new T3 path)

- **Bracketed-paste landed cleanly against Bun+OpenTUI Grok 4.20 Reasoning.** Live panel `518adedc` received the full 3677-byte synthetic boot prompt intact, executed `read_file` on AGENTS.md + PLANNING.md, used `edit_file` to append a multi-paragraph response to STATUS.md. Validates: (a) `acceptsPaste:true` correct for Grok — chunkedFallback stays defensive infrastructure for future agents, not the Grok default; (b) Grok read/write tools functional in TermDeck PTY; (c) `memory_recall` unavailable as predicted (Mnestra MCP not wired in `~/.grok/user-settings.json`) — Grok fell back gracefully to filesystem reads per the boot-prompt-grok.md template's fallback path; (d) Grok's PART 2/3 prose response is a **real-world fixture for T4's status-merger free-form-idiom branch** (validates T4 design without waiting for Sprint 48 dogfood).
- **Sprint 48 actionable insight surfaced from Grok's self-analysis:** the highest-leverage TermDeck wiring is auto-populating `mcpServers` in `~/.grok/user-settings.json` (with the Mnestra endpoint) on panel spawn. Grok's native tool surface (`task` for foreground sub-agents, `delegate` for background, full filesystem + LSP + bash) already provides Claude-parity. Cross-project RAG is the only material gap — and it's a config-injection problem, not a TermDeck-shim problem. **This is the v1.0.0 inflection gate** (see § v1.0.0 decision below).

### Fixed

- **Grok panel type-reclassification bug** (Sprint 47-discovered, surfaced when Joshua opened a Grok panel for the smoke test and the dashboard header still said "Shell"). Root cause: Sprint 45 T3's PROMPT regex `/Message Grok[….]/` only matched the empty-state placeholder, but the TUI rotates placeholders ("What are we building?" was visible in Joshua's screenshot). The model-mode footer `Grok 4.20 Reasoning|Heavy|Code|Auto|Planning` renders on every frame and is the actually-stable signal. Fix: extended PROMPT to alternation `/Message Grok[….]|Grok\s+\d+(?:\.\d+)?\s+(?:Reasoning|Heavy|Code|Auto|Planning)/` (original placeholder match preserved as alternation — no breaking change). +12 LOC in `grok.js` (mostly comment block explaining the rotation issue so Sprint 48+ doesn't re-rediscover), +33 LOC in `tests/agent-adapter-grok.test.js` (4 new fixtures: footer-only Reasoning, Heavy mode, Code mode, real TUI buffer with rotated placeholder + footer combo). 43/43 grok-adapter tests pass.

- **Session-end hook env-var name mismatch** (cross-session orchestrator pickup; Sprint 46 close-out diagnostic surfaced this). Root cause: `packages/stack-installer/assets/hooks/memory-session-end.js` read `SUPABASE_SERVICE_KEY` (singular) but every other consumer in the codebase + `~/.termdeck/secrets.env` declares `SUPABASE_SERVICE_ROLE_KEY` — so the hook's `readEnv()` rejected on missing-required at fire time, silently no-op'd, and Mnestra never received session-summary memories from `Stop` events for 4+ days. Compounded by separate hook drift on Joshua's machine (`~/.claude/hooks/memory-session-end.js` was the Sprint 41 era spawn-process-session.ts version, last bundled before Sprint 38's Mnestra-direct rewrite). Fix: rename `SUPABASE_SERVICE_KEY` → `SUPABASE_SERVICE_ROLE_KEY` in 3 places in the hook (line 25 doc, line 76 required-list, line 84 reader) + matching renames in `packages/stack-installer/assets/hooks/README.md` (2 places) + `tests/stack-installer-hook-merge.test.js` (5 fixture references). **51/51 hook-merge tests pass post-rename.** Joshua's `~/.claude/hooks/memory-session-end.js` will pick up the corrected version after `npx @jhizzard/termdeck-stack@latest` re-bundle. Sprint 38-era latent bug; never caught because Joshua's installed hook was even older and silently failing at a different layer.

### Changed

- **Adapter contract extended from 7 → 8 fields** (T3). All four existing adapters declare `acceptsPaste: true`. The contract documentation in `docs/AGENT-RUNTIMES.md` § 5 should be updated in Sprint 48 to reflect the new field (deferred — not blocking Sprint 47 ship).
- **`docs/INSTALL-FOR-COLLABORATORS.md`** refreshed to v0.16.0 + v0.4.11 + clarifying note about Mnestra hook env var (now reads `SUPABASE_SERVICE_ROLE_KEY` correctly; pre-Sprint 47 installs had the typo).

### v1.0.0 decision (deferred to Sprint 48 close)

Sprint 47 sits AT the v1.0.0 inflection but does **not** cross it. The Grok smoke test surfaced the load-bearing gap: cross-project memory recall (Mnestra MCP) is unavailable to non-Claude agents by default. Outside users running mixed 4+1 today would hit `memory_recall` failures the first time they declared a non-Claude lane. v1.0.0 framing is "production-ready for outside users" — that's not yet true with manual MCP wiring. **Sprint 48's per-agent MCP auto-wire (the top candidate) is the v1.0.0 gate.** Once that ships AND a real mixed-agent sprint dogfoods the path end-to-end, v1.0.0 is the natural call.

Sprint 47 ships as **v0.16.0** — the multi-agent infrastructure is complete; what's missing is the auto-config that makes it usable without manual setup. Single Sprint 48 lane closes the gap.

### Notes

- **Coordinated release:** `@jhizzard/termdeck@0.16.0` + `@jhizzard/termdeck-stack@0.4.11` (audit-trail per RELEASE.md convention; stack-installer source DID change this release — the hook env-var fix). Mnestra @0.3.3 + Rumen @0.4.4 unchanged.
- **Tests:** **847 root + 35 server-package = 882 total / 0 fail / 3 skipped** (was 841 total at Sprint 46 close; **+41 new tests** — T1: 15 sprint-frontmatter, T2: 16 boot-prompt-resolver, T3: 14 sprint-inject-mixed-agent + 1 parity, T4: 23 status-merger, side-task Grok: 4 = 73 new, but some existing test files saw deletions/consolidations net to +41). Skipped count unchanged (transient-substrate gates).
- **Sprint 47 wall-clock: ~28 min** (inject 16:11 ET → last lane DONE 16:39 ET on 2026-05-01). Above the recent 9-17 min band; T4's hung full-suite run accounts for the overage. T1=13 min, T2=4 min, T3=10 min, T4=28 min (16 min hung + 12 min real work).
- **Stack-installer audit-trail bump** from 0.4.10 → 0.4.11; this release the source genuinely changed (env-var rename in `assets/hooks/memory-session-end.js` + matching test/README updates).
- **Sprint 48 stub plan** staged at `docs/sprint-48-mcp-autowire-and-dogfood/PLANNING.md` re-anchoring the per-agent MCP auto-wire work + actual mixed-agent dogfood lane + 14 carry-over Sprint 46 deferrals + Sprint 47 carry-overs (resolver-into-helper wiring, AGENT-RUNTIMES.md 8-field doc, Grok 16-sub-agent observability).
- **Restart prompt** for the next session at `docs/RESTART-PROMPT-2026-05-02-sprint-48.md`.

## [0.15.0] - 2026-05-01

### Added — Sprint 46: Dashboard functionality audit (graph + flashback history + transcripts + quick-launchers)

> **Theme:** Sprint 46 (4+1, **~16 minutes** wall-clock — exact match to Sprint 45). Defensive sweep across the topbar's right-hand button row and the surfaces those buttons open. After Sprint 38–43 churn + Sprint 45's launcher refactor, silent regressions were plausible. The audit caught and fixed **5 real bugs across 3 lanes** while T2 verified the flashback dashboard works as designed. None catastrophic, all the silent-regression class — exactly the failure mode Joshua suspected when he flagged the audit need 2026-05-01: *"we need to audit the functionality of all the functions (transcript, flashbacks, graph, et c) in that row of buttons. We haven't paid much attention to them."*

### Lanes

- **T1 — Graph viewer audit.** 9 surfaces classified (6 works, 5 sub-optimal-deferred, 1 broken-fix-shipped). **Surface 6 fix:** node hover tooltip was missing — design intent said "Hover tooltip shows project + content snippet" but `mouseenter` on circles only dimmed non-incident neighbors. Fix: `graph.js` +21 LOC — new `showNodeTooltip(event, node)` helper + `escapeHtml` guard against literal `<`/`>`/`&`/`"` in user content. NEW `tests/graph-viewer-e2e.test.js` (+211 LOC, 11 tests) closes wiring contracts: every `getElementById` in graph.js points at an existing graph.html id, graph-controls.js loads BEFORE graph.js, `EDGE_COLORS` keys match migration-009's 8-type vocabulary, live-shape fixtures preserve renderer-consumed fields, URL-codec → applyControls integration accepts hostile params cleanly. **The e2e test caught a near-miss:** the lane initially proposed trimming `blocks`/`inspired_by`/`cross_project_link` from `EDGE_COLORS` as "dead UI"; on re-check those are intentional pre-wired affordances (migration-009's 8-type vocabulary, dormant only because `GRAPH_LLM_CLASSIFY=1` is unset). The cleanup was reverted; the test now locks parity so future cleanup attempts trip a test failure instead of breaking LLM-classification activation. 5 sub-optimal items deferred to Sprint 47 (edge click handler design call, cross-project edges visual distinction, chip "all on/off" preset, perf at 2000+ nodes, URL state codec edge case).

- **T2 — Flashback history audit.** 10 surfaces classified — **NO CODE CHANGES this sprint**. Dashboard works as designed; existing 48 tests (`flashback-events.test.js` 31 + `flashback-diag.test.js` 17) are comprehensive when run against live DB. Funnel matches SQLite source-of-truth exactly. Dismiss/click POSTs round-trip cleanly: idempotent on second call, HTTP 400 on non-numeric id, HTTP 200 `updated:false` on missing id. **Brief↔implementation mismatch surfaced:** brief surfaces #4/#5 assumed per-row dismiss/click buttons on the history page; reality is the page is intentionally read-only and the dismiss/click flow runs through the live toast in the main dashboard (`app.js:605-655`). Brief patched at sprint close. **Audit-write-gap verdict (Sprint 43 T2 follow-up):** the client-side `triggerProactiveMemoryQuery` at `app.js:562` is gated off on installs without `aiQueryAvailable=true` — effectively dead code in default config. Server-side path always inserts into `flashback_events` before WS-emit, so funnel fires count is accurate today. Risk applies only when a user enables RAG-driven flashback; documented as a Known Issue in `INSTALL-FOR-COLLABORATORS.md` §5. 3 sub-optimal items deferred (source-session links, pagination, audit-write-gap cleanup).

- **T3 — Transcripts panel audit.** 12 surfaces + 3 bonus findings classified (10 works, 4 deferred, 2 broken-fix-shipped). **Recent tab fix:** server `/api/transcripts/recent` returns `{sessions: [{session_id, chunks: […]}]}` but `renderRecentTranscripts` was reading `sess.lines/preview/type/project/totalLines` — none of which the server sent. Result: every row in Recent showed `shell` / `0 lines` / empty preview against today's 4 live sessions. Fix: renderer reads `sess.chunks` directly (DESC→reverse, slice top-6 for preview). **Search fix:** time chip silently dropped because server returns `created_at` and renderer was reading `result.timestamp`. 50 results all timestamp-less. Fix: `result.timestamp || result.created_at` with `Date.getTime()` validity check. **Future-proofing:** `tests/transcript-contract.test.js` extended with renderer-contract assertions pinning `chunk.content` / `chunk_index` on Recent and `created_at` / `content` / `session_id` on Search — future server refactors that rename these fields fail the contract test instead of silently breaking the renderer. Net code change: ~25 LOC. 4 sub-optimal items deferred (server metadata gap, TUI spinner spam in stored content, escapeHtml dedup, perf virtualization).

- **T4 — Quick-launchers + topbar UX cross-cut.** 24 surfaces classified (22 works, 2 broken-fix-shipped). **`python` quick-launch fix:** `python3 -m http.server 8080` was routing to `type='shell'` initially because the preemptive python-server regex at `app.js:2527` (`/^python3?\b.*(?:runserver|uvicorn|flask|gunicorn)/i`) didn't include `http\.server`. Runtime detection upgraded within ~1s, but initial badge wrong. Fix: regex extended. **Test-coverage fix (the bigger win):** Sprint 45 T4's launcher refactor at `app.js:2482-2531` shipped without an end-to-end guard. Lane extracted the inline resolver into NEW `packages/client/public/launcher-resolver.js` (~70 LOC, UMD wrapper so the same module loads in the browser via `<script>` AND under `node --test`). NEW `tests/launcher-resolver.test.js` (~155 LOC, 26 tests) pins all routing decisions: 3 quick-launches, 4 free-form CLIs (claude / codex / gemini / grok), `cc` shorthand, `claude code <arg>` parsing, word-boundary semantics, python-server detection, defensive null/empty inputs. `index.html` +1 `<script>` tag. `app.js` -42/+13 LOC. 2 sub-optimal items deferred (cosmetic title gap on `btn-status`/`btn-config`, latent regex-injection risk in `^${binary}\b` if a future adapter declares a binary with regex metachars).

### Fixed

- **5 silent-regression bugs caught by the audit + fixed this sprint** (full diagnostic in `docs/sprint-46-dashboard-audit/AUDIT-FINDINGS.md`). The pattern: original design intent had drifted, or server↔client field-name contracts had broken silently. None were catastrophic; all would have looked weird to outside users (Brad-tier testers).

### Changed

- **Lane brief corrections** (T1 + T2 self-flagged at sprint close):
  - `T1-graph-audit.md` endpoint citations: actual endpoints are `/api/graph/all|project/:name|memory/:id|stats|stats/inference`. Original brief incorrectly cited `/api/graph/nodes` + `/api/graph/edges`.
  - `T2-flashback-audit.md` surfaces #4/#5: dismiss/click flow runs through the live toast in the main dashboard, not on-page UI. The history page is intentionally read-only.
- **`docs/INSTALL-FOR-COLLABORATORS.md` refreshed** to v0.15.0 + v0.4.10. Added Known Issue §5: flashback funnel undercount risk when `aiQueryAvailable=true`.

### Notes

- **Coordinated release:** `@jhizzard/termdeck@0.15.0` + `@jhizzard/termdeck-stack@0.4.10` (audit-trail per RELEASE.md convention; stack-installer source unchanged this release). Mnestra @0.3.3 + Rumen @0.4.4 unchanged.
- **Tests:** **806 root + 35 server-package = 841 total / 0 fail / 3 skipped** (was 772 total at Sprint 45 close; **+69 new tests** — T1: 11 graph-viewer-e2e, T3: 4 transcript-contract assertions, T4: 26 launcher-resolver, T2: 0 new but exercised 48 existing against live DB). Skipped count unchanged (transient-substrate gates).
- **Sprint 46 wall-clock: ~16 minutes** (inject 15:22 EDT → last lane DONE 15:38 EDT on 2026-05-01). Exact match to Sprint 45's wall-clock. T2 was the fastest at ~16 min (no code; existing tests sufficient); T1 was the slowest at ~16 min including the near-miss revert + e2e test authoring.
- **Stack-installer audit-trail bump** from 0.4.9 → 0.4.10 per RELEASE.md convention.
- **Sprint 47 stub plan** staged at `docs/sprint-47-mixed-4plus1/PLANNING.md` re-anchoring the deferred mixed-4+1 work + the 14 Sprint 46 deferrals so the next session has a clean starting point.
- **What's queued for Sprint 47:** mixed 4+1 (per-lane agent assignment, per-agent boot prompts, inject script extension, cross-agent STATUS.md merger). Plus Sprint 46's 14 sub-optimal deferrals, plus carry-overs (Grok memory-hook SQLite extraction, upstream rumen `createJob.started_at` patch, mnestra doctor subcommand, Sprint 40 analyzer / PROJECT_MAP candidates).
- **v1.0.0 inflection:** Sprint 47 close is the natural decision point. Multi-agent + cron + observability + audited-dashboard reads as production-ready for outside users.

## [0.14.0] - 2026-05-01

### Added — Sprint 45: Multi-agent adapter implementations (Codex + Gemini + Grok + launcher refactor)

> **Theme:** Sprint 45 (4+1, **~16 minutes** wall-clock — within band of recent records: 41=9, 42=12, 43=17, 44=11). Second lane of the multi-agent trilogy memorialized at `docs/multi-agent-substrate/DESIGN-MEMORIALIZATION-2026-04-29.md`. Sprint 44 shipped the registry skeleton + Claude adapter migration; Sprint 45 fills in the three other adapters and refactors the launcher / memory-hook to drive entirely from the registry. Sprint 46 ships mixed-agent 4+1 (per-lane agent assignments).

### Lanes

- **T1 — Codex adapter.** NEW `packages/server/src/agent-adapters/codex.js` (199 LOC) implementing the 7-field contract. Prompt regex accepts three shapes (`codex>`, bare `codex` speaker line, `--------` divider) covering both TUI and headless `codex exec` modes. Status patterns: `Thinking|Reasoning|Working` for thinking; `Apply patch` (Codex's `apply_patch` tool name) plus generic `Edit|Create|Update|Delete|Modified` for editing; `$|→|exec(_command)?|Running|Calling` for tool-use. Error pattern adds OpenAI-API failure modes line-anchored: `429 Too Many Requests`, `rate.?limit`, `invalid_api_key`, `model_not_found`, `insufficient_quota`. **Sessions persist at `~/.codex/sessions/YYYY/MM/DD/rollout-<ISO-ts>-<uuid>.jsonl` (NOT flat `~/.codex/sessions/*.jsonl` as the briefing suspected)** — JSONL with `{ timestamp, type, payload }` envelopes; canonical message channel is `type=response_item, payload.type=message, payload.role={user|assistant|developer}`. `developer` role carries permissions/sandbox prelude → skipped. `parseTranscript` pins this format with real on-disk fixtures. NEW `tests/agent-adapter-codex.test.js` (486 LOC, **42/42 pass**). Memory hook (`packages/stack-installer/assets/hooks/memory-session-end.js`) `buildSummary` extended with per-line dual-shape parser (Claude-first → Codex fallback) as a stop-gap; T4 promoted it to registry-driven.

- **T2 — Gemini adapter.** NEW `packages/server/src/agent-adapters/gemini.js` (147 LOC). Loose `/gemini/i` command match preserves parity with legacy `_detectType` regex. `patterns.{prompt,thinking}` lifted verbatim from legacy `PATTERNS.geminiCli` (reference-equal so any external `===` checks survive). `patterns.error` deliberately OMITTED so `_detectErrors` falls back to generic `PATTERNS.error` matching pre-Sprint-45 behavior. **Sessions persist at `~/.gemini/tmp/<projectName>/chats/session-<ISO>-<id>.json` as a SINGLE JSON object** (NOT JSONL like Claude): `{ sessionId, projectHash, startTime, lastUpdated, messages: [...], kind }`. `parseTranscript` normalizes `type='gemini' → role='assistant'` with 400-char cap matching the Claude adapter. Auth is OAuth (`settings.json selectedType: oauth-personal`); `GEMINI_API_KEY` env-var fallback supported. NEW `tests/agent-adapter-gemini.test.js` (245 LOC, **27/27 pass**). REFACTOR `session.js`: removed dead `case 'gemini':` from `_updateStatus` and dead gemini fall-through from `_detectType` (registry handles both now). Memory hook gains a Gemini-format short-circuit BEFORE the per-line JSONL loop (one-shot `JSON.parse` + Gemini-shape sniff).

- **T3 — Grok adapter (with `chooseModel` heuristic).** NEW `packages/server/src/agent-adapters/grok-models.js` (115 LOC) — 8-tier symbolic→canonical model map + 3 legacy aliases (`grok-beta` → `grok-4.20-0309-reasoning`). `chooseModel(taskHint)` returns `grok-4-1-fast-non-reasoning` ($0.2/$0.5) for unknown / undefined / typo'd hints — **bill-safety property pinned in tests** (e.g. `reaonsing-deep` typo must NOT silently route to Heavy tier). Heavy tier ($2/$6) requires correctly-spelled `reasoning-deep` / `multi-agent` / `reasoning-non-cot`. NEW `packages/server/src/agent-adapters/grok.js` (253 LOC) implementing the 7-field contract. **Critical pivot from prep notes:** Grok stores sessions in **SQLite at `~/.grok/grok.db` (STRICT tables, requires SQLite ≥3.37)** — NOT JSON files in `~/.grok/sessions/`. Schema confirmed: `sessions(id, workspace_id, title, model, mode, status, created_at, updated_at)` + `messages(session_id, seq, role, message_json, created_at)`. `messages.message_json` is AI-SDK provider shape `{ role, content: string | Array<{type:'text'|'tool-call'|'tool-result', ...}> }`. `parseTranscript` accepts JSON-array OR JSONL of `messages.message_json` rows (SQLite extraction is hook-layer concern, queued for Sprint 46). `bootPromptTemplate` points at `AGENTS.md` (Grok's instructional file convention, shared with Codex). `costBand: 'subscription'`. `grok-dev` v1.1.5 binary at `/usr/local/bin/grok` has shebang `#!/usr/bin/env bun` — bun on PATH inherited from Joshua's shell works in practice. NEW `tests/grok-models.test.js` (24 tests) + `tests/agent-adapter-grok.test.js` (39 tests). All **63/63 pass**.

- **T4 — Launcher refactor + memory hook adapter-pluggable + PATTERNS shim removal + parity tests.** NEW `tests/agent-adapter-parity.test.js` (170 LOC, **10 tests**) — iterates whatever's in `AGENT_ADAPTERS` at run-time so it picks up T1's codex / T2's gemini / T3's grok automatically. Asserts: 7-field contract uniformity, names + sessionTypes unique, `matches()` handles null/undefined/empty without throwing, `matches()` mutually exclusive on each adapter's own canonical binary, `parseTranscript()` fail-soft on garbage, `statusFor()` returns null or `{status, statusDetail}` strings, `bootPromptTemplate()` non-empty multi-line string. NEW `GET /api/agent-adapters` endpoint (~25 LOC) returning a serializable projection (`[{name, sessionType, binary, costBand}]`). REFACTOR `packages/client/public/app.js` launcher (~50 LOC delta) — removed hardcoded `claude`/`cc`/`gemini`/`python` regex branches; type detection now driven from `state.agentAdapters` (loaded from the new endpoint at init, with bootstrap-fallback to claude-only for stale-server rolling-upgrade). The `cc → claude` alias kept as an explicit client-side normalize step (Claude-specific UX); the Claude-shorthand argument-parser gated behind `adapter.name === 'claude'`. REFACTOR `packages/stack-installer/assets/hooks/memory-session-end.js` (+85 LOC net) — extracted T1's inline Codex stop-gap and T2's inline Gemini stop-gap into named parsers (`parseClaudeJsonl`, `parseCodexJsonl`, `parseGeminiJson`, `parseAutoDetect`). New `TRANSCRIPT_PARSERS` dispatch table keyed by `sessionType`. `processStdinPayload` reads `sessionType` from payload → `TERMDECK_SESSION_TYPE` env var → `'auto'` default. **REMOVED `PATTERNS.claudeCode` + `PATTERNS.errorLineStart` shim entries from `session.js`** (Sprint 44 T3 shim, kept "one release"). Updated tests that consumed the removed shim: `tests/rcfile-noise.test.js` and `tests/rcfile-noise-fixtures/analyze.js` now import `claudeAdapter.patterns.error` directly. `tests/agent-adapter-claude.test.js` shim-equivalence test replaced with an inverse assertion locking the shim REMAINS removed. `geminiCli` shim left intact (T2's territory; same one-release deprecation pattern applies for Sprint 46).

### Fixed

- **DNS-resilience in the rumen pg.Pool** (orchestrator side-task, per Joshua's directive 2026-05-01: "comprehensive, most architecturally correct fix"). NEW `packages/server/src/rumen-pool-resilience.js` (~95 LOC, two DI factories): `createCachedLookup` provides an in-process DNS cache with 30s TTL + jittered exponential backoff retry (100/500/2000/5000ms caps) + serve-stale-on-exhausted-retries so transient DNS flickers don't tear the pool down; `createFailureLogger` provides recency-graded logging where the first failure logs `warn`, consecutive failures within a 60s window downgrade to `debug`, and the recovery logs `info` exactly once. Wired into `getRumenPool` factory at `packages/server/src/index.js` via `lookup:` config + `pool.on('error'|'connect')` (~7-line delta to existing factory; sync API preserved so the four downstream call-sites are unchanged). NEW `tests/rumen-pool-resilience.test.js` (7/7 pass) — hermetic, stubs `dns` + `console`. Fixes the log-flood Joshua observed overnight: `[rumen] GET /status failed: getaddrinfo ENOTFOUND aws-1-us-east-2.pooler.supabase.com`.

- **Rumen-status dashboard staleness** (orchestrator side-task — Sprint 45 PLANNING.md item 2). The brief premise ("`last_job_completed_at: 2026-04-16T03:30:00.956Z` 2 weeks old but insights still landing daily") was a read-side artifact, not a stale cron. Investigation findings in `docs/sprint-45-multi-agent-adapters/SIDE-TASK-rumen-tick-stale-job.md`. Two converging defects: (1) **upstream** — `@jhizzard/rumen`'s `createJob` INSERT (`~/Documents/Graciella/rumen/src/index.ts:177`) omits `started_at` and the column has no default, so 1546/1547 rumen_jobs rows have NULL `started_at`; (2) **downstream** — TermDeck's `GET /api/rumen/status` query at `packages/server/src/index.js:1689` ordered by `started_at DESC`, which puts NULLs last in PostgreSQL, latching the dashboard onto the one surviving non-NULL row from 2026-04-16 forever. **TermDeck-side fix shipped this release:** `ORDER BY COALESCE(started_at, completed_at) DESC NULLS LAST` returns the actual latest row immediately. The upstream rumen 2-line fix is queued (see [Unreleased] § Planned). All three brief-time hypotheses (graph-inference, MCP classifier, partial-success) failed data inspection: 0 insights have NULL `job_id`; latest `completed_at` is 2026-05-01 18:30 UTC (~3h before sprint close).

### Changed

- **`packages/server/src/agent-adapters/index.js` registry now lists all four adapters** (`claude` / `codex` / `gemini` / `grok`) in declaration order. `detectAdapter(data, command)` and `getAdapterForSessionType(type)` accessors unchanged from Sprint 44.
- **`session.js::_detectType` and `_updateStatus`** now consult only the registry for Claude / Codex / Gemini / Grok session types. Python-server detection stays inline (no adapter; it's a server SUBTYPE not a CLI agent). Shell fallback unchanged.
- **`docs/INSTALL-FOR-COLLABORATORS.md` refreshed** post-publish to pin v0.14.0 + v0.4.9 versions and reflect the now-first-class Codex / Gemini / Grok lane agents.

### Notes

- **Coordinated four-package release:** `@jhizzard/termdeck@0.14.0` + `@jhizzard/termdeck-stack@0.4.9` (audit-trail). Mnestra and Rumen unchanged at `@0.3.3` / `@0.4.4` (no API change required this sprint; see [Unreleased] § Planned for the queued rumen-package fix).
- **Tests:** TermDeck root suite **737 tests, 734 pass, 3 skipped, 0 fail** (was 586 at Sprint 44 close). +151 new from T1 (42), T2 (27), T3 (24+39=63), T4 parity (10) + memory-hook merge (+2), orchestrator side-task (7). The skipped count is unchanged from prior sprints (transient-substrate gates: live server availability, recent transcript fixtures).
- **Sprint 45 wall-clock: ~16 minutes** (inject 14:35:17 EDT → last lane DONE 14:51 EDT on 2026-05-01). Within band of recent sprints (41 = 9, 42 = 12, 43 = 17, 44 = 11). The two slowest lanes were T2 (Gemini hook short-circuit needed end-to-end Claude-JSONL-non-swallow guard) and T4 (cross-cutting refactor + shim removal + dependent test migrations).
- **Stack-installer audit-trail bump** from 0.4.8 → 0.4.9 per RELEASE.md convention (validates against `@jhizzard/termdeck@0.14.0`). Stack-installer source is unchanged this release — bump is audit-trail-only.
- **What's queued for Sprint 46:** mixed 4+1 (per-lane `agent: claude|codex|gemini|grok` field, per-agent boot prompts, inject script extension, cross-agent STATUS.md merger). Plus the Grok SQLite extraction in the memory hook + the upstream rumen `createJob.started_at` patch. See [Unreleased] § Planned.

## [0.13.0] - 2026-05-01

### Added — Sprint 44: Multi-agent foundation (Grok install + AGENTS.md sync + adapter registry skeleton + AGENT-RUNTIMES.md doc)

> **Theme:** Sprint 44 (4+1, **~11 minutes** wall-clock — second-fastest sprint behind Sprint 41's 9-min record). Foundation lane of the multi-agent trilogy memorialized at `docs/multi-agent-substrate/DESIGN-MEMORIALIZATION-2026-04-29.md`. Sprint 45 ships the Codex/Gemini/Grok adapter implementations; Sprint 46 ships mixed-agent 4+1.

### Lanes

- **T1 — Grok CLI install + auth wiring.** **Critical pivot:** the canonical `install.sh` whitelists only `darwin-arm64 / linux-x64 / windows-x64` and rejects `darwin-x64` (Joshua's Intel iMac, macOS 13.7.8). Workaround that ships in `T1-grok-install.md`: `npm i -g grok-dev --ignore-scripts` works on Intel Mac — install completed cleanly (1,225 transitive packages, 198.7 MB unpacked, ~2 min). **Corrections to the Sprint-43-era design memo discovered during lane work:**
  - npm package is **`grok-dev`** (not `grok-cli`). Same source tree as the script-installed bun-compiled binary; just the npm-distributed form per `bin: { grok: "dist/index.js" }`.
  - **Five** built-in sub-agents (general / explore / **vision** / verify / computer) — the design memo had only four; `vision` was missing.
  - Multi-agent model id is **`grok-4.20-multi-agent-0309`** (alias `grok-4.20-multi-agent`). Heavy reasoning model is **`grok-4.20-0309-reasoning`** (alias `grok-beta`). The `-0309` date suffix is canonical, not optional. Cheaper Fast variants exist for routine work (`grok-4-1-fast-reasoning` etc.).
  - **"SuperGrok Heavy" is a subscription tier, not a model name** — Heavy unlocks the `grok-4.20` family ($2/$6 per 1M tokens) vs Fast variants ($0.2/$0.5). The CLI itself has no special Heavy integration; it sees only the API key obtained from `console.x.ai`.
  - **End-to-end verification (Joshua, 2026-05-01):** `grok --model grok-4.20-0309-reasoning --prompt "what is 2+2"` returned the correct answer with bash sub-agent verification (`▸ bash: echo $((2 + 2))`) — confirming the binary works AND the sub-agent / tool-use machinery is reachable on Intel Mac via the npm install path.
  - `T1-grok-install.md` (~250 LOC, the canonical install reference) ships in the sprint docs directory; T4 lifts the structured sections into `docs/AGENT-RUNTIMES.md`.

- **T2 — `scripts/sync-agent-instructions.js`.** 10/10 tests. CommonJS, ~80 LOC. Idempotent (mtime-preserving) sync of `CLAUDE.md` → `AGENTS.md` (2,438 B, Codex+Grok lead) + `GEMINI.md` (2,426 B, Gemini lead) with `<!-- AUTO-GENERATED ... Do not edit directly. -->` banners. `npm run sync:agents` wired into root `package.json`. Both mirrors committed at repo root for external GitHub visibility (Brad / Unagi SWE / future contributors land on GitHub first). Re-running on already-synced files reports both unchanged; `git diff --stat AGENTS.md GEMINI.md` is empty after consecutive runs.

- **T3 — Agent adapter registry skeleton + Claude adapter migration.** 32/32 new tests + 35/35 server suite + parity guards green. `packages/server/src/agent-adapters/{index.js, claude.js}` ship the 7-field contract (plus `sessionType` + `patternNames` extras for backward-compat). `session.js::_detectType / _updateStatus / _detectErrors` route through the registry for claude-code; gemini/python branches preserved verbatim under the `else` arm. **Reference equality preserved:** `PATTERNS.errorLineStart === claudeAdapter.patterns.error` so any external `===` checks survive the migration. The `PATTERNS` top-level export is retained as a shim for one release; Sprint 45 T4 removes it after the launcher UI refactor.

- **T4 — `docs/AGENT-RUNTIMES.md`** (259 lines, canonical reference). All 9 brief sections present: overview (capacity safety valve + audit-quality lever + cost bands); supported agents table (Sprint 44 = Claude only; Codex/Gemini/Grok in Sprint 45); auth keys table (Anthropic / OpenAI / Gemini / Grok-via-SuperGrok-Heavy); sync mechanism (`npm run sync:agents`, banner, idempotency); 7-field adapter contract spec with Claude examples; how-to-add-a-new-agent worked example for Codex with copy-pasteable `codex.js` skeleton + 6-step recipe; TheHarness alignment note (transport-swap test); sprint sequencing 44/45/46/47+; cross-references to memorialization + plan file + Sprint 44/45/46 plans + adapter source path + sync script + existing PATTERNS map + orchestrator guide + TheHarness vision. T1's darwin-x64 finding lifted into § 2 as install caveat with pointer to `T1-grok-install.md`.

### Notes

- **Coordinated four-package release:** `@jhizzard/termdeck@0.13.0` + `@jhizzard/termdeck-stack@0.4.8` (audit-trail). Mnestra and Rumen unchanged at `@0.3.3` / `@0.4.4` (no API change required this sprint).
- **Tests:** TermDeck root suite **586 tests, 579 pass, 7 skipped, 0 fail** (was 544 at Sprint 42 close). +42 new from T2 (10) + T3 (32). The skipped count includes tests that gate on transient substrate (live server availability, recent transcript fixtures).
- **Sprint 44 wall-clock: ~11 minutes** (inject 21:45 ET → last lane DONE 21:56 ET on 2026-04-30). Second-fastest sprint behind Sprint 41's 9-min record. T1's binary verification (`grok --version`, smoke test, `grok models` enumeration) was sandbox-deferred during the lane and completed during close-out via Joshua-driven follow-up — not a Sprint 44 acceptance blocker since T2/T3/T4 ship independently and the documentation deliverable establishes the path forward.
- **What's queued for Sprint 45:**
  - **Codex / Gemini / Grok adapter implementations** (the actual adapters using Sprint 44's registry skeleton). Lane breakdown: T1 Codex, T2 Gemini, T3 Grok, T4 launcher UI + memory hook adapter-pluggable refactor.
  - **DNS-resilience fix in the rumen pool client** — Joshua observed overnight DNS-flakiness producing `[rumen] GET /status failed: getaddrinfo ENOTFOUND aws-1-us-east-2.pooler.supabase.com` log noise. Diagnosis: the log prefix is the `/api/rumen/status` Express route handler at `packages/server/src/index.js:1689`; the actual error is the pg client's DNS lookup failing intermittently against the (correctly-configured) pooler hostname. Pooler resolves cleanly on direct test. **Joshua's directive 2026-05-01: "apply the comprehensive, most architecturally correct fix in the next sprint."** Approach: pg connection retry with exponential backoff + DNS-cache TTL respect + structured log levels by failure recency (first failure warn; consecutive within window downgrade to debug; recovery info). ~30-50 LOC. Sprint 45 side-lane (or appended to T4).
  - **Rumen-tick stale-job investigation** — `last_job_completed_at: 2026-04-16T03:30:00.956Z` (2 weeks old) but insights still landing daily (`latest_insight_at: 2026-05-01T02:00:08.194Z`, 307 total / 243 unseen). Either rumen-tick is partial-succeeding (insights inserted, job-row not finalized) or a different writer (graph-inference cron, MCP-side classifier) is doing inserts without creating `rumen_jobs` entries. Independent finding from this morning's DNS diagnosis. Sprint 45+ candidate.
  - **`mnestra doctor` subcommand** (Brad's third upstream suggestion 2026-04-28). Still queued.
- **`docs/INSTALL-FOR-COLLABORATORS.md` refresh queued in close-out add-on:** the "DEFER Rumen tier" guidance flips to "do it" because Sprint 43 T3 fixed the wizard. Pin to v0.13.0 / v0.4.8 versions.
- **Stack-installer audit-trail bump** from 0.4.7 → 0.4.8 per RELEASE.md convention (validates against `@jhizzard/termdeck@0.13.0`).

## [0.12.0] - 2026-04-30

### Added — Sprint 43: Graph viewer controls + flashback persistence + init-rumen wizard repair + Telegram orchestration

> **Theme:** Sprint 43 (4+1, **~17 minutes** wall-clock — 5 minutes over Sprint 42's 12-min and 8 over Sprint 41's 9-min records, but still in the same order of magnitude). Closes the visibility gaps that surfaced after Sprint 42's substrate hardening: the graph viewer was sparse and uncontrollable at default density; flashback-diag was in-memory only so "did flashback fire?" was unanswerable; the init-rumen wizard was broken for fresh users without a sibling rumen repo; and the Telegram orchestration story was an aspirational stub. All four close. Sprint 43 also produced an unexpected strategic upgrade: **T4 redirected from "ship a third-party Telegram MCP" to "use the Anthropic-official `telegram@claude-plugins-official` plugin"** — dramatically superior runtime + permission-relay capability that lets Joshua approve tool calls from his phone via inline buttons.

### Lanes

- **T1 — Graph viewer controls (hide isolated, min-degree, time window, layout selector).** NEW `packages/client/public/graph-controls.js` (217 LOC, dual-export pure-function module — `module.exports` for `node:test`, `window.GraphControls` for the browser, loaded via `<script>` before `graph.js`). Five pure functions: `computeDegrees`, `windowMinTime`, `applyControls`, `encodeControls`, `decodeControls`. Toolbar gains a second row (`.graph-filters-row2`) with four controls; chip-styled edge-type filters from Sprint 38 stay in row 1 unchanged. Layout switcher is force-replacement (force / hierarchical via BFS-level→forceY / radial via forceRadial keyed to inverse degree) — no DAG re-layout needed. URL state codec handles `hideIsolated` / `minDegree` / `window=7d|30d|90d|all` / `layout=force|hierarchical|radial` keys. NEW `tests/graph-controls.test.js` (341 LOC, **22/22 green**) — degree counting with both raw-id and d3-mutated object endpoints, time-window cascade, BFS hierarchy levels, radial-radius hub-vs-leaf ordering, URL codec round-trip + default stripping + invalid-value rejection, **realistic 1042/392/719 termdeck-density stub asserts ≤323 visible after hide-isolated**. `style.css` +56 LOC (`.graph-control` selects + checkbox, "X of Y visible" pill that lights up only when filters are active). Sprint 43 T1.

- **T2 — Flashback persistence + audit dashboard.** NEW SQLite migration `migrations/001_flashback_events.sql` (43 LOC — canonical schema source-of-truth, exec'd by `database.js::initDatabase` with inline-fallback constant for packaged installs that don't ship the `migrations/` directory). Schema: `flashback_events(id PK, fired_at, session_id, project, error_text, hits_count, top_hit_id, top_hit_score, dismissed_at, clicked_through)` + indexes on `fired_at DESC` and `session_id`. `flashback-diag.js` extended with `recordFlashback / markDismissed / markClickedThrough / getRecentFlashbacks / getFunnelStats` — all SQLite-aware, all no-op gracefully when `db` is null so existing tests + DB-unavailable installs don't crash the live emit path. The Sprint 39 in-memory ring (`log/snapshot/_resetForTest/RING_SIZE`) stays intact so the live UI is unchanged. NEW routes `GET /api/flashback/history?since=<ISO>&limit=N` (returns `{count, events, funnel}` in one round-trip), `POST /api/flashback/:id/dismissed`, `POST /api/flashback/:id/clicked`. WS frame `proactive_memory` extended with `flashback_event_id` so the client can post dismiss/click-through events back. NEW `packages/client/public/flashback-history.html` + `flashback-history.js` standalone dashboard with a 3-tier funnel (fires → dismissed → clicked-through), filterable table (24h/7d/30d/all), and an explicit zero-state message that names diag-ring queries Joshua should run when 0 fires shows. Topbar gains a single nav button mirroring the Sprint 38 graph-button pattern. Tests: NEW `tests/flashback-events.test.js`, **31/31 green** (no regression to Sprint 39's 17 flashback-diag tests). **Known follow-up flagged by lane:** the client-side `triggerProactiveMemoryQuery` path (`app.js:543`) is parallel to the server-side WS-emit path and does NOT write to `flashback_events`; deduplicating or also-auditing the client-side path is Sprint 44+ work. Funnel numbers represent server-side-WS-emit fires only. Sprint 43 T2.

- **T3 — `init --rumen` wizard repair: bundle Edge Function source + deploy both functions.** Sprint 42 close-out hit `Error: entrypoint path does not exist (supabase/functions/rumen-tick/index.ts)` because `rumenFunctionDir()` (`packages/server/src/setup/migrations.js:76`) resolved npm-first then bundled-fallback (opposite of `listMnestraMigrations`/`listRumenMigrations`'s bundled-first since v0.6.8) and the `@jhizzard/rumen` npm package's `files` glob doesn't include `supabase/functions/`. T3 picks **option (b) from the lane brief** — bundle the Edge Function source into TermDeck. NEW `packages/server/src/setup/rumen/functions/graph-inference/{index.ts, tsconfig.json}` (copied verbatim from sibling rumen repo via the new sync script; verified in `npm pack --dry-run` — 13.7 kB index.ts now in tarball alongside the existing rumen-tick files). `migrations.js` +44/-9 LOC: NEW `rumenFunctionsRoot()` (bundled-FIRST resolution, mirrors the migration-loader pattern locked in v0.6.8) and `listRumenFunctions()` (auto-discovers function-name subdirs containing `index.ts`); `rumenFunctionDir()` retained as a back-compat shim. `init-rumen.js` +71/-50 LOC: `deployFunction → deployFunctions(rumenVersion, dryRun)` iterates the function list and runs `supabase functions deploy <name> --no-verify-jwt` for each in the same staging dir; `stageRumenFunction → stageRumenFunctions` lays out a multi-function `<stage>/supabase/functions/<name>/...` tree with one `config.toml` carrying one `[functions.<name>]` block per function; `__RUMEN_VERSION__` substitution gated by `FUNCTIONS_WITH_VERSION_PLACEHOLDER` set (rumen-tick only). `printNextSteps` now lists both Edge Function URLs, both pg_cron schedules, and both Vault key names. NEW `scripts/sync-rumen-functions.sh` (94 LOC, executable, idempotent — re-injects `__RUMEN_VERSION__` placeholder on rumen-tick after copy from sibling repo). `package.json` adds `"sync-rumen-functions"` script. NEW `tests/init-rumen-deploy.test.js` (155 LOC, **12/12 green**) — bundled-functions invariant (3), `migrations.js` exports (3), `stageRumenFunctions` contract (6). `docs/RELEASE.md` +13/-3 LOC: NEW Step 1 "Sync bundled Rumen Edge Functions" (re-numbers subsequent steps; Step 2 verify-tarball now spot-checks both function `index.ts` paths). 55/55 init-rumen + migrations adjacent suite green; 118/118 CLI + setup adjacent suite green. **Acceptance criteria fully met:** fresh users no longer need a sibling rumen checkout; `init --rumen` deploys BOTH `rumen-tick` AND `graph-inference`. Sprint 43 T3.

- **T4 — Telegram orchestration: pivot to Anthropic-official plugin (NOT a third-party MCP).** Lane discovery rejected three npm candidates (`chigwell/telegram-mcp` broken, `@chaindead/telegram-mcp` uses MTProto user-account login not Bot API, `telegram-mcp-bot` lacks chat-ID allowlist) and surfaced the **Anthropic-official `telegram@claude-plugins-official` plugin** already cached at `~/.claude/plugins/marketplaces/claude-plugins-official/external_plugins/telegram/` (server.ts, v0.0.6). Dramatically superior: bidirectional via MCP `notifications/claude/channel`; pairing-then-allowlist flow built-in (more robust than env-var allowlist); `/telegram:configure` and `/telegram:access` skills handle setup; **permission-relay capability** (Joshua approves tool calls from his phone via inline buttons — bonus the brief didn't ask for); anti-prompt-injection guidance baked into the MCP `instructions` block; 409-conflict orphan-killer prevents zombie pollers. Lane deliverable is the **rewritten `docs/TELEGRAM-ORCHESTRATION.md`** (+201/-117 LOC, now 308 lines): architecture diagram, 8-step bootstrap (Bun install → BotFather → `/plugin install` → `/reload-plugins` → `/telegram:configure` → relaunch with `--channels` → DM bot → `/telegram:access pair` + `policy allowlist`), raw-API round-trip test, TermDeck command conventions, permission-relay, threat model, revocation flow, troubleshooting × 5. **No code changes** — the plugin is the MCP; state lives at `~/.claude/channels/telegram/` (managed by the plugin); TermDeck's `~/.termdeck/secrets.env` and `~/.claude.json` are not touched. Joshua completed the bootstrap end-to-end during sprint close: bot is **@JoshTermDeckBot**, paired user ID **6943410589**, allowlist policy active. **Discovered gotcha (memory-recorded):** the listener requires a **separate `claude --channels` process** — `/reload-plugins` does NOT activate it. Sprint 43 T4.

### Notes

- **Coordinated four-package release:** `@jhizzard/termdeck@0.12.0` + `@jhizzard/termdeck-stack@0.4.7` (audit-trail). Mnestra and Rumen unchanged at `@0.3.3` / `@0.4.4` (no API change required from any lane in this sprint).
- **Tests:** TermDeck root suite **544 tests, 541 pass, 3 skipped, 0 fail** (was 479 at Sprint 42 close — +65 from T1/T2/T3 new tests). Server suite 0 fail. WS handler-contract parity guard re-runs green with the new `proactive_memory` `flashback_event_id` field present in both main and reconnect switches.
- **Sprint 43's strategic upgrade — Telegram permission-relay.** The lane originally scoped a basic outbound Telegram MCP. The pivot to the official plugin gives bidirectional + permission-relay (approve tool calls from the phone) for free. This unlocks "in bed, in airplane, on commute" orchestration where the Joshua-tap requirement no longer requires a laptop. The pattern: orchestrator session running locally → tool needs permission → inline buttons fire to Joshua's phone via Telegram → tap → orchestrator continues.
- **Stack-installer audit-trail bump** from 0.4.6 → 0.4.7 per RELEASE.md convention (validates against `@jhizzard/termdeck@0.12.0`). RELEASE.md itself was modified in this sprint (T3) — a NEW Step 1 "Sync bundled Rumen Edge Functions" runs ahead of the existing publish flow so future releases automatically pick up Edge Function source updates from the sibling rumen repo.
- **What's queued for Sprint 44:** the deferred client-side proactive-memory audit-write gap (T2 finding), the multi-agent substrate trilogy (Sprint 44/45/46 — see `docs/multi-agent-substrate/DESIGN-MEMORIALIZATION-2026-04-29.md`), and the deferred `mnestra doctor` subcommand from Brad's 2026-04-28 third upstream suggestion.

## [0.11.0] - 2026-04-29

### Added — Sprint 42 T2: TermDeck PTY orphan reaper + observability route

- **NEW `packages/server/src/pty-reaper.js`** (~230 LOC) — `createPtyReaper({ sessions, intervalMs=30000, ps, kill, now, logger })` factory with injectable seams for hermetic testing. Each tick walks `ps -e -o pid=,ppid=,command=`, builds a `childrenOf` map, BFS-refreshes per-session descendant registry while each session leader is alive, kills surviving descendants when leader is gone or `session.meta.status === 'exited'`. Three reap reasons (`leader_dead` / `session_exited` / `session_removed`); ESRCH-safe kill; RING_SIZE=200 reaped-history ring; idempotent start/stop; `_resetForTest` for deterministic tests.
- **NEW `GET /api/pty-reaper/status` route** returns `{ tickCount, lastTickAt, intervalMs, registry, reapedHistory, reapedCount }` for observability. Mirrors the Sprint 39 `flashback-diag` ring-buffer + structured-log pattern (single source of truth for reaped-history, no separate logging path).
- **Wired at `createServer` boot** (gated on `pty` availability + opt-out via `TERMDECK_PTY_REAPER` env or `config.ptyReaper.enabled`); `ptyReaper` exported from `createServer` return for tests + future CLI integration. Shutdown hook ensures clean stop.
- **Why this lane was a substrate-reliability gap.** 2026-04-28 morning incident: `forkpty: Device not configured` blocked Joshua from opening any new terminal. Root cause: 585 PTY/tty references open vs `kern.tty.ptmx_max = 511`. MCP children (rag-system, imessage-mcp) escape via `setsid` and reparent to launchd, holding their PTY fds after their Claude Code parent terminates. The reaper detects the orphans and kills them. Tests: NEW `tests/pty-reaper.test.js` (15 tests — 15/15 green, hermetic with stubbed `ps`/`kill`).

### Added — Sprint 42 T4: Project removal + PTY drag/drop reordering

- **NEW `DELETE /api/projects/:name?force=true` route** with full safety semantics. 404 if unknown, 409 if any session has `meta.project===name && meta.status!=='exited'` unless `?force=true`, 400 on bad-name regex, 200 on success. Success envelope: `{ ok, removed, forced, projects, files_on_disk: 'untouched' }` — the `files_on_disk: 'untouched'` field is explicitly serialized to underline the contract for any client that confuses project removal with file deletion. 409 response includes `liveSessions` count + `sessionIds` array for clean UI affordance.
- **NEW `removeProject(name, configPath)` in `packages/server/src/config.js`** mirroring `addProject`: regex-validate, `NOT_FOUND` / `BAD_NAME` `err.code`s for HTTP mapping, `.bak` before overwrite, returns post-delete projects map.
- **NEW `packages/server/src/projects-routes.js`** (~110 LOC) `createProjectsRoutes({ app, config, getSessions, addProject, removeProject, broadcast })` factory mirroring `sprint-routes.js` style. Hosts both POST and DELETE; the existing inline POST at `index.js:1269` was moved into this factory for symmetry. Broadcasts `projects_changed` WS event (matches `config_changed` shape; both main panel and reconnect WS handlers route it through `setState({ projects })` for cross-tab sync).
- **NEW project-removal modal** (`#removeProjectModal`) with **explicit "Files on disk are untouched" `<strong>` callout**, project select, warning banner for live-PTY 409 prompt, and confirm button that flips to "remove anyway" with `dataset.force='true'` after a 409 response (single-click escalation, no double-prompt friction).
- **PTY panel drag/drop reordering** in `packages/client/public/app.js`. Handle-gated HTML5 drag (`.panel-drag-handle` cursor-grab grip in `.panel-header-left`); drop-position determined by cursor x within target (left half = insertBefore, right half = insertAfter). DOM-only reorder — `state.sessions` insertion order remains canonical for Alt+1…9 and `panel-index` so inject identifiers (session UUIDs) are untouched.
- **Tests:** NEW `tests/projects-routes.test.js` (13 tests, 13/13 green) covers happy path + broadcast, 400 bad name, 404 unknown, 409 with multi-session counting + exited filter + cross-project filter, `?force=true` override, `removeProject` `.bak` + map-update, NOT_FOUND / BAD_NAME / empty-map cases. Full root suite **479 tests, 475 pass, 4 skipped, 0 fail** — including the WS handler-contract parity guard which now sees `projects_changed` in both the main and reconnect switches.

### Changed — Sprint 42 T3: migration-templating + init-rumen applies 002 AND 003

- **NEW `packages/server/src/setup/migration-templating.js`** (~70 LOC) — pure `applyTemplating(sql, vars)` helper supporting both `<project-ref>` and `{{PROJECT_REF}}` placeholder syntaxes; idempotent on no-placeholder inputs; throws on missing-vars-when-placeholder-present. NEW `tests/migration-templating.test.js` (10 tests, 10/10 green) covers both syntaxes, multi-occurrence, no-placeholder pass-through, idempotency, missing-vars throw, non-string-input throw, and live-bundle integration against migrations 002+003.
- **`packages/cli/src/init-rumen.js::applySchedule` rewritten** from inline-substitute-002-only to a `SCHEDULE_MIGRATIONS` table + per-migration substitute-then-apply loop covering BOTH 002 (rumen-tick) AND 003 (graph-inference-tick), sharing one Postgres connection. Before this lane, migration 003 was bundled but never applied during fresh install — so the cron schedule never landed for greenfield users (Brad-class). Now it does.

### Changed — Sprint 42 T1 (paired with `@jhizzard/rumen@0.4.4`): graph-inference Edge Function now uses LATERAL+HNSW

- The Rumen graph-inference Edge Function was rewritten from naive nested-loop to LATERAL+HNSW per-row top-K (`O(N²) → O(N log K)`). 11x perf improvement on petvetbid corpus (13.5s vs 150s+). With T3's templating fix shipping the cron schedule on fresh install, the full path (substituted migration → scheduled cron → rewritten Edge Function → ~360 cross-project edges per first tick on petvetbid) is end-to-end ready.
- See [@jhizzard/rumen@0.4.4 CHANGELOG entry](https://github.com/jhizzard/rumen/blob/main/CHANGELOG.md) for the SQL rewrite + EXPLAIN-plan validation details.

### Notes

- **Sprint 42 wall-clock: ~12 minutes** (inject 21:38 → last lane DONE 21:50 ET on 2026-04-28). One minute over Sprint 41's 9-minute record but within the same order of magnitude. All four lanes shipped DONE in their first attempt; no FINDING-FIX-FINDING-FIX iteration loops. Joshua ran the orchestration remote-control from his phone in bed; orchestrator (Claude Opus 4.7 1M context) injected the four lanes via the two-stage submit pattern from another session, with one `/poke cr-flood` recovery on T4 at the 8s verify check.
- **Coordinated four-package release:** `@jhizzard/termdeck@0.11.0` + `@jhizzard/termdeck-stack@0.4.6` + `@jhizzard/mnestra@0.3.3` + `@jhizzard/rumen@0.4.4`. Three of those (termdeck, stack, rumen) ship functional changes; mnestra is the cosmetic `main`/`types` fix paired with the stack.
- **Operational close-out remaining (Joshua's Passkey + supabase CLI):** publish the four packages, then `cd ~/Documents/Graciella/rumen && supabase functions deploy graph-inference --project-ref luvvbrpaopnblvxdxwzb`, then re-enable the `graph-inference-tick` cron via the now-templated migration 003 path, then manually fire once via the cron's `pg_net` to confirm the ~360 cross-project edges land.
- **Stack-installer audit-trail bump** from 0.4.5 → 0.4.6 per RELEASE.md convention (validates against `@jhizzard/termdeck@0.11.0`). No installer code changes; the migration-apply logic lives in `init-rumen.js`, not stack-installer (T3 finding).
- **What's queued for Sprint 43+:** the deferred `mnestra doctor` subcommand (Brad's third upstream suggestion); the LLM-classification pass on the residual 40 chopin-nashville rows (mostly correctly-tagged competition content per Sprint 41's verdict); per-project recency half-life in `memory_recall_graph`; auto-detection of project boundaries from on-disk markers.

## [0.10.4] - 2026-04-28

### Fixed — Brad UX hotfix: panel focus, tour-Enter swallow, guide-rail overlap, graph empty-state CSS specificity

Four small client-side patches addressing Brad Heath's 2026-04-28 panel-UX report ("New behavior of panels super confusing. Selected panel always in upper left. Hitting enter from full screen goes to matrix again. Easy to put wrong response into a chat. Did it several times.") plus a graph empty-state regression Joshua surfaced during validation.

- **`focusPanel` now transfers xterm keyboard focus to the focused panel** (`packages/client/public/app.js`). Previously the function only added the `.focused` CSS class — keystrokes continued going to whichever element had DOM focus before (often the launcher input, which submits a NEW terminal on Enter, or the previously-focused panel). Now mirrors the `entry.terminal.focus()` call from `focusSessionById`. Direct cause of "Easy to put wrong response into a chat. Did it several times" — the user's perception of which panel was active diverged from where keystrokes actually landed.

- **Tour Enter/Arrow swallow now respects terminal + input focus** (`packages/client/public/app.js`). The document-level keydown handler (`if (tourState.active) ...`) previously intercepted Enter/ArrowRight/ArrowLeft unconditionally to advance/exit the tour. Now bails early when `e.target` is inside a `.term-panel`, an `<input>`, a `<textarea>`, or any contentEditable element — so terminal Enter (Claude Code submit, shell command submit) and form-input Enter (launch prompt, modal Save) pass through to their actual handlers. The v0.10.0 onboarding tour ("cover v0.10.0 surface") had been re-firing on first-launch post-upgrade and eating Enter keystrokes mid-typing; this patch is the fix.

- **Grid container reserves 38px right padding for the collapsed guide-rail** (`packages/client/public/style.css`). The orchestrator guide-rail is `position: fixed; right: 0; width: 32px` when collapsed (Sprint 37 Phase C); previously the grid extended underneath it, which left the rightmost panel's close (×) button unclickable beneath the guide toggle strip. Now reserves 32px (rail) + 6px (breathing) on the right of `.grid-container` so right-edge panel controls are always reachable. The expanded-guide state already paints its own backdrop+shadow over panels — that's pre-existing behavior unaffected by this padding.

- **Graph empty-state CSS specificity fix — `Loading graph…` no longer paints behind `No memories yet`** (`packages/client/public/style.css`). Sprint 41 T3 fixed the JS race ordering (reset state, hide overlays, then set the right one) but the CSS rule `.graph-loading { display: flex; ... }` above the empty-state block was higher-specificity than the UA default `[hidden] { display: none }` selector. Result: when graph.js called `el.hidden = true` to hide the loading spinner, it had no visual effect — class > attribute. Empty-state then painted on top, producing Joshua's screenshot of both overlays stacked. Added explicit `.graph-loading[hidden], .graph-empty[hidden] { display: none }` rule to override class-level `display: flex`. One-line fix; no JS or HTML changes.

### Notes

- **Brad-side context.** Brad surfaced the first three issues this morning while validating the v0.10.x upgrade after the npm cache trap was cleared. Joshua surfaced the fourth (graph) during browser-side smoke validation of the first three. All four fixes ship together because the iteration cost on a single hotfix release is the same as one — and stack-installer audit-trail bumps are cheaper batched.
- **No server-side changes.** All four fixes are client-only — `app.js` (two patches) and `style.css` (two patches). Server tests 35/35 still pass; node syntax-check on `app.js` clean.
- **Stack-installer audit-trail bump** from 0.4.4 → 0.4.5 per RELEASE.md convention (validates against `@jhizzard/termdeck@0.10.4`). No installer code changes.

## [0.10.3] - 2026-04-28

### Fixed — Sprint 41: Project taxonomy redesign + chopin-nashville junk-drawer cleanup + graph empty-state UX

> **Theme:** Sprint 41 (4+1, **9 minutes** wall-clock from kickoff to last lane DONE — fastest sprint in project history, beating Sprint 38's 23-min record) closes the project-tagging junk-drawer that's been polluting the `chopin-nashville` tag since the directory's PROJECT_MAP regex was first authored. The headline result: **`chopin-nashville` count drops from 957 → 40** (95.8% reduction; acceptance target was <100). The 40 remaining rows are now ALL legitimate Chopin Nashville competition-management content. Companion: T1 ships `docs/PROJECT-TAXONOMY.md` as the canonical project-tagging reference, T2 the deterministic re-tag migration, T3 the long-standing graph-view UX bug (Loading + No memories yet + visible nodes simultaneously), T4 the LLM-classification pass that did the heavy lifting on the residue.

### Lanes

- **T1 — Project taxonomy authoring + PROJECT_MAP overhaul.** NEW `docs/PROJECT-TAXONOMY.md` (154 LOC) is the canonical reference for project tagging. Joshua's personal `~/.claude/hooks/memory-session-end.js` (out-of-repo) was rewritten to a 13-entry most-specific-first ordered taxonomy: deep code-project patterns first (termdeck, mnestra, rumen, rag-system, podium, chopin-in-bohemia, chopin-scheduler, pvb, claimguard, dor, portfolio, imessage-reader), `chopin-nashville` as the last catch-all so it only matches when no specific code-project pattern fired first. Adopted the Sprint 38 P0 module-export contract (`require.main === module` + `module.exports = { detectProject, PROJECT_MAP }`) so a unit test can `require()` the hook without hanging the stdin reader. NEW `tests/project-taxonomy.test.js` (185 LOC, 25 cases) pins the taxonomy. `packages/stack-installer/assets/hooks/README.md` updated with a "Order matters: most-specific-first" walkthrough and a worked example block referencing the new taxonomy doc. **Maestro = chopin-scheduler alias** documented per orchestrator's mid-inject clarification — same project, two names; on-disk dir is `SchedulingApp/`. Sprint 41 T1.

- **T2 — Re-tag migration with new taxonomy.** NEW `012_project_tag_re_taxonomy.sql` (397 LOC, byte-identical mirror in TermDeck and Mnestra repos). Eight buckets (broadest-first): termdeck (8 keywords vs 011's 3), rumen (3 keywords vs 1), podium (unchanged), **chopin-in-bohemia (NEW)**, **chopin-scheduler+Maestro alias (NEW; case-sensitive POSIX `\mMaestro\M` regex)**, pvb (unchanged), **claimguard (NEW; bare "gorgias" excluded — gorgias and gorgias-ticket-monitor are separate categories)**, dor (verbatim from 011's tightened pattern; naked `%dor%` ILIKE is BANNED to avoid false positives like "Doris"). Every UPDATE gated by `WHERE project = 'chopin-nashville'` (idempotent on re-run). 11 RAISE NOTICE probes (BEFORE + 8 buckets + AFTER + acceptance-target message), all prefixed `[012-retaxonomy]` for grep-ability. NEW `tests/migration-012-shape.test.js` (285 LOC, 12 structural tests). `tests/migration-loader-precedence.test.js` bumped from 11 → 12 expected migrations. **Live apply: 957 → 896** (–61 deterministic re-tags; substantially smaller than expected because Sprint 39's 011 backfill already caught the easy cases — T2's job here was filling the gaps T4 couldn't reach with pure keyword matching). Sprint 41 T2.

- **T3 — Graph empty-state UX fix + "All projects" picker.** Closes the three-way race Joshua reported via screenshot 2026-04-28: Loading + No memories yet + visible nodes rendered simultaneously when `fetchGraph` switched modes. NEW `clearGraphSvg()` helper stops the d3 simulation, drops it to null, removes children of `#graphZoomRoot`, and nulls the three D3 selections. NEW `showToast(msg, durationMs=6000)` for the truncated-result warning. `fetchGraph` rewritten to do the four-step reset (`hideEmpty` → `clearGraphSvg` → reset `state.nodes/edges` → `setLoading`) BEFORE the API await. `showEmpty()` now sets BOTH title and body for project / memory / `__all__` modes (was sticky on `graphEmptyBody`). NEW `GET /api/graph/all` route in `packages/server/src/graph-routes.js` (+100 LOC) with `MAX_NODES_GLOBAL = 2000` cap and `truncated` flag. NEW "All projects" option in the picker dropdown (`__all__` sentinel preserved across URL state). NEW empty-state "View All Projects" button (uses `pointer-events: auto` to defeat the parent overlay's `pointer-events: none`). `tests/graph-routes.test.js` extended with 5 new tests (27 total). 5 files, +399 / -23 LOC. Sprint 41 T3.

- **T4 — LLM-classification of residual uncertain rows.** NEW `~/Documents/Graciella/engram/migrations/013_reclassify_uncertain.sql` (39 LOC) adds `reclassified_by` + `reclassified_at` columns to `memory_items` plus a partial index filtered to non-NULL rows. NEW `scripts/reclassify-chopin-nashville.js` (335 LOC) — Haiku 4.5 batch classifier. CommonJS, idempotent via `reclassified_by IS NULL OR <> 'sprint-41-llm-residual'` filter, transactional per-batch UPDATE, `--dry-run` flag, `--max-batches=N` cap (default 50). 14-tag canonical taxonomy in the Haiku prompt (matches T1's `docs/PROJECT-TAXONOMY.md`). Strips ` ``` ` JSON fences defensively. Validates returned project tags against the 14-tag whitelist; out-of-list classifications are logged as `invalid` and the row stays untouched. `package.json` adds `@anthropic-ai/sdk@^0.39.0` to `devDependencies` (matches engram@^0.39.0; never user-runtime). **Live apply: 896 → 40** chopin-nashville rows after 45 batches × ~20 rows = 896 classified, **0 errors, 0 invalid responses, ~$0.18 actual spend.** Per-tag delta: termdeck +261, global +204, mnestra +162, podium +71, rumen +64, portfolio +42, chopin-scheduler +25, dor +14, rag-system +11, chopin-in-bohemia +2. The 40 rows the LLM voted to keep as `chopin-nashville` are the legitimate residue (stamped `reclassified_by = 'sprint-41-llm-residual'` for audit). Sprint 41 T4.

### Notes

- **Sprint 41 wall-clock: 9 minutes from kickoff (12:51 ET) to last lane DONE (T2 at 13:01 ET) — fastest sprint in project history.** Beats Sprint 38's 23-min record by 14 minutes. Cleanest inject too: all four panels reached `status=thinking` simultaneously on first attempt, no `/poke` recovery needed. Single mid-inject orchestrator clarification (Maestro = chopin-scheduler alias, posted to STATUS.md preamble at 12:51 ET) was picked up by T1 in its FINDING and propagated to T2 + T4's deliverables without further intervention.
- **Final substrate state at sprint close:** 5,956 total memory_items. Top tags: pvb 1,605, chopin-scheduler 1,024 (Maestro), termdeck 965, global 601, gorgias 468, gorgias-ticket-monitor 207, podium 194, mnestra 167, rumen 163, imessage-reader 114, portfolio 42, **chopin-nashville 40 (down from 957 → 947 → 896 → 40 across Sprints 39 + 41)**, antigravity 30, chopin-in-bohemia 19 (new tag), dor 16, claimguard 15, rag-system 13. The chopin-nashville junk-drawer is officially closed.
- **Companion releases:** `@jhizzard/termdeck-stack@0.4.4` audit-trail bump. `@jhizzard/mnestra@0.3.1` ships migration 012 + 013 mirrors only — no `dist/` changes. (Mnestra's repo working tree carries 012 + 013 untracked at sprint close; orchestrator commits at the close-out point.) Rumen unchanged.
- **Joshua's roadmap after Sprint 41:** 1-2 ClaimGuard-AI sprints → Chopin in Bohemia → re-start Maestro (chopin-scheduler app). The taxonomy fix landing now means the next project switches won't re-pollute the chopin-nashville tag — every code project under the umbrella has its own specific PROJECT_MAP entry now.
- **One known leftover:** Sprint 39+ T2-followup (task #19) — graph-inference cron stays disabled until LATERAL+HNSW SQL rewrite ships. Not blocking; rag-system's MCP-side classifier continues to write edges in real time as Joshua's daily-driver flow generates new memories.

## [0.10.2] - 2026-04-27

### Fixed — Sprint 40: Defensive hardening (single-orchestrator, post-Sprint-39)

> **Theme:** Sprint 40 ships the structural defenses that would have caught Sprint 39's 9-day Flashback regression on first CI run. Single-orchestrator overnight (the four lane panels were still doing post-DONE work and weren't safe to inject); 3 lanes shipped (T3 dashboard UI deferred to Sprint 41 because Joshua was asleep and couldn't browser-verify). No companion releases — `@jhizzard/termdeck-stack@0.4.3` unchanged from Sprint 39 close (no installer source touched). No live database writes; no migration; no server restart required for tests to pass.

### Lanes
- **T1 — WS handler contract smoke test.** NEW `tests/ws-handler-contract.test.js` (3 tests). Statically scans `packages/server/src/` for every `JSON.stringify({ type: '<X>' })` emit (both inline and multi-line shapes, recursive across all server `.js` files), scans `packages/client/public/app.js` for every `case '<X>':` literal inside every `ws.onmessage` switch, and asserts every emitted type has a handler in EVERY switch. Includes a parity guard: both switches must have identical case sets (the contract drift between Sprint 39's main panel WS and reconnect WS is exactly the class of bug that compounded the 9-day silence). Provides an `ALLOWED_OMISSIONS` map for documenting deliberate omissions. Surface fix from running the new test: reconnect WS handler at `packages/client/public/app.js:1245+` was missing `case 'config_changed':` (silent drop on reconnected sessions whenever `PATCH /api/config` broadcasts a config change); added with parity comment + idempotent semantics matching the main panel handler. **The new contract test would have caught Sprint 39's missing `case 'proactive_memory':` on first CI run after the gap was introduced.**
- **T2 — Analyzer broadening.** Closed all 3 pre-existing case-sensitivity gaps in `packages/server/tests/session.test.js` that the Sprint 39 T2 audit documented as out-of-scope-for-that-lane false-negatives. `PATTERNS.error` extended to include uppercase `ERROR:`, Node errno colon-prefix shapes (`ENOENT:`, `EACCES:`, `ECONNREFUSED:`); `PATTERNS.errorLineStart` extended to include mixed-case `Fatal` and the `npm ERR!` shape (special-cased outside the alternation because `!` is not a word character so `\b` after `npm ERR!` doesn't match). NEW `PATTERNS.pythonServer.serverError` matches `"GET /foo HTTP/1.1" 503 -` log lines; wired into `_detectErrors` as a python-server-typed fallback when the prose analyzers miss. `packages/server/tests/session.test.js`: 32/35 → **35/35**. Sprint 39 T2 fixtures (`tests/rcfile-noise.test.js`, `tests/analyzer-error-fixtures.test.js`): all still pass — the broadening doesn't reintroduce false positives because every new branch requires structured shape (colon-prefix, errno-token, HTTP-status), not bare keyword presence.
- **T4 — Sprint 39 postmortem + WS message contract docs.** NEW `docs/POSTMORTEM-sprint-39.md` (~6 KB) covering: TL;DR, sprint-by-sprint timeline (Sprint 6 ship → Sprint 21 + 33 + 34 partial fixes → Sprint 38 timeline-lock → Sprint 39 smoking gun), root cause analysis (the actual bug + why the fallback path looked like it worked + why prior fixes missed + why Sprint 39 succeeded), 5 lessons learned, Sprint 40 changes recap, deferred items. NEW `docs/WS-MESSAGE-CONTRACT.md` (~4 KB) — source-of-truth for the 6 WS message types with emit sites (file:line), recipients, frame shape, client handler responsibility, sprint origin. Includes the "adding a new message type" checklist and the "shapes the contract test does NOT cover" caveat.

### Out of scope (deferred from Sprint 40)
- **T3 — `/api/flashback/diag` dashboard surface.** Joshua asleep; UI work needs browser verification per CLAUDE.md ("if you can't test the UI, say so explicitly rather than claiming success"). Deferred to Sprint 41.
- **Server-side `onStatusChange` per-session meta push** (defense-in-depth alongside the client handler) — Sprint 41 candidate.
- **LLM-classification backfill for the ~876 chopin-nashville "other/uncertain" rows** — Sprint 41+ candidate.
- **Sprint 38 follow-up: graph-inference SQL rewrite (LATERAL + HNSW)** — task #19, deferred.
- **Out-of-repo:** `~/.claude/hooks/memory-session-end.js` PROJECT_MAP forward-fix is Joshua's morning checklist item, not orchestrator territory.

### Test status
- TermDeck top-level `tests/`: **394 tests, 391 pass, 0 fail, 3 skipped** (was 391/388/0/3 at Sprint 39 close, +3 net new from T1's three contract tests). Zero new failures.
- TermDeck `packages/server/tests/`: **35/35 pass** (was 32/35; T2 closed all 3 case-sensitivity gaps).
- All Sprint 39 fixture corpora still pass: `tests/rcfile-noise.test.js` 8/8, `tests/analyzer-error-fixtures.test.js` 4/4. T2's broadening is purely additive.

### Substrate state
- Sprint 40 made zero database writes. `petvetbid` row counts unchanged from Sprint 39 close. `chopin-nashville=947` remains.
- Server NOT restarted — Sprint 40 changes are server + client + tests + docs only; the running TermDeck server keeps Sprint 38-baseline runtime. Sprint 39's pending restart still pending Joshua's morning action.

## [0.10.1] - 2026-04-27

### Fixed — Phase D: Flashback Resurrection 2.0 (Sprint 39, diagnostic-first)

> **Theme:** Sprint 39 (4+1, 22 minutes wall-clock from kickoff to last lane DONE — second-fastest sprint in project history) ends a 9-day P0 user-facing regression: Joshua's daily-flow Flashback toast had been silent since approximately Sprint 26 (~2026-04-18) despite Sprint 21 and Sprint 33 both shipping fixes that passed isolated e2e tests. The sprint was deliberately diagnostic-first — four lanes worked in parallel to *measure* where the pipeline was failing before any fix landed. The smoking gun (T4): the server-side WS-push of `proactive_memory` frames works correctly, but `packages/client/public/app.js` had no `case 'proactive_memory':` branch in either of its two `ws.onmessage` switches — every emitted frame went into the void. The actually-functional client path (`status_broadcast` polling for `meta.status === 'errored'`) only catches the ~10–50 ms errored window inside a 2000 ms broadcast cycle (~2.5% of broadcasts), so the client transitions `active → idle` without ever passing through `errored` and `triggerProactiveMemoryQuery` is never invoked. The 3-line orchestrator hotfix at app.js:233 + app.js:1245 closes the loop. T2's `PATTERNS.shellError` tightening and T3's chopin-nashville corpus backfill ship as defense-in-depth + corpus hygiene; both lane investigations independently confirmed their own hypotheses (rcfile noise / project-tag mismatch) were *not* the active root cause for Joshua's silence, but each surfaced real issues worth fixing. Companion release: `@jhizzard/termdeck-stack@0.4.3` (audit-trail patch).

### Lanes
- **T1 — Daily-flow Flashback instrumentation.** NEW `packages/server/src/flashback-diag.js` (53 LOC) — in-memory ring buffer (size 200) with `log` / `snapshot` / `_resetForTest` / `RING_SIZE`. Six decision-point events emitted: `pattern_match` (every PATTERNS hit, with the matched line), `error_detected` (before the rate-limit gate), `rate_limit_blocked` (when the 30s gate rejects), `bridge_query` (with `project_tag_in_filter` + `query_text` + `mode` + `rpc_args` + `duration_ms`), `bridge_result` (with `result_count` + `error_message?` + `top_3_project_tags`), `proactive_memory_emit` (4 outcomes: `emitted` / `dropped_no_ws` / `dropped_empty` / `error`). NEW route `GET /api/flashback/diag?sessionId=&eventType=&limit=` returns `{count, events}`. NEW `tests/flashback-diag.test.js` (250 LOC, 17 tests, all pass). Threading: `sessionId` added to `mnestraBridge.queryMnestra` for session-attributable diag entries on the Flashback path. Zero behavior change in `_detectErrors` — same status transition, same rate-limit semantics, same `onErrorDetected` dispatch.
- **T2 — zsh/bash rcfile-noise filter audit.** Empirical capture rig (`tests/rcfile-noise-fixtures/capture.js`) spawns interactive shells under the same node-pty binding production uses, mirrors TermDeck's prod spawn env, captures the first 5s of output. Eight fixtures committed (zsh-termdeck-prod, bash-termdeck-prod, zsh/bash josh-interactive + josh-login, zsh/bash vanilla). Verdict on Joshua's actual zsh prod-spawn: **0 PATTERNS hits** in the rcfile-init window — the strong rcfile-noise hypothesis is **REFUTED for Joshua's flow**. Verdict on synthetic adversarial corpus: at least one real-world false-positive case found (pyenv user with `command not found in path` shape). Tightened `PATTERNS.shellError` command-not-found branch — the keyword now requires a colon or whitespace-then-EOL terminator instead of bare `\b`. NEW `tests/rcfile-noise.test.js` (245 LOC, 8 tests, all pass) covering captured corpus + synthetic adversarial corpus + 16 canonical real-error regression cases. Pre-existing `session.test.js` failures (3 case-sensitivity gaps) confirmed orthogonal — false-negative gaps, not the false-positive class T2 owns; deferred to Sprint 40.
- **T3 — Project-tag write-path verification + chopin-nashville backfill.** Audited every project-tag write path that lands in `memory_items` (Flashback's read target) and every read path that filters on `project`. Verdict on the bridge-side project-mismatch hypothesis: **REFUTED at the code level** — the bridge passes `session.meta.project` faithfully through `effectiveProject` to the RPC's `filter_project` parameter; SQL function `match_memories` honors exact-string match. **CONFIRMED at the corpus level**: of 1,237 chopin-nashville-tagged rows on live `petvetbid`, 290 carried termdeck/mnestra/rumen/podium/pvb/dor signal (sequential bucketed match), traceable to Joshua's harness session-end hook (`~/.claude/hooks/memory-session-end.js`) PROJECT_MAP iterating against `/ChopinNashville/i` first with no entries for the projects that live under that path. NEW `packages/server/src/setup/mnestra-migrations/011_project_tag_backfill.sql` (230 LOC) — five sequential bucketed UPDATEs wrapped in BEGIN/COMMIT, idempotent (every WHERE clause includes `project='chopin-nashville'`), `RAISE NOTICE` count probes via `GET DIAGNOSTICS ROW_COUNT`. Tightened `dor` bucket using POSIX word boundary `\mDOR\M` + path/identifier markers + `openclaw` substring (eliminates "dormant" / "vendored" / "indoor" false positives observed in T3's heuristic dry-run). NEW `tests/mnestra-bridge.test.js` (130 LOC, 5 tests) pinning the project-tag passthrough invariant so future instrumentation refactors can't silently mutate the filter shape. Per-bucket apply counts (live, post-COMMIT): 146 termdeck/mnestra + 83 rumen + 56 podium + 3 pvb + 2 dor = **290 rows re-tagged**. chopin-nashville: 1,237 → 947 (947 = ~71 legitimate signal + ~876 ambiguous deferred to LLM-classification pass).
- **T4 — Production-flow Flashback e2e test.** NEW `tests/flashback-production-flow.test.js` (357 LOC, 2 cases zsh + bash) — pre-flight server/Mnestra/SQLite probe, corpus probe with `needs-backfill` skip directive, real interactive shell spawn (`/bin/zsh -i` and `/bin/bash`) with rcfile loading, 2.5s rcfile-settle window, transcript probe for rcfile-noise observability, cat trigger, 5s WS frame budget. Asserts: frame received, `hit.content` non-empty, `hit.project ∈ {'termdeck', null}`. Diagnostics: pre-trigger rcfile-noise count, status_broadcast race count (the diagnostic that surfaced the smoking gun), T1 diag dump (gracefully skips when `/api/flashback/diag` returns non-JSON, automatically activates once T1's working-tree changes are loaded by the running server). NEW `tests/_flashback-helpers.js` (219 LOC) — extracted shared utilities. Light-touch refactor of `tests/flashback-e2e.test.js` (zero assertion-byte changes; only inline-utility-to-shared-helper substitution). NEW `tests/fixtures/flashback-production-flow/README.md` (56 LOC). Test executes against current HEAD-baseline server: 2/2 pass.

### Orchestrator-applied at sprint close

- **3-line client hotfix** at `packages/client/public/app.js:237` + `app.js:1245` — added `case 'proactive_memory': showProactiveToast(id, msg.hit); break;` to both `ws.onmessage` switches. This is the actual P0 fix for the 9-day silence: T4's diagnostic confirmed the WS-push pipeline works server-side end-to-end; the client switch had no handler for the frame type the server emits. `showProactiveToast(id, hit)` was already defined at line 509 with the correct signature.
- **flashback-e2e.test.js assertion shape correction** at lines 466–501 — converted from `proactiveMemoryFrame.memories[]` array shape (which never matched the production emit shape, so the assertion was structurally unreachable) to the actual single-hit shape (`proactiveMemoryFrame.content` and `proactiveMemoryFrame.project` directly). The previously-failing `project-bound flashback` test now passes against the orchestrator-edit working tree.
- **T3 cwd-propagation 2-liner** in `packages/server/src/index.js` at the `onErrorDetected` call (Flashback path) and at `/api/ai/query` — added `cwd: sess.meta.cwd` (or `session.meta.cwd`) at the top-level call AND inside `sessionContext`. Defensive (Joshua's daily flow always has explicit `meta.project`, so the cwd fallback path doesn't fire on his sessions today), but eliminates a silent-fallback failure mode for any session created without an explicit project.
- **Migration-loader-precedence cascading test fix** at `tests/migration-loader-precedence.test.js` — bumped the migration-count assertion from 10 to 11 in two places and appended `'011_project_tag_backfill.sql'` to two expected-array deepEqual assertions.
- **Migration 011 applied to live `petvetbid`** — see counts under T3 above.

### Test status

- TermDeck top-level `tests/`: **391 tests, 388 pass, 0 fail, 3 skipped** (was 335/336/0/3 pre-Sprint-39, +56 net new tests across the four lanes plus the orchestrator's e2e assertion fix; the previously-failing `project-bound flashback` test now passes).
- TermDeck `packages/server/tests/`: **32/35 pass** — the 3 remaining failures are the same pre-existing case-sensitivity / missing-feature gaps documented in PLANNING.md and re-confirmed by T2 (uppercase `ERROR:`, lowercase `no such file or directory` / `ENOENT:`, HTTP 5xx server log shape). Sprint 40 candidate.
- No new failures introduced by any of the four lanes or by the orchestrator's hotfixes.

### Substrate state at sprint close

- petvetbid: 5,631 memory_items (was 5,530 at sprint kickoff, +101 from Mnestra ingestion during the 22-min sprint). 778 memory_relationships unchanged (sprint contract: no live-DB writes from lane workers). chopin-nashville tag count after migration 011: 947 (was 1,237).
- graph-inference cron: still UNSCHEDULED (per pre-sprint guidance — LATERAL+HNSW SQL rewrite is task #19, deferred).
- TermDeck server: NOT restarted by orchestrator. The new `/api/flashback/diag` route, the `case 'proactive_memory':` client handler, and T2's tightened `PATTERNS.shellError` regex all require a server restart to surface in Joshua's running TermDeck. The orchestrator-applied edits are committed; the restart is Joshua's first action in the morning.

### Out of scope (Sprint 40+ ledger)
- Forward-fix to `~/.claude/hooks/memory-session-end.js` PROJECT_MAP — out-of-repo file, Joshua's responsibility. Without it, new sessions under `~/Documents/Graciella/ChopinNashville/...` continue to land under `chopin-nashville`.
- Analyzer broadening for the 3 case-sensitivity gaps + 2 claude-code-matcher gaps T2 documented (`npm ERR!` not in `errorLineStart`, mixed-case `Fatal:` not covered).
- LLM-classification backfill pass for the ~876 chopin-nashville "other/uncertain" rows.
- T1 `/api/flashback/diag` UI surface (today: curl-only).
- Server-side `onStatusChange` per-session `meta` push as an alternative to T4's recommended client-handler fix (defense-in-depth — both could ship, but the 3-line client fix is sufficient).


- Fully-local path: SQLite + local embeddings for Mnestra (currently requires Supabase + OpenAI)
- Multi-user data validation (today's testing is single-developer)
- Control panel dashboard with Yes/No buttons for AI agent permission prompts
- Migration-001 idempotency — `CREATE OR REPLACE FUNCTION` return-type collision when re-running migrations against an already-upgraded store. Sprint-32 candidate, deferred from v0.7.0.
- Rumen-MCP gap — NULL `source_session_id` on memories written via the MCP path means they never reach Rumen synthesis. Sprint-33 candidate.

## [0.10.0] - 2026-04-27

### Added — Phase C part 2: Knowledge graph + visualization (Sprint 38)

> **Theme:** Sprint 38 (4+1, **23 minutes** wall-clock from kickoff to last lane DONE — fastest sprint in project history) brings the dormant `memory_relationships` table to life as a Supabase-native knowledge graph layer, with three new MCP tools for connecting and traversing memories, a nightly inference cron that auto-discovers latent relationships across the corpus, graph-aware recall that re-ranks vector hits through edge-weighted neighborhood expansion, and a D3.js v7 force-directed visualization that doubles as marketing material. The 749 edges quietly populated by Joshua's private `rag-system` MCP-side classifier become the seed graph, and from this release forward the cron pass extends the graph automatically. Sprint 38 also ships the Mnestra-direct bundled hook (Sprint 38 P0 orchestrator-applied pre-sprint task) — the Brad-empty-pgvector-ingestion-gap fix that drops the `rag-system` runtime dependency from `@jhizzard/termdeck-stack`.

> **Companion releases:** `@jhizzard/mnestra@0.3.0` ships in lockstep with three new MCP tools (`memory_link`, `memory_unlink`, `memory_related`) plus `memory_recall_graph`, an extended 8-value `RelationshipType` union, and migrations 009 + 010 mirrored from the TermDeck-bundled installer. `@jhizzard/termdeck-stack@0.4.2` is the audit-trail patch bump.

- **NEW Mnestra graph MCP tools + traversal RPC.** Three new MCP tools land on `@jhizzard/mnestra@0.3.0`: `memory_link(source_id, target_id, kind, weight?)`, `memory_unlink(source_id, target_id, kind?)`, `memory_related(id, depth=2, kind=*)`. Migration `009_memory_relationship_metadata.sql` (110 LOC, byte-identical in TermDeck `packages/server/src/setup/mnestra-migrations/` and Mnestra repo `migrations/`) adds three columns (`weight float`, `inferred_at timestamptz`, `inferred_by text`), expands the relationship-type CHECK from 5 values to 8 (`supersedes / relates_to / contradicts / elaborates / caused_by / blocks / inspired_by / cross_project_link`, underscore convention) via a `DO $$ pg_constraint walk $$` block (the original CHECK is anonymous; a hardcoded `DROP CONSTRAINT IF EXISTS` would silently no-op), and creates `expand_memory_neighborhood(start_id uuid, max_depth int default 2) RETURNS TABLE` — a recursive CTE that traverses edges bidirectionally (CASE-WHEN flips source/target) with cycle protection (`NOT (next_id = ANY (path))`). NEW `~/Documents/Graciella/engram/src/relationships.ts` (~225 LOC) wraps the SQL with full input validation (UUID format, kind enum, weight ∈ [0,1], depth ∈ [1,5], source≠target), uses `ON CONFLICT (source_id, target_id, relationship_type) DO UPDATE` for idempotent upserts (no need to name the auto-generated unique constraint), and stamps `inferred_by = 'mcp:memory_link'` to distinguish tool-driven inserts from cron-inferred and ingest-time edges. NEW `tests/relationships.test.ts` (~290 LOC, **14 tests**) and `tests/mnestra-migration-009.test.js` (110 LOC, 11 tests) — both pass clean. The 749 pre-existing rag-system edges (supersedes 469, elaborates 167, relates_to 91, contradicts 14, caused_by 8) are preserved untouched: every existing value remains valid against the expanded CHECK. Sprint 38 T1.
- **NEW edge-inference cron job.** Nightly pairwise cosine-similarity pass over `memory_items`; inserts/updates `memory_relationships` rows above a configurable threshold (default 0.85). NEW `packages/server/src/setup/rumen/migrations/003_graph_inference_schedule.sql` (49 LOC) schedules the `graph-inference-tick` pg_cron job on a `0 3 * * *` UTC cadence (3 AM UTC = 11 PM ET, after Joshua's typical work hours), mirroring `rumen-tick`'s vault-backed `Authorization: Bearer ${decrypted_secret}` pattern with idempotent `cron.unschedule WHERE EXISTS`; vault key name `graph_inference_service_role_key` is distinct from `rumen_service_role_key` so a key rotation on one cron does not affect the other. NEW `~/Documents/Graciella/rumen/supabase/functions/graph-inference/index.ts` (335 LOC, Deno) is the Edge Function: pairwise SQL self-join on `memory_items` with pgvector `<=>` op, threshold + since-watermark + LIMIT (`m1.id < m2.id` to avoid double-counting), `ON CONFLICT (source_id, target_id, relationship_type) DO UPDATE` guarded on `weight IS NULL OR inferred_at IS NULL OR inferred_at < now() - interval '7 days'` (so re-classification only fires on new or stale edges, not on every run), `RETURNING (xmax = 0)` to distinguish insert vs update so LLM classification only fires on NEW edges. LLM classification gated by env var `GRAPH_LLM_CLASSIFY=1`; uses Anthropic REST API directly (Deno doesn't bundle the SDK well in Supabase Edge Functions); model `claude-haiku-4-5-20251001`; symmetric 8-type prompt with no `supersedes` bias (rag-system's existing classifier IS biased toward `supersedes` — explained in T2's FINDING in the sprint STATUS.md). Per-tick caps via env: `GRAPH_INFERENCE_THRESHOLD` (0.85), `GRAPH_INFERENCE_MAX_PAIRS` (5000), `GRAPH_INFERENCE_MAX_LLM_CALLS` (200), `GRAPH_LLM_CLASSIFY` (off by default). Awaiting-migration runtime guard returns `{ ok: false, error: 'awaiting migration 009' }` instead of trashing the DB if T1's columns are missing — redundant once T1 has DONE'd but kept for fresh-install-deploy ordering safety. NEW `GET /api/graph/stats/inference` route in `packages/server/src/graph-routes.js` (~95 LOC helper + 12 LOC route) surfaces edge counts, last-inference timestamp, cron-vs-rag-system-vs-MCP breakdown by `inferred_by` namespace; namespace-distinct from T4's `/api/graph/stats` (topology) — pipeline-health vs topology, no route shadow. NEW `tests/graph-inference.test.js` (319 LOC, **20 tests**) — Edge Function structural asserts (Haiku model id, 8-type vocabulary, ON CONFLICT tuple, weight=similarity-not-distance, refresh-policy guard, `inferredByTag` UTC date format, env-var defaults, LLM gate, cost-cap arithmetic, awaiting-migration guard) plus migration shape and HTTP route asserts. Sprint 38 T2.
- **NEW graph-aware recall (`memory_recall_graph` RPC + Mnestra MCP tool).** Two-stage recall: vector seed via `match_memories` → graph expansion via T1's `expand_memory_neighborhood` → re-rank by `vector_score × edge_weight × recency_score`. Migration `010_memory_recall_graph.sql` (147 LOC, byte-identical in both repos) ships the SQL function returning `(memory_id, content, project, depth, vector_score, edge_weight, recency_score, final_score, path)` with `LIMIT 50`. Path-edge weight uses `coalesce(r.weight, 0.5)` — the 749 pre-T2 edges contribute neutrally until T2's first cron pass populates real weights. `DISTINCT ON (memory_id) … ORDER BY memory_id, final_score DESC, depth ASC` keeps the strongest path when a memory is reached multiple ways. Path-edge lookup is undirected (CASE-WHEN matches A→B or B→A) — aligns with T1's bidirectional `expand_memory_neighborhood` contract. 30-day recency half-life via `exp(-age_seconds / (30 * 86400))`. NEW `~/Documents/Graciella/engram/src/recall_graph.ts` (125 LOC) wraps the RPC; reuses the existing `generateEmbedding` + `formatEmbedding` + `getSupabase` helpers; returns `{ hits: GraphRecallHit[], depth_distribution: Record<number, number>, text: string }` where `text` rendering uses `(d{depth} {final_score})` prefix so callers can eyeball vector-vs-graph hits at a glance. NEW `memory_recall_graph` MCP tool in Mnestra. `packages/server/src/rag.js` updated with `graphRecall: bool` config flag (default false), `graphRecallDepth` (default 2, clamped [1,5]), `graphRecallK` (default 10, clamped [1,50]), `graphRecallRecencyHalflifeDays` (default 30, exposed as escape hatch); new `recall(query, options)` method gates on `this.graphRecall`; new `setBridge(bridge)` setter for vector-path delegation; new `_recallViaGraph(query, options)` mirrors `bridge.queryDirect`'s OpenAI-embed → Supabase-RPC pattern. Returns the same `{ memories, total }` shape as the vector path so downstream consumers (Flashback, AI query route) need zero changes when `graphRecall` flips. `packages/server/src/preflight.js` gains a `graph_health` check that queries `select count(*), max(coalesce(inferred_at, created_at)) as last_inference from memory_relationships`; passes with descriptive "disabled" detail when `graphRecall` is off; on: pass if edges > 0 AND last < 48h, warn if edges > 0 AND last > 48h (cron drift), fail if pg unreachable / table missing / zero edges. NEW `tests/memory_recall_graph.test.js` (426 LOC, **14 tests**) covers constructor flag readout + clamping, vector-path-delegates-to-bridge, graph-path-bypasses-bridge, RPC body shape, result-mapping fidelity, error paths, project-filter pinning, preflight pass/fail per substrate, migration SQL contract tokens, cross-repo byte-identity. Full TermDeck suite: 335/336 pass, 3 skipped, 1 pre-existing failure (`flashback-e2e.test.js: project-bound flashback…` — Joshua's already-flagged P0 about flashback regression, unrelated to this lane). Sprint 38 T3.
- **NEW D3.js v7 force-directed graph visualization (marketing-screenshot-ready).** Standalone `/graph.html` page + new dashboard topbar button. Per-project view (all memories tagged with project + their edges) and per-memory neighborhood view (one node + N-hop expansion). NEW `packages/server/src/graph-routes.js` (~310 LOC) exports `createGraphRoutes({ app, getPool })` with `GET /api/graph/project/:name`, `GET /api/graph/memory/:id`, `GET /api/graph/stats`. SQL-side computation of degree + recency, project-scoped edge fetch via `ANY($1::uuid[])`, inline recursive CTE for the per-memory neighborhood (cycle-safe via `path[]`). T2-tolerant: edge SELECT uses `to_jsonb(memory_relationships) AS _row` so the route works whether or not migration 009's `weight`/`inferred_at`/`inferred_by` columns have landed; the `lastInferredAt` lookup is wrapped in a try/catch fallback. Caps at `MAX_NODES_PER_PROJECT = 2000` and `MAX_DEPTH = 4`. Graceful-degrades to `{ enabled: false }` when `getPool()` returns null. NEW `packages/client/public/graph.html` (104 LOC) — standalone page, loads D3 v7 from `https://cdn.jsdelivr.net/npm/d3@7` (zero-build-step preserved). Top bar (project picker + search + re-heat + fit), edge-type filter chips row, SVG stage with radial-gradient backdrop + glow filter, right-side drawer skeleton, tooltip layer. NEW `packages/client/public/graph.js` (~520 LOC) — D3 force simulation with `forceLink` + `forceManyBody(-300)` + `forceCenter` + `forceCollide` and `alphaDecay(0.02)` for graceful settle. Node radius = `sqrt(degree+1) × recency` (exp half-life 30 days); node color hashed onto a 12-hue Tokyo-Night-friendly palette; edge color per `relationship_type` (one per the 8-value vocabulary). Interactions: drag, zoom/pan via `d3.zoom`, click-node opens drawer (fetches `/api/graph/memory/:id?depth=1` for full content + first-hop neighbors), hover dims non-incident edges to 0.08 + non-incident nodes to 0.18, hover-edge tooltip with kind/weight/inferredBy, search pulses matching node strokes, edge-filter chips fade non-active edges to 0 over 200ms (animate, not just hide — per the marketing-gate acceptance criterion), URL state via `?project=` / `?memory=&depth=` for shareable links, neighbor click in drawer navigates the view to that memory. `packages/client/public/style.css` (+427 LOC scoped to `body.graph-page` and `.graph-*` so dashboard styles aren't polluted). NEW `tests/graph-routes.test.js` (**21 tests**, all pass) — pure-helper coverage (UUID/PROJECT regex, snippet truncation, row mappers w/ pre-T2 fallback), function-level coverage of `fetchProjectGraph` / `fetchNeighborhood` / `fetchStats`, HTTP integration over an in-process Express app w/ stub pool covering shape, validation rejection, 404 root-not-found, graceful-degrade. Sprint 38 T4.
- **NEW Mnestra-direct bundled session-end hook (Sprint 38 P0, orchestrator-applied pre-sprint task).** `packages/stack-installer/assets/hooks/memory-session-end.js` rewritten end-to-end (113 → 225 LOC) to drop the `rag-system` runtime dependency. The hook now calls OpenAI `text-embedding-3-small` + Supabase REST `/rest/v1/memory_items` POST directly; one row per session, `source_type='session_summary'`, `category='workflow'`. Module-export contract: when `require.main === module`, runs as a hook (reads stdin); when imported, exposes `detectProject` / `readEnv` / `buildSummary` / `embedText` / `postMemoryItem` / `processStdinPayload` / `PROJECT_MAP` / `LOG_FILE` for unit testability. PROJECT_MAP defaults to **empty** — fresh users add their own entries by editing the installed file in place; previously hardcoded with Joshua's project taxonomy which made no sense to ship to every user. Env-var validation at entry (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `OPENAI_API_KEY`); clear log line names missing vars without blocking session close. Fail-soft contract preserved: any error logs and exits 0. **Coexists** with Joshua's personal rag-system hook via distinct `source_type` (`'session_summary'` vs `'fact'`) — no dedup collision. Closes Brad's empty-pgvector ingestion gap without requiring `rag-system` on his box. `tests/stack-installer-hook-merge.test.js` extended with **22 new test cases** (env-var read × 3, transcript-summary extraction × 5, OpenAI call signature × 3, Supabase POST shape × 3, end-to-end stdin payload × 4) — mocks fetch at module boundary, no live network calls, 49/49 pass on the extended file. `packages/stack-installer/assets/hooks/README.md` rewritten end-to-end with required-environment table, "Customizing the project map" walkthrough, and coexistence-with-rag-system explanation. Pre-sprint task per `docs/sprint-38-knowledge-graph/PLANNING.md § P0`.

### Notes

- **Sprint 38 wall-clock: 23 minutes from kickoff (18:47 ET) to last lane DONE (T2 at 19:10 ET) — fastest sprint in project history.** All four lanes coordinated entirely through STATUS.md cross-reads. T1 caught + folded T3's `migration-loader-precedence.test.js` count fix (same array literal). T2 designed for coexistence with the rag-system classifier (different `inferred_by` namespaces: `mcp:memory_link` / `cron-YYYY-MM-DD` / NULL for legacy ingest-time edges) — no role conflict. T4's `to_jsonb(memory_relationships)` + `try`/`catch` fallback works pre-T1-migration-landing, so the visualization renders day-one whether or not the columns have shipped. Three independent MCP tools (T1's three) + one new MCP tool (T3's `memory_recall_graph`) registered alongside the existing 9 — no namespace collision.
- **The "table is dormant" PLANNING.md framing was wrong** — `memory_relationships` was already populated with **749 edges** at sprint kickoff, the output of Joshua's private `~/Documents/Graciella/rag-system/src/lib/relationships.ts` MCP-side classifier called from `detectAndStoreRelationships()` after every `memory_remember` insert. T2's first FINDING traced the source. The cron is therefore **additive and complementary**, not replacing — it catches missed pairs from rag-system's 0.75-floor cutoff and surfaces cross-project edges (rag-system filters by `project`; T2 cron scans across).
- **Migration ordering at deploy:** T1's 009 → T3's 010 → T2's 003 in the bundled installer. The migration runner globs alphabetically and bundled-FIRST takes precedence (v0.6.8+ behavior); fresh installs handle this automatically. Live `petvetbid` deployment: orchestrator at sprint close runs psql against dev schema first, verifies the 749 existing rows pass the new 8-value CHECK, then applies to prod in dependency order.
- **Substrate health snapshot at sprint close**: Mnestra ingestion +21 memories during the 23-minute sprint window (5,455 → 5,476) — confirms lane workers were actively calling `memory_remember` in addition to file work. Rumen `rumen-tick` cron green throughout; the next tick after sprint close synthesizes the new sprint memories into `rumen_insights`. **memory_relationships** count unchanged at 749 during the sprint (no live-DB writes from lane workers per sprint contract); will start growing the moment T2's `graph-inference` cron fires its first tick post-deploy.
- **One known-failing test carry-over**: `tests/flashback-e2e.test.js: project-bound flashback…` — the documented Flashback P0 regression Joshua flagged in commit 72cf9fe (2026-04-27). E2E test fails; production Flashback toasts have been silent since approximately Sprint 26 (~9 days). Sprint 39 candidate: **Flashback Resurrection 2.0 (diagnostic-first)**. Do NOT bundle into v0.10.0 close-out — it deserves its own sprint with daily-flow instrumentation, zsh/bash rcfile-noise filter audit, project-tag write-path verification, and a production-flow e2e test that runs against actual shell streams rather than synthetic harnesses.

## [0.9.0] - 2026-04-27

### Added — Phase C part 1: Orchestrator-as-product

> **Theme:** Sprint 37 (4+1, ~30 min effective wall-clock with one mid-sprint nudge to T3) takes the orchestration patterns Joshua uses every day — the 4+1 sprint, the inject mandate, the CLAUDE.md hierarchy, the per-project scaffolding rituals — and ships them as a first-class shipped product feature. After this lands, a developer picking up TermDeck for the first time can read `docs/orchestrator-guide.md`, run `termdeck init --project <name>` to scaffold a full agent-ready project, and kick off a 4+1 sprint from the dashboard without touching the CLI. The two-stage submit pattern (paste, settle, `\r`) is now encoded server-side in `sprint-inject.js` so users cannot accidentally bypass it.

- **NEW `docs/orchestrator-guide.md` — canonical 9-section orchestrator reference.** Sections: 4+1 pattern, inject mandate (with two-stage submit + cURL examples + `/poke` recovery), CLAUDE.md hierarchy (global / project / session-prompt), memory-first discipline, enforcement-vs-convention rule, sprint discipline (FINDING / FIX-PROPOSED / DONE), restart-prompt rituals, per-project scaffolding (cross-references the `init --project` template manifest), channel inject patterns (WhatsApp / iMessage / self). Each section ends with a `> See also:` line pointing to the canonical source. Shipped in the npm tarball — `package.json` `files` allowlist now includes `docs/orchestrator-guide.md`. Sprint 37 T1.
- **NEW dashboard right-rail Guide panel.** Collapsible aside with toggle tab (📖 Guide), search input, TOC nav, content pane. Lazy-loads `/docs/orchestrator-guide.md` on first expand via a new `app.use('/docs', express.static(docsDir))` static mount in `packages/server/src/index.js` (+9 LOC, fs-existsSync-guarded so test environments without a `docs/` dir don't crash). Purpose-built ~110-line markdown converter with no external deps (preserves zero-build-step ethos) — handles ATX headings, paragraphs, bullets, ordered lists, fenced code, blockquotes, tables, hr, bold, italic, inline code, links, with `escapeHtml` defense-in-depth before inline regex transforms. Scroll-spy marks active TOC entry. Search filters sections + TOC entries together. Contextual auto-expand: clicking a terminal panel scrolls the Guide to "the-4-1-pattern" section. `g` keyboard shortcut toggles when no input is focused (skipped inside `.term-panel`). z-index: 50 (modals at ≥1000 sit cleanly above). +21 LOC `index.html`, +234 LOC `style.css`, +245 LOC `app.js`. Sprint 37 T1.
- **NEW `termdeck init --project <name>` scaffolding generator.** Creates a complete project skeleton in one command — `CLAUDE.md`, `CONTRADICTIONS.md`, `project_facts.md`, `README.md`, `docs/orchestration/README.md`, `docs/orchestration/RESTART-PROMPT.md.tmpl`, `.claude/settings.json`, `.gitignore`. NEW `packages/cli/templates/` directory with the 8 `.tmpl` files (~10 kB total); flat layout with hyphenated prefix encoding subdirectory placement (e.g., `docs-orchestration-README.md.tmpl` → `docs/orchestration/README.md`). NEW `packages/cli/src/templates.js` (+73 LOC) — shared module exporting `MANIFEST` (canonical 8-entry array), `listTemplates()`, `readTemplate(identifier)`, `renderTemplate(identifier, vars)`, `TEMPLATES_DIR`; identifier resolution accepts either `name` or template filename. Placeholders supported: `{{project_name}}`, `{{project_path}}` (absolute), `{{generated_at}}` (ISO 8601), `{{termdeck_version}}` (read from root `package.json` at run time); unknown `{{keys}}` left untouched so typos are visible. NEW `packages/cli/src/init-project.js` (+173 LOC) — `initProject({ name, dryRun, force, cwd })`. Validates project name (lowercase letters/digits/hyphens, no slashes / `..` / leading or trailing hyphen), refuses non-empty target without `--force` (accepts existing-but-empty), iterates `MANIFEST` to render+write 8 files, prints "Next steps" block. `--dry-run` flag prints the destination tree + first 5 lines of each rendered file without writing. Subcommand registration in `packages/cli/src/index.js` (+~28 LOC) with mode-conflict guard against `--mnestra + --project`. `package.json` `files` allowlist gains `packages/cli/templates/**`; `npm pack --dry-run` confirms all 8 `.tmpl` files appear in the tarball. NEW `tests/init-project.test.js` (+158 LOC, 11/11 pass): happy path file tree, `{{project_name}}` and `{{project_path}}` substitution, `.claude/settings.json` parses as JSON with permissions allow/deny arrays, dry-run writes nothing + lists every destination, refuses on non-empty target without `--force`, `--force` overwrites preserving siblings, refuses on subdirs-only non-empty target, accepts existing-but-empty target, `_validateName` rejects bad inputs, bad name → exit code 1. Sprint 37 T2.
- **NEW dashboard orchestration-preview surface.** "If you ran `termdeck init --project <name>` here, here's exactly what would be created" — read-only preview before commit. NEW `packages/server/src/orchestration-preview.js` (+256 LOC) — pure helper accepting `templates`, `destFor`, `initProject` by injection so logic is unit-testable without T2's CLI modules; exports `buildPreview()`, `generateScaffolding()` (async), `resolveTargetPath()`, `validateName()`, `normalizeTemplateItems()`. Two routes added to `packages/server/src/index.js` (+123 LOC) — `GET /api/projects/:name/orchestration-preview` returns `{ projectName, targetPath, exists, wouldCreate[], wouldSkip[] }`, `POST /api/projects/:name/orchestration-preview/generate` body `{ force?: boolean }` calls T2's `initProject({ dryRun: false })`. Lazy resolvers `_getT2Templates() / _getT2InitProject() / _getT2DestFor()` so a missing module returns 503 with a clear error rather than crashing. Target-path resolution: existing `config.projects[name]` → preview against the project's resolved path; otherwise `path.resolve(process.cwd(), name)` for fresh-scaffold preview. Preview button next to the existing `+` add-project button (disabled until a project is selected) opens `#previewProjectModal` mirroring the add-project-modal pattern; `.preview-project-modal` / `.ppm-*` styles include header, meta strip with exists/fresh tag, scrollable file tree with create/skip sections, expand-on-click row bodies with `<pre>` content snippets, force checkbox, generate/cancel buttons. Tokyo-Night palette consistent with existing modals. +25 LOC `index.html`, +245 LOC `style.css`, +200 LOC `app.js`. NEW `tests/orchestration-preview.test.js` (+601 LOC, 26/26 pass) — covers shape contract, name validation, fresh/existing-dir branches, T2 string-array AND object-array shapes, async exitCode propagation, force/no-force, plus an integration test against the live T2 `templates.js` + `init-project.js._destFor` so future shape drift fails this test rather than silently breaking the dashboard. Sprint 37 T3.
- **NEW in-dashboard 4+1 sprint runner with `--isolation=worktree` opt-in.** Define a sprint, name the lanes, click "kick off." TermDeck spawns four panels through the same `spawnTerminalSession({...})` code path as `POST /api/sessions` (transcripts, RAG telemetry, Mnestra flashback, command logging, status-change → RAG — no drift), runs the two-stage submit injection server-side, tails `STATUS.md` in a sidebar panel showing per-lane FINDING/FIX-PROPOSED/DONE counts. NEW `packages/server/src/sprint-inject.js` (+160 LOC) — pure two-stage submit logic: `injectSprintPrompts({ sessionIds, prompts, writeBytes, getStatus, sleep, options })` returns `{ ok, lanes:[{paste, submit, verified, poked, finalStatus}] }`, auto-pokes (`\r\r\r`) any lane that doesn't reach `status:'thinking'` within `verifyTimeoutMs` (default 8000ms). Tests pass mock `writeBytes` / `getStatus` / `sleep` so no live PTY is needed. Writes via `session.pty.write` directly, bypassing the CRLF-normalize + rate-limit in `/api/sessions/:id/input` — necessary because the bracketed-paste payload must preserve embedded `\n` as `\n`, not `\r`; routing through the public endpoint is a single-line override point if a future security review requires it. NEW `packages/server/src/sprint-routes.js` (+370 LOC) — `POST /api/sprints` (validate → scaffold sprint dir + PLANNING.md + T1-T4 lane briefs + STATUS.md + lane prompt templates → optional `git worktree add <sprint_dir>/worktrees/T<n> -b sprint-<name>-T<n>` per lane → spawn 4 panels via injected `spawnTerminalSession` callback → run `injectSprintPrompts`), `GET /api/sprints` (list), `GET /api/sprints/:name/status` (parse STATUS.md headers with cheap regex, return per-lane FINDING / FIX-PROPOSED / DONE counts + lastEntryAt + lastModifiedAt), `GET /api/sprints/:name/tail` (raw tail, `?lines=N` capped at 2000). `packages/server/src/index.js` (+24 / -3 LOC) extracts the existing inline PTY-spawn-and-wire body of `POST /api/sessions` into a `spawnTerminalSession({ command, cwd, project, label, type, theme, reason })` closure inside `createServer`; both the existing handler (now a 4-line shim) and `createSprintRoutes({ app, config, spawnTerminalSession, getSession })` call it. Worktree opt-in defaults ON in the UI per the Sprint 36 incident decision (memory: 2026-04-27 12:55 ET — worktree-based 4+1 sprint orchestration is the going-forward protocol; merge-at-close stays manual for v0.9.0, automation is Sprint 38+). UI: `#btn-sprint` topbar button + sprint modal (project picker, slug-validated name, target version, goal, T1–T4 name+goal pairs, worktree checkbox, auto-inject checkbox) + result panel with per-lane verified/poked counts from the inject result + 80-line STATUS.md tail polling every 3s. +97 LOC `index.html`, +205 LOC `app.js`, +190 LOC `style.css`. NEW `tests/sprint-inject.test.js` (+~190 LOC, 7/7 pass): bracketed-paste shape, no-CR-in-stage-1, settle/gap timing, paste-failure short-circuit, verify-and-poke, getStatus-omitted, validation. NEW `tests/sprint-routes.test.js` (+~360 LOC, 10/10 pass): parse, slugify, list/next-number, full HTTP scaffold + 8 expected PTY writes shape (4 paste + 4 lone CR, in order), validation rejections, status, tail, list. The `POST /api/sessions` refactor is regression-clean: 41/41 cross-check tests pass (`tests/health-contract.test.js tests/auth-cookie.test.js tests/cli-default-routing.test.js tests/cli-stack-detection.test.js tests/preconditions.test.js`). Sprint 37 T4.

### Notes

- **Sprint 37 wall-clock**: ~30 minutes from kickoff (16:50 ET) to last lane DONE (T3 at 17:16 ET). Mid-sprint nudge fired at T3 (17:11 ET) when its `lastActivity` went stale for 7+ minutes between turns; the nudge unblocked the lane and T3 posted DONE within five minutes. Two-stage submit pattern delivered all four boot prompts on first attempt — zero stuck panels, zero recovery `/poke` calls.
- **All four lanes coordinated entirely through STATUS.md cross-reads** — no orchestrator-mediated handoffs needed. T2 locked the templates module surface in its FIRST FINDING entry; T3 adopted T2's contract verbatim in its FIX-PROPOSED. T1 cited T2's 8-name template manifest verbatim in Guide § 8. T1's right-rail z-index (50) and T3's preview modal (3000) and T4's sprint-runner modal (also high) chose non-conflicting layers without coordination. Two `package.json` `files` allowlist additions (T1's `docs/orchestrator-guide.md` and T2's `packages/cli/templates/**`) stacked additively.
- **Server restart required** to surface the new `/docs` static route, the orchestration-preview routes, and the `/api/sprints*` routes in production browsers. Folded into the next 4+1 cohort spawn for Sprint 38 — one restart covers all four lanes' new endpoints.
- **`@jhizzard/termdeck-stack@0.4.1`** ships in lockstep as an audit-trail patch bump (Sprint 35 lesson: every termdeck release gets a paired stack-installer patch bump even when the installer source is untouched, so package-version archaeology can pin the user-visible TermDeck version that shipped alongside any installer release).
- **Substrate health snapshot at sprint close**: Mnestra ingestion +129 memories in 6 hours (5,323 → 5,452 → 5,455 by close); Rumen `rumen-tick` cron green with last 5 runs all succeeded on `*/15 * * * *` schedule; rumen_insights at 275 rows; **`memory_relationships` already populated with 749 edges** across all five schema-allowed kinds (supersedes 469, elaborates 167, relates_to 91, contradicts 14, caused_by 8) — Sprint 38's "table is dormant" framing is wrong and the lane briefs are corrected for it.

## [0.8.0] - 2026-04-27

### Added — Phase B reconciliation: launcher + UI parity, hook bundling, MCP path migration

> **Theme:** Sprint 36 (4+1, ~30 min effective wall-clock once two orchestrator hotfixes stopped the kill class) closes the personal-vs-product asymmetry that Sprint 35 (Phase A) made non-fatal. After this lands, `npx @jhizzard/termdeck` matches `scripts/start.sh` step-for-step on a fresh box, the dashboard exposes RAG-toggle in-product, the stack-installer ships Joshua's `~/.claude/hooks/memory-session-end.js` so fresh users get the same Mnestra-ingestion wiring, and Claude Code v2.1.119+ MCP entries land in the canonical `~/.claude.json` instead of the legacy `~/.claude/mcp.json` that Claude Code no longer reads.

- **`npx @jhizzard/termdeck` now runs the full `scripts/start.sh` choreography by default.** `packages/cli/src/auto-orchestrate.js`: `shouldAutoOrchestrate()` returns `true` unconditionally (was: gated on `~/.termdeck/secrets.env` + `~/.termdeck/config.yaml` presence + autoStart/rag.enabled — the Sprint 24 default that excluded fresh boxes from Step 1/4–4/4 entirely). Function preserved as a future telemetry seam. `--no-stack` still opts out for users who want the Tier-1-only path. `packages/cli/src/stack.js`: exports `CLAUDE_MCP_PATH_CANONICAL` (`~/.claude.json`) and `CLAUDE_MCP_PATH_LEGACY` (`~/.claude/mcp.json`); MCP-absence hint now checks both paths via new `hasMnestraMcpEntry()` helper and prints `TermDeck doesn't see Mnestra wired in Claude Code yet. Run: npx @jhizzard/termdeck-stack` only when neither file references Mnestra. `tests/cli-stack-detection.test.js` (7/7 pass) and `tests/cli-default-routing.test.js` (6/6 pass) updated for the new always-on policy. Sprint 36 T1.
- **MCP wiring migrated from legacy `~/.claude/mcp.json` to canonical `~/.claude.json`.** Claude Code v2.1.119+ reads top-level `mcpServers` exclusively from `~/.claude.json`; the legacy path is no longer consulted on most installs, which silently broke every fresh `npx @jhizzard/termdeck-stack` since the v2.1 release. NEW `packages/cli/src/mcp-config.js` (+174 LOC) and sibling `packages/stack-installer/src/mcp-config.js` (+138 LOC) — same exports (`CLAUDE_MCP_PATH_CANONICAL`, `CLAUDE_MCP_PATH_LEGACY`, `readMcpServers`, `mergeMcpServers`, `writeMcpServers`, `migrateLegacyIfPresent`), structure-preserving read-modify-write that only touches the `.mcpServers` key, atomic via `${path}.tmp.${pid}` then `rename`, mode 0600, current-wins on key collision. `packages/stack-installer/src/index.js` and `packages/cli/src/init-rumen.js` updated to write through the new helpers; `~/.claude/mcp.json` is left untouched (user may have other tooling pinned to it). NEW `tests/mcp-config-merge.test.js` (+331 LOC) covers legacy-only / current-only / overlap / disjoint / malformed / empty-file / preserves-other-top-level-keys (`oauthAccount`, `projects` etc.) / idempotent-second-run / mode-0600 / atomic-rename / migrate-noop-when-no-legacy. `tests/init-rumen-mcp-json.test.js` updated with one new test pinning that backfilling `SUPABASE_ACCESS_TOKEN` against a `~/.claude.json`-shaped fixture leaves all sibling top-level keys byte-identical. Sprint 36 T2.
- **Dashboard `Settings → RAG mode` toggle.** New `PATCH /api/config` endpoint (+45 LOC `packages/server/src/index.js`) accepts `{ rag: { enabled: bool } }`, validates against a strict whitelist (only `rag.enabled` writable today), persists to `~/.termdeck/config.yaml` via new `updateConfig` (+101 LOC `packages/server/src/config.js`) which round-trips YAML and writes a `.bak` before overwriting, then live-updates `RAGIntegration.enabled` via new `setEnabled()` (+22 LOC `packages/server/src/rag.js`) which re-evaluates eligibility and starts/stops the sync timer without restart, and broadcasts `config_changed` over WebSocket so multi-tab dashboards sync within ~100ms. `packages/client/public/app.js` (+103 LOC): new `renderSettingsPanel()` slot inside the existing setup modal renders an MCP-only-mode-explainer + toggle row above the tier list; new `updateRagIndicator()` paints a `RAG · on/off` chip in the topbar; `init()` wires the indicator after first config load; WS message handler adds `case 'config_changed'`. `packages/client/public/style.css` (+114 LOC): `.setup-settings`, `.settings-row`, `.toggle`, `.topbar-stat.rag-on/off` all using existing design tokens. NEW `tests/config-update.test.js` (13/13 pass, 162ms) pins the contract: rejects non-object / empty / non-whitelisted patches, rejects non-boolean values, writes/round-trips both true and false, creates the `rag` block when missing, creates a new file when none exists, preserves the projects map verbatim, refuses to overwrite malformed YAML, `.bak` write-before-overwrite, `flattenPatch` flattens nested objects, `UPDATABLE_PATHS` contract verified. Sprint 36 T3 Deliverable A.
- **Stack-installer ships the personal `~/.claude/hooks/memory-session-end.js` hook.** Joshua's box has carried this Stop-hook since 2026-03-11; fresh users running `npx @jhizzard/termdeck-stack` previously got Mnestra wired but no ingestion path because the hook was never bundled. NEW `packages/stack-installer/assets/hooks/memory-session-end.js` (vendored copy with `RAG_DIR` parameterized to `process.env.HOME` and a graceful skip-and-log when `<RAG_DIR>/src/scripts/process-session.ts` is absent — Joshua's private `rag-system` repo is not on a fresh user's box, so the hook is a no-op until that repo is published OR the hook is rewritten to call Mnestra MCP tools directly; future-sprint scope flagged). NEW `packages/stack-installer/assets/hooks/README.md` documents what the hook does, when it fires, what it writes to Mnestra, and how to disable. `packages/stack-installer/src/index.js`: new opt-in prompt `Install TermDeck's session-end memory hook? (Y/n)` (defaults Y), copies the asset to `~/.claude/hooks/memory-session-end.js` with overwrite-prompt protection on existing-and-different files, and merges a `Stop` matcher-group into `~/.claude/settings.json` preserving the 6 sibling top-level keys (`permissions`, `hooks`, `enabledPlugins`, `extraKnownMarketplaces`, `skipAutoPermissionPrompt`, `agentPushNotifEnabled`); idempotent — second install run produces no diff in settings.json. `packages/stack-installer/package.json` `files` array gains `"assets/**"` (the release-blocker T4 caught mid-lane: without this, the vendored hook would not have shipped in the npm tarball — silent install break). NEW `tests/stack-installer-hook-merge.test.js` (+443 LOC) covers idempotent install, existing-Stop-without-our-entry append, existing-Stop-WITH-our-entry no-op, malformed settings.json graceful error, all 6 top-level keys preserved verbatim, mode preservation, .bak written. Sprint 36 T4.

### Fixed

- **Server-kill class via `reclaimStalePort` cascade-kill — root cause of four Sprint 36 PTY-cohort losses.** `packages/cli/src/index.js`: `reclaimStalePort(port)` (a sibling of `stack.js`'s `reclaimPort` that I missed when patching the latter at 14:08 ET) used the same regex match `/packages\/cli\/src\/index\.js/ || /termdeck/i` and SIGTERM+SIGKILL'd anything matching, with no liveness probe. Any lane worker invoking `node --test` against a CLI test (e.g., T1's `tests/cli-default-routing.test.js`) spawned a CLI child whose entry path called `reclaimStalePort(3000)`, found the live orchestrator's server PID, and cascade-killed it + every child PTY. Reproduced four times in 30 minutes (13:35, 13:47, 14:55, 15:20 ET). Fix: liveness-probe self-recognition via `execSync curl -sf -m 1.5 -w "%{http_code}" http://127.0.0.1:${port}/api/sessions`; `HTTP 200` means the server is the orchestrator's live instance — log `[port] :${port} held by live TermDeck — not killing. Use --port <other> for a second instance.` and `process.exit(0)` instead of SIGKILLing. Mirror of `stack.js:isTermDeckLive`. Defense-in-depth: `cli/src/index.js` now also reads `TERMDECK_PORT` env into `config.port` (`else if (process.env.TERMDECK_PORT) config.port = parseInt(...)`), so tests setting `TERMDECK_PORT=0` actually take effect. Orchestrator hotfix #2 — applied 15:25 ET, validated by T1 cohort surviving its full test run against PID 2983 immediately after.
- **Server-kill class via `stack.js reclaimPort` cascade-kill — same flaw, sibling site.** `packages/cli/src/stack.js`: same liveness-probe self-recognition pattern added to `reclaimPort(port)` via new `isTermDeckLive(port)` async helper using `httpJson` (the in-module fetch wrapper). On HTTP 200 from `/api/sessions`, returns `{ reclaimed: false, blockerPids, alreadyLive: true }` and prints `TermDeck on port ${port} is live (PIDs: ...) — not killing`. Orchestrator hotfix — applied 14:08 ET.
- **`rag.js` status_changed flood — orchestrator-applied debounce.** `packages/server/src/rag.js`: rapid `active ↔ thinking` cycling from busy 4+1 lanes produced dozens of `[rag] write event=status_changed` lines per second per session, which floods stdout and the outbox without value (status edges within 1s are noise, not signal). Constructor adds `_statusWriteAt = new Map()` (sessionId → last write timestamp ms) and `_statusDebounceMs = 1000`. `onStatusChanged()` early-returns when `now - last < 1000` AND neither old nor new status is `errored` (error transitions always pass through — they carry signal). `stop()` clears the map. Designed by T3 Deliverable D, applied at the orchestrator layer (enforcement-vs-convention) after two server-kill incidents convinced both author and orchestrator that delegating to a lane that the bug kept killing was not going to converge. Validated by aggregate ~4 lines/sec across 4 active sessions = correct (1/sec/session × 4 sessions). Orchestrator hotfix — applied 14:08 ET.
- **`startTour()` half-initialized state could leave the 78%-black `.tour-spotlight` overlay stuck visible.** `packages/client/public/app.js`: T3's static audit of every viewport-spanning dark CSS rule located the literal "dark veil" symptom both Joshua and Brad reported as `.tour-spotlight` (`box-shadow: 0 0 0 9999px rgba(0, 0, 0, 0.78)` at style.css:191–200) — the only rule in the file producing a viewport-wide dark layer. Fault path: `startTour()` set the spotlight's `display = 'block'` BEFORE calling `await ensurePanelForTour()` and `renderTourStep()`, both of which can throw (POST to `/api/sessions`, DOM not ready). On any throw, the 9999px shadow stayed up with no tooltip — looks like a stuck modal overlay. Fix: wrap the post-display-block work in `try`/`catch`; on any throw, log the error and call `endTour()` (idempotent — nulls `tourState.active`, removes `.active` from backdrop, hides spotlight + tooltip with guards). +8 LOC. Zero behavior change on the happy path. Sprint 36 T3 Deliverable B.
- **Hard-refresh PTY loss verified-fixed by orchestrator hotfix #2.** T3's static + live verification confirmed the briefing's diagnosis (WS-close cascading to PTY kill) was contradicted by the current code: `ws.on('close')` only nulls `session.ws`, `session.destroy()` only clears timers + buffer, only `DELETE /api/sessions/:id` (gated behind `confirm()`) kills PTYs, and the client has no `beforeunload`/`pagehide`/`unload` handler. The original symptom Joshua reported on 2026-04-27 13:00 ET (`/api/sessions` returns count: 0 immediately after Cmd+Shift+R) was the same `reclaimStalePort` cascade-kill that took down the four Sprint 36 PTY cohorts — the live server died, taking its in-memory sessions Map with it. With the kill class closed, hard-refresh is non-destructive. T3 added a `+9 LOC` comment block to `ws.on('close')` documenting the intent so the next cohort doesn't re-misdiagnose. Sprint 36 T3 Deliverable C.
- **`migration-loader-precedence.test.js` stale assertions updated.** Test hardcoded `length === 7` from v0.6.5; Sprint 35 T2 added `008_legacy_rag_tables.sql` making the bundled count 8. Three assertions corrected (4/4 pass). Pre-existing test debt closed as part of Sprint 36 close-out.

### Notes

- **Sprint 36 wall-clock**: ~3 hours from 12:08 ET first kickoff to 15:46 ET final lane DONE — but ~2 of those hours were lost to the four server-kill incidents (cohort losses at 13:35, 13:47, 14:55, 15:20). The actual productive time once the kill class was closed (15:25 ET → 15:46 ET = 21 min) covered T3's three deliverables (A: PATCH /api/config + RAG toggle in 5 min, B: tour-spotlight try/catch in 3 min, C: verified-fixed verification in 2 min) plus T1, T2, T4 finishing their FIX-PROPOSED work that had survived the kills as on-disk edits. The lesson: enforcement-level fixes (orchestrator-applied) on convergent failure modes beat convention-level guidance ("don't run the CLI") that survives one cohort but not four.
- **Two-stage submit pattern locked into `~/.claude/CLAUDE.md`** (15:35 ET): 4+1 sprint inject MUST POST the bracketed-paste payload (`\x1b[200~ ... \x1b[201~`) and a `\r` submit as **separate** `/api/sessions/:id/input` calls with a 400ms gap. Single-stage `<text>\x1b[201~\r` injection is BANNED — OS-level chunk boundary into the PTY is non-deterministic, leaving a stuck-paste-no-submit panel ~25% of the time. Three cohort injects in this sprint validated the two-stage pattern's deterministic delivery (zero stuck panels). The cardinal sin clause: leaving a panel waiting for a human Enter press cost Joshua broken sleep on 2026-04-26 during ClaimGuard Sprints 4–5 and again on 2026-04-27 during Sprint 36's first inject. Must never recur.
- **Brad's empty-pgvector ingestion gap** is now substantively addressed by T2 (MCP path migration) + T4 (hook bundling). The full fix path requires v0.8.0 plus either publishing `rag-system` OR rewriting the hook to call Mnestra MCP tools directly (queued for a future sprint per T4's FINDING). Brad-side workaround drafted at `/tmp/brad-mnestra-hook.js` and sent via WhatsApp at 15:30 ET — fully self-contained, no `rag-system` dependency, ~120 LOC.
- **Pre-existing test failures not fixed in this release** (4 total; deferred — none are Sprint 36 regressions): (1) `stripAnsi removes standard CSI sequences (SGR colors)` — session.js util test predating Sprint 35; (2-3) `Error detection: common Node errno codes` and `Error detection: HTTP 5xx from server log` — `PATTERNS.error` regex too narrow per Sprint 33 finding; (4) `project-bound flashback: termdeck session surfaces termdeck/null memories` — depends on Mnestra Supabase fixture state. Confirmed pre-existing via stash + clean-main rerun. Flagged for an analyzer-pattern fix sprint.
- **Sprint 37 (Orchestrator-as-product) and Sprint 38 (Knowledge graph + D3.js)** plans preserved at `docs/sprint-37-orchestrator-product/PLANNING.md` and `docs/sprint-38-knowledge-graph/PLANNING.md`. Sprint 37 T4 (worktree-isolated 4+1 sprint runner) graduates from "going-forward protocol" to first-class shipped product feature — every lane in its own git worktree, no chance to cascade-kill the orchestrator's server. Today's enforcement-level liveness-probe fix is a stopgap; Sprint 37 closes the structural gap.

## [0.7.3] - 2026-04-27

### Changed — Reconciliation: align fresh-install behavior with the personal-box reality

> **Theme:** Brad's overnight crash (`/opt/structural-mvp` box, 2026-04-27) exposed an asymmetry that's been latent for months: Joshua runs `rag.enabled: false` on his daily-driver box (MCP-only mode — Mnestra fills via Claude Code's `memory_remember` calls and the personal `~/.claude/hooks/memory-session-end.js`), while `termdeck init --mnestra` flips fresh users to `rag.enabled: true` and immediately tries to write to legacy schema (`mnestra_session_memory`, `mnestra_project_memory`, `mnestra_developer_memory`, `mnestra_commands`) that no init path creates. Result: 404 cascade on every fresh-install RAG event, circuit breaker opens, all RAG syncs silently drop. **Sprint 35 (4+1, ~17 minutes wall-clock T1 → T2)** flips the install default to MCP-only AND ships the legacy schema as a real shipped migration so opt-in users no longer hit the silent 404 cascade. Plus packaging fix for the transcript table.

- **`init --mnestra` defaults to `rag.enabled: false` (MCP-only mode).** `packages/cli/src/init-mnestra.js`: the `writeYamlConfig()` step now writes `rag.enabled: false` and prints a 5-line "Setup mode: MCP-only (default)" block before the config-update step explaining how MCP-only fills `memory_items` via Claude Code memory tools, that TermDeck-side event tables stay off, and the dashboard toggle URL `http://localhost:3000/#config` plus the config.yaml fallback. `printNextSteps()` appends a 3-line restatement so users who tab away during step output still get the disclosure. File-header docstring (step 5), HELP block (step 4), and `printBanner` step 7 all rewritten to match. Sprint 35 T1 (5 edits, all in one file).
- **Legacy RAG schema now ships as `008_legacy_rag_tables.sql`.** `packages/server/src/setup/mnestra-migrations/008_legacy_rag_tables.sql` (NEW) is a byte-identical mirror of `config/supabase-migration.sql` with a four-line provenance header. `migration-runner.js::listAllMigrations()` discovers it via the existing alphabetical glob over `mnestra-migrations/*.sql` — no wiring change needed; dropping the file IS the wiring. The runner's header comment was generalized from "7-migration bootstrap sequence" to a description that defers to glob discovery. `config/supabase-migration.sql` itself was rewritten for idempotency: `CREATE EXTENSION IF NOT EXISTS pg_trgm` moved to the top so the dependent FTS index resolves on a vanilla Postgres first run; all eleven `CREATE INDEX` got `IF NOT EXISTS`; all eight `CREATE POLICY` statements gained a `DROP POLICY IF EXISTS … ON <table>;` guard immediately above them (Postgres-version-agnostic; works pre-15). Sprint 35 T2 (4 files within lane).
- **`pg_cron` / `pg_net` precondition hints now project-specific.** `packages/server/src/setup/preconditions.js`: new `extensionsDashboardUrl(supabaseUrl)` helper derives `https://supabase.com/dashboard/project/<ref>/database/extensions` from `secrets.SUPABASE_URL` (via existing `setup/supabase-url.js`) and injects it into the pg_cron and pg_net hints when derivable, falling back to the generic copy otherwise. `init --rumen` already gates on these via the v0.6.9 `auditRumenPreconditions` framework — no `init-rumen.js` change needed (T3 FINDING confirmed the gate was in place since v0.6.9; Sprint 35 T3's lane brief had described an idealized API that didn't match reality). Sprint 35 T3 (2 files).
- **`termdeck doctor` extended with Supabase schema-check section.** `packages/cli/src/doctor.js` (+312 LOC) extends Sprint 28's version-check doctor with a `_runSchemaCheck(secrets)` test seam that verifies presence of: Mnestra modern schema (`memory_items`, `memory_sessions`, `memory_relationships`, RPCs); Mnestra legacy schema (`mnestra_session_memory`, etc. — checks for the new 008 migration); transcript table (`termdeck_transcripts`); Rumen tables + `created_at` column; extensions (`pg_cron`, `pg_net`, `pgvector`, `pg_trgm`, `pgcrypto`). New `--no-schema` opt-out flag. JSON output gains a `schema` key alongside the existing `rows` key. Combined exit-code precedence: 2 (network failure) > 1 (update available OR schema gap) > 0 (clean). New tests in `tests/cli-doctor.test.js` (+130 LOC) using the fake-pg-client substring-routing pattern from `tests/preconditions.test.js`; existing tests pass `--no-schema` to stay deterministic. Sprint 35 T3 (3 files + 2 test files).
- **Boot banner shows RAG state.** `packages/cli/src/index.js`: after the `╚═══╝` box, a single dim line prints either `RAG: on — events syncing to mnestra_session_memory / mnestra_project_memory / mnestra_developer_memory` or `RAG: off (MCP-only mode) — toggle in dashboard at <url>/#config…`. `packages/server/src/index.js:1736`: matching label for direct-invocation banner — `config.rag?.supabaseUrl ? 'configured' : 'not configured'` (which had nothing to do with `enabled`) replaced with `config.rag?.enabled === true ? 'on (...)' : 'off (MCP-only mode)'` so both banners stay coherent. Sprint 35 T4.
- **Stale-port reclaim and transcript-table-missing hint ported from `scripts/start.sh`.** `packages/cli/src/index.js`: new `reclaimStalePort(port)` helper (lifted from `scripts/start.sh:127–154`) runs before `server.listen()` — `lsof -ti TCP:<port>` (with `fuser` Linux fallback), `ps -o command= -p <pid>` to identify TermDeck holders, SIGTERM → 1s grace → SIGKILL on TermDeck PIDs, hard-stop with a useful message on non-TermDeck holders. New `checkTranscriptTableHint(databaseUrl)` fire-and-forget helper (lifted from `scripts/start.sh:309–313`) probes `SELECT 1 FROM termdeck_transcripts LIMIT 0` and prints `[hint] Transcript backup table missing. Run: termdeck doctor (or psql $DATABASE_URL -f config/transcript-migration.sql)` on failure. Manual test matrix on port 3099: rag-on/off banners ✓, stale-TermDeck reclaim ✓, non-TermDeck holder hard-stops with `--port <n+1>` hint ✓, transcript hint exercised via syntax check (Joshua's Supabase already has the table). Sprint 35 T4.

### Fixed — packaging gap caught during sprint close-out

- **`config/transcript-migration.sql` now actually ships in the npm tarball.** Pre-Sprint-35 `package.json` `files` array did not include this path; `migration-runner.js:33` does `fs.existsSync(TRANSCRIPT_MIGRATION)` before applying, so on every fresh `npm install -g @jhizzard/termdeck` install since v0.6.8, the runner silently skipped the transcript migration. Brad's 2026-04-27 crash log (`relation "termdeck_transcripts" does not exist`) is the user-visible symptom. Now the file is in `files` and the migration runs as documented. Orchestrator-side correction during Sprint 35 close-out (one-line edit, audited via `npm pack --dry-run`).

### Notes
- **Sprint 35 wall-clock**: 4+1 sprint kicked off 2026-04-27 12:08 ET. T4 DONE at 12:20, T1 DONE at 12:25, T2 DONE at 12:26. T3 work product complete (verified by orchestrator at close — terminal closed before posting [T3 DONE], but `node --check` passes on all touched files, all tests pass for T3's additions, and lane scope is honored). Orchestrator close at 13:05 ET.
- **T2's pre-publish idempotency test against a scratch Supabase project deferred** — no scratch DB available at close. T2's offline statement-class analysis covers all forms (`CREATE EXTENSION IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, `ALTER TABLE … ENABLE ROW LEVEL SECURITY` (no-op when already enabled), `DROP POLICY IF EXISTS … ; CREATE POLICY …`, `CREATE OR REPLACE VIEW`). Every statement re-runs cleanly.
- **Pre-existing test failures in `packages/server/tests/session.test.js`** (Error detection assertions returning `'active'` instead of `'errored'`) confirmed via stash-and-rerun against `HEAD` to predate this sprint; last commit on that file was v0.2.5 (95c577d). NOT introduced by Sprint 35; not blocking publish; flagged for a separate analyzer-pattern fix sprint.
- **Sprints 36–38 planned** in `docs/sprint-36-launcher-ui-parity/`, `docs/sprint-37-orchestrator-product/`, `docs/sprint-38-knowledge-graph/`. Sprint 36 (Phase B) covers full `scripts/start.sh` parity in the published CLI, MCP path drift (`~/.claude.json` vs `~/.claude/mcp.json`), dashboard RAG-toggle UI, and bundling the personal session-end hook into `@jhizzard/termdeck-stack`. Sprint 37 ships the orchestrator-as-product layer (Orchestrator Guide, per-project scaffolding generators, in-dashboard 4+1 sprint runner with worktree-isolation option). Sprint 38 brings the dormant `memory_relationships` table to life as a Supabase-native knowledge graph with D3.js v7 force-directed visualization.
- **Two install-UX bugs surfaced during Sprint 35 are queued for Sprint 36 / Phase B**: (1) dashboard "dark veil" — stuck modal/overlay blocks pointer events to xterm.js terminals; both Joshua and Brad have hit it independently. Workaround: hard-refresh OR `document.querySelectorAll('.modal-backdrop, [class*="modal"]').forEach(el => el.remove())` in dev console. (2) Hard-refresh kills server-side PTYs — `/api/sessions` returns 0 immediately after a Cmd+Shift+R; WebSocket close event being misinterpreted as session shutdown. Both are HIGH priority for Phase B.

## [0.7.2] - 2026-04-26

### Fixed
- **chopin-nashville mis-tag of TermDeck memories — corpus repair + TermDeck-side regression lock.** Sprint 34 closes the second of two converging bugs Sprint 33 diagnosed under the *"Flashbacks are vaporware"* report. v0.7.1 fixed the analyzer regex so Flashback fires on common Unix shell errors; v0.7.2 makes the resulting toast content actually relevant for TermDeck panels by repairing the mis-tagged corpus and pinning the resolver against future drift. Sprint 34's 4+1 audit converged in ~15 minutes on a finding that resets the framing: **the chopin-nashville source is not in TermDeck.**
- **What T1 actually found.** TermDeck's `resolveProjectName(cwd, config)` (rag.js:19-41) is correct — longest-prefix-wins with explicit `path.sep` boundary so `/a/b` cannot spuriously match `/a/bb`. Live ground-truth probe against `~/.termdeck/config.yaml` (15 projects) returns `termdeck` for the TermDeck repo cwd and every subdirectory. All five RAG hooks (`onSessionCreated`, `onCommandExecuted`, `onStatusChanged`, `onSessionEnded`, `onFileEdited`) route through `_projectFor(session)`, which prefers explicit `session.meta.project` and only falls back to `resolveProjectName` if absent. **`mnestra-bridge.queryMnestra` falls back the same way.** No path-segment side-channel exists. Sprint 21 T2's writer fix landed correctly and is intact. The `1,165` mis-tagged `memory_items` rows (was 1,126 in Sprint 33's snapshot — Rumen added ~39 since) came from a writer **outside the TermDeck repo entirely:** `~/.claude/hooks/memory-session-end.js:17` carries a literal `{ pattern: /ChopinNashville|ChopinInBohemia/i, project: 'chopin-nashville' }` with no `termdeck` entry above it; first-match-wins on cwd, so every Claude Code session-end inside any TermDeck panel pattern-matches `ChopinNashville` before reaching anything more specific. The hook then spawns `~/Documents/Graciella/rag-system/src/scripts/process-session.ts --project chopin-nashville <transcript>`, ingesting the full transcript into `memory_items` with that tag. Compounded by Rumen's `extract.ts:62` (`(ARRAY_AGG(m.project))[1] AS project`) — every 15 minutes Rumen synthesizes new insights inheriting the first project tag from each session group, so the harness hook's bad tag propagates into `rumen_insights` and back into `memory_items` as synthesized rows. **Same shape as T3's separately-flagged `gorgias`-vs-`claimguard` finding** (line ~25 of the same hook: `{ pattern: /gorgias/i, project: 'gorgias' }` with no `claimguard` mapping), out of v0.7.2 scope.
- **What v0.7.2 ships in the TermDeck repo (T1, +30 LOC source + 12 tests):** `packages/server/src/rag.js` +22 LOC introducing `_resolveProjectAttribution(session)` (returns `{ tag, source: 'explicit'|'cwd'|'fallback' }`) and `_recordForSession()` (refactored common path); the five RAG hooks now route through it and emit a `[rag] write project=<tag> source=<...> session=<id> event=<type>` audit line on every legacy-table write. `packages/server/src/mnestra-bridge/index.js` +8 LOC: every `queryMnestra` invocation logs `[mnestra-bridge] query project=<tag> source=<explicit|cwd|none> mode=<direct|webhook|mcp>` so future drift in either the resolver chain OR a caller passing the wrong project becomes visible at runtime, not via SQL spelunking weeks later. NEW `tests/project-tag-resolution.test.js` (12 tests, all green) pins the contract: leaf wins over ancestor, explicit `meta.project` beats cwd resolution, missing config falls back to basename, empty config returns null, `.../ChopinNashville/SideHustles/TermDeck/termdeck` resolves to `termdeck` (the regression pin), trailing-slash cwd handled, sibling path with shared prefix does not cross-match (the `path.sep` boundary case), `_projectFor` honors explicit `meta.project` over cwd-derived value. Bridge constructor + `queryMnestra` signature unchanged externally; existing v0.7.1 tests all green (analyzer-error-fixtures, theme-persistence, init-mnestra-resume, etc.).
- **Corpus repair (T2, ~210 LOC SQL).** `scripts/migrate-chopin-nashville-tag.sql` with `docs/sprint-34-project-tag-fix/SQL-PLAN.md` documents the full plan. Three-block layout: **Block 1** (dry-run, runs always, zero mutations) emits the candidate count + per-target-project distribution + a 10-stay-+-5-move sample inspection; **Block 2** (UPDATE, commented out — requires manual uncomment after orchestrator+Josh approve Block 1's output) flips matching rows; **Block 3** (REVERT, commented out — one-shot rollback via `metadata->>'rebrand_v0_7_2_from'` stash) is the safety net. Multi-branch OR heuristic, priority-ordered: termdeck > mnestra > rumen > pvb > claimguard. **Conservative-by-design** — rows that match no branch stay `chopin-nashville`. Reversibility: every reclassified row carries its old project tag under `metadata.rebrand_v0_7_2_from` so Block 3 restores via a single statement. Path branch falls back on `metadata->>'cwd'` because `source_file_path IS NULL` for all 1,165 chopin-nashville rows (Rumen-synthesized rows do not carry a file path). Pre-flight aggregate counts (Block 1 dry-run output): chopin-nashville total = 1,165; broad-keyword `termdeck` family match = 352 (the v0.7.2 narrative number; Sprint 33's 1,126 figure was optimistic — broad-keyword union catches 352, the remaining ~700 are festival/Chopin-in-Bohemia content, pianist references, or oblique TermDeck mentions that would coin-flip if reclassified). Estimated Block 2 reclassification: ~340–352 → termdeck, ~5–20 → mnestra, ~10–25 → rumen, ~3–7 → pvb, ~2–4 → claimguard, ~750–800 stay. Out-of-scope (flagged in SQL-PLAN): gorgias→claimguard rebrand, PVB case-dupe, engram→mnestra rebrand.
- **Live-corpus regression catches (T3, 13 new test cases).** `tests/project-tag-invariant.test.js` (NEW, 6 tests): 1 distribution-sanity guard (no project >50% of total unless dominant), 4 content-vs-tag invariants (top tag for `content ILIKE '%termdeck%'` must be `termdeck`; same for pvb/claimguard/mnestra identifiers), 1 residual guard (≤25% of termdeck-identifier rows tagged chopin-nashville post-backfill — pre-backfill state today is 86%, 113/131). Skips cleanly with `pg connect/probe failed` when `DATABASE_URL` is unset (matches `failure-injection.test.js` skip-on-no-server pattern), so CI without a live Mnestra stays green. **Designed to fail pre-backfill, pass post-backfill** — the v0.7.2 release ships them red on Josh's live corpus today and turning them green is one of the success criteria for whether to RUN Block 2. `tests/flashback-e2e.test.js` grows from 2 → 3 tests with a project-bound case that creates a session with `meta.project='termdeck'`, triggers the canonical shell error, and asserts the `proactive_memory` frame includes a non-empty `memories` array. Pre-flight queries `/api/ai/query` with `project='termdeck'` and skips with a `needs-backfill` directive when zero matches, avoiding generic 8s timeouts. v0.7.1 PHASE-A diagnostic instrumentation preserved.

### Notes
- **What v0.7.2 deliberately does NOT fix.** The actual upstream emitter — `~/.claude/hooks/memory-session-end.js:17` — is in Josh's user-owned global Claude Code harness config, NOT in any TermDeck/Mnestra/Rumen repo. No code in this release touches that file. Recommended one-paste fix (also addresses T3's gorgias→claimguard finding): insert `{ pattern: /SideHustles\/TermDeck\/termdeck/i, project: 'termdeck' }` and matching leaf entries for engram/mnestra, rumen, podium, claimguard BEFORE the `ChopinNashville` and `gorgias` entries — or, more durably, rewrite `detectProject` as longest-substring-wins over a project map keyed by canonical name. Without this fix, every new Claude Code session-end inside a TermDeck panel will continue to land in `memory_items` with `project='chopin-nashville'`. The TermDeck-side audit logs added in v0.7.2 will not show this drift (the harness hook does not go through TermDeck's writer); only the live-corpus invariant probe will. POSTMORTEM at `docs/sprint-34-project-tag-fix/POSTMORTEM.md` has the full chain.
- **The principle this locks in further.** v0.6.9 added install-time `auditPreconditions()`. v0.7.0 added runtime `auditPreconditions()` via `/api/health/full`. v0.7.1 added analyzer-coverage fixtures. v0.7.2 adds **writer-side observability** — every memory write and every query now logs its project tag and resolution source, so the chain that produced 1,165 silently-mis-tagged rows over 2+ weeks would surface within minutes the next time anything similar happens.
- **Stack-installer audit-trail bumped 0.3.1 → 0.3.2.** Mnestra (0.2.2) and Rumen (0.4.3) unchanged. **A separate `@jhizzard/rumen@0.4.4` would harden Rumen's `extract.ts:62` to use mode-of-source-projects or distinct-count guard instead of `(ARRAY_AGG)[1]`** — but fixing the harness hook first heals Rumen on next tick (synthesized rows pick up the correct upstream tag), so the Rumen-side fix is lower priority than the hook fix.
- **Full CLI suite: 12 new tests in `tests/project-tag-resolution.test.js` all green; 6 new tests in `tests/project-tag-invariant.test.js` skip cleanly without a live `DATABASE_URL` and intentionally fail against the pre-backfill live corpus (turning green is the post-Block-2 success signal); `tests/flashback-e2e.test.js` grows 2 → 3 tests; all v0.7.1 tests still green.** Adjacent suites unchanged.
- **Sprint 34 wall-clock: ~15 minutes from open to all-three-DONE** (T2 22:14:45Z, T3 22:09:27Z, T1 22:15:00Z; sprint opened 22:00Z). T4 Phase A 22:10:30Z, READY post-integration.
- **Live-store backfill execution status:** *deferred — `scripts/migrate-chopin-nashville-tag.sql` ships in the package with a default-runs dry-run block; whether Block 2 is executed against the live `petvetbid` store is an orchestrator+Josh decision after sample inspection. Run via `psql "$DATABASE_URL" -f scripts/migrate-chopin-nashville-tag.sql` for the dry-run, or uncomment Block 2 first to apply.* <!-- Orchestrator: replace this line with "executed at <ts> against petvetbid; N rows reclassified" if/when Block 2 runs. -->

## [0.7.1] - 2026-04-26

### Fixed
- **Flashback never firing on common Unix shell errors.** Reported by Josh as *"Flashbacks are vaporware. They never happen, never any suggestions."* `tests/flashback-e2e.test.js` reproduced the silence cleanly: the pipeline times out at 8.6s waiting for a `status_changed→errored` row in `rag_events`. Sprint 33's 4+1 forensic audit (T1 analyzer + T2 bridge + T3 Mnestra query path + T4 e2e/postmortem) converged in ~24 minutes on a regex coverage gap in `PATTERNS.error`.
- The session.js header comment claims `PATTERNS.error` catches plain-English Unix tool errors (`No such file or directory`, `Permission denied`, `command not found`, etc.) — the regex never actually included those phrases. Pure "documentation is not verification" miss; the code lied to its own comment. The analyzer fired on structured errors (`Error:`, `Traceback`, `npm ERR!`, `error[Ennn]:`) but stayed silent for the plain-English shell errors that dominate real terminal usage. Josh's `cat /nonexistent/file/path` → `cat: /nonexistent/file/path: No such file or directory` produced zero status flips, zero bridge calls, zero `proactive_memory` frames.
- Fix in `packages/server/src/session.js` (+14 LOC): new `PATTERNS.shellError` regex covering colon-prefixed `<cmd>: <path>: No such file or directory|Permission denied|Is a directory|Not a directory|command not found`, plus `curl: (NN) Could not resolve host`, `Segmentation fault`, `ModuleNotFoundError:`, lowercase git `fatal:`. Anchored on real shell-error structure so adversarial prose mentioning the same phrases in narrative context does NOT trigger (verified against a 7-fixture false-positive suite). `_detectErrors` now falls through to `PATTERNS.shellError` when the primary pattern misses.
- New regression coverage in `tests/analyzer-error-fixtures.test.js` (+48 LOC): `SHELL_ERROR_SHOULD_TRIGGER` (10 fixtures) and `SHELL_ERROR_SHOULD_NOT_TRIGGER` (7 prose fixtures). All 4 tests in the file pass. Net-positive on `packages/server/tests/session.test.js` — turns a pre-existing `'zsh: command not found: kubectl'` failure GREEN with no other breakage.
- New instrumentation in `tests/flashback-e2e.test.js` (T4 Phase A): on test failure the dump now includes the WS frame log, sampled `session.meta` per poll, all `rag_events` for the session, and a transcript snapshot. Turns black-hole timeouts into labeled fault-isolating diagnostics — the regression defense that would have prevented Flashback going silent for 5+ sprints.

### Notes
- **Two independent root causes were diagnosed; only one is fixed in v0.7.1.** The second (T3's finding: `chopin-nashville` project-tag regression that accumulated 1,126 rows of TermDeck content under the wrong tag while the canonical `termdeck` tag has only 68) excludes Josh's relevant memories from the bridge's strict project filter even when the analyzer fires correctly. v0.7.1 ships the analyzer fix only because (a) it's surgical and pure-code, (b) T3's fix touches the live Supabase store via UPDATE statements + needs writer-side source review, and (c) shipping them separately keeps the changelog narrative honest. T1's fix unblocks `flashback-e2e.test.js` and Flashback for sessions WITHOUT a `meta.project` set (the bridge passes `filter_project=null` and search-all surfaces matches). Josh's daily live workflow needs both.
- **Sprint 34 ships T3's fix:** chopin-nashville → real-project-tag backfill SQL + a writer-side audit to find where the mis-tag happens. Fix lands as v0.7.2. POSTMORTEM at `docs/sprint-33-flashback-debug/POSTMORTEM.md` has the full diagnosis + Sprint 34 acceptance criteria.
- **The principle locked in further:** v0.6.9 added `auditPreconditions()`. v0.7.0 added runtime `auditPreconditions()` via `/api/health/full`. v0.7.1 adds a fixture suite that's the contract test that would have caught this in 2026-04-15 when the comment was written but the regex wasn't updated.
- **Stack-installer audit-trail bumped 0.3.0 → 0.3.1.** Mnestra (0.2.2) and Rumen (0.4.3) unchanged.
- Sprint 33 wall-clock: 24 minutes from open to converge across four parallel terminals. T1 + T2 + T3 + T4 all DONE within 8 minutes of each other.

## [0.7.0] - 2026-04-26

### Added — runtime correctness, the v0.6 → v0.7 arc

> **Theme:** v0.6.x closed install-time correctness with `auditPreconditions()` + `verifyOutcomes()`. v0.7.0 extends that pattern from install-time into runtime: themes that track config edits, auth that doesn't ask twice, and a runtime health endpoint that mirrors the install-time audit.

- **Theme persistence — `session.meta.theme` is now a render-time getter, not a creation-time snapshot.** Closes Brad's 2026-04-26 report (*"can't get theme changed. ignores changes to config.yaml and is stuck in tokyo night"*). New module `packages/server/src/theme-resolver.js` exports `resolveTheme(session, config)` walking `{ session.theme_override → config.projects[p].defaultTheme → config.defaultTheme → 'tokyo-night' }`, plus an mtime-keyed disk-cache (`getCurrentConfig`) so `meta.theme` doesn't re-read `~/.termdeck/config.yaml` on every 2s metadata broadcast yet still picks up edits without a server restart. `database.js`: added `sessions.theme_override TEXT NULL` via in-place schema migration with a one-shot backfill from the legacy `theme` column (only fires the first time the column is added — so post-migration `NULL` inserts stay `NULL`, preserving the user-set defaults from before the upgrade while making fresh sessions follow config out of the box). Dropped dead `projects.default_theme` column (grep confirmed never read or written; removed as a latent contract-drift trap). `session.js`: `meta.theme` is now an `Object.defineProperty` getter that resolves at read time; setter routes to `theme_override`. `SessionManager.create()` writes both columns (`theme` = legacy snapshot for back-compat, `theme_override` = `NULL` on create, populated only via PATCH). `updateMeta()` on theme writes to `theme_override`. PATCH `{ theme: null }` clears the override and reverts to the config-derived default. Client (theme region only): added an `↺ default` reset link next to the theme dropdown that PATCHes `theme: null` and applies the resolved value from the response. **No `index.js` touched** — the existing `meta.theme` reader in the metadata broadcast picks up the getter without modification. 13 new tests in `tests/theme-persistence.test.js` (resolveTheme path coverage ×5, backfill correctness, idempotency on second init, fresh-create NULL invariant, config edit propagates with no SQL update between reads, override wins over config, PATCH null clears, getter reflects current resolveTheme output, setter routes through `theme_override`).
- **Auth cookie now persists 30 days, HttpOnly, SameSite=Lax.** Closes Brad's 2026-04-26 report (*"is there a way not to have to enter the token at each termdeck session?"*). `Set-Cookie: termdeck_token=...; Max-Age=2592000; HttpOnly; SameSite=Lax`. The `Secure` flag is set when the request was over HTTPS — detected via `req.protocol === 'https'` or the `X-Forwarded-Proto` header from a reverse proxy. New `POST /api/auth/login` handler intercepted by `createAuthMiddleware` issues the cookie server-side, so **no `index.js` change was needed** — the existing middleware mount point was the only edit surface. The login page now POSTs the token instead of writing `document.cookie` from the client; this is what finally lets `HttpOnly` actually be honored (you can't write an HttpOnly cookie from JavaScript, which is why every prior auth iteration silently dropped the flag). Same cookie name and value format as v0.6.x — no migration, no new flag, no new config. Head-comment in `auth.js` documents the 30-day trade-off: TermDeck is intended as a local dev tool; cookie-compromise risk is bounded by the local-only attack surface; the UX win materially impacts adoption. 11 new tests in `tests/auth-cookie.test.js`.
- **`GET /api/health/full` runtime health endpoint.** New module `packages/server/src/health.js` exports `getFullHealth(config, db, opts)` → `{ ok, checks: [{ name, status: 'pass' | 'fail' | 'warn', detail? }] }`. Five required pg-side checks mirror the v0.6.9 audit/verify SQL — Postgres reachability, `memory_items.source_session_id` present, `pg_cron` + `pg_net` enabled, Vault `rumen_service_role_key` present, `cron.job` rumen-tick active — plus SQLite reachability, plus two warn-only probes for the Mnestra webhook (`/healthz`) and the Rumen pool. Returns `ok: true` only when every required check passes; warn-only failures surface in the report but do not flip `ok`. 30-second module-scope cache so polling from a status bar doesn't hammer Postgres; `?refresh=1` bypasses the cache. Never-throws contract — internal exceptions are caught and surface as `{ status: 'fail', detail }` rather than 500ing the request. One block added to `index.js`: `require('./health')` + a handler that returns 200 when `ok`, 503 when any required check fails, 500 only when the aggregator itself throws. Reuses `preconditions.js` helpers from v0.6.9 — install-time and runtime share one source of truth for what "healthy" means. 8 new tests in `tests/health-full.test.js` (7 unit with a fake pg client + fake sqlite handle covering happy path, missing-source_session_id with the npm-cache-clean hint, `pg_cron`-disabled with the dashboard hint, webhook warn doesn't flip `ok`, cache hit/miss/refresh, throw-swallow on warn probe, throw-swallow on required check; 1 live-server smoke spawning the CLI on a fresh-`HOME` free port and asserting the JSON shape end-to-end).

### Notes
- **The principle this extends:** v0.6.9 locked in *"documentation is not verification."* v0.7.0 extends that into runtime: an install that was correct at install time can drift later (config edits ignored, auth state dropped, extensions disabled by an admin upgrade, pools quietly stopping). Without a runtime equivalent of the audit, the user finds out via a missing Flashback toast two days later. `/api/health/full` is the on-demand answer to the same principle.
- **Behavior change for users on v0.6.x:** existing per-session theme picks are backfilled into `theme_override` and survive untouched, so customizations don't reset on upgrade. Auth cookies set on v0.6.x will be re-issued with the longer max-age and `HttpOnly` flag on the next login — no manual action. The new health endpoint is additive; the existing `/api/health` is unchanged.
- **Stack-installer audit-trail bumped 0.2.8 → 0.3.0.** Mnestra (0.2.2) and Rumen (0.4.3) unchanged.
- **Full CLI suite: 104/104 green** (was 72, +32 new — 13 in `tests/theme-persistence.test.js`, 11 in `tests/auth-cookie.test.js`, 8 in `tests/health-full.test.js`). Adjacent suites still green: preconditions 11/11, migration-loader-precedence 4/4, setup-prompts 5/5, health-contract 3/3, transcript-contract 4/4, analyzer-error-fixtures 9/9.
- **Recovery if you upgrade and a session looks wrong:** `npm cache clean --force && npm i -g @jhizzard/termdeck@latest`, restart termdeck. The `theme_override` column is added in-place by the database init code; no manual migration needed. To clear all per-session overrides and let `config.yaml` take over for fresh sessions only, click `↺ default` in each panel's theme dropdown. To reset every session at once, `UPDATE sessions SET theme_override = NULL` in `~/.termdeck/termdeck.db`.
- **What v0.7.0 deliberately does NOT include** (deferred to v0.7.x or later): migration-001 idempotency fix (`CREATE OR REPLACE FUNCTION` return-type collision on re-runs against an already-upgraded store), Rumen-MCP gap (NULL `source_session_id` on MCP-written memories never reaching Rumen synthesis), theme picker UX overhaul (per-project palette, custom themes).

## [0.6.9] - 2026-04-26

### Added — the deliberate close to the v0.6.x incident saga
- **`auditPreconditions()` and `verifyOutcomes()` for both wizards.** New module `packages/server/src/setup/preconditions.js`. Closes the failure class that produced four of the eight v0.6.x patches (v0.6.4 token, v0.6.6 pgbouncer, v0.6.7 mcp.json, "v0.6.9-equivalent" extensions): a documented manual step that wasn't verified in code.
- **`auditRumenPreconditions(secrets, env)`** runs FIRST in `init-rumen.js`, before any state-changing operation. Surfaces every gap in one pass with actionable hints: Supabase CLI auth (token in env OR `supabase login` worked), `pg_cron` extension enabled, `pg_net` extension enabled, Vault secret `rumen_service_role_key` present. Distinguishes Vault-permission-denied from Vault-secret-missing — different hints. Refuses to proceed on any gap; no partial work.
- **`verifyRumenOutcomes(secrets)`** runs after the schedule SQL applies. Confirms `cron.job` has an active rumen-tick row. Doesn't poll for the first 15-min tick (too long for an interactive wizard) but tells the user exactly what query to run after waiting.
- **`verifyMnestraOutcomes(secrets)`** runs after migrations apply. Confirms `memory_items` exists, `memory_items.source_session_id` exists (the v0.6.5 column whose absence cascaded into Brad's Rumen failures), and `memory_status_aggregation()` exists. **This is the test that, if it had existed before v0.6.5, would have caught the silent-shadow saga at install time instead of cron-tick time.**
- **10 regression tests** in `tests/preconditions.test.js` using a fake pg client. Covers all-gaps-surface, all-clean-passes, vault-permission-vs-missing-distinction, cron-active vs cron-inactive vs cron-missing, the source_session_id gap with the npm-cache-clean recovery hint, the missing-table gap, and smoke tests for the report renderers.

### Notes
- **The principle this locks in:** *"Documentation is not verification. Anything documented as a manual step must also be runtime-checked, or the first unsupervised user pays."* Saved as a global universal memory before this release shipped.
- **Behavior change for users with incomplete preconditions:** wizards that previously half-succeeded now fail-fast with a consolidated gap report. The exit code from a missing precondition is 10 (audit) or 11 (verify). Old behavior — proceeding into a half-applied state — is gone. This is a deliberate UX shift, in service of "first unsupervised user shouldn't pay."
- **Stack-installer audit-trail bumped 0.2.7 → 0.2.8.** Mnestra (0.2.2) and Rumen (0.4.3) unchanged.
- **Full CLI suite: 72/72 green** (was 62, +10 new).
- **What v0.6.9 deliberately does NOT include** (deferred to v0.7.0): theme persistence fix (Brad's "stuck in tokyo night" report), auth-cookie 30-day persistence (Brad's "have to enter token at each browser session"), `/api/health/full` runtime health endpoint. v0.7.0 is "the install you can trust to stay healthy" — extends the audit/verify pattern from install-time into runtime.
- **End of the v0.6.x arc.** Eight patches in 48 hours started with a CRLF leak and ended with a precondition-audit framework. The longitudinal failure-class analysis is in the global memory `UNIVERSAL DEBUGGING PATTERN — When a project's release stream becomes "one patch per user report"...`

## [0.6.8] - 2026-04-26

### Fixed
- **Migration loader silently shadowing newer bundled migrations with a stale `@jhizzard/mnestra` in global node_modules.** Brad reported 2026-04-26 12:47 PM after upgrading to v0.6.5 and re-running `termdeck init --mnestra --yes`: *"Still getting column m.source_session_id does not exist. The 6 migrations all applied cleanly but the column is still missing. Looks like the new migration wasn't included in the --yes run."* Root cause: `packages/server/src/setup/migrations.js`'s `listMnestraMigrations()` checked `node_modules/@jhizzard/mnestra/migrations/` BEFORE the bundled directory and used it whenever any `.sql` files were found there. The meta-installer (`@jhizzard/termdeck-stack`) installs `@jhizzard/mnestra` globally as a peer; `npm i -g @jhizzard/termdeck@latest` doesn't touch that sibling install. So Brad's stale `mnestra@0.2.1` (6 migrations) silently shadowed v0.6.5's bundled 7 migrations. The wizard reported "6 migrations applied" because it really was applying 6 — the wrong six.
- v0.6.8 flips the precedence: bundled FIRST. `node_modules/@jhizzard/mnestra/migrations/` is now used only as a safety-valve fallback when the bundled directory is missing entirely (e.g. someone `rm -rf`'d it manually). Bundled is what TermDeck developed and tested against; that's the source of truth. Same change applied to `listRumenMigrations()` for symmetry.
- New `tests/migration-loader-precedence.test.js` (4 cases) — the regression test that, if it had existed before v0.6.5, would have caught Brad's bug at test time. Pins (a) the bundled directory contains all 7 migrations, (b) `listMnestraMigrations()` returns 7 in lexical order, (c) returned paths are under the bundled directory not node_modules, (d) **a fake stale `@jhizzard/mnestra@0.2.1` with only 6 migrations does NOT shadow the bundled 7** (Brad's exact scenario).

### Notes
- **Recovery for anyone affected (Brad and any other v0.6.5/.6/.7 user with stale global Mnestra):** `npm cache clean --force && npm i -g @jhizzard/termdeck@latest`, then `termdeck init --mnestra --yes`. With v0.6.8 the wizard reads bundled migrations regardless of what's in node_modules — no separate Mnestra upgrade required. Anyone who already manually added the column or already upgraded their global Mnestra is fine; the new migration is `IF NOT EXISTS` idempotent.
- This is the bug the v0.6.5 fix was meant to ship. The schema patch was correct; the loader hid it. The right defensive change earlier would have been a contract test that exercised the loader against a stale-shadow scenario — exactly what `tests/migration-loader-precedence.test.js` now does. (Mirror of universal lesson #5 from the v0.6.x post-mortem: cross-package contracts need contract tests, not goodwill.)
- Stack-installer audit-trail bumped 0.2.6 → 0.2.7. Mnestra (0.2.2) and Rumen (0.4.3) unchanged.
- Full CLI suite: 62/62 green (was 58, +4 new).

## [0.6.7] - 2026-04-26

### Fixed
- **`SUPABASE_ACCESS_TOKEN` placeholder never being replaced in `~/.claude/mcp.json`.** Brad reported 2026-04-26: *"the token hadn't been written to the Json file which we updated manually, but you may want to put that in the patch at some point."* Root cause: the meta-installer (`@jhizzard/termdeck-stack`) wires the Supabase MCP server entry with `SUPABASE_ACCESS_TOKEN: 'SUPABASE_PAT_HERE'` as a literal placeholder. v0.6.4 told users to `export SUPABASE_ACCESS_TOKEN=...` for `supabase link` — but the export only affected their shell, never propagated into the JSON config. Claude Code's Supabase MCP server stayed broken with the placeholder until the user manually edited the file.
- New helper `wireAccessTokenInMcpJson()` in `packages/cli/src/init-rumen.js`. Runs after `supabase link` succeeds (token verified-real at that point). Reads `~/.termdeck/.claude/mcp.json` (correctly: `~/.claude/mcp.json`), replaces the placeholder if it's the literal `SUPABASE_PAT_HERE`, leaves any real user-set token untouched, atomic write with mode 0600, preserves all other `mcpServers` entries verbatim. Idempotent. No-op when the file is missing, the supabase MCP entry is missing, or `process.env.SUPABASE_ACCESS_TOKEN` is unset. Malformed JSON surfaces a soft warning but doesn't throw.
- New `tests/init-rumen-mcp-json.test.js` (10 cases): placeholder gets replaced, sibling entries preserved verbatim, mode 0600 enforced, real token never overwritten, same-token-already-set is a no-op, missing token / missing file / missing supabase entry all return clean status codes, malformed JSON returns `status=malformed`, supabase entry without an env block gets one created.

### Notes
- This closes the v0.6.4–v0.6.7 incident arc end-to-end. Brad's stack should now: (1) Mnestra schema correct (v0.6.5), (2) DATABASE_URL normalized (v0.6.6), (3) Supabase MCP token wired automatically (v0.6.7). Anyone re-running `termdeck init --rumen` after upgrading will get the JSON updated automatically — no manual edit needed.
- Stack-installer audit-trail bumped 0.2.5 → 0.2.6. Mnestra (0.2.2) and Rumen (0.4.3) unchanged.
- Full CLI suite: 58/58 green (was 48, +10 new in `init-rumen-mcp-json.test.js`).

## [0.6.6] - 2026-04-26

### Added
- **Auto-append `?pgbouncer=true&connection_limit=1` on Supabase transaction-pooler URLs.** Brad's 2026-04-26 Rumen logs surfaced the warning Supabase recommends but our wizard had been ignoring: a Shared Pooler URL on port 6543 (transaction mode) needs those query params or PgBouncer can return prepared-statement errors under load. The wizard now detects the URL shape — host ends in `.pooler.supabase.com` AND port is `6543` — and appends the params on the user's behalf. Direct connections (port 5432, `db.<ref>.supabase.co`) and session-mode pooler URLs (port 5432 on pooler hostname) are unchanged. Idempotent: a URL that already has `pgbouncer=true` is returned untouched. Wiring lives in two places: (a) `init-mnestra.js writeSecretsFile()` normalizes at write time so the URL hits disk clean; (b) `init-rumen.js preflight()` normalizes again when reading from `secrets.env` to forward to the Edge Function as a function secret, so partial-upgrade installs (users who ran v0.6.5 wizards previously) still get a clean URL handed to Rumen on a v0.6.6 re-run.
- New `tests/supabase-url-normalize.test.js` (15 cases): transaction-pooler detection (incl. future regional prefixes like `gcp-1-*`), session-mode pooler not detected, direct connection not detected, non-Supabase host not detected, append-when-missing, idempotent on already-set, preserve user-set `connection_limit`, preserve unrelated query params, defensive on null/empty/malformed input.

### Notes
- This is a UX polish release — the v0.6.5 schema fix already unblocked Rumen end-to-end. v0.6.6 closes the secondary warning Brad's logs showed alongside the schema error. Anyone whose `~/.termdeck/secrets.env` already has a transaction-pooler URL without the params can simply re-run `termdeck init --mnestra --yes` after upgrading; the normalizer rewrites the line in place via the merge-aware writer in `dotenv-io.js`.
- Stack-installer audit-trail bumped 0.2.4 → 0.2.5. Mnestra and Rumen unchanged.
- Full CLI suite: 48/48 green (was 33, +15 new in `supabase-url-normalize.test.js`).

## [0.6.5] - 2026-04-26

### Fixed
- **`termdeck init --rumen` deploying cleanly but the Edge Function failing on every cron tick with `column m.source_session_id does not exist` (Postgres SQLSTATE 42703).** Reported 2026-04-26 by Brad after v0.6.4 unblocked his Rumen install. The Rumen Edge Function deploys and runs, but the manual POST test returns 500 and every subsequent `pg_cron` tick fails inside `extract.ts` at `SELECT m.source_session_id ... FROM memory_items m GROUP BY m.source_session_id`.
- Root cause: schema drift between the published Mnestra migration set and Rumen's runtime contract. The `source_session_id TEXT` column on `memory_items` existed in the original `rag-system` schema (and is still present on stores that were upgraded from rag-system → Engram → Mnestra), but was dropped from the published Mnestra migrations during the rebrand. Rumen v0.4.x's Extract phase depends on it. Fresh installs of TermDeck → Mnestra got a schema that worked for TermDeck/Flashback but couldn't host Rumen.
- Fix: new bundled migration `007_add_source_session_id.sql` adds the column back as `TEXT`, idempotent (`ADD COLUMN IF NOT EXISTS`), with a partial index on `WHERE source_session_id IS NOT NULL`. Mirrored to the Mnestra source repo's `migrations/` directory so direct `@jhizzard/mnestra` installers also pick it up. NULL on every existing row is the correct default — old memories were never tagged with a session, and Rumen's `WHERE source_session_id IS NOT NULL` filter excludes them naturally.
- The migration loader at `packages/server/src/setup/migrations.js` already globs `*.sql` from `mnestra-migrations/` and applies in lexical order, so the new file is picked up automatically — no code changes needed.

### Notes
- **Recovery path for anyone affected (e.g. Brad):** `npm cache clean --force && npm i -g @jhizzard/termdeck@latest && termdeck init --mnestra --yes`. The `--yes` flag reuses saved secrets, the wizard re-applies all 7 migrations idempotently, the new column lands, and the next pg_cron tick (within 15 min) will succeed against memory_items. No Edge Function redeploy required — the failure is at query time, not deploy time.
- This is the **migration drift between layers** problem Codex flagged in the 2026-04-25 audit. Sprint-32's "schema_migrations tracking table" candidate (deferred from v0.6.3 live test) becomes more important after this incident — currently every fresh install re-runs all 7 migrations, which works because they're all idempotent, but a `schema_migrations` table would make this auditable.
- Stack-installer audit-trail bumped 0.2.3 → 0.2.4. Mnestra source repo also bumped 0.2.1 → 0.2.2 to ship the migration in published `@jhizzard/mnestra` for direct installers.

## [0.6.4] - 2026-04-26

### Fixed
- **`termdeck init --rumen` failing with raw "Access token not provided" stderr from `supabase link`.** Brad reported on 2026-04-26 00:25 UTC after v0.6.3 unblocked `init --mnestra`: the Supabase CLI's actionable suggestion is *"run supabase login or set SUPABASE_ACCESS_TOKEN"*, but `supabase login` opens a browser, which doesn't work on MobaXterm SSH or any headless install. The wizard previously dumped that message verbatim and exited. Now `link()` in `packages/cli/src/init-rumen.js` detects the access-token-missing signature and prints a path-aware hint with the Supabase dashboard URL (`https://supabase.com/dashboard/account/tokens`) plus the exact `export SUPABASE_ACCESS_TOKEN=sbp_...` command and a re-run instruction. Detection lives in the small `looksLikeMissingAccessToken(stderr)` helper (anchored on the literal phrase plus the env-var name so future Supabase CLI rewordings still match).
- New `tests/init-rumen-access-token.test.js` (4 cases): real-stderr match, env-var-name fallback match, no false positives on unrelated link errors, and a content lock on the printed hint (dashboard URL + `export sbp_` shape + retry command).

### Added
- **`termdeck init --mnestra --from-env`** — skip every `askSecret` prompt and read all five secrets from `process.env` (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`, `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`). Defensive bypass for any terminal that fights with our raw-mode secret prompt and a clean primitive for CI / one-shot installers. Strict by design — missing or malformed env vars exit 2 with an actionable error including the corrected invocation. Anthropic remains optional. The persist-first guarantee from v0.6.3 still applies: secrets land on disk before any pg work.
- New flag entries in `--help` and the head-of-file docstring at `packages/cli/src/init-mnestra.js`. 3 new tests in `tests/init-mnestra-resume.test.js` covering successful skip-prompts path, missing-env-var error path, and shape-validation rejection. Test harness now strips inherited Mnestra/Supabase env vars from spawned children so host credentials cannot leak.

### Notes
- All 10 net-new tests in this release pass. v0.6.3's resume-path tests still pass — no regressions in the `--yes` / `--reset` surface.
- Brad's fourth incident is fully closed by v0.6.3 + this hint. He cleared his npm cache (was running v0.6.0 the whole time despite earlier installs), upgraded to v0.6.3, and Mnestra applied cleanly end-to-end against project `rrzkceirgciiqgeefvbe`. The Rumen blocker that surfaced next is what v0.6.4 addresses.
- `@jhizzard/termdeck-stack` audit-trail bumped 0.2.2 → 0.2.3 (no installer behavior change; npx always pulls `@jhizzard/termdeck@latest`).

## [0.6.3] - 2026-04-25

### Fixed
- **`termdeck init --mnestra` discarding the user's typed-in DATABASE_URL when the Postgres connection or migration step failed — fourth report from Brad after v0.6.2.** v0.6.2 dropped the post-key confirm gate and the wizard ran cleanly past the Anthropic prompt, but if the subsequent `pgRunner.connect`, `checkExistingStore`, or `applyMigrations` step threw, the wizard exited before `writeLocalConfig()` ever ran — so `~/.termdeck/secrets.env` was never updated and the user had to retype every secret on the next attempt. Brad's exact words: *"It's killing before writing the file. Postgrep line not added to my existing file, so it wasn't changed."*
- The fix splits the file writes into two passes: (1) `writeSecretsFile()` runs immediately after `collectInputs()` returns, before any pg work — `secrets.env` lands on disk regardless of what the database does; (2) `writeYamlConfig()` (which flips `rag.enabled: true`) runs only after migrations apply cleanly, so the server can never come up against a half-applied schema.
- Added a resume path: `collectInputs()` now reads `~/.termdeck/secrets.env` at start and, if a complete set of required keys is present, offers *"Found saved secrets … Reuse?"*. With `--yes` the wizard skips the prompt and proceeds straight to the database step. With `--reset` the wizard ignores saved secrets and re-prompts from scratch. Both flags answer Brad's 17:50 ET question *"If it got that far did it write the correct secret.env? If so, can I manually do the next steps?"*
- On any pg-side failure the wizard now prints the resume hint: `termdeck init --mnestra --yes`.
- New `tests/init-mnestra-resume.test.js` (3 cases): persist-first under pg failure, `--yes` skips prompts when saved secrets are complete, `--reset` re-prompts and overwrites.

### Notes
- Reported by Brad on 2026-04-25 (fourth occurrence — first three drove v0.6.1 and v0.6.2). No retype required on existing partial installs: rerun `npx @jhizzard/termdeck-stack` (or `npm i -g @jhizzard/termdeck@latest`), then `termdeck init --mnestra --yes` will pick up the saved keys.
- `@jhizzard/termdeck-stack` does not need a republish — it always installs `@jhizzard/termdeck@latest`. An audit-trail bump to `@jhizzard/termdeck-stack@0.2.2` follows in `packages/stack-installer/CHANGELOG.md` to validate against this release.

## [0.6.2] - 2026-04-25

### Fixed
- **`termdeck init --mnestra` cancelling after the Anthropic API key prompt — third report from Brad after v0.6.1.** v0.6.1 hardened `askSecret` against CRLF leaks, ANSI escape pollution, and hard-SIGINT-on-Ctrl-C, but the wizard still aborted on Brad's terminal because the `confirm("Proceed with setup for project X?")` gate that ran immediately after the key prompt was the consistent failure surface — leftover bytes (CRLF tail, terminal cursor reports, paste-bracketing markers) carried into the readline that powered the confirm and resolved it as a cancel before the user could answer. Removed the confirm. The user already opted in by typing `termdeck init --mnestra` and supplying every secret; Mnestra's migrations are `IF NOT EXISTS` so re-runs are idempotent; Ctrl-C still aborts cleanly. The `--yes` flag is preserved as a no-op for forward compatibility (scripts that already pass it keep working). `termdeck init --rumen`'s confirm is intentionally retained — it gates a heavier deploy step and runs before any secret prompt, so the byte-contamination surface is different there.

### Notes
- Reported by Brad on 2026-04-25 (third occurrence). Upgrade with `npm install -g @jhizzard/termdeck@latest` and the Anthropic key prompt is the last interactive step before the wizard runs.
- `@jhizzard/termdeck-stack` does **not** need a republish — it always installs `@jhizzard/termdeck@latest`, so a fresh `npx @jhizzard/termdeck-stack` picks this up. `termdeck doctor` will surface the available update on existing installs.

## [0.6.1] - 2026-04-25

### Fixed
- **`termdeck init --mnestra` aborting after the Anthropic key prompt on MobaXterm SSH and other terminals that send CRLF for Enter.** Three separate bugs in the secret-prompt raw-mode loop in `packages/server/src/setup/prompts.js`:
  1. **CRLF leak.** When the terminal delivered `\r\n` as a single chunk (Windows / MobaXterm Enter), the loop matched the `\r`, resolved, and dropped the rest. The trailing `\n` then surfaced through the next prompt's stream and slipped through to the confirm() that follows the Anthropic prompt. With unfortunate timing the chunk also contained `` from a stray keystroke and the original SIGINT branch fired, killing the wizard mid-flow. Now: CRLF is drained inside the same chunk; non-newline trailing bytes are pushed back via `stdin.unshift` so the next consumer reads cleanly.
  2. **ANSI escape pollution.** Some terminals emit `[…]` sequences for non-character events (focus changes, cursor reports, paste-bracketing). The original loop fed those bytes into the password buffer and echoed `*` for each one. Now consumed silently.
  3. **Hard SIGINT during a secret prompt** is now a soft cancel — return empty string, let the caller's shape validator re-prompt — instead of `process.kill`-ing the process. The hard-kill was masking the CRLF bug above and aborting the wizard from stray bytes.
- New `tests/setup-prompts.test.js` — 7 fixtures covering Unix LF, Windows CRLF, type-ahead carry-over, ANSI cursor-position-report mid-buffer, bracketed-paste markers, soft-cancel on Ctrl-C, and DEL/backspace. All pass.

### Notes
- Reported by Brad on 2026-04-25 (twice). No workaround required — upgrade with `npm install -g @jhizzard/termdeck@latest` (or rerun `npx @jhizzard/termdeck-stack`) and the wizard works on every terminal we've tested.

## [0.6.0] - 2026-04-25

### Added — Sprint 25 (Supabase MCP wizard)
- **One-click Supabase auto-fill** in the Tier 2 setup wizard. User pastes a Supabase Personal Access Token, the wizard lists their projects via `@supabase/mcp-server-supabase`, the user picks one, and the existing Sprint 23 configure+migrate flow takes it from there. Manual paste path is preserved. Three new endpoints: `POST /api/setup/supabase/{connect,projects,select}`. PAT held in a closure for the wizard's lifetime only — never persisted, never logged.
- **Yellow `[hint]` line** on startup when RAG is enabled and the Supabase MCP isn't installed: `Supabase MCP not installed — wizard auto-fill unavailable. Install with: npx @jhizzard/termdeck-stack --tier 4`. Suppressed when `~/.claude/mcp.json` already declares a `supabase` server. Tier 1 users see no output.
- **`packages/server/src/setup/supabase-mcp.js`** — bridge module that spawns `@supabase/mcp-server-supabase` as a child, JSON-RPC over stdio, timeout-protected, kills child on resolve/reject. Zero new npm deps.

### Added — Sprint 28 (update-signal mechanisms)
- **`termdeck doctor`** subcommand prints a four-package version-check table (TermDeck + Mnestra + Rumen + termdeck-stack), comparing installed vs npm latest. Exit codes 0 (all current) / 1 (updates available) / 2 (network failure). `--json` and `--no-color` flags.
- **Rate-limited startup update-check banner.** When a TermDeck update is available, exactly one yellow `[hint]` line prints on startup. Cache at `~/.termdeck/update-check.json` enforces a 24h TTL so the check never hits the registry more than once per day. Suppressed by `TERMDECK_NO_UPDATE_CHECK=1`, by non-TTY stdout, and by a fresh cache. All failures (network, cache write, parse) are swallowed — never blocks startup.
- **README "Staying current" section** + new `docs/SEMVER-POLICY.md` documenting per-package version semantics and the upgrade-risk table.

### Notes
- v0.5.1 was queued (start.sh silent-exit fix) but folded into v0.6.0 — the fix is included.
- This is a minor bump because two new feature surfaces ship together: the Supabase MCP wizard auto-fill (Sprint 25) and the doctor + update-check signals (Sprint 28). No breaking changes for users on v0.5.x.

## [0.5.1] - 2026-04-25

### Fixed
- **`scripts/start.sh` silent exit** — running the repo-clone launcher under v0.5.0 dropped users back to the shell after printing two "Stack Launcher" banners. Root cause: the v0.5.0 CLI auto-orchestrate detection routed back into `stack.js`, whose `execTermDeck` then `require()`-d the already-cached `index.js` and the server never started. Fix is two-part: (1) `stack.js` now spawns a fresh node process for the CLI (avoids the require-cache trap) and passes `--no-stack` so the inner CLI definitively skips re-detection; (2) `scripts/start.sh` itself now passes `--no-stack` to the CLI to eliminate the duplicate Stack Launcher banner. Auto-orchestrate behavior on plain `termdeck` (no script) is unchanged.

## [0.5.0] - 2026-04-25

### Changed
- **`termdeck` (no subcommand) auto-orchestrates the stack** when a configured stack is detected (`~/.termdeck/secrets.env` present AND (`mnestra.autoStart: true` OR `rag.enabled: true`)). Otherwise behavior is unchanged from v0.4.6 — Tier-1-only boot. This eliminates the discovery friction of `termdeck stack` for returning users while keeping a friction-free path for first-run testers.
- Added `--no-stack` flag to force a Tier-1-only boot regardless of detection.
- `termdeck stack` retained as an explicit-force alias — v0.4.6 docs and muscle memory keep working.

### Added
- 13 new tests under `tests/cli-stack-detection.test.js` (8 detection cases) and `tests/cli-default-routing.test.js` (5 dispatch cases) — covers the v0.4.5 → v0.5.0 upgrade-path silence guarantee for unconfigured users.
- README + `docs/GETTING-STARTED.md` reflect the new default behavior.

### Notes
- No new fields required in `config.yaml`. Detection reads existing `mnestra.autoStart` / `rag.enabled` keys.
- Users who upgrade from v0.4.5 with no `secrets.env` see no behavioral change.

## [0.4.6] - 2026-04-25

### Added
- **`termdeck stack` subcommand**: Node port of `scripts/start.sh` that ships in the npm tarball. Boots Mnestra (when installed and `mnestra.autoStart: true`), checks Rumen via `DATABASE_URL`, and starts TermDeck — same numbered four-step output as the bash launcher. Cross-platform (no bash dependency). Closes the gap surfaced when a tester `npm install`-ed TermDeck and discovered `scripts/start.sh` wasn't in the published package.
- `--no-mnestra` flag on `termdeck stack` for Tier-1-only stack runs.
- README "Alternative install paths" entry pointing at the new subcommand.

### Notes
- `scripts/start.sh` continues to work for repo clones — unchanged. The new subcommand is a parallel path, not a replacement.

## [0.4.5] - 2026-04-19

### Added
- **Responsive layouts**: media queries for 13" laptops through 27" iMacs, min panel dimensions, toolbar compaction on small screens, resize debounce on fitAll
- **Setup wizard writes config**: user pastes Supabase credentials in the browser wizard, wizard validates + saves secrets.env + config.yaml + runs all 7 migrations automatically
- **Welcome-back flow**: returning users see a brief status toast, not the full wizard
- **Orchestrator layout v2**: 2x2 workers TOP + full-width orchestrator BOTTOM (equal thirds). JS-driven column count handles 1-5 panels.
- **Bulletproof start.sh**: numbered step-by-step output, smart Mnestra handling (kill/restart if 0 memories), first-run config creation
- **Rumen re-kickstart**: 166 insights generated with hybrid embeddings, 44 PVB-specific (up from 13). Cross-project pattern discovery confirmed working.

### Fixed
- **CI lint failures**: docs-lint job now handles clean checkouts without synced docs-site content
- **Flashback resurrection**: root cause found — `queryDirect` sent `recency_weight`/`decay_days` to an 8-argument SQL function. Removed the extra args. Flashback now fires end-to-end (verified: error → analyzer → Mnestra query → toast). Silent for 15 sprints due to this bug.
- **Data quality cleanup**: 145 sub-0.10 confidence noise insights deleted, all "chopin-nashville" project tags fixed to real config.yaml names (termdeck, scheduling, claimguard)

### Added
- **SkillForge foundation** (Tier 5): `termdeck forge` CLI command reads memory count from Mnestra and projects Opus cost. 4-phase prompt template (quality audit → pattern extraction → skill generation → self-critique). Skill installer writes .md files to `~/.claude/skills/`. Actual Opus call stubbed for v0.5.
- **Orchestrator layout fix**: explicit 4-row grid (fixes `grid-row: 1/-1` failure with auto rows)
- **Setup wizard**: click "config" to see 4-tier status (green/amber/gray), next-step commands, and a re-check button. Auto-opens on first run.
- **GET /api/setup**: tier detection endpoint returning config state and firstRun flag
- **First-run detection**: CLI prints setup hint when no config.yaml exists
- **Orchestrator layout**: new "orch" preset — 1 large panel (60% left) + stacked workers (40% right). Keyboard shortcut: Cmd/Ctrl+Shift+7
- **Session ID in panel headers**: first 8 chars visible at a glance for orchestration workflows
- **Mnestra auto-start**: `mnestra.autoStart: true` in config.yaml makes start.sh boot Mnestra automatically. Detects already-running instances.
- **Protocol-aware WebSocket**: auto-detects `ws://` vs `wss://` based on page protocol (enables HTTPS deployment)
- **TranscriptWriter pool TTL retry**: 30s cooldown matching getRumenPool pattern (was permanent failure)
- **RAG circuit breaker half-open**: retries after 5 minutes instead of staying permanently open

### Changed
- `actions/checkout` v4→v6 in CI workflow
- `uuid` 9→13 (named export `{ v4 }` verified)
- Contradictions #6 marked resolved (engram/ directory no longer exists)
- RAG telemetry push disabled by default (Mnestra + Rumen handle memory independently; engram_* tables were never created)

## [0.3.8] - 2026-04-17

### Added
- **Flashback modal**: clicking a Flashback toast now opens a proper centered modal with content, project tag, similarity score, and feedback buttons (previously opened buried drawer tab)
- **Project name resolution**: sessions and insights use config.yaml project names instead of directory path segments (fixes "chopin-nashville" appearing everywhere)
- **Insight confidence filter**: `GET /api/rumen/insights` accepts `?minConfidence=0.15` (default) to filter low-quality noise
- **Insight quality guide**: `docs/INSIGHT-QUALITY.md` with confidence score interpretation and tuning recommendations
- **`scripts/bump-version.sh`**: one-command version bump across package.json + all active docs

### Fixed
- **CLI banner right border**: dynamic version string now centers correctly in the ASCII box
- **Error detection false positive**: clean PTY exits (code 0) no longer persist "Error detected in output" status. Claude Code sessions use stricter line-start error matching to avoid flagging grep results and tool output
- **CLI bind guardrail bypass**: guard now enforced in the CLI entrypoint, not just the direct server path
- **Health badge false-green**: shows all checks when DATABASE_URL is configured, regardless of pass/fail

## [0.3.6] - 2026-04-16

### Added
- **`0.0.0.0` bind guardrail**: server refuses to start on a non-loopback host unless `auth.token` (or `TERMDECK_AUTH_TOKEN`) is configured; exits with `[security]` lines pointing to both configuration paths
- **Flashback end-to-end test** (`tests/flashback-e2e.test.js`): exercises `input → PTY error → analyzer → rag_events → mnestra-bridge` against a live server, observes via `rag_events` (write-once, race-free) rather than transient `meta.status`, skips gracefully when server or Mnestra are unavailable
- **Failure-injection test suite** (`tests/failure-injection.test.js`): 5 scenarios — Mnestra unreachable, component-failure isolation, PTY crash recovery, rapid create/destroy leak check, `/api/health` latency budget
- **`scripts/verify-release.sh`**: seven pre-publish checks (version alignment, working tree clean, `node -c` parse, `lint-docs.sh`, test suite, bin shebang, `files[]` coverage) with aggregated exit status
- `RELEASE_CHECKLIST.md`: rewritten as a TermDeck-only playbook that delegates mechanical checks to `verify-release.sh`

### Fixed
- `DEPLOYMENT.md` Binding section: previous wording was directionally correct but under-specified; now documents the exact `[security]` exit behavior, both config paths, and the three allowed loopback hosts (`127.0.0.1`, `localhost`, `::1`)

## [0.3.4] - 2026-04-16

### Added
- **Two-row toolbar layout**: replaces the horizontal-scroll toolbar; every button visible without scrolling
- **Optional token auth** (`packages/server/src/auth.js`): `Authorization: Bearer`, `?token=` query, and `termdeck_token=` cookie. Token source is `config.auth.token` OR `TERMDECK_AUTH_TOKEN`; unset = zero behavior change for local users
- **`docs/SECURITY.md`**: threat model, default posture, auth mechanics, secrets handling, transcript data hygiene
- **`docs/DEPLOYMENT.md`**: pre-exposure checklist, nginx/caddy reverse-proxy configs with 3600s WebSocket timeouts, systemd unit skeleton

### Changed
- **Status and Config buttons wired**: previously stubs; now open the status drawer and config viewer
- Removed the dead RAG indicator from the toolbar (never surfaced real state)
- `start.sh`: Node 18+ check, transcript-migration check, MCP hint, `--port` flag, stack summary on boot

## [0.3.3] - 2026-04-16

### Added
- **Contract tests** for `/api/health`, `/api/rumen/*`, and the transcript API (recent/search/replay shapes)

### Fixed
- Preflight Mnestra probe hits `/healthz` (not `/health`); resolves false-red health badge when Mnestra ≥0.2.0 is running correctly
- Toolbar overflow: tighter spacing, `overflow-x: auto`, `flex-shrink` on right section (stopgap before the 0.3.4 two-row redesign)
- `getRumenPool`: 30-second TTL retry after a transient failure, replacing the permanent per-process failure flag

### Changed
- `docs/GETTING-STARTED.md`: split `npx` vs. `clone` install paths

## [0.3.2] - 2026-04-16

### Fixed
- **RAG outbox data-loss bug**: rows were deleted before Supabase ack; writes now flushed durably
- **Transcript API contract**: request/response shape aligned with client expectations
- **Health badge**: tier-aware rendering; no longer reports false-green when Mnestra is offline
- Preflight Mnestra check: hits `/healthz` (not `/health`) and parses `store.rows`

### Changed
- Removed personal names from docs; bumped internal version refs to 0.3.2

## [0.3.1] - 2026-04-16

### Added
- **Preflight health check**: 6 parallel checks at startup (Node, ports, config, Mnestra, Rumen, Supabase)
- **Session transcripts**: schema + writer module; transcripts wired into server and surfaced via recovery UI
- **Health badge** in top bar with tier-aware display
- GETTING-STARTED.md: tier-aware guide (Tiers 1-4) with CLAUDE.md specifics and stop markers
- Tester brief: DM template + 5-step checklist for pre-launch feedback

### Fixed
- Transcript buffer cap
- RAG telemetry re-enabled with per-table 404 circuit breaker
- CI lint: bare catch blocks

## [0.3.0] - 2026-04-15

### Added
- **Rumen insights API**: async learning layer integration (Extract → Relate → Synthesize → Surface)
- **Vector embeddings** for hybrid recall across memory store
- **Client split**: `public/index.html` split into `index.html` + `style.css` + `app.js`
- **Launch collateral**: Show HN, Twitter, LinkedIn, dev.to drafts
- **Hero GIF** re-shot post-Rumen integration
- Supabase Edge Function + trigger script for Rumen
- RUMEN-UNBLOCK runbook
- `init-rumen` forwards `OPENAI_API_KEY`; migration self-heals

### Changed
- Flashback decoupled from `rag.enabled` — works standalone against Mnestra
- README rewritten with Flashback-first pitch and three-tier stack diagram

## [0.2.5] - 2026-04-15

### Added
- External audit remediation: test suite, PATCH field whitelist, hardened port regex

## [0.2.4] - 2026-04-14

### Added
- Hero GIF wired into README
- Flashback demo GIF and post-rename stills
- Tier 2 verification log

### Changed
- Sprint 3 launch materials + T1 docs sweep

## [0.2.3] - 2026-04-14

### Fixed
- Broken `termdeck init --mnestra` dispatch in 0.2.2

### Changed
- `init-engram.js` → `init-mnestra.js`
- `setup/engram-migrations/` → `setup/mnestra-migrations/`
- Stripped lingering Ingram refs (mid-session pivot residue)

## [0.2.2] - 2026-04-14

### Changed
- **Rename: Engram → Mnestra** (Ingram rejected via Ingram Industries sponsor conflict)
- Flashback toast header renders "Mnestra — possible match"
- Docs, setup wizard default package name updated
- `engram-bridge/` → `mnestra-bridge/`

### Added
- 4+1 orchestration blog post + X thread documenting the meta-moment where Flashback surfaced its own rename research mid-crisis

## [0.2.1] - 2026-04-14

### Added
- Help button links to live docs at `termdeck-docs.vercel.app`
- docs-site Vercel deploy config

## [0.2.0] - 2026-04-14

### Added
- **npm publishing**: flatten to root `@jhizzard/termdeck` with bundled server+client+config
- **Onboarding tour**: 13-step spotlight walkthrough, first-run auto-trigger, keyboard nav
- **Add-project modal** for adding projects from the UI
- **Engram bridge** (direct/webhook/mcp) — memory MCP integration (later renamed Mnestra in 0.2.2)
- **Proactive error events** surfaced as Flashback toasts
- **Session logger** and `POST /api/sessions/:id/input` for agent replies
- **INSTALL guide** and ship checklist
- Panel info tabs, switcher overlay, reply form, first-run empty state
- Alt-key handling via `e.code`, persistent launch buttons, panel indices
- dotenv secrets refactor; `@homebridge/node-pty` prebuilds
- Codespaces devcontainer; port 3000 silent auto-forward
- AI query scoped to project with `all:` prefix for cross-project search

### Changed
- Hoist runtime deps; mark CLI workspace pkg private
- Shell spawn fix (respect `SHELL` env var)
- zsh is the default shell
- Banner padding; session-logs fix

### Fixed
- Duplicate WebSocket on session create (claim `state.sessions` slot at entry)
- Disabled racy `status_broadcast` + 3s-poller auto-`createTerminalPanel` paths
- Respect `rag.enabled=false` — stop 404 push spam
- Codespaces: detect `CODESPACES` env var, skip auto-open

## [0.1.1] - 2026-04-11

### Added
- Hero screenshot and 30-second demo GIF in README
- `CONTRIBUTORS.md` documenting Joshua Izzard as sole author
- 4 reference docs in `docs/`: Rumen architecture plan, Rumen deploy checklist, Podium-derived lessons, cross-project analysis methodology
- Promotion drafts for v0.1 launch (Show HN, tweet thread, dev.to article, Reddit posts)

### Changed
- All 12 silent `catch {}` blocks replaced with `[tag]`-prefixed `console.error` calls. The 2 intentional require() fallbacks at module load are preserved.
- Established `[tag]` logging convention: `[pty]`, `[ws]`, `[db]`, `[rag]`, `[config]`, `[cli]`, `[client]`
- Tight loops (fitAll, 3-second session poll) use rate-limited one-shot warnings to avoid console spam

## [0.1.0] - 2026-03-19

### Added
- **Core PTY + WebSocket loop**: real PTY terminals via node-pty, one WebSocket per session, xterm.js client
- **6 grid layouts** (1x1, 2x1, 2x2, 3x2, 2x4, 4x2) plus focus and half modes
- **Output analyzer**: detects Claude Code, Gemini CLI, Python servers, plain shells; extracts status, ports, request counts, last commands
- **8 curated terminal themes**: Tokyo Night, Rosé Pine Dawn, Catppuccin Mocha, GitHub Light, Dracula, Solarized Dark, Nord, Gruvbox Dark
- **SQLite persistence** for sessions, commands, and RAG events
- **Project-aware launcher**: `~/.termdeck/config.yaml` defines projects with auto-cd and default themes
- **macOS .app launcher** (no terminal needed to start)
- **Windows installer** with Start Menu and Desktop shortcuts
- **WebSocket auto-reconnect** with exponential backoff
- **Status broadcast** every 2 seconds for live metadata updates
- **Keyboard shortcuts**: Ctrl+Shift+N for prompt bar, Ctrl+Shift+1-6 for layouts, Ctrl+Shift+]/[ to cycle terminals, Escape to exit focus

[Unreleased]: https://github.com/jhizzard/termdeck/compare/v0.7.2...HEAD
[0.7.2]: https://github.com/jhizzard/termdeck/compare/v0.7.1...v0.7.2
[0.7.1]: https://github.com/jhizzard/termdeck/compare/v0.7.0...v0.7.1
[0.7.0]: https://github.com/jhizzard/termdeck/compare/v0.6.9...v0.7.0
[0.4.5]: https://github.com/jhizzard/termdeck/compare/v0.3.4...v0.4.5
[0.3.4]: https://github.com/jhizzard/termdeck/compare/v0.3.3...v0.3.4
[0.3.3]: https://github.com/jhizzard/termdeck/compare/v0.3.2...v0.3.3
[0.3.2]: https://github.com/jhizzard/termdeck/compare/v0.3.1...v0.3.2
[0.3.1]: https://github.com/jhizzard/termdeck/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/jhizzard/termdeck/compare/v0.2.5...v0.3.0
[0.2.5]: https://github.com/jhizzard/termdeck/compare/v0.2.4...v0.2.5
[0.2.4]: https://github.com/jhizzard/termdeck/compare/v0.2.3...v0.2.4
[0.2.3]: https://github.com/jhizzard/termdeck/compare/v0.2.2...v0.2.3
[0.2.2]: https://github.com/jhizzard/termdeck/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/jhizzard/termdeck/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/jhizzard/termdeck/compare/v0.1.1...v0.2.0
[0.1.1]: https://github.com/jhizzard/termdeck/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/jhizzard/termdeck/releases/tag/v0.1.0
