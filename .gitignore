node_modules/
*.db
*.db-journal
*.db-wal
.env
secrets.env
dist/
.DS_Store
*.gif
*.mp4
!assets/demo.gif
!docs/screenshots/*.gif
!docs/screenshots/*.mp4

# Supabase local state (project-ref, pooler-url, versions — from `supabase link`)
supabase/.temp/

# Claude Code workspace state (hooks, plugin settings, skill cache)
.claude/

# Sprint worktrees (Sprint 47+ pattern — per-lane git worktree add paths)
.worktrees/

# Playwright MCP cache (per-session page snapshots)
.playwright-mcp/
