# Proof reports

Generated output of `scripts/proof/cold-vs-warm.js` — one `<runId>.md` (human)
and `<runId>.json` (machine) per run. All run outputs are gitignored except the
committed `SAMPLE-plumbing-demo.*`, which is kept as documentation of the report
shape (it is a **stub** run — a plumbing demo, not evidence; see the banner in
the file).

ORCH regenerates the real, attributed record at sprint close-out with a live
recall + a real answerer — see `../README.md` § "The real proof".
